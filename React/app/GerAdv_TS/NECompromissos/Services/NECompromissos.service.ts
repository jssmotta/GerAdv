// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
"use client";
import { CRUD_CONSTANTS } from "@/app/tools/crud";
import {
  NECompromissosApi,
  NECompromissosApiError,
} from "../Apis/ApiNECompromissos";
import { FilterNECompromissos } from "../Filters/NECompromissos";
import { INECompromissos } from "../Interfaces/interface.NECompromissos";
import { NECompromissosEmpty } from "../../Models/NECompromissos";
import { ICommandSpeakerRequest } from "@/app/models/ICommandSpeakerRequest";
import { AxiosResponse } from "axios";

export class NECompromissosValidator {
  static validateNECompromissos(necompromissos: INECompromissos): {
    isValid: boolean;
    errors: string[];
  } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes

    return {
      isValid: errors.length === 0,
      errors,
    };
  }
}

export interface INECompromissosService {
  fetchNECompromissosById: (id: number) => Promise<INECompromissos>;
  saveNECompromissos: (
    necompromissos: INECompromissos,
  ) => Promise<INECompromissos>;

  getAll: (filtro?: FilterNECompromissos) => Promise<INECompromissos[]>;
  deleteNECompromissos: (id: number) => Promise<void>;
  validateNECompromissos: (necompromissos: INECompromissos) => {
    isValid: boolean;
    errors: string[];
  };
  filterVoice: (
    filtro?: FilterNECompromissos,
    voiceCommand?: ICommandSpeakerRequest,
  ) => Promise<AxiosResponse>;
}

export class NECompromissosService implements INECompromissosService {
  constructor(private api: NECompromissosApi) {}

  async fetchNECompromissosById(id: number): Promise<INECompromissos> {
    if (id <= 0) {
      throw new NECompromissosApiError("ID inválido", 400, "INVALID_ID");
    }

    try {
      const response = await this.api.getById(id);
      return response.data;
    } catch (error) {
      if (error instanceof NECompromissosApiError) {
        throw error;
      }
      throw new NECompromissosApiError(
        "Erro ao buscar necompromissos",
        500,
        "FETCH_ERROR",
        error,
      );
    }
  }

  async saveNECompromissos(
    necompromissos: INECompromissos,
  ): Promise<INECompromissos> {
    const validation = this.validateNECompromissos(necompromissos);
    if (!validation.isValid) {
      throw new NECompromissosApiError(
        `Dados inválidos: ${validation.errors.join(", ")}`,
        400,
        "VALIDATION_ERROR",
      );
    }

    try {
      const response = await this.api.addAndUpdate(necompromissos);
      return response.data;
    } catch (error) {
      if (error instanceof NECompromissosApiError) {
        throw error;
      }
      throw new NECompromissosApiError(
        "Erro ao salvar necompromissos",
        500,
        "SAVE_ERROR",
        error,
      );
    }
  }

  async getAll(
    filtro?: FilterNECompromissos,
    onOnlineData?: (data: INECompromissos[]) => void,
  ): Promise<INECompromissos[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(
        0,
        (filtro ?? {}) as any,
      );
      // Normaliza offline
      const offlineRaw = preloadResponse?.data;
      const offlineData = Array.isArray(offlineRaw)
        ? offlineRaw
        : (offlineRaw?.data ?? offlineRaw?.items ?? offlineRaw?.rows ?? []);

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api
          .filter(0, (filtro ?? {}) as any)
          .then((response) => {
            const raw = response?.data;
            const onlineData = Array.isArray(raw)
              ? raw
              : (raw?.data ?? raw?.items ?? raw?.rows ?? []);
            if (onlineData) {
              onOnlineData(onlineData);
            }
          })
          .catch((error) => {
            if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
              console.log("Error fetching online NECompromissos");
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(
            0,
            (filtro ?? {}) as any,
          );
          const raw = onlineResponse?.data;
          const onlineData = Array.isArray(raw)
            ? raw
            : (raw?.data ?? raw?.items ?? raw?.rows ?? []);
          return onlineData || offlineData;
        } catch (error) {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error fetching online NECompromissos");
          return offlineData;
        }
      }
    } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
        console.log("Error fetching all NECompromissos:");
      return [];
    }
  }

  async deleteNECompromissos(id: number): Promise<void> {
    if (id <= 0) {
      throw new NECompromissosApiError(
        "ID inválido para exclusão",
        400,
        "INVALID_ID",
      );
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof NECompromissosApiError) {
        throw error;
      }
      throw new NECompromissosApiError(
        "Erro ao excluir necompromissos",
        500,
        "DELETE_ERROR",
        error,
      );
    }
  }

  validateNECompromissos(necompromissos: INECompromissos): {
    isValid: boolean;
    errors: string[];
  } {
    return NECompromissosValidator.validateNECompromissos(necompromissos);
  }

  async filterVoice(
    filtro?: FilterNECompromissos,
    voiceCommand?: ICommandSpeakerRequest,
  ): Promise<AxiosResponse> {
    try {
      const response = await this.api.filterVoice(filtro, voiceCommand);
      return response;
    } catch (error) {
      if (error instanceof NECompromissosApiError) {
        throw error;
      }
      throw new NECompromissosApiError(
        "Erro ao processar filtro de voz",
        500,
        "VOICE_FILTER_ERROR",
        error,
      );
    }
  }
}
