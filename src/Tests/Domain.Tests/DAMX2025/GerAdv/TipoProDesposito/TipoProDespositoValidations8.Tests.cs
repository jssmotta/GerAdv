// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoProDespositoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFTipoProDespositoFactory> _mockTipoProDespositoFactory;
    private readonly Mock<ITipoProDespositoReader> _mockReader;
    private readonly TipoProDespositoValidation _validation;
    private readonly Mock<ITipoProDespositoService> _mockTipoProDespositoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoProDespositoService _service;
    private readonly string _validUri = "test-uri";
    public TipoProDespositoValidationTests()
    {
        _mockTipoProDespositoFactory = new Mock<IFTipoProDespositoFactory>();
        _mockReader = new Mock<ITipoProDespositoReader>();
        _validation = new TipoProDespositoValidation();
        _mockTipoProDespositoService = new Mock<ITipoProDespositoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoProDespositoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoProDespositoValidation>();
    }

    [Fact]
    public void TipoProDespositoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoProDespositoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoProDespositoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoProDespositoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.TipoProDesposito CreateValidTipoProDesposito()
    {
        return new Models.TipoProDesposito
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoProDespositoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoProDesposito>(), It.IsAny<string>())).ReturnsAsync(new List<TipoProDespositoResponseAll>());
    // Setup other mocks but don't override the TipoProDespositos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoProDespositoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoProDesposito>(), It.IsAny<string>())).ReturnsAsync(new List<TipoProDespositoResponseAll>());
    // Setup other mocks but don't override the TipoProDespositos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipoprodesposito = new Models.TipoProDesposito
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipoprodesposito, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoprodesposito = CreateValidTipoProDesposito();
        tipoprodesposito.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoprodesposito, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithNullNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoprodesposito = CreateValidTipoProDesposito();
        tipoprodesposito.Nome = null;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoprodesposito, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidDataNome_ShouldReturnTrue()
    {
        // Arrange
        var tipoprodesposito = CreateValidTipoProDesposito();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipoprodesposito, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespaceNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoprodesposito = CreateValidTipoProDesposito();
        tipoprodesposito.Nome = "   ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoprodesposito, _mockTipoProDespositoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}