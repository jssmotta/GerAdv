// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OponentesService, OponentesValidator } from '@/app/GerAdv_TS/Oponentes/Services/Oponentes.service';
import { OponentesApi, OponentesApiError } from '@/app/GerAdv_TS/Oponentes/Apis/ApiOponentes';
import { IOponentes } from '@/app/GerAdv_TS/Oponentes/Interfaces/interface.Oponentes';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OponentesApi;

const sample: IOponentes = { id: 1, nome: 'Teste' };

describe('OponentesService', () => {
  let service: OponentesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OponentesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OponentesValidator, 'validateOponentes').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOponentesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOponentesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOponentesById lança OponentesApiError para id inválido', async () => {
    await expect(service.fetchOponentesById(0)).rejects.toBeInstanceOf(OponentesApiError);
  });

  it('saveOponentes chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOponentes(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOponentes lança OponentesApiError quando validação falha', async () => {
    (OponentesValidator.validateOponentes as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOponentes(sample)).rejects.toBeInstanceOf(OponentesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOponentes chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOponentes(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOponentes lança OponentesApiError para id inválido', async () => {
    await expect(service.deleteOponentes(0)).rejects.toBeInstanceOf(OponentesApiError);
  });

  it('validateOponentes delega para OponentesValidator', () => {
    (OponentesValidator.validateOponentes as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOponentes(sample);
    expect(r.isValid).toBe(true);
  });
});
