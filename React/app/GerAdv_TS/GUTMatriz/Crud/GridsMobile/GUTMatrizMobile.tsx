// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// GridsMobileHook.tsx.txt
'use client';
import React, { useMemo } from 'react';
import { Grid, GridColumn } from '@progress/kendo-react-grid';
import { IGUTMatriz } from '../../Interfaces/interface.GUTMatriz';
import { CRUD_CONSTANTS, formatCurrency, formatDateBrowser } from '@/app/tools/crud';
import '@/app/styles/MobileCrudGrid.css';
import { useGUTMatrizGrid } from '../../Hooks/hookGUTMatrizGrid';
import { useRouter } from 'next/navigation';
import { SvgIcon } from '@progress/kendo-react-common';
import { pencilIcon, searchIcon } from '@progress/kendo-svg-icons';
interface GUTMatrizGridProps {
  data: IGUTMatriz[];
  onRowClick: (gutmatriz: IGUTMatriz) => void;
  onDeleteClick: (e: any) => void;
  setSelectedId: (id: number | null) => void;
}
export const GUTMatrizGridMobileComponent = React.memo(({
  data, 
  onRowClick, 
  onDeleteClick, 
  setSelectedId, 

}: GUTMatrizGridProps) => {

const router = useRouter();
// ===== USO DO HOOK CENTRALIZADO =====
const {
  processedData, 
  sort, 
  handleSortChange, 
  page, 
  handlePageChange, 
  handleFilterChange, 
  handleRowClick: hookHandleRowClick, 
  RowNumberCell, 
} = useGUTMatrizGrid({
data, 
onRowClick, 
onDeleteClick, 
setSelectedId, 
initialTake: 10, 
useCustomSort: false, // Mobile usa useGridSort interno
});

const openSearchCellGUTAtividadesMatriz = (id: number) => {
  router.push(`/pages/gutatividadesmatriz/?gutmatriz=${id}`);
};
const SearchFromCellGUTAtividadesMatriz = (props: any) => {
  return (
  <>
  <td>
    <div onClick={() => openSearchCellGUTAtividadesMatriz(props.dataItem.id)}><span title='Pesquisar relacionados em G U T Atividades Matriz'><SvgIcon icon={searchIcon} /></span></div>
  </td>
</>
);
};
// ===== CONFIGURAÇÃO DE COLUNAS =====
const gridColumns = useMemo(() => [
  <GridColumn format='{0:n0}' field='index' title='#' sortable={false} filterable={false} width='55px' cells={{ data: RowNumberCell }} />,
  <GridColumn
  field='id_edit_GUTAtividadesMatriz'
  filterable={false}
  sortable={false}
  width={'65px'}
  title='G U T Atividades Matriz'
  cells={{ data: SearchFromCellGUTAtividadesMatriz }}
  />, 

  ], [RowNumberCell]);
  // ===== RENDER =====
  return (
  <Grid
  className='grid-mobile-crud grid-mobile-gutmatriz'
  data={processedData.slice(page.skip, page.skip + page.take)}
  skip={page.skip}
  take={page.take}
  total={processedData.length}
  pageable={{
    pageSizes: Array.from(CRUD_CONSTANTS.PAGINATION.PAGE_SIZES), 
    buttonCount: CRUD_CONSTANTS.PAGINATION.BUTTON_COUNT, 
  }}
  onPageChange={handlePageChange}
  sortable={true}
  sort={sort}
  onSortChange={handleSortChange}
  resizable={true}
  reorderable={true}
  filterable={true}
  onFilterChange={handleFilterChange}
  onRowClick={(e) => hookHandleRowClick(e)}
>
{gridColumns}
</Grid>
);
});