// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class GraphWriterTests
{
    private readonly Mock<IFGraphFactory> _mockGraphFactory;
    private readonly Mock<FGraph> _mockFGraph;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly GraphWriter _graphWriter;
    public GraphWriterTests()
    {
        _mockGraphFactory = new Mock<IFGraphFactory>();
        _mockFGraph = new Mock<FGraph>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _graphWriter = new GraphWriter(_mockGraphFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var graph = CreateValidGraphModel();
        graph.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockGraphFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGraph.Object);
        _mockFGraph.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _graphWriter.WriteAsync(graph, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGraph.Object);
        _mockGraphFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockGraphFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFGraph.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var graph = CreateValidGraphModel();
        graph.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockGraphFactory.Setup(x => x.CreateFromIdAsync(graph.Id, _mockConnection.Object)).ReturnsAsync(_mockFGraph.Object);
        _mockFGraph.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _graphWriter.WriteAsync(graph, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGraph.Object);
        _mockGraphFactory.Verify(x => x.CreateFromIdAsync(graph.Id, _mockConnection.Object), Times.Once);
        _mockGraphFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFGraph.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var graph = CreateValidGraphModel();
        var auditorQuem = 99;
        _mockGraphFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGraph.Object);
        _mockFGraph.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _graphWriter.WriteAsync(graph, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGraph.Object);
        _mockFGraph.VerifySet(x => x.FTabela = graph.Tabela, Times.Once);
        _mockFGraph.VerifySet(x => x.FTabelaId = graph.TabelaId, Times.Once);
        _mockFGraph.VerifySet(x => x.FGUID = graph.GUID, Times.Once);
        _mockFGraph.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var graph = CreateValidGraphModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockGraphFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _graphWriter.WriteAsync(graph, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var graph = CreateValidGraphModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockGraphFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGraph.Object);
        _mockFGraph.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _graphWriter.WriteAsync(graph, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var graphResponse = new GraphResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockGraphFactory.Setup(x => x.DeleteAsync(operadorId, graphResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _graphWriter.Delete(graphResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockGraphFactory.Verify(x => x.DeleteAsync(operadorId, graphResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var graphResponse = new GraphResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockGraphFactory.Setup(x => x.DeleteAsync(operadorId, graphResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _graphWriter.Delete(graphResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var graphResponse = new GraphResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockGraphFactory.Setup(x => x.DeleteAsync(operadorId, graphResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _graphWriter.Delete(graphResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new GraphWriter(_mockGraphFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new GraphWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Graph CreateValidGraphModel()
    {
        return new Models.Graph
        {
            Id = 0,
            Tabela = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            TabelaId = 1,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}