// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for NEPalavrasChaves page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock NEPalavrasChavesGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/NEPalavrasChaves/Adapter/NEPalavrasChavesGridAdapter', () => ({
  NEPalavrasChavesGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock NEPalavrasChavesGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/NEPalavrasChaves/Components/NEPalavrasChavesGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='nepalavraschaves-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import NEPalavrasChavesPage from '@/app/pages/nepalavraschaves/page';

describe('NEPalavrasChaves page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and NEPalavrasChavesGridContainer', () => {
    render(<NEPalavrasChavesPage />);

    expect(screen.getByRole('heading', { name: /N E Palavras Chaves/i })).toBeInTheDocument();
    expect(screen.getByTestId('nepalavraschaves-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

