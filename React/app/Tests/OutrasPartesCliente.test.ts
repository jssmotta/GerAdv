// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useOutrasPartesClienteForm, useOutrasPartesClienteList, useValidationsOutrasPartesCliente } from '../GerAdv_TS/OutrasPartesCliente/Hooks/hookOutrasPartesCliente';
import { IOutrasPartesCliente } from '../GerAdv_TS/OutrasPartesCliente/Interfaces/interface.OutrasPartesCliente';
import { IOutrasPartesClienteService } from '../GerAdv_TS/OutrasPartesCliente/Services/OutrasPartesCliente.service';
import { OutrasPartesClienteTestEmpty } from '../GerAdv_TS/Models/OutrasPartesCliente';
import { useOutrasPartesClienteComboBox } from '../GerAdv_TS/OutrasPartesCliente/Hooks/hookOutrasPartesCliente';

// Mock do serviço
const mockOutrasPartesClienteService: jest.Mocked<IOutrasPartesClienteService> = {
  fetchOutrasPartesClienteById: jest.fn(),
  saveOutrasPartesCliente: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteOutrasPartesCliente: jest.fn(),
  validateOutrasPartesCliente: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialOutrasPartesCliente: IOutrasPartesCliente = { ...OutrasPartesClienteTestEmpty() };

describe('useOutrasPartesClienteForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    expect(result.current.data).toEqual(initialOutrasPartesCliente);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Outras Partes Cliente',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Outras Partes Cliente');
  });

   test('deve carregar Outras Partes Cliente por ID', async () => {
    const mockOutrasPartesCliente = { ...initialOutrasPartesCliente, id: 1, nome: 'Outras Partes Cliente Teste' };
    mockOutrasPartesClienteService.fetchOutrasPartesClienteById.mockResolvedValue(mockOutrasPartesCliente);

    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.loadOutrasPartesCliente(1);
    });

    expect(mockOutrasPartesClienteService.fetchOutrasPartesClienteById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockOutrasPartesCliente);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Outras Partes Cliente', async () => {
    const errorMessage = 'Erro ao carregar Outras Partes Cliente';
    mockOutrasPartesClienteService.fetchOutrasPartesClienteById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.loadOutrasPartesCliente(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialOutrasPartesCliente, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialOutrasPartesCliente);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.loadOutrasPartesCliente(0);
    });

    expect(mockOutrasPartesClienteService.fetchOutrasPartesClienteById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialOutrasPartesCliente);
  });
});

describe('useOutrasPartesClienteList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteList(mockOutrasPartesClienteService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialOutrasPartesCliente, id: 1, nome: 'Outras Partes Cliente 1' },
      { ...initialOutrasPartesCliente, id: 2, nome: 'Outras Partes Cliente 2' }
    ];
    mockOutrasPartesClienteService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOutrasPartesClienteList(mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockOutrasPartesClienteService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockOutrasPartesClienteService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOutrasPartesClienteList(mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialOutrasPartesCliente, id: 1, nome: 'Outras Partes Cliente Filtrado' }];
    const filtro = { nome: 'Outras Partes Cliente' };
    mockOutrasPartesClienteService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOutrasPartesClienteList(mockOutrasPartesClienteService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockOutrasPartesClienteService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsOutrasPartesCliente', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsOutrasPartesCliente());

    const validData = { ...initialOutrasPartesCliente, nome: 'Outras Partes Cliente Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsOutrasPartesCliente());

    const invalidData = { ...initialOutrasPartesCliente, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsOutrasPartesCliente());

    const invalidData = { 
      ...initialOutrasPartesCliente, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsOutrasPartesCliente());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialOutrasPartesCliente, id: 1, nome: 'Outras Partes Cliente Teste' }];
    mockOutrasPartesClienteService.getAll.mockResolvedValue(mockData);
    mockOutrasPartesClienteService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useOutrasPartesClienteList(mockOutrasPartesClienteService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsOutrasPartesCliente()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Outras Partes Cliente Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Outras Partes Cliente Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteForm(initialOutrasPartesCliente, mockOutrasPartesClienteService)
    );

    const mockEvent = {
      target: {
        name: 'terceirizado',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.terceirizado).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Outras Partes Cliente 1' },
      { id: 2, nome: 'Outras Partes Cliente 2' }
    ];
    mockOutrasPartesClienteService.getList.mockResolvedValue(mockOptions as IOutrasPartesCliente[]);


    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Outras Partes Cliente 1' },
        { id: 2, nome: 'Outras Partes Cliente 2' }
      ]);
    });


    expect(mockOutrasPartesClienteService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Outras Partes Cliente ABC' },
      { id: 2, nome: 'Outras Partes Cliente XYZ' }
    ];
    mockOutrasPartesClienteService.getList.mockResolvedValue(mockOptions as IOutrasPartesCliente[]);   


 const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Outras Partes Cliente ABC' },
        { id: 2, nome: 'Outras Partes Cliente XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Outras Partes Cliente ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Outras Partes Cliente ABC' },
      { id: 2, nome: 'Outras Partes Cliente XYZ' }
    ];
    mockOutrasPartesClienteService.getList.mockResolvedValue(mockOptions as IOutrasPartesCliente[]);
  


    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Outras Partes Cliente ABC' },
        { id: 2, nome: 'Outras Partes Cliente XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Outras Partes Cliente ABC' },
          {id: 2, nome: 'Outras Partes Cliente XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );

    const newValue = { id: 1, nome: 'Outras Partes Cliente Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Outras Partes Cliente Inicial' };
    
    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useOutrasPartesClienteComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Outras Partes Cliente Inicial' };
    
    const { result } = renderHook(() => 
      useOutrasPartesClienteComboBox(mockOutrasPartesClienteService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






