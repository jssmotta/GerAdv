// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

//CrudGridHook.tsx.txt
"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { AppGridToolbar } from "@/app/components/Cruds/GridToolbar";
import { useIsMobile } from "@/app/context/MobileContext";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { TipoCompromissoEmpty } from "../../../Models/TipoCompromisso";
import { useWindow } from "@/app/hooks/useWindows";
import { useRouter } from "next/navigation";
import { ITipoCompromisso } from "../../Interfaces/interface.TipoCompromisso";
import { TipoCompromissoService } from "../../Services/TipoCompromisso.service";
import { TipoCompromissoApi } from "../../Apis/TipoCompromissoApi";
import { TipoCompromissoGridMobileComponent } from "../GridsMobile/TipoCompromissoMobile";
import { TipoCompromissoGridDesktopComponent } from "../GridsDesktop/TipoCompromissoDesktop";
import { getParamFromUrl } from "@/app/tools/helpers";
import { FilterTipoCompromisso } from "../../Filters/TipoCompromisso";
import { ConfirmationModal } from "@/app/components/Cruds/ConfirmationModal";
import TipoCompromissoWindow from "./TipoCompromissoWindow";
import ErrorMessage from "@/app/components/Cruds/ErrorMessage";
import { useTipoCompromissoList } from "../../Hooks/hookTipoCompromisso";
import { LoadingSpinner } from "@/app/components/Cruds/LoadingSpinner";
import {
  NotifySystemActions,
  subscribeToNotifications,
} from "@/app/tools/NotifySystem";
import { useTipoCompromissoFilter } from "../../Hooks/hookTipoCompromissoFilter";
import GenericFilterDialog from "@/app/components/Cruds/GenericFilterDialog";
import { ICommandSpeakerRequest } from "@/app/models/ICommandSpeakerRequest";

interface TipoCompromissoGridProps {
  selectItem?: (item: ITipoCompromisso) => void;
}
const TipoCompromissoGrid: React.FC<TipoCompromissoGridProps> = ({
  selectItem,
}) => {
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dimensions = useWindow();

  const [showInc, setShowInc] = useState(false);
  const [selectedTipoCompromisso, setSelectedTipoCompromisso] =
    useState<ITipoCompromisso>(TipoCompromissoEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<
    FilterTipoCompromisso | undefined | null
  >(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [tipocompromissoData, setTipoCompromissoData] = useState<
    ITipoCompromisso[]
  >([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const tipocompromissoService = useMemo(() => {
    return new TipoCompromissoService(
      new TipoCompromissoApi(
        systemContext?.Uri ?? "",
        systemContext?.Token ?? "",
      ),
    );
  }, [systemContext?.Uri, systemContext?.Token]);

  const {
    data: tipocompromisso,
    fetchData,
    refreshData,
  } = useTipoCompromissoList(tipocompromissoService, currFilter);

  const handleFetchWithFilter = useCallback(
    async (filtro?: FilterTipoCompromisso | undefined | null) => {
      setCurrFilter(filtro);

      if (filtro && Object.keys(filtro).length > 0) {
        sessionStorage.setItem(
          btoa("TipoCompromissoFilter"),
          btoa(JSON.stringify(filtro)),
        );
      }

      setLoading(true);
      setError(null);

      try {
        const offlineData = await tipocompromissoService.getAll(
          filtro ?? ({} as FilterTipoCompromisso),
          (onlineData) => {
            setTipoCompromissoData(onlineData);
            setLoading(false);
            setError(null);
          },
        );

        if (offlineData && offlineData.length > 0) {
          setTipoCompromissoData(offlineData);
          setLoading(false);
        } else {
          setTipoCompromissoData([]);
        }
      } catch (err: any) {
        setError(err?.message || "Erro ao carregar dados de Tipo Compromisso");
        setLoading(false);
        setTipoCompromissoData([]);
      }
    },
    [],
  );

  const loadFilter = useCallback(() => {
    if (isInitialized) return;

    const filtro = {} as FilterTipoCompromisso;

    const filtroMem = sessionStorage.getItem(btoa("TipoCompromissoFilter"))
      ? JSON.parse(
          atob(sessionStorage.getItem(btoa("TipoCompromissoFilter")) ?? "") ||
            "{}",
        )
      : ({} as FilterTipoCompromisso);
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);

  const handleRowClick = (tipocompromisso: ITipoCompromisso) => {
    setSelectedTipoCompromisso(tipocompromisso);
    setShowInc(true);
  };

  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);

  const handleAdd = () => {
    setSelectedTipoCompromisso(TipoCompromissoEmpty());
    setShowInc(true);
  };

  const handleClose = () => {
    setShowInc(false);
  };

  const handleSuccess = () => {
    setShowInc(false);

    refreshData();
  };

  const handleError = () => {
    setShowInc(false);
  };

  const onDeleteClick = (e: any) => {
    const tipocompromisso = e.dataItem;
    setDeleteId(tipocompromisso.id);
    setIsModalOpen(true);
  };

  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await tipocompromissoService.deleteTipoCompromisso(deleteId);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao excluir");
        setErrorMessage(
          "Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.",
        );
      } finally {
        setDeleteId(null);
        setIsModalOpen(false);
      }
    }
  };

  const cancelDelete = () => {
    setDeleteId(null);
    setIsModalOpen(false);
  };

  const displayError = error || errorMessage;

  useEffect(() => {
    const unsubscribe = subscribeToNotifications("*", (entity) => {
      if (entity.action == NotifySystemActions.ERROR) {
        return;
      }
      handleFetchWithFilter(currFilter);
    });

    return () => {
      unsubscribe();
    };
  }, [currFilter]);

  const {
    showSearch,
    windowFilter,
    setWindowFilter,
    handleSearch,
    handleCloseSearch,
    handleConfirmSearch,
    renderInputFilters,
    clearFilter,
    hasActiveFilter,
  } = useTipoCompromissoFilter({ handleFetchWithFilter });

  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);

  const handleVoiceFilter = useCallback(
    async (voiceCommand: ICommandSpeakerRequest) => {
      try {
        setLoading(true);
        setError(null);

        const response = await tipocompromissoService.filterVoice(
          currFilter || ({} as FilterTipoCompromisso),
          voiceCommand,
        );

        if (response.data) {
          let processedFilter: FilterTipoCompromisso;
          let filteredData: ITipoCompromisso[];

          if (response.data.items && Array.isArray(response.data.items)) {
            filteredData = response.data.items;
            processedFilter =
              response.data.filter || (response.data as FilterTipoCompromisso);
          } else if (Array.isArray(response.data)) {
            filteredData = response.data;
            processedFilter = currFilter || ({} as FilterTipoCompromisso);
          } else {
            processedFilter = response.data as FilterTipoCompromisso;
            filteredData = tipocompromissoData;
          }

          setTipoCompromissoData(filteredData);
          setCurrFilter(processedFilter);
          setWindowFilter(processedFilter);

          if (processedFilter && Object.keys(processedFilter).length > 0) {
            sessionStorage.setItem(
              btoa("TipoCompromissoFilter"),
              btoa(JSON.stringify(processedFilter)),
            );
          }

          await handleFetchWithFilter(processedFilter);
        }
      } catch (error) {
        console.error("Erro no filtro por comando de voz:", error);
        setError("Erro ao processar comando de voz. Tente novamente.");
      } finally {
        setLoading(false);
      }
    },
    [currFilter, tipocompromissoData, setWindowFilter, handleFetchWithFilter],
  );

  return (
    <>
      {loading && <LoadingSpinner />}

      {isMobile ? (
        <TipoCompromissoGridMobileComponent
          data={tipocompromissoData}
          onRowClick={handleRowClick}
          onDeleteClick={onDeleteClick}
          setSelectedId={setSelectedId}
          selectItem={selectItem}
          toolbar={
            <AppGridToolbar
              onAdd={handleAdd}
              onSearch={handleSearch}
              onClearFilter={hasActiveFilter ? clearFilter : undefined}
              hasActiveFilter={hasActiveFilter}
              onVoiceFilter={handleVoiceFilter}
            />
          }
        />
      ) : (
        <TipoCompromissoGridDesktopComponent
          data={tipocompromissoData}
          onRowClick={handleRowClick}
          onDeleteClick={onDeleteClick}
          setSelectedId={setSelectedId}
          selectItem={selectItem}
          toolbar={
            <AppGridToolbar
              onAdd={handleAdd}
              onSearch={handleSearch}
              onClearFilter={hasActiveFilter ? clearFilter : undefined}
              hasActiveFilter={hasActiveFilter}
              onVoiceFilter={handleVoiceFilter}
            />
          }
        />
      )}

      <TipoCompromissoWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedTipoCompromisso={selectedTipoCompromisso}
      />

      <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
      />

      <ErrorMessage mensagem={displayError} setErrorMessage={setErrorMessage} />

      <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title="Filtrar Tipo Compromisso"
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText="Buscar"
        cancelText="Cancelar"
      />
    </>
  );
};

export default TipoCompromissoGrid;
