// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AgendaWindowId from '@/app/GerAdv_TS/Agenda/Crud/Grids/AgendaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AgendaWindowId from '@/app/GerAdv_TS/Agenda/Crud/Grids/AgendaWindowId';
import { AgendaEmpty } from '@/app/GerAdv_TS/Models/Agenda';

describe('AgendaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AgendaWindowId).toBeDefined();
    expect(typeof AgendaWindowId === 'function' || typeof AgendaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AgendaService class so constructor usage doesn't throw and we can control fetchAgendaById
const mockFetchAgendaById = jest.fn();
jest.mock('@/app/GerAdv_TS/Agenda/Services/Agenda.service', () => ({
  AgendaService: jest.fn().mockImplementation(() => ({
    fetchAgendaById: (...args: any[]) => mockFetchAgendaById(...args),
  })),
}));

// Mock the child AgendaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Agenda/Crud/Grids/AgendaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='agenda-window' {...p} />,
}));

describe('AgendaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AgendaWindowId).toBeDefined();
    expect(typeof AgendaWindowId === 'function' || typeof AgendaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AgendaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AgendaWindow with AgendaEmpty when id is 0', async () => {
    render(
      <AgendaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AgendaWindow should be rendered with selectedAgenda equal to AgendaEmpty()
    const node = await screen.findByTestId('agenda-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedAgenda"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedagenda') || node.getAttribute('selectedAgenda')).not.toBeNull();
  });

  it('fetches agenda by id and passes it to AgendaWindow when id > 0', async () => {
    const fakeAgenda = { id: 123, nomecliente: 'Test' };
    mockFetchAgendaById.mockResolvedValue(fakeAgenda);

    render(
      <AgendaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('agenda-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAgendaById).toHaveBeenCalledWith(123));
  });
});
