// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProcessOutputEngineWindowId from '@/app/GerAdv_TS/ProcessOutputEngine/Crud/Grids/ProcessOutputEngineWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProcessOutputEngineWindowId from '@/app/GerAdv_TS/ProcessOutputEngine/Crud/Grids/ProcessOutputEngineWindowId';
import { ProcessOutputEngineEmpty } from '@/app/GerAdv_TS/Models/ProcessOutputEngine';

describe('ProcessOutputEngineWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProcessOutputEngineWindowId).toBeDefined();
    expect(typeof ProcessOutputEngineWindowId === 'function' || typeof ProcessOutputEngineWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProcessOutputEngineService class so constructor usage doesn't throw and we can control fetchProcessOutputEngineById
const mockFetchProcessOutputEngineById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProcessOutputEngine/Services/ProcessOutputEngine.service', () => ({
  ProcessOutputEngineService: jest.fn().mockImplementation(() => ({
    fetchProcessOutputEngineById: (...args: any[]) => mockFetchProcessOutputEngineById(...args),
  })),
}));

// Mock the child ProcessOutputEngineWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProcessOutputEngine/Crud/Grids/ProcessOutputEngineWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='processoutputengine-window' {...p} />,
}));

describe('ProcessOutputEngineWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProcessOutputEngineWindowId).toBeDefined();
    expect(typeof ProcessOutputEngineWindowId === 'function' || typeof ProcessOutputEngineWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProcessOutputEngineWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProcessOutputEngineWindow with ProcessOutputEngineEmpty when id is 0', async () => {
    render(
      <ProcessOutputEngineWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProcessOutputEngineWindow should be rendered with selectedProcessOutputEngine equal to ProcessOutputEngineEmpty()
    const node = await screen.findByTestId('processoutputengine-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProcessOutputEngine"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprocessoutputengine') || node.getAttribute('selectedProcessOutputEngine')).not.toBeNull();
  });

  it('fetches processoutputengine by id and passes it to ProcessOutputEngineWindow when id > 0', async () => {
    const fakeProcessOutputEngine = { id: 123, nomecliente: 'Test' };
    mockFetchProcessOutputEngineById.mockResolvedValue(fakeProcessOutputEngine);

    render(
      <ProcessOutputEngineWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('processoutputengine-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProcessOutputEngineById).toHaveBeenCalledWith(123));
  });
});
