// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using MenphisSI.GerAdv.Models.Response.All;
using MenphisSI.GerAdv.Readers;
using System.Data;

namespace MenphisSI.GerAdv.Tests.Readers;
/// <summary>
/// Comprehensive unit tests for AgendaReader class
/// Tests all public methods including async operations, data reading, and data transformation
/// </summary>
public class AgendaReaderTests : IDisposable
{
    private readonly Mock<IFAgendaFactory> _mockAgendaFactory;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IDataRecord> _mockDataRecord;
    private readonly AgendaReader _agendaReader;
    public AgendaReaderTests()
    {
        _mockAgendaFactory = new Mock<IFAgendaFactory>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockDataRecord = new Mock<IDataRecord>();
        _agendaReader = new AgendaReader(_mockAgendaFactory.Object);
    }

#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldInitializeReader()
    {
        // Act & Assert
        _agendaReader.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new AgendaReader(null !));
    }

#endregion
#region ListarN Tests
    [Fact]
    public async Task ListarN_WithValidParameters_ShouldCallDevourerSqlData()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "ageCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "agedata";
        // Act & Assert
        // Since this calls static methods and external dependencies,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task ListarN_WithEmptyParameters_ShouldThrowException()
    {
        // Arrange
        var max = 0;
        var uri = string.Empty; // Empty URI should cause an exception
        var cWhere = string.Empty;
        var parameters = new List<SqlParameter>();
        var order = string.Empty;
        // Act & Assert
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.ListarN(max, uri, cWhere, parameters, order));
    }

#endregion
#region Listar Tests
    [Fact]
    public async Task Listar_WithValidParameters_ShouldCallListarTabela()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "ageCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // Since this calls external dependencies and database connections,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

    [Fact]
    public async Task Listar_WithCancellationToken_ShouldRespectCancellation()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri";
        var cWhere = "carCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = new CancellationToken(true); // Already cancelled
        // Act & Assert
        // Even with cancellation, this should throw an exception due to invalid URI
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Read Tests with ID and Connection
    [Fact]
    public async Task Read_WithValidId_ShouldReturnAgendaResponse()
    {
        // Arrange
        var id = 123;
        var expectedAgenda = new FAgenda
        {
            ID = id,
            FHrFinal = "27/05/2022"
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedAgenda);
        // Act
        var result = await _agendaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public async Task Read_WithEmptyId_ShouldReturnNull()
    {
        // Arrange
        var id = 0; // Empty ID
        var emptyFAgenda = new FAgenda
        {
            ID = 0
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFAgenda);
        // Act
        var result = await _agendaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public async Task Read_WithNegativeId_ShouldReturnNull()
    {
        // Arrange
        var id = -1;
        var emptyFAgenda = new FAgenda
        {
            ID = 0
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFAgenda);
        // Act
        var result = await _agendaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region ReadM Tests
    [Fact]
    public async Task ReadM_WithValidId_ShouldReturnAgendaModel()
    {
        // Arrange
        var id = 123;
        var expectedFAgenda = new FAgenda
        {
            ID = id,
            FHrFinal = "27/05/2022"
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFAgenda);
        // Act
        var result = await _agendaReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public async Task ReadM_WithEmptyId_ShouldReturnModelWithDefaults()
    {
        // Arrange
        var id = 0;
        var emptyFAgenda = new FAgenda
        {
            ID = 0,
            FHrFinal = null
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFAgenda);
        // Act
        var result = await _agendaReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(0);
        result?.HrFinal.Should().Be(string.Empty);
    }

#endregion
#region Read with FAgenda and Connection Tests
    [Fact]
    public void Read_WithValidFAgendaAndConnection_ShouldReturnAgendaResponse()
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        // Act
        var result = _agendaReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void Read_WithNullFAgendaAndConnection_ShouldReturnNull()
    {
        // Arrange
        FAgenda? dbRec = null;
        // Act
        var result = _agendaReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with Where and Parameters Tests
    [Fact]
    public void Read_WithWhereAndParameters_ShouldReturnAgendaResponse()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedFAgenda = new FAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        _mockAgendaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(expectedFAgenda);
        // Act
        var result = _agendaReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void Read_WithWhereAndParameters_WhenRecordNotFound_ShouldReturnNull()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 999)
        };
        var emptyFAgenda = new FAgenda
        {
            ID = 0
        };
        _mockAgendaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(emptyFAgenda);
        // Act
        var result = _agendaReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with FAgenda Tests
    [Fact]
    public void Read_WithValidFAgenda_ShouldReturnAgendaResponse()
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void Read_WithNullFAgenda_ShouldReturnNull()
    {
        // Arrange
        FAgenda? dbRec = null;
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithFAgendaNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = 123,
            FHrFinal = null
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be(string.Empty);
    }

#endregion
#region Read with DBAgenda Tests
    [Fact]
    public void Read_WithValidSGDBAgenda_ShouldReturnAgendaResponse()
    {
        // Arrange
        var dbRec = new DBAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void Read_WithNullSGDBAgenda_ShouldReturnNull()
    {
        // Arrange
        DBAgenda? dbRec = null;
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithSGDBAgendaNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBAgenda
        {
            ID = 123,
            FHrFinal = null // This should result in empty string in response
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with FAgenda and IDataRecord Tests
    [Fact]
    public void ReadAll_WithValidFAgendaAndDataRecord_ShouldReturnAgendaResponseAll()
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void ReadAll_WithNullFAgendaAndDataRecord_ShouldReturnNull()
    {
        // Arrange
        FAgenda? dbRec = null;
        // Act
        var result = _agendaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithFAgendaNullNomeAndDataRecord_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = 123,
            FHrFinal = null
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with DBAgenda and DataRow Tests
    [Fact]
    public void ReadAll_WithValidSGDBAgendaAndDataRow_ShouldReturnAgendaResponseAll()
    {
        // Arrange
        var dbRec = new DBAgenda
        {
            ID = 123,
            FHrFinal = "27/05/2022"
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be("27/05/2022");
    }

    [Fact]
    public void ReadAll_WithNullSGDBAgendaAndDataRow_ShouldReturnNull()
    {
        // Arrange
        DBAgenda? dbRec = null;
        // Act
        var result = _agendaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithSGDBAgendaNullNomeAndDataRow_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBAgenda
        {
            ID = 123,
            FHrFinal = null // This should result in empty string in response
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.HrFinal.Should().Be(string.Empty);
    }

#endregion
#region Edge Cases and Error Handling Tests
    [Fact]
    public async Task Read_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _agendaReader.Read(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task ReadM_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _agendaReader.ReadM(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public void Read_WithWhereParameters_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedException = new InvalidOperationException("Factory error");
        _mockAgendaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Throws(expectedException);
        // Act & Assert
        var exception = Assert.Throws<InvalidOperationException>(() => _agendaReader.Read(where, parameters, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Data Consistency Tests
    [Theory]
    [InlineData(1, "27/05/2022")]
    [InlineData(999, "NOME2")]
    [InlineData(0, "")]
    public void Read_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = id,
            FHrFinal = nome
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        if (id == 0)
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
        }
        else
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
            result?.HrFinal.Should().Be(nome);
        }
    }

    [Theory]
    [InlineData(1, "27/05/2022")]
    [InlineData(999, "NOME2")]
    [InlineData(0, "")]
    public void ReadAll_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FAgenda
        {
            ID = id,
            FHrFinal = nome
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.HrFinal.Should().Be(nome);
    }

#endregion
#region Async Behavior Tests
    [Fact]
    public async Task AsyncMethods_ShouldCompleteWithoutBlocking()
    {
        // Arrange
        var id = 123;
        var expectedFAgenda = new FAgenda
        {
            ID = id,
            FHrFinal = "27/05/2022"
        };
        _mockAgendaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFAgenda);
        // Act & Assert
        // Test only the methods that don't depend on external URI connections
        var readTask = _agendaReader.Read(id, _mockConnection.Object);
        var readMTask = _agendaReader.ReadM(id, _mockConnection.Object);
        await Task.WhenAll(readTask, readMTask);
        // If we reach here, async methods completed without deadlock
        Assert.True(true);
    }

    [Fact]
    public async Task ListarN_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task Listar_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _agendaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Parameter Validation Tests
    [Fact]
    public async Task Read_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        // This would typically throw an exception or handle gracefully
        // depending on the implementation
        try
        {
            await _agendaReader.Read(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public async Task ReadM_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        try
        {
            await _agendaReader.ReadM(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public void Read_WithNullParametersList_ShouldHandleGracefully()
    {
        // Arrange
        var where = "ageCodigo = @id";
        // Act & Assert
        try
        {
            _agendaReader.Read(where, null !, _mockConnection.Object);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

#endregion
#region Integration-like Tests
    [Fact]
    public void Read_FAgenda_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 456;
        var testNome = "MA";
        var dbRec = new FAgenda
        {
            ID = testId,
            FHrFinal = testNome
        };
        // Act
        var result = _agendaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<AgendaResponse>();
        result!.Id.Should().Be(testId);
        result.HrFinal.Should().Be(testNome);
    }

    [Fact]
    public void ReadAll_FAgenda_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 789;
        var testNome = "27/05/2022";
        var dbRec = new FAgenda
        {
            ID = testId,
            FHrFinal = testNome
        };
        // Act
        var result = _agendaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<AgendaResponseAll>();
        result!.Id.Should().Be(testId);
        result.HrFinal.Should().Be(testNome);
    }

#endregion
#region Dispose
    public virtual void Dispose()
    {
        // Clean up any resources if needed
        GC.SuppressFinalize(this);
    }
#endregion
}