// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:LivroCaixa
// Fonte:ValidationsGeneratorTests
namespace MenphisSI.GerAdv.Tests;
public class LivroCaixaValidationTests : IDisposable
{
    private readonly LivroCaixaValidation _validation;
    private readonly Mock<ILivroCaixaService> _mockLivroCaixaService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly string _validUri = "test-uri";
    public LivroCaixaValidationTests()
    {
        _validation = new LivroCaixaValidation();
        _mockLivroCaixaService = new Mock<ILivroCaixaService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void LivroCaixaValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ILivroCaixaValidation>();
    }

    [Fact]
    public void LivroCaixaValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(LivroCaixaValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void LivroCaixaValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(LivroCaixaValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static GerAdv.Models.LivroCaixa CreateValidLivroCaixa()
    {
        return new GerAdv.Models.LivroCaixa
        {
            Id = 1,
            Previsto = false,
            IDDes = 0,
            Pessoal = 0,
            Ajuste = true,
            IDHon = 0,
            IDHonParc = 0,
            IDHonSuc = false,
            Data = "24/04/1975",
            Processo = 0,
            Valor = 1m,
            Tipo = true,
            Historico = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Grupo = 0
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockLivroCaixaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterLivroCaixa>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the LivroCaixas service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockLivroCaixaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterLivroCaixa>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the LivroCaixas service mock
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null !, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region Data Validation Tests
    [Theory]
    [InlineData("01/01/1899")]
    [InlineData("31/12/1899")]
    public async Task ValidateReg_WithDataBeforeMinDate_ShouldThrowSGValidationException(string invalidDate)
    {
        // Arrange
        var livrocaixa = CreateValidLivroCaixa();
        livrocaixa.Data = invalidDate;
        SetupValidMocks();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(livrocaixa, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("01/01/1900.");
    }

    [Fact]
    public async Task ValidateReg_WithValidData_ShouldPass()
    {
        // Arrange
        var livrocaixa = CreateValidLivroCaixa();
        livrocaixa.Data = "01/01/1990";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(livrocaixa, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNull_Data_ShouldPass()
    {
        // Arrange
        var livrocaixa = CreateValidLivroCaixa();
        livrocaixa.Data = null !;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(livrocaixa, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithInvalidDateDataFormat_ShouldPass()
    {
        // Arrange
        var livrocaixa = CreateValidLivroCaixa();
        livrocaixa.Data = "invalid-date";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(livrocaixa, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue(); // Invalid format is ignored, not validated
    }

    [Fact]
    public async Task ValidateReg_WithEmptyData_ShouldPass()
    {
        // Arrange
        var livrocaixa = CreateValidLivroCaixa();
        livrocaixa.Data = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(livrocaixa, _mockLivroCaixaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    public virtual void Dispose()
    {
        GC.SuppressFinalize(this);
    }
}