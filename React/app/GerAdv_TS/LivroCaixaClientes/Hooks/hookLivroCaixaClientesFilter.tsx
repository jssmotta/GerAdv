// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterLivroCaixaClientes } from '@/app/GerAdv_TS/LivroCaixaClientes/Filters/LivroCaixaClientes';
import LivroCaixaComboBox from '@/app/GerAdv_TS/LivroCaixa/ComboBox/LivroCaixa';
import ClientesComboBox from '@/app/GerAdv_TS/Clientes/ComboBox/Clientes';
interface UseLivroCaixaClientesFilterProps {
  handleFetchWithFilter: (filtro?: FilterLivroCaixaClientes | undefined | null) => Promise<void>;
}
interface UseLivroCaixaClientesFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterLivroCaixaClientes;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterLivroCaixaClientes>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterLivroCaixaClientes) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterLivroCaixaClientes>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useLivroCaixaClientesFilter = ({ handleFetchWithFilter }: UseLivroCaixaClientesFilterProps): UseLivroCaixaClientesFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterLivroCaixaClientes>({} as FilterLivroCaixaClientes);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterLivroCaixaClientes;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterLivroCaixaClientes) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('LivroCaixaClientesFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterLivroCaixaClientes>) => (
  <>
  <LivroCaixaComboBox
  name='livrocaixa'
  dataForm={null}
  value={handlers.windowFilter?.livrocaixa}
  setValue={(e:any) => handlers.handleComboChange(e, 'livrocaixa')}
  className='inputSearch inputSearchComboboxTab'
  label='Livro Caixa'
  />
  <ClientesComboBox
  name='cliente'
  dataForm={null}
  value={handlers.windowFilter?.cliente}
  setValue={(e:any) => handlers.handleComboChange(e, 'cliente')}
  className='inputSearch inputSearchComboboxTab'
  label='Clientes'
  />
  <InputComboFilterYesNo
  type='text'
  id='lancado'
  name='lancado'
  value={handlers.windowFilter?.lancado ?? -2147483648}
  onChange={handlers.handleInputChange}
  label='Lancado'
  className='inputSearch inputSearchCheckbox'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};