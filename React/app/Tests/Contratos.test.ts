// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useContratosForm, useContratosList, useValidationsContratos } from '../GerAdv_TS/Contratos/Hooks/hookContratos';
import { IContratos } from '../GerAdv_TS/Contratos/Interfaces/interface.Contratos';
import { IContratosService } from '../GerAdv_TS/Contratos/Services/Contratos.service';
import { ContratosTestEmpty } from '../GerAdv_TS/Models/Contratos';


// Mock do serviço
const mockContratosService: jest.Mocked<IContratosService> = {
  fetchContratosById: jest.fn(),
  saveContratos: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteContratos: jest.fn(),
  validateContratos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialContratos: IContratos = { ...ContratosTestEmpty() };

describe('useContratosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    expect(result.current.data).toEqual(initialContratos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    const mockEvent = {
      target: {
        name: 'guid',
        value: 'Novo Contratos',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.guid).toBe('Novo Contratos');
  });

   test('deve carregar Contratos por ID', async () => {
    const mockContratos = { ...initialContratos, id: 1, guid: 'Contratos Teste' };
    mockContratosService.fetchContratosById.mockResolvedValue(mockContratos);

    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    await act(async () => {
      await result.current.loadContratos(1);
    });

    expect(mockContratosService.fetchContratosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockContratos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Contratos', async () => {
    const errorMessage = 'Erro ao carregar Contratos';
    mockContratosService.fetchContratosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    await act(async () => {
      await result.current.loadContratos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialContratos, guid: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialContratos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    await act(async () => {
      await result.current.loadContratos(0);
    });

    expect(mockContratosService.fetchContratosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialContratos);
  });
});

describe('useContratosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useContratosList(mockContratosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialContratos, id: 1, guid: 'Contratos 1' },
      { ...initialContratos, id: 2, guid: 'Contratos 2' }
    ];
    mockContratosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useContratosList(mockContratosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockContratosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockContratosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useContratosList(mockContratosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialContratos, id: 1, guid: 'Contratos Filtrado' }];
    const filtro = { guid: 'Contratos' };
    mockContratosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useContratosList(mockContratosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockContratosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsContratos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsContratos());

    const validData = { ...initialContratos, guid: 'Contratos Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar guid vazio', () => {
    const { result } = renderHook(() => useValidationsContratos());

    const invalidData = { ...initialContratos, guid: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ficar vazio.');
  });

  
  test('deve invalidar guid muito longo', () => {
    const { result } = renderHook(() => useValidationsContratos());

    const invalidData = { 
      ...initialContratos, 
      guid: 'a'.repeat(100+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ter mais de 100 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsContratos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialContratos, id: 1, guid: 'Contratos Teste' }];
    mockContratosService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useContratosList(mockContratosService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsContratos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useContratosForm(initialContratos, mockContratosService)
    );

    const mockEvent = {
      target: {
        name: 'ocultarrelatorio',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.ocultarrelatorio).toBe(true);
  });


