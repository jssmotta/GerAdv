// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProValoresWindowId from '@/app/GerAdv_TS/ProValores/Crud/Grids/ProValoresWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProValoresWindowId from '@/app/GerAdv_TS/ProValores/Crud/Grids/ProValoresWindowId';
import { ProValoresEmpty } from '@/app/GerAdv_TS/Models/ProValores';

describe('ProValoresWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProValoresWindowId).toBeDefined();
    expect(typeof ProValoresWindowId === 'function' || typeof ProValoresWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProValoresService class so constructor usage doesn't throw and we can control fetchProValoresById
const mockFetchProValoresById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProValores/Services/ProValores.service', () => ({
  ProValoresService: jest.fn().mockImplementation(() => ({
    fetchProValoresById: (...args: any[]) => mockFetchProValoresById(...args),
  })),
}));

// Mock the child ProValoresWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProValores/Crud/Grids/ProValoresWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='provalores-window' {...p} />,
}));

describe('ProValoresWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProValoresWindowId).toBeDefined();
    expect(typeof ProValoresWindowId === 'function' || typeof ProValoresWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProValoresWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProValoresWindow with ProValoresEmpty when id is 0', async () => {
    render(
      <ProValoresWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProValoresWindow should be rendered with selectedProValores equal to ProValoresEmpty()
    const node = await screen.findByTestId('provalores-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProValores"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprovalores') || node.getAttribute('selectedProValores')).not.toBeNull();
  });

  it('fetches provalores by id and passes it to ProValoresWindow when id > 0', async () => {
    const fakeProValores = { id: 123, nomecliente: 'Test' };
    mockFetchProValoresById.mockResolvedValue(fakeProValores);

    render(
      <ProValoresWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('provalores-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProValoresById).toHaveBeenCalledWith(123));
  });
});
