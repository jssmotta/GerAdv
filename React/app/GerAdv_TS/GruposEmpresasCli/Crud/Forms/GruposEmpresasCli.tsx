// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tracking: Forms.tsx.txt
'use client';
import { IGruposEmpresasCli } from '@/app/GerAdv_TS/GruposEmpresasCli/Interfaces/interface.GruposEmpresasCli';
import { useRouter } from 'next/navigation';
import React, { useEffect, useState, useRef } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { getParamFromUrl } from '@/app/tools/helpers';
import '@/app/styles/CrudFormsBase.css';
import '@/app/styles/CrudFormsMobile.css';
import '@/app/styles/CrudForms5.css'; // [ INDEX_SIZE ]
import ButtonSalvarCrud from '@/app/components/Cruds/ButtonSalvarCrud';
import { useIsMobile } from '@/app/context/MobileContext';
import DeleteButton from '@/app/components/Cruds/DeleteButton';
import { GruposEmpresasCliApi } from '../../Apis/ApiGruposEmpresasCli';
import { useValidationsGruposEmpresasCli } from '../../Hooks/hookGruposEmpresasCli';
import GruposEmpresasComboBox from '@/app/GerAdv_TS/GruposEmpresas/ComboBox/GruposEmpresas';
import ClientesComboBox from '@/app/GerAdv_TS/Clientes/ComboBox/Clientes';
import { GruposEmpresasApi } from '@/app/GerAdv_TS/GruposEmpresas/Apis/ApiGruposEmpresas';
import { ClientesApi } from '@/app/GerAdv_TS/Clientes/Apis/ApiClientes';
import InputName from '@/app/components/Inputs/InputName';
import InputCheckbox from '@/app/components/Inputs/InputCheckbox';
interface GruposEmpresasCliFormProps {
  gruposempresascliData: IGruposEmpresasCli;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const GruposEmpresasCliForm: React.FC<GruposEmpresasCliFormProps> = ({
  gruposempresascliData, 
  onChange, 
  onSubmit, 
  onClose, 
  onError, 
  onReload, 
  onSuccess, 
}) => {
const router = useRouter();
const { systemContext } = useSystemContext();
const isMobile = useIsMobile();
const dadoApi = new GruposEmpresasCliApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [isSubmitting, setIsSubmitting] = useState(false);
const initialized = useRef(false);
const validationForm = useValidationsGruposEmpresasCli();
const [nomeGruposEmpresas, setNomeGruposEmpresas] = useState('');
const gruposempresasApi = new GruposEmpresasApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [nomeClientes, setNomeClientes] = useState('');
const clientesApi = new ClientesApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');

if (getParamFromUrl('gruposempresas') > 0) {
  if (gruposempresascliData.id === 0 && gruposempresascliData.grupo == 0) {
    gruposempresasApi
    .getById(getParamFromUrl('gruposempresas'))
    .then((response) => {
      setNomeGruposEmpresas(response.data.descricao);
    })
    .catch((error) => {
      console.log('Error unexpected');
    });

    gruposempresascliData.grupo = getParamFromUrl('gruposempresas');
  }
}

if (getParamFromUrl('clientes') > 0) {
  if (gruposempresascliData.id === 0 && gruposempresascliData.cliente == 0) {
    clientesApi
    .getById(getParamFromUrl('clientes'))
    .then((response) => {
      setNomeClientes(response.data.nome);
    })
    .catch((error) => {
      console.log('Error unexpected');
    });

    gruposempresascliData.cliente = getParamFromUrl('clientes');
  }
}
const addValorGrupo = (e: any) => {
  if (e?.id>0)
    onChange({ target: { name: 'grupo', value: e.id } });
  };
  const addValorCliente = (e: any) => {
    if (e?.id>0)
      onChange({ target: { name: 'cliente', value: e.id } });
    };
    const onConfirm = (e: React.FormEvent) => {
      e.preventDefault();
      if (e.stopPropagation) e.stopPropagation();

        if (!isSubmitting) {
          setIsSubmitting(true);

          try {
            onSubmit(e);
          } catch (error) {
          console.log('Erro ao submeter formulário de GruposEmpresasCli:');
          setIsSubmitting(false);
          if (onError) onError();
          }
        }
      };
      const handleCancel = () => {
        if (onReload) {
          onReload(); // Recarrega os dados originais
        } else {
        onClose(); // Comportamento padrão se não há callback de recarga
      }
    };

    const handleDirectSave = () => {
      if (!isSubmitting) {
        setIsSubmitting(true);

        try {
          const syntheticEvent = {
            preventDefault: () => { }, 
            target: document.getElementById(`GruposEmpresasCliForm-${gruposempresascliData.id}`)
          } as unknown as React.FormEvent;

          onSubmit(syntheticEvent);
        } catch (error) {
        console.log('Erro ao salvar GruposEmpresasCli diretamente');
        setIsSubmitting(false);
        if (onError) onError();
        }
      }
    };
    useEffect(() => {
      const el = document.querySelector('.nameFormMobile');
      if (el) {
        el.textContent = gruposempresascliData?.id == 0 ? 'Editar GruposEmpresasCli' : 'Adicionar Grupos Empresas Cli';
      }
    }, [gruposempresascliData.id]);
    return (
    <>
    {!isMobile ? <style jsx global>{`
      @media (max-width: 1366px) {
        html {
          zoom: 0.8 !important;
        }
      }
      `}</style> : null}

      <div className={isMobile ? 'form-container form-container-GruposEmpresasCli' : 'form-container5 form-container-GruposEmpresasCli'}>

        <form className='formInputCadInc' id={`GruposEmpresasCliForm-${gruposempresascliData.id}`} onSubmit={onConfirm}>
          {!isMobile && (
            <ButtonSalvarCrud isMobile={false} validationForm={validationForm} entity='GruposEmpresasCli' data={gruposempresascliData} isSubmitting={isSubmitting} onClose={onClose} formId={`GruposEmpresasCliForm-${gruposempresascliData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
            )}
            <div className='grid-container'>


              <GruposEmpresasComboBox
              name={'grupo'}
              dataForm={gruposempresascliData}
              value={gruposempresascliData.grupo}
              setValue={addValorGrupo}
              label={'Grupos Empresas'}
              />

              <ClientesComboBox
              name={'cliente'}
              dataForm={gruposempresascliData}
              value={gruposempresascliData.cliente}
              setValue={addValorCliente}
              label={'Clientes'}
              />
              <InputCheckbox dataForm={gruposempresascliData} label='Oculto' name='oculto' checked={gruposempresascliData.oculto} onChange={onChange} />
            </div>
          </form>


          {isMobile && (
            <ButtonSalvarCrud isMobile={true} validationForm={validationForm} entity='GruposEmpresasCli' data={gruposempresascliData} isSubmitting={isSubmitting} onClose={onClose} formId={`GruposEmpresasCliForm-${gruposempresascliData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
            )}
            <DeleteButton page={'/pages/gruposempresascli'} id={gruposempresascliData.id} closeModel={onClose} dadoApi={dadoApi} />
          </div>
          <div className='form-spacer'></div>
          </>
        );
      };