// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoStatusBiuWindowId from '@/app/GerAdv_TS/TipoStatusBiu/Crud/Grids/TipoStatusBiuWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoStatusBiuWindowId from '@/app/GerAdv_TS/TipoStatusBiu/Crud/Grids/TipoStatusBiuWindowId';
import { TipoStatusBiuEmpty } from '@/app/GerAdv_TS/Models/TipoStatusBiu';

describe('TipoStatusBiuWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoStatusBiuWindowId).toBeDefined();
    expect(typeof TipoStatusBiuWindowId === 'function' || typeof TipoStatusBiuWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoStatusBiuService class so constructor usage doesn't throw and we can control fetchTipoStatusBiuById
const mockFetchTipoStatusBiuById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoStatusBiu/Services/TipoStatusBiu.service', () => ({
  TipoStatusBiuService: jest.fn().mockImplementation(() => ({
    fetchTipoStatusBiuById: (...args: any[]) => mockFetchTipoStatusBiuById(...args),
  })),
}));

// Mock the child TipoStatusBiuWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoStatusBiu/Crud/Grids/TipoStatusBiuWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipostatusbiu-window' {...p} />,
}));

describe('TipoStatusBiuWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoStatusBiuWindowId).toBeDefined();
    expect(typeof TipoStatusBiuWindowId === 'function' || typeof TipoStatusBiuWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoStatusBiuWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoStatusBiuWindow with TipoStatusBiuEmpty when id is 0', async () => {
    render(
      <TipoStatusBiuWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoStatusBiuWindow should be rendered with selectedTipoStatusBiu equal to TipoStatusBiuEmpty()
    const node = await screen.findByTestId('tipostatusbiu-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoStatusBiu"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipostatusbiu') || node.getAttribute('selectedTipoStatusBiu')).not.toBeNull();
  });

  it('fetches tipostatusbiu by id and passes it to TipoStatusBiuWindow when id > 0', async () => {
    const fakeTipoStatusBiu = { id: 123, nomecliente: 'Test' };
    mockFetchTipoStatusBiuById.mockResolvedValue(fakeTipoStatusBiu);

    render(
      <TipoStatusBiuWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipostatusbiu-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoStatusBiuById).toHaveBeenCalledWith(123));
  });
});
