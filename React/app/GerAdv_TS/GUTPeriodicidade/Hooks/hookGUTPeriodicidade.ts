// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IGUTPeriodicidadeService } from '../Services/GUTPeriodicidade.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IGUTPeriodicidade } from '../Interfaces/interface.GUTPeriodicidade';
import { FilterGUTPeriodicidade } from '../Filters/GUTPeriodicidade';
import { isValidDate } from '@/app/tools/datetime';
import { GUTPeriodicidadeApi } from '../Apis/ApiGUTPeriodicidade';

export const useGUTPeriodicidadeForm = (
  initialGUTPeriodicidade: IGUTPeriodicidade,
  dataService: IGUTPeriodicidadeService
) => {
  const [data, setData] = useState<IGUTPeriodicidade>(initialGUTPeriodicidade);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadGUTPeriodicidade = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialGUTPeriodicidade);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchGUTPeriodicidadeById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar G U T Periodicidade';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar G U T Periodicidade');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialGUTPeriodicidade]);

  const resetForm = useCallback(() => {
    setData(initialGUTPeriodicidade);
    setError(null);
  }, [initialGUTPeriodicidade]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadGUTPeriodicidade,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadGUTPeriodicidade, resetForm]);
  return returnValue;
};


export const useGUTPeriodicidadeNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('GUTPeriodicidade', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
            console.log('Erro no listener de notificações.');
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useGUTPeriodicidadeList = (dataService: IGUTPeriodicidadeService, currentFilter?: FilterGUTPeriodicidade | undefined | null) => {
  const [data, setData] = useState<IGUTPeriodicidade[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterGUTPeriodicidade | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterGUTPeriodicidade | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterGUTPeriodicidade);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar gutperiodicidade';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar gutperiodicidade');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useGUTPeriodicidadeNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsGUTPeriodicidade() {

  async function runValidation(data: IGUTPeriodicidade, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const gutperiodicidadeApi = new GUTPeriodicidadeApi(uri ?? '', token ?? '');

    const result = await gutperiodicidadeApi.validation(data);

    return result;
  }

  function validate(data: IGUTPeriodicidade): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.nome.length <= 0) { 
                                             return { isValid: false, message: 'O campo Nome não pode ficar vazio.' };
                                         } 
if (data.nome.length > 20) { 
                                             return { isValid: false, message: 'O campo Nome não pode ter mais de 20 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}export const useGUTPeriodicidadeComboBox = (
  dataService: IGUTPeriodicidadeService,
  initialValue?: any
) => {
  const [options, setOptions] = useState<any[]>([]);
  const [filteredOptions, setFilteredOptions] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [selectedValue, setSelectedValue] = useState(initialValue);
  const [hasLoaded, setHasLoaded] = useState(false);

  const fetchOptions = useCallback(async () => {
    if (loading) return; // Evita múltiplas requisições simultâneas
    
    setLoading(true);
    try {
      const response = await dataService.getList();
      const mappedOptions = response.map(item => ({
        id: item.id,
        nome: item.nome
      }));
      setOptions(mappedOptions);
      setFilteredOptions(mappedOptions);
      setHasLoaded(true);
    } catch (err) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
          console.log('Erro ao buscar opções do ComboBox');
    } finally {
      setLoading(false);
    }
  }, [dataService, loading]);

  const handleFilter = useCallback((filterText: string) => {
    if (!filterText) {
      setFilteredOptions(options);
      return;
    }
    
    const filter = filterText.toLowerCase();
    const filtered = options.filter(option =>
      option.nome.toLowerCase().includes(filter)
    );
    setFilteredOptions(filtered);
  }, [options]);

  const handleValueChange = useCallback((newValue: any) => {
    setSelectedValue(newValue);
  }, []);
  
  useEffect(() => {
    if (!hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  const refreshCallback = useCallback(() => {
    if (hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  useGUTPeriodicidadeNotifications(
    refreshCallback, // onUpdate
    refreshCallback, // onDelete
    refreshCallback  // onAdd
  );

  return {
    options: filteredOptions,
    loading,
    selectedValue,
    handleFilter,
    handleValueChange,
    refreshOptions: fetchOptions,
    clearValue: () => setSelectedValue(null)
  };
};