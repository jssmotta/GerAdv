// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProResumosForm, useProResumosList, useValidationsProResumos } from '../GerAdv_TS/ProResumos/Hooks/hookProResumos';
import { IProResumos } from '../GerAdv_TS/ProResumos/Interfaces/interface.ProResumos';
import { IProResumosService } from '../GerAdv_TS/ProResumos/Services/ProResumos.service';
import { ProResumosTestEmpty } from '../GerAdv_TS/Models/ProResumos';


// Mock do serviço
const mockProResumosService: jest.Mocked<IProResumosService> = {
  fetchProResumosById: jest.fn(),
  saveProResumos: jest.fn(),
  
  getAll: jest.fn(),
  deleteProResumos: jest.fn(),
  validateProResumos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProResumos: IProResumos = { ...ProResumosTestEmpty() };

describe('useProResumosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    expect(result.current.data).toEqual(initialProResumos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    const mockEvent = {
      target: {
        name: 'resumo',
        value: 'Novo Pro Resumos',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.resumo).toBe('Novo Pro Resumos');
  });

   test('deve carregar Pro Resumos por ID', async () => {
    const mockProResumos = { ...initialProResumos, id: 1, resumo: 'Pro Resumos Teste' };
    mockProResumosService.fetchProResumosById.mockResolvedValue(mockProResumos);

    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    await act(async () => {
      await result.current.loadProResumos(1);
    });

    expect(mockProResumosService.fetchProResumosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProResumos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Pro Resumos', async () => {
    const errorMessage = 'Erro ao carregar Pro Resumos';
    mockProResumosService.fetchProResumosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    await act(async () => {
      await result.current.loadProResumos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProResumos, resumo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProResumos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProResumosForm(initialProResumos, mockProResumosService)
    );

    await act(async () => {
      await result.current.loadProResumos(0);
    });

    expect(mockProResumosService.fetchProResumosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProResumos);
  });
});

describe('useProResumosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProResumosList(mockProResumosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProResumos, id: 1, resumo: 'Pro Resumos 1' },
      { ...initialProResumos, id: 2, resumo: 'Pro Resumos 2' }
    ];
    mockProResumosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProResumosList(mockProResumosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProResumosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProResumosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProResumosList(mockProResumosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProResumos, id: 1, resumo: 'Pro Resumos Filtrado' }];
    const filtro = { resumo: 'Pro Resumos' };
    mockProResumosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProResumosList(mockProResumosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProResumosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProResumos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProResumos());

    const validData = { ...initialProResumos, resumo: 'Pro Resumos Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProResumos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProResumos, id: 1, resumo: 'Pro Resumos Teste' }];
    mockProResumosService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProResumosList(mockProResumosService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsProResumos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});