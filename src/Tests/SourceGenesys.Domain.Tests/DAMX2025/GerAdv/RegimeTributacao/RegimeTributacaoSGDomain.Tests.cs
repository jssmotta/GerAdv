// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBRegimeTributacao - Nome da classe (ex: DBClientes)
/// RegimeTributacao - Nome da tabela (ex: Clientes)
/// rdt - Prefixo dos campos (ex: cli)
/// rdtCodigo - Nome do campo código (ex: cliCodigo)
/// rdtNome - Nome do campo nome (ex: cliNome)
/// DBRegimeTributacao.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBRegimeTributacaoTests : IDisposable
{
    private readonly DBRegimeTributacao _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBRegimeTributacaoTests()
    {
        _instance = new DBRegimeTributacao();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("rdtCodigo", typeof(int));
        dt.Columns.Add("rdtQuemCad", typeof(int));
        dt.Columns.Add("rdtDtCad", typeof(DateTime));
        dt.Columns.Add("rdtQuemAtu", typeof(int));
        dt.Columns.Add("rdtDtAtu", typeof(DateTime));
        dt.Columns.Add("rdtVisto", typeof(bool));
        dt.Columns.Add("rdtNome", typeof(string));
        dt.Columns.Add("rdtGuid", typeof(string));
        return dt;
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["rdtCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBRegimeTributacao(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("RegimeTributacao", DBRegimeTributacao.PTabelaNome);
        Assert.Equal(" RegimeTributacao.* ", DBRegimeTributacao.CamposSqlX);
        Assert.Equal("rdtCodigo", DBRegimeTributacao.CampoCodigo);
        Assert.Equal("rdtNome", DBRegimeTributacao.CampoNome);
        Assert.Equal("rdt", DBRegimeTributacao.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBRegimeTributacao();
        Assert.Equal(0, instance.ID);
        Assert.Equal("RegimeTributacao", instance.ITableName());
        Assert.Equal("rdt", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBRegimeTributacao((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBRegimeTributacao((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICrud_Implementation_ShouldWork()
    {
        ICrud cadastro = (ICrud)_instance;
        Assert.Equal("RegimeTributacao", cadastro.ITableName());
        Assert.Equal("rdtCodigo", cadastro.IFieldId());
        Assert.Equal("rdtNome", cadastro.IFieldNameDescription());
        Assert.Equal("rdt", cadastro.IPrefix());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBRegimeTributacao.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBRegimeTributacao.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBRegimeTributacao.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBRegimeTributacao();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBRegimeTributacao();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 50 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 50);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Guid_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FGuid = input;
        Assert.Equal(expected, _instance.FGuid);
    }

    [Fact]
    public void Guid_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 100 + 10);
        _instance.FGuid = longString;
        Assert.True(_instance.FGuid.Length <= 100);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}