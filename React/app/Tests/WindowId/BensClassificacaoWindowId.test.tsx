// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import BensClassificacaoWindowId from '@/app/GerAdv_TS/BensClassificacao/Crud/Grids/BensClassificacaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import BensClassificacaoWindowId from '@/app/GerAdv_TS/BensClassificacao/Crud/Grids/BensClassificacaoWindowId';
import { BensClassificacaoEmpty } from '@/app/GerAdv_TS/Models/BensClassificacao';

describe('BensClassificacaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(BensClassificacaoWindowId).toBeDefined();
    expect(typeof BensClassificacaoWindowId === 'function' || typeof BensClassificacaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the BensClassificacaoService class so constructor usage doesn't throw and we can control fetchBensClassificacaoById
const mockFetchBensClassificacaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/BensClassificacao/Services/BensClassificacao.service', () => ({
  BensClassificacaoService: jest.fn().mockImplementation(() => ({
    fetchBensClassificacaoById: (...args: any[]) => mockFetchBensClassificacaoById(...args),
  })),
}));

// Mock the child BensClassificacaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/BensClassificacao/Crud/Grids/BensClassificacaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='bensclassificacao-window' {...p} />,
}));

describe('BensClassificacaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(BensClassificacaoWindowId).toBeDefined();
    expect(typeof BensClassificacaoWindowId === 'function' || typeof BensClassificacaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <BensClassificacaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders BensClassificacaoWindow with BensClassificacaoEmpty when id is 0', async () => {
    render(
      <BensClassificacaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // BensClassificacaoWindow should be rendered with selectedBensClassificacao equal to BensClassificacaoEmpty()
    const node = await screen.findByTestId('bensclassificacao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedBensClassificacao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedbensclassificacao') || node.getAttribute('selectedBensClassificacao')).not.toBeNull();
  });

  it('fetches bensclassificacao by id and passes it to BensClassificacaoWindow when id > 0', async () => {
    const fakeBensClassificacao = { id: 123, nomecliente: 'Test' };
    mockFetchBensClassificacaoById.mockResolvedValue(fakeBensClassificacao);

    render(
      <BensClassificacaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('bensclassificacao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchBensClassificacaoById).toHaveBeenCalledWith(123));
  });
});
