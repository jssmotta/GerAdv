// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class FornecedoresValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFFornecedoresFactory> _mockFornecedoresFactory;
    private readonly Mock<IFornecedoresReader> _mockReader;
    private readonly FornecedoresValidation _validation;
    private readonly Mock<IFornecedoresService> _mockFornecedoresService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<ICidadeReader> _mockCidadeReader;
    private readonly FornecedoresService _service;
    private readonly string _validUri = "test-uri";
    public FornecedoresValidationTests()
    {
        _mockFornecedoresFactory = new Mock<IFFornecedoresFactory>();
        _mockReader = new Mock<IFornecedoresReader>();
        _validation = new FornecedoresValidation();
        _mockFornecedoresService = new Mock<IFornecedoresService>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockCidadeReader = new Mock<ICidadeReader>();
    }

#region Class Structure Tests
    [Fact]
    public void FornecedoresValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IFornecedoresValidation>();
    }

    [Fact]
    public void FornecedoresValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(FornecedoresValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void FornecedoresValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(FornecedoresValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Fornecedores CreateValidFornecedores()
    {
        return new Models.Fornecedores
        {
            Id = 1,
            Grupo = 0,
            Nome = "João",
            SubGrupo = 0,
            Tipo = false,
            Sexo = true,
            CNPJ = "93016944000138",
            InscEst = "AAAAAAAAAAAAA",
            CPF = "544.506.718-13",
            RG = "12.345.678-9",
            Endereco = "Rua das Flores, 123",
            Bairro = "Centro",
            CEP = "01234-567",
            Cidade = 0,
            Fone = "(11) 99999-9999",
            Fax = "(11) 88888-9999",
            Email = "test@email.com",
            Site = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Obs = "",
            Produtos = "",
            Contatos = ""
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll>());
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll>());
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = 0 }));
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var fornecedores = new Models.Fornecedores
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Tipo = false,
            Sexo = true,
            CNPJ = null,
            InscEst = null,
            CPF = null,
            RG = null,
            Endereco = null,
            Bairro = null,
            CEP = null,
            Fone = null,
            Fax = null,
            Email = null,
            Site = null,
            Obs = null,
            Produtos = null,
            Contatos = null
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region CPF Validation Tests
    [Fact]
    public async Task ValidateReg_WithDuplicateCPF_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CPF = "81833354036";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 2,
            Nome = "Existing User",
            CPF = "81833354036"
        };
        // Setup the Filter method with any filter to see if it's being called
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        // Verify that the Filter method was called
        _mockFornecedoresService.Verify(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>()), Times.AtLeastOnce);
    }

    [Fact]
    public async Task ValidateReg_WithDuplicateCPF_NoName_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CPF = "34666807004";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 2,
            Nome = "João",
            CPF = "34666807004"
        };
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        exception.Message.Should().Contain("(");
    }

    [Fact]
    public async Task ValidateReg_WithSameCPF_SameId_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CPF = "67290209117";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 1, // Same ID
            Nome = "Same User",
            CPF = "67290209117"
        };
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyCPF_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CPF = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullCPF_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CPF = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region CNPJ Validation Tests
    [Fact]
    public async Task ValidateReg_WithDuplicateCNPJ_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CNPJ = "90724474000179";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 2,
            Nome = "Existing User",
            CNPJ = "90724474000179"
        };
        // Setup the Filter method with any filter to see if it's being called
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        // Verify that the Filter method was called
        _mockFornecedoresService.Verify(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>()), Times.AtLeastOnce);
    }

    [Fact]
    public async Task ValidateReg_WithDuplicateCNPJ_NoName_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CNPJ = "84590488000144";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 2,
            Nome = "João",
            CNPJ = "84590488000144"
        };
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
    }

    [Fact]
    public async Task ValidateReg_WithSameCNPJ_SameId_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CNPJ = "34692293000137";
        fornecedores.Id = 1;
        var existingFornecedores = new FornecedoresResponseAll
        {
            Id = 1, // Same ID
            Nome = "Same User",
            CNPJ = "34692293000137"
        };
        _mockFornecedoresService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFornecedores>(), It.IsAny<string>())).ReturnsAsync(new List<FornecedoresResponseAll> { existingFornecedores });
        // Setup other mocks but don't override the Fornecedoress service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyCNPJ_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CNPJ = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullCNPJ_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.CNPJ = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region Email Validation Tests
    [Fact]
    public async Task ValidateReg_WithInvalidEmail_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Email = "invalid-email";
        SetupValidMocks();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("formato inválido.");
    }

    [Fact]
    public async Task ValidateReg_WithValidEmail_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Email = "menphis@example.com";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyEmail_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Email = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region Foreign Key Validation Tests - Cidade
    [Fact]
    public async Task ValidateReg_WithInvalidCidade_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Cidade = 999;
        _mockCidadeReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult<Models.Response.CidadeResponse>(null));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithMismatchedCidadeId_ShouldThrowSGValidationException()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Cidade = 999;
        var reg888 = new Models.Response.CidadeResponse
        {
            Id = 888
        }; // Different ID
        _mockCidadeReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult(reg888));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithValidCidade_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Cidade = 123;
        var reg123 = new Models.Response.CidadeResponse
        {
            Id = 123
        };
        _mockCidadeReader.Setup(x => x.Read(123, _mockConnection.Object)).Returns(Task.FromResult(reg123));
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithEmptyCidade_ShouldPass()
    {
        // Arrange
        var fornecedores = CreateValidFornecedores();
        fornecedores.Cidade = 0; // Assuming 0 is considered empty
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(fornecedores, _mockFornecedoresService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
        _mockCidadeReader.Verify(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}