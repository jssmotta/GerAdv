// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBNEPalavrasChaves - Nome da classe (ex: DBClientes)
/// NEPalavrasChaves - Nome da tabela (ex: Clientes)
/// npc - Prefixo dos campos (ex: cli)
/// npcCodigo - Nome do campo código (ex: cliCodigo)
/// npcNome - Nome do campo nome (ex: cliNome)
/// DBNEPalavrasChaves.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBNEPalavrasChavesTests : IDisposable
{
    private readonly DBNEPalavrasChaves _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBNEPalavrasChavesTests()
    {
        _instance = new DBNEPalavrasChaves();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("npcCodigo", typeof(int));
        dt.Columns.Add("npcQuemCad", typeof(int));
        dt.Columns.Add("npcDtCad", typeof(DateTime));
        dt.Columns.Add("npcQuemAtu", typeof(int));
        dt.Columns.Add("npcDtAtu", typeof(DateTime));
        dt.Columns.Add("npcVisto", typeof(bool));
        dt.Columns.Add("npcNome", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("NEPalavrasChaves", DBNEPalavrasChaves.PTabelaNome);
        Assert.Equal(" NEPalavrasChaves.* ", DBNEPalavrasChaves.CamposSqlX);
        Assert.Equal("npcCodigo", DBNEPalavrasChaves.CampoCodigo);
        Assert.Equal("npcNome", DBNEPalavrasChaves.CampoNome);
        Assert.Equal("npc", DBNEPalavrasChaves.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBNEPalavrasChaves();
        Assert.Equal(0, instance.ID);
        Assert.Equal("NEPalavrasChaves", instance.ITabelaName());
        Assert.Equal("npc", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBNEPalavrasChaves((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBNEPalavrasChaves((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["npcCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBNEPalavrasChaves(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("NEPalavrasChaves", cadastro.ITabelaName());
        Assert.Equal("npcCodigo", cadastro.ICampoCodigo());
        Assert.Equal("npcNome", cadastro.ICampoNome());
        Assert.Equal("npc", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBNEPalavrasChaves.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBNEPalavrasChaves.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBNEPalavrasChaves.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBNEPalavrasChaves();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBNEPalavrasChaves();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 80 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 80);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}