// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IEscritorios } from "@/app/GerAdv_TS/Escritorios/Interfaces/interface.Escritorios";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { EscritoriosApi } from "../../Apis/EscritoriosApi";
import { useValidationsEscritorios } from "../../Hooks/hookEscritorios";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpEscritorios from "../../Help/HelpEscritorios";
import EscritoriosPage from "@/app/pages/escritorios/page";

import CidadeComboBox from "@/app/GerAdv_TS/Cidade/ComboBox/CidadeComboBox";
import { CidadeApi } from "@/app/GerAdv_TS/Cidade/Apis/CidadeApi";
import InputName from "@/app/components/Inputs/InputName";
import InputCnpj from "@/app/components/Inputs/InputCnpj";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCep from "@/app/components/Inputs/InputCep";

interface EscritoriosFormProps {
  escritoriosData: IEscritorios;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const EscritoriosForm: React.FC<EscritoriosFormProps> = ({
  escritoriosData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new EscritoriosApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsEscritorios();

  const [_nomeCidade, setNomeCidade] = useState("");
  const cidadeApi = new CidadeApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("cidade") > 0) {
    if (escritoriosData.id === 0 && escritoriosData.cidade == 0) {
      cidadeApi
        .getById(getParamFromUrl("cidade"))
        .then((response) => {
          setNomeCidade(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      escritoriosData.cidade = getParamFromUrl("cidade");
    }
  }
  const addValorCidade = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "cidade", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Escritorios:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `EscritoriosForm-${escritoriosData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Escritorios diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        escritoriosData?.id == 0
          ? "Editar Escritorios"
          : "Adicionar Escritorios";
    }
  }, [escritoriosData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Escritorios"
            : "form-container form-container-Escritorios"
        }
      >
        <form
          className="formInputCadInc"
          id={`EscritoriosForm-${escritoriosData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Escritorios"
            data={escritoriosData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`EscritoriosForm-${escritoriosData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={escritoriosData}
              className="input-default-main"
              name="nome"
              value={escritoriosData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputCnpj
              tabIndex={1}
              type="text"
              id="cnpj"
              label="CNPJ"
              dataForm={escritoriosData}
              className="input-default-main"
              name="cnpj"
              value={escritoriosData.cnpj}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={escritoriosData}
              label="Casa"
              name="casa"
              checked={escritoriosData.casa}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={escritoriosData}
              label="Parceria"
              name="parceria"
              checked={escritoriosData.parceria}
              onChange={onChange}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={15}
              id="oab"
              label="OAB"
              dataForm={escritoriosData}
              className="input-default-main"
              name="oab"
              value={escritoriosData.oab}
              onChange={onChange}
            />

            <InputInput
              tabIndex={3}
              type="text"
              maxLength={50}
              id="endereco"
              label="Endereco"
              dataForm={escritoriosData}
              className="input-default-main"
              name="endereco"
              value={escritoriosData.endereco}
              onChange={onChange}
            />

            <CidadeComboBox
              name={"cidade"}
              dataForm={escritoriosData}
              value={escritoriosData.cidade}
              setValue={addValorCidade}
              label={"Cidade"}
            />

            <InputInput
              tabIndex={4}
              type="text"
              maxLength={30}
              id="bairro"
              label="Bairro"
              dataForm={escritoriosData}
              className="input-default-main"
              name="bairro"
              value={escritoriosData.bairro}
              onChange={onChange}
            />

            <InputCep
              tabIndex={5}
              type="text"
              id="cep"
              label="CEP"
              dataForm={escritoriosData}
              className="input-default-main"
              name="cep"
              value={escritoriosData.cep}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputInput
              tabIndex={6}
              type="text"
              maxLength={2147483647}
              id="fone"
              label="Fone"
              dataForm={escritoriosData}
              className="input-default-main"
              name="fone"
              value={escritoriosData.fone}
              onChange={onChange}
            />

            <InputInput
              tabIndex={7}
              type="text"
              maxLength={2147483647}
              id="fax"
              label="Fax"
              dataForm={escritoriosData}
              className="input-default-main"
              name="fax"
              value={escritoriosData.fax}
              onChange={onChange}
            />

            <InputInput
              tabIndex={8}
              type="text"
              maxLength={200}
              id="site"
              label="Site"
              dataForm={escritoriosData}
              className="input-default-main"
              name="site"
              value={escritoriosData.site}
              onChange={onChange}
            />

            <InputInput
              tabIndex={9}
              type="email"
              maxLength={100}
              id="email"
              label="EMail"
              dataForm={escritoriosData}
              className="input-default-main"
              name="email"
              value={escritoriosData.email}
              onChange={onChange}
            />

            <InputInput
              tabIndex={10}
              type="text"
              maxLength={2147483647}
              id="obs"
              label="OBS"
              dataForm={escritoriosData}
              className="input-default-main"
              name="obs"
              value={escritoriosData.obs}
              onChange={onChange}
            />

            <InputInput
              tabIndex={11}
              type="text"
              maxLength={80}
              id="advresponsavel"
              label="AdvResponsavel"
              dataForm={escritoriosData}
              className="input-default-main"
              name="advresponsavel"
              value={escritoriosData.advresponsavel}
              onChange={onChange}
            />

            <InputInput
              tabIndex={12}
              type="text"
              maxLength={80}
              id="secretaria"
              label="Secretaria"
              dataForm={escritoriosData}
              className="input-default-main"
              name="secretaria"
              value={escritoriosData.secretaria}
              onChange={onChange}
            />

            <InputInput
              tabIndex={13}
              type="text"
              maxLength={15}
              id="inscest"
              label="InscEst"
              dataForm={escritoriosData}
              className="input-default-main"
              name="inscest"
              value={escritoriosData.inscest}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={escritoriosData}
              label="Correspondente"
              name="correspondente"
              checked={escritoriosData.correspondente}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputCheckbox
              dataForm={escritoriosData}
              label="Top"
              name="top"
              checked={escritoriosData.top}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Escritorios" windowCrud={EscritoriosPage} />

          <CopyButton formId={`EscritoriosForm-${escritoriosData.id}`} />

          <PrintButton formId={`EscritoriosForm-${escritoriosData.id}`} />

          <AuditorButton
            tableName="Escritorios"
            coolName="Escritorios"
            id={escritoriosData.id}
          />

          <DeleteButton
            page={"/pages/escritorios"}
            id={escritoriosData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Escritorios"
            page={(onClose) => <HelpEscritorios onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Escritorios"
          data={escritoriosData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`EscritoriosForm-${escritoriosData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
