// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class StatusHTrabValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFStatusHTrabFactory> _mockStatusHTrabFactory;
    private readonly Mock<IStatusHTrabReader> _mockReader;
    private readonly StatusHTrabValidation _validation;
    private readonly Mock<IStatusHTrabService> _mockStatusHTrabService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly StatusHTrabService _service;
    private readonly string _validUri = "test-uri";
    public StatusHTrabValidationTests()
    {
        _mockStatusHTrabFactory = new Mock<IFStatusHTrabFactory>();
        _mockReader = new Mock<IStatusHTrabReader>();
        _validation = new StatusHTrabValidation();
        _mockStatusHTrabService = new Mock<IStatusHTrabService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void StatusHTrabValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IStatusHTrabValidation>();
    }

    [Fact]
    public void StatusHTrabValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(StatusHTrabValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void StatusHTrabValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(StatusHTrabValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.StatusHTrab CreateValidStatusHTrab()
    {
        return new Models.StatusHTrab
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            ResID = 0
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockStatusHTrabService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusHTrab>(), It.IsAny<string>())).ReturnsAsync(new List<StatusHTrabResponseAll>());
    // Setup other mocks but don't override the StatusHTrabs service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockStatusHTrabService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusHTrab>(), It.IsAny<string>())).ReturnsAsync(new List<StatusHTrabResponseAll>());
    // Setup other mocks but don't override the StatusHTrabs service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var statushtrab = new Models.StatusHTrab
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(statushtrab, _mockStatusHTrabService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockStatusHTrabService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}