// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { INECompromissosService } from '../Services/NECompromissos.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { INECompromissos } from '../Interfaces/interface.NECompromissos';
import { FilterNECompromissos } from '../Filters/NECompromissos';
import { isValidDate } from '@/app/tools/datetime';
import { NECompromissosApi } from '../Apis/ApiNECompromissos';

export const useNECompromissosForm = (
  initialNECompromissos: INECompromissos,
  dataService: INECompromissosService
) => {
  const [data, setData] = useState<INECompromissos>(initialNECompromissos);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadNECompromissos = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialNECompromissos);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchNECompromissosById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar N E Compromissos';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar N E Compromissos');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialNECompromissos]);

  const resetForm = useCallback(() => {
    setData(initialNECompromissos);
    setError(null);
  }, [initialNECompromissos]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadNECompromissos,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadNECompromissos, resetForm]);
  return returnValue;
};


export const useNECompromissosNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('NECompromissos', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
            console.log('Erro no listener de notificações.');
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useNECompromissosList = (dataService: INECompromissosService, currentFilter?: FilterNECompromissos | undefined | null) => {
  const [data, setData] = useState<INECompromissos[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterNECompromissos | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterNECompromissos | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterNECompromissos);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar necompromissos';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar necompromissos');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useNECompromissosNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsNECompromissos() {

  async function runValidation(data: INECompromissos, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const necompromissosApi = new NECompromissosApi(uri ?? '', token ?? '');

    const result = await necompromissosApi.validation(data);

    return result;
  }

  function validate(data: INECompromissos): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.textocompromisso.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo TextoCompromisso não pode ter mais de 2147483647 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}