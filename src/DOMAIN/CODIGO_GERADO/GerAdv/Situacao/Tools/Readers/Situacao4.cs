// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Fonte:ReadersGenerator
namespace MenphisSI.GerAdv.Readers;
public partial class SituacaoReader(IFSituacaoFactory situacaoFactory) : ISituacaoReader
{
    private readonly IFSituacaoFactory _situacaoFactory = situacaoFactory ?? throw new ArgumentNullException();
    public async Task<IEnumerable<DBNomeID>?> ListarNAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order) => await DevourerSqlData.ListarNomeID(BuildSqlQuery("sitCodigo, sitParte_Int", cWhere, order, max), parameters, uri, caching: false, max: max);
    public async Task<IEnumerable<SituacaoResponseAll>> ListarAsync(MsiSqlConnection oCnn, int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        var stopwatch = SituacaoDatabaseMetrics.StartTimer();
        var connStopwatch = SituacaoDatabaseMetrics.StartTimer();
        try
        {
            SituacaoDatabaseMetrics.IncrementActiveConnections("ListarAsync", oCnn?.Uri);
            SituacaoDatabaseMetrics.RecordConnectionOpen("ListarAsync", oCnn?.Uri, connStopwatch);
            var result = await ListarTabelaAsync(oCnn, BuildSqlQuery(DBSituacao.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
            SituacaoDatabaseMetrics.RecordSqlQuery("ListarAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count());
            return result;
        }
        catch (SqlException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ListarAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ListarAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            SituacaoDatabaseMetrics.DecrementActiveConnections("ListarAsync", oCnn?.Uri);
        }
    }

    private async Task<IEnumerable<SituacaoResponseAll>> ListarTabelaAsync(MsiSqlConnection? oCnn, string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var stopwatch = SituacaoDatabaseMetrics.StartTimer();
        var result = new List<SituacaoResponseAll>(max);
        try
        {
            await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: oCnn?.InnerConnection)
            {
                CommandTimeout = 120
            };
            if (parameters != null && parameters.Count > 0)
                foreach (var param in parameters)
                {
                    if (!cmd.Parameters.Contains(param.ParameterName))
                    {
                        var newParam = new SqlParameter(param.ParameterName, param.Value)
                        {
                            SqlDbType = param.SqlDbType,
                            Direction = param.Direction,
                            Size = param.Size,
                            Precision = param.Precision,
                            Scale = param.Scale
                        };
                        cmd.Parameters.Add(newParam);
                    }
                }

            await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.Default, cancellationToken);
            while (await reader.ReadAsync(cancellationToken))
            {
                if (cancellationToken.IsCancellationRequested)
                    return result;
                result.Add(ReadAll(_situacaoFactory.CreateFromReader(reader), reader)!);
            }

            SituacaoDatabaseMetrics.RecordSqlQuery("ListarTabelaAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count);
            return result;
        }
        catch (SqlException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "Timeout", oCnn?.Uri);
            throw;
        }
    }

    public async Task<SituacaoResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        var stopwatch = SituacaoDatabaseMetrics.StartTimer();
        try
        {
            SituacaoDatabaseMetrics.IncrementActiveConnections("ReadAsync", oCnn?.Uri);
            using var dbRec = await _situacaoFactory.CreateFromIdAsync(id, oCnn);
            var result = dbRec == null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
            SituacaoDatabaseMetrics.RecordSqlQuery("ReadAsync", "SELECT", oCnn?.Uri, stopwatch, result != null ? 1 : 0);
            return result;
        }
        catch (SqlException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ReadAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("ReadAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
        // Ignore
        }
    }

    public async Task<Models.Situacao?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _situacaoFactory.CreateFromIdAsync(id, oCnn);
        var situacao = new Models.Situacao
        {
            Id = dbRec.ID,
            Parte_Int = dbRec.FParte_Int ?? string.Empty,
            Parte_Opo = dbRec.FParte_Opo ?? string.Empty,
            Top = dbRec.FTop,
            Bold = dbRec.FBold,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return situacao;
    }

    public SituacaoResponse? Read(FSituacao dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public SituacaoResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _situacaoFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec is null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public SituacaoResponse? Read(FSituacao dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var situacao = new SituacaoResponse
        {
            Id = dbRec.ID,
            Parte_Int = dbRec.FParte_Int ?? string.Empty,
            Parte_Opo = dbRec.FParte_Opo ?? string.Empty,
            Top = dbRec.FTop,
            Bold = dbRec.FBold,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return situacao;
    }

    public SituacaoResponse? Read(SG.GerAdv.DBSituacao dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var situacao = new SituacaoResponse
        {
            Id = dbRec.ID,
            Parte_Int = dbRec.FParte_Int ?? string.Empty,
            Parte_Opo = dbRec.FParte_Opo ?? string.Empty,
            Top = dbRec.FTop,
            Bold = dbRec.FBold,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return situacao;
    }

    public SituacaoResponseAll? ReadAll(FSituacao dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var situacao = new SituacaoResponseAll
        {
            Id = dbRec.ID,
            Parte_Int = dbRec.FParte_Int ?? string.Empty,
            Parte_Opo = dbRec.FParte_Opo ?? string.Empty,
            Top = dbRec.FTop,
            Bold = dbRec.FBold,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return situacao;
    }

    public SituacaoResponseAll? ReadAll(SG.GerAdv.DBSituacao dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var situacao = new SituacaoResponseAll
        {
            Id = dbRec.ID,
            Parte_Int = dbRec.FParte_Int ?? string.Empty,
            Parte_Opo = dbRec.FParte_Opo ?? string.Empty,
            Top = dbRec.FTop,
            Bold = dbRec.FBold,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return situacao;
    }
}