// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProcessOutputRequestForm, useProcessOutputRequestList, useValidationsProcessOutputRequest } from '../GerAdv_TS/ProcessOutputRequest/Hooks/hookProcessOutputRequest';
import { IProcessOutputRequest } from '../GerAdv_TS/ProcessOutputRequest/Interfaces/interface.ProcessOutputRequest';
import { IProcessOutputRequestService } from '../GerAdv_TS/ProcessOutputRequest/Services/ProcessOutputRequest.service';
import { ProcessOutputRequestTestEmpty } from '../GerAdv_TS/Models/ProcessOutputRequest';


// Mock do serviço
const mockProcessOutputRequestService: jest.Mocked<IProcessOutputRequestService> = {
  fetchProcessOutputRequestById: jest.fn(),
  saveProcessOutputRequest: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteProcessOutputRequest: jest.fn(),
  validateProcessOutputRequest: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProcessOutputRequest: IProcessOutputRequest = { ...ProcessOutputRequestTestEmpty() };

describe('useProcessOutputRequestForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    expect(result.current.data).toEqual(initialProcessOutputRequest);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    const mockEvent = {
      target: {
        name: 'guid',
        value: 'Novo Process Output Request',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.guid).toBe('Novo Process Output Request');
  });

   test('deve carregar Process Output Request por ID', async () => {
    const mockProcessOutputRequest = { ...initialProcessOutputRequest, id: 1, guid: 'Process Output Request Teste' };
    mockProcessOutputRequestService.fetchProcessOutputRequestById.mockResolvedValue(mockProcessOutputRequest);

    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.loadProcessOutputRequest(1);
    });

    expect(mockProcessOutputRequestService.fetchProcessOutputRequestById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProcessOutputRequest);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Process Output Request', async () => {
    const errorMessage = 'Erro ao carregar Process Output Request';
    mockProcessOutputRequestService.fetchProcessOutputRequestById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.loadProcessOutputRequest(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProcessOutputRequest, guid: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProcessOutputRequest);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProcessOutputRequestForm(initialProcessOutputRequest, mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.loadProcessOutputRequest(0);
    });

    expect(mockProcessOutputRequestService.fetchProcessOutputRequestById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProcessOutputRequest);
  });
});

describe('useProcessOutputRequestList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProcessOutputRequestList(mockProcessOutputRequestService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProcessOutputRequest, id: 1, guid: 'Process Output Request 1' },
      { ...initialProcessOutputRequest, id: 2, guid: 'Process Output Request 2' }
    ];
    mockProcessOutputRequestService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProcessOutputRequestList(mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProcessOutputRequestService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProcessOutputRequestService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProcessOutputRequestList(mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProcessOutputRequest, id: 1, guid: 'Process Output Request Filtrado' }];
    const filtro = { guid: 'Process Output Request' };
    mockProcessOutputRequestService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProcessOutputRequestList(mockProcessOutputRequestService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProcessOutputRequestService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProcessOutputRequest', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProcessOutputRequest());

    const validData = { ...initialProcessOutputRequest, guid: 'Process Output Request Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar guid vazio', () => {
    const { result } = renderHook(() => useValidationsProcessOutputRequest());

    const invalidData = { ...initialProcessOutputRequest, guid: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ficar vazio.');
  });

  
  test('deve invalidar guid muito longo', () => {
    const { result } = renderHook(() => useValidationsProcessOutputRequest());

    const invalidData = { 
      ...initialProcessOutputRequest, 
      guid: 'a'.repeat(150+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ter mais de 150 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProcessOutputRequest());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProcessOutputRequest, id: 1, guid: 'Process Output Request Teste' }];
    mockProcessOutputRequestService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProcessOutputRequestList(mockProcessOutputRequestService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsProcessOutputRequest()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});