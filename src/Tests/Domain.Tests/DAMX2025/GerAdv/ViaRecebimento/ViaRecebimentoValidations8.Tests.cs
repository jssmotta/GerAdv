// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class ViaRecebimentoValidationTests : IDisposable
{
    private readonly ViaRecebimentoValidation _validation;
    private readonly Mock<IViaRecebimentoService> _mockViaRecebimentoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly ViaRecebimentoService _service;
    private readonly string _validUri = "test-uri";
    public ViaRecebimentoValidationTests()
    {
        _validation = new ViaRecebimentoValidation();
        _mockViaRecebimentoService = new Mock<IViaRecebimentoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void ViaRecebimentoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IViaRecebimentoValidation>();
    }

    [Fact]
    public void ViaRecebimentoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(ViaRecebimentoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void ViaRecebimentoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(ViaRecebimentoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.ViaRecebimento CreateValidViaRecebimento()
    {
        return new Models.ViaRecebimento
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockViaRecebimentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterViaRecebimento>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the ViaRecebimentos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockViaRecebimentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterViaRecebimento>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the ViaRecebimentos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var viarecebimento = new Models.ViaRecebimento
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(viarecebimento, _mockViaRecebimentoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockViaRecebimentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}