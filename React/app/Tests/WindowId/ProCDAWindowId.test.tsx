// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProCDAWindowId from '@/app/GerAdv_TS/ProCDA/Crud/Grids/ProCDAWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProCDAWindowId from '@/app/GerAdv_TS/ProCDA/Crud/Grids/ProCDAWindowId';
import { ProCDAEmpty } from '@/app/GerAdv_TS/Models/ProCDA';

describe('ProCDAWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProCDAWindowId).toBeDefined();
    expect(typeof ProCDAWindowId === 'function' || typeof ProCDAWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProCDAService class so constructor usage doesn't throw and we can control fetchProCDAById
const mockFetchProCDAById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProCDA/Services/ProCDA.service', () => ({
  ProCDAService: jest.fn().mockImplementation(() => ({
    fetchProCDAById: (...args: any[]) => mockFetchProCDAById(...args),
  })),
}));

// Mock the child ProCDAWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProCDA/Crud/Grids/ProCDAWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='procda-window' {...p} />,
}));

describe('ProCDAWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProCDAWindowId).toBeDefined();
    expect(typeof ProCDAWindowId === 'function' || typeof ProCDAWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProCDAWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProCDAWindow with ProCDAEmpty when id is 0', async () => {
    render(
      <ProCDAWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProCDAWindow should be rendered with selectedProCDA equal to ProCDAEmpty()
    const node = await screen.findByTestId('procda-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProCDA"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprocda') || node.getAttribute('selectedProCDA')).not.toBeNull();
  });

  it('fetches procda by id and passes it to ProCDAWindow when id > 0', async () => {
    const fakeProCDA = { id: 123, nomecliente: 'Test' };
    mockFetchProCDAById.mockResolvedValue(fakeProCDA);

    render(
      <ProCDAWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('procda-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProCDAById).toHaveBeenCalledWith(123));
  });
});
