// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OponentesRepLegalService, OponentesRepLegalValidator } from '@/app/GerAdv_TS/OponentesRepLegal/Services/OponentesRepLegal.service';
import { OponentesRepLegalApi, OponentesRepLegalApiError } from '@/app/GerAdv_TS/OponentesRepLegal/Apis/ApiOponentesRepLegal';
import { IOponentesRepLegal } from '@/app/GerAdv_TS/OponentesRepLegal/Interfaces/interface.OponentesRepLegal';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OponentesRepLegalApi;

const sample: IOponentesRepLegal = { id: 1, nome: 'Teste' };

describe('OponentesRepLegalService', () => {
  let service: OponentesRepLegalService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OponentesRepLegalService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OponentesRepLegalValidator, 'validateOponentesRepLegal').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOponentesRepLegalById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOponentesRepLegalById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOponentesRepLegalById lança OponentesRepLegalApiError para id inválido', async () => {
    await expect(service.fetchOponentesRepLegalById(0)).rejects.toBeInstanceOf(OponentesRepLegalApiError);
  });

  it('saveOponentesRepLegal chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOponentesRepLegal(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOponentesRepLegal lança OponentesRepLegalApiError quando validação falha', async () => {
    (OponentesRepLegalValidator.validateOponentesRepLegal as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOponentesRepLegal(sample)).rejects.toBeInstanceOf(OponentesRepLegalApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOponentesRepLegal chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOponentesRepLegal(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOponentesRepLegal lança OponentesRepLegalApiError para id inválido', async () => {
    await expect(service.deleteOponentesRepLegal(0)).rejects.toBeInstanceOf(OponentesRepLegalApiError);
  });

  it('validateOponentesRepLegal delega para OponentesRepLegalValidator', () => {
    (OponentesRepLegalValidator.validateOponentesRepLegal as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOponentesRepLegal(sample);
    expect(r.isValid).toBe(true);
  });
});
