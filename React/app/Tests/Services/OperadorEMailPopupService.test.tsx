// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OperadorEMailPopupService, OperadorEMailPopupValidator } from '@/app/GerAdv_TS/OperadorEMailPopup/Services/OperadorEMailPopup.service';
import { OperadorEMailPopupApi, OperadorEMailPopupApiError } from '@/app/GerAdv_TS/OperadorEMailPopup/Apis/ApiOperadorEMailPopup';
import { IOperadorEMailPopup } from '@/app/GerAdv_TS/OperadorEMailPopup/Interfaces/interface.OperadorEMailPopup';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OperadorEMailPopupApi;

const sample: IOperadorEMailPopup = { id: 1, nome: 'Teste' };

describe('OperadorEMailPopupService', () => {
  let service: OperadorEMailPopupService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OperadorEMailPopupService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OperadorEMailPopupValidator, 'validateOperadorEMailPopup').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOperadorEMailPopupById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOperadorEMailPopupById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOperadorEMailPopupById lança OperadorEMailPopupApiError para id inválido', async () => {
    await expect(service.fetchOperadorEMailPopupById(0)).rejects.toBeInstanceOf(OperadorEMailPopupApiError);
  });

  it('saveOperadorEMailPopup chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOperadorEMailPopup(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOperadorEMailPopup lança OperadorEMailPopupApiError quando validação falha', async () => {
    (OperadorEMailPopupValidator.validateOperadorEMailPopup as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOperadorEMailPopup(sample)).rejects.toBeInstanceOf(OperadorEMailPopupApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOperadorEMailPopup chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOperadorEMailPopup(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOperadorEMailPopup lança OperadorEMailPopupApiError para id inválido', async () => {
    await expect(service.deleteOperadorEMailPopup(0)).rejects.toBeInstanceOf(OperadorEMailPopupApiError);
  });

  it('validateOperadorEMailPopup delega para OperadorEMailPopupValidator', () => {
    (OperadorEMailPopupValidator.validateOperadorEMailPopup as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOperadorEMailPopup(sample);
    expect(r.isValid).toBe(true);
  });
});
