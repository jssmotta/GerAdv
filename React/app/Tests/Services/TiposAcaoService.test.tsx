// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TiposAcaoService, TiposAcaoValidator } from '@/app/GerAdv_TS/TiposAcao/Services/TiposAcao.service';
import { TiposAcaoApi, TiposAcaoApiError } from '@/app/GerAdv_TS/TiposAcao/Apis/ApiTiposAcao';
import { ITiposAcao } from '@/app/GerAdv_TS/TiposAcao/Interfaces/interface.TiposAcao';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TiposAcaoApi;

const sample: ITiposAcao = { id: 1, nome: 'Teste' };

describe('TiposAcaoService', () => {
  let service: TiposAcaoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TiposAcaoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TiposAcaoValidator, 'validateTiposAcao').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTiposAcaoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTiposAcaoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTiposAcaoById lança TiposAcaoApiError para id inválido', async () => {
    await expect(service.fetchTiposAcaoById(0)).rejects.toBeInstanceOf(TiposAcaoApiError);
  });

  it('saveTiposAcao chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTiposAcao(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTiposAcao lança TiposAcaoApiError quando validação falha', async () => {
    (TiposAcaoValidator.validateTiposAcao as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTiposAcao(sample)).rejects.toBeInstanceOf(TiposAcaoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTiposAcao chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTiposAcao(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTiposAcao lança TiposAcaoApiError para id inválido', async () => {
    await expect(service.deleteTiposAcao(0)).rejects.toBeInstanceOf(TiposAcaoApiError);
  });

  it('validateTiposAcao delega para TiposAcaoValidator', () => {
    (TiposAcaoValidator.validateTiposAcao as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTiposAcao(sample);
    expect(r.isValid).toBe(true);
  });
});
