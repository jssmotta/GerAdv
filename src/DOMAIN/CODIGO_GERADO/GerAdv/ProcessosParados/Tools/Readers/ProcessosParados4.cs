// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Readers;
public partial class ProcessosParadosReader(IFProcessosParadosFactory processosparadosFactory) : IProcessosParadosReader
{
    private readonly IFProcessosParadosFactory _processosparadosFactory = processosparadosFactory;
    public async Task<IEnumerable<ProcessosParadosResponseAll>> Listar(int max, string uri, string cWhere, List<SqlParameter> parameters, string order, CancellationToken cancellationToken) => await ListarTabela(BuildSqlQuery(DBProcessosParados.CamposSqlX, cWhere, order, max), parameters, uri, caching: DevourerOne.PCachingDefault, max: max, cancellationToken: cancellationToken);
    private async Task<IEnumerable<ProcessosParadosResponseAll>> ListarTabela(string sql, List<SqlParameter> parameters, string uri, bool caching = DevourerOne.PCachingDefault, int max = 200, CancellationToken cancellationToken = default)
    {
        var result = new List<ProcessosParadosResponseAll>(max);
        await using var connection = Configuracoes.GetConnectionByUri(uri);
        await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: connection?.InnerConnection)
        {
            CommandTimeout = 30
        };
        foreach (var param in parameters)
        {
            if (!cmd.Parameters.Contains(param.ParameterName))
            {
                var newParam = new SqlParameter(param.ParameterName, param.Value)
                {
                    SqlDbType = param.SqlDbType,
                    Direction = param.Direction,
                    Size = param.Size,
                    Precision = param.Precision,
                    Scale = param.Scale
                };
                cmd.Parameters.Add(newParam);
            }
        }

        await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.SingleResult);
        while (await reader.ReadAsync())
        {
            if (cancellationToken.IsCancellationRequested)
                return result;
            result.Add(ReadAll(_processosparadosFactory.CreateFromReader(reader), reader)!);
        }

        return result;
    }

    public async Task<ProcessosParadosResponse?> Read(int id, MsiSqlConnection oCnn)
    {
        using var dbRec = await _processosparadosFactory.CreateFromIdAsync(id, oCnn);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public async Task<Models.ProcessosParados?> ReadM(int id, MsiSqlConnection oCnn)
    {
        using var dbRec = await _processosparadosFactory.CreateFromIdAsync(id, oCnn);
        var processosparados = new Models.ProcessosParados
        {
            Id = dbRec.ID,
            Processo = dbRec.FProcesso,
            Semana = dbRec.FSemana,
            Ano = dbRec.FAno,
            Operador = dbRec.FOperador,
        };
        if (DateTime.TryParse(dbRec.FDataHora, out DateTime XDataHora))
        {
            processosparados.DataHora = dbRec.FDataHora;
            processosparados.DataHora_date = XDataHora;
        }

        if (DateTime.TryParse(dbRec.FDataHistorico, out DateTime XDataHistorico))
        {
            processosparados.DataHistorico = dbRec.FDataHistorico;
            processosparados.DataHistorico_date = XDataHistorico;
        }

        if (DateTime.TryParse(dbRec.FDataNENotas, out DateTime XDataNENotas))
        {
            processosparados.DataNENotas = dbRec.FDataNENotas;
            processosparados.DataNENotas_date = XDataNENotas;
        }

        return processosparados;
    }

    public ProcessosParadosResponse? Read(FProcessosParados dbRec, MsiSqlConnection oCnn)
    {
        return Read(dbRec);
    }

    public ProcessosParadosResponse? Read(string where, List<SqlParameter> parameters, MsiSqlConnection oCnn)
    {
        using var dbRec = _processosparadosFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public ProcessosParadosResponse? Read(FProcessosParados dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var processosparados = new ProcessosParadosResponse
        {
            Id = dbRec.ID,
            Processo = dbRec.FProcesso,
            Semana = dbRec.FSemana,
            Ano = dbRec.FAno,
            Operador = dbRec.FOperador,
        };
        if (DateTime.TryParse(dbRec.FDataHora, out DateTime XDataHora))
        {
            processosparados.DataHora = dbRec.FDataHora;
            processosparados.DataHora_date = XDataHora;
        }

        if (DateTime.TryParse(dbRec.FDataHistorico, out DateTime XDataHistorico))
        {
            processosparados.DataHistorico = dbRec.FDataHistorico;
            processosparados.DataHistorico_date = XDataHistorico;
        }

        if (DateTime.TryParse(dbRec.FDataNENotas, out DateTime XDataNENotas))
        {
            processosparados.DataNENotas = dbRec.FDataNENotas;
            processosparados.DataNENotas_date = XDataNENotas;
        }

        return processosparados;
    }

    public ProcessosParadosResponse? Read(SG.GerAdv.DBProcessosParados dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var processosparados = new ProcessosParadosResponse
        {
            Id = dbRec.ID,
            Processo = dbRec.FProcesso,
            Semana = dbRec.FSemana,
            Ano = dbRec.FAno,
            Operador = dbRec.FOperador,
        };
        if (DateTime.TryParse(dbRec.FDataHora, out DateTime XDataHora))
        {
            processosparados.DataHora = dbRec.FDataHora;
            processosparados.DataHora_date = XDataHora;
        }

        if (DateTime.TryParse(dbRec.FDataHistorico, out DateTime XDataHistorico))
        {
            processosparados.DataHistorico = dbRec.FDataHistorico;
            processosparados.DataHistorico_date = XDataHistorico;
        }

        if (DateTime.TryParse(dbRec.FDataNENotas, out DateTime XDataNENotas))
        {
            processosparados.DataNENotas = dbRec.FDataNENotas;
            processosparados.DataNENotas_date = XDataNENotas;
        }

        return processosparados;
    }

    public ProcessosParadosResponseAll? ReadAll(FProcessosParados dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var processosparados = new ProcessosParadosResponseAll
        {
            Id = dbRec.ID,
            Processo = dbRec.FProcesso,
            Semana = dbRec.FSemana,
            Ano = dbRec.FAno,
            Operador = dbRec.FOperador,
        };
        if (DateTime.TryParse(dbRec.FDataHora, out DateTime XDataHora))
        {
            processosparados.DataHora = dbRec.FDataHora;
            processosparados.DataHora_date = XDataHora;
        }

        if (DateTime.TryParse(dbRec.FDataHistorico, out DateTime XDataHistorico))
        {
            processosparados.DataHistorico = dbRec.FDataHistorico;
            processosparados.DataHistorico_date = XDataHistorico;
        }

        if (DateTime.TryParse(dbRec.FDataNENotas, out DateTime XDataNENotas))
        {
            processosparados.DataNENotas = dbRec.FDataNENotas;
            processosparados.DataNENotas_date = XDataNENotas;
        }

        try
        {
            processosparados.NroPastaProcessos = dr[DBProcessosDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            processosparados.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return processosparados;
    }

    public ProcessosParadosResponseAll? ReadAll(SG.GerAdv.DBProcessosParados dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var processosparados = new ProcessosParadosResponseAll
        {
            Id = dbRec.ID,
            Processo = dbRec.FProcesso,
            Semana = dbRec.FSemana,
            Ano = dbRec.FAno,
            Operador = dbRec.FOperador,
        };
        if (DateTime.TryParse(dbRec.FDataHora, out DateTime XDataHora))
        {
            processosparados.DataHora = dbRec.FDataHora;
            processosparados.DataHora_date = XDataHora;
        }

        if (DateTime.TryParse(dbRec.FDataHistorico, out DateTime XDataHistorico))
        {
            processosparados.DataHistorico = dbRec.FDataHistorico;
            processosparados.DataHistorico_date = XDataHistorico;
        }

        if (DateTime.TryParse(dbRec.FDataNENotas, out DateTime XDataNENotas))
        {
            processosparados.DataNENotas = dbRec.FDataNENotas;
            processosparados.DataNENotas_date = XDataNENotas;
        }

        try
        {
            processosparados.NroPastaProcessos = dr[DBProcessosDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            processosparados.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return processosparados;
    }
}