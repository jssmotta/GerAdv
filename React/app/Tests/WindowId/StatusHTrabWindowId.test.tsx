// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import StatusHTrabWindowId from '@/app/GerAdv_TS/StatusHTrab/Crud/Grids/StatusHTrabWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import StatusHTrabWindowId from '@/app/GerAdv_TS/StatusHTrab/Crud/Grids/StatusHTrabWindowId';
import { StatusHTrabEmpty } from '@/app/GerAdv_TS/Models/StatusHTrab';

describe('StatusHTrabWindowId (basic)', () => {
  it('exports the component', () => {
    expect(StatusHTrabWindowId).toBeDefined();
    expect(typeof StatusHTrabWindowId === 'function' || typeof StatusHTrabWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the StatusHTrabService class so constructor usage doesn't throw and we can control fetchStatusHTrabById
const mockFetchStatusHTrabById = jest.fn();
jest.mock('@/app/GerAdv_TS/StatusHTrab/Services/StatusHTrab.service', () => ({
  StatusHTrabService: jest.fn().mockImplementation(() => ({
    fetchStatusHTrabById: (...args: any[]) => mockFetchStatusHTrabById(...args),
  })),
}));

// Mock the child StatusHTrabWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/StatusHTrab/Crud/Grids/StatusHTrabWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='statushtrab-window' {...p} />,
}));

describe('StatusHTrabWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(StatusHTrabWindowId).toBeDefined();
    expect(typeof StatusHTrabWindowId === 'function' || typeof StatusHTrabWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <StatusHTrabWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders StatusHTrabWindow with StatusHTrabEmpty when id is 0', async () => {
    render(
      <StatusHTrabWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // StatusHTrabWindow should be rendered with selectedStatusHTrab equal to StatusHTrabEmpty()
    const node = await screen.findByTestId('statushtrab-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedStatusHTrab"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedstatushtrab') || node.getAttribute('selectedStatusHTrab')).not.toBeNull();
  });

  it('fetches statushtrab by id and passes it to StatusHTrabWindow when id > 0', async () => {
    const fakeStatusHTrab = { id: 123, nomecliente: 'Test' };
    mockFetchStatusHTrabById.mockResolvedValue(fakeStatusHTrab);

    render(
      <StatusHTrabWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('statushtrab-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchStatusHTrabById).toHaveBeenCalledWith(123));
  });
});
