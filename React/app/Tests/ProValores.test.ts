// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProValoresForm, useProValoresList, useValidationsProValores } from '../GerAdv_TS/ProValores/Hooks/hookProValores';
import { IProValores } from '../GerAdv_TS/ProValores/Interfaces/interface.ProValores';
import { IProValoresService } from '../GerAdv_TS/ProValores/Services/ProValores.service';
import { ProValoresTestEmpty } from '../GerAdv_TS/Models/ProValores';


// Mock do serviço
const mockProValoresService: jest.Mocked<IProValoresService> = {
  fetchProValoresById: jest.fn(),
  saveProValores: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteProValores: jest.fn(),
  validateProValores: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProValores: IProValores = { ...ProValoresTestEmpty() };

describe('useProValoresForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    expect(result.current.data).toEqual(initialProValores);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    const mockEvent = {
      target: {
        name: 'data',
        value: 'Novo Pro Valores',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.data).toBe('Novo Pro Valores');
  });

   test('deve carregar Pro Valores por ID', async () => {
    const mockProValores = { ...initialProValores, id: 1, data: 'Pro Valores Teste' };
    mockProValoresService.fetchProValoresById.mockResolvedValue(mockProValores);

    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    await act(async () => {
      await result.current.loadProValores(1);
    });

    expect(mockProValoresService.fetchProValoresById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProValores);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Pro Valores', async () => {
    const errorMessage = 'Erro ao carregar Pro Valores';
    mockProValoresService.fetchProValoresById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    await act(async () => {
      await result.current.loadProValores(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProValores, data: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProValores);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    await act(async () => {
      await result.current.loadProValores(0);
    });

    expect(mockProValoresService.fetchProValoresById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProValores);
  });
});

describe('useProValoresList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProValoresList(mockProValoresService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProValores, id: 1, data: 'Pro Valores 1' },
      { ...initialProValores, id: 2, data: 'Pro Valores 2' }
    ];
    mockProValoresService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProValoresList(mockProValoresService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProValoresService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProValoresService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProValoresList(mockProValoresService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProValores, id: 1, data: 'Pro Valores Filtrado' }];
    const filtro = { data: 'Pro Valores' };
    mockProValoresService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProValoresList(mockProValoresService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProValoresService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProValores', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProValores());

    const validData = { ...initialProValores, data: 'Pro Valores Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar data vazio', () => {
    const { result } = renderHook(() => useValidationsProValores());

    const invalidData = { ...initialProValores, data: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ficar vazio.');
  });

  
  test('deve invalidar data muito longo', () => {
    const { result } = renderHook(() => useValidationsProValores());

    const invalidData = { 
      ...initialProValores, 
      data: 'a'.repeat(-1+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ter mais de -1 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProValores());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProValores, id: 1, data: 'Pro Valores Teste' }];
    mockProValoresService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProValoresList(mockProValoresService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsProValores()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useProValoresForm(initialProValores, mockProValoresService)
    );

    const mockEvent = {
      target: {
        name: 'ignorar',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.ignorar).toBe(true);
  });


