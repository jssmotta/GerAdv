// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tracking: Forms.tsx.txt
'use client';
import { ILivroCaixaClientes } from '@/app/GerAdv_TS/LivroCaixaClientes/Interfaces/interface.LivroCaixaClientes';
import { useRouter } from 'next/navigation';
import React, { useEffect, useState, useRef } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { getParamFromUrl } from '@/app/tools/helpers';
import '@/app/styles/CrudFormsBase.css';
import '@/app/styles/CrudFormsMobile.css';
import '@/app/styles/CrudForms5.css'; // [ INDEX_SIZE ]
import ButtonSalvarCrud from '@/app/components/Cruds/ButtonSalvarCrud';
import { useIsMobile } from '@/app/context/MobileContext';
import DeleteButton from '@/app/components/Cruds/DeleteButton';
import { LivroCaixaClientesApi } from '../../Apis/ApiLivroCaixaClientes';
import { useValidationsLivroCaixaClientes } from '../../Hooks/hookLivroCaixaClientes';
import LivroCaixaComboBox from '@/app/GerAdv_TS/LivroCaixa/ComboBox/LivroCaixa';
import ClientesComboBox from '@/app/GerAdv_TS/Clientes/ComboBox/Clientes';
import { LivroCaixaApi } from '@/app/GerAdv_TS/LivroCaixa/Apis/ApiLivroCaixa';
import { ClientesApi } from '@/app/GerAdv_TS/Clientes/Apis/ApiClientes';
import InputName from '@/app/components/Inputs/InputName';
import InputCheckbox from '@/app/components/Inputs/InputCheckbox';
interface LivroCaixaClientesFormProps {
  livrocaixaclientesData: ILivroCaixaClientes;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const LivroCaixaClientesForm: React.FC<LivroCaixaClientesFormProps> = ({
  livrocaixaclientesData, 
  onChange, 
  onSubmit, 
  onClose, 
  onError, 
  onReload, 
  onSuccess, 
}) => {
const router = useRouter();
const { systemContext } = useSystemContext();
const isMobile = useIsMobile();
const dadoApi = new LivroCaixaClientesApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [isSubmitting, setIsSubmitting] = useState(false);
const initialized = useRef(false);
const validationForm = useValidationsLivroCaixaClientes();
const [nomeLivroCaixa, setNomeLivroCaixa] = useState('');
const livrocaixaApi = new LivroCaixaApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [nomeClientes, setNomeClientes] = useState('');
const clientesApi = new ClientesApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');

if (getParamFromUrl('livrocaixa') > 0) {
  if (livrocaixaclientesData.id === 0 && livrocaixaclientesData.livrocaixa == 0) {
    livrocaixaApi
    .getById(getParamFromUrl('livrocaixa'))
    .then((response) => {
      setNomeLivroCaixa(response.data.data);
    })
    .catch((error) => {
      console.log('Error unexpected');
    });

    livrocaixaclientesData.livrocaixa = getParamFromUrl('livrocaixa');
  }
}

if (getParamFromUrl('clientes') > 0) {
  if (livrocaixaclientesData.id === 0 && livrocaixaclientesData.cliente == 0) {
    clientesApi
    .getById(getParamFromUrl('clientes'))
    .then((response) => {
      setNomeClientes(response.data.nome);
    })
    .catch((error) => {
      console.log('Error unexpected');
    });

    livrocaixaclientesData.cliente = getParamFromUrl('clientes');
  }
}
const addValorLivroCaixa = (e: any) => {
  if (e?.id>0)
    onChange({ target: { name: 'livrocaixa', value: e.id } });
  };
  const addValorCliente = (e: any) => {
    if (e?.id>0)
      onChange({ target: { name: 'cliente', value: e.id } });
    };
    const onConfirm = (e: React.FormEvent) => {
      e.preventDefault();
      if (e.stopPropagation) e.stopPropagation();

        if (!isSubmitting) {
          setIsSubmitting(true);

          try {
            onSubmit(e);
          } catch (error) {
          console.log('Erro ao submeter formulário de LivroCaixaClientes:');
          setIsSubmitting(false);
          if (onError) onError();
          }
        }
      };
      const handleCancel = () => {
        if (onReload) {
          onReload(); // Recarrega os dados originais
        } else {
        onClose(); // Comportamento padrão se não há callback de recarga
      }
    };

    const handleDirectSave = () => {
      if (!isSubmitting) {
        setIsSubmitting(true);

        try {
          const syntheticEvent = {
            preventDefault: () => { }, 
            target: document.getElementById(`LivroCaixaClientesForm-${livrocaixaclientesData.id}`)
          } as unknown as React.FormEvent;

          onSubmit(syntheticEvent);
        } catch (error) {
        console.log('Erro ao salvar LivroCaixaClientes diretamente');
        setIsSubmitting(false);
        if (onError) onError();
        }
      }
    };
    useEffect(() => {
      const el = document.querySelector('.nameFormMobile');
      if (el) {
        el.textContent = livrocaixaclientesData?.id == 0 ? 'Editar LivroCaixaClientes' : 'Adicionar Livro Caixa Clientes';
      }
    }, [livrocaixaclientesData.id]);
    return (
    <>
    {!isMobile ? <style jsx global>{`
      @media (max-width: 1366px) {
        html {
          zoom: 0.8 !important;
        }
      }
      `}</style> : null}

      <div className={isMobile ? 'form-container form-container-LivroCaixaClientes' : 'form-container5 form-container-LivroCaixaClientes'}>

        <form className='formInputCadInc' id={`LivroCaixaClientesForm-${livrocaixaclientesData.id}`} onSubmit={onConfirm}>
          {!isMobile && (
            <ButtonSalvarCrud isMobile={false} validationForm={validationForm} entity='LivroCaixaClientes' data={livrocaixaclientesData} isSubmitting={isSubmitting} onClose={onClose} formId={`LivroCaixaClientesForm-${livrocaixaclientesData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
            )}
            <div className='grid-container'>


              <LivroCaixaComboBox
              name={'livrocaixa'}
              dataForm={livrocaixaclientesData}
              value={livrocaixaclientesData.livrocaixa}
              setValue={addValorLivroCaixa}
              label={'Livro Caixa'}
              />

              <ClientesComboBox
              name={'cliente'}
              dataForm={livrocaixaclientesData}
              value={livrocaixaclientesData.cliente}
              setValue={addValorCliente}
              label={'Clientes'}
              />
              <InputCheckbox dataForm={livrocaixaclientesData} label='Lancado' name='lancado' checked={livrocaixaclientesData.lancado} onChange={onChange} />
            </div>
          </form>


          {isMobile && (
            <ButtonSalvarCrud isMobile={true} validationForm={validationForm} entity='LivroCaixaClientes' data={livrocaixaclientesData} isSubmitting={isSubmitting} onClose={onClose} formId={`LivroCaixaClientesForm-${livrocaixaclientesData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
            )}
            <DeleteButton page={'/pages/livrocaixaclientes'} id={livrocaixaclientesData.id} closeModel={onClose} dadoApi={dadoApi} />
          </div>
          <div className='form-spacer'></div>
          </>
        );
      };