// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ProcessOutputEngine
// Fonte:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class ProcessOutputEngineServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFProcessOutputEngineFactory> _mockProcessOutputEngineFactory;
    private readonly Mock<IProcessOutputEngineReader> _mockReader;
    private readonly Mock<IProcessOutputEngineValidation> _mockValidation;
    private readonly Mock<IProcessOutputEngineWriter> _mockWriter;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IGenericVoiceFilterService<Filters.FilterProcessOutputEngine>> _mockVoiceFilterService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly ProcessOutputEngineService _service;
    private readonly ServiceFilter _serviceS;
    public ProcessOutputEngineServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockProcessOutputEngineFactory = new Mock<IFProcessOutputEngineFactory>();
        _mockReader = new Mock<IProcessOutputEngineReader>();
        _mockValidation = new Mock<IProcessOutputEngineValidation>();
        _mockWriter = new Mock<IProcessOutputEngineWriter>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockVoiceFilterService = new Mock<IGenericVoiceFilterService<Filters.FilterProcessOutputEngine>>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new ProcessOutputEngineService(_mockAppSettings.Object, _mockProcessOutputEngineFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _mockVoiceFilterService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterProcessOutputEngine? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Nome = "João",
            Database = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Tabela = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Campo = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Valor = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Output = "",
            Administrador = 2,
            OutputSource = 1,
            DisabledItem = 1,
            IDModulo = 1,
            IsOnlyProcesso = 2,
            MyID = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Guid_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string GuidComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = GuidComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{GuidGigante}%");
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Guid_ComCaracteresEspeciais_DeveProcessarCorretamente(string GuidComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = GuidComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(GuidComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_GuidProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_GuidProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_GuidProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_GuidProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "Guid",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Guid}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Guid_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Guid_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Guid = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Nome_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string NomeComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = NomeComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Nome_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var NomeGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Nome = NomeGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{NomeGigante}%");
    }

    [Fact]
    public void WFiltro_Nome_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var NomeGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Nome = NomeGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Nome_ComCaracteresEspeciais_DeveProcessarCorretamente(string NomeComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = NomeComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(NomeComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_NomeProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_NomeProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_NomeProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NomeProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Nome_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Nome_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Nome_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Nome_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Nome}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Nome_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Nome_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Database_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string DatabaseComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = DatabaseComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Database_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var DatabaseGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Database = DatabaseGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{DatabaseGigante}%");
    }

    [Fact]
    public void WFiltro_Database_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var DatabaseGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Database = DatabaseGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Database_ComCaracteresEspeciais_DeveProcessarCorretamente(string DatabaseComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = DatabaseComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(DatabaseComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_DatabaseProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_DatabaseProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DatabaseProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_DatabaseProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Database_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "Database",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Database_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Database_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Database_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Database}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Database_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Database_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Database = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Tabela_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string TabelaComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = TabelaComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Tabela_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var TabelaGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = TabelaGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{TabelaGigante}%");
    }

    [Fact]
    public void WFiltro_Tabela_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var TabelaGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = TabelaGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Tabela_ComCaracteresEspeciais_DeveProcessarCorretamente(string TabelaComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = TabelaComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(TabelaComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_TabelaProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_TabelaProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_TabelaProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_TabelaProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Tabela_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "Tabela",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Tabela_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Tabela_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Tabela_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Tabela}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Tabela_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Tabela_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Tabela = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Campo_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string CampoComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = CampoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Campo_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var CampoGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Campo = CampoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{CampoGigante}%");
    }

    [Fact]
    public void WFiltro_Campo_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var CampoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Campo = CampoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Campo_ComCaracteresEspeciais_DeveProcessarCorretamente(string CampoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = CampoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(CampoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_CampoProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_CampoProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_CampoProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_CampoProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Campo_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "Campo",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Campo_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Campo_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Campo_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Campo}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Campo_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Campo_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Campo = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Valor_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string ValorComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = ValorComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Valor_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var ValorGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Valor = ValorGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{ValorGigante}%");
    }

    [Fact]
    public void WFiltro_Valor_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var ValorGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Valor = ValorGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Valor_ComCaracteresEspeciais_DeveProcessarCorretamente(string ValorComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = ValorComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(ValorComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_ValorProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_ValorProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ValorProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ValorProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Valor_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "Valor",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Valor_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Valor_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Valor_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Valor}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Valor_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Valor_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Valor = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Output_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string OutputComWhitespace)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = OutputComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Output_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var OutputGigante = new string ('A', 1000);
        var filtro = new FilterProcessOutputEngine
        {
            Output = OutputGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{OutputGigante}%");
    }

    [Fact]
    public void WFiltro_Output_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var OutputGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProcessOutputEngine
        {
            Output = OutputGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Output_ComCaracteresEspeciais_DeveProcessarCorretamente(string OutputComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = OutputComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(OutputComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_OutputProcessOutputEngine_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_OutputProcessOutputEngine_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_OutputProcessOutputEngine_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_OutputProcessOutputEngine_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Output_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "Output",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Output_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Output_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Output_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Output}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Output_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Output_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Output = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region OutputSource Filter Tests
    [Fact]
    public void WFiltro_OutputSourceEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            OutputSource = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.OutputSource));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.OutputSource);
    }

    [Fact]
    public void WFiltro_OutputSourceHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var OutputSourceValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            OutputSource = OutputSourceValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var OutputSourceParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.OutputSource}");
        OutputSourceParameter.Should().NotBeNull();
        OutputSourceParameter.Value.Should().Be(OutputSourceValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.OutputSource}] = @{DBProcessOutputEngineDicInfo.OutputSource}");
    }

    [Fact]
    public void WFiltro_OutputSourceWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for OutputSource = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            OutputSource = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.OutputSource}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for OutputSource = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            OutputSource = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.OutputSource}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_OutputSourceAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            OutputSource = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.OutputSource}] = @{DBProcessOutputEngineDicInfo.OutputSource}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_OutputSourceWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            OutputSource = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_OutputSourceWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            OutputSource = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDModulo Filter Tests
    [Fact]
    public void WFiltro_IDModuloEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            IDModulo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.IDModulo));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.IDModulo);
    }

    [Fact]
    public void WFiltro_IDModuloHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDModuloValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            IDModulo = IDModuloValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDModuloParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IDModulo}");
        IDModuloParameter.Should().NotBeNull();
        IDModuloParameter.Value.Should().Be(IDModuloValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.IDModulo}] = @{DBProcessOutputEngineDicInfo.IDModulo}");
    }

    [Fact]
    public void WFiltro_IDModuloWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDModulo = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            IDModulo = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IDModulo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDModulo = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            IDModulo = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IDModulo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDModuloAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            IDModulo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.IDModulo}] = @{DBProcessOutputEngineDicInfo.IDModulo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDModuloWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            IDModulo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDModuloWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            IDModulo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region MyID Filter Tests
    [Fact]
    public void WFiltro_MyIDEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            MyID = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.MyID));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.MyID);
    }

    [Fact]
    public void WFiltro_MyIDHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var MyIDValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            MyID = MyIDValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var MyIDParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.MyID}");
        MyIDParameter.Should().NotBeNull();
        MyIDParameter.Value.Should().Be(MyIDValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.MyID}] = @{DBProcessOutputEngineDicInfo.MyID}");
    }

    [Fact]
    public void WFiltro_MyIDWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for MyID = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            MyID = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.MyID}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for MyID = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            MyID = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.MyID}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_MyIDAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            MyID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.MyID}] = @{DBProcessOutputEngineDicInfo.MyID}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_MyIDWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            MyID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_MyIDWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            MyID = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#endregion
#region DateTime Tests
#endregion
#region Bool Tests
#region Administrador Filter Tests
    [Fact]
    public void WFiltro_AdministradorEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Administrador = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.Administrador));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.Administrador);
    }

    [Fact]
    public void WFiltro_AdministradorHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var AdministradorValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            Administrador = AdministradorValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var AdministradorParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.Administrador}");
        AdministradorParameter.Should().NotBeNull();
        AdministradorParameter.Value.Should().Be(AdministradorValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Administrador}] = @{DBProcessOutputEngineDicInfo.Administrador}");
    }

    [Fact]
    public void WFiltro_AdministradorWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Administrador = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            Administrador = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.Administrador}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Administrador = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            Administrador = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.Administrador}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_AdministradorAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Administrador = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.Administrador}] = @{DBProcessOutputEngineDicInfo.Administrador}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_AdministradorWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            Administrador = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_AdministradorWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            Administrador = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region DisabledItem Filter Tests
    [Fact]
    public void WFiltro_DisabledItemEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            DisabledItem = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.DisabledItem));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.DisabledItem);
    }

    [Fact]
    public void WFiltro_DisabledItemHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var DisabledItemValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            DisabledItem = DisabledItemValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var DisabledItemParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.DisabledItem}");
        DisabledItemParameter.Should().NotBeNull();
        DisabledItemParameter.Value.Should().Be(DisabledItemValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.DisabledItem}] = @{DBProcessOutputEngineDicInfo.DisabledItem}");
    }

    [Fact]
    public void WFiltro_DisabledItemWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for DisabledItem = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            DisabledItem = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.DisabledItem}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for DisabledItem = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            DisabledItem = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.DisabledItem}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_DisabledItemAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            DisabledItem = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.DisabledItem}] = @{DBProcessOutputEngineDicInfo.DisabledItem}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_DisabledItemWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            DisabledItem = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_DisabledItemWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            DisabledItem = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IsOnlyProcesso Filter Tests
    [Fact]
    public void WFiltro_IsOnlyProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            IsOnlyProcesso = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProcessOutputEngineDicInfo.IsOnlyProcesso));
        result.Value.where.Should().NotContain(DBProcessOutputEngineDicInfo.IsOnlyProcesso);
    }

    [Fact]
    public void WFiltro_IsOnlyProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IsOnlyProcessoValue = 1;
        var filtro = new FilterProcessOutputEngine
        {
            IsOnlyProcesso = IsOnlyProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IsOnlyProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IsOnlyProcesso}");
        IsOnlyProcessoParameter.Should().NotBeNull();
        IsOnlyProcessoParameter.Value.Should().Be(IsOnlyProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.IsOnlyProcesso}] = @{DBProcessOutputEngineDicInfo.IsOnlyProcesso}");
    }

    [Fact]
    public void WFiltro_IsOnlyProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IsOnlyProcesso = 0 (typically female)
        var filtroFemale = new FilterProcessOutputEngine
        {
            IsOnlyProcesso = 0
        };
        var resultFemale = _serviceS.WFiltroProcessOutputEngine(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IsOnlyProcesso}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IsOnlyProcesso = 1 (typically male)
        var filtroMale = new FilterProcessOutputEngine
        {
            IsOnlyProcesso = 1
        };
        var resultMale = _serviceS.WFiltroProcessOutputEngine(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProcessOutputEngineDicInfo.IsOnlyProcesso}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IsOnlyProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            IsOnlyProcesso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProcessOutputEngineDicInfo.PTabelaNome}].[{DBProcessOutputEngineDicInfo.IsOnlyProcesso}] = @{DBProcessOutputEngineDicInfo.IsOnlyProcesso}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IsOnlyProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            IsOnlyProcesso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IsOnlyProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProcessOutputEngine
        {
            Nome = "Nome", // Campo string para testar combinação
            IsOnlyProcesso = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProcessOutputEngine(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}