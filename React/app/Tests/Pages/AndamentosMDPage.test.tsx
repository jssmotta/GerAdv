// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for AndamentosMD page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock AndamentosMDGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/AndamentosMD/Adapter/AndamentosMDGridAdapter', () => ({
  AndamentosMDGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock AndamentosMDGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/AndamentosMD/Components/AndamentosMDGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='andamentosmd-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import AndamentosMDPage from '@/app/pages/andamentosmd/page';

describe('AndamentosMD page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and AndamentosMDGridContainer', () => {
    render(<AndamentosMDPage />);

    expect(screen.getByRole('heading', { name: /Andamentos M D/i })).toBeInTheDocument();
    expect(screen.getByTestId('andamentosmd-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

