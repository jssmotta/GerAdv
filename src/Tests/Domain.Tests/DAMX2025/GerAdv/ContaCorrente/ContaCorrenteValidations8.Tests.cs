// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class ContaCorrenteValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFContaCorrenteFactory> _mockContaCorrenteFactory;
    private readonly Mock<IContaCorrenteReader> _mockReader;
    private readonly ContaCorrenteValidation _validation;
    private readonly Mock<IContaCorrenteService> _mockContaCorrenteService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IClientesReader> _mockClientesReader;
    private readonly ContaCorrenteService _service;
    private readonly string _validUri = "test-uri";
    public ContaCorrenteValidationTests()
    {
        _mockContaCorrenteFactory = new Mock<IFContaCorrenteFactory>();
        _mockReader = new Mock<IContaCorrenteReader>();
        _validation = new ContaCorrenteValidation();
        _mockContaCorrenteService = new Mock<IContaCorrenteService>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockClientesReader = new Mock<IClientesReader>();
    }

#region Class Structure Tests
    [Fact]
    public void ContaCorrenteValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IContaCorrenteValidation>();
    }

    [Fact]
    public void ContaCorrenteValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(ContaCorrenteValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void ContaCorrenteValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(ContaCorrenteValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.ContaCorrente CreateValidContaCorrente()
    {
        return new Models.ContaCorrente
        {
            Id = 1,
            CIAcordo = 0,
            Quitado = false,
            IDContrato = 0,
            QuitadoID = 0,
            DebitoID = 0,
            LivroCaixaID = 0,
            Sucumbencia = true,
            DistRegra = false,
            DtOriginal = "24/04/1975",
            Processo = 0,
            ParcelaX = 0,
            Valor = 1m,
            Data = "27/05/2022",
            Cliente = 0,
            Historico = "",
            Contrato = true,
            Pago = false,
            Distribuir = true,
            LC = false,
            IDHTrab = 0,
            NroParcelas = 0,
            ValorPrincipal = 1m,
            ParcelaPrincipalID = 0,
            Hide = true,
            DataPgto = "24/04/1975"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockContaCorrenteService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterContaCorrente>(), It.IsAny<string>())).ReturnsAsync(new List<ContaCorrenteResponseAll>());
        // Setup other mocks but don't override the ContaCorrentes service mock
        _mockClientesReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.ClientesResponse { Id = id }));
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockContaCorrenteService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterContaCorrente>(), It.IsAny<string>())).ReturnsAsync(new List<ContaCorrenteResponseAll>());
        // Setup other mocks but don't override the ContaCorrentes service mock
        _mockClientesReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.ClientesResponse { Id = 0 }));
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var contacorrente = new Models.ContaCorrente
        {
            Id = 1,
            Quitado = false,
            Sucumbencia = true,
            DistRegra = false,
            DtOriginal = null,
            Data = "27/05/2022",
            Historico = null,
            Contrato = true,
            Pago = false,
            Distribuir = true,
            LC = false,
            DataPgto = null
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region DtOriginal Validation Tests
    [Theory]
    [InlineData("01/01/1899")]
    [InlineData("31/12/1899")]
    public async Task ValidateReg_WithDtOriginalBeforeMinDate_ShouldThrowSGValidationException(string invalidDate)
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DtOriginal = invalidDate;
        SetupValidMocks();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("01/01/1900.");
    }

    [Fact]
    public async Task ValidateReg_WithValidDtOriginal_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DtOriginal = "01/01/1990";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullDtOriginal_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DtOriginal = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithInvalidDateDtOriginalFormat_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DtOriginal = "invalid-date";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue(); // Invalid format is ignored, not validated
    }

    [Fact]
    public async Task ValidateReg_WithEmptyDtOriginal_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DtOriginal = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region DataPgto Validation Tests
    [Theory]
    [InlineData("01/01/1899")]
    [InlineData("31/12/1899")]
    public async Task ValidateReg_WithDataPgtoBeforeMinDate_ShouldThrowSGValidationException(string invalidDate)
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DataPgto = invalidDate;
        SetupValidMocks();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("01/01/1900.");
    }

    [Fact]
    public async Task ValidateReg_WithValidDataPgto_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DataPgto = "01/01/1990";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullDataPgto_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DataPgto = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithInvalidDateDataPgtoFormat_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DataPgto = "invalid-date";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue(); // Invalid format is ignored, not validated
    }

    [Fact]
    public async Task ValidateReg_WithEmptyDataPgto_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.DataPgto = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region Foreign Key Validation Tests - Clientes
    [Fact]
    public async Task ValidateReg_WithInvalidClientes_ShouldThrowSGValidationException()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.Cliente = 999;
        _mockClientesReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult<Models.Response.ClientesResponse>(null));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithMismatchedClientesId_ShouldThrowSGValidationException()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.Cliente = 999;
        var reg888 = new Models.Response.ClientesResponse
        {
            Id = 888
        }; // Different ID
        _mockClientesReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult(reg888));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithValidClientes_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.Cliente = 123;
        var reg123 = new Models.Response.ClientesResponse
        {
            Id = 123
        };
        _mockClientesReader.Setup(x => x.Read(123, _mockConnection.Object)).Returns(Task.FromResult(reg123));
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithEmptyClientes_ShouldPass()
    {
        // Arrange
        var contacorrente = CreateValidContaCorrente();
        contacorrente.Cliente = 0; // Assuming 0 is considered empty
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(contacorrente, _mockContaCorrenteService.Object, _mockClientesReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
        _mockClientesReader.Verify(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}