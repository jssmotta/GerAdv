// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Penhora
// Fonte:ServiceGenerator
using MenphisSI.GerAdv.Metrics;

namespace MenphisSI.GerAdv.Services;
using MenphisSI.GerAdv.AI.Filters;

public partial class ServiceFilter
{
    public (string where, List<SqlParameter> parametros)? WFiltroPenhora(Filters.FilterPenhora? filtro)
    {
        if (filtro == null)
            return null;
        var parameters = new List<SqlParameter>();
        if (filtro.QuemCad != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.QuemCad)}", filtro.QuemCad));
            if (filtro.QuemCad_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.QuemCad)}_end", filtro.QuemCad_end));
            }
        }

        if (!filtro.DtCad.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtCad, out var dataParam1))
                parameters.Add(new($"@{(DBPenhoraDicInfo.DtCad)}", dataParam1));
            if (!filtro.DtCad_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtCad_end, out var dataParam2))
                    parameters.Add(new($"@{(DBPenhoraDicInfo.DtCad)}_end", dataParam2));
            }
        }

        if (filtro.QuemAtu != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.QuemAtu)}", filtro.QuemAtu));
            if (filtro.QuemAtu_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.QuemAtu)}_end", filtro.QuemAtu_end));
            }
        }

        if (!filtro.DtAtu.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtAtu, out var dataParam3))
                parameters.Add(new($"@{(DBPenhoraDicInfo.DtAtu)}", dataParam3));
            if (!filtro.DtAtu_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtAtu_end, out var dataParam4))
                    parameters.Add(new($"@{(DBPenhoraDicInfo.DtAtu)}_end", dataParam4));
            }
        }

        if (filtro.Visto != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Visto)}", filtro.Visto));
        }

        if (!string.IsNullOrWhiteSpace(filtro.Guid))
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Guid)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Guid)));
        }

        if (filtro.Processo != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Processo)}", filtro.Processo));
            if (filtro.Processo_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.Processo)}_end", filtro.Processo_end));
            }
        }

        if (!string.IsNullOrWhiteSpace(filtro.Nome))
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Nome)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Nome)));
        }

        if (!string.IsNullOrWhiteSpace(filtro.Descricao))
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Descricao)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Descricao)));
        }

        if (!filtro.DataPenhora.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DataPenhora, out var dataParam5))
                parameters.Add(new($"@{(DBPenhoraDicInfo.DataPenhora)}", dataParam5));
            if (!filtro.DataPenhora_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DataPenhora_end, out var dataParam6))
                    parameters.Add(new($"@{(DBPenhoraDicInfo.DataPenhora)}_end", dataParam6));
            }
        }

        if (filtro.PenhoraStatus != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.PenhoraStatus)}", filtro.PenhoraStatus));
            if (filtro.PenhoraStatus_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.PenhoraStatus)}_end", filtro.PenhoraStatus_end));
            }
        }

        if (filtro.Master != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.Master)}", filtro.Master));
            if (filtro.Master_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.Master)}_end", filtro.Master_end));
            }
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{(DBPenhoraDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
            if (filtro.Codigo_filtro_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBPenhoraDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
            }
        }

        if (filtro.LogicalOperator.IsEmptyX() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var penhorastatus = this.WFiltroPenhoraStatus(filtro.FilterPenhoraStatus);
        if (penhorastatus?.parametros != null)
        {
            parameters.AddRange(penhorastatus?.parametros!);
        }

        var cWhere = new StringBuilder();
        cWhere.Append(penhorastatus?.where);
        if (!(filtro.QuemCad.IsEmptyX()) && filtro.QuemCad_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemCad.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.QuemCad}] = @{(DBPenhoraDicInfo.QuemCad)}");
        }
        else if (!(filtro.QuemCad.IsEmptyX()) && !(filtro.QuemCad_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.QuemCad} BETWEEN @{(DBPenhoraDicInfo.QuemCad)} AND @{(DBPenhoraDicInfo.QuemCad)}_end");
        }

        if (!(filtro.DtCad.IsEmptyDX()) && filtro.DtCad_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtCad.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.DtCad}], 103) = CONVERT(DATE, @{(DBPenhoraDicInfo.DtCad)}, 103)");
        }
        else if (!(filtro.DtCad.IsEmptyDX()) && !(filtro.DtCad_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.DtCad} BETWEEN @{(DBPenhoraDicInfo.DtCad)} AND @{(DBPenhoraDicInfo.DtCad)}_end");
        }

        if (!(filtro.QuemAtu.IsEmptyX()) && filtro.QuemAtu_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemAtu.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.QuemAtu}] = @{(DBPenhoraDicInfo.QuemAtu)}");
        }
        else if (!(filtro.QuemAtu.IsEmptyX()) && !(filtro.QuemAtu_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.QuemAtu} BETWEEN @{(DBPenhoraDicInfo.QuemAtu)} AND @{(DBPenhoraDicInfo.QuemAtu)}_end");
        }

        if (!(filtro.DtAtu.IsEmptyDX()) && filtro.DtAtu_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtAtu.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.DtAtu}], 103) = CONVERT(DATE, @{(DBPenhoraDicInfo.DtAtu)}, 103)");
        }
        else if (!(filtro.DtAtu.IsEmptyDX()) && !(filtro.DtAtu_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.DtAtu} BETWEEN @{(DBPenhoraDicInfo.DtAtu)} AND @{(DBPenhoraDicInfo.DtAtu)}_end");
        }

        cWhere.Append(filtro.Visto == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Visto}] = @{(DBPenhoraDicInfo.Visto)}");
        cWhere.Append(filtro.Guid.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Guid}]  {DevourerConsts.MsiCollate} like @{(DBPenhoraDicInfo.Guid)}");
        if (!(filtro.Processo.IsEmptyX()) && filtro.Processo_end.IsEmptyX())
        {
            cWhere.Append(filtro.Processo.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Processo}] = @{(DBPenhoraDicInfo.Processo)}");
        }
        else if (!(filtro.Processo.IsEmptyX()) && !(filtro.Processo_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.Processo} BETWEEN @{(DBPenhoraDicInfo.Processo)} AND @{(DBPenhoraDicInfo.Processo)}_end");
        }

        cWhere.Append(filtro.Nome.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Nome}]  {DevourerConsts.MsiCollate} like @{(DBPenhoraDicInfo.Nome)}");
        cWhere.Append(filtro.Descricao.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Descricao}]  {DevourerConsts.MsiCollate} like @{(DBPenhoraDicInfo.Descricao)}");
        if (!(filtro.DataPenhora.IsEmptyDX()) && filtro.DataPenhora_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DataPenhora.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.DataPenhora}], 103) = CONVERT(DATE, @{(DBPenhoraDicInfo.DataPenhora)}, 103)");
        }
        else if (!(filtro.DataPenhora.IsEmptyDX()) && !(filtro.DataPenhora_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.DataPenhora} BETWEEN @{(DBPenhoraDicInfo.DataPenhora)} AND @{(DBPenhoraDicInfo.DataPenhora)}_end");
        }

        if (!(filtro.PenhoraStatus.IsEmptyX()) && filtro.PenhoraStatus_end.IsEmptyX())
        {
            cWhere.Append(filtro.PenhoraStatus.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.PenhoraStatus}] = @{(DBPenhoraDicInfo.PenhoraStatus)}");
        }
        else if (!(filtro.PenhoraStatus.IsEmptyX()) && !(filtro.PenhoraStatus_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.PenhoraStatus} BETWEEN @{(DBPenhoraDicInfo.PenhoraStatus)} AND @{(DBPenhoraDicInfo.PenhoraStatus)}_end");
        }

        if (!(filtro.Master.IsEmptyX()) && filtro.Master_end.IsEmptyX())
        {
            cWhere.Append(filtro.Master.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.Master}] = @{(DBPenhoraDicInfo.Master)}");
        }
        else if (!(filtro.Master.IsEmptyX()) && !(filtro.Master_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.Master} BETWEEN @{(DBPenhoraDicInfo.Master)} AND @{(DBPenhoraDicInfo.Master)}_end");
        }

        if (!(filtro.Codigo_filtro.IsEmptyX()) && filtro.Codigo_filtro_end.IsEmptyX())
        {
            cWhere.Append(filtro.Codigo_filtro.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].[{DBPenhoraDicInfo.CampoCodigo}] = @{(DBPenhoraDicInfo.CampoCodigo)}");
        }
        else if (!(filtro.Codigo_filtro.IsEmptyX()) && !(filtro.Codigo_filtro_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBPenhoraDicInfo.PTabelaNome}].{DBPenhoraDicInfo.CampoCodigo} BETWEEN @{(DBPenhoraDicInfo.CampoCodigo)} AND @{(DBPenhoraDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }
}

public partial class PenhoraService
{
    /// <summary>
    /// Processa comando de voz para gerar filtro via OpenAI usando serviço genérico
    /// </summary>
    /// <param name = "message">Requisição com comando de voz</param>
    /// <param name = "existingFilter">Filtro existente para combinar</param>
    /// <param name = "uri">URI da base de dados</param>
    /// <returns>FilterPenhora processado</returns>
    private async Task<Filters.FilterPenhora?> ProcessFilterVoiceCommandAsync(CommandSpeakerRequest message, Filters.FilterPenhora? existingFilter, string uri)
    {
        try
        {
            // Usar o contexto do FilterPenhoraTXT
            var context = FilterPenhoraTXT.GetPrompt(existingFilter, FilterPenhoraTXT.GetOptions());
            // Instruções específicas para Penhora
            var additionalInstructions = "";
            // Processar com o serviço genérico
            var filtroProcessado = await _voiceFilterService.ProcessVoiceCommandWithFallbackAsync(message.Message, context, existingFilter, additionalInstructions, uri);
            return filtroProcessado;
        }
        catch (Exception)
        {
            // Se falhar, retornar filtro existente ou novo
            return existingFilter ?? new Filters.FilterPenhora();
        }
    }

    private async Task<IEnumerable<PenhoraResponseAll>> GetDataAllAsync(MsiSqlConnection oCnn, int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken cancellationToken)
    {
        if (oCnn == null || oCnn?.InnerConnection?.State == ConnectionState.Closed)
        {
            return[];
        }

        try
        {
            // Usar o reader para obter os dados
            return await reader.ListarAsync(oCnn!, max, uri, where.Replace(TSql.Where, ""), parameters, "", cancellationToken);
        }
        catch (Exception)
        {
            return[];
        }
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterPenhora? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-0
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("Penhora: URI inválida");
        }

        var filtroResult = filtro == null ? null : servicesFilter.WFiltroPenhora(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter>? parameters = filtroResult?.parametros ?? [];
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new Exception($"Coneão nula.");
        }

        var keyCache = await reader.ReadStringAuditorAsync(uri, oCnn, _cache);
        var cacheKey = $"{uri}-Penhora-{max}-{where.GetHashCode()}-GetListN-{keyCache}";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter>? parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarNAsync(max, uri, where, parameters, DBPenhoraDicInfo.CampoNome);
        if (lista == null || !lista.Any())
        {
            return result;
        }

        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                continue;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    public async Task<IEnumerable<PenhoraResponseAll>> GetAll(int max, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        ThrowIfDisposed();
        max = Math.Min(Math.Max(max, 1), BaseConsts.PMaxItens);
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("Penhora: URI inválida");
        }

        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache),
            LocalCacheExpiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache)
        };
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        var keyCache = await reader.ReadStringAuditorAsync(uri, oCnn, _cache);
        var cacheKey = $"{uri}-Penhora-Filter-{max}-{keyCache}";
        var result = await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataAllAsync2(oCnn!, max, string.Empty, [], uri, cancel), entryOptions, cancellationToken: token);
        return result;
    }

    private async Task<IEnumerable<PenhoraResponseAll>> GetDataAllAsync2(MsiSqlConnection oCnn, int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken token)
    {
        if (oCnn == null || oCnn?.InnerConnection?.State == ConnectionState.Closed)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.ListarAsync(oCnn!, max, uri, where, parameters, string.Empty, token);
        return result;
    }
}