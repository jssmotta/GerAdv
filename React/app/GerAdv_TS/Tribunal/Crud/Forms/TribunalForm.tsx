// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { ITribunal } from "@/app/GerAdv_TS/Tribunal/Interfaces/interface.Tribunal";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { TribunalApi } from "../../Apis/TribunalApi";
import { useValidationsTribunal } from "../../Hooks/hookTribunal";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpTribunal from "../../Help/HelpTribunal";
import TribunalPage from "@/app/pages/tribunal/page";

import AreaComboBox from "@/app/GerAdv_TS/Area/ComboBox/AreaComboBox";
import JusticaComboBox from "@/app/GerAdv_TS/Justica/ComboBox/JusticaComboBox";
import InstanciaComboBox from "@/app/GerAdv_TS/Instancia/ComboBox/InstanciaComboBox";
import { AreaApi } from "@/app/GerAdv_TS/Area/Apis/AreaApi";
import { JusticaApi } from "@/app/GerAdv_TS/Justica/Apis/JusticaApi";
import { InstanciaApi } from "@/app/GerAdv_TS/Instancia/Apis/InstanciaApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";

interface TribunalFormProps {
  tribunalData: ITribunal;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const TribunalForm: React.FC<TribunalFormProps> = ({
  tribunalData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new TribunalApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsTribunal();

  const [_nomeArea, setNomeArea] = useState("");
  const areaApi = new AreaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeJustica, setNomeJustica] = useState("");
  const justicaApi = new JusticaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeInstancia, setNomeInstancia] = useState("");
  const instanciaApi = new InstanciaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("area") > 0) {
    if (tribunalData.id === 0 && tribunalData.area == 0) {
      areaApi
        .getById(getParamFromUrl("area"))
        .then((response) => {
          setNomeArea(response.data.descricao);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      tribunalData.area = getParamFromUrl("area");
    }
  }

  if (getParamFromUrl("justica") > 0) {
    if (tribunalData.id === 0 && tribunalData.justica == 0) {
      justicaApi
        .getById(getParamFromUrl("justica"))
        .then((response) => {
          setNomeJustica(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      tribunalData.justica = getParamFromUrl("justica");
    }
  }

  if (getParamFromUrl("instancia") > 0) {
    if (tribunalData.id === 0 && tribunalData.instancia == 0) {
      instanciaApi
        .getById(getParamFromUrl("instancia"))
        .then((response) => {
          setNomeInstancia(response.data.nroprocesso);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      tribunalData.instancia = getParamFromUrl("instancia");
    }
  }
  const addValorArea = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "area", value: e.id } });
  };
  const addValorJustica = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "justica", value: e.id } });
  };
  const addValorInstancia = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "instancia", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Tribunal:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`TribunalForm-${tribunalData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Tribunal diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        tribunalData?.id == 0 ? "Editar Tribunal" : "Adicionar Tribunal";
    }
  }, [tribunalData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Tribunal"
            : "form-container5 form-container-Tribunal"
        }
      >
        <form
          className="formInputCadInc"
          id={`TribunalForm-${tribunalData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Tribunal"
            data={tribunalData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`TribunalForm-${tribunalData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={tribunalData}
              className="input-default-main"
              name="nome"
              value={tribunalData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <AreaComboBox
              name={"area"}
              dataForm={tribunalData}
              value={tribunalData.area}
              setValue={addValorArea}
              label={"Área"}
            />

            <JusticaComboBox
              name={"justica"}
              dataForm={tribunalData}
              value={tribunalData.justica}
              setValue={addValorJustica}
              label={"Justiça"}
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={50}
              id="descricao"
              label="Descricao"
              dataForm={tribunalData}
              className="input-default-main"
              name="descricao"
              value={tribunalData.descricao}
              onChange={onChange}
            />

            <InstanciaComboBox
              name={"instancia"}
              dataForm={tribunalData}
              value={tribunalData.instancia}
              setValue={addValorInstancia}
              label={"Instancia"}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={20}
              id="sigla"
              label="Sigla"
              dataForm={tribunalData}
              className="input-default-main"
              name="sigla"
              value={tribunalData.sigla}
              onChange={onChange}
            />

            <InputInput
              tabIndex={3}
              type="text"
              maxLength={255}
              id="web"
              label="Web"
              dataForm={tribunalData}
              className="input-default-main"
              name="web"
              value={tribunalData.web}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Tribunal" windowCrud={TribunalPage} />

          <CopyButton formId={`TribunalForm-${tribunalData.id}`} />

          <PrintButton formId={`TribunalForm-${tribunalData.id}`} />

          <AuditorButton
            tableName="Tribunal"
            coolName="Tribunal"
            id={tribunalData.id}
          />

          <DeleteButton
            page={"/pages/tribunal"}
            id={tribunalData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Tribunal"
            page={(onClose) => <HelpTribunal onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Tribunal"
          data={tribunalData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`TribunalForm-${tribunalData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
