// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { DivisaoTribunalService, DivisaoTribunalValidator } from '@/app/GerAdv_TS/DivisaoTribunal/Services/DivisaoTribunal.service';
import { DivisaoTribunalApi, DivisaoTribunalApiError } from '@/app/GerAdv_TS/DivisaoTribunal/Apis/ApiDivisaoTribunal';
import { IDivisaoTribunal } from '@/app/GerAdv_TS/DivisaoTribunal/Interfaces/interface.DivisaoTribunal';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as DivisaoTribunalApi;

const sample: IDivisaoTribunal = { id: 1, nome: 'Teste' };

describe('DivisaoTribunalService', () => {
  let service: DivisaoTribunalService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new DivisaoTribunalService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(DivisaoTribunalValidator, 'validateDivisaoTribunal').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchDivisaoTribunalById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchDivisaoTribunalById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchDivisaoTribunalById lança DivisaoTribunalApiError para id inválido', async () => {
    await expect(service.fetchDivisaoTribunalById(0)).rejects.toBeInstanceOf(DivisaoTribunalApiError);
  });

  it('saveDivisaoTribunal chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveDivisaoTribunal(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveDivisaoTribunal lança DivisaoTribunalApiError quando validação falha', async () => {
    (DivisaoTribunalValidator.validateDivisaoTribunal as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveDivisaoTribunal(sample)).rejects.toBeInstanceOf(DivisaoTribunalApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteDivisaoTribunal chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteDivisaoTribunal(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteDivisaoTribunal lança DivisaoTribunalApiError para id inválido', async () => {
    await expect(service.deleteDivisaoTribunal(0)).rejects.toBeInstanceOf(DivisaoTribunalApiError);
  });

  it('validateDivisaoTribunal delega para DivisaoTribunalValidator', () => {
    (DivisaoTribunalValidator.validateDivisaoTribunal as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateDivisaoTribunal(sample);
    expect(r.isValid).toBe(true);
  });
});
