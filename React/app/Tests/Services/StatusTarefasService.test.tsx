// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { StatusTarefasService, StatusTarefasValidator } from '@/app/GerAdv_TS/StatusTarefas/Services/StatusTarefas.service';
import { StatusTarefasApi, StatusTarefasApiError } from '@/app/GerAdv_TS/StatusTarefas/Apis/ApiStatusTarefas';
import { IStatusTarefas } from '@/app/GerAdv_TS/StatusTarefas/Interfaces/interface.StatusTarefas';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as StatusTarefasApi;

const sample: IStatusTarefas = { id: 1, nome: 'Teste' };

describe('StatusTarefasService', () => {
  let service: StatusTarefasService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new StatusTarefasService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(StatusTarefasValidator, 'validateStatusTarefas').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchStatusTarefasById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchStatusTarefasById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchStatusTarefasById lança StatusTarefasApiError para id inválido', async () => {
    await expect(service.fetchStatusTarefasById(0)).rejects.toBeInstanceOf(StatusTarefasApiError);
  });

  it('saveStatusTarefas chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveStatusTarefas(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveStatusTarefas lança StatusTarefasApiError quando validação falha', async () => {
    (StatusTarefasValidator.validateStatusTarefas as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveStatusTarefas(sample)).rejects.toBeInstanceOf(StatusTarefasApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteStatusTarefas chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteStatusTarefas(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteStatusTarefas lança StatusTarefasApiError para id inválido', async () => {
    await expect(service.deleteStatusTarefas(0)).rejects.toBeInstanceOf(StatusTarefasApiError);
  });

  it('validateStatusTarefas delega para StatusTarefasValidator', () => {
    (StatusTarefasValidator.validateStatusTarefas as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateStatusTarefas(sample);
    expect(r.isValid).toBe(true);
  });
});
