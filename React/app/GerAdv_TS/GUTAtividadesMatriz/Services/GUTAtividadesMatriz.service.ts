// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
"use client";
import { CRUD_CONSTANTS } from "@/app/tools/crud";
import {
  GUTAtividadesMatrizApi,
  GUTAtividadesMatrizApiError,
} from "../Apis/ApiGUTAtividadesMatriz";
import { FilterGUTAtividadesMatriz } from "../Filters/GUTAtividadesMatriz";
import { IGUTAtividadesMatriz } from "../Interfaces/interface.GUTAtividadesMatriz";
import { GUTAtividadesMatrizEmpty } from "../../Models/GUTAtividadesMatriz";
import { ICommandSpeakerRequest } from "@/app/models/ICommandSpeakerRequest";
import { AxiosResponse } from "axios";

export class GUTAtividadesMatrizValidator {
  static validateGUTAtividadesMatriz(
    gutatividadesmatriz: IGUTAtividadesMatriz,
  ): { isValid: boolean; errors: string[] } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes

    return {
      isValid: errors.length === 0,
      errors,
    };
  }
}

export interface IGUTAtividadesMatrizService {
  fetchGUTAtividadesMatrizById: (id: number) => Promise<IGUTAtividadesMatriz>;
  saveGUTAtividadesMatriz: (
    gutatividadesmatriz: IGUTAtividadesMatriz,
  ) => Promise<IGUTAtividadesMatriz>;

  getAll: (
    filtro?: FilterGUTAtividadesMatriz,
  ) => Promise<IGUTAtividadesMatriz[]>;
  deleteGUTAtividadesMatriz: (id: number) => Promise<void>;
  validateGUTAtividadesMatriz: (gutatividadesmatriz: IGUTAtividadesMatriz) => {
    isValid: boolean;
    errors: string[];
  };
  filterVoice: (
    filtro?: FilterGUTAtividadesMatriz,
    voiceCommand?: ICommandSpeakerRequest,
  ) => Promise<AxiosResponse>;
}

export class GUTAtividadesMatrizService implements IGUTAtividadesMatrizService {
  constructor(private api: GUTAtividadesMatrizApi) {}

  async fetchGUTAtividadesMatrizById(
    id: number,
  ): Promise<IGUTAtividadesMatriz> {
    if (id <= 0) {
      throw new GUTAtividadesMatrizApiError("ID inválido", 400, "INVALID_ID");
    }

    try {
      const response = await this.api.getById(id);
      return response.data;
    } catch (error) {
      if (error instanceof GUTAtividadesMatrizApiError) {
        throw error;
      }
      throw new GUTAtividadesMatrizApiError(
        "Erro ao buscar gutatividadesmatriz",
        500,
        "FETCH_ERROR",
        error,
      );
    }
  }

  async saveGUTAtividadesMatriz(
    gutatividadesmatriz: IGUTAtividadesMatriz,
  ): Promise<IGUTAtividadesMatriz> {
    const validation = this.validateGUTAtividadesMatriz(gutatividadesmatriz);
    if (!validation.isValid) {
      throw new GUTAtividadesMatrizApiError(
        `Dados inválidos: ${validation.errors.join(", ")}`,
        400,
        "VALIDATION_ERROR",
      );
    }

    try {
      const response = await this.api.addAndUpdate(gutatividadesmatriz);
      return response.data;
    } catch (error) {
      if (error instanceof GUTAtividadesMatrizApiError) {
        throw error;
      }
      throw new GUTAtividadesMatrizApiError(
        "Erro ao salvar gutatividadesmatriz",
        500,
        "SAVE_ERROR",
        error,
      );
    }
  }

  async getAll(
    filtro?: FilterGUTAtividadesMatriz,
    onOnlineData?: (data: IGUTAtividadesMatriz[]) => void,
  ): Promise<IGUTAtividadesMatriz[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(
        0,
        (filtro ?? {}) as any,
      );
      // Normaliza offline
      const offlineRaw = preloadResponse?.data;
      const offlineData = Array.isArray(offlineRaw)
        ? offlineRaw
        : (offlineRaw?.data ?? offlineRaw?.items ?? offlineRaw?.rows ?? []);

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api
          .filter(0, (filtro ?? {}) as any)
          .then((response) => {
            const raw = response?.data;
            const onlineData = Array.isArray(raw)
              ? raw
              : (raw?.data ?? raw?.items ?? raw?.rows ?? []);
            if (onlineData) {
              onOnlineData(onlineData);
            }
          })
          .catch((error) => {
            if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
              console.log("Error fetching online GUTAtividadesMatriz");
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(
            0,
            (filtro ?? {}) as any,
          );
          const raw = onlineResponse?.data;
          const onlineData = Array.isArray(raw)
            ? raw
            : (raw?.data ?? raw?.items ?? raw?.rows ?? []);
          return onlineData || offlineData;
        } catch (error) {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error fetching online GUTAtividadesMatriz");
          return offlineData;
        }
      }
    } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
        console.log("Error fetching all GUTAtividadesMatriz:");
      return [];
    }
  }

  async deleteGUTAtividadesMatriz(id: number): Promise<void> {
    if (id <= 0) {
      throw new GUTAtividadesMatrizApiError(
        "ID inválido para exclusão",
        400,
        "INVALID_ID",
      );
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof GUTAtividadesMatrizApiError) {
        throw error;
      }
      throw new GUTAtividadesMatrizApiError(
        "Erro ao excluir gutatividadesmatriz",
        500,
        "DELETE_ERROR",
        error,
      );
    }
  }

  validateGUTAtividadesMatriz(gutatividadesmatriz: IGUTAtividadesMatriz): {
    isValid: boolean;
    errors: string[];
  } {
    return GUTAtividadesMatrizValidator.validateGUTAtividadesMatriz(
      gutatividadesmatriz,
    );
  }

  async filterVoice(
    filtro?: FilterGUTAtividadesMatriz,
    voiceCommand?: ICommandSpeakerRequest,
  ): Promise<AxiosResponse> {
    try {
      const response = await this.api.filterVoice(filtro, voiceCommand);
      return response;
    } catch (error) {
      if (error instanceof GUTAtividadesMatrizApiError) {
        throw error;
      }
      throw new GUTAtividadesMatrizApiError(
        "Erro ao processar filtro de voz",
        500,
        "VOICE_FILTER_ERROR",
        error,
      );
    }
  }
}
