// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterUltimosProcessos } from '@/app/GerAdv_TS/UltimosProcessos/Filters/UltimosProcessos';
interface UseUltimosProcessosFilterProps {
  handleFetchWithFilter: (filtro?: FilterUltimosProcessos | undefined | null) => Promise<void>;
}
interface UseUltimosProcessosFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterUltimosProcessos;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterUltimosProcessos>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterUltimosProcessos) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterUltimosProcessos>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useUltimosProcessosFilter = ({ handleFetchWithFilter }: UseUltimosProcessosFilterProps): UseUltimosProcessosFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterUltimosProcessos>({} as FilterUltimosProcessos);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterUltimosProcessos;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterUltimosProcessos) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('UltimosProcessosFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterUltimosProcessos>) => (
  <>
  <InputInput
  type='text'
  id='processo'
  name='processo'
  value={handlers.windowFilter?.processo ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Processo'
  label='Processo (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='processo_end'
  name='processo_end'
  value={handlers.windowFilter?.processo_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Processo final'
  label='Processo final'
  disabled={handlers.windowFilter?.processo ? false: true}
  className='inputIncNome inputSearch'
  />
  <InputDate
  type='text'
  id='quando'
  label='Quando (igual ou início)'
  dataForm={null}
  className='inputSearch'
  name='quando'
  value={handlers.windowFilter?.quando ?? ''}
  onChange={(value: string) => handlers.handleDateChange('quando', value)}
  />
  <InputDate
  type='text'
  id='quando_end'
  label='Quando (final)'
  dataForm={null}
  className='inputSearch'
  name='quando_end'
  value={handlers.windowFilter?.quando_end ?? ''}
  disabled={handlers.windowFilter?.quando ? false: true}
  onChange={(value: string) => handlers.handleDateChange('quando_end', value)}
  />
  <InputInput
  type='text'
  id='quem'
  name='quem'
  value={handlers.windowFilter?.quem ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Quem'
  label='Quem (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='quem_end'
  name='quem_end'
  value={handlers.windowFilter?.quem_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Quem final'
  label='Quem final'
  disabled={handlers.windowFilter?.quem ? false: true}
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};