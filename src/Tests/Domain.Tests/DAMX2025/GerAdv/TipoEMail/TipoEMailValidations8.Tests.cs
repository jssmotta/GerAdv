// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoEMailValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFTipoEMailFactory> _mockTipoEMailFactory;
    private readonly Mock<ITipoEMailReader> _mockReader;
    private readonly TipoEMailValidation _validation;
    private readonly Mock<ITipoEMailService> _mockTipoEMailService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoEMailService _service;
    private readonly string _validUri = "test-uri";
    public TipoEMailValidationTests()
    {
        _mockTipoEMailFactory = new Mock<IFTipoEMailFactory>();
        _mockReader = new Mock<ITipoEMailReader>();
        _validation = new TipoEMailValidation();
        _mockTipoEMailService = new Mock<ITipoEMailService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoEMailValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoEMailValidation>();
    }

    [Fact]
    public void TipoEMailValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoEMailValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoEMailValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoEMailValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.TipoEMail CreateValidTipoEMail()
    {
        return new Models.TipoEMail
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoEMailService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoEMail>(), It.IsAny<string>())).ReturnsAsync(new List<TipoEMailResponseAll>());
    // Setup other mocks but don't override the TipoEMails service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoEMailService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoEMail>(), It.IsAny<string>())).ReturnsAsync(new List<TipoEMailResponseAll>());
    // Setup other mocks but don't override the TipoEMails service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipoemail = new Models.TipoEMail
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipoemail, _mockTipoEMailService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoEMailService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoemail = CreateValidTipoEMail();
        tipoemail.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoemail, _mockTipoEMailService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithNullNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoemail = CreateValidTipoEMail();
        tipoemail.Nome = null;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoemail, _mockTipoEMailService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidDataNome_ShouldReturnTrue()
    {
        // Arrange
        var tipoemail = CreateValidTipoEMail();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipoemail, _mockTipoEMailService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespaceNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipoemail = CreateValidTipoEMail();
        tipoemail.Nome = "   ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipoemail, _mockTipoEMailService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}