// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProcessOutPutIDsService, ProcessOutPutIDsValidator } from '@/app/GerAdv_TS/ProcessOutPutIDs/Services/ProcessOutPutIDs.service';
import { ProcessOutPutIDsApi, ProcessOutPutIDsApiError } from '@/app/GerAdv_TS/ProcessOutPutIDs/Apis/ApiProcessOutPutIDs';
import { IProcessOutPutIDs } from '@/app/GerAdv_TS/ProcessOutPutIDs/Interfaces/interface.ProcessOutPutIDs';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProcessOutPutIDsApi;

const sample: IProcessOutPutIDs = { id: 1, nome: 'Teste' };

describe('ProcessOutPutIDsService', () => {
  let service: ProcessOutPutIDsService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProcessOutPutIDsService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProcessOutPutIDsValidator, 'validateProcessOutPutIDs').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProcessOutPutIDsById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProcessOutPutIDsById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProcessOutPutIDsById lança ProcessOutPutIDsApiError para id inválido', async () => {
    await expect(service.fetchProcessOutPutIDsById(0)).rejects.toBeInstanceOf(ProcessOutPutIDsApiError);
  });

  it('saveProcessOutPutIDs chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProcessOutPutIDs(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProcessOutPutIDs lança ProcessOutPutIDsApiError quando validação falha', async () => {
    (ProcessOutPutIDsValidator.validateProcessOutPutIDs as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProcessOutPutIDs(sample)).rejects.toBeInstanceOf(ProcessOutPutIDsApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProcessOutPutIDs chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProcessOutPutIDs(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProcessOutPutIDs lança ProcessOutPutIDsApiError para id inválido', async () => {
    await expect(service.deleteProcessOutPutIDs(0)).rejects.toBeInstanceOf(ProcessOutPutIDsApiError);
  });

  it('validateProcessOutPutIDs delega para ProcessOutPutIDsValidator', () => {
    (ProcessOutPutIDsValidator.validateProcessOutPutIDs as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProcessOutPutIDs(sample);
    expect(r.isValid).toBe(true);
  });
});
