// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IContatoCRMViewService } from '../Services/ContatoCRMView.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IContatoCRMView } from '../Interfaces/interface.ContatoCRMView';
import { FilterContatoCRMView } from '../Filters/ContatoCRMView';
import { isValidDate } from '@/app/tools/datetime';
import { ContatoCRMViewApi } from '../Apis/ApiContatoCRMView';

export const useContatoCRMViewForm = (
  initialContatoCRMView: IContatoCRMView,
  dataService: IContatoCRMViewService
) => {
  const [data, setData] = useState<IContatoCRMView>(initialContatoCRMView);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadContatoCRMView = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialContatoCRMView);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchContatoCRMViewById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Contato C R M View';
      setError(errorMessage);
      //console.log('Erro ao carregar Contato C R M View');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialContatoCRMView]);

  const resetForm = useCallback(() => {
    setData(initialContatoCRMView);
    setError(null);
  }, [initialContatoCRMView]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadContatoCRMView,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadContatoCRMView, resetForm]);
  return returnValue;
};


export const useContatoCRMViewNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('ContatoCRMView', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useContatoCRMViewList = (dataService: IContatoCRMViewService, currentFilter?: FilterContatoCRMView | undefined | null) => {
  const [data, setData] = useState<IContatoCRMView[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterContatoCRMView | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterContatoCRMView | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterContatoCRMView);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar contatocrmview';
      setError(errorMessage);
      //console.log('Erro ao carregar contatocrmview');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useContatoCRMViewNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsContatoCRMView() {

  async function runValidation(data: IContatoCRMView, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const contatocrmviewApi = new ContatoCRMViewApi(uri ?? '', token ?? '');

    const result = await contatocrmviewApi.validation(data);

    return result;
  }

  function validate(data: IContatoCRMView): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.data.length <= 0) { 
                                             return { isValid: false, message: 'O campo Data não pode ficar vazio.' };
                                         } 
if (data.cguid.length > 100) { 
                                             return { isValid: false, message: 'O campo CGUID não pode ter mais de 100 caracteres.' };
                                         } 
if (data.ip.length > 50) { 
                                             return { isValid: false, message: 'O campo IP não pode ter mais de 50 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}