// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OperadorService, OperadorValidator } from '@/app/GerAdv_TS/Operador/Services/Operador.service';
import { OperadorApi, OperadorApiError } from '@/app/GerAdv_TS/Operador/Apis/ApiOperador';
import { IOperador } from '@/app/GerAdv_TS/Operador/Interfaces/interface.Operador';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OperadorApi;

const sample: IOperador = { id: 1, nome: 'Teste' };

describe('OperadorService', () => {
  let service: OperadorService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OperadorService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OperadorValidator, 'validateOperador').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOperadorById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOperadorById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOperadorById lança OperadorApiError para id inválido', async () => {
    await expect(service.fetchOperadorById(0)).rejects.toBeInstanceOf(OperadorApiError);
  });

  it('saveOperador chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOperador(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOperador lança OperadorApiError quando validação falha', async () => {
    (OperadorValidator.validateOperador as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOperador(sample)).rejects.toBeInstanceOf(OperadorApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOperador chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOperador(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOperador lança OperadorApiError para id inválido', async () => {
    await expect(service.deleteOperador(0)).rejects.toBeInstanceOf(OperadorApiError);
  });

  it('validateOperador delega para OperadorValidator', () => {
    (OperadorValidator.validateOperador as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOperador(sample);
    expect(r.isValid).toBe(true);
  });
});
