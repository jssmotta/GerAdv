// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class JusticaWriterTests
{
    private readonly Mock<IFJusticaFactory> _mockJusticaFactory;
    private readonly Mock<FJustica> _mockFJustica;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly JusticaWriter _justicaWriter;
    public JusticaWriterTests()
    {
        _mockJusticaFactory = new Mock<IFJusticaFactory>();
        _mockFJustica = new Mock<FJustica>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _justicaWriter = new JusticaWriter(_mockJusticaFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var justica = CreateValidJusticaModel();
        justica.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockJusticaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFJustica.Object);
        _mockFJustica.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _justicaWriter.WriteAsync(justica, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFJustica.Object);
        _mockJusticaFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockJusticaFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFJustica.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var justica = CreateValidJusticaModel();
        justica.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockJusticaFactory.Setup(x => x.CreateFromIdAsync(justica.Id, _mockConnection.Object)).ReturnsAsync(_mockFJustica.Object);
        _mockFJustica.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _justicaWriter.WriteAsync(justica, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFJustica.Object);
        _mockJusticaFactory.Verify(x => x.CreateFromIdAsync(justica.Id, _mockConnection.Object), Times.Once);
        _mockJusticaFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFJustica.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var justica = CreateValidJusticaModel();
        var auditorQuem = 99;
        _mockJusticaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFJustica.Object);
        _mockFJustica.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _justicaWriter.WriteAsync(justica, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFJustica.Object);
        _mockFJustica.VerifySet(x => x.FGUID = justica.GUID, Times.Once);
        _mockFJustica.VerifySet(x => x.FNome = justica.Nome, Times.Once);
        _mockFJustica.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var justica = CreateValidJusticaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockJusticaFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _justicaWriter.WriteAsync(justica, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var justica = CreateValidJusticaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockJusticaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFJustica.Object);
        _mockFJustica.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _justicaWriter.WriteAsync(justica, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var justicaResponse = new JusticaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockJusticaFactory.Setup(x => x.DeleteAsync(operadorId, justicaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _justicaWriter.Delete(justicaResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockJusticaFactory.Verify(x => x.DeleteAsync(operadorId, justicaResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var justicaResponse = new JusticaResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockJusticaFactory.Setup(x => x.DeleteAsync(operadorId, justicaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _justicaWriter.Delete(justicaResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var justicaResponse = new JusticaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockJusticaFactory.Setup(x => x.DeleteAsync(operadorId, justicaResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _justicaWriter.Delete(justicaResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new JusticaWriter(_mockJusticaFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new JusticaWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Justica CreateValidJusticaModel()
    {
        return new Models.Justica
        {
            Id = 0,
            GUID = Guid.NewGuid().ToString(),
            Nome = "João"
        };
    }
#endregion
}