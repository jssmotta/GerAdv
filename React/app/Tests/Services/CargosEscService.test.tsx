// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { CargosEscService, CargosEscValidator } from '@/app/GerAdv_TS/CargosEsc/Services/CargosEsc.service';
import { CargosEscApi, CargosEscApiError } from '@/app/GerAdv_TS/CargosEsc/Apis/ApiCargosEsc';
import { ICargosEsc } from '@/app/GerAdv_TS/CargosEsc/Interfaces/interface.CargosEsc';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as CargosEscApi;

const sample: ICargosEsc = { id: 1, nome: 'Teste' };

describe('CargosEscService', () => {
  let service: CargosEscService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new CargosEscService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(CargosEscValidator, 'validateCargosEsc').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchCargosEscById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchCargosEscById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchCargosEscById lança CargosEscApiError para id inválido', async () => {
    await expect(service.fetchCargosEscById(0)).rejects.toBeInstanceOf(CargosEscApiError);
  });

  it('saveCargosEsc chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveCargosEsc(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveCargosEsc lança CargosEscApiError quando validação falha', async () => {
    (CargosEscValidator.validateCargosEsc as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveCargosEsc(sample)).rejects.toBeInstanceOf(CargosEscApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteCargosEsc chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteCargosEsc(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteCargosEsc lança CargosEscApiError para id inválido', async () => {
    await expect(service.deleteCargosEsc(0)).rejects.toBeInstanceOf(CargosEscApiError);
  });

  it('validateCargosEsc delega para CargosEscValidator', () => {
    (CargosEscValidator.validateCargosEsc as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateCargosEsc(sample);
    expect(r.isValid).toBe(true);
  });
});
