// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useStatusInstanciaForm, useStatusInstanciaList, useValidationsStatusInstancia } from '../GerAdv_TS/StatusInstancia/Hooks/hookStatusInstancia';
import { IStatusInstancia } from '../GerAdv_TS/StatusInstancia/Interfaces/interface.StatusInstancia';
import { IStatusInstanciaService } from '../GerAdv_TS/StatusInstancia/Services/StatusInstancia.service';
import { StatusInstanciaTestEmpty } from '../GerAdv_TS/Models/StatusInstancia';
import { useStatusInstanciaComboBox } from '../GerAdv_TS/StatusInstancia/Hooks/hookStatusInstancia';

// Mock do serviço
const mockStatusInstanciaService: jest.Mocked<IStatusInstanciaService> = {
  fetchStatusInstanciaById: jest.fn(),
  saveStatusInstancia: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteStatusInstancia: jest.fn(),
  validateStatusInstancia: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialStatusInstancia: IStatusInstancia = { ...StatusInstanciaTestEmpty() };

describe('useStatusInstanciaForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    expect(result.current.data).toEqual(initialStatusInstancia);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Status Instancia',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Status Instancia');
  });

   test('deve carregar Status Instancia por ID', async () => {
    const mockStatusInstancia = { ...initialStatusInstancia, id: 1, nome: 'Status Instancia Teste' };
    mockStatusInstanciaService.fetchStatusInstanciaById.mockResolvedValue(mockStatusInstancia);

    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.loadStatusInstancia(1);
    });

    expect(mockStatusInstanciaService.fetchStatusInstanciaById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockStatusInstancia);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Status Instancia', async () => {
    const errorMessage = 'Erro ao carregar Status Instancia';
    mockStatusInstanciaService.fetchStatusInstanciaById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.loadStatusInstancia(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialStatusInstancia, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialStatusInstancia);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useStatusInstanciaForm(initialStatusInstancia, mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.loadStatusInstancia(0);
    });

    expect(mockStatusInstanciaService.fetchStatusInstanciaById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialStatusInstancia);
  });
});

describe('useStatusInstanciaList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaList(mockStatusInstanciaService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialStatusInstancia, id: 1, nome: 'Status Instancia 1' },
      { ...initialStatusInstancia, id: 2, nome: 'Status Instancia 2' }
    ];
    mockStatusInstanciaService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusInstanciaList(mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockStatusInstanciaService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockStatusInstanciaService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusInstanciaList(mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialStatusInstancia, id: 1, nome: 'Status Instancia Filtrado' }];
    const filtro = { nome: 'Status Instancia' };
    mockStatusInstanciaService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusInstanciaList(mockStatusInstanciaService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockStatusInstanciaService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsStatusInstancia', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsStatusInstancia());

    const validData = { ...initialStatusInstancia, nome: 'Status Instancia Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsStatusInstancia());

    const invalidData = { ...initialStatusInstancia, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsStatusInstancia());

    const invalidData = { 
      ...initialStatusInstancia, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsStatusInstancia());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialStatusInstancia, id: 1, nome: 'Status Instancia Teste' }];
    mockStatusInstanciaService.getAll.mockResolvedValue(mockData);
    mockStatusInstanciaService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useStatusInstanciaList(mockStatusInstanciaService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsStatusInstancia()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status Instancia Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status Instancia Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Instancia 1' },
      { id: 2, nome: 'Status Instancia 2' }
    ];
    mockStatusInstanciaService.getList.mockResolvedValue(mockOptions as IStatusInstancia[]);


    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Instancia 1' },
        { id: 2, nome: 'Status Instancia 2' }
      ]);
    });


    expect(mockStatusInstanciaService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Instancia ABC' },
      { id: 2, nome: 'Status Instancia XYZ' }
    ];
    mockStatusInstanciaService.getList.mockResolvedValue(mockOptions as IStatusInstancia[]);   


 const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Instancia ABC' },
        { id: 2, nome: 'Status Instancia XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Status Instancia ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Instancia ABC' },
      { id: 2, nome: 'Status Instancia XYZ' }
    ];
    mockStatusInstanciaService.getList.mockResolvedValue(mockOptions as IStatusInstancia[]);
  


    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Instancia ABC' },
        { id: 2, nome: 'Status Instancia XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Status Instancia ABC' },
          {id: 2, nome: 'Status Instancia XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );

    const newValue = { id: 1, nome: 'Status Instancia Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Status Instancia Inicial' };
    
    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useStatusInstanciaComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Status Instancia Inicial' };
    
    const { result } = renderHook(() => 
      useStatusInstanciaComboBox(mockStatusInstanciaService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






