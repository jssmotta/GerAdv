// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class ProTipoBaixaValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFProTipoBaixaFactory> _mockProTipoBaixaFactory;
    private readonly Mock<IProTipoBaixaReader> _mockReader;
    private readonly ProTipoBaixaValidation _validation;
    private readonly Mock<IProTipoBaixaService> _mockProTipoBaixaService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly ProTipoBaixaService _service;
    private readonly string _validUri = "test-uri";
    public ProTipoBaixaValidationTests()
    {
        _mockProTipoBaixaFactory = new Mock<IFProTipoBaixaFactory>();
        _mockReader = new Mock<IProTipoBaixaReader>();
        _validation = new ProTipoBaixaValidation();
        _mockProTipoBaixaService = new Mock<IProTipoBaixaService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void ProTipoBaixaValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IProTipoBaixaValidation>();
    }

    [Fact]
    public void ProTipoBaixaValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(ProTipoBaixaValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void ProTipoBaixaValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(ProTipoBaixaValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.ProTipoBaixa CreateValidProTipoBaixa()
    {
        return new Models.ProTipoBaixa
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockProTipoBaixaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterProTipoBaixa>(), It.IsAny<string>())).ReturnsAsync(new List<ProTipoBaixaResponseAll>());
    // Setup other mocks but don't override the ProTipoBaixas service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockProTipoBaixaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterProTipoBaixa>(), It.IsAny<string>())).ReturnsAsync(new List<ProTipoBaixaResponseAll>());
    // Setup other mocks but don't override the ProTipoBaixas service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var protipobaixa = new Models.ProTipoBaixa
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(protipobaixa, _mockProTipoBaixaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockProTipoBaixaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}