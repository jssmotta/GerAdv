// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IGruposEmpresasCliService } from '../Services/GruposEmpresasCli.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IGruposEmpresasCli } from '../Interfaces/interface.GruposEmpresasCli';
import { FilterGruposEmpresasCli } from '../Filters/GruposEmpresasCli';
import { isValidDate } from '@/app/tools/datetime';
import { GruposEmpresasCliApi } from '../Apis/ApiGruposEmpresasCli';

export const useGruposEmpresasCliForm = (
  initialGruposEmpresasCli: IGruposEmpresasCli,
  dataService: IGruposEmpresasCliService
) => {
  const [data, setData] = useState<IGruposEmpresasCli>(initialGruposEmpresasCli);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadGruposEmpresasCli = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialGruposEmpresasCli);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchGruposEmpresasCliById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Grupos Empresas Cli';
      setError(errorMessage);
      //console.log('Erro ao carregar Grupos Empresas Cli');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialGruposEmpresasCli]);

  const resetForm = useCallback(() => {
    setData(initialGruposEmpresasCli);
    setError(null);
  }, [initialGruposEmpresasCli]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadGruposEmpresasCli,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadGruposEmpresasCli, resetForm]);
  return returnValue;
};


export const useGruposEmpresasCliNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('GruposEmpresasCli', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useGruposEmpresasCliList = (dataService: IGruposEmpresasCliService, currentFilter?: FilterGruposEmpresasCli | undefined | null) => {
  const [data, setData] = useState<IGruposEmpresasCli[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterGruposEmpresasCli | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterGruposEmpresasCli | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterGruposEmpresasCli);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar gruposempresascli';
      setError(errorMessage);
      //console.log('Erro ao carregar gruposempresascli');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useGruposEmpresasCliNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsGruposEmpresasCli() {

  async function runValidation(data: IGruposEmpresasCli, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const gruposempresascliApi = new GruposEmpresasCliApi(uri ?? '', token ?? '');

    const result = await gruposempresascliApi.validation(data);

    return result;
  }

  function validate(data: IGruposEmpresasCli): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}