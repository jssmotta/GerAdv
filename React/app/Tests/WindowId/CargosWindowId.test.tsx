// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import CargosWindowId from '@/app/GerAdv_TS/Cargos/Crud/Grids/CargosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import CargosWindowId from '@/app/GerAdv_TS/Cargos/Crud/Grids/CargosWindowId';
import { CargosEmpty } from '@/app/GerAdv_TS/Models/Cargos';

describe('CargosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(CargosWindowId).toBeDefined();
    expect(typeof CargosWindowId === 'function' || typeof CargosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the CargosService class so constructor usage doesn't throw and we can control fetchCargosById
const mockFetchCargosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Cargos/Services/Cargos.service', () => ({
  CargosService: jest.fn().mockImplementation(() => ({
    fetchCargosById: (...args: any[]) => mockFetchCargosById(...args),
  })),
}));

// Mock the child CargosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Cargos/Crud/Grids/CargosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='cargos-window' {...p} />,
}));

describe('CargosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(CargosWindowId).toBeDefined();
    expect(typeof CargosWindowId === 'function' || typeof CargosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <CargosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders CargosWindow with CargosEmpty when id is 0', async () => {
    render(
      <CargosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // CargosWindow should be rendered with selectedCargos equal to CargosEmpty()
    const node = await screen.findByTestId('cargos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedCargos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcargos') || node.getAttribute('selectedCargos')).not.toBeNull();
  });

  it('fetches cargos by id and passes it to CargosWindow when id > 0', async () => {
    const fakeCargos = { id: 123, nomecliente: 'Test' };
    mockFetchCargosById.mockResolvedValue(fakeCargos);

    render(
      <CargosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('cargos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchCargosById).toHaveBeenCalledWith(123));
  });
});
