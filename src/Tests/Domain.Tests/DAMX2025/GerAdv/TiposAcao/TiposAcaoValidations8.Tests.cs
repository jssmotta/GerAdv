// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TiposAcaoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFTiposAcaoFactory> _mockTiposAcaoFactory;
    private readonly Mock<ITiposAcaoReader> _mockReader;
    private readonly TiposAcaoValidation _validation;
    private readonly Mock<ITiposAcaoService> _mockTiposAcaoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TiposAcaoService _service;
    private readonly string _validUri = "test-uri";
    public TiposAcaoValidationTests()
    {
        _mockTiposAcaoFactory = new Mock<IFTiposAcaoFactory>();
        _mockReader = new Mock<ITiposAcaoReader>();
        _validation = new TiposAcaoValidation();
        _mockTiposAcaoService = new Mock<ITiposAcaoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TiposAcaoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITiposAcaoValidation>();
    }

    [Fact]
    public void TiposAcaoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TiposAcaoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TiposAcaoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TiposAcaoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.TiposAcao CreateValidTiposAcao()
    {
        return new Models.TiposAcao
        {
            Id = 1,
            Nome = "João",
            Inativo = false
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTiposAcaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTiposAcao>(), It.IsAny<string>())).ReturnsAsync(new List<TiposAcaoResponseAll>());
    // Setup other mocks but don't override the TiposAcaos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTiposAcaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTiposAcao>(), It.IsAny<string>())).ReturnsAsync(new List<TiposAcaoResponseAll>());
    // Setup other mocks but don't override the TiposAcaos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tiposacao = new Models.TiposAcao
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Inativo = false
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tiposacao, _mockTiposAcaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTiposAcaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}