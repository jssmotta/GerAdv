// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import OponentesRepLegalWindowId from '@/app/GerAdv_TS/OponentesRepLegal/Crud/Grids/OponentesRepLegalWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import OponentesRepLegalWindowId from '@/app/GerAdv_TS/OponentesRepLegal/Crud/Grids/OponentesRepLegalWindowId';
import { OponentesRepLegalEmpty } from '@/app/GerAdv_TS/Models/OponentesRepLegal';

describe('OponentesRepLegalWindowId (basic)', () => {
  it('exports the component', () => {
    expect(OponentesRepLegalWindowId).toBeDefined();
    expect(typeof OponentesRepLegalWindowId === 'function' || typeof OponentesRepLegalWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the OponentesRepLegalService class so constructor usage doesn't throw and we can control fetchOponentesRepLegalById
const mockFetchOponentesRepLegalById = jest.fn();
jest.mock('@/app/GerAdv_TS/OponentesRepLegal/Services/OponentesRepLegal.service', () => ({
  OponentesRepLegalService: jest.fn().mockImplementation(() => ({
    fetchOponentesRepLegalById: (...args: any[]) => mockFetchOponentesRepLegalById(...args),
  })),
}));

// Mock the child OponentesRepLegalWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/OponentesRepLegal/Crud/Grids/OponentesRepLegalWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='oponentesreplegal-window' {...p} />,
}));

describe('OponentesRepLegalWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(OponentesRepLegalWindowId).toBeDefined();
    expect(typeof OponentesRepLegalWindowId === 'function' || typeof OponentesRepLegalWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <OponentesRepLegalWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders OponentesRepLegalWindow with OponentesRepLegalEmpty when id is 0', async () => {
    render(
      <OponentesRepLegalWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // OponentesRepLegalWindow should be rendered with selectedOponentesRepLegal equal to OponentesRepLegalEmpty()
    const node = await screen.findByTestId('oponentesreplegal-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedOponentesRepLegal"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedoponentesreplegal') || node.getAttribute('selectedOponentesRepLegal')).not.toBeNull();
  });

  it('fetches oponentesreplegal by id and passes it to OponentesRepLegalWindow when id > 0', async () => {
    const fakeOponentesRepLegal = { id: 123, nomecliente: 'Test' };
    mockFetchOponentesRepLegalById.mockResolvedValue(fakeOponentesRepLegal);

    render(
      <OponentesRepLegalWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('oponentesreplegal-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchOponentesRepLegalById).toHaveBeenCalledWith(123));
  });
});
