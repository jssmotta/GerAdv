// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { CRUD_CONSTANTS } from '@/app/tools/crud';
import { Agenda2AgendaApi, Agenda2AgendaApiError } from '../Apis/ApiAgenda2Agenda';
import { FilterAgenda2Agenda } from '../Filters/Agenda2Agenda';
import { IAgenda2Agenda } from '../Interfaces/interface.Agenda2Agenda';
import { Agenda2AgendaEmpty } from '../../Models/Agenda2Agenda';

export class Agenda2AgendaValidator {
  static validateAgenda2Agenda(agenda2agenda: IAgenda2Agenda): { isValid: boolean; errors: string[] } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes
    
    return {
      isValid: errors.length === 0,
      errors
    };
  }
}

export interface IAgenda2AgendaService {
  fetchAgenda2AgendaById: (id: number) => Promise<IAgenda2Agenda>;
  saveAgenda2Agenda: (agenda2agenda: IAgenda2Agenda) => Promise<IAgenda2Agenda>;  
  
  getAll: (filtro?: FilterAgenda2Agenda) => Promise<IAgenda2Agenda[]>;
  deleteAgenda2Agenda: (id: number) => Promise<void>;
  validateAgenda2Agenda: (agenda2agenda: IAgenda2Agenda) => { isValid: boolean; errors: string[] };
}

export class Agenda2AgendaService implements IAgenda2AgendaService {
  constructor(private api: Agenda2AgendaApi) {}

  async fetchAgenda2AgendaById(id: number): Promise<IAgenda2Agenda> {
    if (id <= 0) {
      throw new Agenda2AgendaApiError('ID inválido', 400, 'INVALID_ID');
    }

    try {
      
      const response = await this.api.getById(id);
      return response.data;

    } catch (error) {
      if (error instanceof Agenda2AgendaApiError) {
        throw error;
      }
      throw new Agenda2AgendaApiError('Erro ao buscar agenda2agenda', 500, 'FETCH_ERROR', error);
    }
  }

  async saveAgenda2Agenda(agenda2agenda: IAgenda2Agenda): Promise<IAgenda2Agenda> {    
    const validation = this.validateAgenda2Agenda(agenda2agenda);
    if (!validation.isValid) {
      throw new Agenda2AgendaApiError(
        `Dados inválidos: ${validation.errors.join(', ')}`,
        400,
        'VALIDATION_ERROR'
      );
    }

    try {
      const response = await this.api.addAndUpdate(agenda2agenda);
      return response.data;
    } catch (error) {
      if (error instanceof Agenda2AgendaApiError) {
        throw error;
      }
      throw new Agenda2AgendaApiError('Erro ao salvar agenda2agenda', 500, 'SAVE_ERROR', error);
    }
  }

  
  

   async getAll(
    filtro?: FilterAgenda2Agenda,
    onOnlineData?: (data: IAgenda2Agenda[]) => void
  ): Promise<IAgenda2Agenda[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(filtro ?? {});
      const offlineData = preloadResponse?.data || [];

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api.filter(filtro ?? {})
          .then(response => {
            if (response?.data) {
              onOnlineData(response.data);
            }
          })
          .catch(error => {
            console.log('Error fetching online Agenda2Agenda');
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(filtro ?? {});
          return onlineResponse?.data || offlineData;
        } catch (error) {
          console.log('Error fetching online Agenda2Agenda');
          return offlineData;
        }
      }
    } catch (error) {
      console.log('Error fetching all Agenda2Agenda:');
      return [];
    }
  }


  async deleteAgenda2Agenda(id: number): Promise<void> {
    if (id <= 0) {
      throw new Agenda2AgendaApiError('ID inválido para exclusão', 400, 'INVALID_ID');
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof Agenda2AgendaApiError) {
        throw error;
      }
      throw new Agenda2AgendaApiError('Erro ao excluir agenda2agenda', 500, 'DELETE_ERROR', error);
    }
  }

  validateAgenda2Agenda(agenda2agenda: IAgenda2Agenda): { isValid: boolean; errors: string[] } {
    return Agenda2AgendaValidator.validateAgenda2Agenda(agenda2agenda);
  }
}