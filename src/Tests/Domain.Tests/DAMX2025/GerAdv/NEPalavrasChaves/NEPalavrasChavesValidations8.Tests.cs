// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class NEPalavrasChavesValidationTests : IDisposable
{
    private readonly NEPalavrasChavesValidation _validation;
    private readonly Mock<INEPalavrasChavesService> _mockNEPalavrasChavesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly NEPalavrasChavesService _service;
    private readonly string _validUri = "test-uri";
    public NEPalavrasChavesValidationTests()
    {
        _validation = new NEPalavrasChavesValidation();
        _mockNEPalavrasChavesService = new Mock<INEPalavrasChavesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void NEPalavrasChavesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<INEPalavrasChavesValidation>();
    }

    [Fact]
    public void NEPalavrasChavesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(NEPalavrasChavesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void NEPalavrasChavesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(NEPalavrasChavesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.NEPalavrasChaves CreateValidNEPalavrasChaves()
    {
        return new Models.NEPalavrasChaves
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockNEPalavrasChavesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterNEPalavrasChaves>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the NEPalavrasChavess service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockNEPalavrasChavesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterNEPalavrasChaves>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the NEPalavrasChavess service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var nepalavraschaves = new Models.NEPalavrasChaves
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(nepalavraschaves, _mockNEPalavrasChavesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockNEPalavrasChavesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}