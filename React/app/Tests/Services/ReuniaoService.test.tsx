// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ReuniaoService, ReuniaoValidator } from '@/app/GerAdv_TS/Reuniao/Services/Reuniao.service';
import { ReuniaoApi, ReuniaoApiError } from '@/app/GerAdv_TS/Reuniao/Apis/ApiReuniao';
import { IReuniao } from '@/app/GerAdv_TS/Reuniao/Interfaces/interface.Reuniao';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ReuniaoApi;

const sample: IReuniao = { id: 1, nome: 'Teste' };

describe('ReuniaoService', () => {
  let service: ReuniaoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ReuniaoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ReuniaoValidator, 'validateReuniao').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchReuniaoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchReuniaoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchReuniaoById lança ReuniaoApiError para id inválido', async () => {
    await expect(service.fetchReuniaoById(0)).rejects.toBeInstanceOf(ReuniaoApiError);
  });

  it('saveReuniao chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveReuniao(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveReuniao lança ReuniaoApiError quando validação falha', async () => {
    (ReuniaoValidator.validateReuniao as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveReuniao(sample)).rejects.toBeInstanceOf(ReuniaoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteReuniao chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteReuniao(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteReuniao lança ReuniaoApiError para id inválido', async () => {
    await expect(service.deleteReuniao(0)).rejects.toBeInstanceOf(ReuniaoApiError);
  });

  it('validateReuniao delega para ReuniaoValidator', () => {
    (ReuniaoValidator.validateReuniao as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateReuniao(sample);
    expect(r.isValid).toBe(true);
  });
});
