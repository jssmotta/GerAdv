// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { EscritoriosService, EscritoriosValidator } from '@/app/GerAdv_TS/Escritorios/Services/Escritorios.service';
import { EscritoriosApi, EscritoriosApiError } from '@/app/GerAdv_TS/Escritorios/Apis/ApiEscritorios';
import { IEscritorios } from '@/app/GerAdv_TS/Escritorios/Interfaces/interface.Escritorios';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as EscritoriosApi;

const sample: IEscritorios = { id: 1, nome: 'Teste' };

describe('EscritoriosService', () => {
  let service: EscritoriosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new EscritoriosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(EscritoriosValidator, 'validateEscritorios').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchEscritoriosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchEscritoriosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchEscritoriosById lança EscritoriosApiError para id inválido', async () => {
    await expect(service.fetchEscritoriosById(0)).rejects.toBeInstanceOf(EscritoriosApiError);
  });

  it('saveEscritorios chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveEscritorios(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveEscritorios lança EscritoriosApiError quando validação falha', async () => {
    (EscritoriosValidator.validateEscritorios as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveEscritorios(sample)).rejects.toBeInstanceOf(EscritoriosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteEscritorios chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteEscritorios(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteEscritorios lança EscritoriosApiError para id inválido', async () => {
    await expect(service.deleteEscritorios(0)).rejects.toBeInstanceOf(EscritoriosApiError);
  });

  it('validateEscritorios delega para EscritoriosValidator', () => {
    (EscritoriosValidator.validateEscritorios as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateEscritorios(sample);
    expect(r.isValid).toBe(true);
  });
});
