// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { LivroCaixaService, LivroCaixaValidator } from '@/app/GerAdv_TS/LivroCaixa/Services/LivroCaixa.service';
import { LivroCaixaApi, LivroCaixaApiError } from '@/app/GerAdv_TS/LivroCaixa/Apis/ApiLivroCaixa';
import { ILivroCaixa } from '@/app/GerAdv_TS/LivroCaixa/Interfaces/interface.LivroCaixa';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as LivroCaixaApi;

const sample: ILivroCaixa = { id: 1, nome: 'Teste' };

describe('LivroCaixaService', () => {
  let service: LivroCaixaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new LivroCaixaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(LivroCaixaValidator, 'validateLivroCaixa').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchLivroCaixaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchLivroCaixaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchLivroCaixaById lança LivroCaixaApiError para id inválido', async () => {
    await expect(service.fetchLivroCaixaById(0)).rejects.toBeInstanceOf(LivroCaixaApiError);
  });

  it('saveLivroCaixa chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveLivroCaixa(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveLivroCaixa lança LivroCaixaApiError quando validação falha', async () => {
    (LivroCaixaValidator.validateLivroCaixa as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveLivroCaixa(sample)).rejects.toBeInstanceOf(LivroCaixaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteLivroCaixa chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteLivroCaixa(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteLivroCaixa lança LivroCaixaApiError para id inválido', async () => {
    await expect(service.deleteLivroCaixa(0)).rejects.toBeInstanceOf(LivroCaixaApiError);
  });

  it('validateLivroCaixa delega para LivroCaixaValidator', () => {
    (LivroCaixaValidator.validateLivroCaixa as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateLivroCaixa(sample);
    expect(r.isValid).toBe(true);
  });
});
