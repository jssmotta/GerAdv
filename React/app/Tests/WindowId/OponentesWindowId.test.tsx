// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import OponentesWindowId from '@/app/GerAdv_TS/Oponentes/Crud/Grids/OponentesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import OponentesWindowId from '@/app/GerAdv_TS/Oponentes/Crud/Grids/OponentesWindowId';
import { OponentesEmpty } from '@/app/GerAdv_TS/Models/Oponentes';

describe('OponentesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(OponentesWindowId).toBeDefined();
    expect(typeof OponentesWindowId === 'function' || typeof OponentesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the OponentesService class so constructor usage doesn't throw and we can control fetchOponentesById
const mockFetchOponentesById = jest.fn();
jest.mock('@/app/GerAdv_TS/Oponentes/Services/Oponentes.service', () => ({
  OponentesService: jest.fn().mockImplementation(() => ({
    fetchOponentesById: (...args: any[]) => mockFetchOponentesById(...args),
  })),
}));

// Mock the child OponentesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Oponentes/Crud/Grids/OponentesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='oponentes-window' {...p} />,
}));

describe('OponentesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(OponentesWindowId).toBeDefined();
    expect(typeof OponentesWindowId === 'function' || typeof OponentesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <OponentesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders OponentesWindow with OponentesEmpty when id is 0', async () => {
    render(
      <OponentesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // OponentesWindow should be rendered with selectedOponentes equal to OponentesEmpty()
    const node = await screen.findByTestId('oponentes-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedOponentes"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedoponentes') || node.getAttribute('selectedOponentes')).not.toBeNull();
  });

  it('fetches oponentes by id and passes it to OponentesWindow when id > 0', async () => {
    const fakeOponentes = { id: 123, nomecliente: 'Test' };
    mockFetchOponentesById.mockResolvedValue(fakeOponentes);

    render(
      <OponentesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('oponentes-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchOponentesById).toHaveBeenCalledWith(123));
  });
});
