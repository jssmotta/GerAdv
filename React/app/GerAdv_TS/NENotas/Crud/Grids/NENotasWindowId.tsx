// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { INENotas } from '../../Interfaces/interface.NENotas';
import { NENotasService } from '../../Services/NENotas.service';
import { NENotasApi } from '../../Apis/ApiNENotas';
import NENotasWindow from './NENotasWindow';
import {NENotasEmpty } from '@/app/GerAdv_TS/Models/NENotas';
interface NENotasWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const NENotasWindowId: React.FC<NENotasWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const nenotasService = useMemo(() => {
  return new NENotasService(
  new NENotasApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<INENotas | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(NENotasEmpty() as INENotas);
      return;
    }
    if (id) {
      const response = await nenotasService.fetchNENotasById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <NENotasWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedNENotas={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default NENotasWindowId;