// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Situacao
// Fonte:WritersGenerator
namespace MenphisSI.GerAdv.Writers;
public partial interface ISituacaoWriter
{
    Task<FSituacao> WriteAsync(Models.Situacao situacao, int auditorQuem, MsiSqlConnection? oCnn);
    Task DeleteAsync(SituacaoResponse situacao, int operadorId, MsiSqlConnection? oCnn);
}

public class SituacaoWriter(IFSituacaoFactory situacaoFactory) : ISituacaoWriter
{
    private readonly IFSituacaoFactory _situacaoFactory = situacaoFactory ?? throw new ArgumentNullException(nameof(situacaoFactory));
    public virtual async Task DeleteAsync(SituacaoResponse situacao, int operadorId, MsiSqlConnection? oCnn)
    {
        var stopwatch = SituacaoDatabaseMetrics.StartTimer();
        try
        {
            SituacaoDatabaseMetrics.IncrementActiveConnections("DeleteAsync", oCnn?.Uri);
            await _situacaoFactory.DeleteAsync(operadorId, situacao.Id, oCnn);
            SituacaoDatabaseMetrics.RecordSqlQuery("DeleteAsync", "DELETE", oCnn?.Uri, stopwatch, 0);
        }
        catch (SqlException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("DeleteAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("DeleteAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            SituacaoDatabaseMetrics.DecrementActiveConnections("DeleteAsync", oCnn?.Uri);
        }
    }

    public virtual async Task<FSituacao> WriteAsync(Models.Situacao situacao, int auditorQuem, MsiSqlConnection? oCnn)
    {
        var stopwatch = SituacaoDatabaseMetrics.StartTimer();
        var isInsert = situacao.Id.IsEmptyIDNumber();
        var tipoComando = isInsert ? "INSERT" : "UPDATE";
        try
        {
            SituacaoDatabaseMetrics.IncrementActiveConnections("WriteAsync", oCnn?.Uri);
            using var dbRec = await (isInsert ? _situacaoFactory.CreateAsync() : _situacaoFactory.CreateFromIdAsync(situacao.Id, oCnn));
            dbRec.FParte_Int = situacao.Parte_Int;
            dbRec.FParte_Opo = situacao.Parte_Opo;
            dbRec.FTop = situacao.Top;
            dbRec.FBold = situacao.Bold;
            dbRec.FGuid = situacao.Guid;
            dbRec.AuditorQuem = auditorQuem;
            await dbRec.UpdateAsync(oCnn);
            SituacaoDatabaseMetrics.RecordSqlQuery("WriteAsync", tipoComando, oCnn?.Uri, stopwatch, 0);
            return dbRec;
        }
        catch (SqlException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("WriteAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SituacaoDatabaseMetrics.RecordDatabaseError("WriteAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            SituacaoDatabaseMetrics.DecrementActiveConnections("WriteAsync", oCnn?.Uri);
        }
    }
}