// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class PosicaoOutrasPartesValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFPosicaoOutrasPartesFactory> _mockPosicaoOutrasPartesFactory;
    private readonly Mock<IPosicaoOutrasPartesReader> _mockReader;
    private readonly PosicaoOutrasPartesValidation _validation;
    private readonly Mock<IPosicaoOutrasPartesService> _mockPosicaoOutrasPartesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly PosicaoOutrasPartesService _service;
    private readonly string _validUri = "test-uri";
    public PosicaoOutrasPartesValidationTests()
    {
        _mockPosicaoOutrasPartesFactory = new Mock<IFPosicaoOutrasPartesFactory>();
        _mockReader = new Mock<IPosicaoOutrasPartesReader>();
        _validation = new PosicaoOutrasPartesValidation();
        _mockPosicaoOutrasPartesService = new Mock<IPosicaoOutrasPartesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void PosicaoOutrasPartesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IPosicaoOutrasPartesValidation>();
    }

    [Fact]
    public void PosicaoOutrasPartesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(PosicaoOutrasPartesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void PosicaoOutrasPartesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(PosicaoOutrasPartesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.PosicaoOutrasPartes CreateValidPosicaoOutrasPartes()
    {
        return new Models.PosicaoOutrasPartes
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockPosicaoOutrasPartesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPosicaoOutrasPartes>(), It.IsAny<string>())).ReturnsAsync(new List<PosicaoOutrasPartesResponseAll>());
    // Setup other mocks but don't override the PosicaoOutrasPartess service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockPosicaoOutrasPartesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPosicaoOutrasPartes>(), It.IsAny<string>())).ReturnsAsync(new List<PosicaoOutrasPartesResponseAll>());
    // Setup other mocks but don't override the PosicaoOutrasPartess service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var posicaooutraspartes = new Models.PosicaoOutrasPartes
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(posicaooutraspartes, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Descricao Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyDescricao_ShouldThrowSGValidationException()
    {
        // Arrange
        var posicaooutraspartes = CreateValidPosicaoOutrasPartes();
        posicaooutraspartes.Descricao = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(posicaooutraspartes, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithNullDescricao_ShouldThrowSGValidationException()
    {
        // Arrange
        var posicaooutraspartes = CreateValidPosicaoOutrasPartes();
        posicaooutraspartes.Descricao = null;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(posicaooutraspartes, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidDataDescricao_ShouldReturnTrue()
    {
        // Arrange
        var posicaooutraspartes = CreateValidPosicaoOutrasPartes();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(posicaooutraspartes, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespaceDescricao_ShouldThrowSGValidationException()
    {
        // Arrange
        var posicaooutraspartes = CreateValidPosicaoOutrasPartes();
        posicaooutraspartes.Descricao = "   ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(posicaooutraspartes, _mockPosicaoOutrasPartesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}