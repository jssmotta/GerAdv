// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ViaRecebimento
// Fonte:ControllerGenerator
namespace MenphisSI.GerAdv.Controller;
[Route("api/v{version:apiVersion}/{uri}/[controller]/[action]")]
[ApiController]
[ApiVersion("1.0")]
public partial class ViaRecebimentoController(IViaRecebimentoService viarecebimentoService) : ControllerBase
{
    private readonly IViaRecebimentoService _viarecebimentoService = viarecebimentoService;
    private static readonly Logger _logger = LogManager.GetCurrentClassLogger();
    [HttpGet]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAll([FromQuery] int max, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "GetAll";
        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.GetAll(max, uri);
            });
            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            ViaRecebimentoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: GetAll failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Filter([FromQuery] int max, [FromBody] MenphisSI.GerAdv.Filters.FilterViaRecebimento filter, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "Filter";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.Filter(max, filter, uri);
            });
            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            ViaRecebimentoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: Filter failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> FilterVoice([FromBody] MenphisSI.GerAdv.Filters.FilterViaRecebimentoWithVoiceRequest request, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "FilterVoice";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.RelatoriosPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.FilterVoice(request.Filter, request.VoiceCommand, uri);
            });
            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: FilterVoice failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetById(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "GetById";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.GetById(id, uri, token);
            });
            if (result == null)
            {
                ViaRecebimentoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetById: No ViaRecebimento found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            ViaRecebimentoMetrics.RecordReadByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: GetById failed with exception for id = {0}, {1}", id, uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetListN([FromQuery] int max, [FromBody] Filters.FilterViaRecebimento? filtro, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "GetListN";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.GetListN(max, filtro, uri);
            });
            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            ViaRecebimentoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: GetListN failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [ProducesResponseType(typeof(ViaRecebimentoResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(ViaRecebimentoResponse), StatusCodes.Status201Created)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> AddAndUpdate([FromBody] Models.ViaRecebimento regViaRecebimento, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        var isNew = regViaRecebimento.Id == 0;
        var operacao = isNew ? "Create" : "Update";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.AddAndUpdate(regViaRecebimento, uri);
            });
            if (result == null)
            {
                ViaRecebimentoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("ViaRecebimento: AddAndUpdate failed to add or update ViaRecebimento, {0}", uri);
                return BadRequest();
            }

            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            if (isNew)
            {
                ViaRecebimentoMetrics.RecordCreated(uri);
                ViaRecebimentoMetrics.RecordCreatedByHour(uri);
            }
            else
            {
                ViaRecebimentoMetrics.RecordUpdated(uri);
                ViaRecebimentoMetrics.RecordUpdatedByHour(uri);
            }

            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: AddAndUpdate failed with exception for uri = {0}", uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = ex.Message });
        }
    }

    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [HttpDelete]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> Delete([FromQuery] int id, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "Delete";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.Delete(id, uri);
            });
            if (result == null)
            {
                ViaRecebimentoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("Delete: No ViaRecebimento found to delete with id = {0}, {1}", id, uri);
                return NotFound();
            }

            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            ViaRecebimentoMetrics.RecordDeleted(uri);
            ViaRecebimentoMetrics.RecordDeletedByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordConflict(operacao, uri, stopwatch);
            _logger.Error(ex, "ViaRecebimento: Delete failed with exception for id = {0}, {1}", id, uri);
            return Conflict(new Error500 { success = false, data = "", message = "Não é possível excluir o registro porque ele está sendo referenciado/em uso em outra tabela." });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Validation([FromBody] Models.ViaRecebimento regViaRecebimento, [FromRoute, Required] string uri)
    {
        var stopwatch = ViaRecebimentoMetrics.StartTimer();
        const string operacao = "Validation";
        if (!ModelState.IsValid)
        {
            ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _viarecebimentoService.Validation(regViaRecebimento, uri);
            });
            if (result == null)
            {
                ViaRecebimentoMetrics.RecordInvalid(operacao, uri, stopwatch);
                _logger.Warn("ViaRecebimento: Validation failed to add or update ViaRecebimento, {0}", uri);
                return BadRequest();
            }

            ViaRecebimentoMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            ViaRecebimentoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("ViaRecebimento: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            ViaRecebimentoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "ViaRecebimento: Validation failed with exception for uri = {0}", uri);
            return StatusCode(500, new { success = false, data = "", message = ex.Message });
        }
    }
}