// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useStatusHTrabForm, useStatusHTrabList, useValidationsStatusHTrab } from '../GerAdv_TS/StatusHTrab/Hooks/hookStatusHTrab';
import { IStatusHTrab } from '../GerAdv_TS/StatusHTrab/Interfaces/interface.StatusHTrab';
import { IStatusHTrabService } from '../GerAdv_TS/StatusHTrab/Services/StatusHTrab.service';
import { StatusHTrabTestEmpty } from '../GerAdv_TS/Models/StatusHTrab';
import { useStatusHTrabComboBox } from '../GerAdv_TS/StatusHTrab/Hooks/hookStatusHTrab';

// Mock do serviço
const mockStatusHTrabService: jest.Mocked<IStatusHTrabService> = {
  fetchStatusHTrabById: jest.fn(),
  saveStatusHTrab: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteStatusHTrab: jest.fn(),
  validateStatusHTrab: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialStatusHTrab: IStatusHTrab = { ...StatusHTrabTestEmpty() };

describe('useStatusHTrabForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    expect(result.current.data).toEqual(initialStatusHTrab);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Status H Trab',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Status H Trab');
  });

   test('deve carregar Status H Trab por ID', async () => {
    const mockStatusHTrab = { ...initialStatusHTrab, id: 1, descricao: 'Status H Trab Teste' };
    mockStatusHTrabService.fetchStatusHTrabById.mockResolvedValue(mockStatusHTrab);

    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.loadStatusHTrab(1);
    });

    expect(mockStatusHTrabService.fetchStatusHTrabById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockStatusHTrab);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Status H Trab', async () => {
    const errorMessage = 'Erro ao carregar Status H Trab';
    mockStatusHTrabService.fetchStatusHTrabById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.loadStatusHTrab(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialStatusHTrab, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialStatusHTrab);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useStatusHTrabForm(initialStatusHTrab, mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.loadStatusHTrab(0);
    });

    expect(mockStatusHTrabService.fetchStatusHTrabById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialStatusHTrab);
  });
});

describe('useStatusHTrabList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusHTrabList(mockStatusHTrabService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialStatusHTrab, id: 1, descricao: 'Status H Trab 1' },
      { ...initialStatusHTrab, id: 2, descricao: 'Status H Trab 2' }
    ];
    mockStatusHTrabService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusHTrabList(mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockStatusHTrabService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockStatusHTrabService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusHTrabList(mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialStatusHTrab, id: 1, descricao: 'Status H Trab Filtrado' }];
    const filtro = { descricao: 'Status H Trab' };
    mockStatusHTrabService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusHTrabList(mockStatusHTrabService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockStatusHTrabService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsStatusHTrab', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsStatusHTrab());

    const validData = { ...initialStatusHTrab, descricao: 'Status H Trab Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsStatusHTrab());

    const invalidData = { ...initialStatusHTrab, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsStatusHTrab());

    const invalidData = { 
      ...initialStatusHTrab, 
      descricao: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsStatusHTrab());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialStatusHTrab, id: 1, descricao: 'Status H Trab Teste' }];
    mockStatusHTrabService.getAll.mockResolvedValue(mockData);
    mockStatusHTrabService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useStatusHTrabList(mockStatusHTrabService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsStatusHTrab()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status H Trab Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status H Trab Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Status H Trab 1' },
      { id: 2, descricao: 'Status H Trab 2' }
    ];
    mockStatusHTrabService.getList.mockResolvedValue(mockOptions as IStatusHTrab[]);


    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status H Trab 1' },
        { id: 2, nome: 'Status H Trab 2' }
      ]);
    });


    expect(mockStatusHTrabService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Status H Trab ABC' },
      { id: 2, descricao: 'Status H Trab XYZ' }
    ];
    mockStatusHTrabService.getList.mockResolvedValue(mockOptions as IStatusHTrab[]);   


 const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status H Trab ABC' },
        { id: 2, nome: 'Status H Trab XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Status H Trab ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Status H Trab ABC' },
      { id: 2, descricao: 'Status H Trab XYZ' }
    ];
    mockStatusHTrabService.getList.mockResolvedValue(mockOptions as IStatusHTrab[]);
  


    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status H Trab ABC' },
        { id: 2, nome: 'Status H Trab XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Status H Trab ABC' },
          {id: 2, nome: 'Status H Trab XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );

    const newValue = { id: 1, descricao: 'Status H Trab Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Status H Trab Inicial' };
    
    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useStatusHTrabComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Status H Trab Inicial' };
    
    const { result } = renderHook(() => 
      useStatusHTrabComboBox(mockStatusHTrabService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






