// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { PenhoraStatusService, PenhoraStatusValidator } from '@/app/GerAdv_TS/PenhoraStatus/Services/PenhoraStatus.service';
import { PenhoraStatusApi, PenhoraStatusApiError } from '@/app/GerAdv_TS/PenhoraStatus/Apis/ApiPenhoraStatus';
import { IPenhoraStatus } from '@/app/GerAdv_TS/PenhoraStatus/Interfaces/interface.PenhoraStatus';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as PenhoraStatusApi;

const sample: IPenhoraStatus = { id: 1, nome: 'Teste' };

describe('PenhoraStatusService', () => {
  let service: PenhoraStatusService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new PenhoraStatusService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(PenhoraStatusValidator, 'validatePenhoraStatus').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchPenhoraStatusById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchPenhoraStatusById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchPenhoraStatusById lança PenhoraStatusApiError para id inválido', async () => {
    await expect(service.fetchPenhoraStatusById(0)).rejects.toBeInstanceOf(PenhoraStatusApiError);
  });

  it('savePenhoraStatus chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.savePenhoraStatus(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('savePenhoraStatus lança PenhoraStatusApiError quando validação falha', async () => {
    (PenhoraStatusValidator.validatePenhoraStatus as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.savePenhoraStatus(sample)).rejects.toBeInstanceOf(PenhoraStatusApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deletePenhoraStatus chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deletePenhoraStatus(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deletePenhoraStatus lança PenhoraStatusApiError para id inválido', async () => {
    await expect(service.deletePenhoraStatus(0)).rejects.toBeInstanceOf(PenhoraStatusApiError);
  });

  it('validatePenhoraStatus delega para PenhoraStatusValidator', () => {
    (PenhoraStatusValidator.validatePenhoraStatus as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validatePenhoraStatus(sample);
    expect(r.isValid).toBe(true);
  });
});
