// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProResumosService, ProResumosValidator } from '@/app/GerAdv_TS/ProResumos/Services/ProResumos.service';
import { ProResumosApi, ProResumosApiError } from '@/app/GerAdv_TS/ProResumos/Apis/ApiProResumos';
import { IProResumos } from '@/app/GerAdv_TS/ProResumos/Interfaces/interface.ProResumos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProResumosApi;

const sample: IProResumos = { id: 1, nome: 'Teste' };

describe('ProResumosService', () => {
  let service: ProResumosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProResumosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProResumosValidator, 'validateProResumos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProResumosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProResumosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProResumosById lança ProResumosApiError para id inválido', async () => {
    await expect(service.fetchProResumosById(0)).rejects.toBeInstanceOf(ProResumosApiError);
  });

  it('saveProResumos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProResumos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProResumos lança ProResumosApiError quando validação falha', async () => {
    (ProResumosValidator.validateProResumos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProResumos(sample)).rejects.toBeInstanceOf(ProResumosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProResumos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProResumos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProResumos lança ProResumosApiError para id inválido', async () => {
    await expect(service.deleteProResumos(0)).rejects.toBeInstanceOf(ProResumosApiError);
  });

  it('validateProResumos delega para ProResumosValidator', () => {
    (ProResumosValidator.validateProResumos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProResumos(sample);
    expect(r.isValid).toBe(true);
  });
});
