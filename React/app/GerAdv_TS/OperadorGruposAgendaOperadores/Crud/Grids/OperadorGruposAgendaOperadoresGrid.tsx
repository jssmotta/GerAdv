// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { OperadorGruposAgendaOperadoresEmpty } from '../../../Models/OperadorGruposAgendaOperadores';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IOperadorGruposAgendaOperadores } from '../../Interfaces/interface.OperadorGruposAgendaOperadores';
import { OperadorGruposAgendaOperadoresService } from '../../Services/OperadorGruposAgendaOperadores.service';
import { OperadorGruposAgendaOperadoresApi } from '../../Apis/ApiOperadorGruposAgendaOperadores';
import { OperadorGruposAgendaOperadoresGridMobileComponent } from '../GridsMobile/OperadorGruposAgendaOperadores';
import { OperadorGruposAgendaOperadoresGridDesktopComponent } from '../GridsDesktop/OperadorGruposAgendaOperadores';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterOperadorGruposAgendaOperadores } from '../../Filters/OperadorGruposAgendaOperadores';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import OperadorGruposAgendaOperadoresWindow from './OperadorGruposAgendaOperadoresWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useOperadorGruposAgendaOperadoresList } from '../../Hooks/hookOperadorGruposAgendaOperadores';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useOperadorGruposAgendaOperadoresFilter } from '../../Hooks/hookOperadorGruposAgendaOperadoresFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const OperadorGruposAgendaOperadoresGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedOperadorGruposAgendaOperadores, setSelectedOperadorGruposAgendaOperadores] = useState<IOperadorGruposAgendaOperadores>(OperadorGruposAgendaOperadoresEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterOperadorGruposAgendaOperadores | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [operadorgruposagendaoperadoresData, setOperadorGruposAgendaOperadoresData] = useState<IOperadorGruposAgendaOperadores[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const operadorgruposagendaoperadoresService = useMemo(() => {
    return new OperadorGruposAgendaOperadoresService(
    new OperadorGruposAgendaOperadoresApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: operadorgruposagendaoperadores, 
  fetchData, 
  refreshData
} = useOperadorGruposAgendaOperadoresList(operadorgruposagendaoperadoresService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterOperadorGruposAgendaOperadores | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('OperadorGruposAgendaOperadoresFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await operadorgruposagendaoperadoresService.getAll(
  filtro ?? {} as FilterOperadorGruposAgendaOperadores, 
  onlineData => {
    setOperadorGruposAgendaOperadoresData(onlineData);
    setLoading(false);
  }
);
setOperadorGruposAgendaOperadoresData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterOperadorGruposAgendaOperadores;

    const filtroMem = sessionStorage.getItem(btoa('OperadorGruposAgendaOperadoresFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('OperadorGruposAgendaOperadoresFilter')) ?? '') || '{}') : {} as FilterOperadorGruposAgendaOperadores;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (operadorgruposagendaoperadores: IOperadorGruposAgendaOperadores) => {
    setSelectedOperadorGruposAgendaOperadores(operadorgruposagendaoperadores);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedOperadorGruposAgendaOperadores(OperadorGruposAgendaOperadoresEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const operadorgruposagendaoperadores = e.dataItem;
    setDeleteId(operadorgruposagendaoperadores.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await operadorgruposagendaoperadoresService.deleteOperadorGruposAgendaOperadores(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useOperadorGruposAgendaOperadoresFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <OperadorGruposAgendaOperadoresGridMobileComponent
        data={operadorgruposagendaoperadoresData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <OperadorGruposAgendaOperadoresGridDesktopComponent
        data={operadorgruposagendaoperadoresData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <OperadorGruposAgendaOperadoresWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedOperadorGruposAgendaOperadores={selectedOperadorGruposAgendaOperadores}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Operador Grupos Agenda Operadores'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default OperadorGruposAgendaOperadoresGrid;