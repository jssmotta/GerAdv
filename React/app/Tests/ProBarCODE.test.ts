// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProBarCODEForm, useProBarCODEList, useValidationsProBarCODE } from '../GerAdv_TS/ProBarCODE/Hooks/hookProBarCODE';
import { IProBarCODE } from '../GerAdv_TS/ProBarCODE/Interfaces/interface.ProBarCODE';
import { IProBarCODEService } from '../GerAdv_TS/ProBarCODE/Services/ProBarCODE.service';
import { ProBarCODETestEmpty } from '../GerAdv_TS/Models/ProBarCODE';


// Mock do serviço
const mockProBarCODEService: jest.Mocked<IProBarCODEService> = {
  fetchProBarCODEById: jest.fn(),
  saveProBarCODE: jest.fn(),
  
  getAll: jest.fn(),
  deleteProBarCODE: jest.fn(),
  validateProBarCODE: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProBarCODE: IProBarCODE = { ...ProBarCODETestEmpty() };

describe('useProBarCODEForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    expect(result.current.data).toEqual(initialProBarCODE);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    const mockEvent = {
      target: {
        name: 'barcode',
        value: 'Novo Pro Bar C O D E',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.barcode).toBe('Novo Pro Bar C O D E');
  });

   test('deve carregar Pro Bar C O D E por ID', async () => {
    const mockProBarCODE = { ...initialProBarCODE, id: 1, barcode: 'Pro Bar C O D E Teste' };
    mockProBarCODEService.fetchProBarCODEById.mockResolvedValue(mockProBarCODE);

    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    await act(async () => {
      await result.current.loadProBarCODE(1);
    });

    expect(mockProBarCODEService.fetchProBarCODEById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProBarCODE);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Pro Bar C O D E', async () => {
    const errorMessage = 'Erro ao carregar Pro Bar C O D E';
    mockProBarCODEService.fetchProBarCODEById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    await act(async () => {
      await result.current.loadProBarCODE(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProBarCODE, barcode: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProBarCODE);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProBarCODEForm(initialProBarCODE, mockProBarCODEService)
    );

    await act(async () => {
      await result.current.loadProBarCODE(0);
    });

    expect(mockProBarCODEService.fetchProBarCODEById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProBarCODE);
  });
});

describe('useProBarCODEList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProBarCODEList(mockProBarCODEService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProBarCODE, id: 1, barcode: 'Pro Bar C O D E 1' },
      { ...initialProBarCODE, id: 2, barcode: 'Pro Bar C O D E 2' }
    ];
    mockProBarCODEService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProBarCODEList(mockProBarCODEService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProBarCODEService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProBarCODEService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProBarCODEList(mockProBarCODEService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProBarCODE, id: 1, barcode: 'Pro Bar C O D E Filtrado' }];
    const filtro = { barcode: 'Pro Bar C O D E' };
    mockProBarCODEService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProBarCODEList(mockProBarCODEService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProBarCODEService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProBarCODE', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProBarCODE());

    const validData = { ...initialProBarCODE, barcode: 'Pro Bar C O D E Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProBarCODE());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProBarCODE, id: 1, barcode: 'Pro Bar C O D E Teste' }];
    mockProBarCODEService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProBarCODEList(mockProBarCODEService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsProBarCODE()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});