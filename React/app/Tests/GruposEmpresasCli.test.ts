// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useGruposEmpresasCliForm, useGruposEmpresasCliList, useValidationsGruposEmpresasCli } from '../GerAdv_TS/GruposEmpresasCli/Hooks/hookGruposEmpresasCli';
import { IGruposEmpresasCli } from '../GerAdv_TS/GruposEmpresasCli/Interfaces/interface.GruposEmpresasCli';
import { IGruposEmpresasCliService } from '../GerAdv_TS/GruposEmpresasCli/Services/GruposEmpresasCli.service';
import { GruposEmpresasCliTestEmpty } from '../GerAdv_TS/Models/GruposEmpresasCli';


// Mock do serviço
const mockGruposEmpresasCliService: jest.Mocked<IGruposEmpresasCliService> = {
  fetchGruposEmpresasCliById: jest.fn(),
  saveGruposEmpresasCli: jest.fn(),
  
  getAll: jest.fn(),
  deleteGruposEmpresasCli: jest.fn(),
  validateGruposEmpresasCli: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialGruposEmpresasCli: IGruposEmpresasCli = { ...GruposEmpresasCliTestEmpty() };

describe('useGruposEmpresasCliForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    expect(result.current.data).toEqual(initialGruposEmpresasCli);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    const mockEvent = {
      target: {
        name: 'campo',
        value: 'Novo Grupos Empresas Cli',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.campo).toBe('Novo Grupos Empresas Cli');
  });

   test('deve carregar Grupos Empresas Cli por ID', async () => {
    const mockGruposEmpresasCli = { ...initialGruposEmpresasCli, id: 1, campo: 'Grupos Empresas Cli Teste' };
    mockGruposEmpresasCliService.fetchGruposEmpresasCliById.mockResolvedValue(mockGruposEmpresasCli);

    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.loadGruposEmpresasCli(1);
    });

    expect(mockGruposEmpresasCliService.fetchGruposEmpresasCliById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockGruposEmpresasCli);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Grupos Empresas Cli', async () => {
    const errorMessage = 'Erro ao carregar Grupos Empresas Cli';
    mockGruposEmpresasCliService.fetchGruposEmpresasCliById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.loadGruposEmpresasCli(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialGruposEmpresasCli, campo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialGruposEmpresasCli);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.loadGruposEmpresasCli(0);
    });

    expect(mockGruposEmpresasCliService.fetchGruposEmpresasCliById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialGruposEmpresasCli);
  });
});

describe('useGruposEmpresasCliList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliList(mockGruposEmpresasCliService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialGruposEmpresasCli, id: 1, campo: 'Grupos Empresas Cli 1' },
      { ...initialGruposEmpresasCli, id: 2, campo: 'Grupos Empresas Cli 2' }
    ];
    mockGruposEmpresasCliService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useGruposEmpresasCliList(mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockGruposEmpresasCliService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockGruposEmpresasCliService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useGruposEmpresasCliList(mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialGruposEmpresasCli, id: 1, campo: 'Grupos Empresas Cli Filtrado' }];
    const filtro = { campo: 'Grupos Empresas Cli' };
    mockGruposEmpresasCliService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useGruposEmpresasCliList(mockGruposEmpresasCliService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockGruposEmpresasCliService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsGruposEmpresasCli', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsGruposEmpresasCli());

    const validData = { ...initialGruposEmpresasCli, campo: 'Grupos Empresas Cli Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsGruposEmpresasCli());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialGruposEmpresasCli, id: 1, campo: 'Grupos Empresas Cli Teste' }];
    mockGruposEmpresasCliService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useGruposEmpresasCliList(mockGruposEmpresasCliService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsGruposEmpresasCli()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useGruposEmpresasCliForm(initialGruposEmpresasCli, mockGruposEmpresasCliService)
    );

    const mockEvent = {
      target: {
        name: 'oculto',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.oculto).toBe(true);
  });


