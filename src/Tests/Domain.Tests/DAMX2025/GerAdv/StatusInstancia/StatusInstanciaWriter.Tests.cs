// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class StatusInstanciaWriterTests
{
    private readonly Mock<IFStatusInstanciaFactory> _mockStatusInstanciaFactory;
    private readonly Mock<FStatusInstancia> _mockFStatusInstancia;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly StatusInstanciaWriter _statusinstanciaWriter;
    public StatusInstanciaWriterTests()
    {
        _mockStatusInstanciaFactory = new Mock<IFStatusInstanciaFactory>();
        _mockFStatusInstancia = new Mock<FStatusInstancia>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _statusinstanciaWriter = new StatusInstanciaWriter(_mockStatusInstanciaFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var statusinstancia = CreateValidStatusInstanciaModel();
        statusinstancia.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockStatusInstanciaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFStatusInstancia.Object);
        _mockFStatusInstancia.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _statusinstanciaWriter.WriteAsync(statusinstancia, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFStatusInstancia.Object);
        _mockStatusInstanciaFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockStatusInstanciaFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFStatusInstancia.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var statusinstancia = CreateValidStatusInstanciaModel();
        statusinstancia.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockStatusInstanciaFactory.Setup(x => x.CreateFromIdAsync(statusinstancia.Id, _mockConnection.Object)).ReturnsAsync(_mockFStatusInstancia.Object);
        _mockFStatusInstancia.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _statusinstanciaWriter.WriteAsync(statusinstancia, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFStatusInstancia.Object);
        _mockStatusInstanciaFactory.Verify(x => x.CreateFromIdAsync(statusinstancia.Id, _mockConnection.Object), Times.Once);
        _mockStatusInstanciaFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFStatusInstancia.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var statusinstancia = CreateValidStatusInstanciaModel();
        var auditorQuem = 99;
        _mockStatusInstanciaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFStatusInstancia.Object);
        _mockFStatusInstancia.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _statusinstanciaWriter.WriteAsync(statusinstancia, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFStatusInstancia.Object);
        _mockFStatusInstancia.VerifySet(x => x.FNome = statusinstancia.Nome, Times.Once);
        _mockFStatusInstancia.VerifySet(x => x.FGUID = statusinstancia.GUID, Times.Once);
        _mockFStatusInstancia.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var statusinstancia = CreateValidStatusInstanciaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockStatusInstanciaFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _statusinstanciaWriter.WriteAsync(statusinstancia, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var statusinstancia = CreateValidStatusInstanciaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockStatusInstanciaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFStatusInstancia.Object);
        _mockFStatusInstancia.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _statusinstanciaWriter.WriteAsync(statusinstancia, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var statusinstanciaResponse = new StatusInstanciaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockStatusInstanciaFactory.Setup(x => x.DeleteAsync(operadorId, statusinstanciaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _statusinstanciaWriter.Delete(statusinstanciaResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockStatusInstanciaFactory.Verify(x => x.DeleteAsync(operadorId, statusinstanciaResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var statusinstanciaResponse = new StatusInstanciaResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockStatusInstanciaFactory.Setup(x => x.DeleteAsync(operadorId, statusinstanciaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _statusinstanciaWriter.Delete(statusinstanciaResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var statusinstanciaResponse = new StatusInstanciaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockStatusInstanciaFactory.Setup(x => x.DeleteAsync(operadorId, statusinstanciaResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _statusinstanciaWriter.Delete(statusinstanciaResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new StatusInstanciaWriter(_mockStatusInstanciaFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new StatusInstanciaWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.StatusInstancia CreateValidStatusInstanciaModel()
    {
        return new Models.StatusInstancia
        {
            Id = 0,
            Nome = "João",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}