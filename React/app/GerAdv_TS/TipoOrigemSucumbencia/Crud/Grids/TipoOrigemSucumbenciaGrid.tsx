// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { TipoOrigemSucumbenciaEmpty } from '../../../Models/TipoOrigemSucumbencia';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { ITipoOrigemSucumbencia } from '../../Interfaces/interface.TipoOrigemSucumbencia';
import { TipoOrigemSucumbenciaService } from '../../Services/TipoOrigemSucumbencia.service';
import { TipoOrigemSucumbenciaApi } from '../../Apis/ApiTipoOrigemSucumbencia';
import { TipoOrigemSucumbenciaGridMobileComponent } from '../GridsMobile/TipoOrigemSucumbenciaMobile';
import { TipoOrigemSucumbenciaGridDesktopComponent } from '../GridsDesktop/TipoOrigemSucumbenciaDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterTipoOrigemSucumbencia } from '../../Filters/TipoOrigemSucumbencia';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import TipoOrigemSucumbenciaWindow from './TipoOrigemSucumbenciaWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useTipoOrigemSucumbenciaList } from '../../Hooks/hookTipoOrigemSucumbencia';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useTipoOrigemSucumbenciaFilter } from '../../Hooks/hookTipoOrigemSucumbenciaFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const TipoOrigemSucumbenciaGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedTipoOrigemSucumbencia, setSelectedTipoOrigemSucumbencia] = useState<ITipoOrigemSucumbencia>(TipoOrigemSucumbenciaEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterTipoOrigemSucumbencia | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [tipoorigemsucumbenciaData, setTipoOrigemSucumbenciaData] = useState<ITipoOrigemSucumbencia[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const tipoorigemsucumbenciaService = useMemo(() => {
    return new TipoOrigemSucumbenciaService(
    new TipoOrigemSucumbenciaApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: tipoorigemsucumbencia, 
  fetchData, 
  refreshData
} = useTipoOrigemSucumbenciaList(tipoorigemsucumbenciaService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterTipoOrigemSucumbencia | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('TipoOrigemSucumbenciaFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await tipoorigemsucumbenciaService.getAll(
  filtro ?? {} as FilterTipoOrigemSucumbencia, 
  onlineData => {
    setTipoOrigemSucumbenciaData(onlineData);
    setLoading(false);
  }
);
setTipoOrigemSucumbenciaData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterTipoOrigemSucumbencia;

    const filtroMem = sessionStorage.getItem(btoa('TipoOrigemSucumbenciaFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('TipoOrigemSucumbenciaFilter')) ?? '') || '{}') : {} as FilterTipoOrigemSucumbencia;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (tipoorigemsucumbencia: ITipoOrigemSucumbencia) => {
    setSelectedTipoOrigemSucumbencia(tipoorigemsucumbencia);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedTipoOrigemSucumbencia(TipoOrigemSucumbenciaEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const tipoorigemsucumbencia = e.dataItem;
    setDeleteId(tipoorigemsucumbencia.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await tipoorigemsucumbenciaService.deleteTipoOrigemSucumbencia(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao excluir');
        setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
      } finally {
      setDeleteId(null);
      setIsModalOpen(false);
    }
  }
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useTipoOrigemSucumbenciaFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <TipoOrigemSucumbenciaGridMobileComponent
        data={tipoorigemsucumbenciaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <TipoOrigemSucumbenciaGridDesktopComponent
        data={tipoorigemsucumbenciaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <TipoOrigemSucumbenciaWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedTipoOrigemSucumbencia={selectedTipoOrigemSucumbencia}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Tipo Origem Sucumbencia'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default TipoOrigemSucumbenciaGrid;