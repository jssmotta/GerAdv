// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoContatoCRMWindowId from '@/app/GerAdv_TS/TipoContatoCRM/Crud/Grids/TipoContatoCRMWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoContatoCRMWindowId from '@/app/GerAdv_TS/TipoContatoCRM/Crud/Grids/TipoContatoCRMWindowId';
import { TipoContatoCRMEmpty } from '@/app/GerAdv_TS/Models/TipoContatoCRM';

describe('TipoContatoCRMWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoContatoCRMWindowId).toBeDefined();
    expect(typeof TipoContatoCRMWindowId === 'function' || typeof TipoContatoCRMWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoContatoCRMService class so constructor usage doesn't throw and we can control fetchTipoContatoCRMById
const mockFetchTipoContatoCRMById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoContatoCRM/Services/TipoContatoCRM.service', () => ({
  TipoContatoCRMService: jest.fn().mockImplementation(() => ({
    fetchTipoContatoCRMById: (...args: any[]) => mockFetchTipoContatoCRMById(...args),
  })),
}));

// Mock the child TipoContatoCRMWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoContatoCRM/Crud/Grids/TipoContatoCRMWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipocontatocrm-window' {...p} />,
}));

describe('TipoContatoCRMWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoContatoCRMWindowId).toBeDefined();
    expect(typeof TipoContatoCRMWindowId === 'function' || typeof TipoContatoCRMWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoContatoCRMWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoContatoCRMWindow with TipoContatoCRMEmpty when id is 0', async () => {
    render(
      <TipoContatoCRMWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoContatoCRMWindow should be rendered with selectedTipoContatoCRM equal to TipoContatoCRMEmpty()
    const node = await screen.findByTestId('tipocontatocrm-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoContatoCRM"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipocontatocrm') || node.getAttribute('selectedTipoContatoCRM')).not.toBeNull();
  });

  it('fetches tipocontatocrm by id and passes it to TipoContatoCRMWindow when id > 0', async () => {
    const fakeTipoContatoCRM = { id: 123, nomecliente: 'Test' };
    mockFetchTipoContatoCRMById.mockResolvedValue(fakeTipoContatoCRM);

    render(
      <TipoContatoCRMWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipocontatocrm-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoContatoCRMById).toHaveBeenCalledWith(123));
  });
});
