// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class AreaValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFAreaFactory> _mockAreaFactory;
    private readonly Mock<IAreaReader> _mockReader;
    private readonly AreaValidation _validation;
    private readonly Mock<IAreaService> _mockAreaService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly AreaService _service;
    private readonly string _validUri = "test-uri";
    public AreaValidationTests()
    {
        _mockAreaFactory = new Mock<IFAreaFactory>();
        _mockReader = new Mock<IAreaReader>();
        _validation = new AreaValidation();
        _mockAreaService = new Mock<IAreaService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void AreaValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IAreaValidation>();
    }

    [Fact]
    public void AreaValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(AreaValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void AreaValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(AreaValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Area CreateValidArea()
    {
        return new Models.Area
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Top = false
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockAreaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterArea>(), It.IsAny<string>())).ReturnsAsync(new List<AreaResponseAll>());
    // Setup other mocks but don't override the Areas service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockAreaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterArea>(), It.IsAny<string>())).ReturnsAsync(new List<AreaResponseAll>());
    // Setup other mocks but don't override the Areas service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var area = new Models.Area
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(area, _mockAreaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockAreaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}