// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import FuncaoWindowId from '@/app/GerAdv_TS/Funcao/Crud/Grids/FuncaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import FuncaoWindowId from '@/app/GerAdv_TS/Funcao/Crud/Grids/FuncaoWindowId';
import { FuncaoEmpty } from '@/app/GerAdv_TS/Models/Funcao';

describe('FuncaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(FuncaoWindowId).toBeDefined();
    expect(typeof FuncaoWindowId === 'function' || typeof FuncaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the FuncaoService class so constructor usage doesn't throw and we can control fetchFuncaoById
const mockFetchFuncaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Funcao/Services/Funcao.service', () => ({
  FuncaoService: jest.fn().mockImplementation(() => ({
    fetchFuncaoById: (...args: any[]) => mockFetchFuncaoById(...args),
  })),
}));

// Mock the child FuncaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Funcao/Crud/Grids/FuncaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='funcao-window' {...p} />,
}));

describe('FuncaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(FuncaoWindowId).toBeDefined();
    expect(typeof FuncaoWindowId === 'function' || typeof FuncaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <FuncaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders FuncaoWindow with FuncaoEmpty when id is 0', async () => {
    render(
      <FuncaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // FuncaoWindow should be rendered with selectedFuncao equal to FuncaoEmpty()
    const node = await screen.findByTestId('funcao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedFuncao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedfuncao') || node.getAttribute('selectedFuncao')).not.toBeNull();
  });

  it('fetches funcao by id and passes it to FuncaoWindow when id > 0', async () => {
    const fakeFuncao = { id: 123, nomecliente: 'Test' };
    mockFetchFuncaoById.mockResolvedValue(fakeFuncao);

    render(
      <FuncaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('funcao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchFuncaoById).toHaveBeenCalledWith(123));
  });
});
