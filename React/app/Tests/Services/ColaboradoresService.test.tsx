// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ColaboradoresService, ColaboradoresValidator } from '@/app/GerAdv_TS/Colaboradores/Services/Colaboradores.service';
import { ColaboradoresApi, ColaboradoresApiError } from '@/app/GerAdv_TS/Colaboradores/Apis/ApiColaboradores';
import { IColaboradores } from '@/app/GerAdv_TS/Colaboradores/Interfaces/interface.Colaboradores';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ColaboradoresApi;

const sample: IColaboradores = { id: 1, nome: 'Teste' };

describe('ColaboradoresService', () => {
  let service: ColaboradoresService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ColaboradoresService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ColaboradoresValidator, 'validateColaboradores').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchColaboradoresById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchColaboradoresById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchColaboradoresById lança ColaboradoresApiError para id inválido', async () => {
    await expect(service.fetchColaboradoresById(0)).rejects.toBeInstanceOf(ColaboradoresApiError);
  });

  it('saveColaboradores chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveColaboradores(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveColaboradores lança ColaboradoresApiError quando validação falha', async () => {
    (ColaboradoresValidator.validateColaboradores as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveColaboradores(sample)).rejects.toBeInstanceOf(ColaboradoresApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteColaboradores chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteColaboradores(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteColaboradores lança ColaboradoresApiError para id inválido', async () => {
    await expect(service.deleteColaboradores(0)).rejects.toBeInstanceOf(ColaboradoresApiError);
  });

  it('validateColaboradores delega para ColaboradoresValidator', () => {
    (ColaboradoresValidator.validateColaboradores as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateColaboradores(sample);
    expect(r.isValid).toBe(true);
  });
});
