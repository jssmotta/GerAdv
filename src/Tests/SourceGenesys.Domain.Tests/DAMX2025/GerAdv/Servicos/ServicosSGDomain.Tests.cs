// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBServicos - Nome da classe (ex: DBClientes)
/// Servicos - Nome da tabela (ex: Clientes)
/// ser - Prefixo dos campos (ex: cli)
/// serCodigo - Nome do campo código (ex: cliCodigo)
/// serDescricao - Nome do campo nome (ex: cliNome)
/// DBServicos.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBServicosTests : IDisposable
{
    private readonly DBServicos _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBServicosTests()
    {
        _instance = new DBServicos();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("serCodigo", typeof(int));
        dt.Columns.Add("serQuemCad", typeof(int));
        dt.Columns.Add("serDtCad", typeof(DateTime));
        dt.Columns.Add("serQuemAtu", typeof(int));
        dt.Columns.Add("serDtAtu", typeof(DateTime));
        dt.Columns.Add("serVisto", typeof(bool));
        dt.Columns.Add("serCobrar", typeof(string));
        dt.Columns.Add("serDescricao", typeof(string));
        dt.Columns.Add("serBasico", typeof(string));
        dt.Columns.Add("serGUID", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("Servicos", DBServicos.PTabelaNome);
        Assert.Equal(" Servicos.* ", DBServicos.CamposSqlX);
        Assert.Equal("serCodigo", DBServicos.CampoCodigo);
        Assert.Equal("serDescricao", DBServicos.CampoNome);
        Assert.Equal("ser", DBServicos.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBServicos();
        Assert.Equal(0, instance.ID);
        Assert.Equal("Servicos", instance.ITabelaName());
        Assert.Equal("ser", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBServicos((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBServicos((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["serCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBServicos(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("Servicos", cadastro.ITabelaName());
        Assert.Equal("serCodigo", cadastro.ICampoCodigo());
        Assert.Equal("serDescricao", cadastro.ICampoNome());
        Assert.Equal("ser", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBServicos.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBServicos.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBServicos.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBServicos();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBServicos();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData(true)]
    [InlineData(false)]
    public void Cobrar_ShouldAcceptBooleanValues(bool value)
    {
        _instance.FCobrar = value;
        Assert.Equal(value, _instance.FCobrar);
    }

    [Fact]
    public void Cobrar_DefaultValue_ShouldBeFalse()
    {
        var instance = new DBServicos();
        Assert.False(instance.FCobrar);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Descricao_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FDescricao = input;
        Assert.Equal(expected, _instance.FDescricao);
    }

    [Fact]
    public void Descricao_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 200 + 10);
        _instance.FDescricao = longString;
        Assert.True(_instance.FDescricao.Length <= 200);
    }

    [Theory]
    [InlineData(true)]
    [InlineData(false)]
    public void Basico_ShouldAcceptBooleanValues(bool value)
    {
        _instance.FBasico = value;
        Assert.Equal(value, _instance.FBasico);
    }

    [Fact]
    public void Basico_DefaultValue_ShouldBeFalse()
    {
        var instance = new DBServicos();
        Assert.False(instance.FBasico);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void GUID_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FGUID = input;
        Assert.Equal(expected, _instance.FGUID);
    }

    [Fact]
    public void GUID_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 100 + 10);
        _instance.FGUID = longString;
        Assert.True(_instance.FGUID.Length <= 100);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}