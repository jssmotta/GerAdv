// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IProcessOutputEngineService } from '../Services/ProcessOutputEngine.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IProcessOutputEngine } from '../Interfaces/interface.ProcessOutputEngine';
import { FilterProcessOutputEngine } from '../Filters/ProcessOutputEngine';
import { isValidDate } from '@/app/tools/datetime';
import { ProcessOutputEngineApi } from '../Apis/ApiProcessOutputEngine';

export const useProcessOutputEngineForm = (
  initialProcessOutputEngine: IProcessOutputEngine,
  dataService: IProcessOutputEngineService
) => {
  const [data, setData] = useState<IProcessOutputEngine>(initialProcessOutputEngine);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadProcessOutputEngine = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialProcessOutputEngine);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchProcessOutputEngineById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Process Output Engine';
      setError(errorMessage);
      //console.log('Erro ao carregar Process Output Engine');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialProcessOutputEngine]);

  const resetForm = useCallback(() => {
    setData(initialProcessOutputEngine);
    setError(null);
  }, [initialProcessOutputEngine]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadProcessOutputEngine,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadProcessOutputEngine, resetForm]);
  return returnValue;
};


export const useProcessOutputEngineNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('ProcessOutputEngine', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useProcessOutputEngineList = (dataService: IProcessOutputEngineService, currentFilter?: FilterProcessOutputEngine | undefined | null) => {
  const [data, setData] = useState<IProcessOutputEngine[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterProcessOutputEngine | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterProcessOutputEngine | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterProcessOutputEngine);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar processoutputengine';
      setError(errorMessage);
      //console.log('Erro ao carregar processoutputengine');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useProcessOutputEngineNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsProcessOutputEngine() {

  async function runValidation(data: IProcessOutputEngine, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const processoutputengineApi = new ProcessOutputEngineApi(uri ?? '', token ?? '');

    const result = await processoutputengineApi.validation(data);

    return result;
  }

  function validate(data: IProcessOutputEngine): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.nome.length <= 0) { 
                                             return { isValid: false, message: 'O campo Nome não pode ficar vazio.' };
                                         } 
if (data.nome.length > 255) { 
                                             return { isValid: false, message: 'O campo Nome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.database.length > 255) { 
                                             return { isValid: false, message: 'O campo Database não pode ter mais de 255 caracteres.' };
                                         } 
if (data.tabela.length > 255) { 
                                             return { isValid: false, message: 'O campo Tabela não pode ter mais de 255 caracteres.' };
                                         } 
if (data.campo.length > 255) { 
                                             return { isValid: false, message: 'O campo Campo não pode ter mais de 255 caracteres.' };
                                         } 
if (data.valor.length > 255) { 
                                             return { isValid: false, message: 'O campo Valor não pode ter mais de 255 caracteres.' };
                                         } 
if (data.output.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Output não pode ter mais de 2147483647 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}export const useProcessOutputEngineComboBox = (
  dataService: IProcessOutputEngineService,
  initialValue?: any
) => {
  const [options, setOptions] = useState<any[]>([]);
  const [filteredOptions, setFilteredOptions] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [selectedValue, setSelectedValue] = useState(initialValue);
  const [hasLoaded, setHasLoaded] = useState(false);

  const fetchOptions = useCallback(async () => {
    if (loading) return; // Evita múltiplas requisições simultâneas
    
    setLoading(true);
    try {
      const response = await dataService.getList();
      const mappedOptions = response.map(item => ({
        id: item.id,
        nome: item.nome
      }));
      setOptions(mappedOptions);
      setFilteredOptions(mappedOptions);
      setHasLoaded(true);
    } catch (err) {
      //console.log('Erro ao buscar opções do ComboBox');
    } finally {
      setLoading(false);
    }
  }, [dataService, loading]);

  const handleFilter = useCallback((filterText: string) => {
    if (!filterText) {
      setFilteredOptions(options);
      return;
    }
    
    const filter = filterText.toLowerCase();
    const filtered = options.filter(option =>
      option.nome.toLowerCase().includes(filter)
    );
    setFilteredOptions(filtered);
  }, [options]);

  const handleValueChange = useCallback((newValue: any) => {
    setSelectedValue(newValue);
  }, []);
  
  useEffect(() => {
    if (!hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  const refreshCallback = useCallback(() => {
    if (hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  useProcessOutputEngineNotifications(
    refreshCallback, // onUpdate
    refreshCallback, // onDelete
    refreshCallback  // onAdd
  );

  return {
    options: filteredOptions,
    loading,
    selectedValue,
    handleFilter,
    handleValueChange,
    refreshOptions: fetchOptions,
    clearValue: () => setSelectedValue(null)
  };
};