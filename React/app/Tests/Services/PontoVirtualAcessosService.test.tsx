// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { PontoVirtualAcessosService, PontoVirtualAcessosValidator } from '@/app/GerAdv_TS/PontoVirtualAcessos/Services/PontoVirtualAcessos.service';
import { PontoVirtualAcessosApi, PontoVirtualAcessosApiError } from '@/app/GerAdv_TS/PontoVirtualAcessos/Apis/ApiPontoVirtualAcessos';
import { IPontoVirtualAcessos } from '@/app/GerAdv_TS/PontoVirtualAcessos/Interfaces/interface.PontoVirtualAcessos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as PontoVirtualAcessosApi;

const sample: IPontoVirtualAcessos = { id: 1, nome: 'Teste' };

describe('PontoVirtualAcessosService', () => {
  let service: PontoVirtualAcessosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new PontoVirtualAcessosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(PontoVirtualAcessosValidator, 'validatePontoVirtualAcessos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchPontoVirtualAcessosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchPontoVirtualAcessosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchPontoVirtualAcessosById lança PontoVirtualAcessosApiError para id inválido', async () => {
    await expect(service.fetchPontoVirtualAcessosById(0)).rejects.toBeInstanceOf(PontoVirtualAcessosApiError);
  });

  it('savePontoVirtualAcessos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.savePontoVirtualAcessos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('savePontoVirtualAcessos lança PontoVirtualAcessosApiError quando validação falha', async () => {
    (PontoVirtualAcessosValidator.validatePontoVirtualAcessos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.savePontoVirtualAcessos(sample)).rejects.toBeInstanceOf(PontoVirtualAcessosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deletePontoVirtualAcessos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deletePontoVirtualAcessos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deletePontoVirtualAcessos lança PontoVirtualAcessosApiError para id inválido', async () => {
    await expect(service.deletePontoVirtualAcessos(0)).rejects.toBeInstanceOf(PontoVirtualAcessosApiError);
  });

  it('validatePontoVirtualAcessos delega para PontoVirtualAcessosValidator', () => {
    (PontoVirtualAcessosValidator.validatePontoVirtualAcessos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validatePontoVirtualAcessos(sample);
    expect(r.isValid).toBe(true);
  });
});
