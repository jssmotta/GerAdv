// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { TipoContatoCRMEmpty } from '../../../Models/TipoContatoCRM';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { ITipoContatoCRM } from '../../Interfaces/interface.TipoContatoCRM';
import { TipoContatoCRMService } from '../../Services/TipoContatoCRM.service';
import { TipoContatoCRMApi } from '../../Apis/ApiTipoContatoCRM';
import { TipoContatoCRMGridMobileComponent } from '../GridsMobile/TipoContatoCRM';
import { TipoContatoCRMGridDesktopComponent } from '../GridsDesktop/TipoContatoCRM';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterTipoContatoCRM } from '../../Filters/TipoContatoCRM';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import TipoContatoCRMWindow from './TipoContatoCRMWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useTipoContatoCRMList } from '../../Hooks/hookTipoContatoCRM';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useTipoContatoCRMFilter } from '../../Hooks/hookTipoContatoCRMFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const TipoContatoCRMGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedTipoContatoCRM, setSelectedTipoContatoCRM] = useState<ITipoContatoCRM>(TipoContatoCRMEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterTipoContatoCRM | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [tipocontatocrmData, setTipoContatoCRMData] = useState<ITipoContatoCRM[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const tipocontatocrmService = useMemo(() => {
    return new TipoContatoCRMService(
    new TipoContatoCRMApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: tipocontatocrm, 
  fetchData, 
  refreshData
} = useTipoContatoCRMList(tipocontatocrmService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterTipoContatoCRM | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('TipoContatoCRMFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await tipocontatocrmService.getAll(
  filtro ?? {} as FilterTipoContatoCRM, 
  onlineData => {
    setTipoContatoCRMData(onlineData);
    setLoading(false);
  }
);
setTipoContatoCRMData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterTipoContatoCRM;

    const filtroMem = sessionStorage.getItem(btoa('TipoContatoCRMFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('TipoContatoCRMFilter')) ?? '') || '{}') : {} as FilterTipoContatoCRM;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (tipocontatocrm: ITipoContatoCRM) => {
    setSelectedTipoContatoCRM(tipocontatocrm);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedTipoContatoCRM(TipoContatoCRMEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const tipocontatocrm = e.dataItem;
    setDeleteId(tipocontatocrm.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await tipocontatocrmService.deleteTipoContatoCRM(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useTipoContatoCRMFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <TipoContatoCRMGridMobileComponent
        data={tipocontatocrmData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <TipoContatoCRMGridDesktopComponent
        data={tipocontatocrmData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <TipoContatoCRMWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedTipoContatoCRM={selectedTipoContatoCRM}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Tipo Contato C R M'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default TipoContatoCRMGrid;