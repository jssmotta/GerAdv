// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ContaCorrenteService, ContaCorrenteValidator } from '@/app/GerAdv_TS/ContaCorrente/Services/ContaCorrente.service';
import { ContaCorrenteApi, ContaCorrenteApiError } from '@/app/GerAdv_TS/ContaCorrente/Apis/ApiContaCorrente';
import { IContaCorrente } from '@/app/GerAdv_TS/ContaCorrente/Interfaces/interface.ContaCorrente';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ContaCorrenteApi;

const sample: IContaCorrente = { id: 1, nome: 'Teste' };

describe('ContaCorrenteService', () => {
  let service: ContaCorrenteService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ContaCorrenteService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ContaCorrenteValidator, 'validateContaCorrente').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchContaCorrenteById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchContaCorrenteById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchContaCorrenteById lança ContaCorrenteApiError para id inválido', async () => {
    await expect(service.fetchContaCorrenteById(0)).rejects.toBeInstanceOf(ContaCorrenteApiError);
  });

  it('saveContaCorrente chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveContaCorrente(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveContaCorrente lança ContaCorrenteApiError quando validação falha', async () => {
    (ContaCorrenteValidator.validateContaCorrente as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveContaCorrente(sample)).rejects.toBeInstanceOf(ContaCorrenteApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteContaCorrente chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteContaCorrente(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteContaCorrente lança ContaCorrenteApiError para id inválido', async () => {
    await expect(service.deleteContaCorrente(0)).rejects.toBeInstanceOf(ContaCorrenteApiError);
  });

  it('validateContaCorrente delega para ContaCorrenteValidator', () => {
    (ContaCorrenteValidator.validateContaCorrente as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateContaCorrente(sample);
    expect(r.isValid).toBe(true);
  });
});
