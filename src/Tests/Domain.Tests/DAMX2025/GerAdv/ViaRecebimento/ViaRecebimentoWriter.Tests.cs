// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class ViaRecebimentoWriterTests
{
    private readonly Mock<IFViaRecebimentoFactory> _mockViaRecebimentoFactory;
    private readonly Mock<FViaRecebimento> _mockFViaRecebimento;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly ViaRecebimentoWriter _viarecebimentoWriter;
    public ViaRecebimentoWriterTests()
    {
        _mockViaRecebimentoFactory = new Mock<IFViaRecebimentoFactory>();
        _mockFViaRecebimento = new Mock<FViaRecebimento>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _viarecebimentoWriter = new ViaRecebimentoWriter(_mockViaRecebimentoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var viarecebimento = CreateValidViaRecebimentoModel();
        viarecebimento.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockViaRecebimentoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFViaRecebimento.Object);
        _mockFViaRecebimento.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _viarecebimentoWriter.WriteAsync(viarecebimento, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFViaRecebimento.Object);
        _mockViaRecebimentoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockViaRecebimentoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFViaRecebimento.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var viarecebimento = CreateValidViaRecebimentoModel();
        viarecebimento.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockViaRecebimentoFactory.Setup(x => x.CreateFromIdAsync(viarecebimento.Id, _mockConnection.Object)).ReturnsAsync(_mockFViaRecebimento.Object);
        _mockFViaRecebimento.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _viarecebimentoWriter.WriteAsync(viarecebimento, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFViaRecebimento.Object);
        _mockViaRecebimentoFactory.Verify(x => x.CreateFromIdAsync(viarecebimento.Id, _mockConnection.Object), Times.Once);
        _mockViaRecebimentoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFViaRecebimento.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var viarecebimento = CreateValidViaRecebimentoModel();
        var auditorQuem = 99;
        _mockViaRecebimentoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFViaRecebimento.Object);
        _mockFViaRecebimento.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _viarecebimentoWriter.WriteAsync(viarecebimento, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFViaRecebimento.Object);
        _mockFViaRecebimento.VerifySet(x => x.FNome = viarecebimento.Nome, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var viarecebimento = CreateValidViaRecebimentoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockViaRecebimentoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _viarecebimentoWriter.WriteAsync(viarecebimento, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var viarecebimento = CreateValidViaRecebimentoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockViaRecebimentoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFViaRecebimento.Object);
        _mockFViaRecebimento.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _viarecebimentoWriter.WriteAsync(viarecebimento, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var viarecebimentoResponse = new ViaRecebimentoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockViaRecebimentoFactory.Setup(x => x.DeleteAsync(operadorId, viarecebimentoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _viarecebimentoWriter.Delete(viarecebimentoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockViaRecebimentoFactory.Verify(x => x.DeleteAsync(operadorId, viarecebimentoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var viarecebimentoResponse = new ViaRecebimentoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockViaRecebimentoFactory.Setup(x => x.DeleteAsync(operadorId, viarecebimentoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _viarecebimentoWriter.Delete(viarecebimentoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var viarecebimentoResponse = new ViaRecebimentoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockViaRecebimentoFactory.Setup(x => x.DeleteAsync(operadorId, viarecebimentoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _viarecebimentoWriter.Delete(viarecebimentoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new ViaRecebimentoWriter(_mockViaRecebimentoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new ViaRecebimentoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.ViaRecebimento CreateValidViaRecebimentoModel()
    {
        return new Models.ViaRecebimento
        {
            Id = 0,
            Nome = "João"
        };
    }
#endregion
}