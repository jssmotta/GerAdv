// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ClientesSociosWindowId from '@/app/GerAdv_TS/ClientesSocios/Crud/Grids/ClientesSociosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ClientesSociosWindowId from '@/app/GerAdv_TS/ClientesSocios/Crud/Grids/ClientesSociosWindowId';
import { ClientesSociosEmpty } from '@/app/GerAdv_TS/Models/ClientesSocios';

describe('ClientesSociosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ClientesSociosWindowId).toBeDefined();
    expect(typeof ClientesSociosWindowId === 'function' || typeof ClientesSociosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ClientesSociosService class so constructor usage doesn't throw and we can control fetchClientesSociosById
const mockFetchClientesSociosById = jest.fn();
jest.mock('@/app/GerAdv_TS/ClientesSocios/Services/ClientesSocios.service', () => ({
  ClientesSociosService: jest.fn().mockImplementation(() => ({
    fetchClientesSociosById: (...args: any[]) => mockFetchClientesSociosById(...args),
  })),
}));

// Mock the child ClientesSociosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ClientesSocios/Crud/Grids/ClientesSociosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='clientessocios-window' {...p} />,
}));

describe('ClientesSociosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ClientesSociosWindowId).toBeDefined();
    expect(typeof ClientesSociosWindowId === 'function' || typeof ClientesSociosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ClientesSociosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ClientesSociosWindow with ClientesSociosEmpty when id is 0', async () => {
    render(
      <ClientesSociosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ClientesSociosWindow should be rendered with selectedClientesSocios equal to ClientesSociosEmpty()
    const node = await screen.findByTestId('clientessocios-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedClientesSocios"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedclientessocios') || node.getAttribute('selectedClientesSocios')).not.toBeNull();
  });

  it('fetches clientessocios by id and passes it to ClientesSociosWindow when id > 0', async () => {
    const fakeClientesSocios = { id: 123, nomecliente: 'Test' };
    mockFetchClientesSociosById.mockResolvedValue(fakeClientesSocios);

    render(
      <ClientesSociosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('clientessocios-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchClientesSociosById).toHaveBeenCalledWith(123));
  });
});
