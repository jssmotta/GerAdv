// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { EventoPrazoAgendaService, EventoPrazoAgendaValidator } from '@/app/GerAdv_TS/EventoPrazoAgenda/Services/EventoPrazoAgenda.service';
import { EventoPrazoAgendaApi, EventoPrazoAgendaApiError } from '@/app/GerAdv_TS/EventoPrazoAgenda/Apis/ApiEventoPrazoAgenda';
import { IEventoPrazoAgenda } from '@/app/GerAdv_TS/EventoPrazoAgenda/Interfaces/interface.EventoPrazoAgenda';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as EventoPrazoAgendaApi;

const sample: IEventoPrazoAgenda = { id: 1, nome: 'Teste' };

describe('EventoPrazoAgendaService', () => {
  let service: EventoPrazoAgendaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new EventoPrazoAgendaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(EventoPrazoAgendaValidator, 'validateEventoPrazoAgenda').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchEventoPrazoAgendaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchEventoPrazoAgendaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchEventoPrazoAgendaById lança EventoPrazoAgendaApiError para id inválido', async () => {
    await expect(service.fetchEventoPrazoAgendaById(0)).rejects.toBeInstanceOf(EventoPrazoAgendaApiError);
  });

  it('saveEventoPrazoAgenda chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveEventoPrazoAgenda(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveEventoPrazoAgenda lança EventoPrazoAgendaApiError quando validação falha', async () => {
    (EventoPrazoAgendaValidator.validateEventoPrazoAgenda as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveEventoPrazoAgenda(sample)).rejects.toBeInstanceOf(EventoPrazoAgendaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteEventoPrazoAgenda chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteEventoPrazoAgenda(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteEventoPrazoAgenda lança EventoPrazoAgendaApiError para id inválido', async () => {
    await expect(service.deleteEventoPrazoAgenda(0)).rejects.toBeInstanceOf(EventoPrazoAgendaApiError);
  });

  it('validateEventoPrazoAgenda delega para EventoPrazoAgendaValidator', () => {
    (EventoPrazoAgendaValidator.validateEventoPrazoAgenda as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateEventoPrazoAgenda(sample);
    expect(r.isValid).toBe(true);
  });
});
