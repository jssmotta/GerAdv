// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

using MenphisSI.GerAdv.AI.Filters;

public partial class ServiceFilter
{
    public (string where, List<SqlParameter> parametros)? WFiltroLivroCaixa(Filters.FilterLivroCaixa? filtro)
    {
        if (filtro == null)
            return null;
        var parameters = new List<SqlParameter>();
        if (filtro.Previsto != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Previsto)}", filtro.Previsto));
        }

        if (filtro.QuemCad != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.QuemCad)}", filtro.QuemCad));
            if (filtro.QuemCad_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.QuemCad)}_end", filtro.QuemCad_end));
            }
        }

        if (!filtro.DtCad.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtCad, out var dataParam1))
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.DtCad)}", dataParam1));
            if (!filtro.DtCad_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtCad_end, out var dataParam2))
                    parameters.Add(new($"@{(DBLivroCaixaDicInfo.DtCad)}_end", dataParam2));
            }
        }

        if (filtro.QuemAtu != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.QuemAtu)}", filtro.QuemAtu));
            if (filtro.QuemAtu_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.QuemAtu)}_end", filtro.QuemAtu_end));
            }
        }

        if (!filtro.DtAtu.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtAtu, out var dataParam3))
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.DtAtu)}", dataParam3));
            if (!filtro.DtAtu_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtAtu_end, out var dataParam4))
                    parameters.Add(new($"@{(DBLivroCaixaDicInfo.DtAtu)}_end", dataParam4));
            }
        }

        if (filtro.Visto != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Visto)}", filtro.Visto));
        }

        if (filtro.IDDes != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDDes)}", filtro.IDDes));
            if (filtro.IDDes_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDDes)}_end", filtro.IDDes_end));
            }
        }

        if (filtro.Pessoal != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Pessoal)}", filtro.Pessoal));
            if (filtro.Pessoal_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.Pessoal)}_end", filtro.Pessoal_end));
            }
        }

        if (filtro.Ajuste != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Ajuste)}", filtro.Ajuste));
        }

        if (filtro.IDHon != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDHon)}", filtro.IDHon));
            if (filtro.IDHon_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDHon)}_end", filtro.IDHon_end));
            }
        }

        if (filtro.IDHonParc != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDHonParc)}", filtro.IDHonParc));
            if (filtro.IDHonParc_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDHonParc)}_end", filtro.IDHonParc_end));
            }
        }

        if (filtro.IDHonSuc != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.IDHonSuc)}", filtro.IDHonSuc));
        }

        if (!filtro.Data.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.Data, out var dataParam5))
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.Data)}", dataParam5));
            if (!filtro.Data_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.Data_end, out var dataParam6))
                    parameters.Add(new($"@{(DBLivroCaixaDicInfo.Data)}_end", dataParam6));
            }
        }

        if (filtro.Processo != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Processo)}", filtro.Processo));
            if (filtro.Processo_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.Processo)}_end", filtro.Processo_end));
            }
        }

        if (filtro.Valor != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Valor)}", filtro.Valor));
            if (filtro.Valor_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.Valor)}_end", filtro.Valor_end));
            }
        }

        if (filtro.Tipo != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Tipo)}", filtro.Tipo));
        }

        if (!string.IsNullOrWhiteSpace(filtro.Historico))
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Historico)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Historico)));
        }

        if (filtro.Grupo != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.Grupo)}", filtro.Grupo));
            if (filtro.Grupo_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.Grupo)}_end", filtro.Grupo_end));
            }
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{(DBLivroCaixaDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
            if (filtro.Codigo_filtro_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBLivroCaixaDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
            }
        }

        if (filtro.LogicalOperator.IsEmptyX() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var cWhere = new StringBuilder();
        cWhere.Append(filtro.Previsto == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Previsto}] = @{(DBLivroCaixaDicInfo.Previsto)}");
        if (!(filtro.QuemCad.IsEmptyX()) && filtro.QuemCad_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemCad.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemCad}] = @{(DBLivroCaixaDicInfo.QuemCad)}");
        }
        else if (!(filtro.QuemCad.IsEmptyX()) && !(filtro.QuemCad_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.QuemCad} BETWEEN @{(DBLivroCaixaDicInfo.QuemCad)} AND @{(DBLivroCaixaDicInfo.QuemCad)}_end");
        }

        if (!(filtro.DtCad.IsEmptyDX()) && filtro.DtCad_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtCad.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.DtCad}], 103) = CONVERT(DATE, @{(DBLivroCaixaDicInfo.DtCad)}, 103)");
        }
        else if (!(filtro.DtCad.IsEmptyDX()) && !(filtro.DtCad_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.DtCad} BETWEEN @{(DBLivroCaixaDicInfo.DtCad)} AND @{(DBLivroCaixaDicInfo.DtCad)}_end");
        }

        if (!(filtro.QuemAtu.IsEmptyX()) && filtro.QuemAtu_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemAtu.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemAtu}] = @{(DBLivroCaixaDicInfo.QuemAtu)}");
        }
        else if (!(filtro.QuemAtu.IsEmptyX()) && !(filtro.QuemAtu_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.QuemAtu} BETWEEN @{(DBLivroCaixaDicInfo.QuemAtu)} AND @{(DBLivroCaixaDicInfo.QuemAtu)}_end");
        }

        if (!(filtro.DtAtu.IsEmptyDX()) && filtro.DtAtu_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtAtu.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.DtAtu}], 103) = CONVERT(DATE, @{(DBLivroCaixaDicInfo.DtAtu)}, 103)");
        }
        else if (!(filtro.DtAtu.IsEmptyDX()) && !(filtro.DtAtu_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.DtAtu} BETWEEN @{(DBLivroCaixaDicInfo.DtAtu)} AND @{(DBLivroCaixaDicInfo.DtAtu)}_end");
        }

        cWhere.Append(filtro.Visto == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Visto}] = @{(DBLivroCaixaDicInfo.Visto)}");
        if (!(filtro.IDDes.IsEmptyX()) && filtro.IDDes_end.IsEmptyX())
        {
            cWhere.Append(filtro.IDDes.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDDes}] = @{(DBLivroCaixaDicInfo.IDDes)}");
        }
        else if (!(filtro.IDDes.IsEmptyX()) && !(filtro.IDDes_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.IDDes} BETWEEN @{(DBLivroCaixaDicInfo.IDDes)} AND @{(DBLivroCaixaDicInfo.IDDes)}_end");
        }

        if (!(filtro.Pessoal.IsEmptyX()) && filtro.Pessoal_end.IsEmptyX())
        {
            cWhere.Append(filtro.Pessoal.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Pessoal}] = @{(DBLivroCaixaDicInfo.Pessoal)}");
        }
        else if (!(filtro.Pessoal.IsEmptyX()) && !(filtro.Pessoal_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.Pessoal} BETWEEN @{(DBLivroCaixaDicInfo.Pessoal)} AND @{(DBLivroCaixaDicInfo.Pessoal)}_end");
        }

        cWhere.Append(filtro.Ajuste == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Ajuste}] = @{(DBLivroCaixaDicInfo.Ajuste)}");
        if (!(filtro.IDHon.IsEmptyX()) && filtro.IDHon_end.IsEmptyX())
        {
            cWhere.Append(filtro.IDHon.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHon}] = @{(DBLivroCaixaDicInfo.IDHon)}");
        }
        else if (!(filtro.IDHon.IsEmptyX()) && !(filtro.IDHon_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.IDHon} BETWEEN @{(DBLivroCaixaDicInfo.IDHon)} AND @{(DBLivroCaixaDicInfo.IDHon)}_end");
        }

        if (!(filtro.IDHonParc.IsEmptyX()) && filtro.IDHonParc_end.IsEmptyX())
        {
            cWhere.Append(filtro.IDHonParc.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonParc}] = @{(DBLivroCaixaDicInfo.IDHonParc)}");
        }
        else if (!(filtro.IDHonParc.IsEmptyX()) && !(filtro.IDHonParc_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.IDHonParc} BETWEEN @{(DBLivroCaixaDicInfo.IDHonParc)} AND @{(DBLivroCaixaDicInfo.IDHonParc)}_end");
        }

        cWhere.Append(filtro.IDHonSuc == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonSuc}] = @{(DBLivroCaixaDicInfo.IDHonSuc)}");
        if (!(filtro.Data.IsEmptyDX()) && filtro.Data_end.IsEmptyDX())
        {
            cWhere.Append(filtro.Data.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Data}], 103) = CONVERT(DATE, @{(DBLivroCaixaDicInfo.Data)}, 103)");
        }
        else if (!(filtro.Data.IsEmptyDX()) && !(filtro.Data_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.Data} BETWEEN @{(DBLivroCaixaDicInfo.Data)} AND @{(DBLivroCaixaDicInfo.Data)}_end");
        }

        if (!(filtro.Processo.IsEmptyX()) && filtro.Processo_end.IsEmptyX())
        {
            cWhere.Append(filtro.Processo.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Processo}] = @{(DBLivroCaixaDicInfo.Processo)}");
        }
        else if (!(filtro.Processo.IsEmptyX()) && !(filtro.Processo_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.Processo} BETWEEN @{(DBLivroCaixaDicInfo.Processo)} AND @{(DBLivroCaixaDicInfo.Processo)}_end");
        }

        if (!(filtro.Valor == int.MinValue) && filtro.Valor_end == int.MinValue)
        {
            cWhere.Append(filtro.Valor == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Valor}] = @{(DBLivroCaixaDicInfo.Valor)}");
        }
        else if (!(filtro.Valor == int.MinValue) && !(filtro.Valor_end == int.MinValue))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.Valor} BETWEEN @{(DBLivroCaixaDicInfo.Valor)} AND @{(DBLivroCaixaDicInfo.Valor)}_end");
        }

        cWhere.Append(filtro.Tipo == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Tipo}] = @{(DBLivroCaixaDicInfo.Tipo)}");
        cWhere.Append(filtro.Historico.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Historico}]  {DevourerConsts.MsiCollate} like @{(DBLivroCaixaDicInfo.Historico)}");
        if (!(filtro.Grupo.IsEmptyX()) && filtro.Grupo_end.IsEmptyX())
        {
            cWhere.Append(filtro.Grupo.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Grupo}] = @{(DBLivroCaixaDicInfo.Grupo)}");
        }
        else if (!(filtro.Grupo.IsEmptyX()) && !(filtro.Grupo_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.Grupo} BETWEEN @{(DBLivroCaixaDicInfo.Grupo)} AND @{(DBLivroCaixaDicInfo.Grupo)}_end");
        }

        if (!(filtro.Codigo_filtro.IsEmptyX()) && filtro.Codigo_filtro_end.IsEmptyX())
        {
            cWhere.Append(filtro.Codigo_filtro.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.CampoCodigo}] = @{(DBLivroCaixaDicInfo.CampoCodigo)}");
        }
        else if (!(filtro.Codigo_filtro.IsEmptyX()) && !(filtro.Codigo_filtro_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBLivroCaixaDicInfo.PTabelaNome}].{DBLivroCaixaDicInfo.CampoCodigo} BETWEEN @{(DBLivroCaixaDicInfo.CampoCodigo)} AND @{(DBLivroCaixaDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }
}

public partial class LivroCaixaService
{
    /// <summary>
    /// Processa comando de voz para gerar filtro via OpenAI usando serviço genérico
    /// </summary>
    /// <param name = "message">Requisição com comando de voz</param>
    /// <param name = "existingFilter">Filtro existente para combinar</param>
    /// <param name = "uri">URI da base de dados</param>
    /// <returns>FilterLivroCaixa processado</returns>
    private async Task<Filters.FilterLivroCaixa?> ProcessFilterVoiceCommandAsync(CommandSpeakerRequest message, Filters.FilterLivroCaixa? existingFilter, string uri)
    {
        try
        {
            // Usar o contexto do FilterLivroCaixaTXT
            var context = FilterLivroCaixaTXT.GetPrompt(existingFilter, FilterLivroCaixaTXT.GetOptions());
            // Instruções específicas para LivroCaixa
            var additionalInstructions = "";
            // Processar com o serviço genérico
            var filtroProcessado = await _voiceFilterService.ProcessVoiceCommandWithFallbackAsync(message.Message, context, existingFilter, additionalInstructions, uri);
            return filtroProcessado;
        }
        catch (Exception)
        {
            // Se falhar, retornar filtro existente ou novo
            return existingFilter ?? new Filters.FilterLivroCaixa();
        }
    }

    private async Task<IEnumerable<LivroCaixaResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken cancellationToken)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            return[];
        }

        try
        {
            // Usar o reader para obter os dados
            return await reader.ListarAsync(max, uri, where.Replace(TSql.Where, ""), parameters, "", cancellationToken);
        }
        catch (Exception)
        {
            return[];
        }
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterLivroCaixa? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-0
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("LivroCaixa: URI inválida");
        }

        var filtroResult = filtro == null ? null : servicesFilter.WFiltroLivroCaixa(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter>? parameters = filtroResult?.parametros ?? [];
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new Exception($"Coneão nula.");
        }

        var keyCache = await reader.ReadStringAuditorAsync(max, uri, "", [], oCnn);
        var cacheKey = $"{uri}-LivroCaixa-{max}-{where.GetHashCode()}-GetListN-{keyCache}";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter>? parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarNAsync(max, uri, where, parameters, DBLivroCaixaDicInfo.CampoNome);
        if (lista == null || !lista.Any())
        {
            return result;
        }

        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                continue;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    public async Task<IEnumerable<LivroCaixaResponseAll>> GetAll(int max, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        ThrowIfDisposed();
        max = Math.Min(Math.Max(max, 1), BaseConsts.PMaxItens);
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("LivroCaixa: URI inválida");
        }

        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache),
            LocalCacheExpiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache)
        };
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        var keyCache = await reader.ReadStringAuditorAsync(max, uri, "", [], oCnn);
        var cacheKey = $"{uri}-LivroCaixa-Filter-{max}-{keyCache}";
        var result = await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataAllAsync2(max, string.Empty, [], uri, cancel), entryOptions, cancellationToken: token);
        return result;
    }

    private async Task<IEnumerable<LivroCaixaResponseAll>> GetDataAllAsync2(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken token)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.ListarAsync(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}