// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoEMailWindowId from '@/app/GerAdv_TS/TipoEMail/Crud/Grids/TipoEMailWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoEMailWindowId from '@/app/GerAdv_TS/TipoEMail/Crud/Grids/TipoEMailWindowId';
import { TipoEMailEmpty } from '@/app/GerAdv_TS/Models/TipoEMail';

describe('TipoEMailWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoEMailWindowId).toBeDefined();
    expect(typeof TipoEMailWindowId === 'function' || typeof TipoEMailWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoEMailService class so constructor usage doesn't throw and we can control fetchTipoEMailById
const mockFetchTipoEMailById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoEMail/Services/TipoEMail.service', () => ({
  TipoEMailService: jest.fn().mockImplementation(() => ({
    fetchTipoEMailById: (...args: any[]) => mockFetchTipoEMailById(...args),
  })),
}));

// Mock the child TipoEMailWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoEMail/Crud/Grids/TipoEMailWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipoemail-window' {...p} />,
}));

describe('TipoEMailWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoEMailWindowId).toBeDefined();
    expect(typeof TipoEMailWindowId === 'function' || typeof TipoEMailWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoEMailWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoEMailWindow with TipoEMailEmpty when id is 0', async () => {
    render(
      <TipoEMailWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoEMailWindow should be rendered with selectedTipoEMail equal to TipoEMailEmpty()
    const node = await screen.findByTestId('tipoemail-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoEMail"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipoemail') || node.getAttribute('selectedTipoEMail')).not.toBeNull();
  });

  it('fetches tipoemail by id and passes it to TipoEMailWindow when id > 0', async () => {
    const fakeTipoEMail = { id: 123, nomecliente: 'Test' };
    mockFetchTipoEMailById.mockResolvedValue(fakeTipoEMail);

    render(
      <TipoEMailWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipoemail-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoEMailById).toHaveBeenCalledWith(123));
  });
});
