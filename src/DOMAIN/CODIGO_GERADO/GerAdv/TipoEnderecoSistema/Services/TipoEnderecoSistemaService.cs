// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:TipoEnderecoSistema
// Fonte:ServiceGenerator
namespace MenphisSI.GerAdv.Services;
public partial class TipoEnderecoSistemaService(IOptions<AppSettings> appSettings, IFTipoEnderecoSistemaFactory tipoenderecosistemaFactory, ITipoEnderecoSistemaReader reader, ITipoEnderecoSistemaValidation validation, ITipoEnderecoSistemaWriter writer, IHttpContextAccessor httpContextAccessor, IHybridCache cache, IMemoryCache memory, IConnectionService connectionService, IGenericVoiceFilterService<Filters.FilterTipoEnderecoSistema> voiceFilterService, IServicesFilter serviceFilter, IEntityService entityService) : ITipoEnderecoSistemaService, IDisposable
{
    private readonly IHttpContextAccessor _httpContextAccessor = httpContextAccessor;
    private readonly IOptions<AppSettings> _appSettings = appSettings;
    private readonly IHybridCache _cache = cache;
    private readonly IMemoryCache _memoryCache = memory ?? throw new ArgumentNullException(nameof(memory));
    private readonly IConnectionService _connectionService = connectionService ?? throw new ArgumentNullException(nameof(connectionService));
    private readonly IGenericVoiceFilterService<Filters.FilterTipoEnderecoSistema> _voiceFilterService = voiceFilterService ?? throw new ArgumentNullException(nameof(voiceFilterService));
    private readonly IServicesFilter servicesFilter = serviceFilter ?? throw new ArgumentNullException(nameof(serviceFilter));
    private bool _disposed;
    private readonly IEntityService _entityService = entityService;
    private readonly IFTipoEnderecoSistemaFactory tipoenderecosistemaFactory = tipoenderecosistemaFactory;
    private readonly ITipoEnderecoSistemaReader reader = reader;
    private readonly ITipoEnderecoSistemaValidation validation = validation;
    private readonly ITipoEnderecoSistemaWriter writer = writer;
    public async Task<IEnumerable<TipoEnderecoSistemaResponseAll>> Filter([FromQuery] int max, [FromBody] Filters.FilterTipoEnderecoSistema filtro, [FromRoute, Required] string uri)
    {
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("TipoEnderecoSistema: URI inválida");
        }

        if (max <= 0)
        {
            max = BaseConsts.PMaxItens;
        }

        var connectionStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        try
        {
            var filtroResult = filtro == null ? null : servicesFilter.WFiltroTipoEnderecoSistema(filtro!);
            string where = filtroResult?.where ?? string.Empty;
            List<SqlParameter>? parameters = filtroResult?.parametros ?? [];
            var filterHash = GenericVoiceFilterService<Filters.FilterTipoEnderecoSistema>.GetFilterHash(filtro);
            using var scope = _connectionService.CreateConnectionScope(uri);
            using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
            TipoEnderecoSistemaDatabaseMetrics.RecordConnectionOpen("Filter", uri, connectionStopwatch);
            var keyCache = await reader.ReadStringAuditorAsync(uri, oCnn, _cache);
            var cacheKey = $"{uri}-{max}TipoEnderecoSistema-Filter-{where.GetHashCode2()}{filterHash}{keyCache}";
            var entryOptions = new HybridCacheEntryOptions
            {
                Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId),
                LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId)
            };
            var result = await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataAllAsync(oCnn, max, string.IsNullOrEmpty(where) ? string.Empty : TSql.Where + where, parameters, uri, cancel), entryOptions, cancellationToken: CancellationToken.None);
            return result;
        }
        catch (SqlException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("Filter", "SqlException", uri);
            throw new Exception($"TipoEnderecoSistema - SQL error on filtering {ex.Message}.");
        }
        catch (TimeoutException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("Filter", "Timeout", uri);
            throw new Exception($"TipoEnderecoSistema - timeout on filtering {ex.Message}.");
        }
        catch (Exception ex)
        {
            throw new Exception($"TipoEnderecoSistema - error on filtering {ex.Message}.");
        }
    }

    public async Task<Filters.FilterTipoEnderecoSistema> FilterVoice([FromBody] Filters.FilterTipoEnderecoSistema filtro, [FromBody] CommandSpeakerRequest? message, [FromRoute, Required] string uri)
    {
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("TipoEnderecoSistema: URI inválida");
        }

        try
        {
            // Se há mensagem de comando de voz, processar via OpenAI
            Filters.FilterTipoEnderecoSistema? filtroProcessado = filtro;
            if (message != null && !string.IsNullOrWhiteSpace(message.Message))
            {
                filtro = filtro ?? new Filters.FilterTipoEnderecoSistema();
                filtroProcessado = await ProcessFilterVoiceCommandAsync(message, filtro, uri);
            }

            return filtroProcessado ?? new Filters.FilterTipoEnderecoSistema();
        }
        catch (Exception ex)
        {
            throw new Exception($"TipoEnderecoSistema - error on creating filter voice: {ex.Message}.");
        }
    }

    public async Task<TipoEnderecoSistemaResponse?> GetById([FromQuery] int id, [FromRoute, Required] string uri, CancellationToken token)
    {
        ThrowIfDisposed();
        if (id < 1)
        {
            return new TipoEnderecoSistemaResponse();
        }

        var connectionStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        var queryStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId)
        };
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
        try
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordConnectionOpen("GetById", uri, connectionStopwatch);
            TipoEnderecoSistemaDatabaseMetrics.IncrementActiveConnections("GetById", uri);
            var keyCache = await reader.ReadStringAuditorAsync(id, uri, oCnn);
            var result = await _cache.GetOrCreateAsync($"{uri}-TipoEnderecoSistema-GetById-{id}--{keyCache}", async cancel =>
            {
                var data = await GetDataByIdAsync(id, oCnn, cancel);
                TipoEnderecoSistemaDatabaseMetrics.RecordSqlQuery("GetById", "SELECT", uri, queryStopwatch, data != null ? 1 : 0);
                return data;
            }, entryOptions, cancellationToken: token);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("GetById", uri);
            return result;
        }
        catch (SqlException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("GetById", "SqlException", uri);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("GetById", uri);
            throw new Exception($"TipoEnderecoSistema - SQL error on GetById: {ex.Message}");
        }
        catch (TimeoutException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("GetById", "Timeout", uri);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("GetById", uri);
            throw new Exception($"TipoEnderecoSistema - timeout on GetById: {ex.Message}");
        }
        catch (Exception)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("GetById", uri);
            throw new Exception($"TipoEnderecoSistema - {uri}-: GetById");
        }
    }

    private async Task<TipoEnderecoSistemaResponse?> GetDataByIdAsync(int id, MsiSqlConnection? oCnn, CancellationToken token) => await reader.ReadAsync(id, oCnn);
    public async Task<AuditorResponse?> GetAuditor([FromQuery] int id, [FromRoute, Required] string uri, CancellationToken token)
    {
        ThrowIfDisposed();
        AuditorResponse? result = null;
        if (id < 1)
        {
            return result;
        }

        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
        try
        {
            result = await reader.ReadAuditorAsync(id, uri, oCnn);
            return result;
        }
        catch (Exception)
        {
            throw new Exception($"TipoEnderecoSistema - {uri}-: GetAuditor");
        }
    }

    public async Task<TipoEnderecoSistemaResponse?> AddAndUpdate([FromBody] Models.TipoEnderecoSistema? regTipoEnderecoSistema, [FromRoute, Required] string uri)
    {
        ThrowIfDisposed();
        if (regTipoEnderecoSistema == null)
        {
            return null;
        }

        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("TipoEnderecoSistema: URI inválida");
        }

        var connectionStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        var queryStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        using var scope = _connectionService.CreateConnectionScopeRw(uri);
        using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
        if (oCnn == null)
        {
            return null;
        }

        try
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordConnectionOpen("AddAndUpdate", uri, connectionStopwatch);
            TipoEnderecoSistemaDatabaseMetrics.IncrementActiveConnections("AddAndUpdate", uri);
            var validade = await validation.ValidateReg(regTipoEnderecoSistema, this, uri, oCnn);
            if (!validade)
            {
                throw new Exception("Erro inesperado ao validar 0x0!");
            }
        }
        catch (SGValidationException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("AddAndUpdate", uri);
            throw new Exception(ex.Message);
        }
        catch (Exception)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("AddAndUpdate", uri);
            throw new Exception("Erro inesperado ao validar 0x1!");
        }

        int operadorId = UserTools.GetAuthenticatedUserId(_httpContextAccessor);
        try
        {
            using var saved = await writer.WriteAsync(regTipoEnderecoSistema, operadorId, oCnn);
            string tipoQuery = regTipoEnderecoSistema.Id.IsEmptyIDNumber() ? "INSERT" : "UPDATE";
            TipoEnderecoSistemaDatabaseMetrics.RecordSqlQuery("AddAndUpdate", tipoQuery, uri, queryStopwatch, 1);
            var result = reader.Read(saved, oCnn);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("AddAndUpdate", uri);
            return result;
        }
        catch (Exception)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("AddAndUpdate", "SqlException", uri);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("AddAndUpdate", uri);
            throw;
        }
    }

    public async Task<TipoEnderecoSistemaResponse?> Validation([FromBody] Models.TipoEnderecoSistema? regTipoEnderecoSistema, [FromRoute, Required] string uri)
    {
        ThrowIfDisposed();
        if (regTipoEnderecoSistema == null)
        {
            return null;
        }

        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("TipoEnderecoSistema: URI inválida");
        }

        var connectionStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        var queryStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        using var scope = _connectionService.CreateConnectionScopeRw(uri);
        using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
        if (oCnn == null)
        {
            return null;
        }

        try
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordConnectionOpen("Validation", uri, connectionStopwatch);
            TipoEnderecoSistemaDatabaseMetrics.IncrementActiveConnections("Validation", uri);
            var validade = await validation.ValidateReg(regTipoEnderecoSistema, this, uri, oCnn);
            if (!validade)
            {
                throw new Exception("Erro inesperado ao validar 0x0!");
            }
        }
        catch (SGValidationException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Validation", uri);
            throw new Exception(ex.Message);
        }
        catch (Exception)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Validation", uri);
            throw new Exception("Erro inesperado ao validar 0x1!");
        }

        if (regTipoEnderecoSistema.Id.IsEmptyIDNumber())
        {
            return new TipoEnderecoSistemaResponse();
        }

        return await reader.ReadAsync(regTipoEnderecoSistema.Id, oCnn);
    }

    public async Task<TipoEnderecoSistemaResponse?> Delete([FromQuery] int? id, [FromRoute, Required] string uri)
    {
        if (id == null || id.IsEmptyIDNumber())
        {
            return null;
        }

        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("TipoEnderecoSistema: URI inválida");
        }

        var nOperador = UserTools.GetAuthenticatedUserId(_httpContextAccessor);
        using var scope = _connectionService.CreateConnectionScopeRw(uri);
        using var oCnn = scope.Connection ?? throw new DatabaseConnectionException();
        var connectionStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        var queryStopwatch = TipoEnderecoSistemaDatabaseMetrics.StartTimer();
        if (oCnn == null)
        {
            return null;
        }

        try
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordConnectionOpen("Delete", uri, connectionStopwatch);
            TipoEnderecoSistemaDatabaseMetrics.IncrementActiveConnections("Delete", uri);
            var deleteValidation = await validation.CanDelete(id, this, uri, oCnn);
            if (!deleteValidation)
            {
                throw new Exception("Erro inesperado ao validar 0x0!");
            }
        }
        catch (SGValidationException ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Delete", uri);
            throw new Exception(ex.Message);
        }
        catch (Exception)
        {
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Delete", uri);
            throw new Exception("Erro inesperado ao validar 0x1!");
        }

        var tipoenderecosistema = await reader.ReadAsync(id ?? default, oCnn);
        try
        {
            if (tipoenderecosistema != null)
            {
                await writer.DeleteAsync(tipoenderecosistema, nOperador, oCnn);
                TipoEnderecoSistemaDatabaseMetrics.RecordSqlQuery("Delete", "DELETE", uri, queryStopwatch, 1);
                if (_memoryCache is MemoryCache memCache)
                {
                    memCache.Compact(1.0);
                }
            }

            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Delete", uri);
            return tipoenderecosistema;
        }
        catch (Exception ex)
        {
            TipoEnderecoSistemaDatabaseMetrics.RecordDatabaseError("Delete", "SqlException", uri);
            TipoEnderecoSistemaDatabaseMetrics.DecrementActiveConnections("Delete", uri);
            throw new Exception(ex.Message);
        }
    }

    public virtual void Dispose()
    {
        Dispose(true);
        GC.SuppressFinalize(this);
    }

    protected virtual void Dispose(bool disposing)
    {
        if (_disposed)
            return;
        if (disposing)
        {
        //_cache?.Dispose();
        }

        _disposed = true;
    }

    private void ThrowIfDisposed()
    {
        ObjectDisposedException.ThrowIf(_disposed, this);
    }
}