// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for PontoVirtualAcessos page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock PontoVirtualAcessosGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/PontoVirtualAcessos/Adapter/PontoVirtualAcessosGridAdapter', () => ({
  PontoVirtualAcessosGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock PontoVirtualAcessosGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/PontoVirtualAcessos/Components/PontoVirtualAcessosGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='pontovirtualacessos-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import PontoVirtualAcessosPage from '@/app/pages/pontovirtualacessos/page';

describe('PontoVirtualAcessos page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and PontoVirtualAcessosGridContainer', () => {
    render(<PontoVirtualAcessosPage />);

    expect(screen.getByRole('heading', { name: /Ponto Virtual Acessos/i })).toBeInTheDocument();
    expect(screen.getByTestId('pontovirtualacessos-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

