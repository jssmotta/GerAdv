// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for Cidade page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock CidadeGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/Cidade/Adapter/CidadeGridAdapter', () => ({
  CidadeGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock CidadeGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/Cidade/Components/CidadeGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='cidade-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import CidadePage from '@/app/pages/cidade/page';

describe('Cidade page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and CidadeGridContainer', () => {
    render(<CidadePage />);

    expect(screen.getByRole('heading', { name: /Cidade/i })).toBeInTheDocument();
    expect(screen.getByTestId('cidade-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

