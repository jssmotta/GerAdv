// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class StatusTarefasValidationTests : IDisposable
{
    private readonly StatusTarefasValidation _validation;
    private readonly Mock<IStatusTarefasService> _mockStatusTarefasService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly StatusTarefasService _service;
    private readonly string _validUri = "test-uri";
    public StatusTarefasValidationTests()
    {
        _validation = new StatusTarefasValidation();
        _mockStatusTarefasService = new Mock<IStatusTarefasService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void StatusTarefasValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IStatusTarefasValidation>();
    }

    [Fact]
    public void StatusTarefasValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(StatusTarefasValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void StatusTarefasValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(StatusTarefasValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.StatusTarefas CreateValidStatusTarefas()
    {
        return new Models.StatusTarefas
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockStatusTarefasService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusTarefas>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the StatusTarefass service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockStatusTarefasService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusTarefas>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the StatusTarefass service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var statustarefas = new Models.StatusTarefas
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(statustarefas, _mockStatusTarefasService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockStatusTarefasService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}