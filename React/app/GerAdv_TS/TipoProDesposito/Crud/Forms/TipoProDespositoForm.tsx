// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { ITipoProDesposito } from "@/app/GerAdv_TS/TipoProDesposito/Interfaces/interface.TipoProDesposito";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { TipoProDespositoApi } from "../../Apis/TipoProDespositoApi";
import { useValidationsTipoProDesposito } from "../../Hooks/hookTipoProDesposito";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpTipoProDesposito from "../../Help/HelpTipoProDesposito";
import TipoProDespositoPage from "@/app/pages/tipoprodesposito/page";

import InputName from "@/app/components/Inputs/InputName";

interface TipoProDespositoFormProps {
  tipoprodespositoData: ITipoProDesposito;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const TipoProDespositoForm: React.FC<TipoProDespositoFormProps> = ({
  tipoprodespositoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new TipoProDespositoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsTipoProDesposito();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de TipoProDesposito:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `TipoProDespositoForm-${tipoprodespositoData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar TipoProDesposito diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        tipoprodespositoData?.id == 0
          ? "Editar TipoProDesposito"
          : "Adicionar Tipo Pro Desposito";
    }
  }, [tipoprodespositoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-TipoProDesposito"
            : "form-container5 form-container-TipoProDesposito"
        }
      >
        <form
          className="formInputCadInc"
          id={`TipoProDespositoForm-${tipoprodespositoData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="TipoProDesposito"
            data={tipoprodespositoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`TipoProDespositoForm-${tipoprodespositoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={tipoprodespositoData}
              className="input-default-main"
              name="nome"
              value={tipoprodespositoData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton
            title="Tipo Pro Desposito"
            windowCrud={TipoProDespositoPage}
          />

          <CopyButton
            formId={`TipoProDespositoForm-${tipoprodespositoData.id}`}
          />

          <PrintButton
            formId={`TipoProDespositoForm-${tipoprodespositoData.id}`}
          />

          <AuditorButton
            tableName="TipoProDesposito"
            coolName="Tipo Pro Desposito"
            id={tipoprodespositoData.id}
          />

          <DeleteButton
            page={"/pages/tipoprodesposito"}
            id={tipoprodespositoData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Tipo Pro Desposito"
            page={(onClose) => <HelpTipoProDesposito onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="TipoProDesposito"
          data={tipoprodespositoData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`TipoProDespositoForm-${tipoprodespositoData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
