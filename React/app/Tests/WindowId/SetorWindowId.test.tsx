// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import SetorWindowId from '@/app/GerAdv_TS/Setor/Crud/Grids/SetorWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import SetorWindowId from '@/app/GerAdv_TS/Setor/Crud/Grids/SetorWindowId';
import { SetorEmpty } from '@/app/GerAdv_TS/Models/Setor';

describe('SetorWindowId (basic)', () => {
  it('exports the component', () => {
    expect(SetorWindowId).toBeDefined();
    expect(typeof SetorWindowId === 'function' || typeof SetorWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the SetorService class so constructor usage doesn't throw and we can control fetchSetorById
const mockFetchSetorById = jest.fn();
jest.mock('@/app/GerAdv_TS/Setor/Services/Setor.service', () => ({
  SetorService: jest.fn().mockImplementation(() => ({
    fetchSetorById: (...args: any[]) => mockFetchSetorById(...args),
  })),
}));

// Mock the child SetorWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Setor/Crud/Grids/SetorWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='setor-window' {...p} />,
}));

describe('SetorWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(SetorWindowId).toBeDefined();
    expect(typeof SetorWindowId === 'function' || typeof SetorWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <SetorWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders SetorWindow with SetorEmpty when id is 0', async () => {
    render(
      <SetorWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // SetorWindow should be rendered with selectedSetor equal to SetorEmpty()
    const node = await screen.findByTestId('setor-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedSetor"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedsetor') || node.getAttribute('selectedSetor')).not.toBeNull();
  });

  it('fetches setor by id and passes it to SetorWindow when id > 0', async () => {
    const fakeSetor = { id: 123, nomecliente: 'Test' };
    mockFetchSetorById.mockResolvedValue(fakeSetor);

    render(
      <SetorWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('setor-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchSetorById).toHaveBeenCalledWith(123));
  });
});
