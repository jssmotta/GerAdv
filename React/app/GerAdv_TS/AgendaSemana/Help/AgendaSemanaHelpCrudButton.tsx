// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:HelpCrudGenerator

"use client";
import React, { useState } from "react";
import { Button } from "@progress/kendo-react-buttons";
import { questionCircleIcon } from "@progress/kendo-svg-icons";
import HelpCrudScreen from "@/app/components/Cruds/Help/HelpCrudScreen";

declare global {
  interface Window {
    storage?: any;
  }
}

interface AgendaSemanaHelpCrudButtonProps {
  isMobile: boolean;
}

const AgendaSemanaHelpCrudButton: React.FC<AgendaSemanaHelpCrudButtonProps> = ({
  isMobile,
}) => {
  const [showHelp, setShowHelp] = useState(false);

  const handleOpenHelp = async () => {
    setShowHelp(true);
  };

  return (
    <>
      <Button
        onClick={handleOpenHelp}
        aria-label="Ajuda da tela/cadastro Agenda Semana"
        svgIcon={questionCircleIcon}
      ></Button>
      {showHelp && (
        <HelpCrudScreen
          title="Agenda Semana"
          onBack={() => setShowHelp(false)}
        />
      )}
    </>
  );
};

export default AgendaSemanaHelpCrudButton;
