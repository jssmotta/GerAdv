// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoRecursoServiceTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFTipoRecursoFactory> _mockTipoRecursoFactory;
    private readonly Mock<ITipoRecursoReader> _mockReader;
    private readonly Mock<ITipoRecursoValidation> _mockValidation;
    private readonly Mock<ITipoRecursoWriter> _mockWriter;
    private readonly Mock<IInstanciaService> _mockInstanciaService;
    private readonly Mock<IJusticaReader> _mockJusticaReader;
    private readonly Mock<IAreaReader> _mockAreaReader;
    private readonly Mock<IHttpContextAccessor> _mockHttpContextAccessor;
    private readonly Mock<HybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly TipoRecursoService _service;
    private readonly string _validUri = "test-uri";
    public TipoRecursoServiceTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockTipoRecursoFactory = new Mock<IFTipoRecursoFactory>();
        _mockReader = new Mock<ITipoRecursoReader>();
        _mockValidation = new Mock<ITipoRecursoValidation>();
        _mockWriter = new Mock<ITipoRecursoWriter>();
        _mockInstanciaService = new Mock<IInstanciaService>();
        _mockJusticaReader = new Mock<IJusticaReader>();
        _mockAreaReader = new Mock<IAreaReader>();
        _mockHttpContextAccessor = new Mock<IHttpContextAccessor>();
        _mockCache = new Mock<HybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _service = new TipoRecursoService(_mockAppSettings.Object, _mockTipoRecursoFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockJusticaReader.Object, _mockAreaReader.Object, _mockInstanciaService.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object);
    }

    [Fact]
    public void Constructor_WithValidParameters_InitializesService()
    {
        // Arrange & Act
        var service = new TipoRecursoService(_mockAppSettings.Object, _mockTipoRecursoFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockJusticaReader.Object, _mockAreaReader.Object, _mockInstanciaService.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object);
        // Assert
        Assert.NotNull(service);
    }

    [Fact]
    public async Task AddAndUpdate_WithNullInput_ReturnsNull()
    {
        // Act
        var result = await _service.AddAndUpdate(null, _validUri);
        // Assert
        Assert.Null(result);
    }

    [Fact]
    public async Task Delete_WithZeroId_ReturnsNull()
    {
        // Arrange
        var id = 0;
        // Act  
        var result = await _service.Delete(id, _validUri);
        // Assert
        Assert.Null(result);
    }

    [Fact]
    public async Task Delete_WithNegativeId_ReturnsNull()
    {
        // Arrange
        var id = -5;
        // Act
        var result = await _service.Delete(id, _validUri);
        // Assert
        Assert.Null(result);
    }

    [Fact]
    public async Task GetById_WithZeroId_ReturnsDefaultResponse()
    {
        // Arrange
        var id = 0;
        // Act
        var result = await _service.GetById(id, _validUri, CancellationToken.None);
        // Assert
        Assert.NotNull(result);
        Assert.Equal(0, result.Id);
    }

    [Fact]
    public async Task GetById_WithNegativeId_ReturnsDefaultResponse()
    {
        // Arrange
        var id = -1;
        // Act
        var result = await _service.GetById(id, _validUri, CancellationToken.None);
        // Assert
        Assert.NotNull(result);
        Assert.Equal(0, result.Id);
    }

    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void GetFilterHash_QuandoFiltroNull_DeveRetornarStringVazia()
    {
        // Usar reflexão para testar método privado
        var result = InvokePrivateMethod("GetFilterHash", null);
        result.Should().Be(string.Empty);
    }

    [Fact]
    public void GetFilterHash_QuandoFiltroValido_DeveRetornarHashSHA256()
    {
        var filtro = new FilterTipoRecurso
        {
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        var result = InvokePrivateMethod("GetFilterHash", filtro);
        result.Should().NotBeNull();
        result.Should().BeOfType<string>();
        ((string)result).Should().NotBeNullOrEmpty();
        ((string)result).Length.Should().Be(64); // SHA256 hash length
    }

    private object InvokePrivateMethod(string methodName, Filters.FilterTipoRecurso? parameters)
    {
        var method = typeof(TipoRecursoService).GetMethod(methodName, BindingFlags.NonPublic | BindingFlags.Instance);
        return method.Invoke(_service, [parameters]);
    }

    [Fact]
    public async Task ThrowIfDisposed_AfterDispose_ThrowsObjectDisposedException()
    {
        // Arrange
        _service.Dispose();
        // Act & Assert
        await Assert.ThrowsAsync<ObjectDisposedException>(() => _service.GetAll(10, _validUri));
    }

    [Fact]
    public async Task Filter_WithNullFilter_ReturnsEmptyResult()
    {
        // Arrange
        var max = 10;
        var invalidUri = "invalid-uri";
        // Act & Assert
        await Assert.ThrowsAsync<Exception>(() => _service.Filter(max, null, invalidUri));
    }

    [Fact]
    public async Task GetListN_WithValidParameters_ReturnsNomeIDList()
    {
        // Arrange
        var max = 10;
        // Act
        await Assert.ThrowsAsync<Exception>(async () => await _service.GetListN(max, new Filters.FilterTipoRecurso(), _validUri, CancellationToken.None));
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}