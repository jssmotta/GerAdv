// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IOperador } from "@/app/GerAdv_TS/Operador/Interfaces/interface.Operador";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useSystemContext } from "@/app/context/SystemContext";
import { getParamFromUrl } from "@/app/tools/helpers";
import FooterIncDocker from "@/app/components/Cruds/FooterIncDocker";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms.css"; // [ INDEX_SIZE ]

import ButtonSalvarCrud from "@/app/components/Cruds/ButtonSalvarCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import DeleteButton from "@/app/components/Cruds/DeleteButton";
import AuditorButton from "@/app/components/Cruds/AuditorButton";
import PrintButton from "@/app/components/Cruds/PrintButton";
import CopyButton from "@/app/components/Cruds/CopyButton";
import { OperadorApi } from "../../Apis/ApiOperador";
import { useValidationsOperador } from "../../Hooks/hookOperador";

import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface OperadorFormProps {
  operadorData: IOperador;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const OperadorForm: React.FC<OperadorFormProps> = ({
  operadorData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dadoApi = new OperadorApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsOperador();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Operador:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload(); // Recarrega os dados originais
    } else {
      onClose(); // Comportamento padrão se não há callback de recarga
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`OperadorForm-${operadorData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Operador diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        operadorData?.id == 0 ? "Editar Operador" : "Adicionar Operador";
    }
  }, [operadorData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Operador"
            : "form-container form-container-Operador"
        }
      >
        <form
          className="formInputCadInc"
          id={`OperadorForm-${operadorData.id}`}
          onSubmit={onConfirm}
        >
          {!isMobile && (
            <ButtonSalvarCrud
              isMobile={false}
              validationForm={validationForm}
              entity="Operador"
              data={operadorData}
              isSubmitting={isSubmitting}
              setIsSubmitting={setIsSubmitting}
              onClose={onClose}
              formId={`OperadorForm-${operadorData.id}`}
              preventPropagation={true}
              onSave={handleDirectSave}
              onCancel={handleCancel}
            />
          )}
          <div className="grid-container">
            <InputName
              type="text"
              id="nome"
              label="Nome"
              dataForm={operadorData}
              className="input-default-main"
              name="nome"
              value={operadorData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputInput
              type="email"
              maxLength={150}
              id="email"
              label="EMail"
              dataForm={operadorData}
              className="input-default-main"
              name="email"
              value={operadorData.email}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2147483647}
              id="pasta"
              label="Pasta"
              dataForm={operadorData}
              className="input-default-main"
              name="pasta"
              value={operadorData.pasta}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={50}
              id="nick"
              label="Nick"
              dataForm={operadorData}
              className="input-default-main"
              name="nick"
              value={operadorData.nick}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={20}
              id="ramal"
              label="Ramal"
              dataForm={operadorData}
              className="input-default-main"
              name="ramal"
              value={operadorData.ramal}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="cadid"
              label="CadID"
              dataForm={operadorData}
              className="input-default-main"
              name="cadid"
              value={operadorData.cadid}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="cadcod"
              label="CadCod"
              dataForm={operadorData}
              className="input-default-main"
              name="cadcod"
              value={operadorData.cadcod}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="computador"
              label="Computador"
              dataForm={operadorData}
              className="input-default-main"
              name="computador"
              value={operadorData.computador}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={255}
              id="minhadescricao"
              label="MinhaDescricao"
              dataForm={operadorData}
              className="input-default-main"
              name="minhadescricao"
              value={operadorData.minhadescricao}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputInput
              type="text"
              maxLength={2048}
              id="ultimologoff"
              label="UltimoLogoff"
              dataForm={operadorData}
              className="input-default-main"
              name="ultimologoff"
              value={operadorData.ultimologoff}
              onChange={onChange}
            />

            <InputInput
              type="email"
              maxLength={100}
              id="emailnet"
              label="EMailNet"
              dataForm={operadorData}
              className="input-default-main"
              name="emailnet"
              value={operadorData.emailnet}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={50}
              id="onlineip"
              label="OnlineIP"
              dataForm={operadorData}
              className="input-default-main"
              name="onlineip"
              value={operadorData.onlineip}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="statusid"
              label="StatusId"
              dataForm={operadorData}
              className="input-default-main"
              name="statusid"
              value={operadorData.statusid}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={1024}
              id="statusmessage"
              label="StatusMessage"
              dataForm={operadorData}
              className="input-default-main"
              name="statusmessage"
              value={operadorData.statusmessage}
              onChange={onChange}
            />

            <InputInput
              autoComplete="off"
              type="password"
              maxLength={4000}
              id="senha256"
              label="Senha256"
              dataForm={operadorData}
              className="input-default-main"
              name="senha256"
              value={operadorData.senha256}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="datalimitereset"
              label="DataLimiteReset"
              dataForm={operadorData}
              className="input-default-main"
              name="datalimitereset"
              value={operadorData.datalimitereset}
              onChange={onChange}
            />

            <InputInput
              autoComplete="off"
              type="password"
              maxLength={4000}
              id="suportesenha256"
              label="SuporteSenha256"
              dataForm={operadorData}
              className="input-default-main"
              name="suportesenha256"
              value={operadorData.suportesenha256}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="suportemaxage"
              label="SuporteMaxAge"
              dataForm={operadorData}
              className="input-default-main"
              name="suportemaxage"
              value={operadorData.suportemaxage}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputInput
              type="text"
              maxLength={100}
              id="suportenomesolicitante"
              label="SuporteNomeSolicitante"
              dataForm={operadorData}
              className="input-default-main"
              name="suportenomesolicitante"
              value={operadorData.suportenomesolicitante}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="suporteultimoacesso"
              label="SuporteUltimoAcesso"
              dataForm={operadorData}
              className="input-default-main"
              name="suporteultimoacesso"
              value={operadorData.suporteultimoacesso}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={80}
              id="suporteipultimoacesso"
              label="SuporteIpUltimoAcesso"
              dataForm={operadorData}
              className="input-default-main"
              name="suporteipultimoacesso"
              value={operadorData.suporteipultimoacesso}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={operadorData}
              label="Telefonista"
              name="telefonista"
              checked={operadorData.telefonista}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Master"
              name="master"
              checked={operadorData.master}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Excluido"
              name="excluido"
              checked={operadorData.excluido}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Situacao"
              name="situacao"
              checked={operadorData.situacao}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="OnLine"
              name="online"
              checked={operadorData.online}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="SysOp"
              name="sysop"
              checked={operadorData.sysop}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputCheckbox
              dataForm={operadorData}
              label="IsFinanceiro"
              name="isfinanceiro"
              checked={operadorData.isfinanceiro}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Top"
              name="top"
              checked={operadorData.top}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Sexo"
              name="sexo"
              checked={operadorData.sexo}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Basico"
              name="basico"
              checked={operadorData.basico}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="Externo"
              name="externo"
              checked={operadorData.externo}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={operadorData}
              label="EMailConfirmado"
              name="emailconfirmado"
              checked={operadorData.emailconfirmado}
              onChange={onChange}
            />
          </div>
        </form>

        {isMobile && (
          <ButtonSalvarCrud
            isMobile={true}
            validationForm={validationForm}
            entity="Operador"
            data={operadorData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`OperadorForm-${operadorData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />
        )}

        <FooterIncDocker>
          <CopyButton formId={`OperadorForm-${operadorData.id}`} />

          <PrintButton formId={`OperadorForm-${operadorData.id}`} />

          <AuditorButton
            tableName="Operador"
            coolName="Operador"
            id={operadorData.id}
          />

          <DeleteButton
            page={"/pages/operador"}
            id={operadorData.id}
            dadoApi={dadoApi}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>
    </>
  );
};
