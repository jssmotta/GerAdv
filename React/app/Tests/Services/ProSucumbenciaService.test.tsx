// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProSucumbenciaService, ProSucumbenciaValidator } from '@/app/GerAdv_TS/ProSucumbencia/Services/ProSucumbencia.service';
import { ProSucumbenciaApi, ProSucumbenciaApiError } from '@/app/GerAdv_TS/ProSucumbencia/Apis/ApiProSucumbencia';
import { IProSucumbencia } from '@/app/GerAdv_TS/ProSucumbencia/Interfaces/interface.ProSucumbencia';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProSucumbenciaApi;

const sample: IProSucumbencia = { id: 1, nome: 'Teste' };

describe('ProSucumbenciaService', () => {
  let service: ProSucumbenciaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProSucumbenciaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProSucumbenciaValidator, 'validateProSucumbencia').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProSucumbenciaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProSucumbenciaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProSucumbenciaById lança ProSucumbenciaApiError para id inválido', async () => {
    await expect(service.fetchProSucumbenciaById(0)).rejects.toBeInstanceOf(ProSucumbenciaApiError);
  });

  it('saveProSucumbencia chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProSucumbencia(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProSucumbencia lança ProSucumbenciaApiError quando validação falha', async () => {
    (ProSucumbenciaValidator.validateProSucumbencia as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProSucumbencia(sample)).rejects.toBeInstanceOf(ProSucumbenciaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProSucumbencia chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProSucumbencia(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProSucumbencia lança ProSucumbenciaApiError para id inválido', async () => {
    await expect(service.deleteProSucumbencia(0)).rejects.toBeInstanceOf(ProSucumbenciaApiError);
  });

  it('validateProSucumbencia delega para ProSucumbenciaValidator', () => {
    (ProSucumbenciaValidator.validateProSucumbencia as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProSucumbencia(sample);
    expect(r.isValid).toBe(true);
  });
});
