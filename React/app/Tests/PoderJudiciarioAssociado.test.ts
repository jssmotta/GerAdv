// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { usePoderJudiciarioAssociadoForm, usePoderJudiciarioAssociadoList, useValidationsPoderJudiciarioAssociado } from '../GerAdv_TS/PoderJudiciarioAssociado/Hooks/hookPoderJudiciarioAssociado';
import { IPoderJudiciarioAssociado } from '../GerAdv_TS/PoderJudiciarioAssociado/Interfaces/interface.PoderJudiciarioAssociado';
import { IPoderJudiciarioAssociadoService } from '../GerAdv_TS/PoderJudiciarioAssociado/Services/PoderJudiciarioAssociado.service';
import { PoderJudiciarioAssociadoTestEmpty } from '../GerAdv_TS/Models/PoderJudiciarioAssociado';


// Mock do serviço
const mockPoderJudiciarioAssociadoService: jest.Mocked<IPoderJudiciarioAssociadoService> = {
  fetchPoderJudiciarioAssociadoById: jest.fn(),
  savePoderJudiciarioAssociado: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deletePoderJudiciarioAssociado: jest.fn(),
  validatePoderJudiciarioAssociado: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialPoderJudiciarioAssociado: IPoderJudiciarioAssociado = { ...PoderJudiciarioAssociadoTestEmpty() };

describe('usePoderJudiciarioAssociadoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    expect(result.current.data).toEqual(initialPoderJudiciarioAssociado);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    const mockEvent = {
      target: {
        name: 'guid',
        value: 'Novo Poder Judiciario Associado',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.guid).toBe('Novo Poder Judiciario Associado');
  });

   test('deve carregar Poder Judiciario Associado por ID', async () => {
    const mockPoderJudiciarioAssociado = { ...initialPoderJudiciarioAssociado, id: 1, guid: 'Poder Judiciario Associado Teste' };
    mockPoderJudiciarioAssociadoService.fetchPoderJudiciarioAssociadoById.mockResolvedValue(mockPoderJudiciarioAssociado);

    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.loadPoderJudiciarioAssociado(1);
    });

    expect(mockPoderJudiciarioAssociadoService.fetchPoderJudiciarioAssociadoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockPoderJudiciarioAssociado);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Poder Judiciario Associado', async () => {
    const errorMessage = 'Erro ao carregar Poder Judiciario Associado';
    mockPoderJudiciarioAssociadoService.fetchPoderJudiciarioAssociadoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.loadPoderJudiciarioAssociado(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialPoderJudiciarioAssociado, guid: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialPoderJudiciarioAssociado);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoForm(initialPoderJudiciarioAssociado, mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.loadPoderJudiciarioAssociado(0);
    });

    expect(mockPoderJudiciarioAssociadoService.fetchPoderJudiciarioAssociadoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialPoderJudiciarioAssociado);
  });
});

describe('usePoderJudiciarioAssociadoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoList(mockPoderJudiciarioAssociadoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialPoderJudiciarioAssociado, id: 1, guid: 'Poder Judiciario Associado 1' },
      { ...initialPoderJudiciarioAssociado, id: 2, guid: 'Poder Judiciario Associado 2' }
    ];
    mockPoderJudiciarioAssociadoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoList(mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockPoderJudiciarioAssociadoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockPoderJudiciarioAssociadoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoList(mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialPoderJudiciarioAssociado, id: 1, guid: 'Poder Judiciario Associado Filtrado' }];
    const filtro = { guid: 'Poder Judiciario Associado' };
    mockPoderJudiciarioAssociadoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePoderJudiciarioAssociadoList(mockPoderJudiciarioAssociadoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockPoderJudiciarioAssociadoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsPoderJudiciarioAssociado', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsPoderJudiciarioAssociado());

    const validData = { ...initialPoderJudiciarioAssociado, guid: 'Poder Judiciario Associado Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar guid vazio', () => {
    const { result } = renderHook(() => useValidationsPoderJudiciarioAssociado());

    const invalidData = { ...initialPoderJudiciarioAssociado, guid: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ficar vazio.');
  });

  
  test('deve invalidar guid muito longo', () => {
    const { result } = renderHook(() => useValidationsPoderJudiciarioAssociado());

    const invalidData = { 
      ...initialPoderJudiciarioAssociado, 
      guid: 'a'.repeat(100+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo GUID não pode ter mais de 100 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsPoderJudiciarioAssociado());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialPoderJudiciarioAssociado, id: 1, guid: 'Poder Judiciario Associado Teste' }];
    mockPoderJudiciarioAssociadoService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      usePoderJudiciarioAssociadoList(mockPoderJudiciarioAssociadoService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsPoderJudiciarioAssociado()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});