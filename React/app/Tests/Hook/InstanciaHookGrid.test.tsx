// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// HookGrid.test.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useInstanciaGrid } from '@/app/GerAdv_TS/Instancia/Hooks/hookInstanciaGrid';
import { IInstancia } from '@/app/GerAdv_TS/Instancia/Interfaces/interface.Instancia';

// Sample minimal instancia item for tests
const sampleInstancia = (overrides?: Partial<IInstancia>): IInstancia => ({
  id: 1,
 liminarpedida: '',
objeto: 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA',
statusresultado: 0,
liminarpendente: false,
interpusemosrecurso: true,
liminarconcedida: false,
liminarnegada: true,
processo: 0,
data: '24/04/1975',
liminarparcial: false,
liminarresultado: '',
nroprocesso: 'AAAAAAAAAAAAAAAAAAAAAAA',
divisao: 0,
liminarcliente: true,
comarca: 0,
subdivisao: 0,
principal: false,
acao: 0,
foro: 0,
tiporecurso: 0,
zkey: 'AAAAAAAAAAAAAAAAAAAAAAA',
zkeyquem: 0,
zkeyquando: '24/04/1975',
nroantigo: 'AAAAAAAAAAAAAAAAAAAAAAA',
accesscode: 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA',
julgador: 0,
zkeyia: 'AAAAAAAAAAAAAAAAAAAAAAA',

  ...overrides,
} as IInstancia);

describe('useInstanciaGrid', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    // Clear localStorage to avoid persisted column order
    try {
      localStorage.clear();
    } catch (e) {}
  });

  test('inicializa com dados processados e paginação', () => {
    const data = [sampleInstancia({ id: 1 }), sampleInstancia({ id: 2 })];
    const onRowClick = jest.fn();
    const onDeleteClick = jest.fn();
    const setSelectedId = jest.fn();

    const { result } = renderHook(() =>
      useInstanciaGrid({
        data,
        onRowClick,
        onDeleteClick,
        setSelectedId,
        initialTake: 1,
      })
    );

    // filteredData should equal provided data (no filters applied)
    expect(result.current.filteredData.length).toBe(2);
    // paginatedData should respect initialTake=1
    expect(result.current.paginatedData.length).toBe(1);
    expect(result.current.page.take).toBe(1);
    // gridConfig should include handlers
    expect(typeof result.current.gridConfig.onRowClick).toBe('function');
  });

  test('handleRowClick chama onRowClick com item correto', () => {
    const data = [sampleInstancia({ id: 5 })];
    const onRowClick = jest.fn();
    const onDeleteClick = jest.fn();
    const setSelectedId = jest.fn();

    const { result } = renderHook(() =>
      useInstanciaGrid({ data, onRowClick, onDeleteClick, setSelectedId })
    );

    act(() => {
      result.current.handleRowClick({ dataItem: data[0] });
    });

    expect(onRowClick).toHaveBeenCalledWith(data[0]);
  });

  test('resetColumnsOrder restaura ordem padrão e limpa localStorage', () => {
    const data = [sampleInstancia()];
    const onRowClick = jest.fn();
    const onDeleteClick = jest.fn();
    const setSelectedId = jest.fn();

    const { result } = renderHook(() =>
      useInstanciaGrid({ data, onRowClick, onDeleteClick, setSelectedId })
    );

    // Simula mudança de ordem salvo no localStorage
    try {
      localStorage.setItem(btoa('instancia-columns-order'), JSON.stringify(['nroprocesso','data']));
    } catch(e) {}

    act(() => {
      result.current.resetColumnsOrder();
    });

    expect(result.current.columnsOrder).toBeInstanceOf(Array);
    // localStorage key deve ter sido removida
    try {
      const v = localStorage.getItem(btoa('instancia-columns-order'));
      expect(v).toBeNull();
    } catch (e) {}
  });

  test('filterLogic filtra por nroprocesso', async () => {
    const matching = sampleInstancia({ nroprocesso: 'AAAAAAAAAAAAAAAAAAAAAAA' });
    const notMatching = sampleInstancia({ nroprocesso: 'Outro' });
    const data = [matching, notMatching];
    const onRowClick = jest.fn();
    const onDeleteClick = jest.fn();
    const setSelectedId = jest.fn();

    const { result } = renderHook(() =>
      useInstanciaGrid({ data, onRowClick, onDeleteClick, setSelectedId })
    );

    // Aplica filtro via handleFilterChange
    act(() => {
      result.current.handleFilterChange({ field: 'nroprocesso', value: 'AAAAAAAAAAAAAAAAAAAAAAA' } as any);
    });

    // Espera que filteredData contenha apenas o item correspondente
    await waitFor(() => {
      expect(result.current.filteredData.some((d) => d.nroprocesso === 'AAAAAAAAAAAAAAAAAAAAAAA')).toBe(true);
    });
  });
});
