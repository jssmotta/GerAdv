// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { IDocsRecebidosItens } from '../../Interfaces/interface.DocsRecebidosItens';
import { DocsRecebidosItensService } from '../../Services/DocsRecebidosItens.service';
import { DocsRecebidosItensApi } from '../../Apis/ApiDocsRecebidosItens';
import DocsRecebidosItensWindow from './DocsRecebidosItensWindow';
import {DocsRecebidosItensEmpty } from '@/app/GerAdv_TS/Models/DocsRecebidosItens';
interface DocsRecebidosItensWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const DocsRecebidosItensWindowId: React.FC<DocsRecebidosItensWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const docsrecebidositensService = useMemo(() => {
  return new DocsRecebidosItensService(
  new DocsRecebidosItensApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<IDocsRecebidosItens | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(DocsRecebidosItensEmpty() as IDocsRecebidosItens);
      return;
    }
    if (id) {
      const response = await docsrecebidositensService.fetchDocsRecebidosItensById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <DocsRecebidosItensWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedDocsRecebidosItens={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default DocsRecebidosItensWindowId;