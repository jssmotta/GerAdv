// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTAtividadesWindowId from '@/app/GerAdv_TS/GUTAtividades/Crud/Grids/GUTAtividadesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTAtividadesWindowId from '@/app/GerAdv_TS/GUTAtividades/Crud/Grids/GUTAtividadesWindowId';
import { GUTAtividadesEmpty } from '@/app/GerAdv_TS/Models/GUTAtividades';

describe('GUTAtividadesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTAtividadesWindowId).toBeDefined();
    expect(typeof GUTAtividadesWindowId === 'function' || typeof GUTAtividadesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTAtividadesService class so constructor usage doesn't throw and we can control fetchGUTAtividadesById
const mockFetchGUTAtividadesById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTAtividades/Services/GUTAtividades.service', () => ({
  GUTAtividadesService: jest.fn().mockImplementation(() => ({
    fetchGUTAtividadesById: (...args: any[]) => mockFetchGUTAtividadesById(...args),
  })),
}));

// Mock the child GUTAtividadesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTAtividades/Crud/Grids/GUTAtividadesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='gutatividades-window' {...p} />,
}));

describe('GUTAtividadesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTAtividadesWindowId).toBeDefined();
    expect(typeof GUTAtividadesWindowId === 'function' || typeof GUTAtividadesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTAtividadesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTAtividadesWindow with GUTAtividadesEmpty when id is 0', async () => {
    render(
      <GUTAtividadesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTAtividadesWindow should be rendered with selectedGUTAtividades equal to GUTAtividadesEmpty()
    const node = await screen.findByTestId('gutatividades-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTAtividades"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgutatividades') || node.getAttribute('selectedGUTAtividades')).not.toBeNull();
  });

  it('fetches gutatividades by id and passes it to GUTAtividadesWindow when id > 0', async () => {
    const fakeGUTAtividades = { id: 123, nomecliente: 'Test' };
    mockFetchGUTAtividadesById.mockResolvedValue(fakeGUTAtividades);

    render(
      <GUTAtividadesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('gutatividades-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTAtividadesById).toHaveBeenCalledWith(123));
  });
});
