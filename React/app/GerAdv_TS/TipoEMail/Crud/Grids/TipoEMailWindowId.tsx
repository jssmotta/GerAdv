// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { ITipoEMail } from '../../Interfaces/interface.TipoEMail';
import { TipoEMailService } from '../../Services/TipoEMail.service';
import { TipoEMailApi } from '../../Apis/ApiTipoEMail';
import TipoEMailWindow from './TipoEMailWindow';
import {TipoEMailEmpty } from '@/app/GerAdv_TS/Models/TipoEMail';
interface TipoEMailWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const TipoEMailWindowId: React.FC<TipoEMailWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const tipoemailService = useMemo(() => {
  return new TipoEMailService(
  new TipoEMailApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<ITipoEMail | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(TipoEMailEmpty() as ITipoEMail);
      return;
    }
    if (id) {
      const response = await tipoemailService.fetchTipoEMailById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <TipoEMailWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedTipoEMail={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default TipoEMailWindowId;