// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class SituacaoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFSituacaoFactory> _mockSituacaoFactory;
    private readonly Mock<ISituacaoReader> _mockReader;
    private readonly SituacaoValidation _validation;
    private readonly Mock<ISituacaoService> _mockSituacaoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly SituacaoService _service;
    private readonly string _validUri = "test-uri";
    public SituacaoValidationTests()
    {
        _mockSituacaoFactory = new Mock<IFSituacaoFactory>();
        _mockReader = new Mock<ISituacaoReader>();
        _validation = new SituacaoValidation();
        _mockSituacaoService = new Mock<ISituacaoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void SituacaoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ISituacaoValidation>();
    }

    [Fact]
    public void SituacaoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(SituacaoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void SituacaoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(SituacaoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Situacao CreateValidSituacao()
    {
        return new Models.Situacao
        {
            Id = 1,
            Parte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Parte_Opo = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Top = false
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockSituacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterSituacao>(), It.IsAny<string>())).ReturnsAsync(new List<SituacaoResponseAll>());
    // Setup other mocks but don't override the Situacaos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockSituacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterSituacao>(), It.IsAny<string>())).ReturnsAsync(new List<SituacaoResponseAll>());
    // Setup other mocks but don't override the Situacaos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var situacao = new Models.Situacao
        {
            Id = 1,
            Parte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Parte_Opo = null
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(situacao, _mockSituacaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockSituacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}