// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBEventoPrazoAgenda - Nome da classe (ex: DBClientes)
/// EventoPrazoAgenda - Nome da tabela (ex: Clientes)
/// epa - Prefixo dos campos (ex: cli)
/// epaCodigo - Nome do campo código (ex: cliCodigo)
/// epaNome - Nome do campo nome (ex: cliNome)
/// DBEventoPrazoAgenda.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBEventoPrazoAgendaTests : IDisposable
{
    private readonly DBEventoPrazoAgenda _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBEventoPrazoAgendaTests()
    {
        _instance = new DBEventoPrazoAgenda();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("epaCodigo", typeof(int));
        dt.Columns.Add("epaQuemCad", typeof(int));
        dt.Columns.Add("epaDtCad", typeof(DateTime));
        dt.Columns.Add("epaQuemAtu", typeof(int));
        dt.Columns.Add("epaDtAtu", typeof(DateTime));
        dt.Columns.Add("epaVisto", typeof(bool));
        dt.Columns.Add("epaNome", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("EventoPrazoAgenda", DBEventoPrazoAgenda.PTabelaNome);
        Assert.Equal(" EventoPrazoAgenda.* ", DBEventoPrazoAgenda.CamposSqlX);
        Assert.Equal("epaCodigo", DBEventoPrazoAgenda.CampoCodigo);
        Assert.Equal("epaNome", DBEventoPrazoAgenda.CampoNome);
        Assert.Equal("epa", DBEventoPrazoAgenda.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBEventoPrazoAgenda();
        Assert.Equal(0, instance.ID);
        Assert.Equal("EventoPrazoAgenda", instance.ITabelaName());
        Assert.Equal("epa", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBEventoPrazoAgenda((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBEventoPrazoAgenda((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["epaCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBEventoPrazoAgenda(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("EventoPrazoAgenda", cadastro.ITabelaName());
        Assert.Equal("epaCodigo", cadastro.ICampoCodigo());
        Assert.Equal("epaNome", cadastro.ICampoNome());
        Assert.Equal("epa", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBEventoPrazoAgenda.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBEventoPrazoAgenda.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBEventoPrazoAgenda.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBEventoPrazoAgenda();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBEventoPrazoAgenda();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 50 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 50);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}