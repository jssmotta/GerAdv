// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoOrigemSucumbenciaWindowId from '@/app/GerAdv_TS/TipoOrigemSucumbencia/Crud/Grids/TipoOrigemSucumbenciaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoOrigemSucumbenciaWindowId from '@/app/GerAdv_TS/TipoOrigemSucumbencia/Crud/Grids/TipoOrigemSucumbenciaWindowId';
import { TipoOrigemSucumbenciaEmpty } from '@/app/GerAdv_TS/Models/TipoOrigemSucumbencia';

describe('TipoOrigemSucumbenciaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoOrigemSucumbenciaWindowId).toBeDefined();
    expect(typeof TipoOrigemSucumbenciaWindowId === 'function' || typeof TipoOrigemSucumbenciaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoOrigemSucumbenciaService class so constructor usage doesn't throw and we can control fetchTipoOrigemSucumbenciaById
const mockFetchTipoOrigemSucumbenciaById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoOrigemSucumbencia/Services/TipoOrigemSucumbencia.service', () => ({
  TipoOrigemSucumbenciaService: jest.fn().mockImplementation(() => ({
    fetchTipoOrigemSucumbenciaById: (...args: any[]) => mockFetchTipoOrigemSucumbenciaById(...args),
  })),
}));

// Mock the child TipoOrigemSucumbenciaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoOrigemSucumbencia/Crud/Grids/TipoOrigemSucumbenciaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipoorigemsucumbencia-window' {...p} />,
}));

describe('TipoOrigemSucumbenciaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoOrigemSucumbenciaWindowId).toBeDefined();
    expect(typeof TipoOrigemSucumbenciaWindowId === 'function' || typeof TipoOrigemSucumbenciaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoOrigemSucumbenciaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoOrigemSucumbenciaWindow with TipoOrigemSucumbenciaEmpty when id is 0', async () => {
    render(
      <TipoOrigemSucumbenciaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoOrigemSucumbenciaWindow should be rendered with selectedTipoOrigemSucumbencia equal to TipoOrigemSucumbenciaEmpty()
    const node = await screen.findByTestId('tipoorigemsucumbencia-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoOrigemSucumbencia"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipoorigemsucumbencia') || node.getAttribute('selectedTipoOrigemSucumbencia')).not.toBeNull();
  });

  it('fetches tipoorigemsucumbencia by id and passes it to TipoOrigemSucumbenciaWindow when id > 0', async () => {
    const fakeTipoOrigemSucumbencia = { id: 123, nomecliente: 'Test' };
    mockFetchTipoOrigemSucumbenciaById.mockResolvedValue(fakeTipoOrigemSucumbencia);

    render(
      <TipoOrigemSucumbenciaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipoorigemsucumbencia-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoOrigemSucumbenciaById).toHaveBeenCalledWith(123));
  });
});
