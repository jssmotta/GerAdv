// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using MenphisSI.GerAdv.Models.Response.All;
using MenphisSI.GerAdv.Readers;
using System.Data;

namespace MenphisSI.GerAdv.Tests.Readers;
/// <summary>
/// Comprehensive unit tests for InstanciaReader class
/// Tests all public methods including async operations, data reading, and data transformation
/// </summary>
public class InstanciaReaderTests : IDisposable
{
    private readonly Mock<IFInstanciaFactory> _mockInstanciaFactory;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IDataRecord> _mockDataRecord;
    private readonly InstanciaReader _instanciaReader;
    public InstanciaReaderTests()
    {
        _mockInstanciaFactory = new Mock<IFInstanciaFactory>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockDataRecord = new Mock<IDataRecord>();
        _instanciaReader = new InstanciaReader(_mockInstanciaFactory.Object);
    }

#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldInitializeReader()
    {
        // Act & Assert
        _instanciaReader.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new InstanciaReader(null !));
    }

#endregion
#region ListarN Tests
    [Fact]
    public async Task ListarN_WithValidParameters_ShouldCallDevourerSqlData()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "insCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "insnroprocesso";
        // Act & Assert
        // Since this calls static methods and external dependencies,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task ListarN_WithEmptyParameters_ShouldThrowException()
    {
        // Arrange
        var max = 0;
        var uri = string.Empty; // Empty URI should cause an exception
        var cWhere = string.Empty;
        var parameters = new List<SqlParameter>();
        var order = string.Empty;
        // Act & Assert
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.ListarN(max, uri, cWhere, parameters, order));
    }

#endregion
#region Listar Tests
    [Fact]
    public async Task Listar_WithValidParameters_ShouldCallListarTabela()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "insCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // Since this calls external dependencies and database connections,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

    [Fact]
    public async Task Listar_WithCancellationToken_ShouldRespectCancellation()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri";
        var cWhere = "insCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = new CancellationToken(true); // Already cancelled
        // Act & Assert
        // Even with cancellation, this should throw an exception due to invalid URI
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Read Tests with ID and Connection
    [Fact]
    public async Task Read_WithValidId_ShouldReturnInstanciaResponse()
    {
        // Arrange
        var id = 123;
        var expectedInstancia = new FInstancia
        {
            ID = id,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedInstancia);
        // Act
        var result = await _instanciaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task Read_WithEmptyId_ShouldReturnNull()
    {
        // Arrange
        var id = 0; // Empty ID
        var emptyFInstancia = new FInstancia
        {
            ID = 0
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFInstancia);
        // Act
        var result = await _instanciaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public async Task Read_WithNegativeId_ShouldReturnNull()
    {
        // Arrange
        var id = -1;
        var emptyFInstancia = new FInstancia
        {
            ID = 0
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFInstancia);
        // Act
        var result = await _instanciaReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region ReadM Tests
    [Fact]
    public async Task ReadM_WithValidId_ShouldReturnInstanciaModel()
    {
        // Arrange
        var id = 123;
        var expectedFInstancia = new FInstancia
        {
            ID = id,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFInstancia);
        // Act
        var result = await _instanciaReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task ReadM_WithEmptyId_ShouldReturnModelWithDefaults()
    {
        // Arrange
        var id = 0;
        var emptyFInstancia = new FInstancia
        {
            ID = 0,
            FNroProcesso = null
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFInstancia);
        // Act
        var result = await _instanciaReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(0);
        result?.NroProcesso.Should().Be(string.Empty);
    }

#endregion
#region Read with FInstancia and Connection Tests
    [Fact]
    public void Read_WithValidFInstanciaAndConnection_ShouldReturnInstanciaResponse()
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _instanciaReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFInstanciaAndConnection_ShouldReturnNull()
    {
        // Arrange
        FInstancia? dbRec = null;
        // Act
        var result = _instanciaReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with Where and Parameters Tests
    [Fact]
    public void Read_WithWhereAndParameters_ShouldReturnInstanciaResponse()
    {
        // Arrange
        var where = "insCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedFInstancia = new FInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(expectedFInstancia);
        // Act
        var result = _instanciaReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithWhereAndParameters_WhenRecordNotFound_ShouldReturnNull()
    {
        // Arrange
        var where = "insCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 999)
        };
        var emptyFInstancia = new FInstancia
        {
            ID = 0
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(emptyFInstancia);
        // Act
        var result = _instanciaReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with FInstancia Tests
    [Fact]
    public void Read_WithValidFInstancia_ShouldReturnInstanciaResponse()
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFInstancia_ShouldReturnNull()
    {
        // Arrange
        FInstancia? dbRec = null;
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithFInstanciaNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = 123,
            FNroProcesso = null
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be(string.Empty);
    }

#endregion
#region Read with DBInstancia Tests
    [Fact]
    public void Read_WithValidSGDBInstancia_ShouldReturnInstanciaResponse()
    {
        // Arrange
        var dbRec = new DBInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullSGDBInstancia_ShouldReturnNull()
    {
        // Arrange
        DBInstancia? dbRec = null;
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithSGDBInstanciaNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBInstancia
        {
            ID = 123,
            FNroProcesso = null // This should result in empty string in response
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with FInstancia and IDataRecord Tests
    [Fact]
    public void ReadAll_WithValidFInstanciaAndDataRecord_ShouldReturnInstanciaResponseAll()
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullFInstanciaAndDataRecord_ShouldReturnNull()
    {
        // Arrange
        FInstancia? dbRec = null;
        // Act
        var result = _instanciaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithFInstanciaNullNomeAndDataRecord_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = 123,
            FNroProcesso = null
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with DBInstancia and DataRow Tests
    [Fact]
    public void ReadAll_WithValidSGDBInstanciaAndDataRow_ShouldReturnInstanciaResponseAll()
    {
        // Arrange
        var dbRec = new DBInstancia
        {
            ID = 123,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be("AAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullSGDBInstanciaAndDataRow_ShouldReturnNull()
    {
        // Arrange
        DBInstancia? dbRec = null;
        // Act
        var result = _instanciaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithSGDBInstanciaNullNomeAndDataRow_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBInstancia
        {
            ID = 123,
            FNroProcesso = null // This should result in empty string in response
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.NroProcesso.Should().Be(string.Empty);
    }

#endregion
#region Edge Cases and Error Handling Tests
    [Fact]
    public async Task Read_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _instanciaReader.Read(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task ReadM_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _instanciaReader.ReadM(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public void Read_WithWhereParameters_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var where = "insCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedException = new InvalidOperationException("Factory error");
        _mockInstanciaFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Throws(expectedException);
        // Act & Assert
        var exception = Assert.Throws<InvalidOperationException>(() => _instanciaReader.Read(where, parameters, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Data Consistency Tests
    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void Read_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = id,
            FNroProcesso = nome
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        if (id == 0)
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
        }
        else
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
            result?.NroProcesso.Should().Be(nome);
        }
    }

    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void ReadAll_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FInstancia
        {
            ID = id,
            FNroProcesso = nome
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.NroProcesso.Should().Be(nome);
    }

#endregion
#region Async Behavior Tests
    [Fact]
    public async Task AsyncMethods_ShouldCompleteWithoutBlocking()
    {
        // Arrange
        var id = 123;
        var expectedFInstancia = new FInstancia
        {
            ID = id,
            FNroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockInstanciaFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFInstancia);
        // Act & Assert
        // Test only the methods that don't depend on external URI connections
        var readTask = _instanciaReader.Read(id, _mockConnection.Object);
        var readMTask = _instanciaReader.ReadM(id, _mockConnection.Object);
        await Task.WhenAll(readTask, readMTask);
        // If we reach here, async methods completed without deadlock
        Assert.True(true);
    }

    [Fact]
    public async Task ListarN_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task Listar_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _instanciaReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Parameter Validation Tests
    [Fact]
    public async Task Read_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        // This would typically throw an exception or handle gracefully
        // depending on the implementation
        try
        {
            await _instanciaReader.Read(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public async Task ReadM_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        try
        {
            await _instanciaReader.ReadM(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public void Read_WithNullParametersList_ShouldHandleGracefully()
    {
        // Arrange
        var where = "insCodigo = @id";
        // Act & Assert
        try
        {
            _instanciaReader.Read(where, null !, _mockConnection.Object);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

#endregion
#region Integration-like Tests
    [Fact]
    public void Read_FInstancia_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 456;
        var testNome = "AAAAAAAAAAAAAAAAAAAAAAA";
        var dbRec = new FInstancia
        {
            ID = testId,
            FNroProcesso = testNome
        };
        // Act
        var result = _instanciaReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<InstanciaResponse>();
        result!.Id.Should().Be(testId);
        result.NroProcesso.Should().Be(testNome);
    }

    [Fact]
    public void ReadAll_FInstancia_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 789;
        var testNome = "AAAAAAAAAAAAAAAAAAAAAAA";
        var dbRec = new FInstancia
        {
            ID = testId,
            FNroProcesso = testNome
        };
        // Act
        var result = _instanciaReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<InstanciaResponseAll>();
        result!.Id.Should().Be(testId);
        result.NroProcesso.Should().Be(testNome);
    }

#endregion
#region Dispose
    public virtual void Dispose()
    {
        // Clean up any resources if needed
        GC.SuppressFinalize(this);
    }
#endregion
}