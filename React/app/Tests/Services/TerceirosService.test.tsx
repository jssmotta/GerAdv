// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TerceirosService, TerceirosValidator } from '@/app/GerAdv_TS/Terceiros/Services/Terceiros.service';
import { TerceirosApi, TerceirosApiError } from '@/app/GerAdv_TS/Terceiros/Apis/ApiTerceiros';
import { ITerceiros } from '@/app/GerAdv_TS/Terceiros/Interfaces/interface.Terceiros';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TerceirosApi;

const sample: ITerceiros = { id: 1, nome: 'Teste' };

describe('TerceirosService', () => {
  let service: TerceirosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TerceirosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TerceirosValidator, 'validateTerceiros').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTerceirosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTerceirosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTerceirosById lança TerceirosApiError para id inválido', async () => {
    await expect(service.fetchTerceirosById(0)).rejects.toBeInstanceOf(TerceirosApiError);
  });

  it('saveTerceiros chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTerceiros(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTerceiros lança TerceirosApiError quando validação falha', async () => {
    (TerceirosValidator.validateTerceiros as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTerceiros(sample)).rejects.toBeInstanceOf(TerceirosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTerceiros chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTerceiros(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTerceiros lança TerceirosApiError para id inválido', async () => {
    await expect(service.deleteTerceiros(0)).rejects.toBeInstanceOf(TerceirosApiError);
  });

  it('validateTerceiros delega para TerceirosValidator', () => {
    (TerceirosValidator.validateTerceiros as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTerceiros(sample);
    expect(r.isValid).toBe(true);
  });
});
