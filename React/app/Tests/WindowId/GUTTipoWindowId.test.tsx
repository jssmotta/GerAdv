// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTTipoWindowId from '@/app/GerAdv_TS/GUTTipo/Crud/Grids/GUTTipoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTTipoWindowId from '@/app/GerAdv_TS/GUTTipo/Crud/Grids/GUTTipoWindowId';
import { GUTTipoEmpty } from '@/app/GerAdv_TS/Models/GUTTipo';

describe('GUTTipoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTTipoWindowId).toBeDefined();
    expect(typeof GUTTipoWindowId === 'function' || typeof GUTTipoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTTipoService class so constructor usage doesn't throw and we can control fetchGUTTipoById
const mockFetchGUTTipoById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTTipo/Services/GUTTipo.service', () => ({
  GUTTipoService: jest.fn().mockImplementation(() => ({
    fetchGUTTipoById: (...args: any[]) => mockFetchGUTTipoById(...args),
  })),
}));

// Mock the child GUTTipoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTTipo/Crud/Grids/GUTTipoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='guttipo-window' {...p} />,
}));

describe('GUTTipoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTTipoWindowId).toBeDefined();
    expect(typeof GUTTipoWindowId === 'function' || typeof GUTTipoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTTipoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTTipoWindow with GUTTipoEmpty when id is 0', async () => {
    render(
      <GUTTipoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTTipoWindow should be rendered with selectedGUTTipo equal to GUTTipoEmpty()
    const node = await screen.findByTestId('guttipo-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTTipo"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedguttipo') || node.getAttribute('selectedGUTTipo')).not.toBeNull();
  });

  it('fetches guttipo by id and passes it to GUTTipoWindow when id > 0', async () => {
    const fakeGUTTipo = { id: 123, nomecliente: 'Test' };
    mockFetchGUTTipoById.mockResolvedValue(fakeGUTTipo);

    render(
      <GUTTipoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('guttipo-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTTipoById).toHaveBeenCalledWith(123));
  });
});
