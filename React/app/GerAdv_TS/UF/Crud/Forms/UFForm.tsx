// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IUF } from "@/app/GerAdv_TS/UF/Interfaces/interface.UF";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { UFApi } from "../../Apis/UFApi";
import { useValidationsUF } from "../../Hooks/hookUF";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpUF from "../../Help/HelpUF";
import UFPage from "@/app/pages/uf/page";

import PaisesComboBox from "@/app/GerAdv_TS/Paises/ComboBox/PaisesComboBox";
import { PaisesApi } from "@/app/GerAdv_TS/Paises/Apis/PaisesApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface UFFormProps {
  ufData: IUF;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const UFForm: React.FC<UFFormProps> = ({
  ufData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new UFApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsUF();

  const [_nomePaises, setNomePaises] = useState("");
  const paisesApi = new PaisesApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("paises") > 0) {
    if (ufData.id === 0 && ufData.pais == 0) {
      paisesApi
        .getById(getParamFromUrl("paises"))
        .then((response) => {
          setNomePaises(response.data.inome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      ufData.pais = getParamFromUrl("paises");
    }
  }
  const addValorPais = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "pais", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de UF:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`UFForm-${ufData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar UF diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent = ufData?.id == 0 ? "Editar UF" : "Adicionar UF";
    }
  }, [ufData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-UF"
            : "form-container5 form-container-UF"
        }
      >
        <form
          className="formInputCadInc"
          id={`UFForm-${ufData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="UF"
            data={ufData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`UFForm-${ufData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="iduf"
              label="ID"
              dataForm={ufData}
              className="input-default-main"
              name="iduf"
              value={ufData.iduf}
              placeholder={`Informe ID`}
              onChange={onChange}
              required
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={10}
              id="ddd"
              label="DDD"
              dataForm={ufData}
              className="input-default-main"
              name="ddd"
              value={ufData.ddd}
              onChange={onChange}
            />

            <PaisesComboBox
              name={"pais"}
              dataForm={ufData}
              value={ufData.pais}
              setValue={addValorPais}
              label={"Paises"}
            />

            <InputCheckbox
              dataForm={ufData}
              label="Top"
              name="top"
              checked={ufData.top}
              onChange={onChange}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={40}
              id="descricao"
              label="Descricao"
              dataForm={ufData}
              className="input-default-main"
              name="descricao"
              value={ufData.descricao}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="UF" windowCrud={UFPage} />

          <CopyButton formId={`UFForm-${ufData.id}`} />

          <PrintButton formId={`UFForm-${ufData.id}`} />

          <AuditorButton tableName="UF" coolName="UF" id={ufData.id} />

          <DeleteButton page={"/pages/uf"} id={ufData.id} dadoApi={dadoApi} />

          <HelpButton
            title="UF"
            page={(onClose) => <HelpUF onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="UF"
          data={ufData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`UFForm-${ufData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
