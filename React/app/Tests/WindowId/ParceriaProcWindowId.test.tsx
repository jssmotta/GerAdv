// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ParceriaProcWindowId from '@/app/GerAdv_TS/ParceriaProc/Crud/Grids/ParceriaProcWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ParceriaProcWindowId from '@/app/GerAdv_TS/ParceriaProc/Crud/Grids/ParceriaProcWindowId';
import { ParceriaProcEmpty } from '@/app/GerAdv_TS/Models/ParceriaProc';

describe('ParceriaProcWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ParceriaProcWindowId).toBeDefined();
    expect(typeof ParceriaProcWindowId === 'function' || typeof ParceriaProcWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ParceriaProcService class so constructor usage doesn't throw and we can control fetchParceriaProcById
const mockFetchParceriaProcById = jest.fn();
jest.mock('@/app/GerAdv_TS/ParceriaProc/Services/ParceriaProc.service', () => ({
  ParceriaProcService: jest.fn().mockImplementation(() => ({
    fetchParceriaProcById: (...args: any[]) => mockFetchParceriaProcById(...args),
  })),
}));

// Mock the child ParceriaProcWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ParceriaProc/Crud/Grids/ParceriaProcWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='parceriaproc-window' {...p} />,
}));

describe('ParceriaProcWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ParceriaProcWindowId).toBeDefined();
    expect(typeof ParceriaProcWindowId === 'function' || typeof ParceriaProcWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ParceriaProcWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ParceriaProcWindow with ParceriaProcEmpty when id is 0', async () => {
    render(
      <ParceriaProcWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ParceriaProcWindow should be rendered with selectedParceriaProc equal to ParceriaProcEmpty()
    const node = await screen.findByTestId('parceriaproc-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedParceriaProc"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedparceriaproc') || node.getAttribute('selectedParceriaProc')).not.toBeNull();
  });

  it('fetches parceriaproc by id and passes it to ParceriaProcWindow when id > 0', async () => {
    const fakeParceriaProc = { id: 123, nomecliente: 'Test' };
    mockFetchParceriaProcById.mockResolvedValue(fakeParceriaProc);

    render(
      <ParceriaProcWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('parceriaproc-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchParceriaProcById).toHaveBeenCalledWith(123));
  });
});
