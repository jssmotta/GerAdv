// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { BensMateriaisEmpty } from '../../../Models/BensMateriais';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IBensMateriais } from '../../Interfaces/interface.BensMateriais';
import { BensMateriaisService } from '../../Services/BensMateriais.service';
import { BensMateriaisApi } from '../../Apis/ApiBensMateriais';
import { BensMateriaisGridMobileComponent } from '../GridsMobile/BensMateriaisMobile';
import { BensMateriaisGridDesktopComponent } from '../GridsDesktop/BensMateriaisDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterBensMateriais } from '../../Filters/BensMateriais';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import BensMateriaisWindow from './BensMateriaisWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useBensMateriaisList } from '../../Hooks/hookBensMateriais';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useBensMateriaisFilter } from '../../Hooks/hookBensMateriaisFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const BensMateriaisGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedBensMateriais, setSelectedBensMateriais] = useState<IBensMateriais>(BensMateriaisEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterBensMateriais | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [bensmateriaisData, setBensMateriaisData] = useState<IBensMateriais[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const bensmateriaisService = useMemo(() => {
    return new BensMateriaisService(
    new BensMateriaisApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: bensmateriais, 
  fetchData, 
  refreshData
} = useBensMateriaisList(bensmateriaisService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterBensMateriais | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('BensMateriaisFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await bensmateriaisService.getAll(
  filtro ?? {} as FilterBensMateriais, 
  onlineData => {
    setBensMateriaisData(onlineData);
    setLoading(false);
  }
);
setBensMateriaisData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterBensMateriais;

    const filtroMem = sessionStorage.getItem(btoa('BensMateriaisFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('BensMateriaisFilter')) ?? '') || '{}') : {} as FilterBensMateriais;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (bensmateriais: IBensMateriais) => {
    setSelectedBensMateriais(bensmateriais);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedBensMateriais(BensMateriaisEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const bensmateriais = e.dataItem;
    setDeleteId(bensmateriais.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await bensmateriaisService.deleteBensMateriais(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useBensMateriaisFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <BensMateriaisGridMobileComponent
        data={bensmateriaisData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <BensMateriaisGridDesktopComponent
        data={bensmateriaisData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <BensMateriaisWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedBensMateriais={selectedBensMateriais}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Bens Materiais'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default BensMateriaisGrid;