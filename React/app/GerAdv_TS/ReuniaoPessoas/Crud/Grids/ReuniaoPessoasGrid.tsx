// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { ReuniaoPessoasEmpty } from '../../../Models/ReuniaoPessoas';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IReuniaoPessoas } from '../../Interfaces/interface.ReuniaoPessoas';
import { ReuniaoPessoasService } from '../../Services/ReuniaoPessoas.service';
import { ReuniaoPessoasApi } from '../../Apis/ApiReuniaoPessoas';
import { ReuniaoPessoasGridMobileComponent } from '../GridsMobile/ReuniaoPessoas';
import { ReuniaoPessoasGridDesktopComponent } from '../GridsDesktop/ReuniaoPessoas';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterReuniaoPessoas } from '../../Filters/ReuniaoPessoas';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import ReuniaoPessoasWindow from './ReuniaoPessoasWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useReuniaoPessoasList } from '../../Hooks/hookReuniaoPessoas';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useReuniaoPessoasFilter } from '../../Hooks/hookReuniaoPessoasFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const ReuniaoPessoasGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedReuniaoPessoas, setSelectedReuniaoPessoas] = useState<IReuniaoPessoas>(ReuniaoPessoasEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterReuniaoPessoas | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [reuniaopessoasData, setReuniaoPessoasData] = useState<IReuniaoPessoas[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const reuniaopessoasService = useMemo(() => {
    return new ReuniaoPessoasService(
    new ReuniaoPessoasApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: reuniaopessoas, 
  fetchData, 
  refreshData
} = useReuniaoPessoasList(reuniaopessoasService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterReuniaoPessoas | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('ReuniaoPessoasFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await reuniaopessoasService.getAll(
  filtro ?? {} as FilterReuniaoPessoas, 
  onlineData => {
    setReuniaoPessoasData(onlineData);
    setLoading(false);
  }
);
setReuniaoPessoasData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterReuniaoPessoas;

    const filtroMem = sessionStorage.getItem(btoa('ReuniaoPessoasFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('ReuniaoPessoasFilter')) ?? '') || '{}') : {} as FilterReuniaoPessoas;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (reuniaopessoas: IReuniaoPessoas) => {
    setSelectedReuniaoPessoas(reuniaopessoas);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedReuniaoPessoas(ReuniaoPessoasEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const reuniaopessoas = e.dataItem;
    setDeleteId(reuniaopessoas.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await reuniaopessoasService.deleteReuniaoPessoas(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useReuniaoPessoasFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <ReuniaoPessoasGridMobileComponent
        data={reuniaopessoasData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <ReuniaoPessoasGridDesktopComponent
        data={reuniaopessoasData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <ReuniaoPessoasWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedReuniaoPessoas={selectedReuniaoPessoas}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Reuniao Pessoas'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default ReuniaoPessoasGrid;