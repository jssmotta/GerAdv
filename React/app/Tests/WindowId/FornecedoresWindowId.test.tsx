// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import FornecedoresWindowId from '@/app/GerAdv_TS/Fornecedores/Crud/Grids/FornecedoresWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import FornecedoresWindowId from '@/app/GerAdv_TS/Fornecedores/Crud/Grids/FornecedoresWindowId';
import { FornecedoresEmpty } from '@/app/GerAdv_TS/Models/Fornecedores';

describe('FornecedoresWindowId (basic)', () => {
  it('exports the component', () => {
    expect(FornecedoresWindowId).toBeDefined();
    expect(typeof FornecedoresWindowId === 'function' || typeof FornecedoresWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the FornecedoresService class so constructor usage doesn't throw and we can control fetchFornecedoresById
const mockFetchFornecedoresById = jest.fn();
jest.mock('@/app/GerAdv_TS/Fornecedores/Services/Fornecedores.service', () => ({
  FornecedoresService: jest.fn().mockImplementation(() => ({
    fetchFornecedoresById: (...args: any[]) => mockFetchFornecedoresById(...args),
  })),
}));

// Mock the child FornecedoresWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Fornecedores/Crud/Grids/FornecedoresWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='fornecedores-window' {...p} />,
}));

describe('FornecedoresWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(FornecedoresWindowId).toBeDefined();
    expect(typeof FornecedoresWindowId === 'function' || typeof FornecedoresWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <FornecedoresWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders FornecedoresWindow with FornecedoresEmpty when id is 0', async () => {
    render(
      <FornecedoresWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // FornecedoresWindow should be rendered with selectedFornecedores equal to FornecedoresEmpty()
    const node = await screen.findByTestId('fornecedores-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedFornecedores"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedfornecedores') || node.getAttribute('selectedFornecedores')).not.toBeNull();
  });

  it('fetches fornecedores by id and passes it to FornecedoresWindow when id > 0', async () => {
    const fakeFornecedores = { id: 123, nomecliente: 'Test' };
    mockFetchFornecedoresById.mockResolvedValue(fakeFornecedores);

    render(
      <FornecedoresWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('fornecedores-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchFornecedoresById).toHaveBeenCalledWith(123));
  });
});
