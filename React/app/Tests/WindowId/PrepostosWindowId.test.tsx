// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PrepostosWindowId from '@/app/GerAdv_TS/Prepostos/Crud/Grids/PrepostosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PrepostosWindowId from '@/app/GerAdv_TS/Prepostos/Crud/Grids/PrepostosWindowId';
import { PrepostosEmpty } from '@/app/GerAdv_TS/Models/Prepostos';

describe('PrepostosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PrepostosWindowId).toBeDefined();
    expect(typeof PrepostosWindowId === 'function' || typeof PrepostosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PrepostosService class so constructor usage doesn't throw and we can control fetchPrepostosById
const mockFetchPrepostosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Prepostos/Services/Prepostos.service', () => ({
  PrepostosService: jest.fn().mockImplementation(() => ({
    fetchPrepostosById: (...args: any[]) => mockFetchPrepostosById(...args),
  })),
}));

// Mock the child PrepostosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Prepostos/Crud/Grids/PrepostosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='prepostos-window' {...p} />,
}));

describe('PrepostosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PrepostosWindowId).toBeDefined();
    expect(typeof PrepostosWindowId === 'function' || typeof PrepostosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PrepostosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PrepostosWindow with PrepostosEmpty when id is 0', async () => {
    render(
      <PrepostosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PrepostosWindow should be rendered with selectedPrepostos equal to PrepostosEmpty()
    const node = await screen.findByTestId('prepostos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPrepostos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprepostos') || node.getAttribute('selectedPrepostos')).not.toBeNull();
  });

  it('fetches prepostos by id and passes it to PrepostosWindow when id > 0', async () => {
    const fakePrepostos = { id: 123, nomecliente: 'Test' };
    mockFetchPrepostosById.mockResolvedValue(fakePrepostos);

    render(
      <PrepostosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('prepostos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPrepostosById).toHaveBeenCalledWith(123));
  });
});
