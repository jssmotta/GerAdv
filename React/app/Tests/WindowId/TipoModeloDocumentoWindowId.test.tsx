// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoModeloDocumentoWindowId from '@/app/GerAdv_TS/TipoModeloDocumento/Crud/Grids/TipoModeloDocumentoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoModeloDocumentoWindowId from '@/app/GerAdv_TS/TipoModeloDocumento/Crud/Grids/TipoModeloDocumentoWindowId';
import { TipoModeloDocumentoEmpty } from '@/app/GerAdv_TS/Models/TipoModeloDocumento';

describe('TipoModeloDocumentoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoModeloDocumentoWindowId).toBeDefined();
    expect(typeof TipoModeloDocumentoWindowId === 'function' || typeof TipoModeloDocumentoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoModeloDocumentoService class so constructor usage doesn't throw and we can control fetchTipoModeloDocumentoById
const mockFetchTipoModeloDocumentoById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoModeloDocumento/Services/TipoModeloDocumento.service', () => ({
  TipoModeloDocumentoService: jest.fn().mockImplementation(() => ({
    fetchTipoModeloDocumentoById: (...args: any[]) => mockFetchTipoModeloDocumentoById(...args),
  })),
}));

// Mock the child TipoModeloDocumentoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoModeloDocumento/Crud/Grids/TipoModeloDocumentoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipomodelodocumento-window' {...p} />,
}));

describe('TipoModeloDocumentoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoModeloDocumentoWindowId).toBeDefined();
    expect(typeof TipoModeloDocumentoWindowId === 'function' || typeof TipoModeloDocumentoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoModeloDocumentoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoModeloDocumentoWindow with TipoModeloDocumentoEmpty when id is 0', async () => {
    render(
      <TipoModeloDocumentoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoModeloDocumentoWindow should be rendered with selectedTipoModeloDocumento equal to TipoModeloDocumentoEmpty()
    const node = await screen.findByTestId('tipomodelodocumento-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoModeloDocumento"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipomodelodocumento') || node.getAttribute('selectedTipoModeloDocumento')).not.toBeNull();
  });

  it('fetches tipomodelodocumento by id and passes it to TipoModeloDocumentoWindow when id > 0', async () => {
    const fakeTipoModeloDocumento = { id: 123, nomecliente: 'Test' };
    mockFetchTipoModeloDocumentoById.mockResolvedValue(fakeTipoModeloDocumento);

    render(
      <TipoModeloDocumentoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipomodelodocumento-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoModeloDocumentoById).toHaveBeenCalledWith(123));
  });
});
