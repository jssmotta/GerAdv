// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { FornecedoresService, FornecedoresValidator } from '@/app/GerAdv_TS/Fornecedores/Services/Fornecedores.service';
import { FornecedoresApi, FornecedoresApiError } from '@/app/GerAdv_TS/Fornecedores/Apis/ApiFornecedores';
import { IFornecedores } from '@/app/GerAdv_TS/Fornecedores/Interfaces/interface.Fornecedores';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as FornecedoresApi;

const sample: IFornecedores = { id: 1, nome: 'Teste' };

describe('FornecedoresService', () => {
  let service: FornecedoresService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new FornecedoresService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(FornecedoresValidator, 'validateFornecedores').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchFornecedoresById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchFornecedoresById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchFornecedoresById lança FornecedoresApiError para id inválido', async () => {
    await expect(service.fetchFornecedoresById(0)).rejects.toBeInstanceOf(FornecedoresApiError);
  });

  it('saveFornecedores chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveFornecedores(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveFornecedores lança FornecedoresApiError quando validação falha', async () => {
    (FornecedoresValidator.validateFornecedores as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveFornecedores(sample)).rejects.toBeInstanceOf(FornecedoresApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteFornecedores chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteFornecedores(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteFornecedores lança FornecedoresApiError para id inválido', async () => {
    await expect(service.deleteFornecedores(0)).rejects.toBeInstanceOf(FornecedoresApiError);
  });

  it('validateFornecedores delega para FornecedoresValidator', () => {
    (FornecedoresValidator.validateFornecedores as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateFornecedores(sample);
    expect(r.isValid).toBe(true);
  });
});
