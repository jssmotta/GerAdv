// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:GUTPeriodicidade
// Fonte:ValidationsGeneratorTests
namespace MenphisSI.GerAdv.Tests;
public class GUTPeriodicidadeValidationTests : IDisposable
{
    private readonly GUTPeriodicidadeValidation _validation;
    private readonly Mock<IGUTPeriodicidadeService> _mockGUTPeriodicidadeService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly string _validUri = "test-uri";
    public GUTPeriodicidadeValidationTests()
    {
        _validation = new GUTPeriodicidadeValidation();
        _mockGUTPeriodicidadeService = new Mock<IGUTPeriodicidadeService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void GUTPeriodicidadeValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IGUTPeriodicidadeValidation>();
    }

    [Fact]
    public void GUTPeriodicidadeValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(GUTPeriodicidadeValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void GUTPeriodicidadeValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(GUTPeriodicidadeValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static GerAdv.Models.GUTPeriodicidade CreateValidGUTPeriodicidade()
    {
        return new GerAdv.Models.GUTPeriodicidade
        {
            Id = 1,
            Nome = "João",
            IntervaloDias = 0
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockGUTPeriodicidadeService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterGUTPeriodicidade>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the GUTPeriodicidades service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockGUTPeriodicidadeService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterGUTPeriodicidade>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the GUTPeriodicidades service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var gutperiodicidade = new GerAdv.Models.GUTPeriodicidade
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(gutperiodicidade, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null !, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var gutperiodicidade = CreateValidGUTPeriodicidade();
        gutperiodicidade.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(gutperiodicidade, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().MatchRegex("(é obrigatório|não encontrado)");
    }

    [Fact]
    public async Task ValidateReg_WithNull_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var gutperiodicidade = CreateValidGUTPeriodicidade();
        gutperiodicidade.Nome = null !;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(gutperiodicidade, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidData_Nome_ShouldReturnTrue()
    {
        // Arrange
        var gutperiodicidade = CreateValidGUTPeriodicidade();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(gutperiodicidade, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespace_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var gutperiodicidade = CreateValidGUTPeriodicidade();
        gutperiodicidade.Nome = " ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(gutperiodicidade, _mockGUTPeriodicidadeService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        GC.SuppressFinalize(this);
    }
}