// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { ICidade } from "@/app/GerAdv_TS/Cidade/Interfaces/interface.Cidade";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { CidadeApi } from "../../Apis/CidadeApi";
import { useValidationsCidade } from "../../Hooks/hookCidade";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpCidade from "../../Help/HelpCidade";
import CidadePage from "@/app/pages/cidade/page";

import UFComboBox from "@/app/GerAdv_TS/UF/ComboBox/UFComboBox";
import { UFApi } from "@/app/GerAdv_TS/UF/Apis/UFApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface CidadeFormProps {
  cidadeData: ICidade;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const CidadeForm: React.FC<CidadeFormProps> = ({
  cidadeData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new CidadeApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsCidade();

  const [_nomeUF, setNomeUF] = useState("");
  const ufApi = new UFApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("uf") > 0) {
    if (cidadeData.id === 0 && cidadeData.uf == 0) {
      ufApi
        .getById(getParamFromUrl("uf"))
        .then((response) => {
          setNomeUF(response.data.d);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      cidadeData.uf = getParamFromUrl("uf");
    }
  }
  const addValorUF = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "uf", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Cidade:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`CidadeForm-${cidadeData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Cidade diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        cidadeData?.id == 0 ? "Editar Cidade" : "Adicionar Cidade";
    }
  }, [cidadeData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Cidade"
            : "form-container5 form-container-Cidade"
        }
      >
        <form
          className="formInputCadInc"
          id={`CidadeForm-${cidadeData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Cidade"
            data={cidadeData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`CidadeForm-${cidadeData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={cidadeData}
              className="input-default-main"
              name="nome"
              value={cidadeData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={10}
              id="ddd"
              label="DDD"
              dataForm={cidadeData}
              className="input-default-main"
              name="ddd"
              value={cidadeData.ddd}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={cidadeData}
              label="Top"
              name="top"
              checked={cidadeData.top}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={cidadeData}
              label="Comarca"
              name="comarca"
              checked={cidadeData.comarca}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={cidadeData}
              label="Capital"
              name="capital"
              checked={cidadeData.capital}
              onChange={onChange}
            />

            <UFComboBox
              name={"uf"}
              dataForm={cidadeData}
              value={cidadeData.uf}
              setValue={addValorUF}
              label={"UF"}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={10}
              id="sigla"
              label="Sigla"
              dataForm={cidadeData}
              className="input-default-main"
              name="sigla"
              value={cidadeData.sigla}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Cidade" windowCrud={CidadePage} />

          <CopyButton formId={`CidadeForm-${cidadeData.id}`} />

          <PrintButton formId={`CidadeForm-${cidadeData.id}`} />

          <AuditorButton
            tableName="Cidade"
            coolName="Cidade"
            id={cidadeData.id}
          />

          <DeleteButton
            page={"/pages/cidade"}
            id={cidadeData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Cidade"
            page={(onClose) => <HelpCidade onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Cidade"
          data={cidadeData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`CidadeForm-${cidadeData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
