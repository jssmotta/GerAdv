// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:TipoStatusBiu
// Fonte:ControllerGenerator
namespace MenphisSI.GerAdv.Controller;
[Route("api/v{version:apiVersion}/{uri}/[controller]/[action]")]
[ApiController]
[ApiVersion("1.0")]
public partial class TipoStatusBiuController(ITipoStatusBiuService tipostatusbiuService) : ControllerBase
{
    private readonly ITipoStatusBiuService _tipostatusbiuService = tipostatusbiuService;
    private static readonly Logger _logger = LogManager.GetCurrentClassLogger();
    [HttpGet]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAll([FromQuery] int max, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "GetAll";
        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.GetAll(max, uri);
            });
            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoStatusBiuMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: GetAll failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Filter([FromQuery] int max, [FromBody] MenphisSI.GerAdv.Filters.FilterTipoStatusBiu filter, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "Filter";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.Filter(max, filter, uri);
            });
            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoStatusBiuMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: Filter failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> FilterVoice([FromBody] MenphisSI.GerAdv.Filters.FilterTipoStatusBiuWithVoiceRequest request, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "FilterVoice";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.RelatoriosPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.FilterVoice(request.Filter, request.VoiceCommand, uri);
            });
            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: FilterVoice failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetById(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "GetById";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.GetById(id, uri, token);
            });
            if (result == null)
            {
                TipoStatusBiuMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetById: No TipoStatusBiu found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoStatusBiuMetrics.RecordReadByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: GetById failed with exception for id = {0}, {1}", id, uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetListN([FromQuery] int max, [FromBody] Filters.FilterTipoStatusBiu? filtro, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "GetListN";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.GetListN(max, filtro, uri);
            });
            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoStatusBiuMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: GetListN failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [ProducesResponseType(typeof(TipoStatusBiuResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(TipoStatusBiuResponse), StatusCodes.Status201Created)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> AddAndUpdate([FromBody] Models.TipoStatusBiu regTipoStatusBiu, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        var isNew = regTipoStatusBiu.Id == 0;
        var operacao = isNew ? "Create" : "Update";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.AddAndUpdate(regTipoStatusBiu, uri);
            });
            if (result == null)
            {
                TipoStatusBiuMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("TipoStatusBiu: AddAndUpdate failed to add or update TipoStatusBiu, {0}", uri);
                return BadRequest();
            }

            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            if (isNew)
            {
                TipoStatusBiuMetrics.RecordCreated(uri);
                TipoStatusBiuMetrics.RecordCreatedByHour(uri);
            }
            else
            {
                TipoStatusBiuMetrics.RecordUpdated(uri);
                TipoStatusBiuMetrics.RecordUpdatedByHour(uri);
            }

            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: AddAndUpdate failed with exception for uri = {0}", uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = ex.Message });
        }
    }

    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [HttpDelete]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> Delete([FromQuery] int id, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "Delete";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.Delete(id, uri);
            });
            if (result == null)
            {
                TipoStatusBiuMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("Delete: No TipoStatusBiu found to delete with id = {0}, {1}", id, uri);
                return NotFound();
            }

            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoStatusBiuMetrics.RecordDeleted(uri);
            TipoStatusBiuMetrics.RecordDeletedByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordConflict(operacao, uri, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: Delete failed with exception for id = {0}, {1}", id, uri);
            return Conflict(new Error500 { success = false, data = "", message = "Não é possível excluir o registro porque ele está sendo referenciado/em uso em outra tabela." });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Validation([FromBody] Models.TipoStatusBiu regTipoStatusBiu, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoStatusBiuMetrics.StartTimer();
        const string operacao = "Validation";
        if (!ModelState.IsValid)
        {
            TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipostatusbiuService.Validation(regTipoStatusBiu, uri);
            });
            if (result == null)
            {
                TipoStatusBiuMetrics.RecordInvalid(operacao, uri, stopwatch);
                _logger.Warn("TipoStatusBiu: Validation failed to add or update TipoStatusBiu, {0}", uri);
                return BadRequest();
            }

            TipoStatusBiuMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoStatusBiuMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoStatusBiu: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoStatusBiuMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoStatusBiu: Validation failed with exception for uri = {0}", uri);
            return StatusCode(500, new { success = false, data = "", message = ex.Message });
        }
    }
}