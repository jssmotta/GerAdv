// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { TipoEnderecoSistemaEmpty } from '../../../Models/TipoEnderecoSistema';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { ITipoEnderecoSistema } from '../../Interfaces/interface.TipoEnderecoSistema';
import { TipoEnderecoSistemaService } from '../../Services/TipoEnderecoSistema.service';
import { TipoEnderecoSistemaApi } from '../../Apis/ApiTipoEnderecoSistema';
import { TipoEnderecoSistemaGridMobileComponent } from '../GridsMobile/TipoEnderecoSistemaMobile';
import { TipoEnderecoSistemaGridDesktopComponent } from '../GridsDesktop/TipoEnderecoSistemaDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterTipoEnderecoSistema } from '../../Filters/TipoEnderecoSistema';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import TipoEnderecoSistemaWindow from './TipoEnderecoSistemaWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useTipoEnderecoSistemaList } from '../../Hooks/hookTipoEnderecoSistema';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useTipoEnderecoSistemaFilter } from '../../Hooks/hookTipoEnderecoSistemaFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const TipoEnderecoSistemaGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedTipoEnderecoSistema, setSelectedTipoEnderecoSistema] = useState<ITipoEnderecoSistema>(TipoEnderecoSistemaEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterTipoEnderecoSistema | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [tipoenderecosistemaData, setTipoEnderecoSistemaData] = useState<ITipoEnderecoSistema[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const tipoenderecosistemaService = useMemo(() => {
    return new TipoEnderecoSistemaService(
    new TipoEnderecoSistemaApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: tipoenderecosistema, 
  fetchData, 
  refreshData
} = useTipoEnderecoSistemaList(tipoenderecosistemaService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterTipoEnderecoSistema | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('TipoEnderecoSistemaFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await tipoenderecosistemaService.getAll(
  filtro ?? {} as FilterTipoEnderecoSistema, 
  onlineData => {
    setTipoEnderecoSistemaData(onlineData);
    setLoading(false);
  }
);
setTipoEnderecoSistemaData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterTipoEnderecoSistema;

    const filtroMem = sessionStorage.getItem(btoa('TipoEnderecoSistemaFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('TipoEnderecoSistemaFilter')) ?? '') || '{}') : {} as FilterTipoEnderecoSistema;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (tipoenderecosistema: ITipoEnderecoSistema) => {
    setSelectedTipoEnderecoSistema(tipoenderecosistema);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedTipoEnderecoSistema(TipoEnderecoSistemaEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const tipoenderecosistema = e.dataItem;
    setDeleteId(tipoenderecosistema.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await tipoenderecosistemaService.deleteTipoEnderecoSistema(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao excluir');
        setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
      } finally {
      setDeleteId(null);
      setIsModalOpen(false);
    }
  }
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useTipoEnderecoSistemaFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <TipoEnderecoSistemaGridMobileComponent
        data={tipoenderecosistemaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <TipoEnderecoSistemaGridDesktopComponent
        data={tipoenderecosistemaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <TipoEnderecoSistemaWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedTipoEnderecoSistema={selectedTipoEnderecoSistema}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Tipo Endereco Sistema'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default TipoEnderecoSistemaGrid;