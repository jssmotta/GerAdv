// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class TipoCompromissoWriterTests
{
    private readonly Mock<IFTipoCompromissoFactory> _mockTipoCompromissoFactory;
    private readonly Mock<FTipoCompromisso> _mockFTipoCompromisso;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoCompromissoWriter _tipocompromissoWriter;
    public TipoCompromissoWriterTests()
    {
        _mockTipoCompromissoFactory = new Mock<IFTipoCompromissoFactory>();
        _mockFTipoCompromisso = new Mock<FTipoCompromisso>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _tipocompromissoWriter = new TipoCompromissoWriter(_mockTipoCompromissoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var tipocompromisso = CreateValidTipoCompromissoModel();
        tipocompromisso.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockTipoCompromissoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoCompromisso.Object);
        _mockFTipoCompromisso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tipocompromissoWriter.WriteAsync(tipocompromisso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoCompromisso.Object);
        _mockTipoCompromissoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockTipoCompromissoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFTipoCompromisso.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var tipocompromisso = CreateValidTipoCompromissoModel();
        tipocompromisso.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockTipoCompromissoFactory.Setup(x => x.CreateFromIdAsync(tipocompromisso.Id, _mockConnection.Object)).ReturnsAsync(_mockFTipoCompromisso.Object);
        _mockFTipoCompromisso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _tipocompromissoWriter.WriteAsync(tipocompromisso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoCompromisso.Object);
        _mockTipoCompromissoFactory.Verify(x => x.CreateFromIdAsync(tipocompromisso.Id, _mockConnection.Object), Times.Once);
        _mockTipoCompromissoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFTipoCompromisso.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var tipocompromisso = CreateValidTipoCompromissoModel();
        var auditorQuem = 99;
        _mockTipoCompromissoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoCompromisso.Object);
        _mockFTipoCompromisso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tipocompromissoWriter.WriteAsync(tipocompromisso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoCompromisso.Object);
        _mockFTipoCompromisso.VerifySet(x => x.FGUID = tipocompromisso.GUID, Times.Once);
        _mockFTipoCompromisso.VerifySet(x => x.FIcone = tipocompromisso.Icone, Times.Once);
        _mockFTipoCompromisso.VerifySet(x => x.FDescricao = tipocompromisso.Descricao, Times.Once);
        _mockFTipoCompromisso.VerifySet(x => x.FFinanceiro = tipocompromisso.Financeiro, Times.Once);
        _mockFTipoCompromisso.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var tipocompromisso = CreateValidTipoCompromissoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoCompromissoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipocompromissoWriter.WriteAsync(tipocompromisso, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tipocompromisso = CreateValidTipoCompromissoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoCompromissoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoCompromisso.Object);
        _mockFTipoCompromisso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipocompromissoWriter.WriteAsync(tipocompromisso, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var tipocompromissoResponse = new TipoCompromissoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockTipoCompromissoFactory.Setup(x => x.DeleteAsync(operadorId, tipocompromissoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _tipocompromissoWriter.Delete(tipocompromissoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockTipoCompromissoFactory.Verify(x => x.DeleteAsync(operadorId, tipocompromissoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var tipocompromissoResponse = new TipoCompromissoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockTipoCompromissoFactory.Setup(x => x.DeleteAsync(operadorId, tipocompromissoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _tipocompromissoWriter.Delete(tipocompromissoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tipocompromissoResponse = new TipoCompromissoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockTipoCompromissoFactory.Setup(x => x.DeleteAsync(operadorId, tipocompromissoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipocompromissoWriter.Delete(tipocompromissoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new TipoCompromissoWriter(_mockTipoCompromissoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new TipoCompromissoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.TipoCompromisso CreateValidTipoCompromissoModel()
    {
        return new Models.TipoCompromisso
        {
            Id = 0,
            GUID = Guid.NewGuid().ToString(),
            Icone = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Financeiro = false
        };
    }
#endregion
}