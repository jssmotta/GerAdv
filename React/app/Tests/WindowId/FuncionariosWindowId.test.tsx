// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import FuncionariosWindowId from '@/app/GerAdv_TS/Funcionarios/Crud/Grids/FuncionariosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import FuncionariosWindowId from '@/app/GerAdv_TS/Funcionarios/Crud/Grids/FuncionariosWindowId';
import { FuncionariosEmpty } from '@/app/GerAdv_TS/Models/Funcionarios';

describe('FuncionariosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(FuncionariosWindowId).toBeDefined();
    expect(typeof FuncionariosWindowId === 'function' || typeof FuncionariosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the FuncionariosService class so constructor usage doesn't throw and we can control fetchFuncionariosById
const mockFetchFuncionariosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Funcionarios/Services/Funcionarios.service', () => ({
  FuncionariosService: jest.fn().mockImplementation(() => ({
    fetchFuncionariosById: (...args: any[]) => mockFetchFuncionariosById(...args),
  })),
}));

// Mock the child FuncionariosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Funcionarios/Crud/Grids/FuncionariosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='funcionarios-window' {...p} />,
}));

describe('FuncionariosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(FuncionariosWindowId).toBeDefined();
    expect(typeof FuncionariosWindowId === 'function' || typeof FuncionariosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <FuncionariosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders FuncionariosWindow with FuncionariosEmpty when id is 0', async () => {
    render(
      <FuncionariosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // FuncionariosWindow should be rendered with selectedFuncionarios equal to FuncionariosEmpty()
    const node = await screen.findByTestId('funcionarios-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedFuncionarios"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedfuncionarios') || node.getAttribute('selectedFuncionarios')).not.toBeNull();
  });

  it('fetches funcionarios by id and passes it to FuncionariosWindow when id > 0', async () => {
    const fakeFuncionarios = { id: 123, nomecliente: 'Test' };
    mockFetchFuncionariosById.mockResolvedValue(fakeFuncionarios);

    render(
      <FuncionariosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('funcionarios-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchFuncionariosById).toHaveBeenCalledWith(123));
  });
});
