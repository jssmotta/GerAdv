// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTPeriodicidadeStatusWindowId from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Crud/Grids/GUTPeriodicidadeStatusWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTPeriodicidadeStatusWindowId from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Crud/Grids/GUTPeriodicidadeStatusWindowId';
import { GUTPeriodicidadeStatusEmpty } from '@/app/GerAdv_TS/Models/GUTPeriodicidadeStatus';

describe('GUTPeriodicidadeStatusWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTPeriodicidadeStatusWindowId).toBeDefined();
    expect(typeof GUTPeriodicidadeStatusWindowId === 'function' || typeof GUTPeriodicidadeStatusWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTPeriodicidadeStatusService class so constructor usage doesn't throw and we can control fetchGUTPeriodicidadeStatusById
const mockFetchGUTPeriodicidadeStatusById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTPeriodicidadeStatus/Services/GUTPeriodicidadeStatus.service', () => ({
  GUTPeriodicidadeStatusService: jest.fn().mockImplementation(() => ({
    fetchGUTPeriodicidadeStatusById: (...args: any[]) => mockFetchGUTPeriodicidadeStatusById(...args),
  })),
}));

// Mock the child GUTPeriodicidadeStatusWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTPeriodicidadeStatus/Crud/Grids/GUTPeriodicidadeStatusWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='gutperiodicidadestatus-window' {...p} />,
}));

describe('GUTPeriodicidadeStatusWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTPeriodicidadeStatusWindowId).toBeDefined();
    expect(typeof GUTPeriodicidadeStatusWindowId === 'function' || typeof GUTPeriodicidadeStatusWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTPeriodicidadeStatusWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTPeriodicidadeStatusWindow with GUTPeriodicidadeStatusEmpty when id is 0', async () => {
    render(
      <GUTPeriodicidadeStatusWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTPeriodicidadeStatusWindow should be rendered with selectedGUTPeriodicidadeStatus equal to GUTPeriodicidadeStatusEmpty()
    const node = await screen.findByTestId('gutperiodicidadestatus-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTPeriodicidadeStatus"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgutperiodicidadestatus') || node.getAttribute('selectedGUTPeriodicidadeStatus')).not.toBeNull();
  });

  it('fetches gutperiodicidadestatus by id and passes it to GUTPeriodicidadeStatusWindow when id > 0', async () => {
    const fakeGUTPeriodicidadeStatus = { id: 123, nomecliente: 'Test' };
    mockFetchGUTPeriodicidadeStatusById.mockResolvedValue(fakeGUTPeriodicidadeStatus);

    render(
      <GUTPeriodicidadeStatusWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('gutperiodicidadestatus-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTPeriodicidadeStatusById).toHaveBeenCalledWith(123));
  });
});
