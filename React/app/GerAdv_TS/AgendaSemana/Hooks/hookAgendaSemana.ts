// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IAgendaSemanaService } from '../Services/AgendaSemana.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IAgendaSemana } from '../Interfaces/interface.AgendaSemana';
import { FilterAgendaSemana } from '../Filters/AgendaSemana';
import { isValidDate } from '@/app/tools/datetime';
import { AgendaSemanaApi } from '../Apis/ApiAgendaSemana';

export const useAgendaSemanaForm = (
  initialAgendaSemana: IAgendaSemana,
  dataService: IAgendaSemanaService
) => {
  const [data, setData] = useState<IAgendaSemana>(initialAgendaSemana);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadAgendaSemana = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialAgendaSemana);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchAgendaSemanaById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Agenda Semana';
      setError(errorMessage);
      //console.log('Erro ao carregar Agenda Semana');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialAgendaSemana]);

  const resetForm = useCallback(() => {
    setData(initialAgendaSemana);
    setError(null);
  }, [initialAgendaSemana]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadAgendaSemana,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadAgendaSemana, resetForm]);
  return returnValue;
};


export const useAgendaSemanaNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('AgendaSemana', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useAgendaSemanaList = (dataService: IAgendaSemanaService, currentFilter?: FilterAgendaSemana | undefined | null) => {
  const [data, setData] = useState<IAgendaSemana[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterAgendaSemana | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterAgendaSemana | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterAgendaSemana);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar agendasemana';
      setError(errorMessage);
      //console.log('Erro ao carregar agendasemana');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useAgendaSemanaNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsAgendaSemana() {

  async function runValidation(data: IAgendaSemana, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const agendasemanaApi = new AgendaSemanaApi(uri ?? '', token ?? '');

    const result = await agendasemanaApi.validation(data);

    return result;
  }

  function validate(data: IAgendaSemana): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.paranome.length <= 0) { 
                                             return { isValid: false, message: 'O campo ParaNome não pode ficar vazio.' };
                                         } 
if (data.paranome.length > 60) { 
                                             return { isValid: false, message: 'O campo ParaNome não pode ter mais de 60 caracteres.' };
                                         } 
if (data.compromisso.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Compromisso não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.nome.length > 80) { 
                                             return { isValid: false, message: 'O campo Nome não pode ter mais de 80 caracteres.' };
                                         } 
if (data.nomecliente.length > 80) { 
                                             return { isValid: false, message: 'O campo NomeCliente não pode ter mais de 80 caracteres.' };
                                         } 
if (data.tipo.length > 100) { 
                                             return { isValid: false, message: 'O campo Tipo não pode ter mais de 100 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}