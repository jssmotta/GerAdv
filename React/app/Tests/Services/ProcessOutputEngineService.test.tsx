// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProcessOutputEngineService, ProcessOutputEngineValidator } from '@/app/GerAdv_TS/ProcessOutputEngine/Services/ProcessOutputEngine.service';
import { ProcessOutputEngineApi, ProcessOutputEngineApiError } from '@/app/GerAdv_TS/ProcessOutputEngine/Apis/ApiProcessOutputEngine';
import { IProcessOutputEngine } from '@/app/GerAdv_TS/ProcessOutputEngine/Interfaces/interface.ProcessOutputEngine';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProcessOutputEngineApi;

const sample: IProcessOutputEngine = { id: 1, nome: 'Teste' };

describe('ProcessOutputEngineService', () => {
  let service: ProcessOutputEngineService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProcessOutputEngineService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProcessOutputEngineValidator, 'validateProcessOutputEngine').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProcessOutputEngineById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProcessOutputEngineById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProcessOutputEngineById lança ProcessOutputEngineApiError para id inválido', async () => {
    await expect(service.fetchProcessOutputEngineById(0)).rejects.toBeInstanceOf(ProcessOutputEngineApiError);
  });

  it('saveProcessOutputEngine chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProcessOutputEngine(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProcessOutputEngine lança ProcessOutputEngineApiError quando validação falha', async () => {
    (ProcessOutputEngineValidator.validateProcessOutputEngine as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProcessOutputEngine(sample)).rejects.toBeInstanceOf(ProcessOutputEngineApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProcessOutputEngine chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProcessOutputEngine(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProcessOutputEngine lança ProcessOutputEngineApiError para id inválido', async () => {
    await expect(service.deleteProcessOutputEngine(0)).rejects.toBeInstanceOf(ProcessOutputEngineApiError);
  });

  it('validateProcessOutputEngine delega para ProcessOutputEngineValidator', () => {
    (ProcessOutputEngineValidator.validateProcessOutputEngine as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProcessOutputEngine(sample);
    expect(r.isValid).toBe(true);
  });
});
