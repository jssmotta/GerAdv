// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ColaboradoresWindowId from '@/app/GerAdv_TS/Colaboradores/Crud/Grids/ColaboradoresWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ColaboradoresWindowId from '@/app/GerAdv_TS/Colaboradores/Crud/Grids/ColaboradoresWindowId';
import { ColaboradoresEmpty } from '@/app/GerAdv_TS/Models/Colaboradores';

describe('ColaboradoresWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ColaboradoresWindowId).toBeDefined();
    expect(typeof ColaboradoresWindowId === 'function' || typeof ColaboradoresWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ColaboradoresService class so constructor usage doesn't throw and we can control fetchColaboradoresById
const mockFetchColaboradoresById = jest.fn();
jest.mock('@/app/GerAdv_TS/Colaboradores/Services/Colaboradores.service', () => ({
  ColaboradoresService: jest.fn().mockImplementation(() => ({
    fetchColaboradoresById: (...args: any[]) => mockFetchColaboradoresById(...args),
  })),
}));

// Mock the child ColaboradoresWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Colaboradores/Crud/Grids/ColaboradoresWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='colaboradores-window' {...p} />,
}));

describe('ColaboradoresWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ColaboradoresWindowId).toBeDefined();
    expect(typeof ColaboradoresWindowId === 'function' || typeof ColaboradoresWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ColaboradoresWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ColaboradoresWindow with ColaboradoresEmpty when id is 0', async () => {
    render(
      <ColaboradoresWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ColaboradoresWindow should be rendered with selectedColaboradores equal to ColaboradoresEmpty()
    const node = await screen.findByTestId('colaboradores-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedColaboradores"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcolaboradores') || node.getAttribute('selectedColaboradores')).not.toBeNull();
  });

  it('fetches colaboradores by id and passes it to ColaboradoresWindow when id > 0', async () => {
    const fakeColaboradores = { id: 123, nomecliente: 'Test' };
    mockFetchColaboradoresById.mockResolvedValue(fakeColaboradores);

    render(
      <ColaboradoresWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('colaboradores-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchColaboradoresById).toHaveBeenCalledWith(123));
  });
});
