// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IProcessosParadosService } from '../Services/ProcessosParados.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IProcessosParados } from '../Interfaces/interface.ProcessosParados';
import { FilterProcessosParados } from '../Filters/ProcessosParados';
import { isValidDate } from '@/app/tools/datetime';
import { ProcessosParadosApi } from '../Apis/ApiProcessosParados';

export const useProcessosParadosForm = (
  initialProcessosParados: IProcessosParados,
  dataService: IProcessosParadosService
) => {
  const [data, setData] = useState<IProcessosParados>(initialProcessosParados);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadProcessosParados = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialProcessosParados);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchProcessosParadosById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Processos Parados';
      setError(errorMessage);
      //console.log('Erro ao carregar Processos Parados');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialProcessosParados]);

  const resetForm = useCallback(() => {
    setData(initialProcessosParados);
    setError(null);
  }, [initialProcessosParados]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadProcessosParados,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadProcessosParados, resetForm]);
  return returnValue;
};


export const useProcessosParadosNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('ProcessosParados', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useProcessosParadosList = (dataService: IProcessosParadosService, currentFilter?: FilterProcessosParados | undefined | null) => {
  const [data, setData] = useState<IProcessosParados[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterProcessosParados | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterProcessosParados | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterProcessosParados);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar processosparados';
      setError(errorMessage);
      //console.log('Erro ao carregar processosparados');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useProcessosParadosNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsProcessosParados() {

  async function runValidation(data: IProcessosParados, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const processosparadosApi = new ProcessosParadosApi(uri ?? '', token ?? '');

    const result = await processosparadosApi.validation(data);

    return result;
  }

  function validate(data: IProcessosParados): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}