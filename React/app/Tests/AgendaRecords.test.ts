// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useAgendaRecordsForm, useAgendaRecordsList, useValidationsAgendaRecords } from '../GerAdv_TS/AgendaRecords/Hooks/hookAgendaRecords';
import { IAgendaRecords } from '../GerAdv_TS/AgendaRecords/Interfaces/interface.AgendaRecords';
import { IAgendaRecordsService } from '../GerAdv_TS/AgendaRecords/Services/AgendaRecords.service';
import { AgendaRecordsTestEmpty } from '../GerAdv_TS/Models/AgendaRecords';


// Mock do serviço
const mockAgendaRecordsService: jest.Mocked<IAgendaRecordsService> = {
  fetchAgendaRecordsById: jest.fn(),
  saveAgendaRecords: jest.fn(),
  
  getAll: jest.fn(),
  deleteAgendaRecords: jest.fn(),
  validateAgendaRecords: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialAgendaRecords: IAgendaRecords = { ...AgendaRecordsTestEmpty() };

describe('useAgendaRecordsForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    expect(result.current.data).toEqual(initialAgendaRecords);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    const mockEvent = {
      target: {
        name: 'campo',
        value: 'Novo Agenda Records',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.campo).toBe('Novo Agenda Records');
  });

   test('deve carregar Agenda Records por ID', async () => {
    const mockAgendaRecords = { ...initialAgendaRecords, id: 1, campo: 'Agenda Records Teste' };
    mockAgendaRecordsService.fetchAgendaRecordsById.mockResolvedValue(mockAgendaRecords);

    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.loadAgendaRecords(1);
    });

    expect(mockAgendaRecordsService.fetchAgendaRecordsById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockAgendaRecords);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Agenda Records', async () => {
    const errorMessage = 'Erro ao carregar Agenda Records';
    mockAgendaRecordsService.fetchAgendaRecordsById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.loadAgendaRecords(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialAgendaRecords, campo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialAgendaRecords);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.loadAgendaRecords(0);
    });

    expect(mockAgendaRecordsService.fetchAgendaRecordsById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialAgendaRecords);
  });
});

describe('useAgendaRecordsList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAgendaRecordsList(mockAgendaRecordsService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialAgendaRecords, id: 1, campo: 'Agenda Records 1' },
      { ...initialAgendaRecords, id: 2, campo: 'Agenda Records 2' }
    ];
    mockAgendaRecordsService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAgendaRecordsList(mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockAgendaRecordsService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockAgendaRecordsService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAgendaRecordsList(mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialAgendaRecords, id: 1, campo: 'Agenda Records Filtrado' }];
    const filtro = { campo: 'Agenda Records' };
    mockAgendaRecordsService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAgendaRecordsList(mockAgendaRecordsService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockAgendaRecordsService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsAgendaRecords', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsAgendaRecords());

    const validData = { ...initialAgendaRecords, campo: 'Agenda Records Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsAgendaRecords());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialAgendaRecords, id: 1, campo: 'Agenda Records Teste' }];
    mockAgendaRecordsService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useAgendaRecordsList(mockAgendaRecordsService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsAgendaRecords()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useAgendaRecordsForm(initialAgendaRecords, mockAgendaRecordsService)
    );

    const mockEvent = {
      target: {
        name: 'aviso1',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.aviso1).toBe(true);
  });


