// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IOperadorGruposAgendaOperadoresService } from '../Services/OperadorGruposAgendaOperadores.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IOperadorGruposAgendaOperadores } from '../Interfaces/interface.OperadorGruposAgendaOperadores';
import { FilterOperadorGruposAgendaOperadores } from '../Filters/OperadorGruposAgendaOperadores';
import { isValidDate } from '@/app/tools/datetime';
import { OperadorGruposAgendaOperadoresApi } from '../Apis/ApiOperadorGruposAgendaOperadores';

export const useOperadorGruposAgendaOperadoresForm = (
  initialOperadorGruposAgendaOperadores: IOperadorGruposAgendaOperadores,
  dataService: IOperadorGruposAgendaOperadoresService
) => {
  const [data, setData] = useState<IOperadorGruposAgendaOperadores>(initialOperadorGruposAgendaOperadores);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadOperadorGruposAgendaOperadores = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialOperadorGruposAgendaOperadores);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchOperadorGruposAgendaOperadoresById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Operador Grupos Agenda Operadores';
      setError(errorMessage);
      //console.log('Erro ao carregar Operador Grupos Agenda Operadores');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialOperadorGruposAgendaOperadores]);

  const resetForm = useCallback(() => {
    setData(initialOperadorGruposAgendaOperadores);
    setError(null);
  }, [initialOperadorGruposAgendaOperadores]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadOperadorGruposAgendaOperadores,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadOperadorGruposAgendaOperadores, resetForm]);
  return returnValue;
};


export const useOperadorGruposAgendaOperadoresNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('OperadorGruposAgendaOperadores', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useOperadorGruposAgendaOperadoresList = (dataService: IOperadorGruposAgendaOperadoresService, currentFilter?: FilterOperadorGruposAgendaOperadores | undefined | null) => {
  const [data, setData] = useState<IOperadorGruposAgendaOperadores[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterOperadorGruposAgendaOperadores | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterOperadorGruposAgendaOperadores | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterOperadorGruposAgendaOperadores);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar operadorgruposagendaoperadores';
      setError(errorMessage);
      //console.log('Erro ao carregar operadorgruposagendaoperadores');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useOperadorGruposAgendaOperadoresNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsOperadorGruposAgendaOperadores() {

  async function runValidation(data: IOperadorGruposAgendaOperadores, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const operadorgruposagendaoperadoresApi = new OperadorGruposAgendaOperadoresApi(uri ?? '', token ?? '');

    const result = await operadorgruposagendaoperadoresApi.validation(data);

    return result;
  }

  function validate(data: IOperadorGruposAgendaOperadores): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}