// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { StatusAndamentoService, StatusAndamentoValidator } from '@/app/GerAdv_TS/StatusAndamento/Services/StatusAndamento.service';
import { StatusAndamentoApi, StatusAndamentoApiError } from '@/app/GerAdv_TS/StatusAndamento/Apis/ApiStatusAndamento';
import { IStatusAndamento } from '@/app/GerAdv_TS/StatusAndamento/Interfaces/interface.StatusAndamento';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as StatusAndamentoApi;

const sample: IStatusAndamento = { id: 1, nome: 'Teste' };

describe('StatusAndamentoService', () => {
  let service: StatusAndamentoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new StatusAndamentoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(StatusAndamentoValidator, 'validateStatusAndamento').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchStatusAndamentoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchStatusAndamentoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchStatusAndamentoById lança StatusAndamentoApiError para id inválido', async () => {
    await expect(service.fetchStatusAndamentoById(0)).rejects.toBeInstanceOf(StatusAndamentoApiError);
  });

  it('saveStatusAndamento chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveStatusAndamento(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveStatusAndamento lança StatusAndamentoApiError quando validação falha', async () => {
    (StatusAndamentoValidator.validateStatusAndamento as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveStatusAndamento(sample)).rejects.toBeInstanceOf(StatusAndamentoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteStatusAndamento chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteStatusAndamento(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteStatusAndamento lança StatusAndamentoApiError para id inválido', async () => {
    await expect(service.deleteStatusAndamento(0)).rejects.toBeInstanceOf(StatusAndamentoApiError);
  });

  it('validateStatusAndamento delega para StatusAndamentoValidator', () => {
    (StatusAndamentoValidator.validateStatusAndamento as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateStatusAndamento(sample);
    expect(r.isValid).toBe(true);
  });
});
