// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for ContaCorrente page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock ContaCorrenteGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/ContaCorrente/Adapter/ContaCorrenteGridAdapter', () => ({
  ContaCorrenteGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock ContaCorrenteGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/ContaCorrente/Components/ContaCorrenteGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='contacorrente-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import ContaCorrentePage from '@/app/pages/contacorrente/page';

describe('ContaCorrente page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and ContaCorrenteGridContainer', () => {
    render(<ContaCorrentePage />);

    expect(screen.getByRole('heading', { name: /Conta Corrente/i })).toBeInTheDocument();
    expect(screen.getByTestId('contacorrente-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

