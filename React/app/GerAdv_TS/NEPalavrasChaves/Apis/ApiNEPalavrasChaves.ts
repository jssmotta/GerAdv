// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import axios, { AxiosError, AxiosResponse } from 'axios';
import useSWR from 'swr';
import { FilterNEPalavrasChaves } from '../Filters/NEPalavrasChaves'
import { NEPalavrasChaves } from '../../Models/NEPalavrasChaves';
import { INEPalavrasChaves } from '../Interfaces/interface.NEPalavrasChaves';
import { decodeBase64Token, fetcher } from '@/app/tools/Fetcher';
import { INotificationService, INotifySystemEntity, NotificationService, NotifySystemActions } from '@/app/tools/NotifySystem';
import { CRUD_CONSTANTS, decodeDataFromStorage, encodeDataForStorage } from '@/app/tools/crud';


export class NEPalavrasChavesApiError extends Error {
  status: number;
  code?: string;
  originalError?: any;

  constructor(message: string, status: number, code?: string, originalError?: any) {
    super(message);
    this.name = 'NEPalavrasChavesApiError';
    this.status = status;
    this.code = code;
    this.originalError = originalError;
  }

  toString() {
    return `${this.name}: ${this.message} (status: ${this.status})`;
  }
}

export class NEPalavrasChavesApi {
    private authorization: string;
    private baseUrl: string;
    private notificationService: INotificationService;
    private uri: string;

    constructor(uri: string, authorization: string, version: number = parseInt(process.env.NEXT_PUBLIC_URL_VERSION_API ?? '1')) {
        this.authorization = authorization;
        this.baseUrl = `${process.env.NEXT_PUBLIC_URL_API_BASE}${version}/${uri}/NEPalavrasChaves`;
        this.notificationService = new NotificationService();
        this.uri = uri;
    }

    private getHeaders() {
        return {
            headers: {
                Authorization: `Bearer ${decodeBase64Token(this.authorization)}`,
                'Content-Type': 'application/json',
                'Cache-Control': 'no-cache',     
            },
        };
    }

    private createNotificationEntity(
        id: number, 
        action: NotifySystemActions = NotifySystemActions.DELETE): INotifySystemEntity {
        return {
            entity: 'NEPalavrasChaves',
            id: id,
            action: action
        };
    }

    private createNotificationOffLiveEntity(
        id: number,
        action: NotifySystemActions = NotifySystemActions.INFO,
        message?: string): INotifySystemEntity {
        return {
            entity: 'NEPalavrasChaves',
            id: id,
            action: action,
            message: message ?? `Trabalhando off-line.`
        };
    }


    private handleApiError(error: any, context: string): never {
        if (axios.isAxiosError(error)) {
            const axiosError = error as AxiosError;
            const status = axiosError.response?.status || 500;
            const message = (axiosError.response?.data as { message?: string })?.message || axiosError.message;

            this.notificationService.notify(
                this.createNotificationOffLiveEntity(0, NotifySystemActions.ERROR, message ?? 'Erro desconhecido')
            );

            throw new NEPalavrasChavesApiError(
                `${context}: ${message}`,
                status,
                axiosError.code,
                error
            );
        }
        else {
            this.notificationService.notify(
                this.createNotificationOffLiveEntity(0, NotifySystemActions.ERROR, 'Erro desconhecido')
            );
        }

        throw new NEPalavrasChavesApiError(
            `${context}: Erro desconhecido`,
            500,
            'UNKNOWN_ERROR',
            error
        );
    }


   
        public async getAll(max: number = CRUD_CONSTANTS.DEFAULT_MAX_RECORDS): Promise<AxiosResponse> {
        const storageKey = btoa(`${process.env.NEXT_PUBLIC_APP_ID}-${this.uri}-NEPalavrasChaves_last_getAll_${max}`);
        try {
            const response = await axios.get(`${this.baseUrl}/GetAll?max=${max}`, this.getHeaders());
            
                Promise.resolve().then(() => {
                    try {
                        const encoded = encodeDataForStorage(response.data);
                        localStorage.setItem(storageKey, encoded);
                    } catch (error) {   
                        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                            console.log('Erro ao salvar dados filtrados no localStorage');
                    }
                });
        
            return response;
        } catch (error: any) {
            const offlineData = localStorage.getItem(storageKey);
            if (offlineData) {
                const decoded = decodeDataFromStorage(offlineData);
                this.notificationService.notify(
                    this.createNotificationOffLiveEntity(0, NotifySystemActions.INFO)
                );
                return {
                    data: decoded,
                    status: 200,
                    statusText: 'OK (offline)',
                    headers: {},
                    config: {},
                } as AxiosResponse;
            }
            this.handleApiError(error, 'Erro ao buscar todos os NEPalavrasChaves');
        }
    }
    
        public async getById(id: number): Promise<AxiosResponse> {
        const storageKey = btoa(`${process.env.NEXT_PUBLIC_APP_ID}-${this.uri}-NEPalavrasChaves_last_getById_${id}`);
        try {
            const response = await axios.get(`${this.baseUrl}/GetById/${id}`, this.getHeaders());
            
                Promise.resolve().then(() => {
                    try {
                        const encoded = encodeDataForStorage(response.data);
                        localStorage.setItem(storageKey, encoded);
                    } catch (error) {   
                        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                            console.log('Erro ao salvar dados filtrados no localStorage');
                    }
                });
        
            return response;
        } catch (error: any) {
            const offlineData = localStorage.getItem(storageKey);
            if (offlineData) {
                const decoded = decodeDataFromStorage(offlineData);
                this.notificationService.notify(
                    this.createNotificationOffLiveEntity(id, NotifySystemActions.INFO)
                );
                return {
                    data: decoded,
                    status: 200,
                    statusText: 'OK (offline)',
                    headers: {},
                    config: {},
                } as AxiosResponse;
            }
            this.handleApiError(error, `Erro ao buscar N E Palavras Chaves com ID ${id}`);
        }
    }
    
        public async getListN(max?: number, filtro?: FilterNEPalavrasChaves): Promise<AxiosResponse> {
        if (max === undefined) max = CRUD_CONSTANTS.DEFAULT_MAX_RECORDS;
        const storageKey = btoa(`${process.env.NEXT_PUBLIC_APP_ID}-${this.uri}-NEPalavrasChaves_last_listN_data`);

        try {
            const response = await axios.post(`${this.baseUrl}/GetListN/?max=${max}`, filtro, this.getHeaders());
            
                Promise.resolve().then(() => {
                    try {
                        const encoded = encodeDataForStorage(response.data);
                        localStorage.setItem(storageKey, encoded);
                    } catch (error) {   
                        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                            console.log('Erro ao salvar dados filtrados no localStorage');
                    }
                });
        
            return response;
        } catch (error: any) {
            const offlineData = localStorage.getItem(storageKey);
            if (offlineData) {
                const decoded = decodeDataFromStorage(offlineData);
                this.notificationService.notify(
                    this.createNotificationOffLiveEntity(0, NotifySystemActions.INFO)
                );
                return {
                    data: decoded,
                    status: 200,
                    statusText: 'OK (offline)',
                    headers: {},
                    config: {},
                } as AxiosResponse;
            }
            this.handleApiError(error, 'Erro ao buscar lista de NEPalavrasChaves');
        }
    }
 public async filterPreload(max: number, filtro: FilterNEPalavrasChaves): Promise<AxiosResponse> {
                        const storageKey = btoa(`${process.env.NEXT_PUBLIC_APP_ID}-${this.uri}-NEPalavrasChaves-${max}_last_filter_data_${JSON.stringify(filtro)}`);
                        const offlineData = localStorage.getItem(storageKey);        
                        const decoded = offlineData ? decodeDataFromStorage(offlineData) : [];

                        return {
                            data: decoded,
                            status: 200,
                            statusText: 'OK (offline)',
                            headers: {},
                            config: {},
                        } as AxiosResponse;        
                    }


            public async filter(max: number, filtro: FilterNEPalavrasChaves): Promise<AxiosResponse> {
                const storageKey = btoa(`${process.env.NEXT_PUBLIC_APP_ID}-${this.uri}-NEPalavrasChaves-${max}_last_filter_data_${JSON.stringify(filtro)}`);
                try {
                    const response = await axios.post(`${this.baseUrl}/Filter?max=${max}`, filtro, this.getHeaders());
                    
                Promise.resolve().then(() => {
                    try {
                        const encoded = encodeDataForStorage(response.data);
                        localStorage.setItem(storageKey, encoded);
                    } catch (error) {   
                        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                            console.log('Erro ao salvar dados filtrados no localStorage');
                    }
                });
        
                    return response;
                } catch (error: any) {
                    const offlineData = localStorage.getItem(storageKey);
                    if (offlineData) {
                        const decoded = decodeDataFromStorage(offlineData);
                        this.notificationService.notify(
                            this.createNotificationOffLiveEntity(0, NotifySystemActions.INFO)
                        );
                        return {
                            data: decoded,
                            status: 200,
                            statusText: 'OK (offline)',
                            headers: {},
                            config: {},
                        } as AxiosResponse;
                    }

                    this.handleApiError(error, 'Erro ao filtrar NEPalavrasChaves');
                }
            }


    public async validation(regNEPalavrasChaves: INEPalavrasChaves): Promise<{ isValid: boolean; message: string } | null> {
        try {
            const result = await axios.post(`${this.baseUrl}/validation`, regNEPalavrasChaves as NEPalavrasChaves, this.getHeaders());
            // Se a resposta for bem-sucedida, retorna true
            return { isValid: true, message: 'Validação bem-sucedida' };
        } catch (error: any) {
            if (error.response && (error.response.status === 409 || error.response.status === 500)) {
                if (error.response && error.response.data) {
                    const { message } = error.response.data;
                    // Erro de validação, o registro já existe
                    const errorMessage = message || 'Verifique se o registro já existe!';
                    return { isValid: false, message: errorMessage };                    
                }
            }
            return { isValid: false, message: 'Erro desconhecido ao validar na base.' };;
        }
    }


    public async addAndUpdate(regNEPalavrasChaves: INEPalavrasChaves): Promise<AxiosResponse> {
        try {
            var result = await axios.post(`${this.baseUrl}/AddAndUpdate`, regNEPalavrasChaves as NEPalavrasChaves, this.getHeaders());
            var register = result.data as INEPalavrasChaves;        
            const action = regNEPalavrasChaves.id == 0 ? NotifySystemActions.ADD : NotifySystemActions.UPDATE;
            const notificationEntity = this.createNotificationEntity(register.id, action);        
            this.notificationService.notify(notificationEntity);
            return result; } 
        catch (error: any) {
            if (error.response && error.response.status === 409) {
                if (error.response && error.response.data) {
                    const { message } = error.response.data;
                    // Erro de validação, o registro já existe
                    const errorMessage = message || 'Verifique se o registro já existe!';
                    this.notificationService.notify({
                        entity: 'NEPalavrasChaves',
                        id: regNEPalavrasChaves.id,
                        action: NotifySystemActions.ERROR,
                        message: errorMessage
                    });

                }
            }

            this.handleApiError(error, 'Erro ao salvar N E Palavras Chaves');
        }
    }

    public async delete(id: number): Promise<AxiosResponse | void> {
        try {
            const result = await axios.delete(`${this.baseUrl}/Delete?id=${id}`, this.getHeaders());
             if (result.data) {
                /* if (result.data.success === false) {
                    throw new Error(result.data.message || 'Erro ao excluir.');
                } */
                const notificationEntity = this.createNotificationEntity(id, NotifySystemActions.DELETE);
                this.notificationService.notify(notificationEntity);
            }
            return result;
        } catch (error: any) {
            if (error.response && error.response.status === 409) {
              if (error.response && error.response.data) {
                    const { message } = error.response.data;
                    // Conflito, o registro está vinculado a outros registros
                    const errorMessage = message|| 'Erro ao excluir o NEPalavrasChaves. Verifique se ele não está vinculado a outros registros.';
                    this.notificationService.notify({
                        entity: 'NEPalavrasChaves',
                        id: id,
                        action: NotifySystemActions.ERROR,
                        message: errorMessage
                    });
                    throw new Error(error.data.message || 'Erro ao excluir.');
                }
            }
             this.handleApiError(error, `Erro ao excluir N E Palavras Chaves com ID ${id}`);                
        }
    }




    public useFilter(filtro: FilterNEPalavrasChaves) {
       const url = `${this.baseUrl}/Filter`;
       const key = `${url}::${this.authorization}::${JSON.stringify(filtro)}`;
    
        return useSWR<NEPalavrasChaves[]>(key, fetcher, {
          revalidateOnFocus: false,
          revalidateOnReconnect: false,
          onError: (error) => {            
            if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                console.log('Erro no SWR para filtro de NEPalavrasChaves');
            this.notificationService.notify({
              entity: 'NEPalavrasChaves',
              id: 0,
              action: NotifySystemActions.ERROR,
              message: 'Erro ao carregar dados dos nepalavraschaves'
            });
          }
        });
    }

}
