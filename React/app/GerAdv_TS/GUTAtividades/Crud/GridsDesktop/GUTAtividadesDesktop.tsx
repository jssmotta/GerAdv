// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// GridsDesktopHoook.tsx - Versão Refatorada
'use client';
import React, { useMemo, useCallback, useState, useEffect } from 'react';
import {
  Grid, 
  GridColumn, 
  GridSortChangeEvent, 
  GridColumnReorderEvent, 
} from '@progress/kendo-react-grid';
import { useRouter } from 'next/navigation';
import { useSystemContext } from '@/app/context/SystemContext';
import { IGUTAtividades } from '../../Interfaces/interface.GUTAtividades';
import { CRUD_CONSTANTS, formatCurrency, formatDateBrowser } from '@/app/tools/crud';
import { SvgIcon } from '@progress/kendo-react-common';
import { pencilIcon, searchIcon } from '@progress/kendo-svg-icons';
import { useHiddenColumns } from '@/app/hooks/useHiddenColumns';
import { GridColumnMenu } from '@/app/components/Cruds/GridColumnMenu';
import { getExportColumns, useExportToExcel } from '@/app/hooks/useExportToExcel';
import { getExportColumnsPdf, useExportToPdf } from '@/app/hooks/useExportToPdf';
import { orderBy } from '@progress/kendo-data-query';
import { ExportButtons } from '@/app/components/Cruds/ExportButtons';
import { useGUTAtividadesGrid } from '../../Hooks/hookGUTAtividadesGrid';
interface GUTAtividadesGridProps {
  data: IGUTAtividades[];
  onRowClick: (gutatividades: IGUTAtividades) => void;
  onDeleteClick: (e: any) => void;
  setSelectedId: (id: number | null) => void;
}
export const GUTAtividadesGridDesktopComponent = React.memo(({
  data, 
  onRowClick, 
  onDeleteClick, 
  setSelectedId, 

}: GUTAtividadesGridProps) => {

const router = useRouter();
const { systemContext } = useSystemContext();
const defaultHiddenColumns = [''];
// ===== ESTADO LOCAL PARA REORDENAÇÃO =====
const [columnsOrder, setColumnsOrder] = useState<string[]>(['nome',
'nomegutperiodicidade',
'rnomeoperador']);
// Carregar ordem salva do localStorage na inicialização
useEffect(() => {
  try {
    const savedOrder = localStorage.getItem(btoa('gutatividades-columns-order'));
    if (savedOrder) {
      const parsedOrder = JSON.parse(savedOrder);
      if (Array.isArray(parsedOrder) && parsedOrder.length > 0) {
        setColumnsOrder(parsedOrder);
      }
    }
  } catch (error) {
  console.warn('Não foi possível carregar a ordem das colunas:', error);
}
}, []);

const openSearchCellGUTAtividadesMatriz = (id: number) => {
  router.push(`/pages/gutatividadesmatriz/?gutatividades=${id}`);
};
const SearchFromCellGUTAtividadesMatriz = (props: any) => {
  return (
  <>
  <td>
    <div onClick={() => openSearchCellGUTAtividadesMatriz(props.dataItem.id)}><span title='Pesquisar relacionados em G U T Atividades Matriz'><SvgIcon icon={searchIcon} /></span></div>
  </td>
</>
);
};
const openSearchCellGUTPeriodicidadeStatus = (id: number) => {
  router.push(`/pages/gutperiodicidadestatus/?gutatividades=${id}`);
};
const SearchFromCellGUTPeriodicidadeStatus = (props: any) => {
  return (
  <>
  <td>
    <div onClick={() => openSearchCellGUTPeriodicidadeStatus(props.dataItem.id)}><span title='Pesquisar relacionados em G U T Periodicidade Status'><SvgIcon icon={searchIcon} /></span></div>
  </td>
</>
);
};
const MaskagtMinutosParaRealizarCell = (props: any) => {
  const valor = props.dataItem[props.field];
  const formattedValue = valor ? formatCurrency(valor, 'BRL') : 'R$ 0,00';
  return <td>{formattedValue}</td>;
};
// ===== USO DO HOOK CENTRALIZADO =====
const {
  filteredData, 
  sort, 
  handleSortChange, 
  page, 
  handlePageChange, 
  handleFilterChange, 
  handleRowClick: hookHandleRowClick, 
  RowNumberCell, 
  DeleteRow, 
} = useGUTAtividadesGrid({
data, 
onRowClick, 
onDeleteClick, 
setSelectedId, 
initialTake: 10, 
useCustomSort: true, 
});
// ===== ORDENAÇÃO CUSTOMIZADA PARA DESKTOP =====
const sortedData = useMemo(() => orderBy(filteredData, sort), [filteredData, sort]);
const handleSortChangeCustom = useCallback((event: GridSortChangeEvent) => {
  handleSortChange(event);
}, [handleSortChange]);
// ===== HANDLER PARA REORDENAÇÃO DE COLUNAS =====
const handleColumnReorder = useCallback((event: GridColumnReorderEvent) => {
  if (event.columns) {
    const newOrder = event.columns
    .filter((col: any) => col.field && columnsOrder.includes(col.field))
    .map((col: any) => col.field);
    setColumnsOrder(prevOrder => {
      if (newOrder.length > 0 && JSON.stringify(newOrder) !== JSON.stringify(prevOrder)) {
        // Salvar no localStorage
        try {
          localStorage.setItem(btoa('gutatividades-columns-order'), JSON.stringify(newOrder));
        } catch (error) {
        console.warn('Não foi possível salvar a ordem das colunas:', error);
      }
      return newOrder;
    }
    return prevOrder;
  });
}
}, []);
// ===== COMPONENTES ESPECÍFICOS DO DESKTOP =====
const EditRow = useCallback((e: any) => {
  return (
  <td>
    <span onClick={() => hookHandleRowClick(e)} title='Editar item'>
      <SvgIcon icon={pencilIcon} />
    </span>
  </td>
);
}, [hookHandleRowClick]);
// ===== DEFINIR COLUMN MAP BÁSICO (SEM DEPENDÊNCIA DO HOOK) =====
const basicColumnMap: Record<string, React.ReactElement> = useMemo(() => ({

  'nome': (
  <GridColumn
  key='nome'
  field='nome'
  title='Nome'
  sortable={true}
  filterable={true}
  />
  ), /* Track G.02 */
  'id_edit_GUTAtividadesMatriz': (
  <GridColumn
  key='GUTAtividadesMatriz'
  field='GUTAtividadesMatriz'
  title='G U T Atividades Matriz'
  width={'65px'}
  sortable={false}
  filterable={false}
  cells={{ data: SearchFromCellGUTAtividadesMatriz }}
  />
  ), /* Track G.03 */
  'id_edit_GUTPeriodicidadeStatus': (
  <GridColumn
  key='GUTPeriodicidadeStatus'
  field='GUTPeriodicidadeStatus'
  title='G U T Periodicidade Status'
  width={'65px'}
  sortable={false}
  filterable={false}
  cells={{ data: SearchFromCellGUTPeriodicidadeStatus }}
  />
  ), /* Track G.03 */
  'nomegutperiodicidade': (
  <GridColumn
  key='nomegutperiodicidade'

  field='nomegutperiodicidade'
  title='G U T Periodicidade'
  sortable={false} filterable={false}

  />
  ), /* Track G.04 */
  'rnomeoperador': (
  <GridColumn
  key='rnomeoperador'

  field='rnomeoperador'
  title='Operador'
  sortable={false} filterable={false}

  />
  ), /* Track G.04 */
  // ← Colunas aqui
}), []);
// ===== CONFIGURAÇÃO DE COLUNAS BASE (PARA HIDDEN COLUMNS) =====
const baseGridColumns = useMemo(() => [
  <GridColumn format='{0:n0}' field='index' title='#' sortable={false} filterable={false} width='55px' cells={{ data: RowNumberCell }} />,
  <GridColumn format='{0:n0}' hidden={true}  field='id' title='Código' sortable={true} filterable={true} width='55px' />,
  <GridColumn
  field='id_edit_GUTAtividadesMatriz'
  filterable={false}
  sortable={false}
  width={'65px'}
  title='G U T Atividades Matriz'
  cells={{ data: SearchFromCellGUTAtividadesMatriz }}
  />, 
  <GridColumn
  field='id_edit_GUTPeriodicidadeStatus'
  filterable={false}
  sortable={false}
  width={'65px'}
  title='G U T Periodicidade Status'
  cells={{ data: SearchFromCellGUTPeriodicidadeStatus }}
  />, 
  ], [RowNumberCell, EditRow, DeleteRow]);
  // ===== GERENCIAMENTO DE COLUNAS OCULTAS (SEM INTERFERIR NA REORDENAÇÃO) =====
  const {
    columnsState, 
    initialized, 
    handleColumnsStateChange
  } = useHiddenColumns({
  gridColumns: baseGridColumns, 
  columnMap: basicColumnMap, // ← Usar basicColumnMap ao invés de columnMap
  systemContextId: systemContext?.Id, 
  tableName: 'gutatividades',
  defaultHiddenColumns
});
// ===== Helper para verificar se coluna está visível (APÓS INICIALIZAÇÃO DO HOOK) =====
const isColumnVisible = useCallback((field: string) => {
  if (!columnsState) {
    // ✅ CAMPOS QUE INICIAM HIDDEN POR PADRÃO
    return !defaultHiddenColumns.includes(field);
  }
  const columnState = columnsState.find(state => state.field === field);
  return columnState ? !columnState.hidden: true;
}, [columnsState, defaultHiddenColumns]);
// ===== FUNÇÃO PARA RESETAR ORDEM =====
const resetColumnsOrder = useCallback(() => {
  const defaultOrder = ['nome', 'precomeia', 'precointeira'];
  setColumnsOrder(defaultOrder);
  localStorage.removeItem(btoa('gutatividades-columns-order'));
}, []);
// ===== CONFIGURAÇÃO DE COLUNAS FINAIS (REORDENADAS + OCULTAS) =====
const finalGridColumns = useMemo(() => {
  // Montar array de colunas na ordem especificada
  const finalColumns = [
  <GridColumn format='{0:n0}' field='index' title='#' sortable={false} filterable={false} width='55px' cells={{ data: RowNumberCell }} />,
  <GridColumn format='{0:n0}' hidden={true}  field='id' title='Código' sortable={true} filterable={true} width='55px' />,
  <GridColumn
  field='id_edit_GUTAtividadesMatriz'
  filterable={false}
  sortable={false}
  width={'65px'}
  title='G U T Atividades Matriz'
  cells={{ data: SearchFromCellGUTAtividadesMatriz }}
  />, 
  <GridColumn
  field='id_edit_GUTPeriodicidadeStatus'
  filterable={false}
  sortable={false}
  width={'65px'}
  title='G U T Periodicidade Status'
  cells={{ data: SearchFromCellGUTPeriodicidadeStatus }}
  />, 

  // Colunas reordenáveis na ordem especificada
  ...columnsOrder.map(field => basicColumnMap[field]).filter(Boolean).map(column => {
    const props = (column as React.ReactElement<any>).props;
    return React.cloneElement(column, { ...props, hidden: !isColumnVisible(props.field) });
  }), 
  // Colunas fixas do final (SEM hidden: !isColumnVisible)
  <GridColumn
  field='id_edit_row'
  width={'55px'}
  title='Editar registro'
  sortable={false}
  filterable={false}
  cells={{ data: EditRow }}
  reorderable={false}
  />, 
  <GridColumn
  key='delete'
  field='id_delete_row'
  width={'55px'}
  title='Excluir registro'
  sortable={false}
  filterable={false}
  cells={{ data: DeleteRow }}
  reorderable={false}
  />
];

return finalColumns;
}, [columnsOrder, isColumnVisible, RowNumberCell, EditRow, DeleteRow, basicColumnMap]);
// ===== MENU DE COLUNAS SEPARADO (NÃO PASSADO PARA O GRID) =====
const columnMenuComponent = GridColumnMenu({
  columnsState, 
  onColumnsStateChange: handleColumnsStateChange
});
// ===== CONFIGURAÇÕES DE EXPORTAÇÃO =====
const exportColumns = getExportColumns(finalGridColumns, columnsState, ['id', 'index']);
const { exportToExcel } = useExportToExcel({
  filename: 'planilha-gutatividades',
  columns: exportColumns, 
  sheetName: 'GUTAtividades'
});
const handleExportFiltered = useCallback(() => {
  exportToExcel(filteredData, {
    customFilename: `excel-GUTAtividades-${new Date().toISOString().replace('T', '_').substring(0, 16).replace(':', '-')}`,
  });
}, [exportToExcel, filteredData]);
const exportColumnsPdf = getExportColumnsPdf(finalGridColumns, columnsState, ['id', 'index']);
const { exportToPdf } = useExportToPdf({
  filename: 'pdf-gutatividades',
  columns: exportColumnsPdf, 
  title: 'GUTAtividades'
});
const handleExportFilteredPdf = useCallback(() => {
  exportToPdf(filteredData, {
    customFilename: `pdf-GUTAtividades-${new Date().toISOString().replace('T', '_').substring(0, 16).replace(':', '-')}`,
  });
}, [exportToPdf, filteredData]);
// ===== DADOS PAGINADOS =====
const paginatedSortedData = useMemo(() => {
  if (!sortedData || sortedData.length === 0) {
    return [];
  }
  return sortedData.slice(page.skip, page.skip + page.take);
}, [sortedData, page.skip, page.take]);
// ===== RENDER =====
return (
<>
{initialized && (
  <Grid
  key={`gutatividades-grid-${JSON.stringify(columnsOrder)}-${JSON.stringify(columnsState)}`}
  columnMenu={columnMenuComponent}
  className='grid-desktop-crud grid-desktop-gutatividades'
  data={paginatedSortedData}
  skip={page.skip}
  take={page.take}
  total={sortedData.length}
  pageable={{
    pageSizes: Array.from(CRUD_CONSTANTS.PAGINATION.PAGE_SIZES), 
    buttonCount: CRUD_CONSTANTS.PAGINATION.BUTTON_COUNT, 
  }}
  onPageChange={handlePageChange}
  rowReorderable={true}
  sortable={true}
  sort={sort}
  onSortChange={handleSortChangeCustom}
  resizable={true}
  reorderable={true}
  onColumnReorder={handleColumnReorder}
  filterable={true}
  onFilterChange={handleFilterChange}
  onRowDoubleClick={(e) => hookHandleRowClick(e)}
>
{finalGridColumns}
</Grid>
)}
<div style={{ marginTop: '10px' }}>

</div>
</>
);
});