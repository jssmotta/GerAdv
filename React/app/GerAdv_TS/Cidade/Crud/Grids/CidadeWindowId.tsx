// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { ICidade } from '../../Interfaces/interface.Cidade';
import { CidadeService } from '../../Services/Cidade.service';
import { CidadeApi } from '../../Apis/ApiCidade';
import CidadeWindow from './CidadeWindow';
import {CidadeEmpty } from '@/app/GerAdv_TS/Models/Cidade';
interface CidadeWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const CidadeWindowId: React.FC<CidadeWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const cidadeService = useMemo(() => {
  return new CidadeService(
  new CidadeApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<ICidade | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(CidadeEmpty() as ICidade);
      return;
    }
    if (id) {
      const response = await cidadeService.fetchCidadeById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <CidadeWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedCidade={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default CidadeWindowId;