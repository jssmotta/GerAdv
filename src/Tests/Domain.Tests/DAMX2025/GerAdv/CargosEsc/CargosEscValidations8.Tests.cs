// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class CargosEscValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFCargosEscFactory> _mockCargosEscFactory;
    private readonly Mock<ICargosEscReader> _mockReader;
    private readonly CargosEscValidation _validation;
    private readonly Mock<ICargosEscService> _mockCargosEscService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly CargosEscService _service;
    private readonly string _validUri = "test-uri";
    public CargosEscValidationTests()
    {
        _mockCargosEscFactory = new Mock<IFCargosEscFactory>();
        _mockReader = new Mock<ICargosEscReader>();
        _validation = new CargosEscValidation();
        _mockCargosEscService = new Mock<ICargosEscService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void CargosEscValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ICargosEscValidation>();
    }

    [Fact]
    public void CargosEscValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(CargosEscValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void CargosEscValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(CargosEscValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.CargosEsc CreateValidCargosEsc()
    {
        return new Models.CargosEsc
        {
            Id = 1,
            Percentual = 0m,
            Nome = "João",
            Classificacao = 0
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockCargosEscService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargosEsc>(), It.IsAny<string>())).ReturnsAsync(new List<CargosEscResponseAll>());
    // Setup other mocks but don't override the CargosEscs service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockCargosEscService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargosEsc>(), It.IsAny<string>())).ReturnsAsync(new List<CargosEscResponseAll>());
    // Setup other mocks but don't override the CargosEscs service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var cargosesc = new Models.CargosEsc
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(cargosesc, _mockCargosEscService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockCargosEscService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}