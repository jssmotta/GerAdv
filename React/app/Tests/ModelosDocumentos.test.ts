// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useModelosDocumentosForm, useModelosDocumentosList, useValidationsModelosDocumentos } from '../GerAdv_TS/ModelosDocumentos/Hooks/hookModelosDocumentos';
import { IModelosDocumentos } from '../GerAdv_TS/ModelosDocumentos/Interfaces/interface.ModelosDocumentos';
import { IModelosDocumentosService } from '../GerAdv_TS/ModelosDocumentos/Services/ModelosDocumentos.service';
import { ModelosDocumentosTestEmpty } from '../GerAdv_TS/Models/ModelosDocumentos';
import { useModelosDocumentosComboBox } from '../GerAdv_TS/ModelosDocumentos/Hooks/hookModelosDocumentos';

// Mock do serviço
const mockModelosDocumentosService: jest.Mocked<IModelosDocumentosService> = {
  fetchModelosDocumentosById: jest.fn(),
  saveModelosDocumentos: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteModelosDocumentos: jest.fn(),
  validateModelosDocumentos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialModelosDocumentos: IModelosDocumentos = { ...ModelosDocumentosTestEmpty() };

describe('useModelosDocumentosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    expect(result.current.data).toEqual(initialModelosDocumentos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Modelos Documentos',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Modelos Documentos');
  });

   test('deve carregar Modelos Documentos por ID', async () => {
    const mockModelosDocumentos = { ...initialModelosDocumentos, id: 1, nome: 'Modelos Documentos Teste' };
    mockModelosDocumentosService.fetchModelosDocumentosById.mockResolvedValue(mockModelosDocumentos);

    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.loadModelosDocumentos(1);
    });

    expect(mockModelosDocumentosService.fetchModelosDocumentosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockModelosDocumentos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Modelos Documentos', async () => {
    const errorMessage = 'Erro ao carregar Modelos Documentos';
    mockModelosDocumentosService.fetchModelosDocumentosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.loadModelosDocumentos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialModelosDocumentos, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialModelosDocumentos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useModelosDocumentosForm(initialModelosDocumentos, mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.loadModelosDocumentos(0);
    });

    expect(mockModelosDocumentosService.fetchModelosDocumentosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialModelosDocumentos);
  });
});

describe('useModelosDocumentosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosList(mockModelosDocumentosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialModelosDocumentos, id: 1, nome: 'Modelos Documentos 1' },
      { ...initialModelosDocumentos, id: 2, nome: 'Modelos Documentos 2' }
    ];
    mockModelosDocumentosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useModelosDocumentosList(mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockModelosDocumentosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockModelosDocumentosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useModelosDocumentosList(mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialModelosDocumentos, id: 1, nome: 'Modelos Documentos Filtrado' }];
    const filtro = { nome: 'Modelos Documentos' };
    mockModelosDocumentosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useModelosDocumentosList(mockModelosDocumentosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockModelosDocumentosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsModelosDocumentos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsModelosDocumentos());

    const validData = { ...initialModelosDocumentos, nome: 'Modelos Documentos Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsModelosDocumentos());

    const invalidData = { ...initialModelosDocumentos, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsModelosDocumentos());

    const invalidData = { 
      ...initialModelosDocumentos, 
      nome: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsModelosDocumentos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialModelosDocumentos, id: 1, nome: 'Modelos Documentos Teste' }];
    mockModelosDocumentosService.getAll.mockResolvedValue(mockData);
    mockModelosDocumentosService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useModelosDocumentosList(mockModelosDocumentosService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsModelosDocumentos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Modelos Documentos Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Modelos Documentos Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Modelos Documentos 1' },
      { id: 2, nome: 'Modelos Documentos 2' }
    ];
    mockModelosDocumentosService.getList.mockResolvedValue(mockOptions as IModelosDocumentos[]);


    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Modelos Documentos 1' },
        { id: 2, nome: 'Modelos Documentos 2' }
      ]);
    });


    expect(mockModelosDocumentosService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Modelos Documentos ABC' },
      { id: 2, nome: 'Modelos Documentos XYZ' }
    ];
    mockModelosDocumentosService.getList.mockResolvedValue(mockOptions as IModelosDocumentos[]);   


 const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Modelos Documentos ABC' },
        { id: 2, nome: 'Modelos Documentos XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Modelos Documentos ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Modelos Documentos ABC' },
      { id: 2, nome: 'Modelos Documentos XYZ' }
    ];
    mockModelosDocumentosService.getList.mockResolvedValue(mockOptions as IModelosDocumentos[]);
  


    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Modelos Documentos ABC' },
        { id: 2, nome: 'Modelos Documentos XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Modelos Documentos ABC' },
          {id: 2, nome: 'Modelos Documentos XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );

    const newValue = { id: 1, nome: 'Modelos Documentos Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Modelos Documentos Inicial' };
    
    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useModelosDocumentosComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Modelos Documentos Inicial' };
    
    const { result } = renderHook(() => 
      useModelosDocumentosComboBox(mockModelosDocumentosService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






