// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tracking: Forms.tsx.txt
'use client';
import { IDocsRecebidosItens } from '@/app/GerAdv_TS/DocsRecebidosItens/Interfaces/interface.DocsRecebidosItens';
import { useRouter } from 'next/navigation';
import React, { useEffect, useState, useRef } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { getParamFromUrl } from '@/app/tools/helpers';
import '@/app/styles/CrudFormsBase.css';
import '@/app/styles/CrudFormsMobile.css';
import '@/app/styles/CrudForms5.css'; // [ INDEX_SIZE ]
import ButtonSalvarCrud from '@/app/components/Cruds/ButtonSalvarCrud';
import { useIsMobile } from '@/app/context/MobileContext';
import DeleteButton from '@/app/components/Cruds/DeleteButton';
import { DocsRecebidosItensApi } from '../../Apis/ApiDocsRecebidosItens';
import { useValidationsDocsRecebidosItens } from '../../Hooks/hookDocsRecebidosItens';
import ContatoCRMComboBox from '@/app/GerAdv_TS/ContatoCRM/ComboBox/ContatoCRM';
import { ContatoCRMApi } from '@/app/GerAdv_TS/ContatoCRM/Apis/ApiContatoCRM';
import InputName from '@/app/components/Inputs/InputName';
import InputCheckbox from '@/app/components/Inputs/InputCheckbox';
import InputInput from '@/app/components/Inputs/InputInput'
interface DocsRecebidosItensFormProps {
  docsrecebidositensData: IDocsRecebidosItens;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const DocsRecebidosItensForm: React.FC<DocsRecebidosItensFormProps> = ({
  docsrecebidositensData, 
  onChange, 
  onSubmit, 
  onClose, 
  onError, 
  onReload, 
  onSuccess, 
}) => {
const router = useRouter();
const { systemContext } = useSystemContext();
const isMobile = useIsMobile();
const dadoApi = new DocsRecebidosItensApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [isSubmitting, setIsSubmitting] = useState(false);
const initialized = useRef(false);
const validationForm = useValidationsDocsRecebidosItens();
const [nomeContatoCRM, setNomeContatoCRM] = useState('');
const contatocrmApi = new ContatoCRMApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');

if (getParamFromUrl('contatocrm') > 0) {
  if (docsrecebidositensData.id === 0 && docsrecebidositensData.contatocrm == 0) {
    contatocrmApi
    .getById(getParamFromUrl('contatocrm'))
    .then((response) => {
      setNomeContatoCRM(response.data.campo);
    })
    .catch((error) => {
      console.log('Error unexpected');
    });

    docsrecebidositensData.contatocrm = getParamFromUrl('contatocrm');
  }
}
const addValorContatoCRM = (e: any) => {
  if (e?.id>0)
    onChange({ target: { name: 'contatocrm', value: e.id } });
  };
  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

      if (!isSubmitting) {
        setIsSubmitting(true);

        try {
          onSubmit(e);
        } catch (error) {
        console.log('Erro ao submeter formulário de DocsRecebidosItens:');
        setIsSubmitting(false);
        if (onError) onError();
        }
      }
    };
    const handleCancel = () => {
      if (onReload) {
        onReload(); // Recarrega os dados originais
      } else {
      onClose(); // Comportamento padrão se não há callback de recarga
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => { }, 
          target: document.getElementById(`DocsRecebidosItensForm-${docsrecebidositensData.id}`)
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
      console.log('Erro ao salvar DocsRecebidosItens diretamente');
      setIsSubmitting(false);
      if (onError) onError();
      }
    }
  };
  useEffect(() => {
    const el = document.querySelector('.nameFormMobile');
    if (el) {
      el.textContent = docsrecebidositensData?.id == 0 ? 'Editar DocsRecebidosItens' : 'Adicionar Docs Recebidos Itens';
    }
  }, [docsrecebidositensData.id]);
  return (
  <>
  {!isMobile ? <style jsx global>{`
    @media (max-width: 1366px) {
      html {
        zoom: 0.8 !important;
      }
    }
    `}</style> : null}

    <div className={isMobile ? 'form-container form-container-DocsRecebidosItens' : 'form-container5 form-container-DocsRecebidosItens'}>

      <form className='formInputCadInc' id={`DocsRecebidosItensForm-${docsrecebidositensData.id}`} onSubmit={onConfirm}>
        {!isMobile && (
          <ButtonSalvarCrud isMobile={false} validationForm={validationForm} entity='DocsRecebidosItens' data={docsrecebidositensData} isSubmitting={isSubmitting} onClose={onClose} formId={`DocsRecebidosItensForm-${docsrecebidositensData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
          )}
          <div className='grid-container'>

            <InputName
            type='text'
            id='nome'
            label='Nome'
            dataForm={docsrecebidositensData}
            className='inputIncNome'
            name='nome'
            value={docsrecebidositensData.nome}
            placeholder={`Informe Nome`}
            onChange={onChange}
            required
            />

            <ContatoCRMComboBox
            name={'contatocrm'}
            dataForm={docsrecebidositensData}
            value={docsrecebidositensData.contatocrm}
            setValue={addValorContatoCRM}
            label={'Contato C R M'}
            />
            <InputCheckbox dataForm={docsrecebidositensData} label='Devolvido' name='devolvido' checked={docsrecebidositensData.devolvido} onChange={onChange} />
            <InputCheckbox dataForm={docsrecebidositensData} label='SeraDevolvido' name='seradevolvido' checked={docsrecebidositensData.seradevolvido} onChange={onChange} />

            <InputInput
            type='text'
            maxLength={2147483647}
            id='observacoes'
            label='Observacoes'
            dataForm={docsrecebidositensData}
            className='inputIncNome'
            name='observacoes'
            value={docsrecebidositensData.observacoes}
            onChange={onChange}
            />

          </div>
        </form>


        {isMobile && (
          <ButtonSalvarCrud isMobile={true} validationForm={validationForm} entity='DocsRecebidosItens' data={docsrecebidositensData} isSubmitting={isSubmitting} onClose={onClose} formId={`DocsRecebidosItensForm-${docsrecebidositensData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
          )}
          <DeleteButton page={'/pages/docsrecebidositens'} id={docsrecebidositensData.id} closeModel={onClose} dadoApi={dadoApi} />
        </div>
        <div className='form-spacer'></div>
        </>
      );
    };