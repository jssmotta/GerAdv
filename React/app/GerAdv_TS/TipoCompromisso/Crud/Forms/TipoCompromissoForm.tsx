// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { ITipoCompromisso } from "@/app/GerAdv_TS/TipoCompromisso/Interfaces/interface.TipoCompromisso";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { TipoCompromissoApi } from "../../Apis/TipoCompromissoApi";
import { useValidationsTipoCompromisso } from "../../Hooks/hookTipoCompromisso";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpTipoCompromisso from "../../Help/HelpTipoCompromisso";
import TipoCompromissoPage from "@/app/pages/tipocompromisso/page";

import InputDescription from "@/app/components/Inputs/InputDescription";
import InputIconeAgenda from "@/app/components/Inputs/InputIconeAgenda";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface TipoCompromissoFormProps {
  tipocompromissoData: ITipoCompromisso;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const TipoCompromissoForm: React.FC<TipoCompromissoFormProps> = ({
  tipocompromissoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new TipoCompromissoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsTipoCompromisso();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de TipoCompromisso:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `TipoCompromissoForm-${tipocompromissoData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar TipoCompromisso diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        tipocompromissoData?.id == 0
          ? "Editar TipoCompromisso"
          : "Adicionar Tipo Compromisso";
    }
  }, [tipocompromissoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-TipoCompromisso"
            : "form-container5 form-container-TipoCompromisso"
        }
      >
        <form
          className="formInputCadInc"
          id={`TipoCompromissoForm-${tipocompromissoData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="TipoCompromisso"
            data={tipocompromissoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`TipoCompromissoForm-${tipocompromissoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputDescription
              tabIndex={0}
              type="text"
              id="descricao"
              label="Tipo Compromisso"
              dataForm={tipocompromissoData}
              className="input-default-main"
              name="descricao"
              value={tipocompromissoData.descricao}
              placeholder={`Digite nome/descrição do tipo compromisso`}
              onChange={onChange}
              required
            />

            <InputIconeAgenda
              tabIndex={1}
              type="text"
              maxLength={2048}
              id="icone"
              label="Icone"
              dataForm={tipocompromissoData}
              className="input-default-main"
              name="icone"
              value={tipocompromissoData.icone}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={tipocompromissoData}
              label="Financeiro"
              name="financeiro"
              checked={tipocompromissoData.financeiro}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton
            title="Tipo Compromisso"
            windowCrud={TipoCompromissoPage}
          />

          <CopyButton
            formId={`TipoCompromissoForm-${tipocompromissoData.id}`}
          />

          <PrintButton
            formId={`TipoCompromissoForm-${tipocompromissoData.id}`}
          />

          <AuditorButton
            tableName="TipoCompromisso"
            coolName="Tipo Compromisso"
            id={tipocompromissoData.id}
          />

          <DeleteButton
            page={"/pages/tipocompromisso"}
            id={tipocompromissoData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Tipo Compromisso"
            page={(onClose) => <HelpTipoCompromisso onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="TipoCompromisso"
          data={tipocompromissoData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`TipoCompromissoForm-${tipocompromissoData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
