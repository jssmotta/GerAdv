// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:GUTTipo
// Fonte:ValidationsGeneratorTests
namespace MenphisSI.GerAdv.Tests;
public class GUTTipoValidationTests : IDisposable
{
    private readonly GUTTipoValidation _validation;
    private readonly Mock<IGUTTipoService> _mockGUTTipoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly string _validUri = "test-uri";
    public GUTTipoValidationTests()
    {
        _validation = new GUTTipoValidation();
        _mockGUTTipoService = new Mock<IGUTTipoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void GUTTipoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IGUTTipoValidation>();
    }

    [Fact]
    public void GUTTipoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(GUTTipoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void GUTTipoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(GUTTipoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static GerAdv.Models.GUTTipo CreateValidGUTTipo()
    {
        return new GerAdv.Models.GUTTipo
        {
            Id = 1,
            Nome = "João",
            Ordem = 1
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockGUTTipoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterGUTTipo>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the GUTTipos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockGUTTipoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterGUTTipo>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the GUTTipos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var guttipo = new GerAdv.Models.GUTTipo
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Ordem = 1
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(guttipo, _mockGUTTipoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null !, _mockGUTTipoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var guttipo = CreateValidGUTTipo();
        guttipo.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(guttipo, _mockGUTTipoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().MatchRegex("(é obrigatório|não encontrado)");
    }

    [Fact]
    public async Task ValidateReg_WithNull_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var guttipo = CreateValidGUTTipo();
        guttipo.Nome = null !;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(guttipo, _mockGUTTipoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidData_Nome_ShouldReturnTrue()
    {
        // Arrange
        var guttipo = CreateValidGUTTipo();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(guttipo, _mockGUTTipoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespace_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var guttipo = CreateValidGUTTipo();
        guttipo.Nome = " ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(guttipo, _mockGUTTipoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        GC.SuppressFinalize(this);
    }
}