// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useOperadorEMailPopupForm, useOperadorEMailPopupList, useValidationsOperadorEMailPopup } from '../GerAdv_TS/OperadorEMailPopup/Hooks/hookOperadorEMailPopup';
import { IOperadorEMailPopup } from '../GerAdv_TS/OperadorEMailPopup/Interfaces/interface.OperadorEMailPopup';
import { IOperadorEMailPopupService } from '../GerAdv_TS/OperadorEMailPopup/Services/OperadorEMailPopup.service';
import { OperadorEMailPopupTestEmpty } from '../GerAdv_TS/Models/OperadorEMailPopup';
import { useOperadorEMailPopupComboBox } from '../GerAdv_TS/OperadorEMailPopup/Hooks/hookOperadorEMailPopup';

// Mock do serviço
const mockOperadorEMailPopupService: jest.Mocked<IOperadorEMailPopupService> = {
  fetchOperadorEMailPopupById: jest.fn(),
  saveOperadorEMailPopup: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteOperadorEMailPopup: jest.fn(),
  validateOperadorEMailPopup: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialOperadorEMailPopup: IOperadorEMailPopup = { ...OperadorEMailPopupTestEmpty() };

describe('useOperadorEMailPopupForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    expect(result.current.data).toEqual(initialOperadorEMailPopup);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Operador E Mail Popup',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Operador E Mail Popup');
  });

   test('deve carregar Operador E Mail Popup por ID', async () => {
    const mockOperadorEMailPopup = { ...initialOperadorEMailPopup, id: 1, nome: 'Operador E Mail Popup Teste' };
    mockOperadorEMailPopupService.fetchOperadorEMailPopupById.mockResolvedValue(mockOperadorEMailPopup);

    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.loadOperadorEMailPopup(1);
    });

    expect(mockOperadorEMailPopupService.fetchOperadorEMailPopupById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockOperadorEMailPopup);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Operador E Mail Popup', async () => {
    const errorMessage = 'Erro ao carregar Operador E Mail Popup';
    mockOperadorEMailPopupService.fetchOperadorEMailPopupById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.loadOperadorEMailPopup(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialOperadorEMailPopup, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialOperadorEMailPopup);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.loadOperadorEMailPopup(0);
    });

    expect(mockOperadorEMailPopupService.fetchOperadorEMailPopupById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialOperadorEMailPopup);
  });
});

describe('useOperadorEMailPopupList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupList(mockOperadorEMailPopupService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialOperadorEMailPopup, id: 1, nome: 'Operador E Mail Popup 1' },
      { ...initialOperadorEMailPopup, id: 2, nome: 'Operador E Mail Popup 2' }
    ];
    mockOperadorEMailPopupService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOperadorEMailPopupList(mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockOperadorEMailPopupService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockOperadorEMailPopupService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOperadorEMailPopupList(mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialOperadorEMailPopup, id: 1, nome: 'Operador E Mail Popup Filtrado' }];
    const filtro = { nome: 'Operador E Mail Popup' };
    mockOperadorEMailPopupService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOperadorEMailPopupList(mockOperadorEMailPopupService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockOperadorEMailPopupService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsOperadorEMailPopup', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsOperadorEMailPopup());

    const validData = { ...initialOperadorEMailPopup, nome: 'Operador E Mail Popup Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsOperadorEMailPopup());

    const invalidData = { ...initialOperadorEMailPopup, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsOperadorEMailPopup());

    const invalidData = { 
      ...initialOperadorEMailPopup, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsOperadorEMailPopup());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialOperadorEMailPopup, id: 1, nome: 'Operador E Mail Popup Teste' }];
    mockOperadorEMailPopupService.getAll.mockResolvedValue(mockData);
    mockOperadorEMailPopupService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useOperadorEMailPopupList(mockOperadorEMailPopupService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsOperadorEMailPopup()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Operador E Mail Popup Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Operador E Mail Popup Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupForm(initialOperadorEMailPopup, mockOperadorEMailPopupService)
    );

    const mockEvent = {
      target: {
        name: 'autenticacao',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.autenticacao).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Operador E Mail Popup 1' },
      { id: 2, nome: 'Operador E Mail Popup 2' }
    ];
    mockOperadorEMailPopupService.getList.mockResolvedValue(mockOptions as IOperadorEMailPopup[]);


    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Operador E Mail Popup 1' },
        { id: 2, nome: 'Operador E Mail Popup 2' }
      ]);
    });


    expect(mockOperadorEMailPopupService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Operador E Mail Popup ABC' },
      { id: 2, nome: 'Operador E Mail Popup XYZ' }
    ];
    mockOperadorEMailPopupService.getList.mockResolvedValue(mockOptions as IOperadorEMailPopup[]);   


 const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Operador E Mail Popup ABC' },
        { id: 2, nome: 'Operador E Mail Popup XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Operador E Mail Popup ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Operador E Mail Popup ABC' },
      { id: 2, nome: 'Operador E Mail Popup XYZ' }
    ];
    mockOperadorEMailPopupService.getList.mockResolvedValue(mockOptions as IOperadorEMailPopup[]);
  


    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Operador E Mail Popup ABC' },
        { id: 2, nome: 'Operador E Mail Popup XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Operador E Mail Popup ABC' },
          {id: 2, nome: 'Operador E Mail Popup XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );

    const newValue = { id: 1, nome: 'Operador E Mail Popup Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Operador E Mail Popup Inicial' };
    
    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useOperadorEMailPopupComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Operador E Mail Popup Inicial' };
    
    const { result } = renderHook(() => 
      useOperadorEMailPopupComboBox(mockOperadorEMailPopupService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






