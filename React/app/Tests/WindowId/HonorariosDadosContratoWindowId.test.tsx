// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import HonorariosDadosContratoWindowId from '@/app/GerAdv_TS/HonorariosDadosContrato/Crud/Grids/HonorariosDadosContratoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import HonorariosDadosContratoWindowId from '@/app/GerAdv_TS/HonorariosDadosContrato/Crud/Grids/HonorariosDadosContratoWindowId';
import { HonorariosDadosContratoEmpty } from '@/app/GerAdv_TS/Models/HonorariosDadosContrato';

describe('HonorariosDadosContratoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(HonorariosDadosContratoWindowId).toBeDefined();
    expect(typeof HonorariosDadosContratoWindowId === 'function' || typeof HonorariosDadosContratoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the HonorariosDadosContratoService class so constructor usage doesn't throw and we can control fetchHonorariosDadosContratoById
const mockFetchHonorariosDadosContratoById = jest.fn();
jest.mock('@/app/GerAdv_TS/HonorariosDadosContrato/Services/HonorariosDadosContrato.service', () => ({
  HonorariosDadosContratoService: jest.fn().mockImplementation(() => ({
    fetchHonorariosDadosContratoById: (...args: any[]) => mockFetchHonorariosDadosContratoById(...args),
  })),
}));

// Mock the child HonorariosDadosContratoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/HonorariosDadosContrato/Crud/Grids/HonorariosDadosContratoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='honorariosdadoscontrato-window' {...p} />,
}));

describe('HonorariosDadosContratoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(HonorariosDadosContratoWindowId).toBeDefined();
    expect(typeof HonorariosDadosContratoWindowId === 'function' || typeof HonorariosDadosContratoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <HonorariosDadosContratoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders HonorariosDadosContratoWindow with HonorariosDadosContratoEmpty when id is 0', async () => {
    render(
      <HonorariosDadosContratoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // HonorariosDadosContratoWindow should be rendered with selectedHonorariosDadosContrato equal to HonorariosDadosContratoEmpty()
    const node = await screen.findByTestId('honorariosdadoscontrato-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedHonorariosDadosContrato"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedhonorariosdadoscontrato') || node.getAttribute('selectedHonorariosDadosContrato')).not.toBeNull();
  });

  it('fetches honorariosdadoscontrato by id and passes it to HonorariosDadosContratoWindow when id > 0', async () => {
    const fakeHonorariosDadosContrato = { id: 123, nomecliente: 'Test' };
    mockFetchHonorariosDadosContratoById.mockResolvedValue(fakeHonorariosDadosContrato);

    render(
      <HonorariosDadosContratoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('honorariosdadoscontrato-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchHonorariosDadosContratoById).toHaveBeenCalledWith(123));
  });
});
