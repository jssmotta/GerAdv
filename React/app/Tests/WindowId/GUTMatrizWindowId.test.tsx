// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTMatrizWindowId from '@/app/GerAdv_TS/GUTMatriz/Crud/Grids/GUTMatrizWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTMatrizWindowId from '@/app/GerAdv_TS/GUTMatriz/Crud/Grids/GUTMatrizWindowId';
import { GUTMatrizEmpty } from '@/app/GerAdv_TS/Models/GUTMatriz';

describe('GUTMatrizWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTMatrizWindowId).toBeDefined();
    expect(typeof GUTMatrizWindowId === 'function' || typeof GUTMatrizWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTMatrizService class so constructor usage doesn't throw and we can control fetchGUTMatrizById
const mockFetchGUTMatrizById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTMatriz/Services/GUTMatriz.service', () => ({
  GUTMatrizService: jest.fn().mockImplementation(() => ({
    fetchGUTMatrizById: (...args: any[]) => mockFetchGUTMatrizById(...args),
  })),
}));

// Mock the child GUTMatrizWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTMatriz/Crud/Grids/GUTMatrizWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='gutmatriz-window' {...p} />,
}));

describe('GUTMatrizWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTMatrizWindowId).toBeDefined();
    expect(typeof GUTMatrizWindowId === 'function' || typeof GUTMatrizWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTMatrizWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTMatrizWindow with GUTMatrizEmpty when id is 0', async () => {
    render(
      <GUTMatrizWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTMatrizWindow should be rendered with selectedGUTMatriz equal to GUTMatrizEmpty()
    const node = await screen.findByTestId('gutmatriz-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTMatriz"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgutmatriz') || node.getAttribute('selectedGUTMatriz')).not.toBeNull();
  });

  it('fetches gutmatriz by id and passes it to GUTMatrizWindow when id > 0', async () => {
    const fakeGUTMatriz = { id: 123, nomecliente: 'Test' };
    mockFetchGUTMatrizById.mockResolvedValue(fakeGUTMatriz);

    render(
      <GUTMatrizWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('gutmatriz-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTMatrizById).toHaveBeenCalledWith(123));
  });
});
