// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { HonorariosDadosContratoEmpty } from '../../../Models/HonorariosDadosContrato';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IHonorariosDadosContrato } from '../../Interfaces/interface.HonorariosDadosContrato';
import { HonorariosDadosContratoService } from '../../Services/HonorariosDadosContrato.service';
import { HonorariosDadosContratoApi } from '../../Apis/ApiHonorariosDadosContrato';
import { HonorariosDadosContratoGridMobileComponent } from '../GridsMobile/HonorariosDadosContratoMobile';
import { HonorariosDadosContratoGridDesktopComponent } from '../GridsDesktop/HonorariosDadosContratoDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterHonorariosDadosContrato } from '../../Filters/HonorariosDadosContrato';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import HonorariosDadosContratoWindow from './HonorariosDadosContratoWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useHonorariosDadosContratoList } from '../../Hooks/hookHonorariosDadosContrato';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useHonorariosDadosContratoFilter } from '../../Hooks/hookHonorariosDadosContratoFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const HonorariosDadosContratoGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedHonorariosDadosContrato, setSelectedHonorariosDadosContrato] = useState<IHonorariosDadosContrato>(HonorariosDadosContratoEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterHonorariosDadosContrato | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [honorariosdadoscontratoData, setHonorariosDadosContratoData] = useState<IHonorariosDadosContrato[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const honorariosdadoscontratoService = useMemo(() => {
    return new HonorariosDadosContratoService(
    new HonorariosDadosContratoApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: honorariosdadoscontrato, 
  fetchData, 
  refreshData
} = useHonorariosDadosContratoList(honorariosdadoscontratoService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterHonorariosDadosContrato | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('HonorariosDadosContratoFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await honorariosdadoscontratoService.getAll(
  filtro ?? {} as FilterHonorariosDadosContrato, 
  onlineData => {
    setHonorariosDadosContratoData(onlineData);
    setLoading(false);
  }
);
setHonorariosDadosContratoData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterHonorariosDadosContrato;

    const filtroMem = sessionStorage.getItem(btoa('HonorariosDadosContratoFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('HonorariosDadosContratoFilter')) ?? '') || '{}') : {} as FilterHonorariosDadosContrato;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (honorariosdadoscontrato: IHonorariosDadosContrato) => {
    setSelectedHonorariosDadosContrato(honorariosdadoscontrato);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedHonorariosDadosContrato(HonorariosDadosContratoEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const honorariosdadoscontrato = e.dataItem;
    setDeleteId(honorariosdadoscontrato.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await honorariosdadoscontratoService.deleteHonorariosDadosContrato(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useHonorariosDadosContratoFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <HonorariosDadosContratoGridMobileComponent
        data={honorariosdadoscontratoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <HonorariosDadosContratoGridDesktopComponent
        data={honorariosdadoscontratoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <HonorariosDadosContratoWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedHonorariosDadosContrato={selectedHonorariosDadosContrato}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Honorarios Dados Contrato'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default HonorariosDadosContratoGrid;