// 100% auto-generated by Source Genesys Core
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
// ReSharper disable once InconsistentNaming
public partial class DBEventoPrazoAgenda
{
    public DBEventoPrazoAgenda(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        InitFromRecord(name => dbRec.Table.Columns.Contains(name) ? dbRec[name] : null);
    }

    public DBEventoPrazoAgenda(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec[name]);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EventoPrazoAgenda: {ex.Message}", ex);
        }
    }

    private void InitFromRecord(Func<string, object?> getValue)
    {
        if (DBNull.Value.Equals(getValue(CampoCodigo)))
            return;
        ID = Convert.ToInt32(getValue(CampoCodigo));
        // Checkpoint Carregar 
        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.Bold)))
                m_FBold = Convert.ToBoolean(getValue(DBEventoPrazoAgendaDicInfo.Bold));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.DtAtu)))
                m_FDtAtu = Convert.ToDateTime(getValue(DBEventoPrazoAgendaDicInfo.DtAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.DtCad)))
                m_FDtCad = Convert.ToDateTime(getValue(DBEventoPrazoAgendaDicInfo.DtCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.QuemAtu)))
                m_FQuemAtu = Convert.ToInt32(getValue(DBEventoPrazoAgendaDicInfo.QuemAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.QuemCad)))
                m_FQuemCad = Convert.ToInt32(getValue(DBEventoPrazoAgendaDicInfo.QuemCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEventoPrazoAgendaDicInfo.Visto)))
                m_FVisto = Convert.ToBoolean(getValue(DBEventoPrazoAgendaDicInfo.Visto));
        }
        catch
        {
        }

        try
        {
            m_FNome = getValue(DBEventoPrazoAgendaDicInfo.Nome)?.ToString() ?? string.Empty;
        }
        catch
        {
        }
    }

    public void CarregarDadosBd(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec.Table.Columns.Contains(name) ? dbRec[name] : null);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EventoPrazoAgenda: {ex.Message}", ex);
        }
    }

    public void CarregarDadosBd(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec[name]);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EventoPrazoAgenda: {ex.Message}", ex);
        }
    }
}