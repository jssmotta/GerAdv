// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterAlertasEnviados } from '@/app/GerAdv_TS/AlertasEnviados/Filters/AlertasEnviados';
import OperadorComboBox from '@/app/GerAdv_TS/Operador/ComboBox/Operador';
import AlertasComboBox from '@/app/GerAdv_TS/Alertas/ComboBox/Alertas';
interface UseAlertasEnviadosFilterProps {
  handleFetchWithFilter: (filtro?: FilterAlertasEnviados | undefined | null) => Promise<void>;
}
interface UseAlertasEnviadosFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterAlertasEnviados;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterAlertasEnviados>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterAlertasEnviados) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterAlertasEnviados>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useAlertasEnviadosFilter = ({ handleFetchWithFilter }: UseAlertasEnviadosFilterProps): UseAlertasEnviadosFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterAlertasEnviados>({} as FilterAlertasEnviados);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterAlertasEnviados;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterAlertasEnviados) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('AlertasEnviadosFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterAlertasEnviados>) => (
  <>
  <OperadorComboBox
  name='operador'
  dataForm={null}
  value={handlers.windowFilter?.operador}
  setValue={(e:any) => handlers.handleComboChange(e, 'operador')}
  className='inputSearch inputSearchComboboxTab'
  label='Operador'
  />
  <AlertasComboBox
  name='alerta'
  dataForm={null}
  value={handlers.windowFilter?.alerta}
  setValue={(e:any) => handlers.handleComboChange(e, 'alerta')}
  className='inputSearch inputSearchComboboxTab'
  label='Alertas'
  />
  <InputDate
  type='text'
  id='dataalertado'
  label='DataAlertado (igual ou início)'
  dataForm={null}
  className='inputSearch'
  name='dataalertado'
  value={handlers.windowFilter?.dataalertado ?? ''}
  onChange={(value: string) => handlers.handleDateChange('dataalertado', value)}
  />
  <InputDate
  type='text'
  id='dataalertado_end'
  label='DataAlertado (final)'
  dataForm={null}
  className='inputSearch'
  name='dataalertado_end'
  value={handlers.windowFilter?.dataalertado_end ?? ''}
  disabled={handlers.windowFilter?.dataalertado ? false: true}
  onChange={(value: string) => handlers.handleDateChange('dataalertado_end', value)}
  />
  <InputComboFilterYesNo
  type='text'
  id='visualizado'
  name='visualizado'
  value={handlers.windowFilter?.visualizado ?? -2147483648}
  onChange={handlers.handleInputChange}
  label='Visualizado'
  className='inputSearch inputSearchCheckbox'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};