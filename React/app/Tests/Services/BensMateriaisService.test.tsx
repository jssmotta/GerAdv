// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { BensMateriaisService, BensMateriaisValidator } from '@/app/GerAdv_TS/BensMateriais/Services/BensMateriais.service';
import { BensMateriaisApi, BensMateriaisApiError } from '@/app/GerAdv_TS/BensMateriais/Apis/ApiBensMateriais';
import { IBensMateriais } from '@/app/GerAdv_TS/BensMateriais/Interfaces/interface.BensMateriais';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as BensMateriaisApi;

const sample: IBensMateriais = { id: 1, nome: 'Teste' };

describe('BensMateriaisService', () => {
  let service: BensMateriaisService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new BensMateriaisService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(BensMateriaisValidator, 'validateBensMateriais').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchBensMateriaisById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchBensMateriaisById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchBensMateriaisById lança BensMateriaisApiError para id inválido', async () => {
    await expect(service.fetchBensMateriaisById(0)).rejects.toBeInstanceOf(BensMateriaisApiError);
  });

  it('saveBensMateriais chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveBensMateriais(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveBensMateriais lança BensMateriaisApiError quando validação falha', async () => {
    (BensMateriaisValidator.validateBensMateriais as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveBensMateriais(sample)).rejects.toBeInstanceOf(BensMateriaisApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteBensMateriais chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteBensMateriais(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteBensMateriais lança BensMateriaisApiError para id inválido', async () => {
    await expect(service.deleteBensMateriais(0)).rejects.toBeInstanceOf(BensMateriaisApiError);
  });

  it('validateBensMateriais delega para BensMateriaisValidator', () => {
    (BensMateriaisValidator.validateBensMateriais as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateBensMateriais(sample);
    expect(r.isValid).toBe(true);
  });
});
