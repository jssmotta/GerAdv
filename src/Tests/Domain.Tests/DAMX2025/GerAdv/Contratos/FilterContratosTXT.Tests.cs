// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Contratos
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterContratosTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterContratosTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithProcesso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Processo = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"processo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCliente_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Cliente = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cliente\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithAdvogado_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Advogado = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"advogado\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDia_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Dia = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dia\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithValor_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Valor = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"valor\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithDataInicio_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            DataInicio = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"datainicio\"");
    }

    [Fact]
    public void GetPrompt_WithDataTermino_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            DataTermino = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"datatermino\"");
    }

    [Fact]
    public void GetPrompt_WithPercEscritorio_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            PercEscritorio = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"percescritorio\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithValorConsultoria_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            ValorConsultoria = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"valorconsultoria\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithTipoCobranca_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            TipoCobranca = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"tipocobranca\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithProtestar_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Protestar = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"protestar\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithJuros_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Juros = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"juros\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithValorRealizavel_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            ValorRealizavel = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"valorrealizavel\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithDOCUMENTO_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            DOCUMENTO = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"documento\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEMail1_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            EMail1 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email1\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEMail2_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            EMail2 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email2\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEMail3_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            EMail3 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email3\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPessoa1_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Pessoa1 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pessoa1\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPessoa2_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Pessoa2 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pessoa2\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPessoa3_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Pessoa3 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pessoa3\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithOBS_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            OBS = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"obs\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithClienteContrato_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            ClienteContrato = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"clientecontrato\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithIdExtrangeiro_SerializesNumericValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            IdExtrangeiro = 123
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"idextrangeiro\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithChaveContrato_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            ChaveContrato = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"chavecontrato\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithMulta_SerializesCorrectly()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Multa = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"multa\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterClientes\"");
        prompt.Should().Contain("\"filterAdvogados\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(new FilterContratos(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(new FilterContratos(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(new FilterContratos(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(new FilterContratos(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterContratosTXT.GetOptions();
        var filter = new FilterContratos
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterContratosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}