// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class DocumentosValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFDocumentosFactory> _mockDocumentosFactory;
    private readonly Mock<IDocumentosReader> _mockReader;
    private readonly DocumentosValidation _validation;
    private readonly Mock<IDocumentosService> _mockDocumentosService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DocumentosService _service;
    private readonly string _validUri = "test-uri";
    public DocumentosValidationTests()
    {
        _mockDocumentosFactory = new Mock<IFDocumentosFactory>();
        _mockReader = new Mock<IDocumentosReader>();
        _validation = new DocumentosValidation();
        _mockDocumentosService = new Mock<IDocumentosService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void DocumentosValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IDocumentosValidation>();
    }

    [Fact]
    public void DocumentosValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(DocumentosValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void DocumentosValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(DocumentosValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Documentos CreateValidDocumentos()
    {
        return new Models.Documentos
        {
            Id = 1,
            Processo = 0,
            Data = "27/05/2022",
            Observacao = "Observação teste"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockDocumentosService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterDocumentos>(), It.IsAny<string>())).ReturnsAsync(new List<DocumentosResponseAll>());
    // Setup other mocks but don't override the Documentoss service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockDocumentosService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterDocumentos>(), It.IsAny<string>())).ReturnsAsync(new List<DocumentosResponseAll>());
    // Setup other mocks but don't override the Documentoss service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var documentos = new Models.Documentos
        {
            Id = 1,
            Data = "27/05/2022",
            Observacao = null
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(documentos, _mockDocumentosService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockDocumentosService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}