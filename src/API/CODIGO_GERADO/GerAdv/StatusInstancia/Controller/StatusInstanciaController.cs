// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:StatusInstancia
// Fonte:ControllerGenerator
namespace MenphisSI.GerAdv.Controller;
[Route("api/v{version:apiVersion}/{uri}/[controller]/[action]")]
[ApiController]
[ApiVersion("1.0")]
public partial class StatusInstanciaController(IStatusInstanciaService statusinstanciaService) : ControllerBase
{
    private readonly IStatusInstanciaService _statusinstanciaService = statusinstanciaService;
    private static readonly Logger _logger = LogManager.GetCurrentClassLogger();
    [HttpGet]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAll([FromQuery] int max, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "GetAll";
        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.GetAll(max, uri);
            });
            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            StatusInstanciaMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: GetAll failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Filter([FromQuery] int max, [FromBody] MenphisSI.GerAdv.Filters.FilterStatusInstancia filter, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "Filter";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.Filter(max, filter, uri);
            });
            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            StatusInstanciaMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: Filter failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> FilterVoice([FromBody] MenphisSI.GerAdv.Filters.FilterStatusInstanciaWithVoiceRequest request, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "FilterVoice";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.RelatoriosPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.FilterVoice(request.Filter, request.VoiceCommand, uri);
            });
            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: FilterVoice failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetById(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "GetById";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.GetById(id, uri, token);
            });
            if (result == null)
            {
                StatusInstanciaMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetById: No StatusInstancia found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            StatusInstanciaMetrics.RecordReadByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: GetById failed with exception for id = {0}, {1}", id, uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [ProducesResponseType(typeof(AuditorResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAuditor(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "GetAuditor";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.GetAuditor(id, uri, token);
            });
            if (result == null)
            {
                StatusInstanciaMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetAuditor: No StatusInstancia found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: GetAuditor failed with exception for id = {0}, {1}", id, uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = "Erro 500 Auditor" });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetListN([FromQuery] int max, [FromBody] Filters.FilterStatusInstancia? filtro, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "GetListN";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.GetListN(max, filtro, uri);
            });
            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            StatusInstanciaMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: GetListN failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [ProducesResponseType(typeof(StatusInstanciaResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(StatusInstanciaResponse), StatusCodes.Status201Created)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> AddAndUpdate([FromBody] Models.StatusInstancia regStatusInstancia, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        var isNew = regStatusInstancia.Id == 0;
        var operacao = isNew ? "Create" : "Update";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.AddAndUpdate(regStatusInstancia, uri);
            });
            if (result == null)
            {
                StatusInstanciaMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("StatusInstancia: AddAndUpdate failed to add or update StatusInstancia, {0}", uri);
                return BadRequest();
            }

            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            if (isNew)
            {
                StatusInstanciaMetrics.RecordCreated(uri);
                StatusInstanciaMetrics.RecordCreatedByHour(uri);
            }
            else
            {
                StatusInstanciaMetrics.RecordUpdated(uri);
                StatusInstanciaMetrics.RecordUpdatedByHour(uri);
            }

            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: AddAndUpdate failed with exception for uri = {0}", uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = ex.Message });
        }
    }

    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [HttpDelete]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> Delete([FromQuery] int id, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "Delete";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.Delete(id, uri);
            });
            if (result == null)
            {
                StatusInstanciaMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("Delete: No StatusInstancia found to delete with id = {0}, {1}", id, uri);
                return NotFound();
            }

            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            StatusInstanciaMetrics.RecordDeleted(uri);
            StatusInstanciaMetrics.RecordDeletedByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordConflict(operacao, uri, stopwatch);
            _logger.Error(ex, "StatusInstancia: Delete failed with exception for id = {0}, {1}", id, uri);
            return Conflict(new Error500 { success = false, data = "", message = "Não é possível excluir o registro porque ele está sendo referenciado/em uso em outra tabela." });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Validation([FromBody] Models.StatusInstancia regStatusInstancia, [FromRoute, Required] string uri)
    {
        var stopwatch = StatusInstanciaMetrics.StartTimer();
        const string operacao = "Validation";
        if (!ModelState.IsValid)
        {
            StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _statusinstanciaService.Validation(regStatusInstancia, uri);
            });
            if (result == null)
            {
                StatusInstanciaMetrics.RecordInvalid(operacao, uri, stopwatch);
                _logger.Warn("StatusInstancia: Validation failed to add or update StatusInstancia, {0}", uri);
                return BadRequest();
            }

            StatusInstanciaMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            StatusInstanciaMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("StatusInstancia: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            StatusInstanciaMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "StatusInstancia: Validation failed with exception for uri = {0}", uri);
            return StatusCode(500, new { success = false, data = "", message = ex.Message });
        }
    }
}