// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { ContatoCRMOperadorEmpty } from '../../../Models/ContatoCRMOperador';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IContatoCRMOperador } from '../../Interfaces/interface.ContatoCRMOperador';
import { ContatoCRMOperadorService } from '../../Services/ContatoCRMOperador.service';
import { ContatoCRMOperadorApi } from '../../Apis/ApiContatoCRMOperador';
import { ContatoCRMOperadorGridMobileComponent } from '../GridsMobile/ContatoCRMOperador';
import { ContatoCRMOperadorGridDesktopComponent } from '../GridsDesktop/ContatoCRMOperador';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterContatoCRMOperador } from '../../Filters/ContatoCRMOperador';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import ContatoCRMOperadorWindow from './ContatoCRMOperadorWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useContatoCRMOperadorList } from '../../Hooks/hookContatoCRMOperador';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useContatoCRMOperadorFilter } from '../../Hooks/hookContatoCRMOperadorFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const ContatoCRMOperadorGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedContatoCRMOperador, setSelectedContatoCRMOperador] = useState<IContatoCRMOperador>(ContatoCRMOperadorEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterContatoCRMOperador | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [contatocrmoperadorData, setContatoCRMOperadorData] = useState<IContatoCRMOperador[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const contatocrmoperadorService = useMemo(() => {
    return new ContatoCRMOperadorService(
    new ContatoCRMOperadorApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: contatocrmoperador, 
  fetchData, 
  refreshData
} = useContatoCRMOperadorList(contatocrmoperadorService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterContatoCRMOperador | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('ContatoCRMOperadorFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await contatocrmoperadorService.getAll(
  filtro ?? {} as FilterContatoCRMOperador, 
  onlineData => {
    setContatoCRMOperadorData(onlineData);
    setLoading(false);
  }
);
setContatoCRMOperadorData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterContatoCRMOperador;

    const filtroMem = sessionStorage.getItem(btoa('ContatoCRMOperadorFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('ContatoCRMOperadorFilter')) ?? '') || '{}') : {} as FilterContatoCRMOperador;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (contatocrmoperador: IContatoCRMOperador) => {
    setSelectedContatoCRMOperador(contatocrmoperador);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedContatoCRMOperador(ContatoCRMOperadorEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const contatocrmoperador = e.dataItem;
    setDeleteId(contatocrmoperador.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await contatocrmoperadorService.deleteContatoCRMOperador(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useContatoCRMOperadorFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <ContatoCRMOperadorGridMobileComponent
        data={contatocrmoperadorData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <ContatoCRMOperadorGridDesktopComponent
        data={contatocrmoperadorData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <ContatoCRMOperadorWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedContatoCRMOperador={selectedContatoCRMOperador}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Contato C R M Operador'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default ContatoCRMOperadorGrid;