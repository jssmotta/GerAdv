// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Main.tsx.txt
"use client";
import { PageLayout } from "@/app/components/Cruds/PageLayout";
import { PageTitle } from "@/app/components/PageTitle";
import { IJustica } from "@/app/GerAdv_TS/Justica/Interfaces/interface.Justica";
import { JusticaGridAdapter } from "@/app/GerAdv_TS/Justica/Adapter/JusticaGridAdapter";
import JusticaGridContainer from "@/app/GerAdv_TS/Justica/Components/JusticaGridContainer";

interface JusticaPageProps {
  noLayout?: boolean;
  selectItem: (item: IJustica) => void;
}

const JusticaPage: React.FC<JusticaPageProps> = ({ noLayout, selectItem }) => {
  const JusticaGrid = new JusticaGridAdapter();

  if (noLayout && !!noLayout) {
    return <JusticaGridContainer grid={JusticaGrid} selectItem={selectItem} />;
  }

  return (
    <PageLayout>
      <JusticaGridContainer grid={JusticaGrid} />
    </PageLayout>
  );
};

export default JusticaPage;
