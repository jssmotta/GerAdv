// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProResumosWindowId from '@/app/GerAdv_TS/ProResumos/Crud/Grids/ProResumosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProResumosWindowId from '@/app/GerAdv_TS/ProResumos/Crud/Grids/ProResumosWindowId';
import { ProResumosEmpty } from '@/app/GerAdv_TS/Models/ProResumos';

describe('ProResumosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProResumosWindowId).toBeDefined();
    expect(typeof ProResumosWindowId === 'function' || typeof ProResumosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProResumosService class so constructor usage doesn't throw and we can control fetchProResumosById
const mockFetchProResumosById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProResumos/Services/ProResumos.service', () => ({
  ProResumosService: jest.fn().mockImplementation(() => ({
    fetchProResumosById: (...args: any[]) => mockFetchProResumosById(...args),
  })),
}));

// Mock the child ProResumosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProResumos/Crud/Grids/ProResumosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='proresumos-window' {...p} />,
}));

describe('ProResumosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProResumosWindowId).toBeDefined();
    expect(typeof ProResumosWindowId === 'function' || typeof ProResumosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProResumosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProResumosWindow with ProResumosEmpty when id is 0', async () => {
    render(
      <ProResumosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProResumosWindow should be rendered with selectedProResumos equal to ProResumosEmpty()
    const node = await screen.findByTestId('proresumos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProResumos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedproresumos') || node.getAttribute('selectedProResumos')).not.toBeNull();
  });

  it('fetches proresumos by id and passes it to ProResumosWindow when id > 0', async () => {
    const fakeProResumos = { id: 123, nomecliente: 'Test' };
    mockFetchProResumosById.mockResolvedValue(fakeProResumos);

    render(
      <ProResumosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('proresumos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProResumosById).toHaveBeenCalledWith(123));
  });
});
