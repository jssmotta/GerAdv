// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Readers;
public partial class AreaReader(IFAreaFactory areaFactory, IConnectionService connection) : IAreaReader
{
    private readonly IFAreaFactory _areaFactory = areaFactory ?? throw new ArgumentNullException();
    private readonly IConnectionService _connection = connection ?? throw new ArgumentNullException();
    public async Task<IEnumerable<DBNomeID>?> ListarNAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order) => await DevourerSqlData.ListarNomeID(BuildSqlQuery("areCodigo, areDescricao", cWhere, order, max), parameters, uri, caching: false, max: max);
    public async Task<IEnumerable<AreaResponseAll>> ListarAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        return await ListarTabelaAsync(BuildSqlQuery(DBArea.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
    }

    private async Task<IEnumerable<AreaResponseAll>> ListarTabelaAsync(string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var result = new List<AreaResponseAll>(max);
        await using var connection = _connection.GetConnectionByUri(uri);
        await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: connection?.InnerConnection)
        {
            CommandTimeout = 30
        };
        if (parameters != null && parameters.Count > 0)
            foreach (var param in parameters)
            {
                if (!cmd.Parameters.Contains(param.ParameterName))
                {
                    var newParam = new SqlParameter(param.ParameterName, param.Value)
                    {
                        SqlDbType = param.SqlDbType,
                        Direction = param.Direction,
                        Size = param.Size,
                        Precision = param.Precision,
                        Scale = param.Scale
                    };
                    cmd.Parameters.Add(newParam);
                }
            }

        await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.SingleResult, cancellationToken);
        while (await reader.ReadAsync(cancellationToken))
        {
            if (cancellationToken.IsCancellationRequested)
                return result;
            result.Add(ReadAll(_areaFactory.CreateFromReader(reader), reader)!);
        }

        return result;
    }

    public async Task<AreaResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _areaFactory.CreateFromIdAsync(id, oCnn);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public async Task<Models.Area?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _areaFactory.CreateFromIdAsync(id, oCnn);
        var area = new Models.Area
        {
            Id = dbRec.ID,
            Descricao = dbRec.FDescricao ?? string.Empty,
            Top = dbRec.FTop,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return area;
    }

    public AreaResponse? Read(FArea dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public AreaResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _areaFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public AreaResponse? Read(FArea dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var area = new AreaResponse
        {
            Id = dbRec.ID,
            Descricao = dbRec.FDescricao ?? string.Empty,
            Top = dbRec.FTop,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return area;
    }

    public AreaResponse? Read(SG.GerAdv.DBArea dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var area = new AreaResponse
        {
            Id = dbRec.ID,
            Descricao = dbRec.FDescricao ?? string.Empty,
            Top = dbRec.FTop,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return area;
    }

    public AreaResponseAll? ReadAll(FArea dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var area = new AreaResponseAll
        {
            Id = dbRec.ID,
            Descricao = dbRec.FDescricao ?? string.Empty,
            Top = dbRec.FTop,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return area;
    }

    public AreaResponseAll? ReadAll(SG.GerAdv.DBArea dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var area = new AreaResponseAll
        {
            Id = dbRec.ID,
            Descricao = dbRec.FDescricao ?? string.Empty,
            Top = dbRec.FTop,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return area;
    }
}