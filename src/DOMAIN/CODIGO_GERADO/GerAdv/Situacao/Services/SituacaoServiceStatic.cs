// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial class SituacaoService
{
    private static (string where, List<SqlParameter> parametros)? WFiltro(Filters.FilterSituacao filtro)
    {
        var parameters = new List<SqlParameter>();
        if (!string.IsNullOrEmpty(filtro.Parte_Int))
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.Parte_Int)}", ApplyWildCard(filtro.WildcardChar, filtro.Parte_Int)));
        }

        if (!string.IsNullOrEmpty(filtro.Parte_Opo))
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.Parte_Opo)}", ApplyWildCard(filtro.WildcardChar, filtro.Parte_Opo)));
        }

        if (filtro.Top != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.Top)}", filtro.Top));
        }

        if (!string.IsNullOrEmpty(filtro.GUID))
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.GUID)}", ApplyWildCard(filtro.WildcardChar, filtro.GUID)));
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
        }

        if (filtro.Codigo_filtro_end != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBSituacaoDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
        }

        if (filtro.LogicalOperator.IsEmpty() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var cWhere = new StringBuilder();
        cWhere.Append(filtro.Parte_Int.IsEmpty() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBSituacaoDicInfo.PTabelaNome}].[{DBSituacaoDicInfo.Parte_Int}]  {DevourerConsts.MsiCollate} like @{nameof(DBSituacaoDicInfo.Parte_Int)}");
        cWhere.Append(filtro.Parte_Opo.IsEmpty() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBSituacaoDicInfo.PTabelaNome}].[{DBSituacaoDicInfo.Parte_Opo}]  {DevourerConsts.MsiCollate} like @{nameof(DBSituacaoDicInfo.Parte_Opo)}");
        cWhere.Append(filtro.Top == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBSituacaoDicInfo.PTabelaNome}].[{DBSituacaoDicInfo.Top}] = @{nameof(DBSituacaoDicInfo.Top)}");
        cWhere.Append(filtro.GUID.IsEmpty() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBSituacaoDicInfo.PTabelaNome}].[{DBSituacaoDicInfo.GUID}]  {DevourerConsts.MsiCollate} like @{nameof(DBSituacaoDicInfo.GUID)}");
        if (!filtro.Codigo_filtro.IsEmpty() && filtro.Codigo_filtro_end.IsEmpty())
        {
            cWhere.Append(filtro.Codigo_filtro <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBSituacaoDicInfo.PTabelaNome}].[{DBSituacaoDicInfo.CampoCodigo}] >= @{nameof(DBSituacaoDicInfo.CampoCodigo)}");
        }
        else
        {
            cWhere.Append((filtro.Codigo_filtro <= 0 && filtro.Codigo_filtro_end <= 0) ? string.Empty : (!(filtro.Codigo_filtro <= 0) && !(filtro.Codigo_filtro_end <= 0)) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBSituacaoDicInfo.CampoCodigo} BETWEEN @{nameof(DBSituacaoDicInfo.CampoCodigo)} AND @{nameof(DBSituacaoDicInfo.CampoCodigo)}_end" : !(filtro.Codigo_filtro <= 0) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBSituacaoDicInfo.CampoCodigo} = @{nameof(DBSituacaoDicInfo.CampoCodigo)}" : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBSituacaoDicInfo.CampoCodigo} <= @{nameof(DBSituacaoDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }

    private static string ApplyWildCard(char wildcardChar, string value)
    {
        if (wildcardChar == '\0' || wildcardChar == ' ')
        {
            return value;
        }

        var result = $"{wildcardChar}{value.Replace(" ", wildcardChar.ToString())}{wildcardChar}";
        return result;
    }

    private async Task<IEnumerable<SituacaoResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter> parameters, string uri, CancellationToken token)
    {
        using var oCnn = Configuracoes.GetConnectionByUri(uri);
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.Listar(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}