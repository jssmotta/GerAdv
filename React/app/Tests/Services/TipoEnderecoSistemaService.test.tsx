// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoEnderecoSistemaService, TipoEnderecoSistemaValidator } from '@/app/GerAdv_TS/TipoEnderecoSistema/Services/TipoEnderecoSistema.service';
import { TipoEnderecoSistemaApi, TipoEnderecoSistemaApiError } from '@/app/GerAdv_TS/TipoEnderecoSistema/Apis/ApiTipoEnderecoSistema';
import { ITipoEnderecoSistema } from '@/app/GerAdv_TS/TipoEnderecoSistema/Interfaces/interface.TipoEnderecoSistema';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoEnderecoSistemaApi;

const sample: ITipoEnderecoSistema = { id: 1, nome: 'Teste' };

describe('TipoEnderecoSistemaService', () => {
  let service: TipoEnderecoSistemaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoEnderecoSistemaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoEnderecoSistemaValidator, 'validateTipoEnderecoSistema').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoEnderecoSistemaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoEnderecoSistemaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoEnderecoSistemaById lança TipoEnderecoSistemaApiError para id inválido', async () => {
    await expect(service.fetchTipoEnderecoSistemaById(0)).rejects.toBeInstanceOf(TipoEnderecoSistemaApiError);
  });

  it('saveTipoEnderecoSistema chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoEnderecoSistema(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoEnderecoSistema lança TipoEnderecoSistemaApiError quando validação falha', async () => {
    (TipoEnderecoSistemaValidator.validateTipoEnderecoSistema as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoEnderecoSistema(sample)).rejects.toBeInstanceOf(TipoEnderecoSistemaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoEnderecoSistema chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoEnderecoSistema(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoEnderecoSistema lança TipoEnderecoSistemaApiError para id inválido', async () => {
    await expect(service.deleteTipoEnderecoSistema(0)).rejects.toBeInstanceOf(TipoEnderecoSistemaApiError);
  });

  it('validateTipoEnderecoSistema delega para TipoEnderecoSistemaValidator', () => {
    (TipoEnderecoSistemaValidator.validateTipoEnderecoSistema as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoEnderecoSistema(sample);
    expect(r.isValid).toBe(true);
  });
});
