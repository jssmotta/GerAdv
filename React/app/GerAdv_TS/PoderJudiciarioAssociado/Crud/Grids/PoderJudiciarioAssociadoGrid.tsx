// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { PoderJudiciarioAssociadoEmpty } from '../../../Models/PoderJudiciarioAssociado';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IPoderJudiciarioAssociado } from '../../Interfaces/interface.PoderJudiciarioAssociado';
import { PoderJudiciarioAssociadoService } from '../../Services/PoderJudiciarioAssociado.service';
import { PoderJudiciarioAssociadoApi } from '../../Apis/ApiPoderJudiciarioAssociado';
import { PoderJudiciarioAssociadoGridMobileComponent } from '../GridsMobile/PoderJudiciarioAssociadoMobile';
import { PoderJudiciarioAssociadoGridDesktopComponent } from '../GridsDesktop/PoderJudiciarioAssociadoDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterPoderJudiciarioAssociado } from '../../Filters/PoderJudiciarioAssociado';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import PoderJudiciarioAssociadoWindow from './PoderJudiciarioAssociadoWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { usePoderJudiciarioAssociadoList } from '../../Hooks/hookPoderJudiciarioAssociado';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { usePoderJudiciarioAssociadoFilter } from '../../Hooks/hookPoderJudiciarioAssociadoFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const PoderJudiciarioAssociadoGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedPoderJudiciarioAssociado, setSelectedPoderJudiciarioAssociado] = useState<IPoderJudiciarioAssociado>(PoderJudiciarioAssociadoEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterPoderJudiciarioAssociado | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [poderjudiciarioassociadoData, setPoderJudiciarioAssociadoData] = useState<IPoderJudiciarioAssociado[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const poderjudiciarioassociadoService = useMemo(() => {
    return new PoderJudiciarioAssociadoService(
    new PoderJudiciarioAssociadoApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: poderjudiciarioassociado, 
  fetchData, 
  refreshData
} = usePoderJudiciarioAssociadoList(poderjudiciarioassociadoService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterPoderJudiciarioAssociado | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('PoderJudiciarioAssociadoFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await poderjudiciarioassociadoService.getAll(
  filtro ?? {} as FilterPoderJudiciarioAssociado, 
  onlineData => {
    setPoderJudiciarioAssociadoData(onlineData);
    setLoading(false);
  }
);
setPoderJudiciarioAssociadoData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterPoderJudiciarioAssociado;

    const filtroMem = sessionStorage.getItem(btoa('PoderJudiciarioAssociadoFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('PoderJudiciarioAssociadoFilter')) ?? '') || '{}') : {} as FilterPoderJudiciarioAssociado;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (poderjudiciarioassociado: IPoderJudiciarioAssociado) => {
    setSelectedPoderJudiciarioAssociado(poderjudiciarioassociado);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedPoderJudiciarioAssociado(PoderJudiciarioAssociadoEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const poderjudiciarioassociado = e.dataItem;
    setDeleteId(poderjudiciarioassociado.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await poderjudiciarioassociadoService.deletePoderJudiciarioAssociado(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = usePoderJudiciarioAssociadoFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <PoderJudiciarioAssociadoGridMobileComponent
        data={poderjudiciarioassociadoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <PoderJudiciarioAssociadoGridDesktopComponent
        data={poderjudiciarioassociadoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <PoderJudiciarioAssociadoWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedPoderJudiciarioAssociado={selectedPoderJudiciarioAssociado}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Poder Judiciario Associado'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default PoderJudiciarioAssociadoGrid;