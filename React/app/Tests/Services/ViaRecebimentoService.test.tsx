// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ViaRecebimentoService, ViaRecebimentoValidator } from '@/app/GerAdv_TS/ViaRecebimento/Services/ViaRecebimento.service';
import { ViaRecebimentoApi, ViaRecebimentoApiError } from '@/app/GerAdv_TS/ViaRecebimento/Apis/ApiViaRecebimento';
import { IViaRecebimento } from '@/app/GerAdv_TS/ViaRecebimento/Interfaces/interface.ViaRecebimento';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ViaRecebimentoApi;

const sample: IViaRecebimento = { id: 1, nome: 'Teste' };

describe('ViaRecebimentoService', () => {
  let service: ViaRecebimentoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ViaRecebimentoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ViaRecebimentoValidator, 'validateViaRecebimento').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchViaRecebimentoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchViaRecebimentoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchViaRecebimentoById lança ViaRecebimentoApiError para id inválido', async () => {
    await expect(service.fetchViaRecebimentoById(0)).rejects.toBeInstanceOf(ViaRecebimentoApiError);
  });

  it('saveViaRecebimento chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveViaRecebimento(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveViaRecebimento lança ViaRecebimentoApiError quando validação falha', async () => {
    (ViaRecebimentoValidator.validateViaRecebimento as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveViaRecebimento(sample)).rejects.toBeInstanceOf(ViaRecebimentoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteViaRecebimento chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteViaRecebimento(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteViaRecebimento lança ViaRecebimentoApiError para id inválido', async () => {
    await expect(service.deleteViaRecebimento(0)).rejects.toBeInstanceOf(ViaRecebimentoApiError);
  });

  it('validateViaRecebimento delega para ViaRecebimentoValidator', () => {
    (ViaRecebimentoValidator.validateViaRecebimento as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateViaRecebimento(sample);
    expect(r.isValid).toBe(true);
  });
});
