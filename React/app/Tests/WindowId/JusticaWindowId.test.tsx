// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import JusticaWindowId from '@/app/GerAdv_TS/Justica/Crud/Grids/JusticaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import JusticaWindowId from '@/app/GerAdv_TS/Justica/Crud/Grids/JusticaWindowId';
import { JusticaEmpty } from '@/app/GerAdv_TS/Models/Justica';

describe('JusticaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(JusticaWindowId).toBeDefined();
    expect(typeof JusticaWindowId === 'function' || typeof JusticaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the JusticaService class so constructor usage doesn't throw and we can control fetchJusticaById
const mockFetchJusticaById = jest.fn();
jest.mock('@/app/GerAdv_TS/Justica/Services/Justica.service', () => ({
  JusticaService: jest.fn().mockImplementation(() => ({
    fetchJusticaById: (...args: any[]) => mockFetchJusticaById(...args),
  })),
}));

// Mock the child JusticaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Justica/Crud/Grids/JusticaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='justica-window' {...p} />,
}));

describe('JusticaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(JusticaWindowId).toBeDefined();
    expect(typeof JusticaWindowId === 'function' || typeof JusticaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <JusticaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders JusticaWindow with JusticaEmpty when id is 0', async () => {
    render(
      <JusticaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // JusticaWindow should be rendered with selectedJustica equal to JusticaEmpty()
    const node = await screen.findByTestId('justica-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedJustica"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedjustica') || node.getAttribute('selectedJustica')).not.toBeNull();
  });

  it('fetches justica by id and passes it to JusticaWindow when id > 0', async () => {
    const fakeJustica = { id: 123, nomecliente: 'Test' };
    mockFetchJusticaById.mockResolvedValue(fakeJustica);

    render(
      <JusticaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('justica-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchJusticaById).toHaveBeenCalledWith(123));
  });
});
