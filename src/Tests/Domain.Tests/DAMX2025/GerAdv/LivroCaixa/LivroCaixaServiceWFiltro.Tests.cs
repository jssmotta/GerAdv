// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:LivroCaixa
// Fonte:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class LivroCaixaServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFLivroCaixaFactory> _mockLivroCaixaFactory;
    private readonly Mock<ILivroCaixaReader> _mockReader;
    private readonly Mock<ILivroCaixaValidation> _mockValidation;
    private readonly Mock<ILivroCaixaWriter> _mockWriter;
    private readonly Mock<IHttpContextAccessor> _mockHttpContextAccessor;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IGenericVoiceFilterService<Filters.FilterLivroCaixa>> _mockVoiceFilterService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly LivroCaixaService _service;
    private readonly ServiceFilter _serviceS;
    public LivroCaixaServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockLivroCaixaFactory = new Mock<IFLivroCaixaFactory>();
        _mockReader = new Mock<ILivroCaixaReader>();
        _mockValidation = new Mock<ILivroCaixaValidation>();
        _mockWriter = new Mock<ILivroCaixaWriter>();
        _mockHttpContextAccessor = new Mock<IHttpContextAccessor>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockVoiceFilterService = new Mock<IGenericVoiceFilterService<Filters.FilterLivroCaixa>>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new LivroCaixaService(_mockAppSettings.Object, _mockLivroCaixaFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _mockVoiceFilterService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterLivroCaixa? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterLivroCaixa(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Previsto = 2,
            QuemCad = 1,
            DtCad = "24/04/1975",
            QuemAtu = 1,
            DtAtu = "24/04/1975",
            Visto = 1,
            IDDes = 1,
            Pessoal = 1,
            Ajuste = 2,
            IDHon = 1,
            IDHonParc = 1,
            IDHonSuc = 1,
            Data = "24/04/1975",
            Processo = 1,
            Valor = 1m,
            Tipo = 2,
            Historico = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Grupo = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Historico_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string HistoricoComWhitespace)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = HistoricoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var HistoricoGigante = new string ('A', 1000);
        var filtro = new FilterLivroCaixa
        {
            Historico = HistoricoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{HistoricoGigante}%");
    }

    [Fact]
    public void WFiltro_Historico_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var HistoricoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterLivroCaixa
        {
            Historico = HistoricoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Historico_ComCaracteresEspeciais_DeveProcessarCorretamente(string HistoricoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = HistoricoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(HistoricoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_HistoricoLivroCaixa_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_HistoricoLivroCaixa_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_HistoricoLivroCaixa_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_HistoricoLivroCaixa_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Historico_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Historico_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Historico}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Historico_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Historico_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region QuemCad Filter Tests
    [Fact]
    public void WFiltro_QuemCadEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            QuemCad = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.QuemCad));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.QuemCad);
    }

    [Fact]
    public void WFiltro_QuemCadHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemCadValue = 1;
        var filtro = new FilterLivroCaixa
        {
            QuemCad = QuemCadValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemCadParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemCad}");
        QuemCadParameter.Should().NotBeNull();
        QuemCadParameter.Value.Should().Be(QuemCadValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemCad}] = @{DBLivroCaixaDicInfo.QuemCad}");
    }

    [Fact]
    public void WFiltro_QuemCadWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemCad = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            QuemCad = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemCad}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemCad = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            QuemCad = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemCad}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemCadAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemCad}] = @{DBLivroCaixaDicInfo.QuemCad}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region QuemAtu Filter Tests
    [Fact]
    public void WFiltro_QuemAtuEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            QuemAtu = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.QuemAtu));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.QuemAtu);
    }

    [Fact]
    public void WFiltro_QuemAtuHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemAtuValue = 1;
        var filtro = new FilterLivroCaixa
        {
            QuemAtu = QuemAtuValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemAtuParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemAtu}");
        QuemAtuParameter.Should().NotBeNull();
        QuemAtuParameter.Value.Should().Be(QuemAtuValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemAtu}] = @{DBLivroCaixaDicInfo.QuemAtu}");
    }

    [Fact]
    public void WFiltro_QuemAtuWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemAtu = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            QuemAtu = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemAtu}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemAtu = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            QuemAtu = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.QuemAtu}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemAtuAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.QuemAtu}] = @{DBLivroCaixaDicInfo.QuemAtu}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDDes Filter Tests
    [Fact]
    public void WFiltro_IDDesEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDDes = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.IDDes));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.IDDes);
    }

    [Fact]
    public void WFiltro_IDDesHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDDesValue = 1;
        var filtro = new FilterLivroCaixa
        {
            IDDes = IDDesValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDDesParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDDes}");
        IDDesParameter.Should().NotBeNull();
        IDDesParameter.Value.Should().Be(IDDesValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDDes}] = @{DBLivroCaixaDicInfo.IDDes}");
    }

    [Fact]
    public void WFiltro_IDDesWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDDes = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            IDDes = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDDes}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDDes = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            IDDes = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDDes}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDDesAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDDes = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDDes}] = @{DBLivroCaixaDicInfo.IDDes}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDDesWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDDes = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDDesWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDDes = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Pessoal Filter Tests
    [Fact]
    public void WFiltro_PessoalEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Pessoal = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Pessoal));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Pessoal);
    }

    [Fact]
    public void WFiltro_PessoalHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PessoalValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Pessoal = PessoalValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PessoalParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Pessoal}");
        PessoalParameter.Should().NotBeNull();
        PessoalParameter.Value.Should().Be(PessoalValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Pessoal}] = @{DBLivroCaixaDicInfo.Pessoal}");
    }

    [Fact]
    public void WFiltro_PessoalWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Pessoal = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Pessoal = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Pessoal}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Pessoal = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Pessoal = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Pessoal}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_PessoalAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Pessoal = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Pessoal}] = @{DBLivroCaixaDicInfo.Pessoal}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_PessoalWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Pessoal = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_PessoalWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Pessoal = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDHon Filter Tests
    [Fact]
    public void WFiltro_IDHonEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHon = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.IDHon));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.IDHon);
    }

    [Fact]
    public void WFiltro_IDHonHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDHonValue = 1;
        var filtro = new FilterLivroCaixa
        {
            IDHon = IDHonValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDHonParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHon}");
        IDHonParameter.Should().NotBeNull();
        IDHonParameter.Value.Should().Be(IDHonValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHon}] = @{DBLivroCaixaDicInfo.IDHon}");
    }

    [Fact]
    public void WFiltro_IDHonWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDHon = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            IDHon = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHon}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDHon = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            IDHon = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHon}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDHonAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHon = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHon}] = @{DBLivroCaixaDicInfo.IDHon}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDHonWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHon = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDHonWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHon = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDHonParc Filter Tests
    [Fact]
    public void WFiltro_IDHonParcEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHonParc = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.IDHonParc));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.IDHonParc);
    }

    [Fact]
    public void WFiltro_IDHonParcHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDHonParcValue = 1;
        var filtro = new FilterLivroCaixa
        {
            IDHonParc = IDHonParcValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDHonParcParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonParc}");
        IDHonParcParameter.Should().NotBeNull();
        IDHonParcParameter.Value.Should().Be(IDHonParcValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonParc}] = @{DBLivroCaixaDicInfo.IDHonParc}");
    }

    [Fact]
    public void WFiltro_IDHonParcWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDHonParc = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            IDHonParc = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonParc}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDHonParc = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            IDHonParc = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonParc}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDHonParcAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHonParc = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonParc}] = @{DBLivroCaixaDicInfo.IDHonParc}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDHonParcWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHonParc = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDHonParcWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHonParc = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Processo Filter Tests
    [Fact]
    public void WFiltro_ProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Processo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Processo));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Processo);
    }

    [Fact]
    public void WFiltro_ProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ProcessoValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Processo = ProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Processo}");
        ProcessoParameter.Should().NotBeNull();
        ProcessoParameter.Value.Should().Be(ProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Processo}] = @{DBLivroCaixaDicInfo.Processo}");
    }

    [Fact]
    public void WFiltro_ProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Processo = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Processo = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Processo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Processo = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Processo = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Processo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Processo}] = @{DBLivroCaixaDicInfo.Processo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Grupo Filter Tests
    [Fact]
    public void WFiltro_GrupoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Grupo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Grupo));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Grupo);
    }

    [Fact]
    public void WFiltro_GrupoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var GrupoValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Grupo = GrupoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var GrupoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Grupo}");
        GrupoParameter.Should().NotBeNull();
        GrupoParameter.Value.Should().Be(GrupoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Grupo}] = @{DBLivroCaixaDicInfo.Grupo}");
    }

    [Fact]
    public void WFiltro_GrupoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Grupo = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Grupo = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Grupo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Grupo = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Grupo = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Grupo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_GrupoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Grupo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Grupo}] = @{DBLivroCaixaDicInfo.Grupo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_GrupoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Grupo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_GrupoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Grupo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#region Valor Filter Tests
    [Fact]
    public void WFiltro_Valor_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Valor = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Valor));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Valor);
    }

    [Fact]
    public void WFiltro_Valor_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorValue = 123.45m;
        var filtro = new FilterLivroCaixa
        {
            Valor = ValorValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Valor}");
        ValorParameter.Should().NotBeNull();
        ValorParameter.Value.Should().Be(ValorValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Valor}] = @{DBLivroCaixaDicInfo.Valor}");
    }

    [Fact]
    public void WFiltro_Valor_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Valor = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Valor}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_Valor_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Valor = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Valor}]");
    }

    [Fact]
    public void WFiltro_Valor_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Valor = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_Valor_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Valor = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#endregion
#region DateTime Tests
#region DtCad Filter Tests
    [Fact]
    public void WFiltro_DtCad_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.DtCad}], 103)");
    }

    [Fact]
    public void WFiltro_DtCad_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.DtCad));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBLivroCaixaDicInfo.DtCad);
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtCad = startDate,
            DtCad_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtCad_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtCad = string.Empty,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtCad = null,
            DtCad_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterLivroCaixa
        {
            DtCad = minValueDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtCad_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtCad = inputDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DtAtu Filter Tests
    [Fact]
    public void WFiltro_DtAtu_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.DtAtu}], 103)");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.DtAtu));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBLivroCaixaDicInfo.DtAtu);
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterLivroCaixa
        {
            DtAtu = startDate,
            DtAtu_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtAtu = string.Empty,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtAtu = null,
            DtAtu_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterLivroCaixa
        {
            DtAtu = minValueDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtAtu_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            DtAtu = inputDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region Data Filter Tests
    [Fact]
    public void WFiltro_Data_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Data}], 103)");
    }

    [Fact]
    public void WFiltro_Data_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterLivroCaixa
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Data));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBLivroCaixaDicInfo.Data);
    }

    [Fact]
    public void WFiltro_Data_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterLivroCaixa
        {
            Data = startDate,
            Data_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_Data_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Data = string.Empty,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Data = null,
            Data_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterLivroCaixa
        {
            Data = minValueDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_Data_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Data = inputDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#endregion
#region Bool Tests
#region Previsto Filter Tests
    [Fact]
    public void WFiltro_PrevistoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Previsto = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Previsto));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Previsto);
    }

    [Fact]
    public void WFiltro_PrevistoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PrevistoValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Previsto = PrevistoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PrevistoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Previsto}");
        PrevistoParameter.Should().NotBeNull();
        PrevistoParameter.Value.Should().Be(PrevistoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Previsto}] = @{DBLivroCaixaDicInfo.Previsto}");
    }

    [Fact]
    public void WFiltro_PrevistoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Previsto = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Previsto = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Previsto}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Previsto = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Previsto = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Previsto}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_PrevistoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Previsto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Previsto}] = @{DBLivroCaixaDicInfo.Previsto}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_PrevistoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Previsto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_PrevistoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Previsto = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Visto Filter Tests
    [Fact]
    public void WFiltro_VistoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Visto = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Visto));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Visto);
    }

    [Fact]
    public void WFiltro_VistoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var VistoValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Visto = VistoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var VistoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Visto}");
        VistoParameter.Should().NotBeNull();
        VistoParameter.Value.Should().Be(VistoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Visto}] = @{DBLivroCaixaDicInfo.Visto}");
    }

    [Fact]
    public void WFiltro_VistoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Visto = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Visto = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Visto}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Visto = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Visto = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Visto}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_VistoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Visto}] = @{DBLivroCaixaDicInfo.Visto}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Ajuste Filter Tests
    [Fact]
    public void WFiltro_AjusteEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Ajuste = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Ajuste));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Ajuste);
    }

    [Fact]
    public void WFiltro_AjusteHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var AjusteValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Ajuste = AjusteValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var AjusteParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Ajuste}");
        AjusteParameter.Should().NotBeNull();
        AjusteParameter.Value.Should().Be(AjusteValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Ajuste}] = @{DBLivroCaixaDicInfo.Ajuste}");
    }

    [Fact]
    public void WFiltro_AjusteWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Ajuste = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Ajuste = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Ajuste}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Ajuste = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Ajuste = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Ajuste}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_AjusteAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Ajuste = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Ajuste}] = @{DBLivroCaixaDicInfo.Ajuste}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_AjusteWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Ajuste = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_AjusteWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Ajuste = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDHonSuc Filter Tests
    [Fact]
    public void WFiltro_IDHonSucEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHonSuc = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.IDHonSuc));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.IDHonSuc);
    }

    [Fact]
    public void WFiltro_IDHonSucHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDHonSucValue = 1;
        var filtro = new FilterLivroCaixa
        {
            IDHonSuc = IDHonSucValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDHonSucParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonSuc}");
        IDHonSucParameter.Should().NotBeNull();
        IDHonSucParameter.Value.Should().Be(IDHonSucValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonSuc}] = @{DBLivroCaixaDicInfo.IDHonSuc}");
    }

    [Fact]
    public void WFiltro_IDHonSucWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDHonSuc = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            IDHonSuc = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonSuc}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDHonSuc = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            IDHonSuc = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.IDHonSuc}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDHonSucAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            IDHonSuc = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.IDHonSuc}] = @{DBLivroCaixaDicInfo.IDHonSuc}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDHonSucWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHonSuc = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDHonSucWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            IDHonSuc = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Tipo Filter Tests
    [Fact]
    public void WFiltro_TipoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Tipo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBLivroCaixaDicInfo.Tipo));
        result.Value.where.Should().NotContain(DBLivroCaixaDicInfo.Tipo);
    }

    [Fact]
    public void WFiltro_TipoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var TipoValue = 1;
        var filtro = new FilterLivroCaixa
        {
            Tipo = TipoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var TipoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Tipo}");
        TipoParameter.Should().NotBeNull();
        TipoParameter.Value.Should().Be(TipoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Tipo}] = @{DBLivroCaixaDicInfo.Tipo}");
    }

    [Fact]
    public void WFiltro_TipoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Tipo = 0 (typically female)
        var filtroFemale = new FilterLivroCaixa
        {
            Tipo = 0
        };
        var resultFemale = _serviceS.WFiltroLivroCaixa(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Tipo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Tipo = 1 (typically male)
        var filtroMale = new FilterLivroCaixa
        {
            Tipo = 1
        };
        var resultMale = _serviceS.WFiltroLivroCaixa(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBLivroCaixaDicInfo.Tipo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_TipoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Tipo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBLivroCaixaDicInfo.PTabelaNome}].[{DBLivroCaixaDicInfo.Tipo}] = @{DBLivroCaixaDicInfo.Tipo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_TipoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Tipo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_TipoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterLivroCaixa
        {
            Historico = "Historico", // Campo string para testar combinação
            Tipo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroLivroCaixa(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}