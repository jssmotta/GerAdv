// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { CRUD_CONSTANTS } from '@/app/tools/crud';
import { ProSucumbenciaApi, ProSucumbenciaApiError } from '../Apis/ApiProSucumbencia';
import { FilterProSucumbencia } from '../Filters/ProSucumbencia';
import { IProSucumbencia } from '../Interfaces/interface.ProSucumbencia';
import { ProSucumbenciaEmpty } from '../../Models/ProSucumbencia';

export class ProSucumbenciaValidator {
  static validateProSucumbencia(prosucumbencia: IProSucumbencia): { isValid: boolean; errors: string[] } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes
    
    return {
      isValid: errors.length === 0,
      errors
    };
  }
}

export interface IProSucumbenciaService {
  fetchProSucumbenciaById: (id: number) => Promise<IProSucumbencia>;
  saveProSucumbencia: (prosucumbencia: IProSucumbencia) => Promise<IProSucumbencia>;  
  getList: (filtro?: FilterProSucumbencia) => Promise<IProSucumbencia[]>;
  getAll: (filtro?: FilterProSucumbencia) => Promise<IProSucumbencia[]>;
  deleteProSucumbencia: (id: number) => Promise<void>;
  validateProSucumbencia: (prosucumbencia: IProSucumbencia) => { isValid: boolean; errors: string[] };
}

export class ProSucumbenciaService implements IProSucumbenciaService {
  constructor(private api: ProSucumbenciaApi) {}

  async fetchProSucumbenciaById(id: number): Promise<IProSucumbencia> {
    if (id <= 0) {
      throw new ProSucumbenciaApiError('ID inválido', 400, 'INVALID_ID');
    }

    try {
      
      const response = await this.api.getById(id);
      return response.data;

    } catch (error) {
      if (error instanceof ProSucumbenciaApiError) {
        throw error;
      }
      throw new ProSucumbenciaApiError('Erro ao buscar prosucumbencia', 500, 'FETCH_ERROR', error);
    }
  }

  async saveProSucumbencia(prosucumbencia: IProSucumbencia): Promise<IProSucumbencia> {    
    const validation = this.validateProSucumbencia(prosucumbencia);
    if (!validation.isValid) {
      throw new ProSucumbenciaApiError(
        `Dados inválidos: ${validation.errors.join(', ')}`,
        400,
        'VALIDATION_ERROR'
      );
    }

    try {
      const response = await this.api.addAndUpdate(prosucumbencia);
      return response.data;
    } catch (error) {
      if (error instanceof ProSucumbenciaApiError) {
        throw error;
      }
      throw new ProSucumbenciaApiError('Erro ao salvar prosucumbencia', 500, 'SAVE_ERROR', error);
    }
  }

  
    async getList(filtro?: FilterProSucumbencia): Promise<IProSucumbencia[]> {
    try {
      const response = await this.api.getListN(CRUD_CONSTANTS.MAX_RECORDS_COMBO, filtro);
      return response.data || [];
    } catch (error) {
      console.log('Error fetching ProSucumbencia list');
      return [];
    }
  }

 
  

   async getAll(
    filtro?: FilterProSucumbencia,
    onOnlineData?: (data: IProSucumbencia[]) => void
  ): Promise<IProSucumbencia[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(filtro ?? {});
      const offlineData = preloadResponse?.data || [];

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api.filter(filtro ?? {})
          .then(response => {
            if (response?.data) {
              onOnlineData(response.data);
            }
          })
          .catch(error => {
            console.log('Error fetching online ProSucumbencia');
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(filtro ?? {});
          return onlineResponse?.data || offlineData;
        } catch (error) {
          console.log('Error fetching online ProSucumbencia');
          return offlineData;
        }
      }
    } catch (error) {
      console.log('Error fetching all ProSucumbencia:');
      return [];
    }
  }


  async deleteProSucumbencia(id: number): Promise<void> {
    if (id <= 0) {
      throw new ProSucumbenciaApiError('ID inválido para exclusão', 400, 'INVALID_ID');
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof ProSucumbenciaApiError) {
        throw error;
      }
      throw new ProSucumbenciaApiError('Erro ao excluir prosucumbencia', 500, 'DELETE_ERROR', error);
    }
  }

  validateProSucumbencia(prosucumbencia: IProSucumbencia): { isValid: boolean; errors: string[] } {
    return ProSucumbenciaValidator.validateProSucumbencia(prosucumbencia);
  }
}