// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Validations;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial interface IAlertasEnviadosValidation
{
    Task<bool> ValidateReg(Models.AlertasEnviados reg, IAlertasEnviadosService service, IOperadorReader operadorReader, IAlertasReader alertasReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
    Task<bool> CanDelete(int id, IAlertasEnviadosService service, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
}

public class AlertasEnviadosValidation : IAlertasEnviadosValidation
{
    public async Task<bool> CanDelete(int id, IAlertasEnviadosService service, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (id <= 0)
            throw new SGValidationException("Id inválido");
        var reg = await service.GetById(id, uri, default);
        if (reg == null)
            throw new SGValidationException($"Registro com id {id} não encontrado.");
        return true;
    }

    private bool ValidSizes(Models.AlertasEnviados reg)
    {
        return true;
    }

    public async Task<bool> ValidateReg(Models.AlertasEnviados reg, IAlertasEnviadosService service, IOperadorReader operadorReader, IAlertasReader alertasReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (reg == null)
            throw new SGValidationException("Objeto está nulo");
        var validSizes = ValidSizes(reg);
        if (!validSizes)
            return false;
        if (reg.Operador == 0)
            throw new SGValidationException("Operador é obrigatório.");
        if (reg.Alerta == 0)
            throw new SGValidationException("Alerta é obrigatório.");
        // Operador
        {
            var regOperador = await operadorReader.Read(reg.Operador, oCnn);
            if (regOperador == null || regOperador.Id != reg.Operador)
            {
                throw new SGValidationException($"Operador não encontrado ({regOperador?.Id}).");
            }
        }

        // Alertas
        {
            var regAlertas = await alertasReader.Read(reg.Alerta, oCnn);
            if (regAlertas == null || regAlertas.Id != reg.Alerta)
            {
                throw new SGValidationException($"Alertas não encontrado ({regAlertas?.Id}).");
            }
        }

        return true;
    }
}