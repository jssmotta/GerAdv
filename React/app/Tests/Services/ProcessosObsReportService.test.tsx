// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProcessosObsReportService, ProcessosObsReportValidator } from '@/app/GerAdv_TS/ProcessosObsReport/Services/ProcessosObsReport.service';
import { ProcessosObsReportApi, ProcessosObsReportApiError } from '@/app/GerAdv_TS/ProcessosObsReport/Apis/ApiProcessosObsReport';
import { IProcessosObsReport } from '@/app/GerAdv_TS/ProcessosObsReport/Interfaces/interface.ProcessosObsReport';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProcessosObsReportApi;

const sample: IProcessosObsReport = { id: 1, nome: 'Teste' };

describe('ProcessosObsReportService', () => {
  let service: ProcessosObsReportService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProcessosObsReportService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProcessosObsReportValidator, 'validateProcessosObsReport').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProcessosObsReportById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProcessosObsReportById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProcessosObsReportById lança ProcessosObsReportApiError para id inválido', async () => {
    await expect(service.fetchProcessosObsReportById(0)).rejects.toBeInstanceOf(ProcessosObsReportApiError);
  });

  it('saveProcessosObsReport chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProcessosObsReport(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProcessosObsReport lança ProcessosObsReportApiError quando validação falha', async () => {
    (ProcessosObsReportValidator.validateProcessosObsReport as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProcessosObsReport(sample)).rejects.toBeInstanceOf(ProcessosObsReportApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProcessosObsReport chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProcessosObsReport(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProcessosObsReport lança ProcessosObsReportApiError para id inválido', async () => {
    await expect(service.deleteProcessosObsReport(0)).rejects.toBeInstanceOf(ProcessosObsReportApiError);
  });

  it('validateProcessosObsReport delega para ProcessosObsReportValidator', () => {
    (ProcessosObsReportValidator.validateProcessosObsReport as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProcessosObsReport(sample);
    expect(r.isValid).toBe(true);
  });
});
