// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import RegimeTributacaoWindowId from '@/app/GerAdv_TS/RegimeTributacao/Crud/Grids/RegimeTributacaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import RegimeTributacaoWindowId from '@/app/GerAdv_TS/RegimeTributacao/Crud/Grids/RegimeTributacaoWindowId';
import { RegimeTributacaoEmpty } from '@/app/GerAdv_TS/Models/RegimeTributacao';

describe('RegimeTributacaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(RegimeTributacaoWindowId).toBeDefined();
    expect(typeof RegimeTributacaoWindowId === 'function' || typeof RegimeTributacaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the RegimeTributacaoService class so constructor usage doesn't throw and we can control fetchRegimeTributacaoById
const mockFetchRegimeTributacaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/RegimeTributacao/Services/RegimeTributacao.service', () => ({
  RegimeTributacaoService: jest.fn().mockImplementation(() => ({
    fetchRegimeTributacaoById: (...args: any[]) => mockFetchRegimeTributacaoById(...args),
  })),
}));

// Mock the child RegimeTributacaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/RegimeTributacao/Crud/Grids/RegimeTributacaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='regimetributacao-window' {...p} />,
}));

describe('RegimeTributacaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(RegimeTributacaoWindowId).toBeDefined();
    expect(typeof RegimeTributacaoWindowId === 'function' || typeof RegimeTributacaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <RegimeTributacaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders RegimeTributacaoWindow with RegimeTributacaoEmpty when id is 0', async () => {
    render(
      <RegimeTributacaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // RegimeTributacaoWindow should be rendered with selectedRegimeTributacao equal to RegimeTributacaoEmpty()
    const node = await screen.findByTestId('regimetributacao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedRegimeTributacao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedregimetributacao') || node.getAttribute('selectedRegimeTributacao')).not.toBeNull();
  });

  it('fetches regimetributacao by id and passes it to RegimeTributacaoWindow when id > 0', async () => {
    const fakeRegimeTributacao = { id: 123, nomecliente: 'Test' };
    mockFetchRegimeTributacaoById.mockResolvedValue(fakeRegimeTributacao);

    render(
      <RegimeTributacaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('regimetributacao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchRegimeTributacaoById).toHaveBeenCalledWith(123));
  });
});
