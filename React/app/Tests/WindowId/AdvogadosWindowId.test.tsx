// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AdvogadosWindowId from '@/app/GerAdv_TS/Advogados/Crud/Grids/AdvogadosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AdvogadosWindowId from '@/app/GerAdv_TS/Advogados/Crud/Grids/AdvogadosWindowId';
import { AdvogadosEmpty } from '@/app/GerAdv_TS/Models/Advogados';

describe('AdvogadosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AdvogadosWindowId).toBeDefined();
    expect(typeof AdvogadosWindowId === 'function' || typeof AdvogadosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AdvogadosService class so constructor usage doesn't throw and we can control fetchAdvogadosById
const mockFetchAdvogadosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Advogados/Services/Advogados.service', () => ({
  AdvogadosService: jest.fn().mockImplementation(() => ({
    fetchAdvogadosById: (...args: any[]) => mockFetchAdvogadosById(...args),
  })),
}));

// Mock the child AdvogadosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Advogados/Crud/Grids/AdvogadosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='advogados-window' {...p} />,
}));

describe('AdvogadosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AdvogadosWindowId).toBeDefined();
    expect(typeof AdvogadosWindowId === 'function' || typeof AdvogadosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AdvogadosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AdvogadosWindow with AdvogadosEmpty when id is 0', async () => {
    render(
      <AdvogadosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AdvogadosWindow should be rendered with selectedAdvogados equal to AdvogadosEmpty()
    const node = await screen.findByTestId('advogados-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedAdvogados"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedadvogados') || node.getAttribute('selectedAdvogados')).not.toBeNull();
  });

  it('fetches advogados by id and passes it to AdvogadosWindow when id > 0', async () => {
    const fakeAdvogados = { id: 123, nomecliente: 'Test' };
    mockFetchAdvogadosById.mockResolvedValue(fakeAdvogados);

    render(
      <AdvogadosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('advogados-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAdvogadosById).toHaveBeenCalledWith(123));
  });
});
