// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useServicosForm, useServicosList, useValidationsServicos } from '../GerAdv_TS/Servicos/Hooks/hookServicos';
import { IServicos } from '../GerAdv_TS/Servicos/Interfaces/interface.Servicos';
import { IServicosService } from '../GerAdv_TS/Servicos/Services/Servicos.service';
import { ServicosTestEmpty } from '../GerAdv_TS/Models/Servicos';
import { useServicosComboBox } from '../GerAdv_TS/Servicos/Hooks/hookServicos';

// Mock do serviço
const mockServicosService: jest.Mocked<IServicosService> = {
  fetchServicosById: jest.fn(),
  saveServicos: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteServicos: jest.fn(),
  validateServicos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialServicos: IServicos = { ...ServicosTestEmpty() };

describe('useServicosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    expect(result.current.data).toEqual(initialServicos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Serviço',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Serviço');
  });

   test('deve carregar Serviço por ID', async () => {
    const mockServicos = { ...initialServicos, id: 1, descricao: 'Serviço Teste' };
    mockServicosService.fetchServicosById.mockResolvedValue(mockServicos);

    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    await act(async () => {
      await result.current.loadServicos(1);
    });

    expect(mockServicosService.fetchServicosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockServicos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Serviço', async () => {
    const errorMessage = 'Erro ao carregar Serviço';
    mockServicosService.fetchServicosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    await act(async () => {
      await result.current.loadServicos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialServicos, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialServicos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    await act(async () => {
      await result.current.loadServicos(0);
    });

    expect(mockServicosService.fetchServicosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialServicos);
  });
});

describe('useServicosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useServicosList(mockServicosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialServicos, id: 1, descricao: 'Serviço 1' },
      { ...initialServicos, id: 2, descricao: 'Serviço 2' }
    ];
    mockServicosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useServicosList(mockServicosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockServicosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockServicosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useServicosList(mockServicosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialServicos, id: 1, descricao: 'Serviço Filtrado' }];
    const filtro = { descricao: 'Serviço' };
    mockServicosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useServicosList(mockServicosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockServicosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsServicos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsServicos());

    const validData = { ...initialServicos, descricao: 'Serviço Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsServicos());

    const invalidData = { ...initialServicos, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsServicos());

    const invalidData = { 
      ...initialServicos, 
      descricao: 'a'.repeat(200+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 200 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsServicos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialServicos, id: 1, descricao: 'Serviço Teste' }];
    mockServicosService.getAll.mockResolvedValue(mockData);
    mockServicosService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useServicosList(mockServicosService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsServicos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Serviço Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Serviço Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useServicosForm(initialServicos, mockServicosService)
    );

    const mockEvent = {
      target: {
        name: 'cobrar',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.cobrar).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Serviço 1' },
      { id: 2, descricao: 'Serviço 2' }
    ];
    mockServicosService.getList.mockResolvedValue(mockOptions as IServicos[]);


    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Serviço 1' },
        { id: 2, nome: 'Serviço 2' }
      ]);
    });


    expect(mockServicosService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Serviço ABC' },
      { id: 2, descricao: 'Serviço XYZ' }
    ];
    mockServicosService.getList.mockResolvedValue(mockOptions as IServicos[]);   


 const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Serviço ABC' },
        { id: 2, nome: 'Serviço XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Serviço ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Serviço ABC' },
      { id: 2, descricao: 'Serviço XYZ' }
    ];
    mockServicosService.getList.mockResolvedValue(mockOptions as IServicos[]);
  


    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Serviço ABC' },
        { id: 2, nome: 'Serviço XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Serviço ABC' },
          {id: 2, nome: 'Serviço XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );

    const newValue = { id: 1, descricao: 'Serviço Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Serviço Inicial' };
    
    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useServicosComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Serviço Inicial' };
    
    const { result } = renderHook(() => 
      useServicosComboBox(mockServicosService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






