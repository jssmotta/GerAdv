// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Operador
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterOperadorTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterOperadorTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithEMail_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            EMail = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPasta_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Pasta = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pasta\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithNome_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Nome = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nome\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithNick_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Nick = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nick\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithRamal_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Ramal = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"ramal\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithComputador_SerializesNumericValue()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Computador = 123
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"computador\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithMinhaDescricao_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            MinhaDescricao = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"minhadescricao\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithUltimoLogoff_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            UltimoLogoff = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"ultimologoff\"");
    }

    [Fact]
    public void GetPrompt_WithEMailNet_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            EMailNet = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"emailnet\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithOnlineIP_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            OnlineIP = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"onlineip\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithStatusId_SerializesNumericValue()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            StatusId = 123
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"statusid\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithStatusMessage_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            StatusMessage = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"statusmessage\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDataLimiteReset_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            DataLimiteReset = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"datalimitereset\"");
    }

    [Fact]
    public void GetPrompt_WithSuporteMaxAge_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            SuporteMaxAge = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"suportemaxage\"");
    }

    [Fact]
    public void GetPrompt_WithSuporteNomeSolicitante_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            SuporteNomeSolicitante = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"suportenomesolicitante\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithSuporteUltimoAcesso_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            SuporteUltimoAcesso = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"suporteultimoacesso\"");
    }

    [Fact]
    public void GetPrompt_WithSuporteIpUltimoAcesso_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            SuporteIpUltimoAcesso = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"suporteipultimoacesso\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(new FilterOperador(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(new FilterOperador(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(new FilterOperador(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(new FilterOperador(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterOperadorTXT.GetOptions();
        var filter = new FilterOperador
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterOperadorTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}