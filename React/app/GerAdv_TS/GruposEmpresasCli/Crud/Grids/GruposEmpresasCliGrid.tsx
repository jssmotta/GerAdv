// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { GruposEmpresasCliEmpty } from '../../../Models/GruposEmpresasCli';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IGruposEmpresasCli } from '../../Interfaces/interface.GruposEmpresasCli';
import { GruposEmpresasCliService } from '../../Services/GruposEmpresasCli.service';
import { GruposEmpresasCliApi } from '../../Apis/ApiGruposEmpresasCli';
import { GruposEmpresasCliGridMobileComponent } from '../GridsMobile/GruposEmpresasCli';
import { GruposEmpresasCliGridDesktopComponent } from '../GridsDesktop/GruposEmpresasCli';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterGruposEmpresasCli } from '../../Filters/GruposEmpresasCli';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import GruposEmpresasCliWindow from './GruposEmpresasCliWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useGruposEmpresasCliList } from '../../Hooks/hookGruposEmpresasCli';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useGruposEmpresasCliFilter } from '../../Hooks/hookGruposEmpresasCliFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const GruposEmpresasCliGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedGruposEmpresasCli, setSelectedGruposEmpresasCli] = useState<IGruposEmpresasCli>(GruposEmpresasCliEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterGruposEmpresasCli | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [gruposempresascliData, setGruposEmpresasCliData] = useState<IGruposEmpresasCli[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const gruposempresascliService = useMemo(() => {
    return new GruposEmpresasCliService(
    new GruposEmpresasCliApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: gruposempresascli, 
  fetchData, 
  refreshData
} = useGruposEmpresasCliList(gruposempresascliService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterGruposEmpresasCli | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('GruposEmpresasCliFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await gruposempresascliService.getAll(
  filtro ?? {} as FilterGruposEmpresasCli, 
  onlineData => {
    setGruposEmpresasCliData(onlineData);
    setLoading(false);
  }
);
setGruposEmpresasCliData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterGruposEmpresasCli;

    const filtroMem = sessionStorage.getItem(btoa('GruposEmpresasCliFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('GruposEmpresasCliFilter')) ?? '') || '{}') : {} as FilterGruposEmpresasCli;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (gruposempresascli: IGruposEmpresasCli) => {
    setSelectedGruposEmpresasCli(gruposempresascli);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedGruposEmpresasCli(GruposEmpresasCliEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const gruposempresascli = e.dataItem;
    setDeleteId(gruposempresascli.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await gruposempresascliService.deleteGruposEmpresasCli(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useGruposEmpresasCliFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <GruposEmpresasCliGridMobileComponent
        data={gruposempresascliData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <GruposEmpresasCliGridDesktopComponent
        data={gruposempresascliData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <GruposEmpresasCliWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedGruposEmpresasCli={selectedGruposEmpresasCli}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Grupos Empresas Cli'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default GruposEmpresasCliGrid;