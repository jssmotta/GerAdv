// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProTipoBaixaWindowId from '@/app/GerAdv_TS/ProTipoBaixa/Crud/Grids/ProTipoBaixaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProTipoBaixaWindowId from '@/app/GerAdv_TS/ProTipoBaixa/Crud/Grids/ProTipoBaixaWindowId';
import { ProTipoBaixaEmpty } from '@/app/GerAdv_TS/Models/ProTipoBaixa';

describe('ProTipoBaixaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProTipoBaixaWindowId).toBeDefined();
    expect(typeof ProTipoBaixaWindowId === 'function' || typeof ProTipoBaixaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProTipoBaixaService class so constructor usage doesn't throw and we can control fetchProTipoBaixaById
const mockFetchProTipoBaixaById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProTipoBaixa/Services/ProTipoBaixa.service', () => ({
  ProTipoBaixaService: jest.fn().mockImplementation(() => ({
    fetchProTipoBaixaById: (...args: any[]) => mockFetchProTipoBaixaById(...args),
  })),
}));

// Mock the child ProTipoBaixaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProTipoBaixa/Crud/Grids/ProTipoBaixaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='protipobaixa-window' {...p} />,
}));

describe('ProTipoBaixaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProTipoBaixaWindowId).toBeDefined();
    expect(typeof ProTipoBaixaWindowId === 'function' || typeof ProTipoBaixaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProTipoBaixaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProTipoBaixaWindow with ProTipoBaixaEmpty when id is 0', async () => {
    render(
      <ProTipoBaixaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProTipoBaixaWindow should be rendered with selectedProTipoBaixa equal to ProTipoBaixaEmpty()
    const node = await screen.findByTestId('protipobaixa-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProTipoBaixa"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprotipobaixa') || node.getAttribute('selectedProTipoBaixa')).not.toBeNull();
  });

  it('fetches protipobaixa by id and passes it to ProTipoBaixaWindow when id > 0', async () => {
    const fakeProTipoBaixa = { id: 123, nomecliente: 'Test' };
    mockFetchProTipoBaixaById.mockResolvedValue(fakeProTipoBaixa);

    render(
      <ProTipoBaixaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('protipobaixa-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProTipoBaixaById).toHaveBeenCalledWith(123));
  });
});
