// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IForo } from "@/app/GerAdv_TS/Foro/Interfaces/interface.Foro";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { ForoApi } from "../../Apis/ForoApi";
import { useValidationsForo } from "../../Hooks/hookForo";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpForo from "../../Help/HelpForo";
import ForoPage from "@/app/pages/foro/page";

import CidadeComboBox from "@/app/GerAdv_TS/Cidade/ComboBox/CidadeComboBox";
import { CidadeApi } from "@/app/GerAdv_TS/Cidade/Apis/CidadeApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";
import InputCep from "@/app/components/Inputs/InputCep";

interface ForoFormProps {
  foroData: IForo;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const ForoForm: React.FC<ForoFormProps> = ({
  foroData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new ForoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsForo();

  const [_nomeCidade, setNomeCidade] = useState("");
  const cidadeApi = new CidadeApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("cidade") > 0) {
    if (foroData.id === 0 && foroData.cidade == 0) {
      cidadeApi
        .getById(getParamFromUrl("cidade"))
        .then((response) => {
          setNomeCidade(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      foroData.cidade = getParamFromUrl("cidade");
    }
  }
  const addValorCidade = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "cidade", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Foro:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`ForoForm-${foroData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Foro diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent = foroData?.id == 0 ? "Editar Foro" : "Adicionar Foro";
    }
  }, [foroData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Foro"
            : "form-container form-container-Foro"
        }
      >
        <form
          className="formInputCadInc"
          id={`ForoForm-${foroData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Foro"
            data={foroData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`ForoForm-${foroData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={foroData}
              className="input-default-main"
              name="nome"
              value={foroData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputInput
              tabIndex={1}
              type="email"
              maxLength={150}
              id="email"
              label="EMail"
              dataForm={foroData}
              className="input-default-main"
              name="email"
              value={foroData.email}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={foroData}
              label="Unico"
              name="unico"
              checked={foroData.unico}
              onChange={onChange}
            />

            <CidadeComboBox
              name={"cidade"}
              dataForm={foroData}
              value={foroData.cidade}
              setValue={addValorCidade}
              label={"Cidade"}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={150}
              id="site"
              label="Site"
              dataForm={foroData}
              className="input-default-main"
              name="site"
              value={foroData.site}
              onChange={onChange}
            />

            <InputInput
              tabIndex={3}
              type="text"
              maxLength={50}
              id="endereco"
              label="Endereco"
              dataForm={foroData}
              className="input-default-main"
              name="endereco"
              value={foroData.endereco}
              onChange={onChange}
            />

            <InputInput
              tabIndex={4}
              type="text"
              maxLength={255}
              id="bairro"
              label="Bairro"
              dataForm={foroData}
              className="input-default-main"
              name="bairro"
              value={foroData.bairro}
              onChange={onChange}
            />

            <InputInput
              tabIndex={5}
              type="text"
              maxLength={2147483647}
              id="fone"
              label="Fone"
              dataForm={foroData}
              className="input-default-main"
              name="fone"
              value={foroData.fone}
              onChange={onChange}
            />

            <InputInput
              tabIndex={6}
              type="text"
              maxLength={2147483647}
              id="fax"
              label="Fax"
              dataForm={foroData}
              className="input-default-main"
              name="fax"
              value={foroData.fax}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputCep
              tabIndex={7}
              type="text"
              id="cep"
              label="CEP"
              dataForm={foroData}
              className="input-default-main"
              name="cep"
              value={foroData.cep}
              onChange={onChange}
            />

            <InputInput
              tabIndex={8}
              type="text"
              maxLength={2147483647}
              id="obs"
              label="OBS"
              dataForm={foroData}
              className="input-default-main"
              name="obs"
              value={foroData.obs}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={foroData}
              label="UnicoConfirmado"
              name="unicoconfirmado"
              checked={foroData.unicoconfirmado}
              onChange={onChange}
            />

            <InputInput
              tabIndex={9}
              type="text"
              maxLength={255}
              id="web"
              label="Web"
              dataForm={foroData}
              className="input-default-main"
              name="web"
              value={foroData.web}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Foro" windowCrud={ForoPage} />

          <CopyButton formId={`ForoForm-${foroData.id}`} />

          <PrintButton formId={`ForoForm-${foroData.id}`} />

          <AuditorButton tableName="Foro" coolName="Foro" id={foroData.id} />

          <DeleteButton
            page={"/pages/foro"}
            id={foroData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Foro"
            page={(onClose) => <HelpForo onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Foro"
          data={foroData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`ForoForm-${foroData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
