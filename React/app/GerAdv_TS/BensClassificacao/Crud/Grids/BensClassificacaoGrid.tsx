// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { BensClassificacaoEmpty } from '../../../Models/BensClassificacao';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IBensClassificacao } from '../../Interfaces/interface.BensClassificacao';
import { BensClassificacaoService } from '../../Services/BensClassificacao.service';
import { BensClassificacaoApi } from '../../Apis/ApiBensClassificacao';
import { BensClassificacaoGridMobileComponent } from '../GridsMobile/BensClassificacaoMobile';
import { BensClassificacaoGridDesktopComponent } from '../GridsDesktop/BensClassificacaoDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterBensClassificacao } from '../../Filters/BensClassificacao';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import BensClassificacaoWindow from './BensClassificacaoWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useBensClassificacaoList } from '../../Hooks/hookBensClassificacao';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useBensClassificacaoFilter } from '../../Hooks/hookBensClassificacaoFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const BensClassificacaoGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedBensClassificacao, setSelectedBensClassificacao] = useState<IBensClassificacao>(BensClassificacaoEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterBensClassificacao | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [bensclassificacaoData, setBensClassificacaoData] = useState<IBensClassificacao[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const bensclassificacaoService = useMemo(() => {
    return new BensClassificacaoService(
    new BensClassificacaoApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: bensclassificacao, 
  fetchData, 
  refreshData
} = useBensClassificacaoList(bensclassificacaoService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterBensClassificacao | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('BensClassificacaoFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await bensclassificacaoService.getAll(
  filtro ?? {} as FilterBensClassificacao, 
  onlineData => {
    setBensClassificacaoData(onlineData);
    setLoading(false);
  }
);
setBensClassificacaoData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterBensClassificacao;

    const filtroMem = sessionStorage.getItem(btoa('BensClassificacaoFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('BensClassificacaoFilter')) ?? '') || '{}') : {} as FilterBensClassificacao;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (bensclassificacao: IBensClassificacao) => {
    setSelectedBensClassificacao(bensclassificacao);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedBensClassificacao(BensClassificacaoEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const bensclassificacao = e.dataItem;
    setDeleteId(bensclassificacao.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await bensclassificacaoService.deleteBensClassificacao(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useBensClassificacaoFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <BensClassificacaoGridMobileComponent
        data={bensclassificacaoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <BensClassificacaoGridDesktopComponent
        data={bensclassificacaoData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <BensClassificacaoWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedBensClassificacao={selectedBensClassificacao}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Classificações de Bens'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default BensClassificacaoGrid;