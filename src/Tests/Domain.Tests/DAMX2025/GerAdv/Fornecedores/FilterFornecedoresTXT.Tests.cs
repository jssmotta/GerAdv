// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Fornecedores
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterFornecedoresTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterFornecedoresTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithGrupo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Grupo = 123
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"grupo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithNome_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Nome = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nome\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithSubGrupo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            SubGrupo = 123
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"subgrupo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCNPJ_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            CNPJ = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cnpj\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithInscEst_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            InscEst = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"inscest\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCPF_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            CPF = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cpf\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithRG_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            RG = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"rg\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEndereco_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Endereco = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"endereco\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithBairro_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Bairro = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"bairro\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCEP_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            CEP = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cep\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCidade_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Cidade = 123
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cidade\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithFone_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Fone = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"fone\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithFax_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Fax = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"fax\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEmail_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Email = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithSite_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Site = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"site\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithObs_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Obs = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"obs\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithProdutos_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Produtos = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"produtos\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithContatos_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Contatos = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"contatos\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterCidade\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(new FilterFornecedores(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(new FilterFornecedores(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(new FilterFornecedores(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(new FilterFornecedores(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterFornecedoresTXT.GetOptions();
        var filter = new FilterFornecedores
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterFornecedoresTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}