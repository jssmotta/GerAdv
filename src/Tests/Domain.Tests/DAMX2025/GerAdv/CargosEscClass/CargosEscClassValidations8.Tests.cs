// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class CargosEscClassValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFCargosEscClassFactory> _mockCargosEscClassFactory;
    private readonly Mock<ICargosEscClassReader> _mockReader;
    private readonly CargosEscClassValidation _validation;
    private readonly Mock<ICargosEscClassService> _mockCargosEscClassService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly CargosEscClassService _service;
    private readonly string _validUri = "test-uri";
    public CargosEscClassValidationTests()
    {
        _mockCargosEscClassFactory = new Mock<IFCargosEscClassFactory>();
        _mockReader = new Mock<ICargosEscClassReader>();
        _validation = new CargosEscClassValidation();
        _mockCargosEscClassService = new Mock<ICargosEscClassService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void CargosEscClassValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ICargosEscClassValidation>();
    }

    [Fact]
    public void CargosEscClassValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(CargosEscClassValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void CargosEscClassValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(CargosEscClassValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.CargosEscClass CreateValidCargosEscClass()
    {
        return new Models.CargosEscClass
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockCargosEscClassService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargosEscClass>(), It.IsAny<string>())).ReturnsAsync(new List<CargosEscClassResponseAll>());
    // Setup other mocks but don't override the CargosEscClasss service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockCargosEscClassService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargosEscClass>(), It.IsAny<string>())).ReturnsAsync(new List<CargosEscClassResponseAll>());
    // Setup other mocks but don't override the CargosEscClasss service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var cargosescclass = new Models.CargosEscClass
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(cargosescclass, _mockCargosEscClassService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockCargosEscClassService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}