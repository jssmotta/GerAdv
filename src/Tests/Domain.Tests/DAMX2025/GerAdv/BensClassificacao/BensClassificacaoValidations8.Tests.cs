// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class BensClassificacaoValidationTests : IDisposable
{
    private readonly BensClassificacaoValidation _validation;
    private readonly Mock<IBensClassificacaoService> _mockBensClassificacaoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly BensClassificacaoService _service;
    private readonly string _validUri = "test-uri";
    public BensClassificacaoValidationTests()
    {
        _validation = new BensClassificacaoValidation();
        _mockBensClassificacaoService = new Mock<IBensClassificacaoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void BensClassificacaoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IBensClassificacaoValidation>();
    }

    [Fact]
    public void BensClassificacaoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(BensClassificacaoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void BensClassificacaoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(BensClassificacaoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.BensClassificacao CreateValidBensClassificacao()
    {
        return new Models.BensClassificacao
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockBensClassificacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterBensClassificacao>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the BensClassificacaos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockBensClassificacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterBensClassificacao>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the BensClassificacaos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var bensclassificacao = new Models.BensClassificacao
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(bensclassificacao, _mockBensClassificacaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockBensClassificacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}