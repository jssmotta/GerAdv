// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for StatusTarefas page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock StatusTarefasGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/StatusTarefas/Adapter/StatusTarefasGridAdapter', () => ({
  StatusTarefasGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock StatusTarefasGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/StatusTarefas/Components/StatusTarefasGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='statustarefas-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import StatusTarefasPage from '@/app/pages/statustarefas/page';

describe('StatusTarefas page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and StatusTarefasGridContainer', () => {
    render(<StatusTarefasPage />);

    expect(screen.getByRole('heading', { name: /Status Tarefas/i })).toBeInTheDocument();
    expect(screen.getByTestId('statustarefas-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

