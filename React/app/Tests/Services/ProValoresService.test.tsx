// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProValoresService, ProValoresValidator } from '@/app/GerAdv_TS/ProValores/Services/ProValores.service';
import { ProValoresApi, ProValoresApiError } from '@/app/GerAdv_TS/ProValores/Apis/ApiProValores';
import { IProValores } from '@/app/GerAdv_TS/ProValores/Interfaces/interface.ProValores';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProValoresApi;

const sample: IProValores = { id: 1, nome: 'Teste' };

describe('ProValoresService', () => {
  let service: ProValoresService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProValoresService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProValoresValidator, 'validateProValores').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProValoresById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProValoresById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProValoresById lança ProValoresApiError para id inválido', async () => {
    await expect(service.fetchProValoresById(0)).rejects.toBeInstanceOf(ProValoresApiError);
  });

  it('saveProValores chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProValores(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProValores lança ProValoresApiError quando validação falha', async () => {
    (ProValoresValidator.validateProValores as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProValores(sample)).rejects.toBeInstanceOf(ProValoresApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProValores chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProValores(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProValores lança ProValoresApiError para id inválido', async () => {
    await expect(service.deleteProValores(0)).rejects.toBeInstanceOf(ProValoresApiError);
  });

  it('validateProValores delega para ProValoresValidator', () => {
    (ProValoresValidator.validateProValores as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProValores(sample);
    expect(r.isValid).toBe(true);
  });
});
