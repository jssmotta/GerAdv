// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial class StatusBiuService
{
    private static (string where, List<SqlParameter> parametros)? WFiltro(Filters.FilterStatusBiu filtro)
    {
        var parameters = new List<SqlParameter>();
        if (!string.IsNullOrEmpty(filtro.Nome))
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.Nome)}", ApplyWildCard(filtro.WildcardChar, filtro.Nome)));
        }

        if (filtro.TipoStatusBiu != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.TipoStatusBiu)}", filtro.TipoStatusBiu));
        }

        if (filtro.Operador != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.Operador)}", filtro.Operador));
        }

        if (filtro.Icone != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.Icone)}", filtro.Icone));
        }

        if (filtro.Icone_end != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.Icone)}_end", filtro.Icone_end));
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
        }

        if (filtro.Codigo_filtro_end != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusBiuDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
        }

        if (filtro.LogicalOperator.IsEmpty() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var cWhere = new StringBuilder();
        cWhere.Append(filtro.Nome.IsEmpty() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusBiuDicInfo.PTabelaNome}].[{DBStatusBiuDicInfo.Nome}]  {DevourerConsts.MsiCollate} like @{nameof(DBStatusBiuDicInfo.Nome)}");
        cWhere.Append(filtro.TipoStatusBiu <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusBiuDicInfo.PTabelaNome}].[{DBStatusBiuDicInfo.TipoStatusBiu}] = @{nameof(DBStatusBiuDicInfo.TipoStatusBiu)}");
        cWhere.Append(filtro.Operador <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusBiuDicInfo.PTabelaNome}].[{DBStatusBiuDicInfo.Operador}] = @{nameof(DBStatusBiuDicInfo.Operador)}");
        if (!filtro.Icone.IsEmpty() && filtro.Icone_end.IsEmpty())
        {
            cWhere.Append(filtro.Icone <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusBiuDicInfo.PTabelaNome}].[{DBStatusBiuDicInfo.Icone}] >= @{nameof(DBStatusBiuDicInfo.Icone)}");
        }
        else
        {
            cWhere.Append((filtro.Icone <= 0 && filtro.Icone_end <= 0) ? string.Empty : (!(filtro.Icone <= 0) && !(filtro.Icone_end <= 0)) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.Icone} BETWEEN @{nameof(DBStatusBiuDicInfo.Icone)} AND @{nameof(DBStatusBiuDicInfo.Icone)}_end" : !(filtro.Icone <= 0) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.Icone} = @{nameof(DBStatusBiuDicInfo.Icone)}" : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.Icone} <= @{nameof(DBStatusBiuDicInfo.Icone)}_end");
        }

        if (!filtro.Codigo_filtro.IsEmpty() && filtro.Codigo_filtro_end.IsEmpty())
        {
            cWhere.Append(filtro.Codigo_filtro <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusBiuDicInfo.PTabelaNome}].[{DBStatusBiuDicInfo.CampoCodigo}] >= @{nameof(DBStatusBiuDicInfo.CampoCodigo)}");
        }
        else
        {
            cWhere.Append((filtro.Codigo_filtro <= 0 && filtro.Codigo_filtro_end <= 0) ? string.Empty : (!(filtro.Codigo_filtro <= 0) && !(filtro.Codigo_filtro_end <= 0)) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.CampoCodigo} BETWEEN @{nameof(DBStatusBiuDicInfo.CampoCodigo)} AND @{nameof(DBStatusBiuDicInfo.CampoCodigo)}_end" : !(filtro.Codigo_filtro <= 0) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.CampoCodigo} = @{nameof(DBStatusBiuDicInfo.CampoCodigo)}" : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusBiuDicInfo.CampoCodigo} <= @{nameof(DBStatusBiuDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }

    private static string ApplyWildCard(char wildcardChar, string value)
    {
        if (wildcardChar == '\0' || wildcardChar == ' ')
        {
            return value;
        }

        var result = $"{wildcardChar}{value.Replace(" ", wildcardChar.ToString())}{wildcardChar}";
        return result;
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterStatusBiu? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-1
        ThrowIfDisposed();
        var filtroResult = filtro == null ? null : WFiltro(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter> parameters = filtroResult?.parametros ?? [];
        var cacheKey = $"{uri}-StatusBiu-{max}-{where.GetHashCode()}-{parameters.GetHashCode()}GetListN";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter> parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarN(max, uri, where, parameters, DBStatusBiuDicInfo.CampoNome);
        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                break;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    private async Task<IEnumerable<StatusBiuResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter> parameters, string uri, CancellationToken token)
    {
        using var oCnn = Configuracoes.GetConnectionByUri(uri);
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.Listar(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}