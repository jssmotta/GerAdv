// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { OutrasPartesClienteEmpty } from '../../../Models/OutrasPartesCliente';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IOutrasPartesCliente } from '../../Interfaces/interface.OutrasPartesCliente';
import { OutrasPartesClienteService } from '../../Services/OutrasPartesCliente.service';
import { OutrasPartesClienteApi } from '../../Apis/ApiOutrasPartesCliente';
import { OutrasPartesClienteGridMobileComponent } from '../GridsMobile/OutrasPartesClienteMobile';
import { OutrasPartesClienteGridDesktopComponent } from '../GridsDesktop/OutrasPartesClienteDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterOutrasPartesCliente } from '../../Filters/OutrasPartesCliente';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import OutrasPartesClienteWindow from './OutrasPartesClienteWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useOutrasPartesClienteList } from '../../Hooks/hookOutrasPartesCliente';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useOutrasPartesClienteFilter } from '../../Hooks/hookOutrasPartesClienteFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const OutrasPartesClienteGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedOutrasPartesCliente, setSelectedOutrasPartesCliente] = useState<IOutrasPartesCliente>(OutrasPartesClienteEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterOutrasPartesCliente | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [outraspartesclienteData, setOutrasPartesClienteData] = useState<IOutrasPartesCliente[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const outraspartesclienteService = useMemo(() => {
    return new OutrasPartesClienteService(
    new OutrasPartesClienteApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: outraspartescliente, 
  fetchData, 
  refreshData
} = useOutrasPartesClienteList(outraspartesclienteService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterOutrasPartesCliente | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('OutrasPartesClienteFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await outraspartesclienteService.getAll(
  filtro ?? {} as FilterOutrasPartesCliente, 
  onlineData => {
    setOutrasPartesClienteData(onlineData);
    setLoading(false);
  }
);
setOutrasPartesClienteData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterOutrasPartesCliente;

    const filtroMem = sessionStorage.getItem(btoa('OutrasPartesClienteFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('OutrasPartesClienteFilter')) ?? '') || '{}') : {} as FilterOutrasPartesCliente;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (outraspartescliente: IOutrasPartesCliente) => {
    setSelectedOutrasPartesCliente(outraspartescliente);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedOutrasPartesCliente(OutrasPartesClienteEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const outraspartescliente = e.dataItem;
    setDeleteId(outraspartescliente.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await outraspartesclienteService.deleteOutrasPartesCliente(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useOutrasPartesClienteFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <OutrasPartesClienteGridMobileComponent
        data={outraspartesclienteData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <OutrasPartesClienteGridDesktopComponent
        data={outraspartesclienteData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <OutrasPartesClienteWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedOutrasPartesCliente={selectedOutrasPartesCliente}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Outras Partes Cliente'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default OutrasPartesClienteGrid;