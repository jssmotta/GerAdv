// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import BensMateriaisWindowId from '@/app/GerAdv_TS/BensMateriais/Crud/Grids/BensMateriaisWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import BensMateriaisWindowId from '@/app/GerAdv_TS/BensMateriais/Crud/Grids/BensMateriaisWindowId';
import { BensMateriaisEmpty } from '@/app/GerAdv_TS/Models/BensMateriais';

describe('BensMateriaisWindowId (basic)', () => {
  it('exports the component', () => {
    expect(BensMateriaisWindowId).toBeDefined();
    expect(typeof BensMateriaisWindowId === 'function' || typeof BensMateriaisWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the BensMateriaisService class so constructor usage doesn't throw and we can control fetchBensMateriaisById
const mockFetchBensMateriaisById = jest.fn();
jest.mock('@/app/GerAdv_TS/BensMateriais/Services/BensMateriais.service', () => ({
  BensMateriaisService: jest.fn().mockImplementation(() => ({
    fetchBensMateriaisById: (...args: any[]) => mockFetchBensMateriaisById(...args),
  })),
}));

// Mock the child BensMateriaisWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/BensMateriais/Crud/Grids/BensMateriaisWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='bensmateriais-window' {...p} />,
}));

describe('BensMateriaisWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(BensMateriaisWindowId).toBeDefined();
    expect(typeof BensMateriaisWindowId === 'function' || typeof BensMateriaisWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <BensMateriaisWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders BensMateriaisWindow with BensMateriaisEmpty when id is 0', async () => {
    render(
      <BensMateriaisWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // BensMateriaisWindow should be rendered with selectedBensMateriais equal to BensMateriaisEmpty()
    const node = await screen.findByTestId('bensmateriais-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedBensMateriais"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedbensmateriais') || node.getAttribute('selectedBensMateriais')).not.toBeNull();
  });

  it('fetches bensmateriais by id and passes it to BensMateriaisWindow when id > 0', async () => {
    const fakeBensMateriais = { id: 123, nomecliente: 'Test' };
    mockFetchBensMateriaisById.mockResolvedValue(fakeBensMateriais);

    render(
      <BensMateriaisWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('bensmateriais-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchBensMateriaisById).toHaveBeenCalledWith(123));
  });
});
