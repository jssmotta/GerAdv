// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ReuniaoWindowId from '@/app/GerAdv_TS/Reuniao/Crud/Grids/ReuniaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ReuniaoWindowId from '@/app/GerAdv_TS/Reuniao/Crud/Grids/ReuniaoWindowId';
import { ReuniaoEmpty } from '@/app/GerAdv_TS/Models/Reuniao';

describe('ReuniaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ReuniaoWindowId).toBeDefined();
    expect(typeof ReuniaoWindowId === 'function' || typeof ReuniaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ReuniaoService class so constructor usage doesn't throw and we can control fetchReuniaoById
const mockFetchReuniaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Reuniao/Services/Reuniao.service', () => ({
  ReuniaoService: jest.fn().mockImplementation(() => ({
    fetchReuniaoById: (...args: any[]) => mockFetchReuniaoById(...args),
  })),
}));

// Mock the child ReuniaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Reuniao/Crud/Grids/ReuniaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='reuniao-window' {...p} />,
}));

describe('ReuniaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ReuniaoWindowId).toBeDefined();
    expect(typeof ReuniaoWindowId === 'function' || typeof ReuniaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ReuniaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ReuniaoWindow with ReuniaoEmpty when id is 0', async () => {
    render(
      <ReuniaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ReuniaoWindow should be rendered with selectedReuniao equal to ReuniaoEmpty()
    const node = await screen.findByTestId('reuniao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedReuniao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedreuniao') || node.getAttribute('selectedReuniao')).not.toBeNull();
  });

  it('fetches reuniao by id and passes it to ReuniaoWindow when id > 0', async () => {
    const fakeReuniao = { id: 123, nomecliente: 'Test' };
    mockFetchReuniaoById.mockResolvedValue(fakeReuniao);

    render(
      <ReuniaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('reuniao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchReuniaoById).toHaveBeenCalledWith(123));
  });
});
