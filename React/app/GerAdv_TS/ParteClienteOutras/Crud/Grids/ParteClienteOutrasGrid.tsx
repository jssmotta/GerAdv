// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { ParteClienteOutrasEmpty } from '../../../Models/ParteClienteOutras';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IParteClienteOutras } from '../../Interfaces/interface.ParteClienteOutras';
import { ParteClienteOutrasService } from '../../Services/ParteClienteOutras.service';
import { ParteClienteOutrasApi } from '../../Apis/ApiParteClienteOutras';
import { ParteClienteOutrasGridMobileComponent } from '../GridsMobile/ParteClienteOutrasMobile';
import { ParteClienteOutrasGridDesktopComponent } from '../GridsDesktop/ParteClienteOutrasDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterParteClienteOutras } from '../../Filters/ParteClienteOutras';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import ParteClienteOutrasWindow from './ParteClienteOutrasWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useParteClienteOutrasList } from '../../Hooks/hookParteClienteOutras';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useParteClienteOutrasFilter } from '../../Hooks/hookParteClienteOutrasFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const ParteClienteOutrasGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedParteClienteOutras, setSelectedParteClienteOutras] = useState<IParteClienteOutras>(ParteClienteOutrasEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterParteClienteOutras | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [parteclienteoutrasData, setParteClienteOutrasData] = useState<IParteClienteOutras[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const parteclienteoutrasService = useMemo(() => {
    return new ParteClienteOutrasService(
    new ParteClienteOutrasApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: parteclienteoutras, 
  fetchData, 
  refreshData
} = useParteClienteOutrasList(parteclienteoutrasService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterParteClienteOutras | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('ParteClienteOutrasFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await parteclienteoutrasService.getAll(
  filtro ?? {} as FilterParteClienteOutras, 
  onlineData => {
    setParteClienteOutrasData(onlineData);
    setLoading(false);
  }
);
setParteClienteOutrasData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterParteClienteOutras;

    const filtroMem = sessionStorage.getItem(btoa('ParteClienteOutrasFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('ParteClienteOutrasFilter')) ?? '') || '{}') : {} as FilterParteClienteOutras;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (parteclienteoutras: IParteClienteOutras) => {
    setSelectedParteClienteOutras(parteclienteoutras);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedParteClienteOutras(ParteClienteOutrasEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const parteclienteoutras = e.dataItem;
    setDeleteId(parteclienteoutras.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await parteclienteoutrasService.deleteParteClienteOutras(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useParteClienteOutrasFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <ParteClienteOutrasGridMobileComponent
        data={parteclienteoutrasData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <ParteClienteOutrasGridDesktopComponent
        data={parteclienteoutrasData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <ParteClienteOutrasWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedParteClienteOutras={selectedParteClienteOutras}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Parte Cliente Outras'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default ParteClienteOutrasGrid;