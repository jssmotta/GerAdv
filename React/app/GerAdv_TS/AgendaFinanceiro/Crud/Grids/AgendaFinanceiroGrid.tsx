// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { AgendaFinanceiroEmpty } from '../../../Models/AgendaFinanceiro';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IAgendaFinanceiro } from '../../Interfaces/interface.AgendaFinanceiro';
import { AgendaFinanceiroService } from '../../Services/AgendaFinanceiro.service';
import { AgendaFinanceiroApi } from '../../Apis/ApiAgendaFinanceiro';
import { AgendaFinanceiroGridMobileComponent } from '../GridsMobile/AgendaFinanceiro';
import { AgendaFinanceiroGridDesktopComponent } from '../GridsDesktop/AgendaFinanceiro';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterAgendaFinanceiro } from '../../Filters/AgendaFinanceiro';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import AgendaFinanceiroWindow from './AgendaFinanceiroWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useAgendaFinanceiroList } from '../../Hooks/hookAgendaFinanceiro';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useAgendaFinanceiroFilter } from '../../Hooks/hookAgendaFinanceiroFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const AgendaFinanceiroGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedAgendaFinanceiro, setSelectedAgendaFinanceiro] = useState<IAgendaFinanceiro>(AgendaFinanceiroEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterAgendaFinanceiro | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [agendafinanceiroData, setAgendaFinanceiroData] = useState<IAgendaFinanceiro[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const agendafinanceiroService = useMemo(() => {
    return new AgendaFinanceiroService(
    new AgendaFinanceiroApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: agendafinanceiro, 
  fetchData, 
  refreshData
} = useAgendaFinanceiroList(agendafinanceiroService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterAgendaFinanceiro | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('AgendaFinanceiroFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await agendafinanceiroService.getAll(
  filtro ?? {} as FilterAgendaFinanceiro, 
  onlineData => {
    setAgendaFinanceiroData(onlineData);
    setLoading(false);
  }
);
setAgendaFinanceiroData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterAgendaFinanceiro;

    const filtroMem = sessionStorage.getItem(btoa('AgendaFinanceiroFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('AgendaFinanceiroFilter')) ?? '') || '{}') : {} as FilterAgendaFinanceiro;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (agendafinanceiro: IAgendaFinanceiro) => {
    setSelectedAgendaFinanceiro(agendafinanceiro);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedAgendaFinanceiro(AgendaFinanceiroEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const agendafinanceiro = e.dataItem;
    setDeleteId(agendafinanceiro.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await agendafinanceiroService.deleteAgendaFinanceiro(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useAgendaFinanceiroFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <AgendaFinanceiroGridMobileComponent
        data={agendafinanceiroData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <AgendaFinanceiroGridDesktopComponent
        data={agendafinanceiroData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <AgendaFinanceiroWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedAgendaFinanceiro={selectedAgendaFinanceiro}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Agenda Financeiro'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default AgendaFinanceiroGrid;