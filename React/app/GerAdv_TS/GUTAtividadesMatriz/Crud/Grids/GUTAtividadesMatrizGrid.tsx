// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { GUTAtividadesMatrizEmpty } from '../../../Models/GUTAtividadesMatriz';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IGUTAtividadesMatriz } from '../../Interfaces/interface.GUTAtividadesMatriz';
import { GUTAtividadesMatrizService } from '../../Services/GUTAtividadesMatriz.service';
import { GUTAtividadesMatrizApi } from '../../Apis/ApiGUTAtividadesMatriz';
import { GUTAtividadesMatrizGridMobileComponent } from '../GridsMobile/GUTAtividadesMatrizMobile';
import { GUTAtividadesMatrizGridDesktopComponent } from '../GridsDesktop/GUTAtividadesMatrizDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterGUTAtividadesMatriz } from '../../Filters/GUTAtividadesMatriz';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import GUTAtividadesMatrizWindow from './GUTAtividadesMatrizWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useGUTAtividadesMatrizList } from '../../Hooks/hookGUTAtividadesMatriz';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useGUTAtividadesMatrizFilter } from '../../Hooks/hookGUTAtividadesMatrizFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const GUTAtividadesMatrizGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedGUTAtividadesMatriz, setSelectedGUTAtividadesMatriz] = useState<IGUTAtividadesMatriz>(GUTAtividadesMatrizEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterGUTAtividadesMatriz | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [gutatividadesmatrizData, setGUTAtividadesMatrizData] = useState<IGUTAtividadesMatriz[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const gutatividadesmatrizService = useMemo(() => {
    return new GUTAtividadesMatrizService(
    new GUTAtividadesMatrizApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: gutatividadesmatriz, 
  fetchData, 
  refreshData
} = useGUTAtividadesMatrizList(gutatividadesmatrizService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterGUTAtividadesMatriz | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('GUTAtividadesMatrizFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await gutatividadesmatrizService.getAll(
  filtro ?? {} as FilterGUTAtividadesMatriz, 
  onlineData => {
    setGUTAtividadesMatrizData(onlineData);
    setLoading(false);
  }
);
setGUTAtividadesMatrizData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterGUTAtividadesMatriz;

    const filtroMem = sessionStorage.getItem(btoa('GUTAtividadesMatrizFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('GUTAtividadesMatrizFilter')) ?? '') || '{}') : {} as FilterGUTAtividadesMatriz;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (gutatividadesmatriz: IGUTAtividadesMatriz) => {
    setSelectedGUTAtividadesMatriz(gutatividadesmatriz);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedGUTAtividadesMatriz(GUTAtividadesMatrizEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const gutatividadesmatriz = e.dataItem;
    setDeleteId(gutatividadesmatriz.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await gutatividadesmatrizService.deleteGUTAtividadesMatriz(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useGUTAtividadesMatrizFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <GUTAtividadesMatrizGridMobileComponent
        data={gutatividadesmatrizData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <GUTAtividadesMatrizGridDesktopComponent
        data={gutatividadesmatrizData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <GUTAtividadesMatrizWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedGUTAtividadesMatriz={selectedGUTAtividadesMatriz}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar G U T Atividades Matriz'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default GUTAtividadesMatrizGrid;