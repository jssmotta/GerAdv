// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoStatusBiuService, TipoStatusBiuValidator } from '@/app/GerAdv_TS/TipoStatusBiu/Services/TipoStatusBiu.service';
import { TipoStatusBiuApi, TipoStatusBiuApiError } from '@/app/GerAdv_TS/TipoStatusBiu/Apis/ApiTipoStatusBiu';
import { ITipoStatusBiu } from '@/app/GerAdv_TS/TipoStatusBiu/Interfaces/interface.TipoStatusBiu';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoStatusBiuApi;

const sample: ITipoStatusBiu = { id: 1, nome: 'Teste' };

describe('TipoStatusBiuService', () => {
  let service: TipoStatusBiuService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoStatusBiuService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoStatusBiuValidator, 'validateTipoStatusBiu').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoStatusBiuById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoStatusBiuById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoStatusBiuById lança TipoStatusBiuApiError para id inválido', async () => {
    await expect(service.fetchTipoStatusBiuById(0)).rejects.toBeInstanceOf(TipoStatusBiuApiError);
  });

  it('saveTipoStatusBiu chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoStatusBiu(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoStatusBiu lança TipoStatusBiuApiError quando validação falha', async () => {
    (TipoStatusBiuValidator.validateTipoStatusBiu as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoStatusBiu(sample)).rejects.toBeInstanceOf(TipoStatusBiuApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoStatusBiu chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoStatusBiu(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoStatusBiu lança TipoStatusBiuApiError para id inválido', async () => {
    await expect(service.deleteTipoStatusBiu(0)).rejects.toBeInstanceOf(TipoStatusBiuApiError);
  });

  it('validateTipoStatusBiu delega para TipoStatusBiuValidator', () => {
    (TipoStatusBiuValidator.validateTipoStatusBiu as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoStatusBiu(sample);
    expect(r.isValid).toBe(true);
  });
});
