// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoEnderecoWindowId from '@/app/GerAdv_TS/TipoEndereco/Crud/Grids/TipoEnderecoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoEnderecoWindowId from '@/app/GerAdv_TS/TipoEndereco/Crud/Grids/TipoEnderecoWindowId';
import { TipoEnderecoEmpty } from '@/app/GerAdv_TS/Models/TipoEndereco';

describe('TipoEnderecoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoEnderecoWindowId).toBeDefined();
    expect(typeof TipoEnderecoWindowId === 'function' || typeof TipoEnderecoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoEnderecoService class so constructor usage doesn't throw and we can control fetchTipoEnderecoById
const mockFetchTipoEnderecoById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoEndereco/Services/TipoEndereco.service', () => ({
  TipoEnderecoService: jest.fn().mockImplementation(() => ({
    fetchTipoEnderecoById: (...args: any[]) => mockFetchTipoEnderecoById(...args),
  })),
}));

// Mock the child TipoEnderecoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoEndereco/Crud/Grids/TipoEnderecoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipoendereco-window' {...p} />,
}));

describe('TipoEnderecoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoEnderecoWindowId).toBeDefined();
    expect(typeof TipoEnderecoWindowId === 'function' || typeof TipoEnderecoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoEnderecoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoEnderecoWindow with TipoEnderecoEmpty when id is 0', async () => {
    render(
      <TipoEnderecoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoEnderecoWindow should be rendered with selectedTipoEndereco equal to TipoEnderecoEmpty()
    const node = await screen.findByTestId('tipoendereco-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoEndereco"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipoendereco') || node.getAttribute('selectedTipoEndereco')).not.toBeNull();
  });

  it('fetches tipoendereco by id and passes it to TipoEnderecoWindow when id > 0', async () => {
    const fakeTipoEndereco = { id: 123, nomecliente: 'Test' };
    mockFetchTipoEnderecoById.mockResolvedValue(fakeTipoEndereco);

    render(
      <TipoEnderecoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipoendereco-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoEnderecoById).toHaveBeenCalledWith(123));
  });
});
