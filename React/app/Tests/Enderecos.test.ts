// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useEnderecosForm, useEnderecosList, useValidationsEnderecos } from '../GerAdv_TS/Enderecos/Hooks/hookEnderecos';
import { IEnderecos } from '../GerAdv_TS/Enderecos/Interfaces/interface.Enderecos';
import { IEnderecosService } from '../GerAdv_TS/Enderecos/Services/Enderecos.service';
import { EnderecosTestEmpty } from '../GerAdv_TS/Models/Enderecos';
import { useEnderecosComboBox } from '../GerAdv_TS/Enderecos/Hooks/hookEnderecos';

// Mock do serviço
const mockEnderecosService: jest.Mocked<IEnderecosService> = {
  fetchEnderecosById: jest.fn(),
  saveEnderecos: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteEnderecos: jest.fn(),
  validateEnderecos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialEnderecos: IEnderecos = { ...EnderecosTestEmpty() };

describe('useEnderecosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    expect(result.current.data).toEqual(initialEnderecos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Endereço',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Endereço');
  });

   test('deve carregar Endereço por ID', async () => {
    const mockEnderecos = { ...initialEnderecos, id: 1, descricao: 'Endereço Teste' };
    mockEnderecosService.fetchEnderecosById.mockResolvedValue(mockEnderecos);

    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    await act(async () => {
      await result.current.loadEnderecos(1);
    });

    expect(mockEnderecosService.fetchEnderecosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockEnderecos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Endereço', async () => {
    const errorMessage = 'Erro ao carregar Endereço';
    mockEnderecosService.fetchEnderecosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    await act(async () => {
      await result.current.loadEnderecos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialEnderecos, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialEnderecos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    await act(async () => {
      await result.current.loadEnderecos(0);
    });

    expect(mockEnderecosService.fetchEnderecosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialEnderecos);
  });
});

describe('useEnderecosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useEnderecosList(mockEnderecosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialEnderecos, id: 1, descricao: 'Endereço 1' },
      { ...initialEnderecos, id: 2, descricao: 'Endereço 2' }
    ];
    mockEnderecosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useEnderecosList(mockEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockEnderecosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockEnderecosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useEnderecosList(mockEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialEnderecos, id: 1, descricao: 'Endereço Filtrado' }];
    const filtro = { descricao: 'Endereço' };
    mockEnderecosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useEnderecosList(mockEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockEnderecosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsEnderecos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsEnderecos());

    const validData = { ...initialEnderecos, descricao: 'Endereço Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsEnderecos());

    const invalidData = { ...initialEnderecos, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsEnderecos());

    const invalidData = { 
      ...initialEnderecos, 
      descricao: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsEnderecos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialEnderecos, id: 1, descricao: 'Endereço Teste' }];
    mockEnderecosService.getAll.mockResolvedValue(mockData);
    mockEnderecosService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useEnderecosList(mockEnderecosService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsEnderecos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Endereço Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Endereço Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useEnderecosForm(initialEnderecos, mockEnderecosService)
    );

    const mockEvent = {
      target: {
        name: 'topindex',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.topindex).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Endereço 1' },
      { id: 2, descricao: 'Endereço 2' }
    ];
    mockEnderecosService.getList.mockResolvedValue(mockOptions as IEnderecos[]);


    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Endereço 1' },
        { id: 2, nome: 'Endereço 2' }
      ]);
    });


    expect(mockEnderecosService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Endereço ABC' },
      { id: 2, descricao: 'Endereço XYZ' }
    ];
    mockEnderecosService.getList.mockResolvedValue(mockOptions as IEnderecos[]);   


 const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Endereço ABC' },
        { id: 2, nome: 'Endereço XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Endereço ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Endereço ABC' },
      { id: 2, descricao: 'Endereço XYZ' }
    ];
    mockEnderecosService.getList.mockResolvedValue(mockOptions as IEnderecos[]);
  


    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Endereço ABC' },
        { id: 2, nome: 'Endereço XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Endereço ABC' },
          {id: 2, nome: 'Endereço XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );

    const newValue = { id: 1, descricao: 'Endereço Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Endereço Inicial' };
    
    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useEnderecosComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Endereço Inicial' };
    
    const { result } = renderHook(() => 
      useEnderecosComboBox(mockEnderecosService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






