// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
public partial class DBAlarmSMS
{
    /// <summary>
    /// Factory method async para carregar [tableMin]
    /// </summary>
    public static async Task<DBAlarmSMS> CreateAsync(int nCodigo, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        var registro = new DBAlarmSMS();
        await registro.CarregarAsync(nCodigo, oCnn, cancellationToken);
        return registro;
    }

    /// <summary>
    /// Carregar dados async
    /// </summary>
    protected async Task CarregarAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (id.IsEmptyIDNumber() || oCnn is null)
            return;
        using var cmd = new SqlCommand($"{ConfiguracoesDBT.SQLNoCount}SELECT TOP (1) {CamposSqlX} FROM {PTabelaNome.dbo(oCnn)} WHERE [{CampoCodigo}] = @ThisIDToLoad", oCnn.InnerConnection);
        cmd.Parameters.AddWithValue("@ThisIDToLoad", id);
        using var ds = await ConfiguracoesDBT.GetDataTableAsync(cmd, CommandBehavior.SingleRow, oCnn, cancellationToken);
        if (ds?.Rows.Count > 0)
        {
            CarregarDadosBd(ds.Rows[0]);
        }
    }

#if (!NOTSTORED_AlarmSMS)
    /// <summary>
    /// Update async com timeout e cancellation
    /// </summary>
    public async Task<int> UpdateAsync(MsiSqlConnection? oCnn, int insertId = 0, CancellationToken cancellationToken = default, int timeoutSeconds = 30)
    {
        var isInsert = insertId == 0 && ID == 0;
        if (!isInsert && !HasAnyFieldChanged())
            return 0;
        if (oCnn is null)
        {
#if DEBUG
            PTabelaNome.PopupBox("oCnn is null - UpdateAsync()");
#endif
            return 0;
        }

        try
        {
            var updateTool = new DBToolWTable32Async(PTabelaNome, CampoCodigo, ID == 0)
            {
                IsMachineCode = true,
            };
            updateTool.Where = isInsert ? string.Empty : $"{CampoCodigo}={ID}";
            ConfigureUpdateFields(updateTool);
            ConfigureAuditorFields(updateTool);
            if (insertId != 0)
            {
                return await GravaNewIdAsync(updateTool, insertId, oCnn, cancellationToken);
            }

            var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken);
            if (updateTool.Insert && result == "OK")
            {
                ID = updateTool.GetCodigo();
                if (ID == 0)
                {
                    Error = -2;
                    ErrorDescription = "O registro não pode ser incluído, tente mais tarde.";
                    return Error;
                }
            }

            return result == "OK" ? 0 : -1;
        }
        catch (OperationCanceledException)
        {
            throw;
        }
        catch (Exception ex)
        {
            Error = -1;
            ErrorDescription = ex.Message;
            return Error;
        }
    }

#endif
    /// <summary>
    /// Verificar se existe async
    /// </summary>
    public static async Task<bool> ExisteAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var existe = await ConfiguracoesDBT.GetScalarAsync<int?>($"SELECT 1 FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = {id}", oCnn, cancellationToken);
        return existe.HasValue;
    }

    /// <summary>
    /// Delete async
    /// </summary>
    /// <param name = "operadorId">For future Audity</param>
    /// <param name = "id"></param>
    /// <param name = "oCnn"></param>
    /// <param name = "cancellationToken"></param>
    /// <returns></returns>
    /// <exception cref = "InvalidOperationException"></exception>
    public static async Task<bool> DeleteRecordAsync(int operadorId, int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var sql = $"DELETE FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = @Id";
        try
        {
            using var cmd = new SqlCommand(sql, oCnn.InnerConnection);
            cmd.Parameters.AddWithValue("@Id", id);
            var rowsAffected = await cmd.ExecuteNonQueryAsync(cancellationToken);
            return rowsAffected > 0;
        }
        catch (SqlException ex)when (ex.Number == 547)
        {
            throw new InvalidOperationException("Este registro não pode ser excluído, pois está vinculado a outros registros.");
        }
    }

#if (!NOTSTORED_AlarmSMS)
    // Helper methods
    private bool HasAnyFieldChanged() => pFldFDescricao || pFldFHora || pFldFMinuto || pFldFD1 || pFldFD2 || pFldFD3 || pFldFD4 || pFldFD5 || pFldFD6 || pFldFD7 || pFldFEMail || pFldFDesativar || pFldFToday || pFldFExcetoDiasFelizes || pFldFDesktop || pFldFAlertarDataHora || pFldFOperador || pFldFGuidExo || pFldFAgenda || pFldFRecado || pFldFEmocao || pFldFGUID;
    private void ConfigureUpdateFields(DBToolWTable32Async updateTool)
    {
        if (pFldFDescricao)
            updateTool.Fields(DBAlarmSMSDicInfo.Descricao, m_FDescricao, ETiposCampos.FString);
        if (pFldFHora)
            updateTool.Fields(DBAlarmSMSDicInfo.Hora, m_FHora, ETiposCampos.FNumber);
        if (pFldFMinuto)
            updateTool.Fields(DBAlarmSMSDicInfo.Minuto, m_FMinuto, ETiposCampos.FNumber);
        if (pFldFD1)
            updateTool.Fields(DBAlarmSMSDicInfo.D1, m_FD1, ETiposCampos.FBoolean);
        if (pFldFD2)
            updateTool.Fields(DBAlarmSMSDicInfo.D2, m_FD2, ETiposCampos.FBoolean);
        if (pFldFD3)
            updateTool.Fields(DBAlarmSMSDicInfo.D3, m_FD3, ETiposCampos.FBoolean);
        if (pFldFD4)
            updateTool.Fields(DBAlarmSMSDicInfo.D4, m_FD4, ETiposCampos.FBoolean);
        if (pFldFD5)
            updateTool.Fields(DBAlarmSMSDicInfo.D5, m_FD5, ETiposCampos.FBoolean);
        if (pFldFD6)
            updateTool.Fields(DBAlarmSMSDicInfo.D6, m_FD6, ETiposCampos.FBoolean);
        if (pFldFD7)
            updateTool.Fields(DBAlarmSMSDicInfo.D7, m_FD7, ETiposCampos.FBoolean);
        if (pFldFEMail)
            updateTool.Fields(DBAlarmSMSDicInfo.EMail, m_FEMail, ETiposCampos.FString);
        if (pFldFDesativar)
            updateTool.Fields(DBAlarmSMSDicInfo.Desativar, m_FDesativar, ETiposCampos.FBoolean);
        if (pFldFToday)
            updateTool.Fields(DBAlarmSMSDicInfo.Today, m_FToday, ETiposCampos.FDate);
        if (pFldFExcetoDiasFelizes)
            updateTool.Fields(DBAlarmSMSDicInfo.ExcetoDiasFelizes, m_FExcetoDiasFelizes, ETiposCampos.FBoolean);
        if (pFldFDesktop)
            updateTool.Fields(DBAlarmSMSDicInfo.Desktop, m_FDesktop, ETiposCampos.FBoolean);
        if (pFldFAlertarDataHora)
            updateTool.Fields(DBAlarmSMSDicInfo.AlertarDataHora, m_FAlertarDataHora, ETiposCampos.FDate);
        if (pFldFOperador)
            updateTool.Fields(DBAlarmSMSDicInfo.Operador, m_FOperador, ETiposCampos.FNumber);
        if (pFldFGuidExo)
            updateTool.Fields(DBAlarmSMSDicInfo.GuidExo, m_FGuidExo, ETiposCampos.FString);
        if (pFldFAgenda)
            updateTool.Fields(DBAlarmSMSDicInfo.Agenda, m_FAgenda, ETiposCampos.FNumber);
        if (pFldFRecado)
            updateTool.Fields(DBAlarmSMSDicInfo.Recado, m_FRecado, ETiposCampos.FNumber);
        if (pFldFEmocao)
            updateTool.Fields(DBAlarmSMSDicInfo.Emocao, m_FEmocao, ETiposCampos.FNumber);
        if (pFldFGUID)
            updateTool.Fields(DBAlarmSMSDicInfo.GUID, m_FGUID, ETiposCampos.FString);
    }

#endif
    private void ConfigureAuditorFields(DBToolWTable32Async updateTool)
    {
        var isInsert = ID.IsEmptyIDNumber();
        if (m_AuditorQuem == 0)
            AuditorQuem = 1;
        if (isInsert)
            updateTool.Fields(DBAlarmSMSDicInfo.QuemCad, AuditorQuem, ETiposCampos.FNumber);
        if (isInsert)
            updateTool.Fields(DBAlarmSMSDicInfo.DtCad, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        if (!isInsert)
            updateTool.Fields(DBAlarmSMSDicInfo.QuemAtu, AuditorQuem, ETiposCampos.FNumber);
        if (!isInsert)
            updateTool.Fields(DBAlarmSMSDicInfo.DtAtu, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        updateTool.Fields(DBAlarmSMSDicInfo.Visto, false, ETiposCampos.FBoolean);
    }

    private async Task<int> GravaNewIdAsync(DBToolWTable32Async updateTool, int insertId, MsiSqlConnection oCnn, CancellationToken cancellationToken)
    {
        ID = insertId;
        updateTool.Fields(CampoCodigo, insertId, ETiposCampos.FNumber);
        var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken, true);
        return result == "OK" ? 0 : -3;
    }
}