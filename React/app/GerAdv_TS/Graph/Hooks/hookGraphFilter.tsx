// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterGraph } from '@/app/GerAdv_TS/Graph/Filters/Graph';
interface UseGraphFilterProps {
  handleFetchWithFilter: (filtro?: FilterGraph | undefined | null) => Promise<void>;
}
interface UseGraphFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterGraph;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterGraph>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterGraph) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterGraph>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useGraphFilter = ({ handleFetchWithFilter }: UseGraphFilterProps): UseGraphFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterGraph>({} as FilterGraph);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterGraph;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterGraph) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('GraphFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterGraph>) => (
  <>
  <InputInput
  type='text'
  id='tabela'
  name='tabela'
  value={handlers.windowFilter?.tabela ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Tabela'
  label='Tabela'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='tabelaid'
  name='tabelaid'
  value={handlers.windowFilter?.tabelaid ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe TabelaId'
  label='TabelaId (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='tabelaid_end'
  name='tabelaid_end'
  value={handlers.windowFilter?.tabelaid_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe TabelaId final'
  label='TabelaId final'
  disabled={handlers.windowFilter?.tabelaid ? false: true}
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};