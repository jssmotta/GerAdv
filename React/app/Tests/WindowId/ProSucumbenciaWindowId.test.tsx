// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProSucumbenciaWindowId from '@/app/GerAdv_TS/ProSucumbencia/Crud/Grids/ProSucumbenciaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProSucumbenciaWindowId from '@/app/GerAdv_TS/ProSucumbencia/Crud/Grids/ProSucumbenciaWindowId';
import { ProSucumbenciaEmpty } from '@/app/GerAdv_TS/Models/ProSucumbencia';

describe('ProSucumbenciaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProSucumbenciaWindowId).toBeDefined();
    expect(typeof ProSucumbenciaWindowId === 'function' || typeof ProSucumbenciaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProSucumbenciaService class so constructor usage doesn't throw and we can control fetchProSucumbenciaById
const mockFetchProSucumbenciaById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProSucumbencia/Services/ProSucumbencia.service', () => ({
  ProSucumbenciaService: jest.fn().mockImplementation(() => ({
    fetchProSucumbenciaById: (...args: any[]) => mockFetchProSucumbenciaById(...args),
  })),
}));

// Mock the child ProSucumbenciaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProSucumbencia/Crud/Grids/ProSucumbenciaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='prosucumbencia-window' {...p} />,
}));

describe('ProSucumbenciaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProSucumbenciaWindowId).toBeDefined();
    expect(typeof ProSucumbenciaWindowId === 'function' || typeof ProSucumbenciaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProSucumbenciaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProSucumbenciaWindow with ProSucumbenciaEmpty when id is 0', async () => {
    render(
      <ProSucumbenciaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProSucumbenciaWindow should be rendered with selectedProSucumbencia equal to ProSucumbenciaEmpty()
    const node = await screen.findByTestId('prosucumbencia-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProSucumbencia"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprosucumbencia') || node.getAttribute('selectedProSucumbencia')).not.toBeNull();
  });

  it('fetches prosucumbencia by id and passes it to ProSucumbenciaWindow when id > 0', async () => {
    const fakeProSucumbencia = { id: 123, nomecliente: 'Test' };
    mockFetchProSucumbenciaById.mockResolvedValue(fakeProSucumbencia);

    render(
      <ProSucumbenciaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('prosucumbencia-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProSucumbenciaById).toHaveBeenCalledWith(123));
  });
});
