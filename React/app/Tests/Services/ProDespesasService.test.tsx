// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProDespesasService, ProDespesasValidator } from '@/app/GerAdv_TS/ProDespesas/Services/ProDespesas.service';
import { ProDespesasApi, ProDespesasApiError } from '@/app/GerAdv_TS/ProDespesas/Apis/ApiProDespesas';
import { IProDespesas } from '@/app/GerAdv_TS/ProDespesas/Interfaces/interface.ProDespesas';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProDespesasApi;

const sample: IProDespesas = { id: 1, nome: 'Teste' };

describe('ProDespesasService', () => {
  let service: ProDespesasService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProDespesasService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProDespesasValidator, 'validateProDespesas').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProDespesasById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProDespesasById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProDespesasById lança ProDespesasApiError para id inválido', async () => {
    await expect(service.fetchProDespesasById(0)).rejects.toBeInstanceOf(ProDespesasApiError);
  });

  it('saveProDespesas chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProDespesas(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProDespesas lança ProDespesasApiError quando validação falha', async () => {
    (ProDespesasValidator.validateProDespesas as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProDespesas(sample)).rejects.toBeInstanceOf(ProDespesasApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProDespesas chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProDespesas(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProDespesas lança ProDespesasApiError para id inválido', async () => {
    await expect(service.deleteProDespesas(0)).rejects.toBeInstanceOf(ProDespesasApiError);
  });

  it('validateProDespesas delega para ProDespesasValidator', () => {
    (ProDespesasValidator.validateProDespesas as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProDespesas(sample);
    expect(r.isValid).toBe(true);
  });
});
