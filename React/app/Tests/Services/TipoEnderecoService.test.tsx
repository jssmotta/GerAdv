// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoEnderecoService, TipoEnderecoValidator } from '@/app/GerAdv_TS/TipoEndereco/Services/TipoEndereco.service';
import { TipoEnderecoApi, TipoEnderecoApiError } from '@/app/GerAdv_TS/TipoEndereco/Apis/ApiTipoEndereco';
import { ITipoEndereco } from '@/app/GerAdv_TS/TipoEndereco/Interfaces/interface.TipoEndereco';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoEnderecoApi;

const sample: ITipoEndereco = { id: 1, nome: 'Teste' };

describe('TipoEnderecoService', () => {
  let service: TipoEnderecoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoEnderecoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoEnderecoValidator, 'validateTipoEndereco').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoEnderecoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoEnderecoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoEnderecoById lança TipoEnderecoApiError para id inválido', async () => {
    await expect(service.fetchTipoEnderecoById(0)).rejects.toBeInstanceOf(TipoEnderecoApiError);
  });

  it('saveTipoEndereco chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoEndereco(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoEndereco lança TipoEnderecoApiError quando validação falha', async () => {
    (TipoEnderecoValidator.validateTipoEndereco as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoEndereco(sample)).rejects.toBeInstanceOf(TipoEnderecoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoEndereco chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoEndereco(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoEndereco lança TipoEnderecoApiError para id inválido', async () => {
    await expect(service.deleteTipoEndereco(0)).rejects.toBeInstanceOf(TipoEnderecoApiError);
  });

  it('validateTipoEndereco delega para TipoEnderecoValidator', () => {
    (TipoEnderecoValidator.validateTipoEndereco as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoEndereco(sample);
    expect(r.isValid).toBe(true);
  });
});
