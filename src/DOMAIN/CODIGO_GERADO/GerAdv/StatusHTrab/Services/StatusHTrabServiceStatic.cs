// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial class StatusHTrabService
{
    private static (string where, List<SqlParameter> parametros)? WFiltro(Filters.FilterStatusHTrab filtro)
    {
        var parameters = new List<SqlParameter>();
        if (!string.IsNullOrEmpty(filtro.Descricao))
        {
            parameters.Add(new($"@{nameof(DBStatusHTrabDicInfo.Descricao)}", ApplyWildCard(filtro.WildcardChar, filtro.Descricao)));
        }

        if (filtro.ResID != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusHTrabDicInfo.ResID)}", filtro.ResID));
        }

        if (filtro.ResID_end != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusHTrabDicInfo.ResID)}_end", filtro.ResID_end));
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusHTrabDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
        }

        if (filtro.Codigo_filtro_end != int.MinValue)
        {
            parameters.Add(new($"@{nameof(DBStatusHTrabDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
        }

        if (filtro.LogicalOperator.IsEmpty() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var cWhere = new StringBuilder();
        cWhere.Append(filtro.Descricao.IsEmpty() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusHTrabDicInfo.PTabelaNome}].[{DBStatusHTrabDicInfo.Descricao}]  {DevourerConsts.MsiCollate} like @{nameof(DBStatusHTrabDicInfo.Descricao)}");
        if (!filtro.ResID.IsEmpty() && filtro.ResID_end.IsEmpty())
        {
            cWhere.Append(filtro.ResID <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusHTrabDicInfo.PTabelaNome}].[{DBStatusHTrabDicInfo.ResID}] >= @{nameof(DBStatusHTrabDicInfo.ResID)}");
        }
        else
        {
            cWhere.Append((filtro.ResID <= 0 && filtro.ResID_end <= 0) ? string.Empty : (!(filtro.ResID <= 0) && !(filtro.ResID_end <= 0)) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.ResID} BETWEEN @{nameof(DBStatusHTrabDicInfo.ResID)} AND @{nameof(DBStatusHTrabDicInfo.ResID)}_end" : !(filtro.ResID <= 0) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.ResID} = @{nameof(DBStatusHTrabDicInfo.ResID)}" : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.ResID} <= @{nameof(DBStatusHTrabDicInfo.ResID)}_end");
        }

        if (!filtro.Codigo_filtro.IsEmpty() && filtro.Codigo_filtro_end.IsEmpty())
        {
            cWhere.Append(filtro.Codigo_filtro <= 0 ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBStatusHTrabDicInfo.PTabelaNome}].[{DBStatusHTrabDicInfo.CampoCodigo}] >= @{nameof(DBStatusHTrabDicInfo.CampoCodigo)}");
        }
        else
        {
            cWhere.Append((filtro.Codigo_filtro <= 0 && filtro.Codigo_filtro_end <= 0) ? string.Empty : (!(filtro.Codigo_filtro <= 0) && !(filtro.Codigo_filtro_end <= 0)) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.CampoCodigo} BETWEEN @{nameof(DBStatusHTrabDicInfo.CampoCodigo)} AND @{nameof(DBStatusHTrabDicInfo.CampoCodigo)}_end" : !(filtro.Codigo_filtro <= 0) ? (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.CampoCodigo} = @{nameof(DBStatusHTrabDicInfo.CampoCodigo)}" : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"{DBStatusHTrabDicInfo.CampoCodigo} <= @{nameof(DBStatusHTrabDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }

    private static string ApplyWildCard(char wildcardChar, string value)
    {
        if (wildcardChar == '\0' || wildcardChar == ' ')
        {
            return value;
        }

        var result = $"{wildcardChar}{value.Replace(" ", wildcardChar.ToString())}{wildcardChar}";
        return result;
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterStatusHTrab? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-1
        ThrowIfDisposed();
        var filtroResult = filtro == null ? null : WFiltro(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter> parameters = filtroResult?.parametros ?? [];
        var cacheKey = $"{uri}-StatusHTrab-{max}-{where.GetHashCode()}-{parameters.GetHashCode()}GetListN";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter> parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarN(max, uri, where, parameters, DBStatusHTrabDicInfo.CampoNome);
        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                break;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    private async Task<IEnumerable<StatusHTrabResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter> parameters, string uri, CancellationToken token)
    {
        using var oCnn = Configuracoes.GetConnectionByUri(uri);
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.Listar(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}