// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PenhoraStatusWindowId from '@/app/GerAdv_TS/PenhoraStatus/Crud/Grids/PenhoraStatusWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PenhoraStatusWindowId from '@/app/GerAdv_TS/PenhoraStatus/Crud/Grids/PenhoraStatusWindowId';
import { PenhoraStatusEmpty } from '@/app/GerAdv_TS/Models/PenhoraStatus';

describe('PenhoraStatusWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PenhoraStatusWindowId).toBeDefined();
    expect(typeof PenhoraStatusWindowId === 'function' || typeof PenhoraStatusWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PenhoraStatusService class so constructor usage doesn't throw and we can control fetchPenhoraStatusById
const mockFetchPenhoraStatusById = jest.fn();
jest.mock('@/app/GerAdv_TS/PenhoraStatus/Services/PenhoraStatus.service', () => ({
  PenhoraStatusService: jest.fn().mockImplementation(() => ({
    fetchPenhoraStatusById: (...args: any[]) => mockFetchPenhoraStatusById(...args),
  })),
}));

// Mock the child PenhoraStatusWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/PenhoraStatus/Crud/Grids/PenhoraStatusWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='penhorastatus-window' {...p} />,
}));

describe('PenhoraStatusWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PenhoraStatusWindowId).toBeDefined();
    expect(typeof PenhoraStatusWindowId === 'function' || typeof PenhoraStatusWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PenhoraStatusWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PenhoraStatusWindow with PenhoraStatusEmpty when id is 0', async () => {
    render(
      <PenhoraStatusWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PenhoraStatusWindow should be rendered with selectedPenhoraStatus equal to PenhoraStatusEmpty()
    const node = await screen.findByTestId('penhorastatus-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPenhoraStatus"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedpenhorastatus') || node.getAttribute('selectedPenhoraStatus')).not.toBeNull();
  });

  it('fetches penhorastatus by id and passes it to PenhoraStatusWindow when id > 0', async () => {
    const fakePenhoraStatus = { id: 123, nomecliente: 'Test' };
    mockFetchPenhoraStatusById.mockResolvedValue(fakePenhoraStatus);

    render(
      <PenhoraStatusWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('penhorastatus-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPenhoraStatusById).toHaveBeenCalledWith(123));
  });
});
