// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Entity:Instancia
// Source:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class InstanciaServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFInstanciaFactory> _mockInstanciaFactory;
    private readonly Mock<IInstanciaReader> _mockReader;
    private readonly Mock<IInstanciaValidation> _mockValidation;
    private readonly Mock<IInstanciaWriter> _mockWriter;
    private readonly Mock<ITribunalService> _mockTribunalService;
    private readonly Mock<IAcaoReader> _mockAcaoReader;
    private readonly Mock<IForoReader> _mockForoReader;
    private readonly Mock<ITipoRecursoReader> _mockTipoRecursoReader;
    private readonly Mock<IHttpContextAccessor> _mockHttpContextAccessor;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly InstanciaService _service;
    private readonly ServiceFilter _serviceS;
    public InstanciaServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockInstanciaFactory = new Mock<IFInstanciaFactory>();
        _mockReader = new Mock<IInstanciaReader>();
        _mockValidation = new Mock<IInstanciaValidation>();
        _mockWriter = new Mock<IInstanciaWriter>();
        _mockTribunalService = new Mock<ITribunalService>();
        _mockAcaoReader = new Mock<IAcaoReader>();
        _mockForoReader = new Mock<IForoReader>();
        _mockTipoRecursoReader = new Mock<ITipoRecursoReader>();
        _mockHttpContextAccessor = new Mock<IHttpContextAccessor>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new InstanciaService(_mockAppSettings.Object, _mockInstanciaFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockAcaoReader.Object, _mockForoReader.Object, _mockTipoRecursoReader.Object, _mockTribunalService.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterInstancia? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterInstancia(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            QuemCad = 1,
            DtCad = "24/04/1975",
            QuemAtu = 1,
            DtAtu = "24/04/1975",
            Visto = 2,
            Guid = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            LiminarPedida = "",
            Objeto = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            StatusResultado = 1,
            LiminarPendente = 1,
            InterpusemosRecurso = 2,
            LiminarConcedida = 1,
            LiminarNegada = 2,
            Processo = 1,
            Data = "24/04/1975",
            LiminarParcial = 1,
            LiminarResultado = "",
            NroProcesso = "AAAAAAAAAAAAAAAAAAAAAAA",
            Divisao = 1,
            LiminarCliente = 2,
            Comarca = 1,
            SubDivisao = 1,
            Principal = 1,
            Acao = 1,
            Foro = 1,
            TipoRecurso = 1,
            ZKey = "AAAAAAAAAAAAAAAAAAAAAAA",
            ZKeyQuem = 1,
            ZKeyQuando = "24/04/1975",
            NroAntigo = "AAAAAAAAAAAAAAAAAAAAAAA",
            AccessCode = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Julgador = 1,
            ZKeyIA = "AAAAAAAAAAAAAAAAAAAAAAA",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroInstancia(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Guid_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string GuidComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = GuidComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{GuidGigante}%");
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Guid_ComCaracteresEspeciais_DeveProcessarCorretamente(string GuidComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = GuidComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(GuidComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_GuidInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_GuidInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_GuidInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_GuidInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Guid}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Guid_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Guid_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_LiminarPedida_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string LiminarPedidaComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = LiminarPedidaComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarPedida_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var LiminarPedidaGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            LiminarPedida = LiminarPedidaGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{LiminarPedidaGigante}%");
    }

    [Fact]
    public void WFiltro_LiminarPedida_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var LiminarPedidaGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            LiminarPedida = LiminarPedidaGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_LiminarPedida_ComCaracteresEspeciais_DeveProcessarCorretamente(string LiminarPedidaComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = LiminarPedidaComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(LiminarPedidaComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_LiminarPedidaInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_LiminarPedidaInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarPedidaInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarPedidaInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarPedida_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "LiminarPedida",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarPedida_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarPedida_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarPedida_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarPedida}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_LiminarPedida_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_LiminarPedida_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPedida = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Objeto_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string ObjetoComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = ObjetoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Objeto_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var ObjetoGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            Objeto = ObjetoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{ObjetoGigante}%");
    }

    [Fact]
    public void WFiltro_Objeto_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var ObjetoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            Objeto = ObjetoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Objeto_ComCaracteresEspeciais_DeveProcessarCorretamente(string ObjetoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = ObjetoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(ObjetoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_ObjetoInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_ObjetoInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ObjetoInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ObjetoInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Objeto_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "Objeto",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Objeto_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Objeto_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Objeto_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Objeto}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Objeto_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Objeto_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Objeto = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_LiminarResultado_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string LiminarResultadoComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = LiminarResultadoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarResultado_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var LiminarResultadoGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            LiminarResultado = LiminarResultadoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{LiminarResultadoGigante}%");
    }

    [Fact]
    public void WFiltro_LiminarResultado_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var LiminarResultadoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            LiminarResultado = LiminarResultadoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_LiminarResultado_ComCaracteresEspeciais_DeveProcessarCorretamente(string LiminarResultadoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = LiminarResultadoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(LiminarResultadoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_LiminarResultadoInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_LiminarResultadoInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarResultadoInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarResultadoInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarResultado_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "LiminarResultado",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarResultado_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_LiminarResultado_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_LiminarResultado_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarResultado}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_LiminarResultado_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_LiminarResultado_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarResultado = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_NroProcesso_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string NroProcessoComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = NroProcessoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_NroProcesso_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var NroProcessoGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            NroProcesso = NroProcessoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{NroProcessoGigante}%");
    }

    [Fact]
    public void WFiltro_NroProcesso_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var NroProcessoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            NroProcesso = NroProcessoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_NroProcesso_ComCaracteresEspeciais_DeveProcessarCorretamente(string NroProcessoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = NroProcessoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(NroProcessoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_NroProcessoInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_NroProcessoInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_NroProcessoInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroProcessoInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroProcesso_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "NroProcesso",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_NroProcesso_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_NroProcesso_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroProcesso_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.NroProcesso}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_NroProcesso_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_NroProcesso_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroProcesso = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_ZKey_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string ZKeyComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = ZKeyComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKey_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var ZKeyGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            ZKey = ZKeyGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{ZKeyGigante}%");
    }

    [Fact]
    public void WFiltro_ZKey_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var ZKeyGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            ZKey = ZKeyGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_ZKey_ComCaracteresEspeciais_DeveProcessarCorretamente(string ZKeyComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = ZKeyComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(ZKeyComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_ZKeyInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_ZKeyInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKeyInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKey_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "ZKey",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_ZKey_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_ZKey_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKey_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.ZKey}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_ZKey_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_ZKey_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKey = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_NroAntigo_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string NroAntigoComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = NroAntigoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_NroAntigo_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var NroAntigoGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            NroAntigo = NroAntigoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{NroAntigoGigante}%");
    }

    [Fact]
    public void WFiltro_NroAntigo_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var NroAntigoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            NroAntigo = NroAntigoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_NroAntigo_ComCaracteresEspeciais_DeveProcessarCorretamente(string NroAntigoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = NroAntigoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(NroAntigoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_NroAntigoInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_NroAntigoInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_NroAntigoInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroAntigoInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroAntigo_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "NroAntigo",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_NroAntigo_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_NroAntigo_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_NroAntigo_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.NroAntigo}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_NroAntigo_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_NroAntigo_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            NroAntigo = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_AccessCode_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string AccessCodeComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = AccessCodeComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_AccessCode_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var AccessCodeGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            AccessCode = AccessCodeGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{AccessCodeGigante}%");
    }

    [Fact]
    public void WFiltro_AccessCode_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var AccessCodeGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            AccessCode = AccessCodeGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_AccessCode_ComCaracteresEspeciais_DeveProcessarCorretamente(string AccessCodeComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = AccessCodeComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(AccessCodeComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_AccessCodeInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_AccessCodeInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_AccessCodeInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_AccessCodeInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_AccessCode_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "AccessCode",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_AccessCode_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_AccessCode_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_AccessCode_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.AccessCode}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_AccessCode_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_AccessCode_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            AccessCode = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_ZKeyIA_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string ZKeyIAComWhitespace)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = ZKeyIAComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyIA_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var ZKeyIAGigante = new string ('A', 1000);
        var filtro = new FilterInstancia
        {
            ZKeyIA = ZKeyIAGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{ZKeyIAGigante}%");
    }

    [Fact]
    public void WFiltro_ZKeyIA_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var ZKeyIAGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterInstancia
        {
            ZKeyIA = ZKeyIAGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_ZKeyIA_ComCaracteresEspeciais_DeveProcessarCorretamente(string ZKeyIAComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = ZKeyIAComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(ZKeyIAComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_ZKeyIAInstancia_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_ZKeyIAInstancia_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyIAInstancia_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKeyIAInstancia_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKeyIA_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "ZKeyIA",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyIA_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyIA_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroInstancia(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_ZKeyIA_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.ZKeyIA}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_ZKeyIA_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_ZKeyIA_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyIA = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region QuemCad Filter Tests
    [Fact]
    public void WFiltro_QuemCadEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            QuemCad = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.QuemCad));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.QuemCad);
    }

    [Fact]
    public void WFiltro_QuemCadHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemCadValue = 1;
        var filtro = new FilterInstancia
        {
            QuemCad = QuemCadValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemCadParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemCad}");
        QuemCadParameter.Should().NotBeNull();
        QuemCadParameter.Value.Should().Be(QuemCadValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.QuemCad}] = @{DBInstanciaDicInfo.QuemCad}");
    }

    [Fact]
    public void WFiltro_QuemCadWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemCad = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            QuemCad = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemCad}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemCad = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            QuemCad = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemCad}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemCadAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.QuemCad}] = @{DBInstanciaDicInfo.QuemCad}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region QuemAtu Filter Tests
    [Fact]
    public void WFiltro_QuemAtuEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            QuemAtu = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.QuemAtu));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.QuemAtu);
    }

    [Fact]
    public void WFiltro_QuemAtuHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemAtuValue = 1;
        var filtro = new FilterInstancia
        {
            QuemAtu = QuemAtuValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemAtuParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemAtu}");
        QuemAtuParameter.Should().NotBeNull();
        QuemAtuParameter.Value.Should().Be(QuemAtuValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.QuemAtu}] = @{DBInstanciaDicInfo.QuemAtu}");
    }

    [Fact]
    public void WFiltro_QuemAtuWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemAtu = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            QuemAtu = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemAtu}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemAtu = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            QuemAtu = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.QuemAtu}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemAtuAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.QuemAtu}] = @{DBInstanciaDicInfo.QuemAtu}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region StatusResultado Filter Tests
    [Fact]
    public void WFiltro_StatusResultadoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            StatusResultado = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.StatusResultado));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.StatusResultado);
    }

    [Fact]
    public void WFiltro_StatusResultadoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var StatusResultadoValue = 1;
        var filtro = new FilterInstancia
        {
            StatusResultado = StatusResultadoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var StatusResultadoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.StatusResultado}");
        StatusResultadoParameter.Should().NotBeNull();
        StatusResultadoParameter.Value.Should().Be(StatusResultadoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.StatusResultado}] = @{DBInstanciaDicInfo.StatusResultado}");
    }

    [Fact]
    public void WFiltro_StatusResultadoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for StatusResultado = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            StatusResultado = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.StatusResultado}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for StatusResultado = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            StatusResultado = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.StatusResultado}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_StatusResultadoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            StatusResultado = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.StatusResultado}] = @{DBInstanciaDicInfo.StatusResultado}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_StatusResultadoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            StatusResultado = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_StatusResultadoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            StatusResultado = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Processo Filter Tests
    [Fact]
    public void WFiltro_ProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Processo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Processo));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Processo);
    }

    [Fact]
    public void WFiltro_ProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ProcessoValue = 1;
        var filtro = new FilterInstancia
        {
            Processo = ProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Processo}");
        ProcessoParameter.Should().NotBeNull();
        ProcessoParameter.Value.Should().Be(ProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Processo}] = @{DBInstanciaDicInfo.Processo}");
    }

    [Fact]
    public void WFiltro_ProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Processo = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Processo = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Processo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Processo = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Processo = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Processo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Processo}] = @{DBInstanciaDicInfo.Processo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Divisao Filter Tests
    [Fact]
    public void WFiltro_DivisaoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Divisao = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Divisao));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Divisao);
    }

    [Fact]
    public void WFiltro_DivisaoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var DivisaoValue = 1;
        var filtro = new FilterInstancia
        {
            Divisao = DivisaoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var DivisaoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Divisao}");
        DivisaoParameter.Should().NotBeNull();
        DivisaoParameter.Value.Should().Be(DivisaoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Divisao}] = @{DBInstanciaDicInfo.Divisao}");
    }

    [Fact]
    public void WFiltro_DivisaoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Divisao = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Divisao = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Divisao}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Divisao = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Divisao = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Divisao}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_DivisaoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Divisao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Divisao}] = @{DBInstanciaDicInfo.Divisao}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_DivisaoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Divisao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_DivisaoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Divisao = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Comarca Filter Tests
    [Fact]
    public void WFiltro_ComarcaEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Comarca = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Comarca));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Comarca);
    }

    [Fact]
    public void WFiltro_ComarcaHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ComarcaValue = 1;
        var filtro = new FilterInstancia
        {
            Comarca = ComarcaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ComarcaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Comarca}");
        ComarcaParameter.Should().NotBeNull();
        ComarcaParameter.Value.Should().Be(ComarcaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Comarca}] = @{DBInstanciaDicInfo.Comarca}");
    }

    [Fact]
    public void WFiltro_ComarcaWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Comarca = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Comarca = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Comarca}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Comarca = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Comarca = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Comarca}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ComarcaAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Comarca = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Comarca}] = @{DBInstanciaDicInfo.Comarca}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ComarcaWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Comarca = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ComarcaWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Comarca = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region SubDivisao Filter Tests
    [Fact]
    public void WFiltro_SubDivisaoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            SubDivisao = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.SubDivisao));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.SubDivisao);
    }

    [Fact]
    public void WFiltro_SubDivisaoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var SubDivisaoValue = 1;
        var filtro = new FilterInstancia
        {
            SubDivisao = SubDivisaoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var SubDivisaoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.SubDivisao}");
        SubDivisaoParameter.Should().NotBeNull();
        SubDivisaoParameter.Value.Should().Be(SubDivisaoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.SubDivisao}] = @{DBInstanciaDicInfo.SubDivisao}");
    }

    [Fact]
    public void WFiltro_SubDivisaoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for SubDivisao = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            SubDivisao = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.SubDivisao}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for SubDivisao = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            SubDivisao = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.SubDivisao}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_SubDivisaoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            SubDivisao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.SubDivisao}] = @{DBInstanciaDicInfo.SubDivisao}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_SubDivisaoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            SubDivisao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_SubDivisaoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            SubDivisao = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Acao Filter Tests
    [Fact]
    public void WFiltro_AcaoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Acao = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Acao));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Acao);
    }

    [Fact]
    public void WFiltro_AcaoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var AcaoValue = 1;
        var filtro = new FilterInstancia
        {
            Acao = AcaoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var AcaoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Acao}");
        AcaoParameter.Should().NotBeNull();
        AcaoParameter.Value.Should().Be(AcaoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Acao}] = @{DBInstanciaDicInfo.Acao}");
    }

    [Fact]
    public void WFiltro_AcaoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Acao = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Acao = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Acao}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Acao = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Acao = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Acao}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_AcaoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Acao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Acao}] = @{DBInstanciaDicInfo.Acao}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_AcaoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Acao = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_AcaoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Acao = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Foro Filter Tests
    [Fact]
    public void WFiltro_ForoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Foro = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Foro));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Foro);
    }

    [Fact]
    public void WFiltro_ForoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ForoValue = 1;
        var filtro = new FilterInstancia
        {
            Foro = ForoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ForoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Foro}");
        ForoParameter.Should().NotBeNull();
        ForoParameter.Value.Should().Be(ForoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Foro}] = @{DBInstanciaDicInfo.Foro}");
    }

    [Fact]
    public void WFiltro_ForoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Foro = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Foro = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Foro}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Foro = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Foro = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Foro}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ForoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Foro = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Foro}] = @{DBInstanciaDicInfo.Foro}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ForoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Foro = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ForoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Foro = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region TipoRecurso Filter Tests
    [Fact]
    public void WFiltro_TipoRecursoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            TipoRecurso = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.TipoRecurso));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.TipoRecurso);
    }

    [Fact]
    public void WFiltro_TipoRecursoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var TipoRecursoValue = 1;
        var filtro = new FilterInstancia
        {
            TipoRecurso = TipoRecursoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var TipoRecursoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.TipoRecurso}");
        TipoRecursoParameter.Should().NotBeNull();
        TipoRecursoParameter.Value.Should().Be(TipoRecursoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.TipoRecurso}] = @{DBInstanciaDicInfo.TipoRecurso}");
    }

    [Fact]
    public void WFiltro_TipoRecursoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for TipoRecurso = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            TipoRecurso = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.TipoRecurso}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for TipoRecurso = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            TipoRecurso = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.TipoRecurso}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_TipoRecursoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            TipoRecurso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.TipoRecurso}] = @{DBInstanciaDicInfo.TipoRecurso}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_TipoRecursoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            TipoRecurso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_TipoRecursoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            TipoRecurso = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region ZKeyQuem Filter Tests
    [Fact]
    public void WFiltro_ZKeyQuemEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyQuem = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.ZKeyQuem));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.ZKeyQuem);
    }

    [Fact]
    public void WFiltro_ZKeyQuemHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ZKeyQuemValue = 1;
        var filtro = new FilterInstancia
        {
            ZKeyQuem = ZKeyQuemValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ZKeyQuemParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.ZKeyQuem}");
        ZKeyQuemParameter.Should().NotBeNull();
        ZKeyQuemParameter.Value.Should().Be(ZKeyQuemValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.ZKeyQuem}] = @{DBInstanciaDicInfo.ZKeyQuem}");
    }

    [Fact]
    public void WFiltro_ZKeyQuemWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for ZKeyQuem = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            ZKeyQuem = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.ZKeyQuem}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for ZKeyQuem = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            ZKeyQuem = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.ZKeyQuem}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ZKeyQuemAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyQuem = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.ZKeyQuem}] = @{DBInstanciaDicInfo.ZKeyQuem}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ZKeyQuemWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            ZKeyQuem = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ZKeyQuemWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            ZKeyQuem = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Julgador Filter Tests
    [Fact]
    public void WFiltro_JulgadorEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Julgador = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Julgador));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Julgador);
    }

    [Fact]
    public void WFiltro_JulgadorHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var JulgadorValue = 1;
        var filtro = new FilterInstancia
        {
            Julgador = JulgadorValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var JulgadorParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Julgador}");
        JulgadorParameter.Should().NotBeNull();
        JulgadorParameter.Value.Should().Be(JulgadorValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Julgador}] = @{DBInstanciaDicInfo.Julgador}");
    }

    [Fact]
    public void WFiltro_JulgadorWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Julgador = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Julgador = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Julgador}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Julgador = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Julgador = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Julgador}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_JulgadorAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Julgador = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Julgador}] = @{DBInstanciaDicInfo.Julgador}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_JulgadorWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Julgador = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_JulgadorWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Julgador = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#endregion
#region DateTime Tests
#region DtCad Filter Tests
    [Fact]
    public void WFiltro_DtCad_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.DtCad}], 103)");
    }

    [Fact]
    public void WFiltro_DtCad_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBInstanciaDicInfo.DtCad));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBInstanciaDicInfo.DtCad);
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterInstancia
        {
            DtCad = startDate,
            DtCad_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtCad_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtCad = string.Empty,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtCad = null,
            DtCad_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterInstancia
        {
            DtCad = minValueDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtCad_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtCad = inputDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DtAtu Filter Tests
    [Fact]
    public void WFiltro_DtAtu_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.DtAtu}], 103)");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBInstanciaDicInfo.DtAtu));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBInstanciaDicInfo.DtAtu);
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterInstancia
        {
            DtAtu = startDate,
            DtAtu_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtAtu = string.Empty,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtAtu = null,
            DtAtu_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterInstancia
        {
            DtAtu = minValueDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtAtu_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            DtAtu = inputDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region Data Filter Tests
    [Fact]
    public void WFiltro_Data_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Data}], 103)");
    }

    [Fact]
    public void WFiltro_Data_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBInstanciaDicInfo.Data));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBInstanciaDicInfo.Data);
    }

    [Fact]
    public void WFiltro_Data_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterInstancia
        {
            Data = startDate,
            Data_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_Data_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Data = string.Empty,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Data = null,
            Data_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterInstancia
        {
            Data = minValueDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_Data_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Data = inputDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region ZKeyQuando Filter Tests
    [Fact]
    public void WFiltro_ZKeyQuando_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            ZKeyQuando = testDate,
            ZKeyQuando_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.ZKeyQuando}], 103)");
    }

    [Fact]
    public void WFiltro_ZKeyQuando_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterInstancia
        {
            ZKeyQuando = testDate,
            ZKeyQuando_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBInstanciaDicInfo.ZKeyQuando));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBInstanciaDicInfo.ZKeyQuando);
    }

    [Fact]
    public void WFiltro_ZKeyQuando_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterInstancia
        {
            ZKeyQuando = startDate,
            ZKeyQuando_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_ZKeyQuando_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyQuando = string.Empty,
            ZKeyQuando_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyQuando_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyQuando = null,
            ZKeyQuando_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ZKeyQuando_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterInstancia
        {
            ZKeyQuando = minValueDate,
            ZKeyQuando_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_ZKeyQuando_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            ZKeyQuando = inputDate,
            ZKeyQuando_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#endregion
#region Bool Tests
#region Visto Filter Tests
    [Fact]
    public void WFiltro_VistoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Visto = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Visto));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Visto);
    }

    [Fact]
    public void WFiltro_VistoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var VistoValue = 1;
        var filtro = new FilterInstancia
        {
            Visto = VistoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var VistoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Visto}");
        VistoParameter.Should().NotBeNull();
        VistoParameter.Value.Should().Be(VistoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Visto}] = @{DBInstanciaDicInfo.Visto}");
    }

    [Fact]
    public void WFiltro_VistoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Visto = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Visto = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Visto}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Visto = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Visto = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Visto}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_VistoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Visto}] = @{DBInstanciaDicInfo.Visto}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LiminarPendente Filter Tests
    [Fact]
    public void WFiltro_LiminarPendenteEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPendente = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.LiminarPendente));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.LiminarPendente);
    }

    [Fact]
    public void WFiltro_LiminarPendenteHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LiminarPendenteValue = 1;
        var filtro = new FilterInstancia
        {
            LiminarPendente = LiminarPendenteValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LiminarPendenteParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarPendente}");
        LiminarPendenteParameter.Should().NotBeNull();
        LiminarPendenteParameter.Value.Should().Be(LiminarPendenteValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarPendente}] = @{DBInstanciaDicInfo.LiminarPendente}");
    }

    [Fact]
    public void WFiltro_LiminarPendenteWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LiminarPendente = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            LiminarPendente = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarPendente}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LiminarPendente = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            LiminarPendente = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarPendente}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LiminarPendenteAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarPendente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarPendente}] = @{DBInstanciaDicInfo.LiminarPendente}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LiminarPendenteWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarPendente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LiminarPendenteWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarPendente = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region InterpusemosRecurso Filter Tests
    [Fact]
    public void WFiltro_InterpusemosRecursoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            InterpusemosRecurso = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.InterpusemosRecurso));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.InterpusemosRecurso);
    }

    [Fact]
    public void WFiltro_InterpusemosRecursoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var InterpusemosRecursoValue = 1;
        var filtro = new FilterInstancia
        {
            InterpusemosRecurso = InterpusemosRecursoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var InterpusemosRecursoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.InterpusemosRecurso}");
        InterpusemosRecursoParameter.Should().NotBeNull();
        InterpusemosRecursoParameter.Value.Should().Be(InterpusemosRecursoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.InterpusemosRecurso}] = @{DBInstanciaDicInfo.InterpusemosRecurso}");
    }

    [Fact]
    public void WFiltro_InterpusemosRecursoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for InterpusemosRecurso = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            InterpusemosRecurso = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.InterpusemosRecurso}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for InterpusemosRecurso = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            InterpusemosRecurso = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.InterpusemosRecurso}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_InterpusemosRecursoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            InterpusemosRecurso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.InterpusemosRecurso}] = @{DBInstanciaDicInfo.InterpusemosRecurso}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_InterpusemosRecursoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            InterpusemosRecurso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_InterpusemosRecursoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            InterpusemosRecurso = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LiminarConcedida Filter Tests
    [Fact]
    public void WFiltro_LiminarConcedidaEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarConcedida = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.LiminarConcedida));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.LiminarConcedida);
    }

    [Fact]
    public void WFiltro_LiminarConcedidaHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LiminarConcedidaValue = 1;
        var filtro = new FilterInstancia
        {
            LiminarConcedida = LiminarConcedidaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LiminarConcedidaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarConcedida}");
        LiminarConcedidaParameter.Should().NotBeNull();
        LiminarConcedidaParameter.Value.Should().Be(LiminarConcedidaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarConcedida}] = @{DBInstanciaDicInfo.LiminarConcedida}");
    }

    [Fact]
    public void WFiltro_LiminarConcedidaWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LiminarConcedida = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            LiminarConcedida = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarConcedida}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LiminarConcedida = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            LiminarConcedida = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarConcedida}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LiminarConcedidaAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarConcedida = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarConcedida}] = @{DBInstanciaDicInfo.LiminarConcedida}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LiminarConcedidaWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarConcedida = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LiminarConcedidaWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarConcedida = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LiminarNegada Filter Tests
    [Fact]
    public void WFiltro_LiminarNegadaEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarNegada = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.LiminarNegada));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.LiminarNegada);
    }

    [Fact]
    public void WFiltro_LiminarNegadaHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LiminarNegadaValue = 1;
        var filtro = new FilterInstancia
        {
            LiminarNegada = LiminarNegadaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LiminarNegadaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarNegada}");
        LiminarNegadaParameter.Should().NotBeNull();
        LiminarNegadaParameter.Value.Should().Be(LiminarNegadaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarNegada}] = @{DBInstanciaDicInfo.LiminarNegada}");
    }

    [Fact]
    public void WFiltro_LiminarNegadaWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LiminarNegada = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            LiminarNegada = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarNegada}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LiminarNegada = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            LiminarNegada = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarNegada}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LiminarNegadaAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarNegada = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarNegada}] = @{DBInstanciaDicInfo.LiminarNegada}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LiminarNegadaWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarNegada = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LiminarNegadaWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarNegada = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LiminarParcial Filter Tests
    [Fact]
    public void WFiltro_LiminarParcialEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarParcial = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.LiminarParcial));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.LiminarParcial);
    }

    [Fact]
    public void WFiltro_LiminarParcialHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LiminarParcialValue = 1;
        var filtro = new FilterInstancia
        {
            LiminarParcial = LiminarParcialValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LiminarParcialParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarParcial}");
        LiminarParcialParameter.Should().NotBeNull();
        LiminarParcialParameter.Value.Should().Be(LiminarParcialValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarParcial}] = @{DBInstanciaDicInfo.LiminarParcial}");
    }

    [Fact]
    public void WFiltro_LiminarParcialWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LiminarParcial = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            LiminarParcial = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarParcial}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LiminarParcial = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            LiminarParcial = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarParcial}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LiminarParcialAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarParcial = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarParcial}] = @{DBInstanciaDicInfo.LiminarParcial}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LiminarParcialWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarParcial = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LiminarParcialWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarParcial = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LiminarCliente Filter Tests
    [Fact]
    public void WFiltro_LiminarClienteEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarCliente = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.LiminarCliente));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.LiminarCliente);
    }

    [Fact]
    public void WFiltro_LiminarClienteHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LiminarClienteValue = 1;
        var filtro = new FilterInstancia
        {
            LiminarCliente = LiminarClienteValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LiminarClienteParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarCliente}");
        LiminarClienteParameter.Should().NotBeNull();
        LiminarClienteParameter.Value.Should().Be(LiminarClienteValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarCliente}] = @{DBInstanciaDicInfo.LiminarCliente}");
    }

    [Fact]
    public void WFiltro_LiminarClienteWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LiminarCliente = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            LiminarCliente = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarCliente}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LiminarCliente = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            LiminarCliente = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.LiminarCliente}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LiminarClienteAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            LiminarCliente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.LiminarCliente}] = @{DBInstanciaDicInfo.LiminarCliente}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LiminarClienteWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarCliente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LiminarClienteWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            LiminarCliente = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Principal Filter Tests
    [Fact]
    public void WFiltro_PrincipalEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Principal = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBInstanciaDicInfo.Principal));
        result.Value.where.Should().NotContain(DBInstanciaDicInfo.Principal);
    }

    [Fact]
    public void WFiltro_PrincipalHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PrincipalValue = 1;
        var filtro = new FilterInstancia
        {
            Principal = PrincipalValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PrincipalParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Principal}");
        PrincipalParameter.Should().NotBeNull();
        PrincipalParameter.Value.Should().Be(PrincipalValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Principal}] = @{DBInstanciaDicInfo.Principal}");
    }

    [Fact]
    public void WFiltro_PrincipalWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Principal = 0 (typically female)
        var filtroFemale = new FilterInstancia
        {
            Principal = 0
        };
        var resultFemale = _serviceS.WFiltroInstancia(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Principal}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Principal = 1 (typically male)
        var filtroMale = new FilterInstancia
        {
            Principal = 1
        };
        var resultMale = _serviceS.WFiltroInstancia(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBInstanciaDicInfo.Principal}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_PrincipalAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Principal = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBInstanciaDicInfo.PTabelaNome}].[{DBInstanciaDicInfo.Principal}] = @{DBInstanciaDicInfo.Principal}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_PrincipalWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Principal = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_PrincipalWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterInstancia
        {
            Guid = "Guid", // Campo string para testar combinação
            Principal = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroInstancia(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}