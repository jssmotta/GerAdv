// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import CargosEscWindowId from '@/app/GerAdv_TS/CargosEsc/Crud/Grids/CargosEscWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import CargosEscWindowId from '@/app/GerAdv_TS/CargosEsc/Crud/Grids/CargosEscWindowId';
import { CargosEscEmpty } from '@/app/GerAdv_TS/Models/CargosEsc';

describe('CargosEscWindowId (basic)', () => {
  it('exports the component', () => {
    expect(CargosEscWindowId).toBeDefined();
    expect(typeof CargosEscWindowId === 'function' || typeof CargosEscWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the CargosEscService class so constructor usage doesn't throw and we can control fetchCargosEscById
const mockFetchCargosEscById = jest.fn();
jest.mock('@/app/GerAdv_TS/CargosEsc/Services/CargosEsc.service', () => ({
  CargosEscService: jest.fn().mockImplementation(() => ({
    fetchCargosEscById: (...args: any[]) => mockFetchCargosEscById(...args),
  })),
}));

// Mock the child CargosEscWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/CargosEsc/Crud/Grids/CargosEscWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='cargosesc-window' {...p} />,
}));

describe('CargosEscWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(CargosEscWindowId).toBeDefined();
    expect(typeof CargosEscWindowId === 'function' || typeof CargosEscWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <CargosEscWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders CargosEscWindow with CargosEscEmpty when id is 0', async () => {
    render(
      <CargosEscWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // CargosEscWindow should be rendered with selectedCargosEsc equal to CargosEscEmpty()
    const node = await screen.findByTestId('cargosesc-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedCargosEsc"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcargosesc') || node.getAttribute('selectedCargosEsc')).not.toBeNull();
  });

  it('fetches cargosesc by id and passes it to CargosEscWindow when id > 0', async () => {
    const fakeCargosEsc = { id: 123, nomecliente: 'Test' };
    mockFetchCargosEscById.mockResolvedValue(fakeCargosEsc);

    render(
      <CargosEscWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('cargosesc-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchCargosEscById).toHaveBeenCalledWith(123));
  });
});
