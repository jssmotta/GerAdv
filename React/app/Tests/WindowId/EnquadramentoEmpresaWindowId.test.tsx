// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import EnquadramentoEmpresaWindowId from '@/app/GerAdv_TS/EnquadramentoEmpresa/Crud/Grids/EnquadramentoEmpresaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import EnquadramentoEmpresaWindowId from '@/app/GerAdv_TS/EnquadramentoEmpresa/Crud/Grids/EnquadramentoEmpresaWindowId';
import { EnquadramentoEmpresaEmpty } from '@/app/GerAdv_TS/Models/EnquadramentoEmpresa';

describe('EnquadramentoEmpresaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(EnquadramentoEmpresaWindowId).toBeDefined();
    expect(typeof EnquadramentoEmpresaWindowId === 'function' || typeof EnquadramentoEmpresaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the EnquadramentoEmpresaService class so constructor usage doesn't throw and we can control fetchEnquadramentoEmpresaById
const mockFetchEnquadramentoEmpresaById = jest.fn();
jest.mock('@/app/GerAdv_TS/EnquadramentoEmpresa/Services/EnquadramentoEmpresa.service', () => ({
  EnquadramentoEmpresaService: jest.fn().mockImplementation(() => ({
    fetchEnquadramentoEmpresaById: (...args: any[]) => mockFetchEnquadramentoEmpresaById(...args),
  })),
}));

// Mock the child EnquadramentoEmpresaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/EnquadramentoEmpresa/Crud/Grids/EnquadramentoEmpresaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='enquadramentoempresa-window' {...p} />,
}));

describe('EnquadramentoEmpresaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(EnquadramentoEmpresaWindowId).toBeDefined();
    expect(typeof EnquadramentoEmpresaWindowId === 'function' || typeof EnquadramentoEmpresaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <EnquadramentoEmpresaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders EnquadramentoEmpresaWindow with EnquadramentoEmpresaEmpty when id is 0', async () => {
    render(
      <EnquadramentoEmpresaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // EnquadramentoEmpresaWindow should be rendered with selectedEnquadramentoEmpresa equal to EnquadramentoEmpresaEmpty()
    const node = await screen.findByTestId('enquadramentoempresa-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedEnquadramentoEmpresa"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedenquadramentoempresa') || node.getAttribute('selectedEnquadramentoEmpresa')).not.toBeNull();
  });

  it('fetches enquadramentoempresa by id and passes it to EnquadramentoEmpresaWindow when id > 0', async () => {
    const fakeEnquadramentoEmpresa = { id: 123, nomecliente: 'Test' };
    mockFetchEnquadramentoEmpresaById.mockResolvedValue(fakeEnquadramentoEmpresa);

    render(
      <EnquadramentoEmpresaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('enquadramentoempresa-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchEnquadramentoEmpresaById).toHaveBeenCalledWith(123));
  });
});
