// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useReuniaoForm, useReuniaoList, useValidationsReuniao } from '../GerAdv_TS/Reuniao/Hooks/hookReuniao';
import { IReuniao } from '../GerAdv_TS/Reuniao/Interfaces/interface.Reuniao';
import { IReuniaoService } from '../GerAdv_TS/Reuniao/Services/Reuniao.service';
import { ReuniaoTestEmpty } from '../GerAdv_TS/Models/Reuniao';


// Mock do serviço
const mockReuniaoService: jest.Mocked<IReuniaoService> = {
  fetchReuniaoById: jest.fn(),
  saveReuniao: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteReuniao: jest.fn(),
  validateReuniao: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialReuniao: IReuniao = { ...ReuniaoTestEmpty() };

describe('useReuniaoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    expect(result.current.data).toEqual(initialReuniao);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    const mockEvent = {
      target: {
        name: 'data',
        value: 'Novo Reunião',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.data).toBe('Novo Reunião');
  });

   test('deve carregar Reunião por ID', async () => {
    const mockReuniao = { ...initialReuniao, id: 1, data: 'Reunião Teste' };
    mockReuniaoService.fetchReuniaoById.mockResolvedValue(mockReuniao);

    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    await act(async () => {
      await result.current.loadReuniao(1);
    });

    expect(mockReuniaoService.fetchReuniaoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockReuniao);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Reunião', async () => {
    const errorMessage = 'Erro ao carregar Reunião';
    mockReuniaoService.fetchReuniaoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    await act(async () => {
      await result.current.loadReuniao(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialReuniao, data: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialReuniao);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    await act(async () => {
      await result.current.loadReuniao(0);
    });

    expect(mockReuniaoService.fetchReuniaoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialReuniao);
  });
});

describe('useReuniaoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useReuniaoList(mockReuniaoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialReuniao, id: 1, data: 'Reunião 1' },
      { ...initialReuniao, id: 2, data: 'Reunião 2' }
    ];
    mockReuniaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useReuniaoList(mockReuniaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockReuniaoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockReuniaoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useReuniaoList(mockReuniaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialReuniao, id: 1, data: 'Reunião Filtrado' }];
    const filtro = { data: 'Reunião' };
    mockReuniaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useReuniaoList(mockReuniaoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockReuniaoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsReuniao', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsReuniao());

    const validData = { ...initialReuniao, data: 'Reunião Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar data vazio', () => {
    const { result } = renderHook(() => useValidationsReuniao());

    const invalidData = { ...initialReuniao, data: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ficar vazio.');
  });

  
  test('deve invalidar data muito longo', () => {
    const { result } = renderHook(() => useValidationsReuniao());

    const invalidData = { 
      ...initialReuniao, 
      data: 'a'.repeat(-1+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ter mais de -1 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsReuniao());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialReuniao, id: 1, data: 'Reunião Teste' }];
    mockReuniaoService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useReuniaoList(mockReuniaoService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsReuniao()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useReuniaoForm(initialReuniao, mockReuniaoService)
    );

    const mockEvent = {
      target: {
        name: 'externa',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.externa).toBe(true);
  });


