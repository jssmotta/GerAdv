// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import NENotasWindowId from '@/app/GerAdv_TS/NENotas/Crud/Grids/NENotasWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import NENotasWindowId from '@/app/GerAdv_TS/NENotas/Crud/Grids/NENotasWindowId';
import { NENotasEmpty } from '@/app/GerAdv_TS/Models/NENotas';

describe('NENotasWindowId (basic)', () => {
  it('exports the component', () => {
    expect(NENotasWindowId).toBeDefined();
    expect(typeof NENotasWindowId === 'function' || typeof NENotasWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the NENotasService class so constructor usage doesn't throw and we can control fetchNENotasById
const mockFetchNENotasById = jest.fn();
jest.mock('@/app/GerAdv_TS/NENotas/Services/NENotas.service', () => ({
  NENotasService: jest.fn().mockImplementation(() => ({
    fetchNENotasById: (...args: any[]) => mockFetchNENotasById(...args),
  })),
}));

// Mock the child NENotasWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/NENotas/Crud/Grids/NENotasWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='nenotas-window' {...p} />,
}));

describe('NENotasWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(NENotasWindowId).toBeDefined();
    expect(typeof NENotasWindowId === 'function' || typeof NENotasWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <NENotasWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders NENotasWindow with NENotasEmpty when id is 0', async () => {
    render(
      <NENotasWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // NENotasWindow should be rendered with selectedNENotas equal to NENotasEmpty()
    const node = await screen.findByTestId('nenotas-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedNENotas"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectednenotas') || node.getAttribute('selectedNENotas')).not.toBeNull();
  });

  it('fetches nenotas by id and passes it to NENotasWindow when id > 0', async () => {
    const fakeNENotas = { id: 123, nomecliente: 'Test' };
    mockFetchNENotasById.mockResolvedValue(fakeNENotas);

    render(
      <NENotasWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('nenotas-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchNENotasById).toHaveBeenCalledWith(123));
  });
});
