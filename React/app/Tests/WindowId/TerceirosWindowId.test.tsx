// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TerceirosWindowId from '@/app/GerAdv_TS/Terceiros/Crud/Grids/TerceirosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TerceirosWindowId from '@/app/GerAdv_TS/Terceiros/Crud/Grids/TerceirosWindowId';
import { TerceirosEmpty } from '@/app/GerAdv_TS/Models/Terceiros';

describe('TerceirosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TerceirosWindowId).toBeDefined();
    expect(typeof TerceirosWindowId === 'function' || typeof TerceirosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TerceirosService class so constructor usage doesn't throw and we can control fetchTerceirosById
const mockFetchTerceirosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Terceiros/Services/Terceiros.service', () => ({
  TerceirosService: jest.fn().mockImplementation(() => ({
    fetchTerceirosById: (...args: any[]) => mockFetchTerceirosById(...args),
  })),
}));

// Mock the child TerceirosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Terceiros/Crud/Grids/TerceirosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='terceiros-window' {...p} />,
}));

describe('TerceirosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TerceirosWindowId).toBeDefined();
    expect(typeof TerceirosWindowId === 'function' || typeof TerceirosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TerceirosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TerceirosWindow with TerceirosEmpty when id is 0', async () => {
    render(
      <TerceirosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TerceirosWindow should be rendered with selectedTerceiros equal to TerceirosEmpty()
    const node = await screen.findByTestId('terceiros-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTerceiros"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedterceiros') || node.getAttribute('selectedTerceiros')).not.toBeNull();
  });

  it('fetches terceiros by id and passes it to TerceirosWindow when id > 0', async () => {
    const fakeTerceiros = { id: 123, nomecliente: 'Test' };
    mockFetchTerceirosById.mockResolvedValue(fakeTerceiros);

    render(
      <TerceirosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('terceiros-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTerceirosById).toHaveBeenCalledWith(123));
  });
});
