// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // CrudWindow.tsx
'use client';
import React, { useEffect } from 'react';
import { EditWindow } from '@/app/components/Cruds/EditWindow';
import AgendaSemanaInc from '../Inc/AgendaSemana';
import { IAgendaSemana } from '../../Interfaces/interface.AgendaSemana';
import { useIsMobile } from '@/app/context/MobileContext';
import { AgendaSemanaEmpty } from '@/app/GerAdv_TS/Models/AgendaSemana';
import { useWindow } from '@/app/hooks/useWindows';
interface AgendaSemanaWindowProps {
  isOpen: boolean;
  onClose: () => void;
  dimensions?: { width: number; height: number };
  selectedAgendaSemana?: IAgendaSemana;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const AgendaSemanaWindow: React.FC<AgendaSemanaWindowProps> = ({
  isOpen, 
  onClose, 
  dimensions, 
  selectedAgendaSemana, 
  onSuccess, 
  onError, 
}) => {

const isMobile = useIsMobile();
const dimensionsEmpty = useWindow();
if (!isOpen)
  return null;
  return (
  <>

  <EditWindow
  tableTitle='Agenda Semana'
  isOpen={isOpen}
  onClose={onClose}
  dimensions={dimensions ?? dimensionsEmpty}
  newHeight={630}
  newWidth={1250}
  mobile={isMobile}
  id={(selectedAgendaSemana?.id ?? 0).toString()}
>
<AgendaSemanaInc
id={selectedAgendaSemana?.id ?? 0}
onClose={onClose}
onSuccess={onSuccess}
onError={onError}
/>
</EditWindow>

</>
);
};
export const NewWindowAgendaSemana: React.FC<AgendaSemanaWindowProps> = ({
  isOpen, 
  onClose, 
}) => {
const dimensions = useWindow();
return (
<AgendaSemanaWindow
isOpen={isOpen}
onClose={onClose}
dimensions={dimensions}
onSuccess={onClose}
onError={onClose}
selectedAgendaSemana={AgendaSemanaEmpty()}>
</AgendaSemanaWindow>
)
};
export default AgendaSemanaWindow;