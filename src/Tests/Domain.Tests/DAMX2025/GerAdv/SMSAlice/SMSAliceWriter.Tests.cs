// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class SMSAliceWriterTests
{
    private readonly Mock<IFSMSAliceFactory> _mockSMSAliceFactory;
    private readonly Mock<FSMSAlice> _mockFSMSAlice;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly SMSAliceWriter _smsaliceWriter;
    public SMSAliceWriterTests()
    {
        _mockSMSAliceFactory = new Mock<IFSMSAliceFactory>();
        _mockFSMSAlice = new Mock<FSMSAlice>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _smsaliceWriter = new SMSAliceWriter(_mockSMSAliceFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var smsalice = CreateValidSMSAliceModel();
        smsalice.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockSMSAliceFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSMSAlice.Object);
        _mockFSMSAlice.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _smsaliceWriter.WriteAsync(smsalice, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSMSAlice.Object);
        _mockSMSAliceFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockSMSAliceFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFSMSAlice.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var smsalice = CreateValidSMSAliceModel();
        smsalice.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockSMSAliceFactory.Setup(x => x.CreateFromIdAsync(smsalice.Id, _mockConnection.Object)).ReturnsAsync(_mockFSMSAlice.Object);
        _mockFSMSAlice.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _smsaliceWriter.WriteAsync(smsalice, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSMSAlice.Object);
        _mockSMSAliceFactory.Verify(x => x.CreateFromIdAsync(smsalice.Id, _mockConnection.Object), Times.Once);
        _mockSMSAliceFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFSMSAlice.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var smsalice = CreateValidSMSAliceModel();
        var auditorQuem = 99;
        _mockSMSAliceFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSMSAlice.Object);
        _mockFSMSAlice.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _smsaliceWriter.WriteAsync(smsalice, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSMSAlice.Object);
        _mockFSMSAlice.VerifySet(x => x.FOperador = smsalice.Operador, Times.Once);
        _mockFSMSAlice.VerifySet(x => x.FNome = smsalice.Nome, Times.Once);
        _mockFSMSAlice.VerifySet(x => x.FTipoEMail = smsalice.TipoEMail, Times.Once);
        _mockFSMSAlice.VerifySet(x => x.FGUID = smsalice.GUID, Times.Once);
        _mockFSMSAlice.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var smsalice = CreateValidSMSAliceModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSMSAliceFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _smsaliceWriter.WriteAsync(smsalice, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var smsalice = CreateValidSMSAliceModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSMSAliceFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSMSAlice.Object);
        _mockFSMSAlice.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _smsaliceWriter.WriteAsync(smsalice, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var smsaliceResponse = new SMSAliceResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockSMSAliceFactory.Setup(x => x.DeleteAsync(operadorId, smsaliceResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _smsaliceWriter.Delete(smsaliceResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockSMSAliceFactory.Verify(x => x.DeleteAsync(operadorId, smsaliceResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var smsaliceResponse = new SMSAliceResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockSMSAliceFactory.Setup(x => x.DeleteAsync(operadorId, smsaliceResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _smsaliceWriter.Delete(smsaliceResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var smsaliceResponse = new SMSAliceResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockSMSAliceFactory.Setup(x => x.DeleteAsync(operadorId, smsaliceResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _smsaliceWriter.Delete(smsaliceResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new SMSAliceWriter(_mockSMSAliceFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new SMSAliceWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.SMSAlice CreateValidSMSAliceModel()
    {
        return new Models.SMSAlice
        {
            Id = 0,
            Operador = 1,
            Nome = "João",
            TipoEMail = 1,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}