// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import EscritoriosWindowId from '@/app/GerAdv_TS/Escritorios/Crud/Grids/EscritoriosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import EscritoriosWindowId from '@/app/GerAdv_TS/Escritorios/Crud/Grids/EscritoriosWindowId';
import { EscritoriosEmpty } from '@/app/GerAdv_TS/Models/Escritorios';

describe('EscritoriosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(EscritoriosWindowId).toBeDefined();
    expect(typeof EscritoriosWindowId === 'function' || typeof EscritoriosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the EscritoriosService class so constructor usage doesn't throw and we can control fetchEscritoriosById
const mockFetchEscritoriosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Escritorios/Services/Escritorios.service', () => ({
  EscritoriosService: jest.fn().mockImplementation(() => ({
    fetchEscritoriosById: (...args: any[]) => mockFetchEscritoriosById(...args),
  })),
}));

// Mock the child EscritoriosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Escritorios/Crud/Grids/EscritoriosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='escritorios-window' {...p} />,
}));

describe('EscritoriosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(EscritoriosWindowId).toBeDefined();
    expect(typeof EscritoriosWindowId === 'function' || typeof EscritoriosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <EscritoriosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders EscritoriosWindow with EscritoriosEmpty when id is 0', async () => {
    render(
      <EscritoriosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // EscritoriosWindow should be rendered with selectedEscritorios equal to EscritoriosEmpty()
    const node = await screen.findByTestId('escritorios-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedEscritorios"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedescritorios') || node.getAttribute('selectedEscritorios')).not.toBeNull();
  });

  it('fetches escritorios by id and passes it to EscritoriosWindow when id > 0', async () => {
    const fakeEscritorios = { id: 123, nomecliente: 'Test' };
    mockFetchEscritoriosById.mockResolvedValue(fakeEscritorios);

    render(
      <EscritoriosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('escritorios-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchEscritoriosById).toHaveBeenCalledWith(123));
  });
});
