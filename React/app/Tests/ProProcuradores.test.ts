// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProProcuradoresForm, useProProcuradoresList, useValidationsProProcuradores } from '../GerAdv_TS/ProProcuradores/Hooks/hookProProcuradores';
import { IProProcuradores } from '../GerAdv_TS/ProProcuradores/Interfaces/interface.ProProcuradores';
import { IProProcuradoresService } from '../GerAdv_TS/ProProcuradores/Services/ProProcuradores.service';
import { ProProcuradoresTestEmpty } from '../GerAdv_TS/Models/ProProcuradores';
import { useProProcuradoresComboBox } from '../GerAdv_TS/ProProcuradores/Hooks/hookProProcuradores';

// Mock do serviço
const mockProProcuradoresService: jest.Mocked<IProProcuradoresService> = {
  fetchProProcuradoresById: jest.fn(),
  saveProProcuradores: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteProProcuradores: jest.fn(),
  validateProProcuradores: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProProcuradores: IProProcuradores = { ...ProProcuradoresTestEmpty() };

describe('useProProcuradoresForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    expect(result.current.data).toEqual(initialProProcuradores);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Pro Procuradores',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Pro Procuradores');
  });

   test('deve carregar Pro Procuradores por ID', async () => {
    const mockProProcuradores = { ...initialProProcuradores, id: 1, nome: 'Pro Procuradores Teste' };
    mockProProcuradoresService.fetchProProcuradoresById.mockResolvedValue(mockProProcuradores);

    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.loadProProcuradores(1);
    });

    expect(mockProProcuradoresService.fetchProProcuradoresById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProProcuradores);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Pro Procuradores', async () => {
    const errorMessage = 'Erro ao carregar Pro Procuradores';
    mockProProcuradoresService.fetchProProcuradoresById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.loadProProcuradores(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProProcuradores, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProProcuradores);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.loadProProcuradores(0);
    });

    expect(mockProProcuradoresService.fetchProProcuradoresById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProProcuradores);
  });
});

describe('useProProcuradoresList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProProcuradoresList(mockProProcuradoresService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProProcuradores, id: 1, nome: 'Pro Procuradores 1' },
      { ...initialProProcuradores, id: 2, nome: 'Pro Procuradores 2' }
    ];
    mockProProcuradoresService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProProcuradoresList(mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProProcuradoresService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProProcuradoresService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProProcuradoresList(mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProProcuradores, id: 1, nome: 'Pro Procuradores Filtrado' }];
    const filtro = { nome: 'Pro Procuradores' };
    mockProProcuradoresService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProProcuradoresList(mockProProcuradoresService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProProcuradoresService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProProcuradores', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProProcuradores());

    const validData = { ...initialProProcuradores, nome: 'Pro Procuradores Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsProProcuradores());

    const invalidData = { ...initialProProcuradores, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsProProcuradores());

    const invalidData = { 
      ...initialProProcuradores, 
      nome: 'a'.repeat(255+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 255 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProProcuradores());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProProcuradores, id: 1, nome: 'Pro Procuradores Teste' }];
    mockProProcuradoresService.getAll.mockResolvedValue(mockData);
    mockProProcuradoresService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProProcuradoresList(mockProProcuradoresService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsProProcuradores()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Pro Procuradores Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Pro Procuradores Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useProProcuradoresForm(initialProProcuradores, mockProProcuradoresService)
    );

    const mockEvent = {
      target: {
        name: 'substabelecimento',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.substabelecimento).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Pro Procuradores 1' },
      { id: 2, nome: 'Pro Procuradores 2' }
    ];
    mockProProcuradoresService.getList.mockResolvedValue(mockOptions as IProProcuradores[]);


    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Pro Procuradores 1' },
        { id: 2, nome: 'Pro Procuradores 2' }
      ]);
    });


    expect(mockProProcuradoresService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Pro Procuradores ABC' },
      { id: 2, nome: 'Pro Procuradores XYZ' }
    ];
    mockProProcuradoresService.getList.mockResolvedValue(mockOptions as IProProcuradores[]);   


 const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Pro Procuradores ABC' },
        { id: 2, nome: 'Pro Procuradores XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Pro Procuradores ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Pro Procuradores ABC' },
      { id: 2, nome: 'Pro Procuradores XYZ' }
    ];
    mockProProcuradoresService.getList.mockResolvedValue(mockOptions as IProProcuradores[]);
  


    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Pro Procuradores ABC' },
        { id: 2, nome: 'Pro Procuradores XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Pro Procuradores ABC' },
          {id: 2, nome: 'Pro Procuradores XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );

    const newValue = { id: 1, nome: 'Pro Procuradores Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Pro Procuradores Inicial' };
    
    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useProProcuradoresComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Pro Procuradores Inicial' };
    
    const { result } = renderHook(() => 
      useProProcuradoresComboBox(mockProProcuradoresService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






