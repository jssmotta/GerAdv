// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useOponentesForm, useOponentesList, useValidationsOponentes } from '../GerAdv_TS/Oponentes/Hooks/hookOponentes';
import { IOponentes } from '../GerAdv_TS/Oponentes/Interfaces/interface.Oponentes';
import { IOponentesService } from '../GerAdv_TS/Oponentes/Services/Oponentes.service';
import { OponentesTestEmpty } from '../GerAdv_TS/Models/Oponentes';
import { useOponentesComboBox } from '../GerAdv_TS/Oponentes/Hooks/hookOponentes';

// Mock do serviço
const mockOponentesService: jest.Mocked<IOponentesService> = {
  fetchOponentesById: jest.fn(),
  saveOponentes: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteOponentes: jest.fn(),
  validateOponentes: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialOponentes: IOponentes = { ...OponentesTestEmpty() };

describe('useOponentesForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    expect(result.current.data).toEqual(initialOponentes);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Oponentes',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Oponentes');
  });

   test('deve carregar Oponentes por ID', async () => {
    const mockOponentes = { ...initialOponentes, id: 1, nome: 'Oponentes Teste' };
    mockOponentesService.fetchOponentesById.mockResolvedValue(mockOponentes);

    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    await act(async () => {
      await result.current.loadOponentes(1);
    });

    expect(mockOponentesService.fetchOponentesById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockOponentes);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Oponentes', async () => {
    const errorMessage = 'Erro ao carregar Oponentes';
    mockOponentesService.fetchOponentesById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    await act(async () => {
      await result.current.loadOponentes(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialOponentes, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialOponentes);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    await act(async () => {
      await result.current.loadOponentes(0);
    });

    expect(mockOponentesService.fetchOponentesById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialOponentes);
  });
});

describe('useOponentesList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOponentesList(mockOponentesService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialOponentes, id: 1, nome: 'Oponentes 1' },
      { ...initialOponentes, id: 2, nome: 'Oponentes 2' }
    ];
    mockOponentesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOponentesList(mockOponentesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockOponentesService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockOponentesService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useOponentesList(mockOponentesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialOponentes, id: 1, nome: 'Oponentes Filtrado' }];
    const filtro = { nome: 'Oponentes' };
    mockOponentesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useOponentesList(mockOponentesService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockOponentesService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsOponentes', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsOponentes());

    const validData = { ...initialOponentes, nome: 'Oponentes Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsOponentes());

    const invalidData = { ...initialOponentes, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsOponentes());

    const invalidData = { 
      ...initialOponentes, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsOponentes());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialOponentes, id: 1, nome: 'Oponentes Teste' }];
    mockOponentesService.getAll.mockResolvedValue(mockData);
    mockOponentesService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useOponentesList(mockOponentesService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsOponentes()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Oponentes Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Oponentes Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useOponentesForm(initialOponentes, mockOponentesService)
    );

    const mockEvent = {
      target: {
        name: 'juridica',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.juridica).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Oponentes 1' },
      { id: 2, nome: 'Oponentes 2' }
    ];
    mockOponentesService.getList.mockResolvedValue(mockOptions as IOponentes[]);


    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Oponentes 1' },
        { id: 2, nome: 'Oponentes 2' }
      ]);
    });


    expect(mockOponentesService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Oponentes ABC' },
      { id: 2, nome: 'Oponentes XYZ' }
    ];
    mockOponentesService.getList.mockResolvedValue(mockOptions as IOponentes[]);   


 const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Oponentes ABC' },
        { id: 2, nome: 'Oponentes XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Oponentes ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Oponentes ABC' },
      { id: 2, nome: 'Oponentes XYZ' }
    ];
    mockOponentesService.getList.mockResolvedValue(mockOptions as IOponentes[]);
  


    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Oponentes ABC' },
        { id: 2, nome: 'Oponentes XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Oponentes ABC' },
          {id: 2, nome: 'Oponentes XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );

    const newValue = { id: 1, nome: 'Oponentes Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Oponentes Inicial' };
    
    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useOponentesComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Oponentes Inicial' };
    
    const { result } = renderHook(() => 
      useOponentesComboBox(mockOponentesService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






