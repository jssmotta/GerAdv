// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProcessosObsReportWindowId from '@/app/GerAdv_TS/ProcessosObsReport/Crud/Grids/ProcessosObsReportWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProcessosObsReportWindowId from '@/app/GerAdv_TS/ProcessosObsReport/Crud/Grids/ProcessosObsReportWindowId';
import { ProcessosObsReportEmpty } from '@/app/GerAdv_TS/Models/ProcessosObsReport';

describe('ProcessosObsReportWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProcessosObsReportWindowId).toBeDefined();
    expect(typeof ProcessosObsReportWindowId === 'function' || typeof ProcessosObsReportWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProcessosObsReportService class so constructor usage doesn't throw and we can control fetchProcessosObsReportById
const mockFetchProcessosObsReportById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProcessosObsReport/Services/ProcessosObsReport.service', () => ({
  ProcessosObsReportService: jest.fn().mockImplementation(() => ({
    fetchProcessosObsReportById: (...args: any[]) => mockFetchProcessosObsReportById(...args),
  })),
}));

// Mock the child ProcessosObsReportWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProcessosObsReport/Crud/Grids/ProcessosObsReportWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='processosobsreport-window' {...p} />,
}));

describe('ProcessosObsReportWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProcessosObsReportWindowId).toBeDefined();
    expect(typeof ProcessosObsReportWindowId === 'function' || typeof ProcessosObsReportWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProcessosObsReportWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProcessosObsReportWindow with ProcessosObsReportEmpty when id is 0', async () => {
    render(
      <ProcessosObsReportWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProcessosObsReportWindow should be rendered with selectedProcessosObsReport equal to ProcessosObsReportEmpty()
    const node = await screen.findByTestId('processosobsreport-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProcessosObsReport"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprocessosobsreport') || node.getAttribute('selectedProcessosObsReport')).not.toBeNull();
  });

  it('fetches processosobsreport by id and passes it to ProcessosObsReportWindow when id > 0', async () => {
    const fakeProcessosObsReport = { id: 123, nomecliente: 'Test' };
    mockFetchProcessosObsReportById.mockResolvedValue(fakeProcessosObsReport);

    render(
      <ProcessosObsReportWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('processosobsreport-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProcessosObsReportById).toHaveBeenCalledWith(123));
  });
});
