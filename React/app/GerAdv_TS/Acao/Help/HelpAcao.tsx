// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:C:\MenphisSI\TEMP\SG\GerAdv_TS\Acao\Help\HelpAcao.tsx

"use client";
import React from "react";
import { EditWindow } from "@/app/components/Cruds/EditWindow";
import HelpCrud, { HelpField } from "@/app/components/Cruds/Help/HelpCrud";
import { useWindow } from "@/app/hooks/useWindows";
import { useIsMobile } from "@/app/context/MobileContext";
import {
  help_entityTitle,
  help_infoDescription,
  help_lastUpdate,
  help_helpFields,
} from "./AcaoHelpDescriptions";
import { PMaxWidthHelpWindow } from "@/app/tools/help";

interface HelpAcaoProps {
  onClose?: () => void;
  recursive?: boolean;
}

const HelpAcao: React.FC<HelpAcaoProps> = ({ onClose, recursive = false }) => {
  const dimensions = useWindow();
  const isMobile = useIsMobile();

  return (
    <EditWindow
      tableTitle={`Ajuda`}
      isOpen={true}
      onClose={onClose ?? (() => {})}
      dimensions={dimensions}
      maxWidth={PMaxWidthHelpWindow}
      mobile={isMobile}
      crud={true}
    >
      <HelpCrud
        title={help_entityTitle}
        infoDescription={help_infoDescription}
        fields={help_helpFields}
        lastUpdate={help_lastUpdate}
        recursive={recursive}
      />
    </EditWindow>
  );
};

export default HelpAcao;
