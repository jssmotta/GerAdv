// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using MenphisSI.GerAdv.Models.Response.All;
using MenphisSI.GerAdv.Readers;
using System.Data;

namespace MenphisSI.GerAdv.Tests.Readers;
/// <summary>
/// Comprehensive unit tests for UFReader class
/// Tests all public methods including async operations, data reading, and data transformation
/// </summary>
public class UFReaderTests : IDisposable
{
    private readonly Mock<IFUFFactory> _mockUFFactory;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IDataRecord> _mockDataRecord;
    private readonly UFReader _ufReader;
    public UFReaderTests()
    {
        _mockUFFactory = new Mock<IFUFFactory>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockDataRecord = new Mock<IDataRecord>();
        _ufReader = new UFReader(_mockUFFactory.Object);
    }

#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldInitializeReader()
    {
        // Act & Assert
        _ufReader.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new UFReader(null !));
    }

#endregion
#region ListarN Tests
    [Fact]
    public async Task ListarN_WithValidParameters_ShouldCallDevourerSqlData()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "ufCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "ufid";
        // Act & Assert
        // Since this calls static methods and external dependencies,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _ufReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task ListarN_WithEmptyParameters_ShouldThrowException()
    {
        // Arrange
        var max = 0;
        var uri = string.Empty; // Empty URI should cause an exception
        var cWhere = string.Empty;
        var parameters = new List<SqlParameter>();
        var order = string.Empty;
        // Act & Assert
        await Assert.ThrowsAsync<Exception>(() => _ufReader.ListarN(max, uri, cWhere, parameters, order));
    }

#endregion
#region Listar Tests
    [Fact]
    public async Task Listar_WithValidParameters_ShouldCallListarTabela()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "ufCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // Since this calls external dependencies and database connections,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _ufReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

    [Fact]
    public async Task Listar_WithCancellationToken_ShouldRespectCancellation()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri";
        var cWhere = "ufCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = new CancellationToken(true); // Already cancelled
        // Act & Assert
        // Even with cancellation, this should throw an exception due to invalid URI
        await Assert.ThrowsAsync<Exception>(() => _ufReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Read Tests with ID and Connection
    [Fact]
    public async Task Read_WithValidId_ShouldReturnUFResponse()
    {
        // Arrange
        var id = 123;
        var expectedUF = new FUF
        {
            ID = id,
            FID = "AA"
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedUF);
        // Act
        var result = await _ufReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public async Task Read_WithEmptyId_ShouldReturnNull()
    {
        // Arrange
        var id = 0; // Empty ID
        var emptyFUF = new FUF
        {
            ID = 0
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFUF);
        // Act
        var result = await _ufReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public async Task Read_WithNegativeId_ShouldReturnNull()
    {
        // Arrange
        var id = -1;
        var emptyFUF = new FUF
        {
            ID = 0
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFUF);
        // Act
        var result = await _ufReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region ReadM Tests
    [Fact]
    public async Task ReadM_WithValidId_ShouldReturnUFModel()
    {
        // Arrange
        var id = 123;
        var expectedFUF = new FUF
        {
            ID = id,
            FID = "AA"
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFUF);
        // Act
        var result = await _ufReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public async Task ReadM_WithEmptyId_ShouldReturnModelWithDefaults()
    {
        // Arrange
        var id = 0;
        var emptyFUF = new FUF
        {
            ID = 0,
            FID = null
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFUF);
        // Act
        var result = await _ufReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(0);
        result?.IdUF.Should().Be(string.Empty);
    }

#endregion
#region Read with FUF and Connection Tests
    [Fact]
    public void Read_WithValidFUFAndConnection_ShouldReturnUFResponse()
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = 123,
            FID = "AA"
        };
        // Act
        var result = _ufReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void Read_WithNullFUFAndConnection_ShouldReturnNull()
    {
        // Arrange
        FUF? dbRec = null;
        // Act
        var result = _ufReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with Where and Parameters Tests
    [Fact]
    public void Read_WithWhereAndParameters_ShouldReturnUFResponse()
    {
        // Arrange
        var where = "ufCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedFUF = new FUF
        {
            ID = 123,
            FID = "AA"
        };
        _mockUFFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(expectedFUF);
        // Act
        var result = _ufReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void Read_WithWhereAndParameters_WhenRecordNotFound_ShouldReturnNull()
    {
        // Arrange
        var where = "ufCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 999)
        };
        var emptyFUF = new FUF
        {
            ID = 0
        };
        _mockUFFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(emptyFUF);
        // Act
        var result = _ufReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with FUF Tests
    [Fact]
    public void Read_WithValidFUF_ShouldReturnUFResponse()
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = 123,
            FID = "AA"
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void Read_WithNullFUF_ShouldReturnNull()
    {
        // Arrange
        FUF? dbRec = null;
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithFUFNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = 123,
            FID = null
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be(string.Empty);
    }

#endregion
#region Read with DBUF Tests
    [Fact]
    public void Read_WithValidSGDBUF_ShouldReturnUFResponse()
    {
        // Arrange
        var dbRec = new DBUF
        {
            ID = 123,
            FID = "AA"
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void Read_WithNullSGDBUF_ShouldReturnNull()
    {
        // Arrange
        DBUF? dbRec = null;
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithSGDBUFNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBUF
        {
            ID = 123,
            FID = null // This should result in empty string in response
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with FUF and IDataRecord Tests
    [Fact]
    public void ReadAll_WithValidFUFAndDataRecord_ShouldReturnUFResponseAll()
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = 123,
            FID = "AA"
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void ReadAll_WithNullFUFAndDataRecord_ShouldReturnNull()
    {
        // Arrange
        FUF? dbRec = null;
        // Act
        var result = _ufReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithFUFNullNomeAndDataRecord_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = 123,
            FID = null
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with DBUF and DataRow Tests
    [Fact]
    public void ReadAll_WithValidSGDBUFAndDataRow_ShouldReturnUFResponseAll()
    {
        // Arrange
        var dbRec = new DBUF
        {
            ID = 123,
            FID = "AA"
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be("AA");
    }

    [Fact]
    public void ReadAll_WithNullSGDBUFAndDataRow_ShouldReturnNull()
    {
        // Arrange
        DBUF? dbRec = null;
        // Act
        var result = _ufReader.ReadAll(dbRec, null);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithSGDBUFNullNomeAndDataRow_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBUF
        {
            ID = 123,
            FID = null // This should result in empty string in response
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.IdUF.Should().Be(string.Empty);
    }

#endregion
#region Edge Cases and Error Handling Tests
    [Fact]
    public async Task Read_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _ufReader.Read(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task ReadM_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _ufReader.ReadM(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public void Read_WithWhereParameters_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var where = "ufCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedException = new InvalidOperationException("Factory error");
        _mockUFFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Throws(expectedException);
        // Act & Assert
        var exception = Assert.Throws<InvalidOperationException>(() => _ufReader.Read(where, parameters, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Data Consistency Tests
    [Theory]
    [InlineData(1, "AA")]
    [InlineData(999, "B")]
    [InlineData(0, "")]
    public void Read_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = id,
            FID = nome
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        if (id == 0)
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
        }
        else
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
            result?.IdUF.Should().Be(nome);
        }
    }

    [Theory]
    [InlineData(1, "AA")]
    [InlineData(999, "B")]
    [InlineData(0, "")]
    public void ReadAll_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FUF
        {
            ID = id,
            FID = nome
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.IdUF.Should().Be(nome);
    }

#endregion
#region Async Behavior Tests
    [Fact]
    public async Task AsyncMethods_ShouldCompleteWithoutBlocking()
    {
        // Arrange
        var id = 123;
        var expectedFUF = new FUF
        {
            ID = id,
            FID = "AA"
        };
        _mockUFFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFUF);
        // Act & Assert
        // Test only the methods that don't depend on external URI connections
        var readTask = _ufReader.Read(id, _mockConnection.Object);
        var readMTask = _ufReader.ReadM(id, _mockConnection.Object);
        await Task.WhenAll(readTask, readMTask);
        // If we reach here, async methods completed without deadlock
        Assert.True(true);
    }

    [Fact]
    public async Task ListarN_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _ufReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task Listar_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _ufReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Parameter Validation Tests
    [Fact]
    public async Task Read_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        // This would typically throw an exception or handle gracefully
        // depending on the implementation
        try
        {
            await _ufReader.Read(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public async Task ReadM_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        try
        {
            await _ufReader.ReadM(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public void Read_WithNullParametersList_ShouldHandleGracefully()
    {
        // Arrange
        var where = "ufCodigo = @id";
        // Act & Assert
        try
        {
            _ufReader.Read(where, null !, _mockConnection.Object);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

#endregion
#region Integration-like Tests
    [Fact]
    public void Read_FUF_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 456;
        var testNome = "AA";
        var dbRec = new FUF
        {
            ID = testId,
            FID = testNome
        };
        // Act
        var result = _ufReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<UFResponse>();
        result!.Id.Should().Be(testId);
        result.IdUF.Should().Be(testNome);
    }

    [Fact]
    public void ReadAll_FUF_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 789;
        var testNome = "AA";
        var dbRec = new FUF
        {
            ID = testId,
            FID = testNome
        };
        // Act
        var result = _ufReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<UFResponseAll>();
        result!.Id.Should().Be(testId);
        result.IdUF.Should().Be(testNome);
    }

#endregion
#region Dispose
    public virtual void Dispose()
    {
        // Clean up any resources if needed
        GC.SuppressFinalize(this);
    }
#endregion
}