// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import OutrasPartesClienteWindowId from '@/app/GerAdv_TS/OutrasPartesCliente/Crud/Grids/OutrasPartesClienteWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import OutrasPartesClienteWindowId from '@/app/GerAdv_TS/OutrasPartesCliente/Crud/Grids/OutrasPartesClienteWindowId';
import { OutrasPartesClienteEmpty } from '@/app/GerAdv_TS/Models/OutrasPartesCliente';

describe('OutrasPartesClienteWindowId (basic)', () => {
  it('exports the component', () => {
    expect(OutrasPartesClienteWindowId).toBeDefined();
    expect(typeof OutrasPartesClienteWindowId === 'function' || typeof OutrasPartesClienteWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the OutrasPartesClienteService class so constructor usage doesn't throw and we can control fetchOutrasPartesClienteById
const mockFetchOutrasPartesClienteById = jest.fn();
jest.mock('@/app/GerAdv_TS/OutrasPartesCliente/Services/OutrasPartesCliente.service', () => ({
  OutrasPartesClienteService: jest.fn().mockImplementation(() => ({
    fetchOutrasPartesClienteById: (...args: any[]) => mockFetchOutrasPartesClienteById(...args),
  })),
}));

// Mock the child OutrasPartesClienteWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/OutrasPartesCliente/Crud/Grids/OutrasPartesClienteWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='outraspartescliente-window' {...p} />,
}));

describe('OutrasPartesClienteWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(OutrasPartesClienteWindowId).toBeDefined();
    expect(typeof OutrasPartesClienteWindowId === 'function' || typeof OutrasPartesClienteWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <OutrasPartesClienteWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders OutrasPartesClienteWindow with OutrasPartesClienteEmpty when id is 0', async () => {
    render(
      <OutrasPartesClienteWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // OutrasPartesClienteWindow should be rendered with selectedOutrasPartesCliente equal to OutrasPartesClienteEmpty()
    const node = await screen.findByTestId('outraspartescliente-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedOutrasPartesCliente"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedoutraspartescliente') || node.getAttribute('selectedOutrasPartesCliente')).not.toBeNull();
  });

  it('fetches outraspartescliente by id and passes it to OutrasPartesClienteWindow when id > 0', async () => {
    const fakeOutrasPartesCliente = { id: 123, nomecliente: 'Test' };
    mockFetchOutrasPartesClienteById.mockResolvedValue(fakeOutrasPartesCliente);

    render(
      <OutrasPartesClienteWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('outraspartescliente-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchOutrasPartesClienteById).toHaveBeenCalledWith(123));
  });
});
