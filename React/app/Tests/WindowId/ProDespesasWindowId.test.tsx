// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProDespesasWindowId from '@/app/GerAdv_TS/ProDespesas/Crud/Grids/ProDespesasWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProDespesasWindowId from '@/app/GerAdv_TS/ProDespesas/Crud/Grids/ProDespesasWindowId';
import { ProDespesasEmpty } from '@/app/GerAdv_TS/Models/ProDespesas';

describe('ProDespesasWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProDespesasWindowId).toBeDefined();
    expect(typeof ProDespesasWindowId === 'function' || typeof ProDespesasWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProDespesasService class so constructor usage doesn't throw and we can control fetchProDespesasById
const mockFetchProDespesasById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProDespesas/Services/ProDespesas.service', () => ({
  ProDespesasService: jest.fn().mockImplementation(() => ({
    fetchProDespesasById: (...args: any[]) => mockFetchProDespesasById(...args),
  })),
}));

// Mock the child ProDespesasWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProDespesas/Crud/Grids/ProDespesasWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='prodespesas-window' {...p} />,
}));

describe('ProDespesasWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProDespesasWindowId).toBeDefined();
    expect(typeof ProDespesasWindowId === 'function' || typeof ProDespesasWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProDespesasWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProDespesasWindow with ProDespesasEmpty when id is 0', async () => {
    render(
      <ProDespesasWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProDespesasWindow should be rendered with selectedProDespesas equal to ProDespesasEmpty()
    const node = await screen.findByTestId('prodespesas-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProDespesas"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprodespesas') || node.getAttribute('selectedProDespesas')).not.toBeNull();
  });

  it('fetches prodespesas by id and passes it to ProDespesasWindow when id > 0', async () => {
    const fakeProDespesas = { id: 123, nomecliente: 'Test' };
    mockFetchProDespesasById.mockResolvedValue(fakeProDespesas);

    render(
      <ProDespesasWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('prodespesas-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProDespesasById).toHaveBeenCalledWith(123));
  });
});
