// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IAgendaSemana } from "@/app/GerAdv_TS/AgendaSemana/Interfaces/interface.AgendaSemana";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { AgendaSemanaApi } from "../../Apis/AgendaSemanaApi";
import { useValidationsAgendaSemana } from "../../Hooks/hookAgendaSemana";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";

import AgendaSemanaPage from "@/app/pages/agendasemana/page";

import FuncionariosComboBox from "@/app/GerAdv_TS/Funcionarios/ComboBox/FuncionariosComboBox";
import AdvogadosComboBox from "@/app/GerAdv_TS/Advogados/ComboBox/AdvogadosComboBox";
import TipoCompromissoComboBox from "@/app/GerAdv_TS/TipoCompromisso/ComboBox/TipoCompromissoComboBox";
import ClientesComboBox from "@/app/GerAdv_TS/Clientes/ComboBox/ClientesComboBox";
import { FuncionariosApi } from "@/app/GerAdv_TS/Funcionarios/Apis/FuncionariosApi";
import { AdvogadosApi } from "@/app/GerAdv_TS/Advogados/Apis/AdvogadosApi";
import { TipoCompromissoApi } from "@/app/GerAdv_TS/TipoCompromisso/Apis/TipoCompromissoApi";
import { ClientesApi } from "@/app/GerAdv_TS/Clientes/Apis/ClientesApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface AgendaSemanaFormProps {
  agendasemanaData: IAgendaSemana;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const AgendaSemanaForm: React.FC<AgendaSemanaFormProps> = ({
  agendasemanaData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new AgendaSemanaApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsAgendaSemana();

  const [_nomeFuncionarios, setNomeFuncionarios] = useState("");
  const funcionariosApi = new FuncionariosApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeAdvogados, setNomeAdvogados] = useState("");
  const advogadosApi = new AdvogadosApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeTipoCompromisso, setNomeTipoCompromisso] = useState("");
  const tipocompromissoApi = new TipoCompromissoApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeClientes, setNomeClientes] = useState("");
  const clientesApi = new ClientesApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("funcionarios") > 0) {
    if (agendasemanaData.id === 0 && agendasemanaData.funcionario == 0) {
      funcionariosApi
        .getById(getParamFromUrl("funcionarios"))
        .then((response) => {
          setNomeFuncionarios(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      agendasemanaData.funcionario = getParamFromUrl("funcionarios");
    }
  }

  if (getParamFromUrl("advogados") > 0) {
    if (agendasemanaData.id === 0 && agendasemanaData.advogado == 0) {
      advogadosApi
        .getById(getParamFromUrl("advogados"))
        .then((response) => {
          setNomeAdvogados(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      agendasemanaData.advogado = getParamFromUrl("advogados");
    }
  }

  if (getParamFromUrl("tipocompromisso") > 0) {
    if (agendasemanaData.id === 0 && agendasemanaData.tipocompromisso == 0) {
      tipocompromissoApi
        .getById(getParamFromUrl("tipocompromisso"))
        .then((response) => {
          setNomeTipoCompromisso(response.data.descricao);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      agendasemanaData.tipocompromisso = getParamFromUrl("tipocompromisso");
    }
  }

  if (getParamFromUrl("clientes") > 0) {
    if (agendasemanaData.id === 0 && agendasemanaData.cliente == 0) {
      clientesApi
        .getById(getParamFromUrl("clientes"))
        .then((response) => {
          setNomeClientes(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      agendasemanaData.cliente = getParamFromUrl("clientes");
    }
  }
  const addValorFuncionario = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "funcionario", value: e.id } });
  };
  const addValorAdvogado = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "advogado", value: e.id } });
  };
  const addValorTipoCompromisso = (e: any) => {
    if (e?.id > 0)
      onChange({ target: { name: "tipocompromisso", value: e.id } });
  };
  const addValorCliente = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "cliente", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de AgendaSemana:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `AgendaSemanaForm-${agendasemanaData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar AgendaSemana diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        agendasemanaData?.id == 0
          ? "Editar AgendaSemana"
          : "Adicionar Agenda Semana";
    }
  }, [agendasemanaData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-AgendaSemana"
            : "form-container form-container-AgendaSemana"
        }
      >
        <form
          className="formInputCadInc"
          id={`AgendaSemanaForm-${agendasemanaData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="AgendaSemana"
            data={agendasemanaData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`AgendaSemanaForm-${agendasemanaData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="paranome"
              label="ParaNome"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="paranome"
              value={agendasemanaData.paranome}
              placeholder={`Informe ParaNome`}
              onChange={onChange}
              required
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={2048}
              id="data"
              label="Data"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="data"
              value={agendasemanaData.data}
              onChange={onChange}
            />

            <FuncionariosComboBox
              name={"funcionario"}
              dataForm={agendasemanaData}
              value={agendasemanaData.funcionario}
              setValue={addValorFuncionario}
              label={"Colaborador"}
            />

            <AdvogadosComboBox
              name={"advogado"}
              dataForm={agendasemanaData}
              value={agendasemanaData.advogado}
              setValue={addValorAdvogado}
              label={"Advogados"}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={2048}
              id="hora"
              label="Hora"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="hora"
              value={agendasemanaData.hora}
              onChange={onChange}
            />

            <TipoCompromissoComboBox
              name={"tipocompromisso"}
              dataForm={agendasemanaData}
              value={agendasemanaData.tipocompromisso}
              setValue={addValorTipoCompromisso}
              label={"Tipo Compromisso"}
            />

            <InputInput
              tabIndex={3}
              type="text"
              maxLength={2147483647}
              id="compromisso"
              label="Compromisso"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="compromisso"
              value={agendasemanaData.compromisso}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={agendasemanaData}
              label="Concluido"
              name="concluido"
              checked={agendasemanaData.concluido}
              onChange={onChange}
            />
            <InputCheckbox
              dataForm={agendasemanaData}
              label="Liberado"
              name="liberado"
              checked={agendasemanaData.liberado}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputCheckbox
              dataForm={agendasemanaData}
              label="Importante"
              name="importante"
              checked={agendasemanaData.importante}
              onChange={onChange}
            />

            <InputInput
              tabIndex={4}
              type="text"
              maxLength={2048}
              id="horafinal"
              label="HoraFinal"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="horafinal"
              value={agendasemanaData.horafinal}
              onChange={onChange}
            />

            <InputInput
              tabIndex={5}
              type="text"
              maxLength={80}
              id="nome"
              label="Nome"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="nome"
              value={agendasemanaData.nome}
              onChange={onChange}
            />

            <ClientesComboBox
              name={"cliente"}
              dataForm={agendasemanaData}
              value={agendasemanaData.cliente}
              setValue={addValorCliente}
              label={"Clientes"}
            />

            <InputInput
              tabIndex={6}
              type="text"
              maxLength={80}
              id="nomecliente"
              label="NomeCliente"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="nomecliente"
              value={agendasemanaData.nomecliente}
              onChange={onChange}
            />

            <InputInput
              tabIndex={7}
              type="text"
              maxLength={100}
              id="tipo"
              label="Tipo"
              dataForm={agendasemanaData}
              className="input-default-main"
              name="tipo"
              value={agendasemanaData.tipo}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Agenda Semana" windowCrud={AgendaSemanaPage} />

          <CopyButton formId={`AgendaSemanaForm-${agendasemanaData.id}`} />

          <PrintButton formId={`AgendaSemanaForm-${agendasemanaData.id}`} />

          <AuditorButton
            tableName="AgendaSemana"
            coolName="Agenda Semana"
            id={agendasemanaData.id}
          />

          <DeleteButton
            page={"/pages/agendasemana"}
            id={agendasemanaData.id}
            dadoApi={dadoApi}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="AgendaSemana"
          data={agendasemanaData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`AgendaSemanaForm-${agendasemanaData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
