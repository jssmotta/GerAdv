// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import StatusTarefasWindowId from '@/app/GerAdv_TS/StatusTarefas/Crud/Grids/StatusTarefasWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import StatusTarefasWindowId from '@/app/GerAdv_TS/StatusTarefas/Crud/Grids/StatusTarefasWindowId';
import { StatusTarefasEmpty } from '@/app/GerAdv_TS/Models/StatusTarefas';

describe('StatusTarefasWindowId (basic)', () => {
  it('exports the component', () => {
    expect(StatusTarefasWindowId).toBeDefined();
    expect(typeof StatusTarefasWindowId === 'function' || typeof StatusTarefasWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the StatusTarefasService class so constructor usage doesn't throw and we can control fetchStatusTarefasById
const mockFetchStatusTarefasById = jest.fn();
jest.mock('@/app/GerAdv_TS/StatusTarefas/Services/StatusTarefas.service', () => ({
  StatusTarefasService: jest.fn().mockImplementation(() => ({
    fetchStatusTarefasById: (...args: any[]) => mockFetchStatusTarefasById(...args),
  })),
}));

// Mock the child StatusTarefasWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/StatusTarefas/Crud/Grids/StatusTarefasWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='statustarefas-window' {...p} />,
}));

describe('StatusTarefasWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(StatusTarefasWindowId).toBeDefined();
    expect(typeof StatusTarefasWindowId === 'function' || typeof StatusTarefasWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <StatusTarefasWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders StatusTarefasWindow with StatusTarefasEmpty when id is 0', async () => {
    render(
      <StatusTarefasWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // StatusTarefasWindow should be rendered with selectedStatusTarefas equal to StatusTarefasEmpty()
    const node = await screen.findByTestId('statustarefas-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedStatusTarefas"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedstatustarefas') || node.getAttribute('selectedStatusTarefas')).not.toBeNull();
  });

  it('fetches statustarefas by id and passes it to StatusTarefasWindow when id > 0', async () => {
    const fakeStatusTarefas = { id: 123, nomecliente: 'Test' };
    mockFetchStatusTarefasById.mockResolvedValue(fakeStatusTarefas);

    render(
      <StatusTarefasWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('statustarefas-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchStatusTarefasById).toHaveBeenCalledWith(123));
  });
});
