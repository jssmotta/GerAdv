// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TiposAcaoWindowId from '@/app/GerAdv_TS/TiposAcao/Crud/Grids/TiposAcaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TiposAcaoWindowId from '@/app/GerAdv_TS/TiposAcao/Crud/Grids/TiposAcaoWindowId';
import { TiposAcaoEmpty } from '@/app/GerAdv_TS/Models/TiposAcao';

describe('TiposAcaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TiposAcaoWindowId).toBeDefined();
    expect(typeof TiposAcaoWindowId === 'function' || typeof TiposAcaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TiposAcaoService class so constructor usage doesn't throw and we can control fetchTiposAcaoById
const mockFetchTiposAcaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/TiposAcao/Services/TiposAcao.service', () => ({
  TiposAcaoService: jest.fn().mockImplementation(() => ({
    fetchTiposAcaoById: (...args: any[]) => mockFetchTiposAcaoById(...args),
  })),
}));

// Mock the child TiposAcaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TiposAcao/Crud/Grids/TiposAcaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tiposacao-window' {...p} />,
}));

describe('TiposAcaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TiposAcaoWindowId).toBeDefined();
    expect(typeof TiposAcaoWindowId === 'function' || typeof TiposAcaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TiposAcaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TiposAcaoWindow with TiposAcaoEmpty when id is 0', async () => {
    render(
      <TiposAcaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TiposAcaoWindow should be rendered with selectedTiposAcao equal to TiposAcaoEmpty()
    const node = await screen.findByTestId('tiposacao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTiposAcao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtiposacao') || node.getAttribute('selectedTiposAcao')).not.toBeNull();
  });

  it('fetches tiposacao by id and passes it to TiposAcaoWindow when id > 0', async () => {
    const fakeTiposAcao = { id: 123, nomecliente: 'Test' };
    mockFetchTiposAcaoById.mockResolvedValue(fakeTiposAcao);

    render(
      <TiposAcaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tiposacao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTiposAcaoById).toHaveBeenCalledWith(123));
  });
});
