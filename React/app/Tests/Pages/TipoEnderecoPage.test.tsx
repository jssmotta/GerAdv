// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for TipoEndereco page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock TipoEnderecoGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/TipoEndereco/Adapter/TipoEnderecoGridAdapter', () => ({
  TipoEnderecoGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock TipoEnderecoGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/TipoEndereco/Components/TipoEnderecoGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='tipoendereco-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import TipoEnderecoPage from '@/app/pages/tipoendereco/page';

describe('TipoEndereco page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and TipoEnderecoGridContainer', () => {
    render(<TipoEnderecoPage />);

    expect(screen.getByRole('heading', { name: /Tipo Endereco/i })).toBeInTheDocument();
    expect(screen.getByTestId('tipoendereco-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

