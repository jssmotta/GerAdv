// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AreaWindowId from '@/app/GerAdv_TS/Area/Crud/Grids/AreaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AreaWindowId from '@/app/GerAdv_TS/Area/Crud/Grids/AreaWindowId';
import { AreaEmpty } from '@/app/GerAdv_TS/Models/Area';

describe('AreaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AreaWindowId).toBeDefined();
    expect(typeof AreaWindowId === 'function' || typeof AreaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AreaService class so constructor usage doesn't throw and we can control fetchAreaById
const mockFetchAreaById = jest.fn();
jest.mock('@/app/GerAdv_TS/Area/Services/Area.service', () => ({
  AreaService: jest.fn().mockImplementation(() => ({
    fetchAreaById: (...args: any[]) => mockFetchAreaById(...args),
  })),
}));

// Mock the child AreaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Area/Crud/Grids/AreaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='area-window' {...p} />,
}));

describe('AreaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AreaWindowId).toBeDefined();
    expect(typeof AreaWindowId === 'function' || typeof AreaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AreaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AreaWindow with AreaEmpty when id is 0', async () => {
    render(
      <AreaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AreaWindow should be rendered with selectedArea equal to AreaEmpty()
    const node = await screen.findByTestId('area-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedArea"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedarea') || node.getAttribute('selectedArea')).not.toBeNull();
  });

  it('fetches area by id and passes it to AreaWindow when id > 0', async () => {
    const fakeArea = { id: 123, nomecliente: 'Test' };
    mockFetchAreaById.mockResolvedValue(fakeArea);

    render(
      <AreaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('area-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAreaById).toHaveBeenCalledWith(123));
  });
});
