// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useParteClienteOutrasForm, useParteClienteOutrasList, useValidationsParteClienteOutras } from '../GerAdv_TS/ParteClienteOutras/Hooks/hookParteClienteOutras';
import { IParteClienteOutras } from '../GerAdv_TS/ParteClienteOutras/Interfaces/interface.ParteClienteOutras';
import { IParteClienteOutrasService } from '../GerAdv_TS/ParteClienteOutras/Services/ParteClienteOutras.service';
import { ParteClienteOutrasTestEmpty } from '../GerAdv_TS/Models/ParteClienteOutras';


// Mock do serviço
const mockParteClienteOutrasService: jest.Mocked<IParteClienteOutrasService> = {
  fetchParteClienteOutrasById: jest.fn(),
  saveParteClienteOutras: jest.fn(),
  
  getAll: jest.fn(),
  deleteParteClienteOutras: jest.fn(),
  validateParteClienteOutras: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialParteClienteOutras: IParteClienteOutras = { ...ParteClienteOutrasTestEmpty() };

describe('useParteClienteOutrasForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    expect(result.current.data).toEqual(initialParteClienteOutras);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    const mockEvent = {
      target: {
        name: 'campo',
        value: 'Novo Parte Cliente Outras',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.campo).toBe('Novo Parte Cliente Outras');
  });

   test('deve carregar Parte Cliente Outras por ID', async () => {
    const mockParteClienteOutras = { ...initialParteClienteOutras, id: 1, campo: 'Parte Cliente Outras Teste' };
    mockParteClienteOutrasService.fetchParteClienteOutrasById.mockResolvedValue(mockParteClienteOutras);

    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.loadParteClienteOutras(1);
    });

    expect(mockParteClienteOutrasService.fetchParteClienteOutrasById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockParteClienteOutras);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Parte Cliente Outras', async () => {
    const errorMessage = 'Erro ao carregar Parte Cliente Outras';
    mockParteClienteOutrasService.fetchParteClienteOutrasById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.loadParteClienteOutras(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialParteClienteOutras, campo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialParteClienteOutras);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.loadParteClienteOutras(0);
    });

    expect(mockParteClienteOutrasService.fetchParteClienteOutrasById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialParteClienteOutras);
  });
});

describe('useParteClienteOutrasList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasList(mockParteClienteOutrasService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialParteClienteOutras, id: 1, campo: 'Parte Cliente Outras 1' },
      { ...initialParteClienteOutras, id: 2, campo: 'Parte Cliente Outras 2' }
    ];
    mockParteClienteOutrasService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useParteClienteOutrasList(mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockParteClienteOutrasService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockParteClienteOutrasService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useParteClienteOutrasList(mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialParteClienteOutras, id: 1, campo: 'Parte Cliente Outras Filtrado' }];
    const filtro = { campo: 'Parte Cliente Outras' };
    mockParteClienteOutrasService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useParteClienteOutrasList(mockParteClienteOutrasService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockParteClienteOutrasService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsParteClienteOutras', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsParteClienteOutras());

    const validData = { ...initialParteClienteOutras, campo: 'Parte Cliente Outras Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsParteClienteOutras());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialParteClienteOutras, id: 1, campo: 'Parte Cliente Outras Teste' }];
    mockParteClienteOutrasService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useParteClienteOutrasList(mockParteClienteOutrasService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsParteClienteOutras()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useParteClienteOutrasForm(initialParteClienteOutras, mockParteClienteOutrasService)
    );

    const mockEvent = {
      target: {
        name: 'primeirareclamada',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.primeirareclamada).toBe(true);
  });


