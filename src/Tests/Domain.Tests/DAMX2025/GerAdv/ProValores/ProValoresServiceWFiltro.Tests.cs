// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ProValores
// Fonte:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class ProValoresServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFProValoresFactory> _mockProValoresFactory;
    private readonly Mock<IProValoresReader> _mockReader;
    private readonly Mock<IProValoresValidation> _mockValidation;
    private readonly Mock<IProValoresWriter> _mockWriter;
    private readonly Mock<IHttpContextAccessor> _mockHttpContextAccessor;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IGenericVoiceFilterService<Filters.FilterProValores>> _mockVoiceFilterService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly ProValoresService _service;
    private readonly ServiceFilter _serviceS;
    public ProValoresServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockProValoresFactory = new Mock<IFProValoresFactory>();
        _mockReader = new Mock<IProValoresReader>();
        _mockValidation = new Mock<IProValoresValidation>();
        _mockWriter = new Mock<IProValoresWriter>();
        _mockHttpContextAccessor = new Mock<IHttpContextAccessor>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockVoiceFilterService = new Mock<IGenericVoiceFilterService<Filters.FilterProValores>>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new ProValoresService(_mockAppSettings.Object, _mockProValoresFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _mockVoiceFilterService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterProValores? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterProValores(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            QuemCad = 1,
            DtCad = "24/04/1975",
            QuemAtu = 1,
            DtAtu = "24/04/1975",
            Visto = 2,
            Guid = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Processo = 1,
            TipoValorProcesso = 1,
            Indice = "AAAAAAAAAAAAAAAAAA",
            Ignorar = 1,
            Data = "24/04/1975",
            ValorOriginal = 1m,
            PercMulta = 1m,
            ValorMulta = 1m,
            PercJuros = 1m,
            ValorOriginalCorrigidoIndice = 1m,
            ValorMultaCorrigido = 1m,
            ValorJurosCorrigido = 1m,
            ValorFinal = 1m,
            DataUltimaCorrecao = "24/04/1975",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroProValores(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Guid_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string GuidComWhitespace)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = GuidComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = new string ('A', 1000);
        var filtro = new FilterProValores
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{GuidGigante}%");
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProValores
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Guid_ComCaracteresEspeciais_DeveProcessarCorretamente(string GuidComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = GuidComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(GuidComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_GuidProValores_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_GuidProValores_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_GuidProValores_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_GuidProValores_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Guid}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Guid_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Guid_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Indice_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string IndiceComWhitespace)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = IndiceComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Indice_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var IndiceGigante = new string ('A', 1000);
        var filtro = new FilterProValores
        {
            Indice = IndiceGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{IndiceGigante}%");
    }

    [Fact]
    public void WFiltro_Indice_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var IndiceGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterProValores
        {
            Indice = IndiceGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Indice_ComCaracteresEspeciais_DeveProcessarCorretamente(string IndiceComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = IndiceComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(IndiceComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_IndiceProValores_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_IndiceProValores_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_IndiceProValores_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_IndiceProValores_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Indice_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "Indice",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Indice_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Indice_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroProValores(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Indice_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Indice}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Indice_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Indice_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Indice = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region QuemCad Filter Tests
    [Fact]
    public void WFiltro_QuemCadEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            QuemCad = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.QuemCad));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.QuemCad);
    }

    [Fact]
    public void WFiltro_QuemCadHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemCadValue = 1;
        var filtro = new FilterProValores
        {
            QuemCad = QuemCadValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemCadParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemCad}");
        QuemCadParameter.Should().NotBeNull();
        QuemCadParameter.Value.Should().Be(QuemCadValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.QuemCad}] = @{DBProValoresDicInfo.QuemCad}");
    }

    [Fact]
    public void WFiltro_QuemCadWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemCad = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            QuemCad = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemCad}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemCad = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            QuemCad = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemCad}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemCadAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.QuemCad}] = @{DBProValoresDicInfo.QuemCad}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region QuemAtu Filter Tests
    [Fact]
    public void WFiltro_QuemAtuEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            QuemAtu = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.QuemAtu));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.QuemAtu);
    }

    [Fact]
    public void WFiltro_QuemAtuHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemAtuValue = 1;
        var filtro = new FilterProValores
        {
            QuemAtu = QuemAtuValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemAtuParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemAtu}");
        QuemAtuParameter.Should().NotBeNull();
        QuemAtuParameter.Value.Should().Be(QuemAtuValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.QuemAtu}] = @{DBProValoresDicInfo.QuemAtu}");
    }

    [Fact]
    public void WFiltro_QuemAtuWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemAtu = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            QuemAtu = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemAtu}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemAtu = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            QuemAtu = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.QuemAtu}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemAtuAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.QuemAtu}] = @{DBProValoresDicInfo.QuemAtu}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Processo Filter Tests
    [Fact]
    public void WFiltro_ProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Processo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.Processo));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.Processo);
    }

    [Fact]
    public void WFiltro_ProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ProcessoValue = 1;
        var filtro = new FilterProValores
        {
            Processo = ProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Processo}");
        ProcessoParameter.Should().NotBeNull();
        ProcessoParameter.Value.Should().Be(ProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Processo}] = @{DBProValoresDicInfo.Processo}");
    }

    [Fact]
    public void WFiltro_ProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Processo = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            Processo = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Processo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Processo = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            Processo = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Processo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Processo}] = @{DBProValoresDicInfo.Processo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region TipoValorProcesso Filter Tests
    [Fact]
    public void WFiltro_TipoValorProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            TipoValorProcesso = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.TipoValorProcesso));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.TipoValorProcesso);
    }

    [Fact]
    public void WFiltro_TipoValorProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var TipoValorProcessoValue = 1;
        var filtro = new FilterProValores
        {
            TipoValorProcesso = TipoValorProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var TipoValorProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.TipoValorProcesso}");
        TipoValorProcessoParameter.Should().NotBeNull();
        TipoValorProcessoParameter.Value.Should().Be(TipoValorProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.TipoValorProcesso}] = @{DBProValoresDicInfo.TipoValorProcesso}");
    }

    [Fact]
    public void WFiltro_TipoValorProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for TipoValorProcesso = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            TipoValorProcesso = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.TipoValorProcesso}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for TipoValorProcesso = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            TipoValorProcesso = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.TipoValorProcesso}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_TipoValorProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            TipoValorProcesso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.TipoValorProcesso}] = @{DBProValoresDicInfo.TipoValorProcesso}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_TipoValorProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            TipoValorProcesso = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_TipoValorProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            TipoValorProcesso = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#region ValorOriginal Filter Tests
    [Fact]
    public void WFiltro_ValorOriginal_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginal = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorOriginal));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorOriginal);
    }

    [Fact]
    public void WFiltro_ValorOriginal_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorOriginalValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorOriginal = ValorOriginalValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorOriginalParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorOriginal}");
        ValorOriginalParameter.Should().NotBeNull();
        ValorOriginalParameter.Value.Should().Be(ValorOriginalValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorOriginal}] = @{DBProValoresDicInfo.ValorOriginal}");
    }

    [Fact]
    public void WFiltro_ValorOriginal_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginal = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorOriginal}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorOriginal_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorOriginal}]");
    }

    [Fact]
    public void WFiltro_ValorOriginal_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorOriginal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorOriginal_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginal = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region PercMulta Filter Tests
    [Fact]
    public void WFiltro_PercMulta_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercMulta = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.PercMulta));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.PercMulta);
    }

    [Fact]
    public void WFiltro_PercMulta_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PercMultaValue = 123.45m;
        var filtro = new FilterProValores
        {
            PercMulta = PercMultaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PercMultaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.PercMulta}");
        PercMultaParameter.Should().NotBeNull();
        PercMultaParameter.Value.Should().Be(PercMultaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.PercMulta}] = @{DBProValoresDicInfo.PercMulta}");
    }

    [Fact]
    public void WFiltro_PercMulta_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercMulta = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.PercMulta}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_PercMulta_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercMulta = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.PercMulta}]");
    }

    [Fact]
    public void WFiltro_PercMulta_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            PercMulta = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_PercMulta_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercMulta = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorMulta Filter Tests
    [Fact]
    public void WFiltro_ValorMulta_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMulta = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorMulta));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorMulta);
    }

    [Fact]
    public void WFiltro_ValorMulta_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorMultaValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorMulta = ValorMultaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorMultaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorMulta}");
        ValorMultaParameter.Should().NotBeNull();
        ValorMultaParameter.Value.Should().Be(ValorMultaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorMulta}] = @{DBProValoresDicInfo.ValorMulta}");
    }

    [Fact]
    public void WFiltro_ValorMulta_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMulta = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorMulta}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorMulta_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMulta = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorMulta}]");
    }

    [Fact]
    public void WFiltro_ValorMulta_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorMulta = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorMulta_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMulta = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region PercJuros Filter Tests
    [Fact]
    public void WFiltro_PercJuros_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercJuros = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.PercJuros));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.PercJuros);
    }

    [Fact]
    public void WFiltro_PercJuros_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PercJurosValue = 123.45m;
        var filtro = new FilterProValores
        {
            PercJuros = PercJurosValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PercJurosParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.PercJuros}");
        PercJurosParameter.Should().NotBeNull();
        PercJurosParameter.Value.Should().Be(PercJurosValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.PercJuros}] = @{DBProValoresDicInfo.PercJuros}");
    }

    [Fact]
    public void WFiltro_PercJuros_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercJuros = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.PercJuros}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_PercJuros_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercJuros = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.PercJuros}]");
    }

    [Fact]
    public void WFiltro_PercJuros_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            PercJuros = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_PercJuros_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            PercJuros = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorOriginalCorrigidoIndice Filter Tests
    [Fact]
    public void WFiltro_ValorOriginalCorrigidoIndice_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginalCorrigidoIndice = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorOriginalCorrigidoIndice));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorOriginalCorrigidoIndice);
    }

    [Fact]
    public void WFiltro_ValorOriginalCorrigidoIndice_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorOriginalCorrigidoIndiceValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorOriginalCorrigidoIndice = ValorOriginalCorrigidoIndiceValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorOriginalCorrigidoIndiceParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorOriginalCorrigidoIndice}");
        ValorOriginalCorrigidoIndiceParameter.Should().NotBeNull();
        ValorOriginalCorrigidoIndiceParameter.Value.Should().Be(ValorOriginalCorrigidoIndiceValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorOriginalCorrigidoIndice}] = @{DBProValoresDicInfo.ValorOriginalCorrigidoIndice}");
    }

    [Fact]
    public void WFiltro_ValorOriginalCorrigidoIndice_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginalCorrigidoIndice = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorOriginalCorrigidoIndice}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorOriginalCorrigidoIndice_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginalCorrigidoIndice = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorOriginalCorrigidoIndice}]");
    }

    [Fact]
    public void WFiltro_ValorOriginalCorrigidoIndice_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorOriginalCorrigidoIndice = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorOriginalCorrigidoIndice_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorOriginalCorrigidoIndice = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorMultaCorrigido Filter Tests
    [Fact]
    public void WFiltro_ValorMultaCorrigido_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMultaCorrigido = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorMultaCorrigido));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorMultaCorrigido);
    }

    [Fact]
    public void WFiltro_ValorMultaCorrigido_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorMultaCorrigidoValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorMultaCorrigido = ValorMultaCorrigidoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorMultaCorrigidoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorMultaCorrigido}");
        ValorMultaCorrigidoParameter.Should().NotBeNull();
        ValorMultaCorrigidoParameter.Value.Should().Be(ValorMultaCorrigidoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorMultaCorrigido}] = @{DBProValoresDicInfo.ValorMultaCorrigido}");
    }

    [Fact]
    public void WFiltro_ValorMultaCorrigido_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMultaCorrigido = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorMultaCorrigido}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorMultaCorrigido_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMultaCorrigido = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorMultaCorrigido}]");
    }

    [Fact]
    public void WFiltro_ValorMultaCorrigido_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorMultaCorrigido = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorMultaCorrigido_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorMultaCorrigido = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorJurosCorrigido Filter Tests
    [Fact]
    public void WFiltro_ValorJurosCorrigido_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorJurosCorrigido = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorJurosCorrigido));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorJurosCorrigido);
    }

    [Fact]
    public void WFiltro_ValorJurosCorrigido_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorJurosCorrigidoValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorJurosCorrigido = ValorJurosCorrigidoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorJurosCorrigidoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorJurosCorrigido}");
        ValorJurosCorrigidoParameter.Should().NotBeNull();
        ValorJurosCorrigidoParameter.Value.Should().Be(ValorJurosCorrigidoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorJurosCorrigido}] = @{DBProValoresDicInfo.ValorJurosCorrigido}");
    }

    [Fact]
    public void WFiltro_ValorJurosCorrigido_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorJurosCorrigido = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorJurosCorrigido}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorJurosCorrigido_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorJurosCorrigido = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorJurosCorrigido}]");
    }

    [Fact]
    public void WFiltro_ValorJurosCorrigido_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorJurosCorrigido = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorJurosCorrigido_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorJurosCorrigido = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorFinal Filter Tests
    [Fact]
    public void WFiltro_ValorFinal_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorFinal = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.ValorFinal));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.ValorFinal);
    }

    [Fact]
    public void WFiltro_ValorFinal_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorFinalValue = 123.45m;
        var filtro = new FilterProValores
        {
            ValorFinal = ValorFinalValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorFinalParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorFinal}");
        ValorFinalParameter.Should().NotBeNull();
        ValorFinalParameter.Value.Should().Be(ValorFinalValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorFinal}] = @{DBProValoresDicInfo.ValorFinal}");
    }

    [Fact]
    public void WFiltro_ValorFinal_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorFinal = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.ValorFinal}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorFinal_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorFinal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.ValorFinal}]");
    }

    [Fact]
    public void WFiltro_ValorFinal_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorFinal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorFinal_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            ValorFinal = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#endregion
#region DateTime Tests
#region DtCad Filter Tests
    [Fact]
    public void WFiltro_DtCad_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.DtCad}], 103)");
    }

    [Fact]
    public void WFiltro_DtCad_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBProValoresDicInfo.DtCad));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBProValoresDicInfo.DtCad);
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterProValores
        {
            DtCad = startDate,
            DtCad_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtCad_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtCad = string.Empty,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtCad = null,
            DtCad_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterProValores
        {
            DtCad = minValueDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtCad_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtCad = inputDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DtAtu Filter Tests
    [Fact]
    public void WFiltro_DtAtu_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.DtAtu}], 103)");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBProValoresDicInfo.DtAtu));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBProValoresDicInfo.DtAtu);
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterProValores
        {
            DtAtu = startDate,
            DtAtu_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtAtu = string.Empty,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtAtu = null,
            DtAtu_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterProValores
        {
            DtAtu = minValueDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtAtu_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DtAtu = inputDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region Data Filter Tests
    [Fact]
    public void WFiltro_Data_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Data}], 103)");
    }

    [Fact]
    public void WFiltro_Data_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBProValoresDicInfo.Data));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBProValoresDicInfo.Data);
    }

    [Fact]
    public void WFiltro_Data_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterProValores
        {
            Data = startDate,
            Data_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_Data_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Data = string.Empty,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Data = null,
            Data_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterProValores
        {
            Data = minValueDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_Data_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Data = inputDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DataUltimaCorrecao Filter Tests
    [Fact]
    public void WFiltro_DataUltimaCorrecao_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = testDate,
            DataUltimaCorrecao_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.DataUltimaCorrecao}], 103)");
    }

    [Fact]
    public void WFiltro_DataUltimaCorrecao_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = testDate,
            DataUltimaCorrecao_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBProValoresDicInfo.DataUltimaCorrecao));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBProValoresDicInfo.DataUltimaCorrecao);
    }

    [Fact]
    public void WFiltro_DataUltimaCorrecao_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = startDate,
            DataUltimaCorrecao_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DataUltimaCorrecao_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = string.Empty,
            DataUltimaCorrecao_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DataUltimaCorrecao_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = null,
            DataUltimaCorrecao_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DataUltimaCorrecao_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = minValueDate,
            DataUltimaCorrecao_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DataUltimaCorrecao_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterProValores
        {
            DataUltimaCorrecao = inputDate,
            DataUltimaCorrecao_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#endregion
#region Bool Tests
#region Visto Filter Tests
    [Fact]
    public void WFiltro_VistoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Visto = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.Visto));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.Visto);
    }

    [Fact]
    public void WFiltro_VistoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var VistoValue = 1;
        var filtro = new FilterProValores
        {
            Visto = VistoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var VistoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Visto}");
        VistoParameter.Should().NotBeNull();
        VistoParameter.Value.Should().Be(VistoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Visto}] = @{DBProValoresDicInfo.Visto}");
    }

    [Fact]
    public void WFiltro_VistoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Visto = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            Visto = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Visto}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Visto = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            Visto = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Visto}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_VistoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Visto}] = @{DBProValoresDicInfo.Visto}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Ignorar Filter Tests
    [Fact]
    public void WFiltro_IgnorarEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Ignorar = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBProValoresDicInfo.Ignorar));
        result.Value.where.Should().NotContain(DBProValoresDicInfo.Ignorar);
    }

    [Fact]
    public void WFiltro_IgnorarHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IgnorarValue = 1;
        var filtro = new FilterProValores
        {
            Ignorar = IgnorarValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IgnorarParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Ignorar}");
        IgnorarParameter.Should().NotBeNull();
        IgnorarParameter.Value.Should().Be(IgnorarValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Ignorar}] = @{DBProValoresDicInfo.Ignorar}");
    }

    [Fact]
    public void WFiltro_IgnorarWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Ignorar = 0 (typically female)
        var filtroFemale = new FilterProValores
        {
            Ignorar = 0
        };
        var resultFemale = _serviceS.WFiltroProValores(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Ignorar}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Ignorar = 1 (typically male)
        var filtroMale = new FilterProValores
        {
            Ignorar = 1
        };
        var resultMale = _serviceS.WFiltroProValores(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBProValoresDicInfo.Ignorar}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IgnorarAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Ignorar = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBProValoresDicInfo.PTabelaNome}].[{DBProValoresDicInfo.Ignorar}] = @{DBProValoresDicInfo.Ignorar}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IgnorarWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Ignorar = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IgnorarWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterProValores
        {
            Guid = "Guid", // Campo string para testar combinação
            Ignorar = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroProValores(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}