// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { NECompromissosService, NECompromissosValidator } from '@/app/GerAdv_TS/NECompromissos/Services/NECompromissos.service';
import { NECompromissosApi, NECompromissosApiError } from '@/app/GerAdv_TS/NECompromissos/Apis/ApiNECompromissos';
import { INECompromissos } from '@/app/GerAdv_TS/NECompromissos/Interfaces/interface.NECompromissos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as NECompromissosApi;

const sample: INECompromissos = { id: 1, nome: 'Teste' };

describe('NECompromissosService', () => {
  let service: NECompromissosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new NECompromissosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(NECompromissosValidator, 'validateNECompromissos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchNECompromissosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchNECompromissosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchNECompromissosById lança NECompromissosApiError para id inválido', async () => {
    await expect(service.fetchNECompromissosById(0)).rejects.toBeInstanceOf(NECompromissosApiError);
  });

  it('saveNECompromissos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveNECompromissos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveNECompromissos lança NECompromissosApiError quando validação falha', async () => {
    (NECompromissosValidator.validateNECompromissos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveNECompromissos(sample)).rejects.toBeInstanceOf(NECompromissosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteNECompromissos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteNECompromissos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteNECompromissos lança NECompromissosApiError para id inválido', async () => {
    await expect(service.deleteNECompromissos(0)).rejects.toBeInstanceOf(NECompromissosApiError);
  });

  it('validateNECompromissos delega para NECompromissosValidator', () => {
    (NECompromissosValidator.validateNECompromissos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateNECompromissos(sample);
    expect(r.isValid).toBe(true);
  });
});
