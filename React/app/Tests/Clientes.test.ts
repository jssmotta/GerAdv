// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useClientesForm, useClientesList, useValidationsClientes } from '../GerAdv_TS/Clientes/Hooks/hookClientes';
import { IClientes } from '../GerAdv_TS/Clientes/Interfaces/interface.Clientes';
import { IClientesService } from '../GerAdv_TS/Clientes/Services/Clientes.service';
import { ClientesTestEmpty } from '../GerAdv_TS/Models/Clientes';
import { useClientesComboBox } from '../GerAdv_TS/Clientes/Hooks/hookClientes';

// Mock do serviço
const mockClientesService: jest.Mocked<IClientesService> = {
  fetchClientesById: jest.fn(),
  saveClientes: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteClientes: jest.fn(),
  validateClientes: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialClientes: IClientes = { ...ClientesTestEmpty() };

describe('useClientesForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    expect(result.current.data).toEqual(initialClientes);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Clientes',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Clientes');
  });

   test('deve carregar Clientes por ID', async () => {
    const mockClientes = { ...initialClientes, id: 1, nome: 'Clientes Teste' };
    mockClientesService.fetchClientesById.mockResolvedValue(mockClientes);

    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    await act(async () => {
      await result.current.loadClientes(1);
    });

    expect(mockClientesService.fetchClientesById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockClientes);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Clientes', async () => {
    const errorMessage = 'Erro ao carregar Clientes';
    mockClientesService.fetchClientesById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    await act(async () => {
      await result.current.loadClientes(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialClientes, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialClientes);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    await act(async () => {
      await result.current.loadClientes(0);
    });

    expect(mockClientesService.fetchClientesById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialClientes);
  });
});

describe('useClientesList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useClientesList(mockClientesService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialClientes, id: 1, nome: 'Clientes 1' },
      { ...initialClientes, id: 2, nome: 'Clientes 2' }
    ];
    mockClientesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useClientesList(mockClientesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockClientesService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockClientesService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useClientesList(mockClientesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialClientes, id: 1, nome: 'Clientes Filtrado' }];
    const filtro = { nome: 'Clientes' };
    mockClientesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useClientesList(mockClientesService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockClientesService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsClientes', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsClientes());

    const validData = { ...initialClientes, nome: 'Clientes Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsClientes());

    const invalidData = { ...initialClientes, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsClientes());

    const invalidData = { 
      ...initialClientes, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsClientes());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialClientes, id: 1, nome: 'Clientes Teste' }];
    mockClientesService.getAll.mockResolvedValue(mockData);
    mockClientesService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useClientesList(mockClientesService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsClientes()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Clientes Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Clientes Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useClientesForm(initialClientes, mockClientesService)
    );

    const mockEvent = {
      target: {
        name: 'inativo',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.inativo).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Clientes 1' },
      { id: 2, nome: 'Clientes 2' }
    ];
    mockClientesService.getList.mockResolvedValue(mockOptions as IClientes[]);


    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Clientes 1' },
        { id: 2, nome: 'Clientes 2' }
      ]);
    });


    expect(mockClientesService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Clientes ABC' },
      { id: 2, nome: 'Clientes XYZ' }
    ];
    mockClientesService.getList.mockResolvedValue(mockOptions as IClientes[]);   


 const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Clientes ABC' },
        { id: 2, nome: 'Clientes XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Clientes ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Clientes ABC' },
      { id: 2, nome: 'Clientes XYZ' }
    ];
    mockClientesService.getList.mockResolvedValue(mockOptions as IClientes[]);
  


    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Clientes ABC' },
        { id: 2, nome: 'Clientes XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Clientes ABC' },
          {id: 2, nome: 'Clientes XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );

    const newValue = { id: 1, nome: 'Clientes Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Clientes Inicial' };
    
    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useClientesComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Clientes Inicial' };
    
    const { result } = renderHook(() => 
      useClientesComboBox(mockClientesService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






