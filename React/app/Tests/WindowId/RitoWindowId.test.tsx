// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import RitoWindowId from '@/app/GerAdv_TS/Rito/Crud/Grids/RitoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import RitoWindowId from '@/app/GerAdv_TS/Rito/Crud/Grids/RitoWindowId';
import { RitoEmpty } from '@/app/GerAdv_TS/Models/Rito';

describe('RitoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(RitoWindowId).toBeDefined();
    expect(typeof RitoWindowId === 'function' || typeof RitoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the RitoService class so constructor usage doesn't throw and we can control fetchRitoById
const mockFetchRitoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Rito/Services/Rito.service', () => ({
  RitoService: jest.fn().mockImplementation(() => ({
    fetchRitoById: (...args: any[]) => mockFetchRitoById(...args),
  })),
}));

// Mock the child RitoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Rito/Crud/Grids/RitoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='rito-window' {...p} />,
}));

describe('RitoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(RitoWindowId).toBeDefined();
    expect(typeof RitoWindowId === 'function' || typeof RitoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <RitoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders RitoWindow with RitoEmpty when id is 0', async () => {
    render(
      <RitoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // RitoWindow should be rendered with selectedRito equal to RitoEmpty()
    const node = await screen.findByTestId('rito-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedRito"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedrito') || node.getAttribute('selectedRito')).not.toBeNull();
  });

  it('fetches rito by id and passes it to RitoWindow when id > 0', async () => {
    const fakeRito = { id: 123, nomecliente: 'Test' };
    mockFetchRitoById.mockResolvedValue(fakeRito);

    render(
      <RitoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('rito-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchRitoById).toHaveBeenCalledWith(123));
  });
});
