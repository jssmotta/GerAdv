// 100% auto-generated by Source Genesys Core
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
// ReSharper disable once InconsistentNaming 1
public partial class DBEnderecoSistema
{
    public DBEnderecoSistema(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        InitFromRecord(name =>
        {
            try
            {
                return dbRec[name];
            }
            catch
            {
                return DBNull.Value;
            }
        });
    }

    public DBEnderecoSistema(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name =>
            {
                try
                {
                    return dbRec[name];
                }
                catch
                {
                    return DBNull.Value;
                }
            });
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EnderecoSistema: {ex.Message}", ex);
        }
    }

    private void InitFromRecord(Func<string, object?> getValue)
    {
        if (DBNull.Value.Equals(getValue(CampoCodigo)))
            return;
        ID = Convert.ToInt32(getValue(CampoCodigo));
        // Checkpoint Carregar 
        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.Cadastro)))
                m_FCadastro = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.Cadastro));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.CadastroExCod)))
                m_FCadastroExCod = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.CadastroExCod));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.Cidade)))
                m_FCidade = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.Cidade));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.DtAtu)))
                m_FDtAtu = Convert.ToDateTime(getValue(DBEnderecoSistemaDicInfo.DtAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.DtCad)))
                m_FDtCad = Convert.ToDateTime(getValue(DBEnderecoSistemaDicInfo.DtCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.Processo)))
                m_FProcesso = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.Processo));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.QuemAtu)))
                m_FQuemAtu = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.QuemAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.QuemCad)))
                m_FQuemCad = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.QuemCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.TipoEnderecoSistema)))
                m_FTipoEnderecoSistema = Convert.ToInt32(getValue(DBEnderecoSistemaDicInfo.TipoEnderecoSistema));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBEnderecoSistemaDicInfo.Visto)))
                m_FVisto = Convert.ToBoolean(getValue(DBEnderecoSistemaDicInfo.Visto));
        }
        catch
        {
        }

        try
        {
            m_FBairro = getValue(DBEnderecoSistemaDicInfo.Bairro)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FCEP = getValue(DBEnderecoSistemaDicInfo.CEP)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FContatoNoLocal = getValue(DBEnderecoSistemaDicInfo.ContatoNoLocal)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FEndereco = getValue(DBEnderecoSistemaDicInfo.Endereco)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FFax = getValue(DBEnderecoSistemaDicInfo.Fax)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FFone = getValue(DBEnderecoSistemaDicInfo.Fone)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FGUID = getValue(DBEnderecoSistemaDicInfo.GUID)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FMotivo = getValue(DBEnderecoSistemaDicInfo.Motivo)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FObservacao = getValue(DBEnderecoSistemaDicInfo.Observacao)?.ToString() ?? string.Empty;
        }
        catch
        {
        }
    }

    public void CarregarDadosBd(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name =>
            {
                try
                {
                    return dbRec[name];
                }
                catch
                {
                    return DBNull.Value;
                }
            });
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EnderecoSistema: {ex.Message}", ex);
        }
    }

    public void CarregarDadosBd(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name =>
            {
                try
                {
                    return dbRec[name];
                }
                catch
                {
                    return DBNull.Value;
                }
            });
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do EnderecoSistema: {ex.Message}", ex);
        }
    }
}