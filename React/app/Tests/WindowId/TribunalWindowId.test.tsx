// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TribunalWindowId from '@/app/GerAdv_TS/Tribunal/Crud/Grids/TribunalWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TribunalWindowId from '@/app/GerAdv_TS/Tribunal/Crud/Grids/TribunalWindowId';
import { TribunalEmpty } from '@/app/GerAdv_TS/Models/Tribunal';

describe('TribunalWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TribunalWindowId).toBeDefined();
    expect(typeof TribunalWindowId === 'function' || typeof TribunalWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TribunalService class so constructor usage doesn't throw and we can control fetchTribunalById
const mockFetchTribunalById = jest.fn();
jest.mock('@/app/GerAdv_TS/Tribunal/Services/Tribunal.service', () => ({
  TribunalService: jest.fn().mockImplementation(() => ({
    fetchTribunalById: (...args: any[]) => mockFetchTribunalById(...args),
  })),
}));

// Mock the child TribunalWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Tribunal/Crud/Grids/TribunalWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tribunal-window' {...p} />,
}));

describe('TribunalWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TribunalWindowId).toBeDefined();
    expect(typeof TribunalWindowId === 'function' || typeof TribunalWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TribunalWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TribunalWindow with TribunalEmpty when id is 0', async () => {
    render(
      <TribunalWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TribunalWindow should be rendered with selectedTribunal equal to TribunalEmpty()
    const node = await screen.findByTestId('tribunal-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTribunal"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtribunal') || node.getAttribute('selectedTribunal')).not.toBeNull();
  });

  it('fetches tribunal by id and passes it to TribunalWindow when id > 0', async () => {
    const fakeTribunal = { id: 123, nomecliente: 'Test' };
    mockFetchTribunalById.mockResolvedValue(fakeTribunal);

    render(
      <TribunalWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tribunal-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTribunalById).toHaveBeenCalledWith(123));
  });
});
