// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { ITiposAcao } from "@/app/GerAdv_TS/TiposAcao/Interfaces/interface.TiposAcao";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { TiposAcaoApi } from "../../Apis/TiposAcaoApi";
import { useValidationsTiposAcao } from "../../Hooks/hookTiposAcao";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpTiposAcao from "../../Help/HelpTiposAcao";
import TiposAcaoPage from "@/app/pages/tiposacao/page";

import InputName from "@/app/components/Inputs/InputName";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface TiposAcaoFormProps {
  tiposacaoData: ITiposAcao;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const TiposAcaoForm: React.FC<TiposAcaoFormProps> = ({
  tiposacaoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new TiposAcaoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsTiposAcao();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de TiposAcao:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`TiposAcaoForm-${tiposacaoData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar TiposAcao diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        tiposacaoData?.id == 0 ? "Editar TiposAcao" : "Adicionar Tipos Acao";
    }
  }, [tiposacaoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-TiposAcao"
            : "form-container5 form-container-TiposAcao"
        }
      >
        <form
          className="formInputCadInc"
          id={`TiposAcaoForm-${tiposacaoData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="TiposAcao"
            data={tiposacaoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`TiposAcaoForm-${tiposacaoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="nome"
              label="Nome"
              dataForm={tiposacaoData}
              className="input-default-main"
              name="nome"
              value={tiposacaoData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputCheckbox
              dataForm={tiposacaoData}
              label="Inativo"
              name="inativo"
              checked={tiposacaoData.inativo}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Tipos Acao" windowCrud={TiposAcaoPage} />

          <CopyButton formId={`TiposAcaoForm-${tiposacaoData.id}`} />

          <PrintButton formId={`TiposAcaoForm-${tiposacaoData.id}`} />

          <AuditorButton
            tableName="TiposAcao"
            coolName="Tipos Acao"
            id={tiposacaoData.id}
          />

          <DeleteButton
            page={"/pages/tiposacao"}
            id={tiposacaoData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Tipos Acao"
            page={(onClose) => <HelpTiposAcao onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="TiposAcao"
          data={tiposacaoData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`TiposAcaoForm-${tiposacaoData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
