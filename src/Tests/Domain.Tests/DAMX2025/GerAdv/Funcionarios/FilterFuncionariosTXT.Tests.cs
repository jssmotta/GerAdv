// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Funcionarios
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterFuncionariosTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterFuncionariosTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEMailPro_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            EMailPro = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"emailpro\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCargo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Cargo = 123
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cargo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithNome_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Nome = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nome\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithFuncao_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Funcao = 123
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"funcao\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithRegistro_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Registro = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"registro\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCPF_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            CPF = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cpf\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithRG_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            RG = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"rg\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithObservacao_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Observacao = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"observacao\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEndereco_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Endereco = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"endereco\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithBairro_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Bairro = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"bairro\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCidade_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Cidade = 123
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cidade\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCEP_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            CEP = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cep\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithContato_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Contato = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"contato\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithFax_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Fax = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"fax\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithFone_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Fone = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"fone\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithEMail_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            EMail = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"email\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPeriodo_Ini_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Periodo_Ini = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"periodo_ini\"");
    }

    [Fact]
    public void GetPrompt_WithPeriodo_Fim_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Periodo_Fim = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"periodo_fim\"");
    }

    [Fact]
    public void GetPrompt_WithCTPSNumero_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            CTPSNumero = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"ctpsnumero\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithCTPSSerie_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            CTPSSerie = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"ctpsserie\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPIS_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            PIS = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pis\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithSalario_SerializesNumericValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Salario = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"salario\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithCTPSDtEmissao_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            CTPSDtEmissao = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"ctpsdtemissao\"");
    }

    [Fact]
    public void GetPrompt_WithDtNasc_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            DtNasc = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtnasc\"");
    }

    [Fact]
    public void GetPrompt_WithData_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Data = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"data\"");
    }

    [Fact]
    public void GetPrompt_WithPasta_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Pasta = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"pasta\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithClass_SerializesCorrectly()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Class = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"class\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterCargos\"");
        prompt.Should().Contain("\"filterFuncao\"");
        prompt.Should().Contain("\"filterCidade\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(new FilterFuncionarios(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(new FilterFuncionarios(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(new FilterFuncionarios(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(new FilterFuncionarios(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterFuncionariosTXT.GetOptions();
        var filter = new FilterFuncionarios
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterFuncionariosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}