// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:SMSAlice
// Fonte:ValidationsGeneratorTests
namespace MenphisSI.GerAdv.Tests;
public class SMSAliceValidationTests : IDisposable
{
    private readonly SMSAliceValidation _validation;
    private readonly Mock<ISMSAliceService> _mockSMSAliceService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IOperadorReader> _mockOperadorReader;
    private readonly Mock<ITipoEMailReader> _mockTipoEMailReader;
    private readonly string _validUri = "test-uri";
    public SMSAliceValidationTests()
    {
        _validation = new SMSAliceValidation();
        _mockSMSAliceService = new Mock<ISMSAliceService>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockOperadorReader = new Mock<IOperadorReader>();
        _mockTipoEMailReader = new Mock<ITipoEMailReader>();
    }

#region Class Structure Tests
    [Fact]
    public void SMSAliceValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ISMSAliceValidation>();
    }

    [Fact]
    public void SMSAliceValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(SMSAliceValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void SMSAliceValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(SMSAliceValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static GerAdv.Models.SMSAlice CreateValidSMSAlice()
    {
        return new GerAdv.Models.SMSAlice
        {
            Id = 1,
            Operador = 1,
            Nome = "João",
            TipoEMail = 1
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockSMSAliceService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterSMSAlice>(), It.IsAny<string>())).ReturnsAsync([]);
        // Setup other mocks but don't override the SMSAlices service mock
        _ = _mockOperadorReader.Setup(x => x.ReadAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>(valueFunction: static async (id, conn) => await Task.FromResult<OperadorResponse?>(new OperadorResponse { Id = id }));
        _ = _mockTipoEMailReader.Setup(x => x.ReadAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>(valueFunction: static async (id, conn) => await Task.FromResult<TipoEMailResponse?>(new TipoEMailResponse { Id = id }));
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockSMSAliceService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterSMSAlice>(), It.IsAny<string>())).ReturnsAsync([]);
        // Setup other mocks but don't override the SMSAlices service mock
        _ = _mockOperadorReader.Setup(x => x.ReadAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>(valueFunction: static async (id, conn) => await Task.FromResult<OperadorResponse?>(new OperadorResponse { Id = 0 }));
        _ = _mockTipoEMailReader.Setup(x => x.ReadAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>(valueFunction: static async (id, conn) => await Task.FromResult<TipoEMailResponse?>(new TipoEMailResponse { Id = 0 }));
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var smsalice = new GerAdv.Models.SMSAlice
        {
            Id = 1,
            Operador = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            TipoEMail = 1
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null !, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().MatchRegex("(é obrigatório|não encontrado)");
    }

    [Fact]
    public async Task ValidateReg_WithNull_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Nome = null !;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidData_Nome_ShouldReturnTrue()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespace_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Nome = " ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
#region Foreign Key Validation Tests - Operador
    [Fact]
    public async Task ValidateReg_WithInvalidOperador_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Operador = 999;
        _mockOperadorReader.Setup(x => x.ReadAsync(999, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.OperadorResponse?>(null !));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithMismatchedOperadorId_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Operador = 999;
        var reg888 = new GerAdv.Models.Response.OperadorResponse
        {
            Id = 888
        }; // Different ID
        _mockOperadorReader.Setup(x => x.ReadAsync(999, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.OperadorResponse?>(reg888));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithValidOperador_ShouldPass()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.Operador = 123;
        var reg123 = new GerAdv.Models.Response.OperadorResponse
        {
            Id = 123
        };
        _mockOperadorReader.Setup(x => x.ReadAsync(123, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.OperadorResponse?>(reg123));
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region Foreign Key Validation Tests - TipoEMail
    [Fact]
    public async Task ValidateReg_WithInvalidTipoEMail_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.TipoEMail = 999;
        _mockTipoEMailReader.Setup(x => x.ReadAsync(999, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.TipoEMailResponse?>(null !));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithMismatchedTipoEMailId_ShouldThrowSGValidationException()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.TipoEMail = 999;
        var reg888 = new GerAdv.Models.Response.TipoEMailResponse
        {
            Id = 888
        }; // Different ID
        _mockTipoEMailReader.Setup(x => x.ReadAsync(999, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.TipoEMailResponse?>(reg888));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithValidTipoEMail_ShouldPass()
    {
        // Arrange
        var smsalice = CreateValidSMSAlice();
        smsalice.TipoEMail = 123;
        var reg123 = new GerAdv.Models.Response.TipoEMailResponse
        {
            Id = 123
        };
        _mockTipoEMailReader.Setup(x => x.ReadAsync(123, _mockConnection.Object)).Returns(Task.FromResult<GerAdv.Models.Response.TipoEMailResponse?>(reg123));
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(smsalice, _mockSMSAliceService.Object, _mockOperadorReader.Object, _mockTipoEMailReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    public virtual void Dispose()
    {
        GC.SuppressFinalize(this);
    }
}