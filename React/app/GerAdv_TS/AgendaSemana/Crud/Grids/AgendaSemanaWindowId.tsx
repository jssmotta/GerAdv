// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// WindowId.tsx.txt
import React, { useEffect, useMemo } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { IAgendaSemana } from "../../Interfaces/interface.AgendaSemana";
import { AgendaSemanaService } from "../../Services/AgendaSemana.service";
import { AgendaSemanaApi } from "../../Apis/AgendaSemanaApi";
import AgendaSemanaWindow from "./AgendaSemanaWindow";
import { AgendaSemanaEmpty } from "@/app/GerAdv_TS/Models/AgendaSemana";

interface AgendaSemanaWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}

const AgendaSemanaWindowId: React.FC<AgendaSemanaWindowIdProps> = ({
  isOpen,
  onClose,
  id,
  onSuccess,
  onError,
}) => {
  const systemContext = useAppSelector(selectSystemContext);
  const agendasemanaService = useMemo(() => {
    return new AgendaSemanaService(
      new AgendaSemanaApi(systemContext?.Uri ?? "", systemContext?.Token ?? ""),
    );
  }, [systemContext?.Uri, systemContext?.Token]);

  const [data, setData] = React.useState<IAgendaSemana | null>(null);

  useEffect(() => {
    const fetchData = async () => {
      if (id !== null && id === 0) {
        setData(AgendaSemanaEmpty() as IAgendaSemana);
        return;
      }
      if (id) {
        const response = await agendasemanaService.fetchAgendaSemanaById(
          id ?? 0,
        );
        setData(response);
      }
    };
    fetchData();
  }, [isOpen]);

  if (!isOpen) return null;

  return (
    <>
      {data && (
        <AgendaSemanaWindow
          isOpen={isOpen}
          onClose={onClose}
          selectedAgendaSemana={data}
          onSuccess={onSuccess}
          onError={onError}
        />
      )}
    </>
  );
};

export default AgendaSemanaWindowId;
