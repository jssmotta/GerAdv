// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import HorasTrabWindowId from '@/app/GerAdv_TS/HorasTrab/Crud/Grids/HorasTrabWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import HorasTrabWindowId from '@/app/GerAdv_TS/HorasTrab/Crud/Grids/HorasTrabWindowId';
import { HorasTrabEmpty } from '@/app/GerAdv_TS/Models/HorasTrab';

describe('HorasTrabWindowId (basic)', () => {
  it('exports the component', () => {
    expect(HorasTrabWindowId).toBeDefined();
    expect(typeof HorasTrabWindowId === 'function' || typeof HorasTrabWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the HorasTrabService class so constructor usage doesn't throw and we can control fetchHorasTrabById
const mockFetchHorasTrabById = jest.fn();
jest.mock('@/app/GerAdv_TS/HorasTrab/Services/HorasTrab.service', () => ({
  HorasTrabService: jest.fn().mockImplementation(() => ({
    fetchHorasTrabById: (...args: any[]) => mockFetchHorasTrabById(...args),
  })),
}));

// Mock the child HorasTrabWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/HorasTrab/Crud/Grids/HorasTrabWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='horastrab-window' {...p} />,
}));

describe('HorasTrabWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(HorasTrabWindowId).toBeDefined();
    expect(typeof HorasTrabWindowId === 'function' || typeof HorasTrabWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <HorasTrabWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders HorasTrabWindow with HorasTrabEmpty when id is 0', async () => {
    render(
      <HorasTrabWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // HorasTrabWindow should be rendered with selectedHorasTrab equal to HorasTrabEmpty()
    const node = await screen.findByTestId('horastrab-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedHorasTrab"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedhorastrab') || node.getAttribute('selectedHorasTrab')).not.toBeNull();
  });

  it('fetches horastrab by id and passes it to HorasTrabWindow when id > 0', async () => {
    const fakeHorasTrab = { id: 123, nomecliente: 'Test' };
    mockFetchHorasTrabById.mockResolvedValue(fakeHorasTrab);

    render(
      <HorasTrabWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('horastrab-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchHorasTrabById).toHaveBeenCalledWith(123));
  });
});
