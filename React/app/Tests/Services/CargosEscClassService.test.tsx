// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { CargosEscClassService, CargosEscClassValidator } from '@/app/GerAdv_TS/CargosEscClass/Services/CargosEscClass.service';
import { CargosEscClassApi, CargosEscClassApiError } from '@/app/GerAdv_TS/CargosEscClass/Apis/ApiCargosEscClass';
import { ICargosEscClass } from '@/app/GerAdv_TS/CargosEscClass/Interfaces/interface.CargosEscClass';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as CargosEscClassApi;

const sample: ICargosEscClass = { id: 1, nome: 'Teste' };

describe('CargosEscClassService', () => {
  let service: CargosEscClassService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new CargosEscClassService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(CargosEscClassValidator, 'validateCargosEscClass').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchCargosEscClassById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchCargosEscClassById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchCargosEscClassById lança CargosEscClassApiError para id inválido', async () => {
    await expect(service.fetchCargosEscClassById(0)).rejects.toBeInstanceOf(CargosEscClassApiError);
  });

  it('saveCargosEscClass chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveCargosEscClass(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveCargosEscClass lança CargosEscClassApiError quando validação falha', async () => {
    (CargosEscClassValidator.validateCargosEscClass as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveCargosEscClass(sample)).rejects.toBeInstanceOf(CargosEscClassApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteCargosEscClass chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteCargosEscClass(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteCargosEscClass lança CargosEscClassApiError para id inválido', async () => {
    await expect(service.deleteCargosEscClass(0)).rejects.toBeInstanceOf(CargosEscClassApiError);
  });

  it('validateCargosEscClass delega para CargosEscClassValidator', () => {
    (CargosEscClassValidator.validateCargosEscClass as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateCargosEscClass(sample);
    expect(r.isValid).toBe(true);
  });
});
