// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { StatusInstanciaService, StatusInstanciaValidator } from '@/app/GerAdv_TS/StatusInstancia/Services/StatusInstancia.service';
import { StatusInstanciaApi, StatusInstanciaApiError } from '@/app/GerAdv_TS/StatusInstancia/Apis/ApiStatusInstancia';
import { IStatusInstancia } from '@/app/GerAdv_TS/StatusInstancia/Interfaces/interface.StatusInstancia';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as StatusInstanciaApi;

const sample: IStatusInstancia = { id: 1, nome: 'Teste' };

describe('StatusInstanciaService', () => {
  let service: StatusInstanciaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new StatusInstanciaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(StatusInstanciaValidator, 'validateStatusInstancia').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchStatusInstanciaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchStatusInstanciaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchStatusInstanciaById lança StatusInstanciaApiError para id inválido', async () => {
    await expect(service.fetchStatusInstanciaById(0)).rejects.toBeInstanceOf(StatusInstanciaApiError);
  });

  it('saveStatusInstancia chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveStatusInstancia(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveStatusInstancia lança StatusInstanciaApiError quando validação falha', async () => {
    (StatusInstanciaValidator.validateStatusInstancia as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveStatusInstancia(sample)).rejects.toBeInstanceOf(StatusInstanciaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteStatusInstancia chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteStatusInstancia(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteStatusInstancia lança StatusInstanciaApiError para id inválido', async () => {
    await expect(service.deleteStatusInstancia(0)).rejects.toBeInstanceOf(StatusInstanciaApiError);
  });

  it('validateStatusInstancia delega para StatusInstanciaValidator', () => {
    (StatusInstanciaValidator.validateStatusInstancia as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateStatusInstancia(sample);
    expect(r.isValid).toBe(true);
  });
});
