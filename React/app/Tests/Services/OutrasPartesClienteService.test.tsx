// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OutrasPartesClienteService, OutrasPartesClienteValidator } from '@/app/GerAdv_TS/OutrasPartesCliente/Services/OutrasPartesCliente.service';
import { OutrasPartesClienteApi, OutrasPartesClienteApiError } from '@/app/GerAdv_TS/OutrasPartesCliente/Apis/ApiOutrasPartesCliente';
import { IOutrasPartesCliente } from '@/app/GerAdv_TS/OutrasPartesCliente/Interfaces/interface.OutrasPartesCliente';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OutrasPartesClienteApi;

const sample: IOutrasPartesCliente = { id: 1, nome: 'Teste' };

describe('OutrasPartesClienteService', () => {
  let service: OutrasPartesClienteService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OutrasPartesClienteService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OutrasPartesClienteValidator, 'validateOutrasPartesCliente').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOutrasPartesClienteById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOutrasPartesClienteById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOutrasPartesClienteById lança OutrasPartesClienteApiError para id inválido', async () => {
    await expect(service.fetchOutrasPartesClienteById(0)).rejects.toBeInstanceOf(OutrasPartesClienteApiError);
  });

  it('saveOutrasPartesCliente chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOutrasPartesCliente(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOutrasPartesCliente lança OutrasPartesClienteApiError quando validação falha', async () => {
    (OutrasPartesClienteValidator.validateOutrasPartesCliente as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOutrasPartesCliente(sample)).rejects.toBeInstanceOf(OutrasPartesClienteApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOutrasPartesCliente chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOutrasPartesCliente(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOutrasPartesCliente lança OutrasPartesClienteApiError para id inválido', async () => {
    await expect(service.deleteOutrasPartesCliente(0)).rejects.toBeInstanceOf(OutrasPartesClienteApiError);
  });

  it('validateOutrasPartesCliente delega para OutrasPartesClienteValidator', () => {
    (OutrasPartesClienteValidator.validateOutrasPartesCliente as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOutrasPartesCliente(sample);
    expect(r.isValid).toBe(true);
  });
});
