// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import OperadoresWindowId from '@/app/GerAdv_TS/Operadores/Crud/Grids/OperadoresWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import OperadoresWindowId from '@/app/GerAdv_TS/Operadores/Crud/Grids/OperadoresWindowId';
import { OperadoresEmpty } from '@/app/GerAdv_TS/Models/Operadores';

describe('OperadoresWindowId (basic)', () => {
  it('exports the component', () => {
    expect(OperadoresWindowId).toBeDefined();
    expect(typeof OperadoresWindowId === 'function' || typeof OperadoresWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the OperadoresService class so constructor usage doesn't throw and we can control fetchOperadoresById
const mockFetchOperadoresById = jest.fn();
jest.mock('@/app/GerAdv_TS/Operadores/Services/Operadores.service', () => ({
  OperadoresService: jest.fn().mockImplementation(() => ({
    fetchOperadoresById: (...args: any[]) => mockFetchOperadoresById(...args),
  })),
}));

// Mock the child OperadoresWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Operadores/Crud/Grids/OperadoresWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='operadores-window' {...p} />,
}));

describe('OperadoresWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(OperadoresWindowId).toBeDefined();
    expect(typeof OperadoresWindowId === 'function' || typeof OperadoresWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <OperadoresWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders OperadoresWindow with OperadoresEmpty when id is 0', async () => {
    render(
      <OperadoresWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // OperadoresWindow should be rendered with selectedOperadores equal to OperadoresEmpty()
    const node = await screen.findByTestId('operadores-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedOperadores"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedoperadores') || node.getAttribute('selectedOperadores')).not.toBeNull();
  });

  it('fetches operadores by id and passes it to OperadoresWindow when id > 0', async () => {
    const fakeOperadores = { id: 123, nomecliente: 'Test' };
    mockFetchOperadoresById.mockResolvedValue(fakeOperadores);

    render(
      <OperadoresWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('operadores-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchOperadoresById).toHaveBeenCalledWith(123));
  });
});
