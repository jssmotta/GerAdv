// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Validations;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial interface IContatoCRMValidation
{
    Task<bool> ValidateReg(Models.ContatoCRM reg, IContatoCRMService service, IOperadorReader operadorReader, IClientesReader clientesReader, IProcessosReader processosReader, ITipoContatoCRMReader tipocontatocrmReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
    Task<bool> CanDelete(int id, IContatoCRMService service, IContatoCRMOperadorService contatocrmoperadorService, IDocsRecebidosItensService docsrecebidositensService, IRecadosService recadosService, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
}

public class ContatoCRMValidation : IContatoCRMValidation
{
    public async Task<bool> CanDelete(int id, IContatoCRMService service, IContatoCRMOperadorService contatocrmoperadorService, IDocsRecebidosItensService docsrecebidositensService, IRecadosService recadosService, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (id <= 0)
            throw new SGValidationException("Id inválido");
        var reg = await service.GetById(id, uri, default);
        if (reg == null)
            throw new SGValidationException($"Registro com id {id} não encontrado.");
        var contatocrmoperadorExists0 = await contatocrmoperadorService.Filter(new Filters.FilterContatoCRMOperador { ContatoCRM = id }, uri);
        if (contatocrmoperadorExists0 != null && contatocrmoperadorExists0.Any())
            throw new SGValidationException("Não é possível excluir o registro, pois existem registros da tabela Contato C R M Operador associados a ele.");
        var docsrecebidositensExists1 = await docsrecebidositensService.Filter(new Filters.FilterDocsRecebidosItens { ContatoCRM = id }, uri);
        if (docsrecebidositensExists1 != null && docsrecebidositensExists1.Any())
            throw new SGValidationException("Não é possível excluir o registro, pois existem registros da tabela Docs Recebidos Itens associados a ele.");
        var recadosExists2 = await recadosService.Filter(new Filters.FilterRecados { ContatoCRM = id }, uri);
        if (recadosExists2 != null && recadosExists2.Any())
            throw new SGValidationException("Não é possível excluir o registro, pois existem registros da tabela Recados associados a ele.");
        return true;
    }

    private bool ValidSizes(Models.ContatoCRM reg)
    {
        if (reg.Assunto.Length > 255)
            throw new SGValidationException($"Assunto deve ter no máximo 255 caracteres.");
        if (reg.PessoaContato.Length > 255)
            throw new SGValidationException($"PessoaContato deve ter no máximo 255 caracteres.");
        if (reg.GUID.Length > 100)
            throw new SGValidationException($"GUID deve ter no máximo 100 caracteres.");
        return true;
    }

    public async Task<bool> ValidateReg(Models.ContatoCRM reg, IContatoCRMService service, IOperadorReader operadorReader, IClientesReader clientesReader, IProcessosReader processosReader, ITipoContatoCRMReader tipocontatocrmReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (reg == null)
            throw new SGValidationException("Objeto está nulo");
        if (string.IsNullOrWhiteSpace(reg.Data))
            throw new SGValidationException("Data é obrigatório");
        var validSizes = ValidSizes(reg);
        if (!validSizes)
            return false;
        // Operador
        if (!reg.Operador.IsEmptyIDNumber())
        {
            var regOperador = await operadorReader.Read(reg.Operador, oCnn);
            if (regOperador == null || regOperador.Id != reg.Operador)
            {
                throw new SGValidationException($"Operador não encontrado ({regOperador?.Id}).");
            }
        }

        // Clientes
        if (!reg.Cliente.IsEmptyIDNumber())
        {
            var regClientes = await clientesReader.Read(reg.Cliente, oCnn);
            if (regClientes == null || regClientes.Id != reg.Cliente)
            {
                throw new SGValidationException($"Clientes não encontrado ({regClientes?.Id}).");
            }
        }

        // Processos
        if (!reg.Processo.IsEmptyIDNumber())
        {
            var regProcessos = await processosReader.Read(reg.Processo, oCnn);
            if (regProcessos == null || regProcessos.Id != reg.Processo)
            {
                throw new SGValidationException($"Processos não encontrado ({regProcessos?.Id}).");
            }
        }

        // TipoContatoCRM
        if (!reg.TipoContatoCRM.IsEmptyIDNumber())
        {
            var regTipoContatoCRM = await tipocontatocrmReader.Read(reg.TipoContatoCRM, oCnn);
            if (regTipoContatoCRM == null || regTipoContatoCRM.Id != reg.TipoContatoCRM)
            {
                throw new SGValidationException($"Tipo Contato C R M não encontrado ({regTipoContatoCRM?.Id}).");
            }
        }

        return true;
    }
}