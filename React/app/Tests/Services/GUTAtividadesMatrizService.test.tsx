// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTAtividadesMatrizService, GUTAtividadesMatrizValidator } from '@/app/GerAdv_TS/GUTAtividadesMatriz/Services/GUTAtividadesMatriz.service';
import { GUTAtividadesMatrizApi, GUTAtividadesMatrizApiError } from '@/app/GerAdv_TS/GUTAtividadesMatriz/Apis/ApiGUTAtividadesMatriz';
import { IGUTAtividadesMatriz } from '@/app/GerAdv_TS/GUTAtividadesMatriz/Interfaces/interface.GUTAtividadesMatriz';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTAtividadesMatrizApi;

const sample: IGUTAtividadesMatriz = { id: 1, nome: 'Teste' };

describe('GUTAtividadesMatrizService', () => {
  let service: GUTAtividadesMatrizService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTAtividadesMatrizService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTAtividadesMatrizValidator, 'validateGUTAtividadesMatriz').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTAtividadesMatrizById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTAtividadesMatrizById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTAtividadesMatrizById lança GUTAtividadesMatrizApiError para id inválido', async () => {
    await expect(service.fetchGUTAtividadesMatrizById(0)).rejects.toBeInstanceOf(GUTAtividadesMatrizApiError);
  });

  it('saveGUTAtividadesMatriz chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTAtividadesMatriz(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTAtividadesMatriz lança GUTAtividadesMatrizApiError quando validação falha', async () => {
    (GUTAtividadesMatrizValidator.validateGUTAtividadesMatriz as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTAtividadesMatriz(sample)).rejects.toBeInstanceOf(GUTAtividadesMatrizApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTAtividadesMatriz chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTAtividadesMatriz(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTAtividadesMatriz lança GUTAtividadesMatrizApiError para id inválido', async () => {
    await expect(service.deleteGUTAtividadesMatriz(0)).rejects.toBeInstanceOf(GUTAtividadesMatrizApiError);
  });

  it('validateGUTAtividadesMatriz delega para GUTAtividadesMatrizValidator', () => {
    (GUTAtividadesMatrizValidator.validateGUTAtividadesMatriz as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTAtividadesMatriz(sample);
    expect(r.isValid).toBe(true);
  });
});
