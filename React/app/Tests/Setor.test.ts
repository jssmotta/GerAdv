// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useSetorForm, useSetorList, useValidationsSetor } from '../GerAdv_TS/Setor/Hooks/hookSetor';
import { ISetor } from '../GerAdv_TS/Setor/Interfaces/interface.Setor';
import { ISetorService } from '../GerAdv_TS/Setor/Services/Setor.service';
import { SetorTestEmpty } from '../GerAdv_TS/Models/Setor';
import { useSetorComboBox } from '../GerAdv_TS/Setor/Hooks/hookSetor';

// Mock do serviço
const mockSetorService: jest.Mocked<ISetorService> = {
  fetchSetorById: jest.fn(),
  saveSetor: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteSetor: jest.fn(),
  validateSetor: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialSetor: ISetor = { ...SetorTestEmpty() };

describe('useSetorForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    expect(result.current.data).toEqual(initialSetor);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Setor',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Setor');
  });

   test('deve carregar Setor por ID', async () => {
    const mockSetor = { ...initialSetor, id: 1, descricao: 'Setor Teste' };
    mockSetorService.fetchSetorById.mockResolvedValue(mockSetor);

    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    await act(async () => {
      await result.current.loadSetor(1);
    });

    expect(mockSetorService.fetchSetorById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockSetor);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Setor', async () => {
    const errorMessage = 'Erro ao carregar Setor';
    mockSetorService.fetchSetorById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    await act(async () => {
      await result.current.loadSetor(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialSetor, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialSetor);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useSetorForm(initialSetor, mockSetorService)
    );

    await act(async () => {
      await result.current.loadSetor(0);
    });

    expect(mockSetorService.fetchSetorById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialSetor);
  });
});

describe('useSetorList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useSetorList(mockSetorService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialSetor, id: 1, descricao: 'Setor 1' },
      { ...initialSetor, id: 2, descricao: 'Setor 2' }
    ];
    mockSetorService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useSetorList(mockSetorService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockSetorService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockSetorService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useSetorList(mockSetorService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialSetor, id: 1, descricao: 'Setor Filtrado' }];
    const filtro = { descricao: 'Setor' };
    mockSetorService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useSetorList(mockSetorService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockSetorService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsSetor', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsSetor());

    const validData = { ...initialSetor, descricao: 'Setor Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsSetor());

    const invalidData = { ...initialSetor, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsSetor());

    const invalidData = { 
      ...initialSetor, 
      descricao: 'a'.repeat(40+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 40 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsSetor());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialSetor, id: 1, descricao: 'Setor Teste' }];
    mockSetorService.getAll.mockResolvedValue(mockData);
    mockSetorService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useSetorList(mockSetorService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsSetor()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Setor Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Setor Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Setor 1' },
      { id: 2, descricao: 'Setor 2' }
    ];
    mockSetorService.getList.mockResolvedValue(mockOptions as ISetor[]);


    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Setor 1' },
        { id: 2, nome: 'Setor 2' }
      ]);
    });


    expect(mockSetorService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Setor ABC' },
      { id: 2, descricao: 'Setor XYZ' }
    ];
    mockSetorService.getList.mockResolvedValue(mockOptions as ISetor[]);   


 const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Setor ABC' },
        { id: 2, nome: 'Setor XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Setor ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Setor ABC' },
      { id: 2, descricao: 'Setor XYZ' }
    ];
    mockSetorService.getList.mockResolvedValue(mockOptions as ISetor[]);
  


    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Setor ABC' },
        { id: 2, nome: 'Setor XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Setor ABC' },
          {id: 2, nome: 'Setor XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );

    const newValue = { id: 1, descricao: 'Setor Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Setor Inicial' };
    
    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useSetorComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Setor Inicial' };
    
    const { result } = renderHook(() => 
      useSetorComboBox(mockSetorService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






