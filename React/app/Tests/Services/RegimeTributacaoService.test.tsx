// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { RegimeTributacaoService, RegimeTributacaoValidator } from '@/app/GerAdv_TS/RegimeTributacao/Services/RegimeTributacao.service';
import { RegimeTributacaoApi, RegimeTributacaoApiError } from '@/app/GerAdv_TS/RegimeTributacao/Apis/ApiRegimeTributacao';
import { IRegimeTributacao } from '@/app/GerAdv_TS/RegimeTributacao/Interfaces/interface.RegimeTributacao';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as RegimeTributacaoApi;

const sample: IRegimeTributacao = { id: 1, nome: 'Teste' };

describe('RegimeTributacaoService', () => {
  let service: RegimeTributacaoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new RegimeTributacaoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(RegimeTributacaoValidator, 'validateRegimeTributacao').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchRegimeTributacaoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchRegimeTributacaoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchRegimeTributacaoById lança RegimeTributacaoApiError para id inválido', async () => {
    await expect(service.fetchRegimeTributacaoById(0)).rejects.toBeInstanceOf(RegimeTributacaoApiError);
  });

  it('saveRegimeTributacao chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveRegimeTributacao(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveRegimeTributacao lança RegimeTributacaoApiError quando validação falha', async () => {
    (RegimeTributacaoValidator.validateRegimeTributacao as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveRegimeTributacao(sample)).rejects.toBeInstanceOf(RegimeTributacaoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteRegimeTributacao chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteRegimeTributacao(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteRegimeTributacao lança RegimeTributacaoApiError para id inválido', async () => {
    await expect(service.deleteRegimeTributacao(0)).rejects.toBeInstanceOf(RegimeTributacaoApiError);
  });

  it('validateRegimeTributacao delega para RegimeTributacaoValidator', () => {
    (RegimeTributacaoValidator.validateRegimeTributacao as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateRegimeTributacao(sample);
    expect(r.isValid).toBe(true);
  });
});
