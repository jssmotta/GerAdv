// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:GUTMatriz
// Fonte:ValidationsGenerator
namespace MenphisSI.GerAdv.Validations;
public partial interface IGUTMatrizValidation
{
    Task<bool> ValidateReg(Models.GUTMatriz reg, IGUTMatrizService service, IGUTTipoReader guttipoReader, [FromRoute, Required] string uri, MsiSqlConnection? oCnn);
    Task<bool> CanDelete(int? id, IGUTMatrizService service, IGUTAtividadesMatrizService gutatividadesmatrizService, [FromRoute, Required] string uri, MsiSqlConnection? oCnn);
}

public class GUTMatrizValidation : IGUTMatrizValidation
{
    public async Task<bool> CanDelete(int? id, IGUTMatrizService service, IGUTAtividadesMatrizService gutatividadesmatrizService, [FromRoute, Required] string uri, MsiSqlConnection? oCnn)
    {
        if (id == null || id <= 0)
            throw new SGValidationException("Id inválido");
        var reg = await service.GetById(id ?? default, uri, default);
        if (reg == null)
            throw new SGValidationException($"Registro com id {id} não encontrado.");
        return true;
    }

    private bool ValidSizes(Models.GUTMatriz reg)
    {
        if (reg.Descricao != null && reg.Descricao.Length > DBGUTMatrizDicInfo.GutDescricao.FTamanho)
            throw new SGValidationException($"Descricao deve ter no máximo {DBGUTMatrizDicInfo.GutDescricao.FTamanho} caracteres.");
        return true;
    }

    public async Task<bool> ValidateReg(Models.GUTMatriz reg, IGUTMatrizService service, IGUTTipoReader guttipoReader, [FromRoute, Required] string uri, MsiSqlConnection? oCnn)
    {
        if (reg == null)
            throw new SGValidationException("Objeto está nulo");
        if (string.IsNullOrWhiteSpace(reg.Descricao))
            throw new SGValidationException("Descrição é obrigatório");
        if (reg.Descricao.Contains("%"))
            throw new SGValidationException("Descrição possui caracter inválido (%)");
        var validSizes = ValidSizes(reg);
        if (!validSizes)
            return false;
        if (reg.Descricao.IsEmpty())
            throw new SGValidationException("Descricao é obrigatório.");
        if (reg.GUTTipo == 0)
            throw new SGValidationException("GUTTipo é obrigatório.");
        if (reg.Valor == 0)
            throw new SGValidationException("Valor é obrigatório.");
        // GUTTipo
        {
            var regGUTTipo = await guttipoReader.ReadAsync(reg.GUTTipo, oCnn);
            if (regGUTTipo == null || regGUTTipo.Id != reg.GUTTipo)
            {
                throw new SGValidationException($"G U T Tipo não encontrado ({regGUTTipo?.Id}).");
            }
        }

        return true;
    }
}