// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IAcao } from "@/app/GerAdv_TS/Acao/Interfaces/interface.Acao";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { AcaoApi } from "../../Apis/AcaoApi";
import { useValidationsAcao } from "../../Hooks/hookAcao";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpAcao from "../../Help/HelpAcao";
import AcaoPage from "@/app/pages/acao/page";

import JusticaComboBox from "@/app/GerAdv_TS/Justica/ComboBox/JusticaComboBox";
import AreaComboBox from "@/app/GerAdv_TS/Area/ComboBox/AreaComboBox";
import { JusticaApi } from "@/app/GerAdv_TS/Justica/Apis/JusticaApi";
import { AreaApi } from "@/app/GerAdv_TS/Area/Apis/AreaApi";
import InputDescription from "@/app/components/Inputs/InputDescription";

interface AcaoFormProps {
  acaoData: IAcao;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const AcaoForm: React.FC<AcaoFormProps> = ({
  acaoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new AcaoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsAcao();

  const [_nomeJustica, setNomeJustica] = useState("");
  const justicaApi = new JusticaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeArea, setNomeArea] = useState("");
  const areaApi = new AreaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("justica") > 0) {
    if (acaoData.id === 0 && acaoData.justica == 0) {
      justicaApi
        .getById(getParamFromUrl("justica"))
        .then((response) => {
          setNomeJustica(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      acaoData.justica = getParamFromUrl("justica");
    }
  }

  if (getParamFromUrl("area") > 0) {
    if (acaoData.id === 0 && acaoData.area == 0) {
      areaApi
        .getById(getParamFromUrl("area"))
        .then((response) => {
          setNomeArea(response.data.descricao);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      acaoData.area = getParamFromUrl("area");
    }
  }
  const addValorJustica = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "justica", value: e.id } });
  };
  const addValorArea = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "area", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Acao:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`AcaoForm-${acaoData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Acao diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent = acaoData?.id == 0 ? "Editar Acao" : "Adicionar Acao";
    }
  }, [acaoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Acao"
            : "form-container5 form-container-Acao"
        }
      >
        <form
          className="formInputCadInc"
          id={`AcaoForm-${acaoData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Acao"
            data={acaoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`AcaoForm-${acaoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputDescription
              tabIndex={0}
              type="text"
              id="descricao"
              label="Acao"
              dataForm={acaoData}
              className="input-default-main"
              name="descricao"
              value={acaoData.descricao}
              placeholder={`Digite nome/descrição do acao`}
              onChange={onChange}
              required
            />

            <JusticaComboBox
              name={"justica"}
              dataForm={acaoData}
              value={acaoData.justica}
              setValue={addValorJustica}
              label={"Justiça"}
            />

            <AreaComboBox
              name={"area"}
              dataForm={acaoData}
              value={acaoData.area}
              setValue={addValorArea}
              label={"Área"}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Acao" windowCrud={AcaoPage} />

          <CopyButton formId={`AcaoForm-${acaoData.id}`} />

          <PrintButton formId={`AcaoForm-${acaoData.id}`} />

          <AuditorButton tableName="Acao" coolName="Acao" id={acaoData.id} />

          <DeleteButton
            page={"/pages/acao"}
            id={acaoData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Acao"
            page={(onClose) => <HelpAcao onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Acao"
          data={acaoData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`AcaoForm-${acaoData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
