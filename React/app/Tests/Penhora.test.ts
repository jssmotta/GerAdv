// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { usePenhoraForm, usePenhoraList, useValidationsPenhora } from '../GerAdv_TS/Penhora/Hooks/hookPenhora';
import { IPenhora } from '../GerAdv_TS/Penhora/Interfaces/interface.Penhora';
import { IPenhoraService } from '../GerAdv_TS/Penhora/Services/Penhora.service';
import { PenhoraTestEmpty } from '../GerAdv_TS/Models/Penhora';
import { usePenhoraComboBox } from '../GerAdv_TS/Penhora/Hooks/hookPenhora';

// Mock do serviço
const mockPenhoraService: jest.Mocked<IPenhoraService> = {
  fetchPenhoraById: jest.fn(),
  savePenhora: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deletePenhora: jest.fn(),
  validatePenhora: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialPenhora: IPenhora = { ...PenhoraTestEmpty() };

describe('usePenhoraForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    expect(result.current.data).toEqual(initialPenhora);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Penhora',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Penhora');
  });

   test('deve carregar Penhora por ID', async () => {
    const mockPenhora = { ...initialPenhora, id: 1, nome: 'Penhora Teste' };
    mockPenhoraService.fetchPenhoraById.mockResolvedValue(mockPenhora);

    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    await act(async () => {
      await result.current.loadPenhora(1);
    });

    expect(mockPenhoraService.fetchPenhoraById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockPenhora);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Penhora', async () => {
    const errorMessage = 'Erro ao carregar Penhora';
    mockPenhoraService.fetchPenhoraById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    await act(async () => {
      await result.current.loadPenhora(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialPenhora, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialPenhora);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      usePenhoraForm(initialPenhora, mockPenhoraService)
    );

    await act(async () => {
      await result.current.loadPenhora(0);
    });

    expect(mockPenhoraService.fetchPenhoraById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialPenhora);
  });
});

describe('usePenhoraList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePenhoraList(mockPenhoraService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialPenhora, id: 1, nome: 'Penhora 1' },
      { ...initialPenhora, id: 2, nome: 'Penhora 2' }
    ];
    mockPenhoraService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePenhoraList(mockPenhoraService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockPenhoraService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockPenhoraService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePenhoraList(mockPenhoraService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialPenhora, id: 1, nome: 'Penhora Filtrado' }];
    const filtro = { nome: 'Penhora' };
    mockPenhoraService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePenhoraList(mockPenhoraService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockPenhoraService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsPenhora', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsPenhora());

    const validData = { ...initialPenhora, nome: 'Penhora Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsPenhora());

    const invalidData = { ...initialPenhora, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsPenhora());

    const invalidData = { 
      ...initialPenhora, 
      nome: 'a'.repeat(255+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 255 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsPenhora());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialPenhora, id: 1, nome: 'Penhora Teste' }];
    mockPenhoraService.getAll.mockResolvedValue(mockData);
    mockPenhoraService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      usePenhoraList(mockPenhoraService)
    );
    
     const { result: comboResult } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsPenhora()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Penhora Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Penhora Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora 1' },
      { id: 2, nome: 'Penhora 2' }
    ];
    mockPenhoraService.getList.mockResolvedValue(mockOptions as IPenhora[]);


    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora 1' },
        { id: 2, nome: 'Penhora 2' }
      ]);
    });


    expect(mockPenhoraService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora ABC' },
      { id: 2, nome: 'Penhora XYZ' }
    ];
    mockPenhoraService.getList.mockResolvedValue(mockOptions as IPenhora[]);   


 const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora ABC' },
        { id: 2, nome: 'Penhora XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Penhora ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora ABC' },
      { id: 2, nome: 'Penhora XYZ' }
    ];
    mockPenhoraService.getList.mockResolvedValue(mockOptions as IPenhora[]);
  


    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora ABC' },
        { id: 2, nome: 'Penhora XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Penhora ABC' },
          {id: 2, nome: 'Penhora XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );

    const newValue = { id: 1, nome: 'Penhora Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Penhora Inicial' };
    
    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('usePenhoraComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Penhora Inicial' };
    
    const { result } = renderHook(() => 
      usePenhoraComboBox(mockPenhoraService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






