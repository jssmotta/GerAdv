// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { FuncionariosService, FuncionariosValidator } from '@/app/GerAdv_TS/Funcionarios/Services/Funcionarios.service';
import { FuncionariosApi, FuncionariosApiError } from '@/app/GerAdv_TS/Funcionarios/Apis/ApiFuncionarios';
import { IFuncionarios } from '@/app/GerAdv_TS/Funcionarios/Interfaces/interface.Funcionarios';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as FuncionariosApi;

const sample: IFuncionarios = { id: 1, nome: 'Teste' };

describe('FuncionariosService', () => {
  let service: FuncionariosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new FuncionariosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(FuncionariosValidator, 'validateFuncionarios').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchFuncionariosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchFuncionariosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchFuncionariosById lança FuncionariosApiError para id inválido', async () => {
    await expect(service.fetchFuncionariosById(0)).rejects.toBeInstanceOf(FuncionariosApiError);
  });

  it('saveFuncionarios chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveFuncionarios(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveFuncionarios lança FuncionariosApiError quando validação falha', async () => {
    (FuncionariosValidator.validateFuncionarios as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveFuncionarios(sample)).rejects.toBeInstanceOf(FuncionariosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteFuncionarios chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteFuncionarios(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteFuncionarios lança FuncionariosApiError para id inválido', async () => {
    await expect(service.deleteFuncionarios(0)).rejects.toBeInstanceOf(FuncionariosApiError);
  });

  it('validateFuncionarios delega para FuncionariosValidator', () => {
    (FuncionariosValidator.validateFuncionarios as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateFuncionarios(sample);
    expect(r.isValid).toBe(true);
  });
});
