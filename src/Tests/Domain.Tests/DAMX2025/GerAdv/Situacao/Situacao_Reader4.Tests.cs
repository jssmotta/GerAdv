// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using MenphisSI.GerAdv.Models.Response.All;
using MenphisSI.GerAdv.Readers;
using System.Data;

namespace MenphisSI.GerAdv.Tests.Readers;
/// <summary>
/// Comprehensive unit tests for SituacaoReader class
/// Tests all public methods including async operations, data reading, and data transformation
/// </summary>
public class SituacaoReaderTests : IDisposable
{
    private readonly Mock<IFSituacaoFactory> _mockSituacaoFactory;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IDataRecord> _mockDataRecord;
    private readonly SituacaoReader _situacaoReader;
    public SituacaoReaderTests()
    {
        _mockSituacaoFactory = new Mock<IFSituacaoFactory>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockDataRecord = new Mock<IDataRecord>();
        _situacaoReader = new SituacaoReader(_mockSituacaoFactory.Object);
    }

#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldInitializeReader()
    {
        // Act & Assert
        _situacaoReader.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new SituacaoReader(null !));
    }

#endregion
#region ListarN Tests
    [Fact]
    public async Task ListarN_WithValidParameters_ShouldCallDevourerSqlData()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "sitCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "sitparte_int";
        // Act & Assert
        // Since this calls static methods and external dependencies,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task ListarN_WithEmptyParameters_ShouldThrowException()
    {
        // Arrange
        var max = 0;
        var uri = string.Empty; // Empty URI should cause an exception
        var cWhere = string.Empty;
        var parameters = new List<SqlParameter>();
        var order = string.Empty;
        // Act & Assert
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.ListarN(max, uri, cWhere, parameters, order));
    }

#endregion
#region Listar Tests
    [Fact]
    public async Task Listar_WithValidParameters_ShouldCallListarTabela()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "sitCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // Since this calls external dependencies and database connections,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

    [Fact]
    public async Task Listar_WithCancellationToken_ShouldRespectCancellation()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri";
        var cWhere = "carCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = new CancellationToken(true); // Already cancelled
        // Act & Assert
        // Even with cancellation, this should throw an exception due to invalid URI
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Read Tests with ID and Connection
    [Fact]
    public async Task Read_WithValidId_ShouldReturnSituacaoResponse()
    {
        // Arrange
        var id = 123;
        var expectedSituacao = new FSituacao
        {
            ID = id,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedSituacao);
        // Act
        var result = await _situacaoReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task Read_WithEmptyId_ShouldReturnNull()
    {
        // Arrange
        var id = 0; // Empty ID
        var emptyFSituacao = new FSituacao
        {
            ID = 0
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFSituacao);
        // Act
        var result = await _situacaoReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public async Task Read_WithNegativeId_ShouldReturnNull()
    {
        // Arrange
        var id = -1;
        var emptyFSituacao = new FSituacao
        {
            ID = 0
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFSituacao);
        // Act
        var result = await _situacaoReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region ReadM Tests
    [Fact]
    public async Task ReadM_WithValidId_ShouldReturnSituacaoModel()
    {
        // Arrange
        var id = 123;
        var expectedFSituacao = new FSituacao
        {
            ID = id,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFSituacao);
        // Act
        var result = await _situacaoReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task ReadM_WithEmptyId_ShouldReturnModelWithDefaults()
    {
        // Arrange
        var id = 0;
        var emptyFSituacao = new FSituacao
        {
            ID = 0,
            FParte_Int = null
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFSituacao);
        // Act
        var result = await _situacaoReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(0);
        result?.Parte_Int.Should().Be(string.Empty);
    }

#endregion
#region Read with FSituacao and Connection Tests
    [Fact]
    public void Read_WithValidFSituacaoAndConnection_ShouldReturnSituacaoResponse()
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _situacaoReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFSituacaoAndConnection_ShouldReturnNull()
    {
        // Arrange
        FSituacao? dbRec = null;
        // Act
        var result = _situacaoReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with Where and Parameters Tests
    [Fact]
    public void Read_WithWhereAndParameters_ShouldReturnSituacaoResponse()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedFSituacao = new FSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(expectedFSituacao);
        // Act
        var result = _situacaoReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithWhereAndParameters_WhenRecordNotFound_ShouldReturnNull()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 999)
        };
        var emptyFSituacao = new FSituacao
        {
            ID = 0
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Returns(emptyFSituacao);
        // Act
        var result = _situacaoReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with FSituacao Tests
    [Fact]
    public void Read_WithValidFSituacao_ShouldReturnSituacaoResponse()
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFSituacao_ShouldReturnNull()
    {
        // Arrange
        FSituacao? dbRec = null;
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithFSituacaoNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = 123,
            FParte_Int = null
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be(string.Empty);
    }

#endregion
#region Read with DBSituacao Tests
    [Fact]
    public void Read_WithValidSGDBSituacao_ShouldReturnSituacaoResponse()
    {
        // Arrange
        var dbRec = new DBSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullSGDBSituacao_ShouldReturnNull()
    {
        // Arrange
        DBSituacao? dbRec = null;
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithSGDBSituacaoNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBSituacao
        {
            ID = 123,
            FParte_Int = null // This should result in empty string in response
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with FSituacao and IDataRecord Tests
    [Fact]
    public void ReadAll_WithValidFSituacaoAndDataRecord_ShouldReturnSituacaoResponseAll()
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullFSituacaoAndDataRecord_ShouldReturnNull()
    {
        // Arrange
        FSituacao? dbRec = null;
        // Act
        var result = _situacaoReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithFSituacaoNullNomeAndDataRecord_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = 123,
            FParte_Int = null
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with DBSituacao and DataRow Tests
    [Fact]
    public void ReadAll_WithValidSGDBSituacaoAndDataRow_ShouldReturnSituacaoResponseAll()
    {
        // Arrange
        var dbRec = new DBSituacao
        {
            ID = 123,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be("AAAAAAAAAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullSGDBSituacaoAndDataRow_ShouldReturnNull()
    {
        // Arrange
        DBSituacao? dbRec = null;
        // Act
        var result = _situacaoReader.ReadAll(dbRec, null);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithSGDBSituacaoNullNomeAndDataRow_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBSituacao
        {
            ID = 123,
            FParte_Int = null // This should result in empty string in response
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Parte_Int.Should().Be(string.Empty);
    }

#endregion
#region Edge Cases and Error Handling Tests
    [Fact]
    public async Task Read_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _situacaoReader.Read(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task ReadM_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _situacaoReader.ReadM(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public void Read_WithWhereParameters_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var where = "carCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedException = new InvalidOperationException("Factory error");
        _mockSituacaoFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", "", where, "")).Throws(expectedException);
        // Act & Assert
        var exception = Assert.Throws<InvalidOperationException>(() => _situacaoReader.Read(where, parameters, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Data Consistency Tests
    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void Read_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = id,
            FParte_Int = nome
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        if (id == 0)
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
        }
        else
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
            result?.Parte_Int.Should().Be(nome);
        }
    }

    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void ReadAll_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FSituacao
        {
            ID = id,
            FParte_Int = nome
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Parte_Int.Should().Be(nome);
    }

#endregion
#region Async Behavior Tests
    [Fact]
    public async Task AsyncMethods_ShouldCompleteWithoutBlocking()
    {
        // Arrange
        var id = 123;
        var expectedFSituacao = new FSituacao
        {
            ID = id,
            FParte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFSituacao);
        // Act & Assert
        // Test only the methods that don't depend on external URI connections
        var readTask = _situacaoReader.Read(id, _mockConnection.Object);
        var readMTask = _situacaoReader.ReadM(id, _mockConnection.Object);
        await Task.WhenAll(readTask, readMTask);
        // If we reach here, async methods completed without deadlock
        Assert.True(true);
    }

    [Fact]
    public async Task ListarN_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task Listar_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _situacaoReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Parameter Validation Tests
    [Fact]
    public async Task Read_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        // This would typically throw an exception or handle gracefully
        // depending on the implementation
        try
        {
            await _situacaoReader.Read(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public async Task ReadM_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        try
        {
            await _situacaoReader.ReadM(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public void Read_WithNullParametersList_ShouldHandleGracefully()
    {
        // Arrange
        var where = "sitCodigo = @id";
        // Act & Assert
        try
        {
            _situacaoReader.Read(where, null !, _mockConnection.Object);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

#endregion
#region Integration-like Tests
    [Fact]
    public void Read_FSituacao_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 456;
        var testNome = "MA";
        var dbRec = new FSituacao
        {
            ID = testId,
            FParte_Int = testNome
        };
        // Act
        var result = _situacaoReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<SituacaoResponse>();
        result!.Id.Should().Be(testId);
        result.Parte_Int.Should().Be(testNome);
    }

    [Fact]
    public void ReadAll_FSituacao_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 789;
        var testNome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA";
        var dbRec = new FSituacao
        {
            ID = testId,
            FParte_Int = testNome
        };
        // Act
        var result = _situacaoReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<SituacaoResponseAll>();
        result!.Id.Should().Be(testId);
        result.Parte_Int.Should().Be(testNome);
    }

#endregion
#region Dispose
    public virtual void Dispose()
    {
        // Clean up any resources if needed
        GC.SuppressFinalize(this);
    }
#endregion
}