// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class CidadeWriterTests
{
    private readonly Mock<IFCidadeFactory> _mockCidadeFactory;
    private readonly Mock<FCidade> _mockFCidade;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly CidadeWriter _cidadeWriter;
    public CidadeWriterTests()
    {
        _mockCidadeFactory = new Mock<IFCidadeFactory>();
        _mockFCidade = new Mock<FCidade>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _cidadeWriter = new CidadeWriter(_mockCidadeFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var cidade = CreateValidCidadeModel();
        cidade.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockCidadeFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFCidade.Object);
        _mockFCidade.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _cidadeWriter.WriteAsync(cidade, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFCidade.Object);
        _mockCidadeFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockCidadeFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFCidade.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var cidade = CreateValidCidadeModel();
        cidade.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockCidadeFactory.Setup(x => x.CreateFromIdAsync(cidade.Id, _mockConnection.Object)).ReturnsAsync(_mockFCidade.Object);
        _mockFCidade.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _cidadeWriter.WriteAsync(cidade, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFCidade.Object);
        _mockCidadeFactory.Verify(x => x.CreateFromIdAsync(cidade.Id, _mockConnection.Object), Times.Once);
        _mockCidadeFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFCidade.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var cidade = CreateValidCidadeModel();
        var auditorQuem = 99;
        _mockCidadeFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFCidade.Object);
        _mockFCidade.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _cidadeWriter.WriteAsync(cidade, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFCidade.Object);
        _mockFCidade.VerifySet(x => x.FDDD = cidade.DDD, Times.Once);
        _mockFCidade.VerifySet(x => x.FTop = cidade.Top, Times.Once);
        _mockFCidade.VerifySet(x => x.FComarca = cidade.Comarca, Times.Once);
        _mockFCidade.VerifySet(x => x.FCapital = cidade.Capital, Times.Once);
        _mockFCidade.VerifySet(x => x.FNome = cidade.Nome, Times.Once);
        _mockFCidade.VerifySet(x => x.FUF = cidade.UF, Times.Once);
        _mockFCidade.VerifySet(x => x.FSigla = cidade.Sigla, Times.Once);
        _mockFCidade.VerifySet(x => x.FGUID = cidade.GUID, Times.Once);
        _mockFCidade.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var cidade = CreateValidCidadeModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockCidadeFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _cidadeWriter.WriteAsync(cidade, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var cidade = CreateValidCidadeModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockCidadeFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFCidade.Object);
        _mockFCidade.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _cidadeWriter.WriteAsync(cidade, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var cidadeResponse = new CidadeResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockCidadeFactory.Setup(x => x.DeleteAsync(operadorId, cidadeResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _cidadeWriter.Delete(cidadeResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockCidadeFactory.Verify(x => x.DeleteAsync(operadorId, cidadeResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var cidadeResponse = new CidadeResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockCidadeFactory.Setup(x => x.DeleteAsync(operadorId, cidadeResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _cidadeWriter.Delete(cidadeResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var cidadeResponse = new CidadeResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockCidadeFactory.Setup(x => x.DeleteAsync(operadorId, cidadeResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _cidadeWriter.Delete(cidadeResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new CidadeWriter(_mockCidadeFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new CidadeWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Cidade CreateValidCidadeModel()
    {
        return new Models.Cidade
        {
            Id = 0,
            DDD = "AAAAAAAA",
            Top = false,
            Comarca = false,
            Capital = false,
            Nome = "João",
            UF = 1,
            Sigla = "AAAAAAAA",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}