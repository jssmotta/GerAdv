// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class SetorWriterTests
{
    private readonly Mock<IFSetorFactory> _mockSetorFactory;
    private readonly Mock<FSetor> _mockFSetor;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly SetorWriter _setorWriter;
    public SetorWriterTests()
    {
        _mockSetorFactory = new Mock<IFSetorFactory>();
        _mockFSetor = new Mock<FSetor>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _setorWriter = new SetorWriter(_mockSetorFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var setor = CreateValidSetorModel();
        setor.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockSetorFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSetor.Object);
        _mockFSetor.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _setorWriter.WriteAsync(setor, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSetor.Object);
        _mockSetorFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockSetorFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFSetor.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var setor = CreateValidSetorModel();
        setor.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockSetorFactory.Setup(x => x.CreateFromIdAsync(setor.Id, _mockConnection.Object)).ReturnsAsync(_mockFSetor.Object);
        _mockFSetor.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _setorWriter.WriteAsync(setor, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSetor.Object);
        _mockSetorFactory.Verify(x => x.CreateFromIdAsync(setor.Id, _mockConnection.Object), Times.Once);
        _mockSetorFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFSetor.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var setor = CreateValidSetorModel();
        var auditorQuem = 99;
        _mockSetorFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSetor.Object);
        _mockFSetor.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _setorWriter.WriteAsync(setor, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSetor.Object);
        _mockFSetor.VerifySet(x => x.FDescricao = setor.Descricao, Times.Once);
        _mockFSetor.VerifySet(x => x.FGUID = setor.GUID, Times.Once);
        _mockFSetor.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var setor = CreateValidSetorModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSetorFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _setorWriter.WriteAsync(setor, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var setor = CreateValidSetorModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSetorFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSetor.Object);
        _mockFSetor.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _setorWriter.WriteAsync(setor, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var setorResponse = new SetorResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockSetorFactory.Setup(x => x.DeleteAsync(operadorId, setorResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _setorWriter.Delete(setorResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockSetorFactory.Verify(x => x.DeleteAsync(operadorId, setorResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var setorResponse = new SetorResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockSetorFactory.Setup(x => x.DeleteAsync(operadorId, setorResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _setorWriter.Delete(setorResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var setorResponse = new SetorResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockSetorFactory.Setup(x => x.DeleteAsync(operadorId, setorResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _setorWriter.Delete(setorResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new SetorWriter(_mockSetorFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new SetorWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Setor CreateValidSetorModel()
    {
        return new Models.Setor
        {
            Id = 0,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}