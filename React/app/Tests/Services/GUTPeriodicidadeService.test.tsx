// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTPeriodicidadeService, GUTPeriodicidadeValidator } from '@/app/GerAdv_TS/GUTPeriodicidade/Services/GUTPeriodicidade.service';
import { GUTPeriodicidadeApi, GUTPeriodicidadeApiError } from '@/app/GerAdv_TS/GUTPeriodicidade/Apis/ApiGUTPeriodicidade';
import { IGUTPeriodicidade } from '@/app/GerAdv_TS/GUTPeriodicidade/Interfaces/interface.GUTPeriodicidade';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTPeriodicidadeApi;

const sample: IGUTPeriodicidade = { id: 1, nome: 'Teste' };

describe('GUTPeriodicidadeService', () => {
  let service: GUTPeriodicidadeService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTPeriodicidadeService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTPeriodicidadeValidator, 'validateGUTPeriodicidade').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTPeriodicidadeById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTPeriodicidadeById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTPeriodicidadeById lança GUTPeriodicidadeApiError para id inválido', async () => {
    await expect(service.fetchGUTPeriodicidadeById(0)).rejects.toBeInstanceOf(GUTPeriodicidadeApiError);
  });

  it('saveGUTPeriodicidade chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTPeriodicidade(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTPeriodicidade lança GUTPeriodicidadeApiError quando validação falha', async () => {
    (GUTPeriodicidadeValidator.validateGUTPeriodicidade as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTPeriodicidade(sample)).rejects.toBeInstanceOf(GUTPeriodicidadeApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTPeriodicidade chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTPeriodicidade(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTPeriodicidade lança GUTPeriodicidadeApiError para id inválido', async () => {
    await expect(service.deleteGUTPeriodicidade(0)).rejects.toBeInstanceOf(GUTPeriodicidadeApiError);
  });

  it('validateGUTPeriodicidade delega para GUTPeriodicidadeValidator', () => {
    (GUTPeriodicidadeValidator.validateGUTPeriodicidade as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTPeriodicidade(sample);
    expect(r.isValid).toBe(true);
  });
});
