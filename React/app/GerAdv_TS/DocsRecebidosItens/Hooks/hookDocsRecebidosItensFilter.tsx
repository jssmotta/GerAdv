// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterDocsRecebidosItens } from '@/app/GerAdv_TS/DocsRecebidosItens/Filters/DocsRecebidosItens';
import ContatoCRMComboBox from '@/app/GerAdv_TS/ContatoCRM/ComboBox/ContatoCRM';
interface UseDocsRecebidosItensFilterProps {
  handleFetchWithFilter: (filtro?: FilterDocsRecebidosItens | undefined | null) => Promise<void>;
}
interface UseDocsRecebidosItensFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterDocsRecebidosItens;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterDocsRecebidosItens>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterDocsRecebidosItens) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterDocsRecebidosItens>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useDocsRecebidosItensFilter = ({ handleFetchWithFilter }: UseDocsRecebidosItensFilterProps): UseDocsRecebidosItensFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterDocsRecebidosItens>({} as FilterDocsRecebidosItens);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterDocsRecebidosItens;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterDocsRecebidosItens) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('DocsRecebidosItensFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterDocsRecebidosItens>) => (
  <>
  <ContatoCRMComboBox
  name='contatocrm'
  dataForm={null}
  value={handlers.windowFilter?.contatocrm}
  setValue={(e:any) => handlers.handleComboChange(e, 'contatocrm')}
  className='inputSearch inputSearchComboboxTab'
  label='Contato C R M'
  />
  <InputInput
  type='text'
  id='nome'
  name='nome'
  value={handlers.windowFilter?.nome ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Nome'
  label='Nome'
  className='inputIncNome inputSearch'
  />
  <InputComboFilterYesNo
  type='text'
  id='devolvido'
  name='devolvido'
  value={handlers.windowFilter?.devolvido ?? -2147483648}
  onChange={handlers.handleInputChange}
  label='Devolvido'
  className='inputSearch inputSearchCheckbox'
  />
  <InputComboFilterYesNo
  type='text'
  id='seradevolvido'
  name='seradevolvido'
  value={handlers.windowFilter?.seradevolvido ?? -2147483648}
  onChange={handlers.handleInputChange}
  label='SeraDevolvido'
  className='inputSearch inputSearchCheckbox'
  />
  <InputInput
  type='text'
  id='observacoes'
  name='observacoes'
  value={handlers.windowFilter?.observacoes ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Observacoes'
  label='Observacoes'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};