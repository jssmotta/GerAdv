// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for Reuniao page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock ReuniaoGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/Reuniao/Adapter/ReuniaoGridAdapter', () => ({
  ReuniaoGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock ReuniaoGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/Reuniao/Components/ReuniaoGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='reuniao-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import ReuniaoPage from '@/app/pages/reuniao/page';

describe('Reuniao page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and ReuniaoGridContainer', () => {
    render(<ReuniaoPage />);

    expect(screen.getByRole('heading', { name: /Reunião/i })).toBeInTheDocument();
    expect(screen.getByTestId('reuniao-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

