// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import SMSAliceWindowId from '@/app/GerAdv_TS/SMSAlice/Crud/Grids/SMSAliceWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import SMSAliceWindowId from '@/app/GerAdv_TS/SMSAlice/Crud/Grids/SMSAliceWindowId';
import { SMSAliceEmpty } from '@/app/GerAdv_TS/Models/SMSAlice';

describe('SMSAliceWindowId (basic)', () => {
  it('exports the component', () => {
    expect(SMSAliceWindowId).toBeDefined();
    expect(typeof SMSAliceWindowId === 'function' || typeof SMSAliceWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the SMSAliceService class so constructor usage doesn't throw and we can control fetchSMSAliceById
const mockFetchSMSAliceById = jest.fn();
jest.mock('@/app/GerAdv_TS/SMSAlice/Services/SMSAlice.service', () => ({
  SMSAliceService: jest.fn().mockImplementation(() => ({
    fetchSMSAliceById: (...args: any[]) => mockFetchSMSAliceById(...args),
  })),
}));

// Mock the child SMSAliceWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/SMSAlice/Crud/Grids/SMSAliceWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='smsalice-window' {...p} />,
}));

describe('SMSAliceWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(SMSAliceWindowId).toBeDefined();
    expect(typeof SMSAliceWindowId === 'function' || typeof SMSAliceWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <SMSAliceWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders SMSAliceWindow with SMSAliceEmpty when id is 0', async () => {
    render(
      <SMSAliceWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // SMSAliceWindow should be rendered with selectedSMSAlice equal to SMSAliceEmpty()
    const node = await screen.findByTestId('smsalice-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedSMSAlice"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedsmsalice') || node.getAttribute('selectedSMSAlice')).not.toBeNull();
  });

  it('fetches smsalice by id and passes it to SMSAliceWindow when id > 0', async () => {
    const fakeSMSAlice = { id: 123, nomecliente: 'Test' };
    mockFetchSMSAliceById.mockResolvedValue(fakeSMSAlice);

    render(
      <SMSAliceWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('smsalice-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchSMSAliceById).toHaveBeenCalledWith(123));
  });
});
