// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import EventoPrazoAgendaWindowId from '@/app/GerAdv_TS/EventoPrazoAgenda/Crud/Grids/EventoPrazoAgendaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import EventoPrazoAgendaWindowId from '@/app/GerAdv_TS/EventoPrazoAgenda/Crud/Grids/EventoPrazoAgendaWindowId';
import { EventoPrazoAgendaEmpty } from '@/app/GerAdv_TS/Models/EventoPrazoAgenda';

describe('EventoPrazoAgendaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(EventoPrazoAgendaWindowId).toBeDefined();
    expect(typeof EventoPrazoAgendaWindowId === 'function' || typeof EventoPrazoAgendaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the EventoPrazoAgendaService class so constructor usage doesn't throw and we can control fetchEventoPrazoAgendaById
const mockFetchEventoPrazoAgendaById = jest.fn();
jest.mock('@/app/GerAdv_TS/EventoPrazoAgenda/Services/EventoPrazoAgenda.service', () => ({
  EventoPrazoAgendaService: jest.fn().mockImplementation(() => ({
    fetchEventoPrazoAgendaById: (...args: any[]) => mockFetchEventoPrazoAgendaById(...args),
  })),
}));

// Mock the child EventoPrazoAgendaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/EventoPrazoAgenda/Crud/Grids/EventoPrazoAgendaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='eventoprazoagenda-window' {...p} />,
}));

describe('EventoPrazoAgendaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(EventoPrazoAgendaWindowId).toBeDefined();
    expect(typeof EventoPrazoAgendaWindowId === 'function' || typeof EventoPrazoAgendaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <EventoPrazoAgendaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders EventoPrazoAgendaWindow with EventoPrazoAgendaEmpty when id is 0', async () => {
    render(
      <EventoPrazoAgendaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // EventoPrazoAgendaWindow should be rendered with selectedEventoPrazoAgenda equal to EventoPrazoAgendaEmpty()
    const node = await screen.findByTestId('eventoprazoagenda-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedEventoPrazoAgenda"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedeventoprazoagenda') || node.getAttribute('selectedEventoPrazoAgenda')).not.toBeNull();
  });

  it('fetches eventoprazoagenda by id and passes it to EventoPrazoAgendaWindow when id > 0', async () => {
    const fakeEventoPrazoAgenda = { id: 123, nomecliente: 'Test' };
    mockFetchEventoPrazoAgendaById.mockResolvedValue(fakeEventoPrazoAgenda);

    render(
      <EventoPrazoAgendaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('eventoprazoagenda-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchEventoPrazoAgendaById).toHaveBeenCalledWith(123));
  });
});
