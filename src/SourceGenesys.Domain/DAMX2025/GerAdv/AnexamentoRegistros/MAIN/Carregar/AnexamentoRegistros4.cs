// 100% auto-generated by Source Genesys Core
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
// ReSharper disable once InconsistentNaming
public partial class DBAnexamentoRegistros
{
    public DBAnexamentoRegistros(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        InitFromRecord(name => dbRec.Table.Columns.Contains(name) ? dbRec[name] : null);
    }

    public DBAnexamentoRegistros(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec[name]);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do AnexamentoRegistros: {ex.Message}", ex);
        }
    }

    private void InitFromRecord(Func<string, object?> getValue)
    {
        if (DBNull.Value.Equals(getValue(CampoCodigo)))
            return;
        ID = Convert.ToInt32(getValue(CampoCodigo));
        // Checkpoint Carregar 
        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.Cliente)))
                m_FCliente = Convert.ToInt32(getValue(DBAnexamentoRegistrosDicInfo.Cliente));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.CodigoReg)))
                m_FCodigoReg = Convert.ToInt32(getValue(DBAnexamentoRegistrosDicInfo.CodigoReg));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.Data)))
                m_FData = Convert.ToDateTime(getValue(DBAnexamentoRegistrosDicInfo.Data));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.DtAtu)))
                m_FDtAtu = Convert.ToDateTime(getValue(DBAnexamentoRegistrosDicInfo.DtAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.DtCad)))
                m_FDtCad = Convert.ToDateTime(getValue(DBAnexamentoRegistrosDicInfo.DtCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.IDReg)))
                m_FIDReg = Convert.ToInt32(getValue(DBAnexamentoRegistrosDicInfo.IDReg));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.QuemAtu)))
                m_FQuemAtu = Convert.ToInt32(getValue(DBAnexamentoRegistrosDicInfo.QuemAtu));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.QuemCad)))
                m_FQuemCad = Convert.ToInt32(getValue(DBAnexamentoRegistrosDicInfo.QuemCad));
        }
        catch
        {
        }

        try
        {
            if (!DBNull.Value.Equals(getValue(DBAnexamentoRegistrosDicInfo.Visto)))
                m_FVisto = Convert.ToBoolean(getValue(DBAnexamentoRegistrosDicInfo.Visto));
        }
        catch
        {
        }

        try
        {
            m_FGUID = getValue(DBAnexamentoRegistrosDicInfo.GUID)?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            m_FGUIDReg = getValue(DBAnexamentoRegistrosDicInfo.GUIDReg)?.ToString() ?? string.Empty;
        }
        catch
        {
        }
    }

    public void CarregarDadosBd(DataRow? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec.Table.Columns.Contains(name) ? dbRec[name] : null);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do AnexamentoRegistros: {ex.Message}", ex);
        }
    }

    public void CarregarDadosBd(SqlDataReader? dbRec)
    {
        if (dbRec is null)
            return;
        try
        {
            InitFromRecord(name => dbRec[name]);
        }
        catch (Exception ex)
        {
            throw new Exception($"Erro ao carregar dados do AnexamentoRegistros: {ex.Message}", ex);
        }
    }
}