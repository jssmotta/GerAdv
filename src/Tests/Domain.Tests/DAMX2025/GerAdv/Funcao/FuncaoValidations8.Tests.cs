// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class FuncaoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFFuncaoFactory> _mockFuncaoFactory;
    private readonly Mock<IFuncaoReader> _mockReader;
    private readonly FuncaoValidation _validation;
    private readonly Mock<IFuncaoService> _mockFuncaoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly FuncaoService _service;
    private readonly string _validUri = "test-uri";
    public FuncaoValidationTests()
    {
        _mockFuncaoFactory = new Mock<IFFuncaoFactory>();
        _mockReader = new Mock<IFuncaoReader>();
        _validation = new FuncaoValidation();
        _mockFuncaoService = new Mock<IFuncaoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void FuncaoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IFuncaoValidation>();
    }

    [Fact]
    public void FuncaoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(FuncaoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void FuncaoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(FuncaoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Funcao CreateValidFuncao()
    {
        return new Models.Funcao
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockFuncaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFuncao>(), It.IsAny<string>())).ReturnsAsync(new List<FuncaoResponseAll>());
    // Setup other mocks but don't override the Funcaos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockFuncaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterFuncao>(), It.IsAny<string>())).ReturnsAsync(new List<FuncaoResponseAll>());
    // Setup other mocks but don't override the Funcaos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var funcao = new Models.Funcao
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(funcao, _mockFuncaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockFuncaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}