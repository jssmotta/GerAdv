// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IPoderJudiciarioAssociadoService } from '../Services/PoderJudiciarioAssociado.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IPoderJudiciarioAssociado } from '../Interfaces/interface.PoderJudiciarioAssociado';
import { FilterPoderJudiciarioAssociado } from '../Filters/PoderJudiciarioAssociado';
import { isValidDate } from '@/app/tools/datetime';
import { PoderJudiciarioAssociadoApi } from '../Apis/ApiPoderJudiciarioAssociado';

export const usePoderJudiciarioAssociadoForm = (
  initialPoderJudiciarioAssociado: IPoderJudiciarioAssociado,
  dataService: IPoderJudiciarioAssociadoService
) => {
  const [data, setData] = useState<IPoderJudiciarioAssociado>(initialPoderJudiciarioAssociado);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadPoderJudiciarioAssociado = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialPoderJudiciarioAssociado);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchPoderJudiciarioAssociadoById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Poder Judiciario Associado';
      setError(errorMessage);
      //console.log('Erro ao carregar Poder Judiciario Associado');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialPoderJudiciarioAssociado]);

  const resetForm = useCallback(() => {
    setData(initialPoderJudiciarioAssociado);
    setError(null);
  }, [initialPoderJudiciarioAssociado]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadPoderJudiciarioAssociado,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadPoderJudiciarioAssociado, resetForm]);
  return returnValue;
};


export const usePoderJudiciarioAssociadoNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('PoderJudiciarioAssociado', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const usePoderJudiciarioAssociadoList = (dataService: IPoderJudiciarioAssociadoService, currentFilter?: FilterPoderJudiciarioAssociado | undefined | null) => {
  const [data, setData] = useState<IPoderJudiciarioAssociado[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterPoderJudiciarioAssociado | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterPoderJudiciarioAssociado | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterPoderJudiciarioAssociado);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar poderjudiciarioassociado';
      setError(errorMessage);
      //console.log('Erro ao carregar poderjudiciarioassociado');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  usePoderJudiciarioAssociadoNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsPoderJudiciarioAssociado() {

  async function runValidation(data: IPoderJudiciarioAssociado, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const poderjudiciarioassociadoApi = new PoderJudiciarioAssociadoApi(uri ?? '', token ?? '');

    const result = await poderjudiciarioassociadoApi.validation(data);

    return result;
  }

  function validate(data: IPoderJudiciarioAssociado): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.guid.length <= 0) { 
                                             return { isValid: false, message: 'O campo GUID não pode ficar vazio.' };
                                         } 
if (data.justicanome.length > 255) { 
                                             return { isValid: false, message: 'O campo JusticaNome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.areanome.length > 255) { 
                                             return { isValid: false, message: 'O campo AreaNome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.tribunalnome.length > 255) { 
                                             return { isValid: false, message: 'O campo TribunalNome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.foronome.length > 255) { 
                                             return { isValid: false, message: 'O campo ForoNome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.subdivisaonome.length > 255) { 
                                             return { isValid: false, message: 'O campo SubDivisaoNome não pode ter mais de 255 caracteres.' };
                                         } 
if (data.cidadenome.length > 255) { 
                                             return { isValid: false, message: 'O campo CidadeNome não pode ter mais de 255 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}