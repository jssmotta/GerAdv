// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { ITipoValorProcesso } from '../../Interfaces/interface.TipoValorProcesso';
import { TipoValorProcessoService } from '../../Services/TipoValorProcesso.service';
import { TipoValorProcessoApi } from '../../Apis/ApiTipoValorProcesso';
import TipoValorProcessoWindow from './TipoValorProcessoWindow';
import {TipoValorProcessoEmpty } from '@/app/GerAdv_TS/Models/TipoValorProcesso';
interface TipoValorProcessoWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const TipoValorProcessoWindowId: React.FC<TipoValorProcessoWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const tipovalorprocessoService = useMemo(() => {
  return new TipoValorProcessoService(
  new TipoValorProcessoApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<ITipoValorProcesso | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(TipoValorProcessoEmpty() as ITipoValorProcesso);
      return;
    }
    if (id) {
      const response = await tipovalorprocessoService.fetchTipoValorProcessoById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <TipoValorProcessoWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedTipoValorProcesso={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default TipoValorProcessoWindowId;