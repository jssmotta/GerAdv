// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useStatusBiuForm, useStatusBiuList, useValidationsStatusBiu } from '../GerAdv_TS/StatusBiu/Hooks/hookStatusBiu';
import { IStatusBiu } from '../GerAdv_TS/StatusBiu/Interfaces/interface.StatusBiu';
import { IStatusBiuService } from '../GerAdv_TS/StatusBiu/Services/StatusBiu.service';
import { StatusBiuTestEmpty } from '../GerAdv_TS/Models/StatusBiu';
import { useStatusBiuComboBox } from '../GerAdv_TS/StatusBiu/Hooks/hookStatusBiu';

// Mock do serviço
const mockStatusBiuService: jest.Mocked<IStatusBiuService> = {
  fetchStatusBiuById: jest.fn(),
  saveStatusBiu: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteStatusBiu: jest.fn(),
  validateStatusBiu: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialStatusBiu: IStatusBiu = { ...StatusBiuTestEmpty() };

describe('useStatusBiuForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    expect(result.current.data).toEqual(initialStatusBiu);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Status Biu',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Status Biu');
  });

   test('deve carregar Status Biu por ID', async () => {
    const mockStatusBiu = { ...initialStatusBiu, id: 1, nome: 'Status Biu Teste' };
    mockStatusBiuService.fetchStatusBiuById.mockResolvedValue(mockStatusBiu);

    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    await act(async () => {
      await result.current.loadStatusBiu(1);
    });

    expect(mockStatusBiuService.fetchStatusBiuById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockStatusBiu);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Status Biu', async () => {
    const errorMessage = 'Erro ao carregar Status Biu';
    mockStatusBiuService.fetchStatusBiuById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    await act(async () => {
      await result.current.loadStatusBiu(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialStatusBiu, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialStatusBiu);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useStatusBiuForm(initialStatusBiu, mockStatusBiuService)
    );

    await act(async () => {
      await result.current.loadStatusBiu(0);
    });

    expect(mockStatusBiuService.fetchStatusBiuById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialStatusBiu);
  });
});

describe('useStatusBiuList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusBiuList(mockStatusBiuService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialStatusBiu, id: 1, nome: 'Status Biu 1' },
      { ...initialStatusBiu, id: 2, nome: 'Status Biu 2' }
    ];
    mockStatusBiuService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusBiuList(mockStatusBiuService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockStatusBiuService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockStatusBiuService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useStatusBiuList(mockStatusBiuService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialStatusBiu, id: 1, nome: 'Status Biu Filtrado' }];
    const filtro = { nome: 'Status Biu' };
    mockStatusBiuService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useStatusBiuList(mockStatusBiuService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockStatusBiuService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsStatusBiu', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsStatusBiu());

    const validData = { ...initialStatusBiu, nome: 'Status Biu Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsStatusBiu());

    const invalidData = { ...initialStatusBiu, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsStatusBiu());

    const invalidData = { 
      ...initialStatusBiu, 
      nome: 'a'.repeat(1024+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 1024 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsStatusBiu());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialStatusBiu, id: 1, nome: 'Status Biu Teste' }];
    mockStatusBiuService.getAll.mockResolvedValue(mockData);
    mockStatusBiuService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useStatusBiuList(mockStatusBiuService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsStatusBiu()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status Biu Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Status Biu Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Biu 1' },
      { id: 2, nome: 'Status Biu 2' }
    ];
    mockStatusBiuService.getList.mockResolvedValue(mockOptions as IStatusBiu[]);


    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Biu 1' },
        { id: 2, nome: 'Status Biu 2' }
      ]);
    });


    expect(mockStatusBiuService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Biu ABC' },
      { id: 2, nome: 'Status Biu XYZ' }
    ];
    mockStatusBiuService.getList.mockResolvedValue(mockOptions as IStatusBiu[]);   


 const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Biu ABC' },
        { id: 2, nome: 'Status Biu XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Status Biu ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Status Biu ABC' },
      { id: 2, nome: 'Status Biu XYZ' }
    ];
    mockStatusBiuService.getList.mockResolvedValue(mockOptions as IStatusBiu[]);
  


    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Status Biu ABC' },
        { id: 2, nome: 'Status Biu XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Status Biu ABC' },
          {id: 2, nome: 'Status Biu XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );

    const newValue = { id: 1, nome: 'Status Biu Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Status Biu Inicial' };
    
    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useStatusBiuComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Status Biu Inicial' };
    
    const { result } = renderHook(() => 
      useStatusBiuComboBox(mockStatusBiuService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






