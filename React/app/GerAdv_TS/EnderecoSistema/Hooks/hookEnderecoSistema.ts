// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IEnderecoSistemaService } from '../Services/EnderecoSistema.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IEnderecoSistema } from '../Interfaces/interface.EnderecoSistema';
import { FilterEnderecoSistema } from '../Filters/EnderecoSistema';
import { isValidDate } from '@/app/tools/datetime';
import { EnderecoSistemaApi } from '../Apis/ApiEnderecoSistema';

export const useEnderecoSistemaForm = (
  initialEnderecoSistema: IEnderecoSistema,
  dataService: IEnderecoSistemaService
) => {
  const [data, setData] = useState<IEnderecoSistema>(initialEnderecoSistema);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadEnderecoSistema = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialEnderecoSistema);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchEnderecoSistemaById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Endereco Sistema';
      setError(errorMessage);
      //console.log('Erro ao carregar Endereco Sistema');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialEnderecoSistema]);

  const resetForm = useCallback(() => {
    setData(initialEnderecoSistema);
    setError(null);
  }, [initialEnderecoSistema]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadEnderecoSistema,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadEnderecoSistema, resetForm]);
  return returnValue;
};


export const useEnderecoSistemaNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('EnderecoSistema', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useEnderecoSistemaList = (dataService: IEnderecoSistemaService, currentFilter?: FilterEnderecoSistema | undefined | null) => {
  const [data, setData] = useState<IEnderecoSistema[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterEnderecoSistema | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterEnderecoSistema | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterEnderecoSistema);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar enderecosistema';
      setError(errorMessage);
      //console.log('Erro ao carregar enderecosistema');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useEnderecoSistemaNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsEnderecoSistema() {

  async function runValidation(data: IEnderecoSistema, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const enderecosistemaApi = new EnderecoSistemaApi(uri ?? '', token ?? '');

    const result = await enderecosistemaApi.validation(data);

    return result;
  }

  function validate(data: IEnderecoSistema): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.guid.length <= 0) { 
                                             return { isValid: false, message: 'O campo GUID não pode ficar vazio.' };
                                         } 
if (data.motivo.length > 200) { 
                                             return { isValid: false, message: 'O campo Motivo não pode ter mais de 200 caracteres.' };
                                         } 
if (data.contatonolocal.length > 50) { 
                                             return { isValid: false, message: 'O campo ContatoNoLocal não pode ter mais de 50 caracteres.' };
                                         } 
if (data.endereco.length > 150) { 
                                             return { isValid: false, message: 'O campo Endereco não pode ter mais de 150 caracteres.' };
                                         } 
if (data.bairro.length > 50) { 
                                             return { isValid: false, message: 'O campo Bairro não pode ter mais de 50 caracteres.' };
                                         } 
if (data.cep.length > 10) { 
                                             return { isValid: false, message: 'O campo CEP não pode ter mais de 10 caracteres.' };
                                         } 
if (data.fone.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Fone não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.fax.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Fax não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.observacao.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Observacao não pode ter mais de 2147483647 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}