// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { AnexamentoRegistrosEmpty } from '../../../Models/AnexamentoRegistros';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IAnexamentoRegistros } from '../../Interfaces/interface.AnexamentoRegistros';
import { AnexamentoRegistrosService } from '../../Services/AnexamentoRegistros.service';
import { AnexamentoRegistrosApi } from '../../Apis/ApiAnexamentoRegistros';
import { AnexamentoRegistrosGridMobileComponent } from '../GridsMobile/AnexamentoRegistrosMobile';
import { AnexamentoRegistrosGridDesktopComponent } from '../GridsDesktop/AnexamentoRegistrosDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterAnexamentoRegistros } from '../../Filters/AnexamentoRegistros';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import AnexamentoRegistrosWindow from './AnexamentoRegistrosWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useAnexamentoRegistrosList } from '../../Hooks/hookAnexamentoRegistros';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useAnexamentoRegistrosFilter } from '../../Hooks/hookAnexamentoRegistrosFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const AnexamentoRegistrosGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedAnexamentoRegistros, setSelectedAnexamentoRegistros] = useState<IAnexamentoRegistros>(AnexamentoRegistrosEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterAnexamentoRegistros | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [anexamentoregistrosData, setAnexamentoRegistrosData] = useState<IAnexamentoRegistros[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const anexamentoregistrosService = useMemo(() => {
    return new AnexamentoRegistrosService(
    new AnexamentoRegistrosApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: anexamentoregistros, 
  fetchData, 
  refreshData
} = useAnexamentoRegistrosList(anexamentoregistrosService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterAnexamentoRegistros | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('AnexamentoRegistrosFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await anexamentoregistrosService.getAll(
  filtro ?? {} as FilterAnexamentoRegistros, 
  onlineData => {
    setAnexamentoRegistrosData(onlineData);
    setLoading(false);
  }
);
setAnexamentoRegistrosData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterAnexamentoRegistros;

    const filtroMem = sessionStorage.getItem(btoa('AnexamentoRegistrosFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('AnexamentoRegistrosFilter')) ?? '') || '{}') : {} as FilterAnexamentoRegistros;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (anexamentoregistros: IAnexamentoRegistros) => {
    setSelectedAnexamentoRegistros(anexamentoregistros);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedAnexamentoRegistros(AnexamentoRegistrosEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const anexamentoregistros = e.dataItem;
    setDeleteId(anexamentoregistros.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await anexamentoregistrosService.deleteAnexamentoRegistros(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useAnexamentoRegistrosFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <AnexamentoRegistrosGridMobileComponent
        data={anexamentoregistrosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <AnexamentoRegistrosGridDesktopComponent
        data={anexamentoregistrosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <AnexamentoRegistrosWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedAnexamentoRegistros={selectedAnexamentoRegistros}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Anexamento Registros'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default AnexamentoRegistrosGrid;