// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { Diario2Service, Diario2Validator } from '@/app/GerAdv_TS/Diario2/Services/Diario2.service';
import { Diario2Api, Diario2ApiError } from '@/app/GerAdv_TS/Diario2/Apis/ApiDiario2';
import { IDiario2 } from '@/app/GerAdv_TS/Diario2/Interfaces/interface.Diario2';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as Diario2Api;

const sample: IDiario2 = { id: 1, nome: 'Teste' };

describe('Diario2Service', () => {
  let service: Diario2Service;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new Diario2Service(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(Diario2Validator, 'validateDiario2').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchDiario2ById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchDiario2ById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchDiario2ById lança Diario2ApiError para id inválido', async () => {
    await expect(service.fetchDiario2ById(0)).rejects.toBeInstanceOf(Diario2ApiError);
  });

  it('saveDiario2 chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveDiario2(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveDiario2 lança Diario2ApiError quando validação falha', async () => {
    (Diario2Validator.validateDiario2 as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveDiario2(sample)).rejects.toBeInstanceOf(Diario2ApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteDiario2 chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteDiario2(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteDiario2 lança Diario2ApiError para id inválido', async () => {
    await expect(service.deleteDiario2(0)).rejects.toBeInstanceOf(Diario2ApiError);
  });

  it('validateDiario2 delega para Diario2Validator', () => {
    (Diario2Validator.validateDiario2 as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateDiario2(sample);
    expect(r.isValid).toBe(true);
  });
});
