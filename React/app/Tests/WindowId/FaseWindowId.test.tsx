// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import FaseWindowId from '@/app/GerAdv_TS/Fase/Crud/Grids/FaseWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import FaseWindowId from '@/app/GerAdv_TS/Fase/Crud/Grids/FaseWindowId';
import { FaseEmpty } from '@/app/GerAdv_TS/Models/Fase';

describe('FaseWindowId (basic)', () => {
  it('exports the component', () => {
    expect(FaseWindowId).toBeDefined();
    expect(typeof FaseWindowId === 'function' || typeof FaseWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the FaseService class so constructor usage doesn't throw and we can control fetchFaseById
const mockFetchFaseById = jest.fn();
jest.mock('@/app/GerAdv_TS/Fase/Services/Fase.service', () => ({
  FaseService: jest.fn().mockImplementation(() => ({
    fetchFaseById: (...args: any[]) => mockFetchFaseById(...args),
  })),
}));

// Mock the child FaseWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Fase/Crud/Grids/FaseWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='fase-window' {...p} />,
}));

describe('FaseWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(FaseWindowId).toBeDefined();
    expect(typeof FaseWindowId === 'function' || typeof FaseWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <FaseWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders FaseWindow with FaseEmpty when id is 0', async () => {
    render(
      <FaseWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // FaseWindow should be rendered with selectedFase equal to FaseEmpty()
    const node = await screen.findByTestId('fase-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedFase"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedfase') || node.getAttribute('selectedFase')).not.toBeNull();
  });

  it('fetches fase by id and passes it to FaseWindow when id > 0', async () => {
    const fakeFase = { id: 123, nomecliente: 'Test' };
    mockFetchFaseById.mockResolvedValue(fakeFase);

    render(
      <FaseWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('fase-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchFaseById).toHaveBeenCalledWith(123));
  });
});
