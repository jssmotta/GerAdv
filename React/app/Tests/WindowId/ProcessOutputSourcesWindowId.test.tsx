// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProcessOutputSourcesWindowId from '@/app/GerAdv_TS/ProcessOutputSources/Crud/Grids/ProcessOutputSourcesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProcessOutputSourcesWindowId from '@/app/GerAdv_TS/ProcessOutputSources/Crud/Grids/ProcessOutputSourcesWindowId';
import { ProcessOutputSourcesEmpty } from '@/app/GerAdv_TS/Models/ProcessOutputSources';

describe('ProcessOutputSourcesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProcessOutputSourcesWindowId).toBeDefined();
    expect(typeof ProcessOutputSourcesWindowId === 'function' || typeof ProcessOutputSourcesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProcessOutputSourcesService class so constructor usage doesn't throw and we can control fetchProcessOutputSourcesById
const mockFetchProcessOutputSourcesById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProcessOutputSources/Services/ProcessOutputSources.service', () => ({
  ProcessOutputSourcesService: jest.fn().mockImplementation(() => ({
    fetchProcessOutputSourcesById: (...args: any[]) => mockFetchProcessOutputSourcesById(...args),
  })),
}));

// Mock the child ProcessOutputSourcesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProcessOutputSources/Crud/Grids/ProcessOutputSourcesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='processoutputsources-window' {...p} />,
}));

describe('ProcessOutputSourcesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProcessOutputSourcesWindowId).toBeDefined();
    expect(typeof ProcessOutputSourcesWindowId === 'function' || typeof ProcessOutputSourcesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProcessOutputSourcesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProcessOutputSourcesWindow with ProcessOutputSourcesEmpty when id is 0', async () => {
    render(
      <ProcessOutputSourcesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProcessOutputSourcesWindow should be rendered with selectedProcessOutputSources equal to ProcessOutputSourcesEmpty()
    const node = await screen.findByTestId('processoutputsources-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProcessOutputSources"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprocessoutputsources') || node.getAttribute('selectedProcessOutputSources')).not.toBeNull();
  });

  it('fetches processoutputsources by id and passes it to ProcessOutputSourcesWindow when id > 0', async () => {
    const fakeProcessOutputSources = { id: 123, nomecliente: 'Test' };
    mockFetchProcessOutputSourcesById.mockResolvedValue(fakeProcessOutputSources);

    render(
      <ProcessOutputSourcesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('processoutputsources-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProcessOutputSourcesById).toHaveBeenCalledWith(123));
  });
});
