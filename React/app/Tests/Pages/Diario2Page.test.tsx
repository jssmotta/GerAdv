// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for Diario2 page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock Diario2GridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/Diario2/Adapter/Diario2GridAdapter', () => ({
  Diario2GridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock Diario2GridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/Diario2/Components/Diario2GridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='diario2-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import Diario2Page from '@/app/pages/diario2/page';

describe('Diario2 page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and Diario2GridContainer', () => {
    render(<Diario2Page />);

    expect(screen.getByRole('heading', { name: /Diario2/i })).toBeInTheDocument();
    expect(screen.getByTestId('diario2-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

