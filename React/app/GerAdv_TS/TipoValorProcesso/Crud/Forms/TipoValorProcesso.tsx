// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tracking: Forms.tsx.txt
'use client';
import { ITipoValorProcesso } from '@/app/GerAdv_TS/TipoValorProcesso/Interfaces/interface.TipoValorProcesso';
import { useRouter } from 'next/navigation';
import React, { useEffect, useState, useRef } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { getParamFromUrl } from '@/app/tools/helpers';
import '@/app/styles/CrudFormsBase.css';
import '@/app/styles/CrudFormsMobile.css';
import '@/app/styles/CrudForms5.css'; // [ INDEX_SIZE ]
import ButtonSalvarCrud from '@/app/components/Cruds/ButtonSalvarCrud';
import { useIsMobile } from '@/app/context/MobileContext';
import DeleteButton from '@/app/components/Cruds/DeleteButton';
import { TipoValorProcessoApi } from '../../Apis/ApiTipoValorProcesso';
import { useValidationsTipoValorProcesso } from '../../Hooks/hookTipoValorProcesso';
import InputDescription from '@/app/components/Inputs/InputDescription';
interface TipoValorProcessoFormProps {
  tipovalorprocessoData: ITipoValorProcesso;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const TipoValorProcessoForm: React.FC<TipoValorProcessoFormProps> = ({
  tipovalorprocessoData, 
  onChange, 
  onSubmit, 
  onClose, 
  onError, 
  onReload, 
  onSuccess, 
}) => {
const router = useRouter();
const { systemContext } = useSystemContext();
const isMobile = useIsMobile();
const dadoApi = new TipoValorProcessoApi(systemContext?.Uri ?? '', systemContext?.Token ?? '');
const [isSubmitting, setIsSubmitting] = useState(false);
const initialized = useRef(false);
const validationForm = useValidationsTipoValorProcesso();

const onConfirm = (e: React.FormEvent) => {
  e.preventDefault();
  if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
      console.log('Erro ao submeter formulário de TipoValorProcesso:');
      setIsSubmitting(false);
      if (onError) onError();
      }
    }
  };
  const handleCancel = () => {
    if (onReload) {
      onReload(); // Recarrega os dados originais
    } else {
    onClose(); // Comportamento padrão se não há callback de recarga
  }
};

const handleDirectSave = () => {
  if (!isSubmitting) {
    setIsSubmitting(true);

    try {
      const syntheticEvent = {
        preventDefault: () => { }, 
        target: document.getElementById(`TipoValorProcessoForm-${tipovalorprocessoData.id}`)
      } as unknown as React.FormEvent;

      onSubmit(syntheticEvent);
    } catch (error) {
    console.log('Erro ao salvar TipoValorProcesso diretamente');
    setIsSubmitting(false);
    if (onError) onError();
    }
  }
};
useEffect(() => {
  const el = document.querySelector('.nameFormMobile');
  if (el) {
    el.textContent = tipovalorprocessoData?.id == 0 ? 'Editar TipoValorProcesso' : 'Adicionar Tipo Valor Processo';
  }
}, [tipovalorprocessoData.id]);
return (
<>
{!isMobile ? <style jsx global>{`
  @media (max-width: 1366px) {
    html {
      zoom: 0.8 !important;
    }
  }
  `}</style> : null}

  <div className={isMobile ? 'form-container form-container-TipoValorProcesso' : 'form-container5 form-container-TipoValorProcesso'}>

    <form className='formInputCadInc' id={`TipoValorProcessoForm-${tipovalorprocessoData.id}`} onSubmit={onConfirm}>
      {!isMobile && (
        <ButtonSalvarCrud isMobile={false} validationForm={validationForm} entity='TipoValorProcesso' data={tipovalorprocessoData} isSubmitting={isSubmitting} onClose={onClose} formId={`TipoValorProcessoForm-${tipovalorprocessoData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
        )}
        <div className='grid-container'>

          <InputDescription
          type='text'
          id='descricao'
          label='tipo valor processo'
          dataForm={tipovalorprocessoData}
          className='inputIncNome'
          name='descricao'
          value={tipovalorprocessoData.descricao}
          placeholder={`Digite nome tipo valor processo`}
          onChange={onChange}
          required
          disabled={tipovalorprocessoData.id > 0}
          />

        </div>
      </form>


      {isMobile && (
        <ButtonSalvarCrud isMobile={true} validationForm={validationForm} entity='TipoValorProcesso' data={tipovalorprocessoData} isSubmitting={isSubmitting} onClose={onClose} formId={`TipoValorProcessoForm-${tipovalorprocessoData.id}`} preventPropagation={true} onSave={handleDirectSave} onCancel={handleCancel} />
        )}
        <DeleteButton page={'/pages/tipovalorprocesso'} id={tipovalorprocessoData.id} closeModel={onClose} dadoApi={dadoApi} />
      </div>
      <div className='form-spacer'></div>
      </>
    );
  };