// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useDadosProcuracaoForm, useDadosProcuracaoList, useValidationsDadosProcuracao } from '../GerAdv_TS/DadosProcuracao/Hooks/hookDadosProcuracao';
import { IDadosProcuracao } from '../GerAdv_TS/DadosProcuracao/Interfaces/interface.DadosProcuracao';
import { IDadosProcuracaoService } from '../GerAdv_TS/DadosProcuracao/Services/DadosProcuracao.service';
import { DadosProcuracaoTestEmpty } from '../GerAdv_TS/Models/DadosProcuracao';


// Mock do serviço
const mockDadosProcuracaoService: jest.Mocked<IDadosProcuracaoService> = {
  fetchDadosProcuracaoById: jest.fn(),
  saveDadosProcuracao: jest.fn(),
  
  getAll: jest.fn(),
  deleteDadosProcuracao: jest.fn(),
  validateDadosProcuracao: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialDadosProcuracao: IDadosProcuracao = { ...DadosProcuracaoTestEmpty() };

describe('useDadosProcuracaoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    expect(result.current.data).toEqual(initialDadosProcuracao);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    const mockEvent = {
      target: {
        name: 'estadocivil',
        value: 'Novo Dados Procuracao',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.estadocivil).toBe('Novo Dados Procuracao');
  });

   test('deve carregar Dados Procuracao por ID', async () => {
    const mockDadosProcuracao = { ...initialDadosProcuracao, id: 1, estadocivil: 'Dados Procuracao Teste' };
    mockDadosProcuracaoService.fetchDadosProcuracaoById.mockResolvedValue(mockDadosProcuracao);

    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.loadDadosProcuracao(1);
    });

    expect(mockDadosProcuracaoService.fetchDadosProcuracaoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockDadosProcuracao);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Dados Procuracao', async () => {
    const errorMessage = 'Erro ao carregar Dados Procuracao';
    mockDadosProcuracaoService.fetchDadosProcuracaoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.loadDadosProcuracao(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialDadosProcuracao, estadocivil: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialDadosProcuracao);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useDadosProcuracaoForm(initialDadosProcuracao, mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.loadDadosProcuracao(0);
    });

    expect(mockDadosProcuracaoService.fetchDadosProcuracaoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialDadosProcuracao);
  });
});

describe('useDadosProcuracaoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useDadosProcuracaoList(mockDadosProcuracaoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialDadosProcuracao, id: 1, estadocivil: 'Dados Procuracao 1' },
      { ...initialDadosProcuracao, id: 2, estadocivil: 'Dados Procuracao 2' }
    ];
    mockDadosProcuracaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useDadosProcuracaoList(mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockDadosProcuracaoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockDadosProcuracaoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useDadosProcuracaoList(mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialDadosProcuracao, id: 1, estadocivil: 'Dados Procuracao Filtrado' }];
    const filtro = { estadocivil: 'Dados Procuracao' };
    mockDadosProcuracaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useDadosProcuracaoList(mockDadosProcuracaoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockDadosProcuracaoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsDadosProcuracao', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsDadosProcuracao());

    const validData = { ...initialDadosProcuracao, estadocivil: 'Dados Procuracao Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsDadosProcuracao());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialDadosProcuracao, id: 1, estadocivil: 'Dados Procuracao Teste' }];
    mockDadosProcuracaoService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useDadosProcuracaoList(mockDadosProcuracaoService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsDadosProcuracao()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});