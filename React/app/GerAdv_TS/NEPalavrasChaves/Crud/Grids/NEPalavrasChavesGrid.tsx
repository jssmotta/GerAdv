// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { NEPalavrasChavesEmpty } from '../../../Models/NEPalavrasChaves';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { INEPalavrasChaves } from '../../Interfaces/interface.NEPalavrasChaves';
import { NEPalavrasChavesService } from '../../Services/NEPalavrasChaves.service';
import { NEPalavrasChavesApi } from '../../Apis/ApiNEPalavrasChaves';
import { NEPalavrasChavesGridMobileComponent } from '../GridsMobile/NEPalavrasChavesMobile';
import { NEPalavrasChavesGridDesktopComponent } from '../GridsDesktop/NEPalavrasChavesDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterNEPalavrasChaves } from '../../Filters/NEPalavrasChaves';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import NEPalavrasChavesWindow from './NEPalavrasChavesWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useNEPalavrasChavesList } from '../../Hooks/hookNEPalavrasChaves';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useNEPalavrasChavesFilter } from '../../Hooks/hookNEPalavrasChavesFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const NEPalavrasChavesGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedNEPalavrasChaves, setSelectedNEPalavrasChaves] = useState<INEPalavrasChaves>(NEPalavrasChavesEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterNEPalavrasChaves | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [nepalavraschavesData, setNEPalavrasChavesData] = useState<INEPalavrasChaves[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const nepalavraschavesService = useMemo(() => {
    return new NEPalavrasChavesService(
    new NEPalavrasChavesApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: nepalavraschaves, 
  fetchData, 
  refreshData
} = useNEPalavrasChavesList(nepalavraschavesService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterNEPalavrasChaves | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('NEPalavrasChavesFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await nepalavraschavesService.getAll(
  filtro ?? {} as FilterNEPalavrasChaves, 
  onlineData => {
    setNEPalavrasChavesData(onlineData);
    setLoading(false);
  }
);
setNEPalavrasChavesData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterNEPalavrasChaves;

    const filtroMem = sessionStorage.getItem(btoa('NEPalavrasChavesFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('NEPalavrasChavesFilter')) ?? '') || '{}') : {} as FilterNEPalavrasChaves;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (nepalavraschaves: INEPalavrasChaves) => {
    setSelectedNEPalavrasChaves(nepalavraschaves);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedNEPalavrasChaves(NEPalavrasChavesEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const nepalavraschaves = e.dataItem;
    setDeleteId(nepalavraschaves.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await nepalavraschavesService.deleteNEPalavrasChaves(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao excluir');
        setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
      } finally {
      setDeleteId(null);
      setIsModalOpen(false);
    }
  }
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useNEPalavrasChavesFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <NEPalavrasChavesGridMobileComponent
        data={nepalavraschavesData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <NEPalavrasChavesGridDesktopComponent
        data={nepalavraschavesData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <NEPalavrasChavesWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedNEPalavrasChaves={selectedNEPalavrasChaves}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar N E Palavras Chaves'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default NEPalavrasChavesGrid;