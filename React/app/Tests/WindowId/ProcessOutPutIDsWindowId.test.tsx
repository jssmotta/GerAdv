// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProcessOutPutIDsWindowId from '@/app/GerAdv_TS/ProcessOutPutIDs/Crud/Grids/ProcessOutPutIDsWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProcessOutPutIDsWindowId from '@/app/GerAdv_TS/ProcessOutPutIDs/Crud/Grids/ProcessOutPutIDsWindowId';
import { ProcessOutPutIDsEmpty } from '@/app/GerAdv_TS/Models/ProcessOutPutIDs';

describe('ProcessOutPutIDsWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProcessOutPutIDsWindowId).toBeDefined();
    expect(typeof ProcessOutPutIDsWindowId === 'function' || typeof ProcessOutPutIDsWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProcessOutPutIDsService class so constructor usage doesn't throw and we can control fetchProcessOutPutIDsById
const mockFetchProcessOutPutIDsById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProcessOutPutIDs/Services/ProcessOutPutIDs.service', () => ({
  ProcessOutPutIDsService: jest.fn().mockImplementation(() => ({
    fetchProcessOutPutIDsById: (...args: any[]) => mockFetchProcessOutPutIDsById(...args),
  })),
}));

// Mock the child ProcessOutPutIDsWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProcessOutPutIDs/Crud/Grids/ProcessOutPutIDsWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='processoutputids-window' {...p} />,
}));

describe('ProcessOutPutIDsWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProcessOutPutIDsWindowId).toBeDefined();
    expect(typeof ProcessOutPutIDsWindowId === 'function' || typeof ProcessOutPutIDsWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProcessOutPutIDsWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProcessOutPutIDsWindow with ProcessOutPutIDsEmpty when id is 0', async () => {
    render(
      <ProcessOutPutIDsWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProcessOutPutIDsWindow should be rendered with selectedProcessOutPutIDs equal to ProcessOutPutIDsEmpty()
    const node = await screen.findByTestId('processoutputids-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProcessOutPutIDs"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedprocessoutputids') || node.getAttribute('selectedProcessOutPutIDs')).not.toBeNull();
  });

  it('fetches processoutputids by id and passes it to ProcessOutPutIDsWindow when id > 0', async () => {
    const fakeProcessOutPutIDs = { id: 123, nomecliente: 'Test' };
    mockFetchProcessOutPutIDsById.mockResolvedValue(fakeProcessOutPutIDs);

    render(
      <ProcessOutPutIDsWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('processoutputids-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProcessOutPutIDsById).toHaveBeenCalledWith(123));
  });
});
