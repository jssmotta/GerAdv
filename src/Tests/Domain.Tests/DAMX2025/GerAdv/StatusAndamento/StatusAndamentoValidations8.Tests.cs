// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class StatusAndamentoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFStatusAndamentoFactory> _mockStatusAndamentoFactory;
    private readonly Mock<IStatusAndamentoReader> _mockReader;
    private readonly StatusAndamentoValidation _validation;
    private readonly Mock<IStatusAndamentoService> _mockStatusAndamentoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly StatusAndamentoService _service;
    private readonly string _validUri = "test-uri";
    public StatusAndamentoValidationTests()
    {
        _mockStatusAndamentoFactory = new Mock<IFStatusAndamentoFactory>();
        _mockReader = new Mock<IStatusAndamentoReader>();
        _validation = new StatusAndamentoValidation();
        _mockStatusAndamentoService = new Mock<IStatusAndamentoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void StatusAndamentoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IStatusAndamentoValidation>();
    }

    [Fact]
    public void StatusAndamentoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(StatusAndamentoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void StatusAndamentoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(StatusAndamentoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.StatusAndamento CreateValidStatusAndamento()
    {
        return new Models.StatusAndamento
        {
            Id = 1,
            Nome = "João",
            Icone = 0
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockStatusAndamentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusAndamento>(), It.IsAny<string>())).ReturnsAsync(new List<StatusAndamentoResponseAll>());
    // Setup other mocks but don't override the StatusAndamentos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockStatusAndamentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusAndamento>(), It.IsAny<string>())).ReturnsAsync(new List<StatusAndamentoResponseAll>());
    // Setup other mocks but don't override the StatusAndamentos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var statusandamento = new Models.StatusAndamento
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(statusandamento, _mockStatusAndamentoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockStatusAndamentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}