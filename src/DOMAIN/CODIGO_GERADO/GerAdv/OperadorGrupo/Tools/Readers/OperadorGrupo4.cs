// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Readers;
public partial class OperadorGrupoReader(IFOperadorGrupoFactory operadorgrupoFactory) : IOperadorGrupoReader
{
    private readonly IFOperadorGrupoFactory _operadorgrupoFactory = operadorgrupoFactory;
    public async Task<IEnumerable<OperadorGrupoResponseAll>> Listar(int max, string uri, string cWhere, List<SqlParameter> parameters, string order, CancellationToken cancellationToken) => await ListarTabela(BuildSqlQuery(DBOperadorGrupo.CamposSqlX, cWhere, order, max), parameters, uri, caching: DevourerOne.PCachingDefault, max: max, cancellationToken: cancellationToken);
    private async Task<IEnumerable<OperadorGrupoResponseAll>> ListarTabela(string sql, List<SqlParameter> parameters, string uri, bool caching = DevourerOne.PCachingDefault, int max = 200, CancellationToken cancellationToken = default)
    {
        var result = new List<OperadorGrupoResponseAll>(max);
        await using var connection = Configuracoes.GetConnectionByUri(uri);
        await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: connection?.InnerConnection)
        {
            CommandTimeout = 30
        };
        foreach (var param in parameters)
        {
            if (!cmd.Parameters.Contains(param.ParameterName))
            {
                var newParam = new SqlParameter(param.ParameterName, param.Value)
                {
                    SqlDbType = param.SqlDbType,
                    Direction = param.Direction,
                    Size = param.Size,
                    Precision = param.Precision,
                    Scale = param.Scale
                };
                cmd.Parameters.Add(newParam);
            }
        }

        await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.SingleResult);
        while (await reader.ReadAsync())
        {
            if (cancellationToken.IsCancellationRequested)
                return result;
            result.Add(ReadAll(_operadorgrupoFactory.CreateFromReader(reader), reader)!);
        }

        return result;
    }

    public async Task<OperadorGrupoResponse?> Read(int id, MsiSqlConnection oCnn)
    {
        using var dbRec = await _operadorgrupoFactory.CreateFromIdAsync(id, oCnn);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public async Task<Models.OperadorGrupo?> ReadM(int id, MsiSqlConnection oCnn)
    {
        using var dbRec = await _operadorgrupoFactory.CreateFromIdAsync(id, oCnn);
        var operadorgrupo = new Models.OperadorGrupo
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Grupo = dbRec.FGrupo,
            Inativo = dbRec.FInativo,
        };
        return operadorgrupo;
    }

    public OperadorGrupoResponse? Read(FOperadorGrupo dbRec, MsiSqlConnection oCnn)
    {
        return Read(dbRec);
    }

    public OperadorGrupoResponse? Read(string where, List<SqlParameter> parameters, MsiSqlConnection oCnn)
    {
        using var dbRec = _operadorgrupoFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public OperadorGrupoResponse? Read(FOperadorGrupo dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var operadorgrupo = new OperadorGrupoResponse
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Grupo = dbRec.FGrupo,
            Inativo = dbRec.FInativo,
        };
        return operadorgrupo;
    }

    public OperadorGrupoResponse? Read(SG.GerAdv.DBOperadorGrupo dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var operadorgrupo = new OperadorGrupoResponse
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Grupo = dbRec.FGrupo,
            Inativo = dbRec.FInativo,
        };
        return operadorgrupo;
    }

    public OperadorGrupoResponseAll? ReadAll(FOperadorGrupo dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var operadorgrupo = new OperadorGrupoResponseAll
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Grupo = dbRec.FGrupo,
            Inativo = dbRec.FInativo,
        };
        try
        {
            operadorgrupo.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return operadorgrupo;
    }

    public OperadorGrupoResponseAll? ReadAll(SG.GerAdv.DBOperadorGrupo dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var operadorgrupo = new OperadorGrupoResponseAll
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Grupo = dbRec.FGrupo,
            Inativo = dbRec.FInativo,
        };
        try
        {
            operadorgrupo.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return operadorgrupo;
    }
}