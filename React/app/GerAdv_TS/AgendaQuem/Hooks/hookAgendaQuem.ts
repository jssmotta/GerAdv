// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IAgendaQuemService } from '../Services/AgendaQuem.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IAgendaQuem } from '../Interfaces/interface.AgendaQuem';
import { FilterAgendaQuem } from '../Filters/AgendaQuem';
import { isValidDate } from '@/app/tools/datetime';
import { AgendaQuemApi } from '../Apis/ApiAgendaQuem';

export const useAgendaQuemForm = (
  initialAgendaQuem: IAgendaQuem,
  dataService: IAgendaQuemService
) => {
  const [data, setData] = useState<IAgendaQuem>(initialAgendaQuem);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadAgendaQuem = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialAgendaQuem);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchAgendaQuemById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Agenda Quem';
      setError(errorMessage);
      //console.log('Erro ao carregar Agenda Quem');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialAgendaQuem]);

  const resetForm = useCallback(() => {
    setData(initialAgendaQuem);
    setError(null);
  }, [initialAgendaQuem]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadAgendaQuem,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadAgendaQuem, resetForm]);
  return returnValue;
};


export const useAgendaQuemNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('AgendaQuem', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useAgendaQuemList = (dataService: IAgendaQuemService, currentFilter?: FilterAgendaQuem | undefined | null) => {
  const [data, setData] = useState<IAgendaQuem[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterAgendaQuem | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterAgendaQuem | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterAgendaQuem);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar agendaquem';
      setError(errorMessage);
      //console.log('Erro ao carregar agendaquem');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useAgendaQuemNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsAgendaQuem() {

  async function runValidation(data: IAgendaQuem, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const agendaquemApi = new AgendaQuemApi(uri ?? '', token ?? '');

    const result = await agendaquemApi.validation(data);

    return result;
  }

  function validate(data: IAgendaQuem): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}