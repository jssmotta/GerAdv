// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:NENotas
// Fonte:ControllerGenerator
namespace MenphisSI.GerAdv.Controller;
[Route("api/v{version:apiVersion}/{uri}/[controller]/[action]")]
[ApiController]
[ApiVersion("1.0")]
public partial class NENotasController(INENotasService nenotasService) : ControllerBase
{
    private readonly INENotasService _nenotasService = nenotasService;
    private static readonly Logger _logger = LogManager.GetCurrentClassLogger();
    [HttpGet]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAll([FromQuery] int max, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "GetAll";
        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.GetAll(max, uri);
            });
            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            NENotasMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: GetAll failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Filter([FromQuery] int max, [FromBody] MenphisSI.GerAdv.Filters.FilterNENotas filter, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "Filter";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.Filter(max, filter, uri);
            });
            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            NENotasMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: Filter failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> FilterVoice([FromBody] MenphisSI.GerAdv.Filters.FilterNENotasWithVoiceRequest request, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "FilterVoice";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.RelatoriosPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.FilterVoice(request.Filter, request.VoiceCommand, uri);
            });
            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: FilterVoice failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetById(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "GetById";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.GetById(id, uri, token);
            });
            if (result == null)
            {
                NENotasMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetById: No NENotas found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            NENotasMetrics.RecordReadByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: GetById failed with exception for id = {0}, {1}", id, uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [ProducesResponseType(typeof(AuditorResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAuditor(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "GetAuditor";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.GetAuditor(id, uri, token);
            });
            if (result == null)
            {
                NENotasMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetAuditor: No NENotas found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: GetAuditor failed with exception for id = {0}, {1}", id, uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = "Erro 500 Auditor" });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetListN([FromQuery] int max, [FromBody] Filters.FilterNENotas? filtro, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "GetListN";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.GetListN(max, filtro, uri);
            });
            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            NENotasMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: GetListN failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [ProducesResponseType(typeof(NENotasResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(NENotasResponse), StatusCodes.Status201Created)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> AddAndUpdate([FromBody] Models.NENotas regNENotas, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        var isNew = regNENotas.Id == 0;
        var operacao = isNew ? "Create" : "Update";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.AddAndUpdate(regNENotas, uri);
            });
            if (result == null)
            {
                NENotasMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("NENotas: AddAndUpdate failed to add or update NENotas, {0}", uri);
                return BadRequest();
            }

            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            if (isNew)
            {
                NENotasMetrics.RecordCreated(uri);
                NENotasMetrics.RecordCreatedByHour(uri);
            }
            else
            {
                NENotasMetrics.RecordUpdated(uri);
                NENotasMetrics.RecordUpdatedByHour(uri);
            }

            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: AddAndUpdate failed with exception for uri = {0}", uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = ex.Message });
        }
    }

    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [HttpDelete]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> Delete([FromQuery] int id, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "Delete";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.Delete(id, uri);
            });
            if (result == null)
            {
                NENotasMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("Delete: No NENotas found to delete with id = {0}, {1}", id, uri);
                return NotFound();
            }

            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            NENotasMetrics.RecordDeleted(uri);
            NENotasMetrics.RecordDeletedByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordConflict(operacao, uri, stopwatch);
            _logger.Error(ex, "NENotas: Delete failed with exception for id = {0}, {1}", id, uri);
            return Conflict(new Error500 { success = false, data = "", message = "Não é possível excluir o registro porque ele está sendo referenciado/em uso em outra tabela." });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Validation([FromBody] Models.NENotas regNENotas, [FromRoute, Required] string uri)
    {
        var stopwatch = NENotasMetrics.StartTimer();
        const string operacao = "Validation";
        if (!ModelState.IsValid)
        {
            NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _nenotasService.Validation(regNENotas, uri);
            });
            if (result == null)
            {
                NENotasMetrics.RecordInvalid(operacao, uri, stopwatch);
                _logger.Warn("NENotas: Validation failed to add or update NENotas, {0}", uri);
                return BadRequest();
            }

            NENotasMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            NENotasMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("NENotas: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            NENotasMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "NENotas: Validation failed with exception for uri = {0}", uri);
            return StatusCode(500, new { success = false, data = "", message = ex.Message });
        }
    }
}