// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using MenphisSI.GerAdv.Models.Response.All;
using MenphisSI.GerAdv.Readers;
using System.Data;

namespace MenphisSI.GerAdv.Tests.Readers;
/// <summary>
/// Comprehensive unit tests for PontoVirtualReader class
/// Tests all public methods including async operations, data reading, and data transformation
/// </summary>
public class PontoVirtualReaderTests : IDisposable
{
    private readonly Mock<IFPontoVirtualFactory> _mockPontoVirtualFactory;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<IDataRecord> _mockDataRecord;
    private readonly PontoVirtualReader _pontovirtualReader;
    public PontoVirtualReaderTests()
    {
        _mockPontoVirtualFactory = new Mock<IFPontoVirtualFactory>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockDataRecord = new Mock<IDataRecord>();
        _pontovirtualReader = new PontoVirtualReader(_mockPontoVirtualFactory.Object);
    }

#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldInitializeReader()
    {
        // Act & Assert
        _pontovirtualReader.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new PontoVirtualReader(null !));
    }

#endregion
#region Listar Tests
    [Fact]
    public async Task Listar_WithValidParameters_ShouldCallListarTabela()
    {
        // Arrange
        var max = 10;
        var uri = "valid-uri"; // This would need to be a valid URI in actual implementation
        var cWhere = "pvtCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // Since this calls external dependencies and database connections,
        // we expect it to throw an exception with our test setup
        await Assert.ThrowsAsync<Exception>(() => _pontovirtualReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

    [Fact]
    public async Task Listar_WithCancellationToken_ShouldRespectCancellation()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri";
        var cWhere = "pvtCodigo > 0";
        var parameters = new List<SqlParameter>();
        var order = "carNome";
        var cancellationToken = new CancellationToken(true); // Already cancelled
        // Act & Assert
        // Even with cancellation, this should throw an exception due to invalid URI
        await Assert.ThrowsAsync<Exception>(() => _pontovirtualReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Read Tests with ID and Connection
    [Fact]
    public async Task Read_WithValidId_ShouldReturnPontoVirtualResponse()
    {
        // Arrange
        var id = 123;
        var expectedPontoVirtual = new FPontoVirtual
        {
            ID = id,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedPontoVirtual);
        // Act
        var result = await _pontovirtualReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task Read_WithEmptyId_ShouldReturnNull()
    {
        // Arrange
        var id = 0; // Empty ID
        var emptyFPontoVirtual = new FPontoVirtual
        {
            ID = 0
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFPontoVirtual);
        // Act
        var result = await _pontovirtualReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public async Task Read_WithNegativeId_ShouldReturnNull()
    {
        // Arrange
        var id = -1;
        var emptyFPontoVirtual = new FPontoVirtual
        {
            ID = 0
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFPontoVirtual);
        // Act
        var result = await _pontovirtualReader.Read(id, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region ReadM Tests
    [Fact]
    public async Task ReadM_WithValidId_ShouldReturnPontoVirtualModel()
    {
        // Arrange
        var id = 123;
        var expectedFPontoVirtual = new FPontoVirtual
        {
            ID = id,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFPontoVirtual);
        // Act
        var result = await _pontovirtualReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public async Task ReadM_WithEmptyId_ShouldReturnModelWithDefaults()
    {
        // Arrange
        var id = 0;
        var emptyFPontoVirtual = new FPontoVirtual
        {
            ID = 0,
            FKey = null
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(emptyFPontoVirtual);
        // Act
        var result = await _pontovirtualReader.ReadM(id, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(0);
        result?.Key.Should().Be(string.Empty);
    }

#endregion
#region Read with FPontoVirtual and Connection Tests
    [Fact]
    public void Read_WithValidFPontoVirtualAndConnection_ShouldReturnPontoVirtualResponse()
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFPontoVirtualAndConnection_ShouldReturnNull()
    {
        // Arrange
        FPontoVirtual? dbRec = null;
        // Act
        var result = _pontovirtualReader.Read(dbRec, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with Where and Parameters Tests
    [Fact]
    public void Read_WithWhereAndParameters_ShouldReturnPontoVirtualResponse()
    {
        // Arrange
        var where = "pvtCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedFPontoVirtual = new FPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", where, "")).Returns(expectedFPontoVirtual);
        // Act
        var result = _pontovirtualReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithWhereAndParameters_WhenRecordNotFound_ShouldReturnNull()
    {
        // Arrange
        var where = "pvtCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 999)
        };
        var emptyFPontoVirtual = new FPontoVirtual
        {
            ID = 0
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", where, "")).Returns(emptyFPontoVirtual);
        // Act
        var result = _pontovirtualReader.Read(where, parameters, _mockConnection.Object);
        // Assert
        result.Should().BeNull();
    }

#endregion
#region Read with FPontoVirtual Tests
    [Fact]
    public void Read_WithValidFPontoVirtual_ShouldReturnPontoVirtualResponse()
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullFPontoVirtual_ShouldReturnNull()
    {
        // Arrange
        FPontoVirtual? dbRec = null;
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithFPontoVirtualNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = 123,
            FKey = null
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be(string.Empty);
    }

#endregion
#region Read with DBPontoVirtual Tests
    [Fact]
    public void Read_WithValidSGDBPontoVirtual_ShouldReturnPontoVirtualResponse()
    {
        // Arrange
        var dbRec = new DBPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void Read_WithNullSGDBPontoVirtual_ShouldReturnNull()
    {
        // Arrange
        DBPontoVirtual? dbRec = null;
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void Read_WithSGDBPontoVirtualNullNome_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBPontoVirtual
        {
            ID = 123,
            FKey = null // This should result in empty string in response
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with FPontoVirtual and IDataRecord Tests
    [Fact]
    public void ReadAll_WithValidFPontoVirtualAndDataRecord_ShouldReturnPontoVirtualResponseAll()
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullFPontoVirtualAndDataRecord_ShouldReturnNull()
    {
        // Arrange
        FPontoVirtual? dbRec = null;
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithFPontoVirtualNullNomeAndDataRecord_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = 123,
            FKey = null
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be(string.Empty);
    }

#endregion
#region ReadAll with DBPontoVirtual and DataRow Tests
    [Fact]
    public void ReadAll_WithValidSGDBPontoVirtualAndDataRow_ShouldReturnPontoVirtualResponseAll()
    {
        // Arrange
        var dbRec = new DBPontoVirtual
        {
            ID = 123,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be("AAAAAAAAAAAAAAAAAAAAA");
    }

    [Fact]
    public void ReadAll_WithNullSGDBPontoVirtualAndDataRow_ShouldReturnNull()
    {
        // Arrange
        DBPontoVirtual? dbRec = null;
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, null);
        // Assert
        result.Should().BeNull();
    }

    [Fact]
    public void ReadAll_WithSGDBPontoVirtualNullNomeAndDataRow_ShouldReturnEmptyStringForNome()
    {
        // Arrange
        var dbRec = new DBPontoVirtual
        {
            ID = 123,
            FKey = null // This should result in empty string in response
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, null);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(123);
        result?.Key.Should().Be(string.Empty);
    }

#endregion
#region Edge Cases and Error Handling Tests
    [Fact]
    public async Task Read_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _pontovirtualReader.Read(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task ReadM_WithFactoryException_ShouldPropagateException()
    {
        // Arrange
        var id = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _pontovirtualReader.ReadM(id, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public void Read_WithWhereParameters_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var where = "pvtCodigo = @id";
        var parameters = new List<SqlParameter>
        {
            new("@id", 123)
        };
        var expectedException = new InvalidOperationException("Factory error");
        _mockPontoVirtualFactory.Setup(x => x.CreateFromParameters(parameters, _mockConnection.Object, "", where, "")).Throws(expectedException);
        // Act & Assert
        var exception = Assert.Throws<InvalidOperationException>(() => _pontovirtualReader.Read(where, parameters, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Data Consistency Tests
    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void Read_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = id,
            FKey = nome
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        if (id == 0)
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
        }
        else
        {
            result.Should().NotBeNull();
            result?.Id.Should().Be(id);
            result?.Key.Should().Be(nome);
        }
    }

    [Theory]
    [InlineData(1, "AAAAAAAAAAAAAAAAAAAAA")]
    [InlineData(999, "BBBBBBBBBBBBBBBBBBBB")]
    [InlineData(0, "")]
    public void ReadAll_WithVariousData_ShouldMaintainConsistency(int id, string nome)
    {
        // Arrange
        var dbRec = new FPontoVirtual
        {
            ID = id,
            FKey = nome
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result?.Id.Should().Be(id);
        result?.Key.Should().Be(nome);
    }

#endregion
#region Async Behavior Tests
    [Fact]
    public async Task AsyncMethods_ShouldCompleteWithoutBlocking()
    {
        // Arrange
        var id = 123;
        var expectedFPontoVirtual = new FPontoVirtual
        {
            ID = id,
            FKey = "AAAAAAAAAAAAAAAAAAAAA"
        };
        _mockPontoVirtualFactory.Setup(x => x.CreateFromIdAsync(id, _mockConnection.Object)).ReturnsAsync(expectedFPontoVirtual);
        // Act & Assert
        // Test only the methods that don't depend on external URI connections
        var readTask = _pontovirtualReader.Read(id, _mockConnection.Object);
        var readMTask = _pontovirtualReader.ReadM(id, _mockConnection.Object);
        await Task.WhenAll(readTask, readMTask);
        // If we reach here, async methods completed without deadlock
        Assert.True(true);
    }

    [Fact]
    public async Task ListarN_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _pontovirtualReader.ListarN(max, uri, cWhere, parameters, order));
    }

    [Fact]
    public async Task Listar_WithInvalidUri_ShouldThrowException()
    {
        // Arrange
        var max = 10;
        var uri = "test-uri"; // Invalid URI
        var cWhere = "";
        var parameters = new List<SqlParameter>();
        var order = "";
        var cancellationToken = CancellationToken.None;
        // Act & Assert
        // This should throw an exception because the URI is invalid
        await Assert.ThrowsAsync<Exception>(() => _pontovirtualReader.Listar(max, uri, cWhere, parameters, order, cancellationToken));
    }

#endregion
#region Parameter Validation Tests
    [Fact]
    public async Task Read_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        // This would typically throw an exception or handle gracefully
        // depending on the implementation
        try
        {
            await _pontovirtualReader.Read(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public async Task ReadM_WithNullConnection_ShouldHandleGracefully()
    {
        // Arrange
        var id = 123;
        // Act & Assert
        try
        {
            await _pontovirtualReader.ReadM(id, null !);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

    [Fact]
    public void Read_WithNullParametersList_ShouldHandleGracefully()
    {
        // Arrange
        var where = "pvtCodigo = @id";
        // Act & Assert
        try
        {
            _pontovirtualReader.Read(where, null !, _mockConnection.Object);
        }
        catch (Exception ex)
        {
            ex.Should().NotBeNull();
        }
    }

#endregion
#region Integration-like Tests
    [Fact]
    public void Read_FPontoVirtual_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 456;
        var testNome = "MA";
        var dbRec = new FPontoVirtual
        {
            ID = testId,
            FKey = testNome
        };
        // Act
        var result = _pontovirtualReader.Read(dbRec);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<PontoVirtualResponse>();
        result!.Id.Should().Be(testId);
        result.Key.Should().Be(testNome);
    }

    [Fact]
    public void ReadAll_FPontoVirtual_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var testId = 789;
        var testNome = "AAAAAAAAAAAAAAAAAAAAA";
        var dbRec = new FPontoVirtual
        {
            ID = testId,
            FKey = testNome
        };
        // Act
        var result = _pontovirtualReader.ReadAll(dbRec, _mockDataRecord.Object);
        // Assert
        result.Should().NotBeNull();
        result.Should().BeOfType<PontoVirtualResponseAll>();
        result!.Id.Should().Be(testId);
        result.Key.Should().Be(testNome);
    }

#endregion
#region Dispose
    public virtual void Dispose()
    {
        // Clean up any resources if needed
        GC.SuppressFinalize(this);
    }
#endregion
}