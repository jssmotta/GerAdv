// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class FaseWriterTests
{
    private readonly Mock<IFFaseFactory> _mockFaseFactory;
    private readonly Mock<FFase> _mockFFase;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly FaseWriter _faseWriter;
    public FaseWriterTests()
    {
        _mockFaseFactory = new Mock<IFFaseFactory>();
        _mockFFase = new Mock<FFase>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _faseWriter = new FaseWriter(_mockFaseFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var fase = CreateValidFaseModel();
        fase.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockFaseFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFFase.Object);
        _mockFFase.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _faseWriter.WriteAsync(fase, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFFase.Object);
        _mockFaseFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockFaseFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFFase.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var fase = CreateValidFaseModel();
        fase.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockFaseFactory.Setup(x => x.CreateFromIdAsync(fase.Id, _mockConnection.Object)).ReturnsAsync(_mockFFase.Object);
        _mockFFase.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _faseWriter.WriteAsync(fase, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFFase.Object);
        _mockFaseFactory.Verify(x => x.CreateFromIdAsync(fase.Id, _mockConnection.Object), Times.Once);
        _mockFaseFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFFase.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var fase = CreateValidFaseModel();
        var auditorQuem = 99;
        _mockFaseFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFFase.Object);
        _mockFFase.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _faseWriter.WriteAsync(fase, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFFase.Object);
        _mockFFase.VerifySet(x => x.FDescricao = fase.Descricao, Times.Once);
        _mockFFase.VerifySet(x => x.FJustica = fase.Justica, Times.Once);
        _mockFFase.VerifySet(x => x.FArea = fase.Area, Times.Once);
        _mockFFase.VerifySet(x => x.FGUID = fase.GUID, Times.Once);
        _mockFFase.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var fase = CreateValidFaseModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockFaseFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _faseWriter.WriteAsync(fase, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var fase = CreateValidFaseModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockFaseFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFFase.Object);
        _mockFFase.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _faseWriter.WriteAsync(fase, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var faseResponse = new FaseResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockFaseFactory.Setup(x => x.DeleteAsync(operadorId, faseResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _faseWriter.Delete(faseResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockFaseFactory.Verify(x => x.DeleteAsync(operadorId, faseResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var faseResponse = new FaseResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockFaseFactory.Setup(x => x.DeleteAsync(operadorId, faseResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _faseWriter.Delete(faseResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var faseResponse = new FaseResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockFaseFactory.Setup(x => x.DeleteAsync(operadorId, faseResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _faseWriter.Delete(faseResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new FaseWriter(_mockFaseFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new FaseWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Fase CreateValidFaseModel()
    {
        return new Models.Fase
        {
            Id = 0,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Justica = 1,
            Area = 1,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}