// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class AtividadesValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFAtividadesFactory> _mockAtividadesFactory;
    private readonly Mock<IAtividadesReader> _mockReader;
    private readonly AtividadesValidation _validation;
    private readonly Mock<IAtividadesService> _mockAtividadesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly AtividadesService _service;
    private readonly string _validUri = "test-uri";
    public AtividadesValidationTests()
    {
        _mockAtividadesFactory = new Mock<IFAtividadesFactory>();
        _mockReader = new Mock<IAtividadesReader>();
        _validation = new AtividadesValidation();
        _mockAtividadesService = new Mock<IAtividadesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void AtividadesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IAtividadesValidation>();
    }

    [Fact]
    public void AtividadesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(AtividadesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void AtividadesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(AtividadesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Atividades CreateValidAtividades()
    {
        return new Models.Atividades
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockAtividadesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterAtividades>(), It.IsAny<string>())).ReturnsAsync(new List<AtividadesResponseAll>());
    // Setup other mocks but don't override the Atividadess service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockAtividadesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterAtividades>(), It.IsAny<string>())).ReturnsAsync(new List<AtividadesResponseAll>());
    // Setup other mocks but don't override the Atividadess service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var atividades = new Models.Atividades
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(atividades, _mockAtividadesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockAtividadesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}