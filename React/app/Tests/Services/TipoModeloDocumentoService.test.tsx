// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoModeloDocumentoService, TipoModeloDocumentoValidator } from '@/app/GerAdv_TS/TipoModeloDocumento/Services/TipoModeloDocumento.service';
import { TipoModeloDocumentoApi, TipoModeloDocumentoApiError } from '@/app/GerAdv_TS/TipoModeloDocumento/Apis/ApiTipoModeloDocumento';
import { ITipoModeloDocumento } from '@/app/GerAdv_TS/TipoModeloDocumento/Interfaces/interface.TipoModeloDocumento';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoModeloDocumentoApi;

const sample: ITipoModeloDocumento = { id: 1, nome: 'Teste' };

describe('TipoModeloDocumentoService', () => {
  let service: TipoModeloDocumentoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoModeloDocumentoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoModeloDocumentoValidator, 'validateTipoModeloDocumento').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoModeloDocumentoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoModeloDocumentoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoModeloDocumentoById lança TipoModeloDocumentoApiError para id inválido', async () => {
    await expect(service.fetchTipoModeloDocumentoById(0)).rejects.toBeInstanceOf(TipoModeloDocumentoApiError);
  });

  it('saveTipoModeloDocumento chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoModeloDocumento(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoModeloDocumento lança TipoModeloDocumentoApiError quando validação falha', async () => {
    (TipoModeloDocumentoValidator.validateTipoModeloDocumento as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoModeloDocumento(sample)).rejects.toBeInstanceOf(TipoModeloDocumentoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoModeloDocumento chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoModeloDocumento(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoModeloDocumento lança TipoModeloDocumentoApiError para id inválido', async () => {
    await expect(service.deleteTipoModeloDocumento(0)).rejects.toBeInstanceOf(TipoModeloDocumentoApiError);
  });

  it('validateTipoModeloDocumento delega para TipoModeloDocumentoValidator', () => {
    (TipoModeloDocumentoValidator.validateTipoModeloDocumento as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoModeloDocumento(sample);
    expect(r.isValid).toBe(true);
  });
});
