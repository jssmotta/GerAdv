// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBGUTTipo - Nome da classe (ex: DBClientes)
/// GUTTipo - Nome da tabela (ex: Clientes)
/// gtt - Prefixo dos campos (ex: cli)
/// gttCodigo - Nome do campo código (ex: cliCodigo)
/// gttNome - Nome do campo nome (ex: cliNome)
/// DBGUTTipo.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBGUTTipoTests : IDisposable
{
    private readonly DBGUTTipo _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBGUTTipoTests()
    {
        _instance = new DBGUTTipo();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("gttCodigo", typeof(int));
        dt.Columns.Add("gttQuemCad", typeof(int));
        dt.Columns.Add("gttDtCad", typeof(DateTime));
        dt.Columns.Add("gttQuemAtu", typeof(int));
        dt.Columns.Add("gttDtAtu", typeof(DateTime));
        dt.Columns.Add("gttVisto", typeof(bool));
        dt.Columns.Add("gttNome", typeof(string));
        dt.Columns.Add("gttOrdem", typeof(int));
        dt.Columns.Add("gttGuid", typeof(string));
        return dt;
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["gttCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBGUTTipo(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("GUTTipo", DBGUTTipo.PTabelaNome);
        Assert.Equal(" GUTTipo.* ", DBGUTTipo.CamposSqlX);
        Assert.Equal("gttCodigo", DBGUTTipo.CampoCodigo);
        Assert.Equal("gttNome", DBGUTTipo.CampoNome);
        Assert.Equal("gtt", DBGUTTipo.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBGUTTipo();
        Assert.Equal(0, instance.ID);
        Assert.Equal("GUTTipo", instance.ITableName());
        Assert.Equal("gtt", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBGUTTipo((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBGUTTipo((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICrud_Implementation_ShouldWork()
    {
        ICrud cadastro = (ICrud)_instance;
        Assert.Equal("GUTTipo", cadastro.ITableName());
        Assert.Equal("gttCodigo", cadastro.IFieldId());
        Assert.Equal("gttNome", cadastro.IFieldNameDescription());
        Assert.Equal("gtt", cadastro.IPrefix());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBGUTTipo.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBGUTTipo.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBGUTTipo.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBGUTTipo();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBGUTTipo();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 150 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 150);
    }

    [Theory]
    [InlineData(0)]
    [InlineData(1)]
    [InlineData(-1)]
    [InlineData(int.MaxValue)]
    [InlineData(int.MinValue)]
    public void Ordem_ShouldAcceptIntegerValues(int value)
    {
        _instance.FOrdem = value;
        Assert.Equal(value, _instance.FOrdem);
    }

    [Fact]
    public void Ordem_DefaultValue_ShouldBeZero()
    {
        var instance = new DBGUTTipo();
        Assert.Equal(0, instance.FOrdem);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Guid_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FGuid = input;
        Assert.Equal(expected, _instance.FGuid);
    }

    [Fact]
    public void Guid_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 50 + 10);
        _instance.FGuid = longString;
        Assert.True(_instance.FGuid.Length <= 50);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}