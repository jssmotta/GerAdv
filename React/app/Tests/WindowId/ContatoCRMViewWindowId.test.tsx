// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ContatoCRMViewWindowId from '@/app/GerAdv_TS/ContatoCRMView/Crud/Grids/ContatoCRMViewWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ContatoCRMViewWindowId from '@/app/GerAdv_TS/ContatoCRMView/Crud/Grids/ContatoCRMViewWindowId';
import { ContatoCRMViewEmpty } from '@/app/GerAdv_TS/Models/ContatoCRMView';

describe('ContatoCRMViewWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ContatoCRMViewWindowId).toBeDefined();
    expect(typeof ContatoCRMViewWindowId === 'function' || typeof ContatoCRMViewWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ContatoCRMViewService class so constructor usage doesn't throw and we can control fetchContatoCRMViewById
const mockFetchContatoCRMViewById = jest.fn();
jest.mock('@/app/GerAdv_TS/ContatoCRMView/Services/ContatoCRMView.service', () => ({
  ContatoCRMViewService: jest.fn().mockImplementation(() => ({
    fetchContatoCRMViewById: (...args: any[]) => mockFetchContatoCRMViewById(...args),
  })),
}));

// Mock the child ContatoCRMViewWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ContatoCRMView/Crud/Grids/ContatoCRMViewWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='contatocrmview-window' {...p} />,
}));

describe('ContatoCRMViewWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ContatoCRMViewWindowId).toBeDefined();
    expect(typeof ContatoCRMViewWindowId === 'function' || typeof ContatoCRMViewWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ContatoCRMViewWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ContatoCRMViewWindow with ContatoCRMViewEmpty when id is 0', async () => {
    render(
      <ContatoCRMViewWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ContatoCRMViewWindow should be rendered with selectedContatoCRMView equal to ContatoCRMViewEmpty()
    const node = await screen.findByTestId('contatocrmview-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedContatoCRMView"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcontatocrmview') || node.getAttribute('selectedContatoCRMView')).not.toBeNull();
  });

  it('fetches contatocrmview by id and passes it to ContatoCRMViewWindow when id > 0', async () => {
    const fakeContatoCRMView = { id: 123, nomecliente: 'Test' };
    mockFetchContatoCRMViewById.mockResolvedValue(fakeContatoCRMView);

    render(
      <ContatoCRMViewWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('contatocrmview-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchContatoCRMViewById).toHaveBeenCalledWith(123));
  });
});
