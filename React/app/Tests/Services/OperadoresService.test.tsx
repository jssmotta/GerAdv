// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OperadoresService, OperadoresValidator } from '@/app/GerAdv_TS/Operadores/Services/Operadores.service';
import { OperadoresApi, OperadoresApiError } from '@/app/GerAdv_TS/Operadores/Apis/ApiOperadores';
import { IOperadores } from '@/app/GerAdv_TS/Operadores/Interfaces/interface.Operadores';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OperadoresApi;

const sample: IOperadores = { id: 1, nome: 'Teste' };

describe('OperadoresService', () => {
  let service: OperadoresService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OperadoresService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OperadoresValidator, 'validateOperadores').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOperadoresById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOperadoresById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOperadoresById lança OperadoresApiError para id inválido', async () => {
    await expect(service.fetchOperadoresById(0)).rejects.toBeInstanceOf(OperadoresApiError);
  });

  it('saveOperadores chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOperadores(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOperadores lança OperadoresApiError quando validação falha', async () => {
    (OperadoresValidator.validateOperadores as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOperadores(sample)).rejects.toBeInstanceOf(OperadoresApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOperadores chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOperadores(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOperadores lança OperadoresApiError para id inválido', async () => {
    await expect(service.deleteOperadores(0)).rejects.toBeInstanceOf(OperadoresApiError);
  });

  it('validateOperadores delega para OperadoresValidator', () => {
    (OperadoresValidator.validateOperadores as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOperadores(sample);
    expect(r.isValid).toBe(true);
  });
});
