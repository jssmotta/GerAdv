// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import SituacaoWindowId from '@/app/GerAdv_TS/Situacao/Crud/Grids/SituacaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import SituacaoWindowId from '@/app/GerAdv_TS/Situacao/Crud/Grids/SituacaoWindowId';
import { SituacaoEmpty } from '@/app/GerAdv_TS/Models/Situacao';

describe('SituacaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(SituacaoWindowId).toBeDefined();
    expect(typeof SituacaoWindowId === 'function' || typeof SituacaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the SituacaoService class so constructor usage doesn't throw and we can control fetchSituacaoById
const mockFetchSituacaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Situacao/Services/Situacao.service', () => ({
  SituacaoService: jest.fn().mockImplementation(() => ({
    fetchSituacaoById: (...args: any[]) => mockFetchSituacaoById(...args),
  })),
}));

// Mock the child SituacaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Situacao/Crud/Grids/SituacaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='situacao-window' {...p} />,
}));

describe('SituacaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(SituacaoWindowId).toBeDefined();
    expect(typeof SituacaoWindowId === 'function' || typeof SituacaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <SituacaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders SituacaoWindow with SituacaoEmpty when id is 0', async () => {
    render(
      <SituacaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // SituacaoWindow should be rendered with selectedSituacao equal to SituacaoEmpty()
    const node = await screen.findByTestId('situacao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedSituacao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedsituacao') || node.getAttribute('selectedSituacao')).not.toBeNull();
  });

  it('fetches situacao by id and passes it to SituacaoWindow when id > 0', async () => {
    const fakeSituacao = { id: 123, nomecliente: 'Test' };
    mockFetchSituacaoById.mockResolvedValue(fakeSituacao);

    render(
      <SituacaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('situacao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchSituacaoById).toHaveBeenCalledWith(123));
  });
});
