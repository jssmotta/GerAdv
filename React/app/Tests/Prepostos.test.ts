// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { usePrepostosForm, usePrepostosList, useValidationsPrepostos } from '../GerAdv_TS/Prepostos/Hooks/hookPrepostos';
import { IPrepostos } from '../GerAdv_TS/Prepostos/Interfaces/interface.Prepostos';
import { IPrepostosService } from '../GerAdv_TS/Prepostos/Services/Prepostos.service';
import { PrepostosTestEmpty } from '../GerAdv_TS/Models/Prepostos';
import { usePrepostosComboBox } from '../GerAdv_TS/Prepostos/Hooks/hookPrepostos';

// Mock do serviço
const mockPrepostosService: jest.Mocked<IPrepostosService> = {
  fetchPrepostosById: jest.fn(),
  savePrepostos: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deletePrepostos: jest.fn(),
  validatePrepostos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialPrepostos: IPrepostos = { ...PrepostosTestEmpty() };

describe('usePrepostosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    expect(result.current.data).toEqual(initialPrepostos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Prepostos',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Prepostos');
  });

   test('deve carregar Prepostos por ID', async () => {
    const mockPrepostos = { ...initialPrepostos, id: 1, nome: 'Prepostos Teste' };
    mockPrepostosService.fetchPrepostosById.mockResolvedValue(mockPrepostos);

    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    await act(async () => {
      await result.current.loadPrepostos(1);
    });

    expect(mockPrepostosService.fetchPrepostosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockPrepostos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Prepostos', async () => {
    const errorMessage = 'Erro ao carregar Prepostos';
    mockPrepostosService.fetchPrepostosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    await act(async () => {
      await result.current.loadPrepostos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialPrepostos, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialPrepostos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    await act(async () => {
      await result.current.loadPrepostos(0);
    });

    expect(mockPrepostosService.fetchPrepostosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialPrepostos);
  });
});

describe('usePrepostosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePrepostosList(mockPrepostosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialPrepostos, id: 1, nome: 'Prepostos 1' },
      { ...initialPrepostos, id: 2, nome: 'Prepostos 2' }
    ];
    mockPrepostosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePrepostosList(mockPrepostosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockPrepostosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockPrepostosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePrepostosList(mockPrepostosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialPrepostos, id: 1, nome: 'Prepostos Filtrado' }];
    const filtro = { nome: 'Prepostos' };
    mockPrepostosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePrepostosList(mockPrepostosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockPrepostosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsPrepostos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsPrepostos());

    const validData = { ...initialPrepostos, nome: 'Prepostos Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsPrepostos());

    const invalidData = { ...initialPrepostos, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsPrepostos());

    const invalidData = { 
      ...initialPrepostos, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsPrepostos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialPrepostos, id: 1, nome: 'Prepostos Teste' }];
    mockPrepostosService.getAll.mockResolvedValue(mockData);
    mockPrepostosService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      usePrepostosList(mockPrepostosService)
    );
    
     const { result: comboResult } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsPrepostos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Prepostos Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Prepostos Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      usePrepostosForm(initialPrepostos, mockPrepostosService)
    );

    const mockEvent = {
      target: {
        name: 'liberaagenda',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.liberaagenda).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Prepostos 1' },
      { id: 2, nome: 'Prepostos 2' }
    ];
    mockPrepostosService.getList.mockResolvedValue(mockOptions as IPrepostos[]);


    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Prepostos 1' },
        { id: 2, nome: 'Prepostos 2' }
      ]);
    });


    expect(mockPrepostosService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Prepostos ABC' },
      { id: 2, nome: 'Prepostos XYZ' }
    ];
    mockPrepostosService.getList.mockResolvedValue(mockOptions as IPrepostos[]);   


 const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Prepostos ABC' },
        { id: 2, nome: 'Prepostos XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Prepostos ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Prepostos ABC' },
      { id: 2, nome: 'Prepostos XYZ' }
    ];
    mockPrepostosService.getList.mockResolvedValue(mockOptions as IPrepostos[]);
  


    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Prepostos ABC' },
        { id: 2, nome: 'Prepostos XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Prepostos ABC' },
          {id: 2, nome: 'Prepostos XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );

    const newValue = { id: 1, nome: 'Prepostos Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Prepostos Inicial' };
    
    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('usePrepostosComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Prepostos Inicial' };
    
    const { result } = renderHook(() => 
      usePrepostosComboBox(mockPrepostosService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






