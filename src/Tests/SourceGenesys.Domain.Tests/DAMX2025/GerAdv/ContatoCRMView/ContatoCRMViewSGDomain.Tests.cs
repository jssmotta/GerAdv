// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBContatoCRMView - Nome da classe (ex: DBClientes)
/// ContatoCRMView - Nome da tabela (ex: Clientes)
/// ccw - Prefixo dos campos (ex: cli)
/// ccwCodigo - Nome do campo código (ex: cliCodigo)
/// ccwData - Nome do campo nome (ex: cliNome)
/// DBContatoCRMView.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBContatoCRMViewTests : IDisposable
{
    private readonly DBContatoCRMView _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBContatoCRMViewTests()
    {
        _instance = new DBContatoCRMView();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("ccwCodigo", typeof(int));
        dt.Columns.Add("ccwCGUID", typeof(string));
        dt.Columns.Add("ccwData", typeof(string));
        dt.Columns.Add("ccwIP", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("ContatoCRMView", DBContatoCRMView.PTabelaNome);
        Assert.Equal(" ContatoCRMView.* ", DBContatoCRMView.CamposSqlX);
        Assert.Equal("ccwCodigo", DBContatoCRMView.CampoCodigo);
        Assert.Equal("ccwData", DBContatoCRMView.CampoNome);
        Assert.Equal("ccw", DBContatoCRMView.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBContatoCRMView();
        Assert.Equal(0, instance.ID);
        Assert.Equal("ContatoCRMView", instance.ITabelaName());
        Assert.Equal("ccw", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBContatoCRMView((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBContatoCRMView((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["ccwCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBContatoCRMView(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("ContatoCRMView", cadastro.ITabelaName());
        Assert.Equal("ccwCodigo", cadastro.ICampoCodigo());
        Assert.Equal("ccwData", cadastro.ICampoNome());
        Assert.Equal("ccw", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBContatoCRMView.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBContatoCRMView.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBContatoCRMView.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBContatoCRMView();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBContatoCRMView();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnFalse()
    {
        Assert.False(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void CGUID_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FCGUID = input;
        Assert.Equal(expected, _instance.FCGUID);
    }

    [Fact]
    public void CGUID_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 100 + 10);
        _instance.FCGUID = longString;
        Assert.True(_instance.FCGUID.Length <= 100);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void IP_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FIP = input;
        Assert.Equal(expected, _instance.FIP);
    }

    [Fact]
    public void IP_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 50 + 10);
        _instance.FIP = longString;
        Assert.True(_instance.FIP.Length <= 50);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}