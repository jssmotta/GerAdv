// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { BensClassificacaoService, BensClassificacaoValidator } from '@/app/GerAdv_TS/BensClassificacao/Services/BensClassificacao.service';
import { BensClassificacaoApi, BensClassificacaoApiError } from '@/app/GerAdv_TS/BensClassificacao/Apis/ApiBensClassificacao';
import { IBensClassificacao } from '@/app/GerAdv_TS/BensClassificacao/Interfaces/interface.BensClassificacao';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as BensClassificacaoApi;

const sample: IBensClassificacao = { id: 1, nome: 'Teste' };

describe('BensClassificacaoService', () => {
  let service: BensClassificacaoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new BensClassificacaoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(BensClassificacaoValidator, 'validateBensClassificacao').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchBensClassificacaoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchBensClassificacaoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchBensClassificacaoById lança BensClassificacaoApiError para id inválido', async () => {
    await expect(service.fetchBensClassificacaoById(0)).rejects.toBeInstanceOf(BensClassificacaoApiError);
  });

  it('saveBensClassificacao chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveBensClassificacao(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveBensClassificacao lança BensClassificacaoApiError quando validação falha', async () => {
    (BensClassificacaoValidator.validateBensClassificacao as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveBensClassificacao(sample)).rejects.toBeInstanceOf(BensClassificacaoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteBensClassificacao chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteBensClassificacao(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteBensClassificacao lança BensClassificacaoApiError para id inválido', async () => {
    await expect(service.deleteBensClassificacao(0)).rejects.toBeInstanceOf(BensClassificacaoApiError);
  });

  it('validateBensClassificacao delega para BensClassificacaoValidator', () => {
    (BensClassificacaoValidator.validateBensClassificacao as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateBensClassificacao(sample);
    expect(r.isValid).toBe(true);
  });
});
