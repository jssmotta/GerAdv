// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { CRUD_CONSTANTS } from '@/app/tools/crud';
import { SetorApi, SetorApiError } from '../Apis/ApiSetor';
import { FilterSetor } from '../Filters/Setor';
import { ISetor } from '../Interfaces/interface.Setor';
import { SetorEmpty } from '../../Models/Setor';

export class SetorValidator {
  static validateSetor(setor: ISetor): { isValid: boolean; errors: string[] } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes
    
    return {
      isValid: errors.length === 0,
      errors
    };
  }
}

export interface ISetorService {
  fetchSetorById: (id: number) => Promise<ISetor>;
  saveSetor: (setor: ISetor) => Promise<ISetor>;  
  getList: (filtro?: FilterSetor) => Promise<ISetor[]>;
  getAll: (filtro?: FilterSetor) => Promise<ISetor[]>;
  deleteSetor: (id: number) => Promise<void>;
  validateSetor: (setor: ISetor) => { isValid: boolean; errors: string[] };
}

export class SetorService implements ISetorService {
  constructor(private api: SetorApi) {}

  async fetchSetorById(id: number): Promise<ISetor> {
    if (id <= 0) {
      throw new SetorApiError('ID inválido', 400, 'INVALID_ID');
    }

    try {
      
      const response = await this.api.getById(id);
      return response.data;

    } catch (error) {
      if (error instanceof SetorApiError) {
        throw error;
      }
      throw new SetorApiError('Erro ao buscar setor', 500, 'FETCH_ERROR', error);
    }
  }

  async saveSetor(setor: ISetor): Promise<ISetor> {    
    const validation = this.validateSetor(setor);
    if (!validation.isValid) {
      throw new SetorApiError(
        `Dados inválidos: ${validation.errors.join(', ')}`,
        400,
        'VALIDATION_ERROR'
      );
    }

    try {
      const response = await this.api.addAndUpdate(setor);
      return response.data;
    } catch (error) {
      if (error instanceof SetorApiError) {
        throw error;
      }
      throw new SetorApiError('Erro ao salvar setor', 500, 'SAVE_ERROR', error);
    }
  }

  
    async getList(filtro?: FilterSetor): Promise<ISetor[]> {
    try {
      const response = await this.api.getListN(CRUD_CONSTANTS.MAX_RECORDS_COMBO, filtro);
      return response.data || [];
    } catch (error) {
      console.log('Error fetching Setor list');
      return [];
    }
  }

 
  

   async getAll(
    filtro?: FilterSetor,
    onOnlineData?: (data: ISetor[]) => void
  ): Promise<ISetor[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(0, filtro ?? {});
      const offlineData = preloadResponse?.data || [];

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api.filter(0, filtro ?? {})
          .then(response => {
            if (response?.data) {
              onOnlineData(response.data);
            }
          })
          .catch(error => {
            console.log('Error fetching online Setor');
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(0, filtro ?? {});
          return onlineResponse?.data || offlineData;
        } catch (error) {
          console.log('Error fetching online Setor');
          return offlineData;
        }
      }
    } catch (error) {
      console.log('Error fetching all Setor:');
      return [];
    }
  }


  async deleteSetor(id: number): Promise<void> {
    if (id <= 0) {
      throw new SetorApiError('ID inválido para exclusão', 400, 'INVALID_ID');
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof SetorApiError) {
        throw error;
      }
      throw new SetorApiError('Erro ao excluir setor', 500, 'DELETE_ERROR', error);
    }
  }

  validateSetor(setor: ISetor): { isValid: boolean; errors: string[] } {
    return SetorValidator.validateSetor(setor);
  }
}