// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { AtividadesService, AtividadesValidator } from '@/app/GerAdv_TS/Atividades/Services/Atividades.service';
import { AtividadesApi, AtividadesApiError } from '@/app/GerAdv_TS/Atividades/Apis/ApiAtividades';
import { IAtividades } from '@/app/GerAdv_TS/Atividades/Interfaces/interface.Atividades';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as AtividadesApi;

const sample: IAtividades = { id: 1, nome: 'Teste' };

describe('AtividadesService', () => {
  let service: AtividadesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new AtividadesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(AtividadesValidator, 'validateAtividades').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchAtividadesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchAtividadesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchAtividadesById lança AtividadesApiError para id inválido', async () => {
    await expect(service.fetchAtividadesById(0)).rejects.toBeInstanceOf(AtividadesApiError);
  });

  it('saveAtividades chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveAtividades(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveAtividades lança AtividadesApiError quando validação falha', async () => {
    (AtividadesValidator.validateAtividades as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveAtividades(sample)).rejects.toBeInstanceOf(AtividadesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteAtividades chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteAtividades(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteAtividades lança AtividadesApiError para id inválido', async () => {
    await expect(service.deleteAtividades(0)).rejects.toBeInstanceOf(AtividadesApiError);
  });

  it('validateAtividades delega para AtividadesValidator', () => {
    (AtividadesValidator.validateAtividades as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateAtividades(sample);
    expect(r.isValid).toBe(true);
  });
});
