// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTPeriodicidadeWindowId from '@/app/GerAdv_TS/GUTPeriodicidade/Crud/Grids/GUTPeriodicidadeWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTPeriodicidadeWindowId from '@/app/GerAdv_TS/GUTPeriodicidade/Crud/Grids/GUTPeriodicidadeWindowId';
import { GUTPeriodicidadeEmpty } from '@/app/GerAdv_TS/Models/GUTPeriodicidade';

describe('GUTPeriodicidadeWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTPeriodicidadeWindowId).toBeDefined();
    expect(typeof GUTPeriodicidadeWindowId === 'function' || typeof GUTPeriodicidadeWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTPeriodicidadeService class so constructor usage doesn't throw and we can control fetchGUTPeriodicidadeById
const mockFetchGUTPeriodicidadeById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTPeriodicidade/Services/GUTPeriodicidade.service', () => ({
  GUTPeriodicidadeService: jest.fn().mockImplementation(() => ({
    fetchGUTPeriodicidadeById: (...args: any[]) => mockFetchGUTPeriodicidadeById(...args),
  })),
}));

// Mock the child GUTPeriodicidadeWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTPeriodicidade/Crud/Grids/GUTPeriodicidadeWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='gutperiodicidade-window' {...p} />,
}));

describe('GUTPeriodicidadeWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTPeriodicidadeWindowId).toBeDefined();
    expect(typeof GUTPeriodicidadeWindowId === 'function' || typeof GUTPeriodicidadeWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTPeriodicidadeWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTPeriodicidadeWindow with GUTPeriodicidadeEmpty when id is 0', async () => {
    render(
      <GUTPeriodicidadeWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTPeriodicidadeWindow should be rendered with selectedGUTPeriodicidade equal to GUTPeriodicidadeEmpty()
    const node = await screen.findByTestId('gutperiodicidade-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTPeriodicidade"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgutperiodicidade') || node.getAttribute('selectedGUTPeriodicidade')).not.toBeNull();
  });

  it('fetches gutperiodicidade by id and passes it to GUTPeriodicidadeWindow when id > 0', async () => {
    const fakeGUTPeriodicidade = { id: 123, nomecliente: 'Test' };
    mockFetchGUTPeriodicidadeById.mockResolvedValue(fakeGUTPeriodicidade);

    render(
      <GUTPeriodicidadeWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('gutperiodicidade-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTPeriodicidadeById).toHaveBeenCalledWith(123));
  });
});
