// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class OperadorGruposValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFOperadorGruposFactory> _mockOperadorGruposFactory;
    private readonly Mock<IOperadorGruposReader> _mockReader;
    private readonly OperadorGruposValidation _validation;
    private readonly Mock<IOperadorGruposService> _mockOperadorGruposService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly OperadorGruposService _service;
    private readonly string _validUri = "test-uri";
    public OperadorGruposValidationTests()
    {
        _mockOperadorGruposFactory = new Mock<IFOperadorGruposFactory>();
        _mockReader = new Mock<IOperadorGruposReader>();
        _validation = new OperadorGruposValidation();
        _mockOperadorGruposService = new Mock<IOperadorGruposService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void OperadorGruposValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IOperadorGruposValidation>();
    }

    [Fact]
    public void OperadorGruposValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(OperadorGruposValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void OperadorGruposValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(OperadorGruposValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.OperadorGrupos CreateValidOperadorGrupos()
    {
        return new Models.OperadorGrupos
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockOperadorGruposService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOperadorGrupos>(), It.IsAny<string>())).ReturnsAsync(new List<OperadorGruposResponseAll>());
    // Setup other mocks but don't override the OperadorGruposs service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockOperadorGruposService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOperadorGrupos>(), It.IsAny<string>())).ReturnsAsync(new List<OperadorGruposResponseAll>());
    // Setup other mocks but don't override the OperadorGruposs service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var operadorgrupos = new Models.OperadorGrupos
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(operadorgrupos, _mockOperadorGruposService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockOperadorGruposService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}