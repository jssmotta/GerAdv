// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ProProcuradoresWindowId from '@/app/GerAdv_TS/ProProcuradores/Crud/Grids/ProProcuradoresWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ProProcuradoresWindowId from '@/app/GerAdv_TS/ProProcuradores/Crud/Grids/ProProcuradoresWindowId';
import { ProProcuradoresEmpty } from '@/app/GerAdv_TS/Models/ProProcuradores';

describe('ProProcuradoresWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ProProcuradoresWindowId).toBeDefined();
    expect(typeof ProProcuradoresWindowId === 'function' || typeof ProProcuradoresWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ProProcuradoresService class so constructor usage doesn't throw and we can control fetchProProcuradoresById
const mockFetchProProcuradoresById = jest.fn();
jest.mock('@/app/GerAdv_TS/ProProcuradores/Services/ProProcuradores.service', () => ({
  ProProcuradoresService: jest.fn().mockImplementation(() => ({
    fetchProProcuradoresById: (...args: any[]) => mockFetchProProcuradoresById(...args),
  })),
}));

// Mock the child ProProcuradoresWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ProProcuradores/Crud/Grids/ProProcuradoresWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='proprocuradores-window' {...p} />,
}));

describe('ProProcuradoresWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ProProcuradoresWindowId).toBeDefined();
    expect(typeof ProProcuradoresWindowId === 'function' || typeof ProProcuradoresWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ProProcuradoresWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ProProcuradoresWindow with ProProcuradoresEmpty when id is 0', async () => {
    render(
      <ProProcuradoresWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ProProcuradoresWindow should be rendered with selectedProProcuradores equal to ProProcuradoresEmpty()
    const node = await screen.findByTestId('proprocuradores-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedProProcuradores"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedproprocuradores') || node.getAttribute('selectedProProcuradores')).not.toBeNull();
  });

  it('fetches proprocuradores by id and passes it to ProProcuradoresWindow when id > 0', async () => {
    const fakeProProcuradores = { id: 123, nomecliente: 'Test' };
    mockFetchProProcuradoresById.mockResolvedValue(fakeProProcuradores);

    render(
      <ProProcuradoresWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('proprocuradores-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchProProcuradoresById).toHaveBeenCalledWith(123));
  });
});
