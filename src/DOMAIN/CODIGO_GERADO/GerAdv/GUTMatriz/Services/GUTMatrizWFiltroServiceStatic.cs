// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

using MenphisSI.GerAdv.AI.Filters;

public partial class ServiceFilter
{
    public (string where, List<SqlParameter> parametros)? WFiltroGUTMatriz(Filters.FilterGUTMatriz? filtro)
    {
        if (filtro == null)
            return null;
        var parameters = new List<SqlParameter>();
        if (!string.IsNullOrWhiteSpace(filtro.Descricao))
        {
            parameters.Add(new($"@{(DBGUTMatrizDicInfo.Descricao)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Descricao)));
        }

        if (filtro.GUTTipo != int.MinValue)
        {
            parameters.Add(new($"@{(DBGUTMatrizDicInfo.GUTTipo)}", filtro.GUTTipo));
            if (filtro.GUTTipo_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBGUTMatrizDicInfo.GUTTipo)}_end", filtro.GUTTipo_end));
            }
        }

        if (filtro.Valor != int.MinValue)
        {
            parameters.Add(new($"@{(DBGUTMatrizDicInfo.Valor)}", filtro.Valor));
            if (filtro.Valor_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBGUTMatrizDicInfo.Valor)}_end", filtro.Valor_end));
            }
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{(DBGUTMatrizDicInfo.CampoCodigo)}", filtro.Codigo_filtro));
            if (filtro.Codigo_filtro_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBGUTMatrizDicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
            }
        }

        if (filtro.LogicalOperator.IsEmptyX() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var guttipo = this.WFiltroGUTTipo(filtro.FilterGUTTipo);
        if (guttipo?.parametros != null)
        {
            parameters.AddRange(guttipo?.parametros!);
        }

        var cWhere = new StringBuilder();
        cWhere.Append(guttipo?.where);
        cWhere.Append(filtro.Descricao.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.Descricao}]  {DevourerConsts.MsiCollate} like @{(DBGUTMatrizDicInfo.Descricao)}");
        if (!(filtro.GUTTipo.IsEmptyX()) && filtro.GUTTipo_end.IsEmptyX())
        {
            cWhere.Append(filtro.GUTTipo.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.GUTTipo}] = @{(DBGUTMatrizDicInfo.GUTTipo)}");
        }
        else if (!(filtro.GUTTipo.IsEmptyX()) && !(filtro.GUTTipo_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].{DBGUTMatrizDicInfo.GUTTipo} BETWEEN @{(DBGUTMatrizDicInfo.GUTTipo)} AND @{(DBGUTMatrizDicInfo.GUTTipo)}_end");
        }

        if (!(filtro.Valor.IsEmptyX()) && filtro.Valor_end.IsEmptyX())
        {
            cWhere.Append(filtro.Valor.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.Valor}] = @{(DBGUTMatrizDicInfo.Valor)}");
        }
        else if (!(filtro.Valor.IsEmptyX()) && !(filtro.Valor_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].{DBGUTMatrizDicInfo.Valor} BETWEEN @{(DBGUTMatrizDicInfo.Valor)} AND @{(DBGUTMatrizDicInfo.Valor)}_end");
        }

        if (!(filtro.Codigo_filtro.IsEmptyX()) && filtro.Codigo_filtro_end.IsEmptyX())
        {
            cWhere.Append(filtro.Codigo_filtro.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.CampoCodigo}] = @{(DBGUTMatrizDicInfo.CampoCodigo)}");
        }
        else if (!(filtro.Codigo_filtro.IsEmptyX()) && !(filtro.Codigo_filtro_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBGUTMatrizDicInfo.PTabelaNome}].{DBGUTMatrizDicInfo.CampoCodigo} BETWEEN @{(DBGUTMatrizDicInfo.CampoCodigo)} AND @{(DBGUTMatrizDicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }
}

public partial class GUTMatrizService
{
    /// <summary>
    /// Processa comando de voz para gerar filtro via OpenAI usando serviço genérico
    /// </summary>
    /// <param name = "message">Requisição com comando de voz</param>
    /// <param name = "existingFilter">Filtro existente para combinar</param>
    /// <param name = "uri">URI da base de dados</param>
    /// <returns>FilterGUTMatriz processado</returns>
    private async Task<Filters.FilterGUTMatriz?> ProcessFilterVoiceCommandAsync(CommandSpeakerRequest message, Filters.FilterGUTMatriz? existingFilter, string uri)
    {
        try
        {
            // Usar o contexto do FilterGUTMatrizTXT
            var context = FilterGUTMatrizTXT.GetPrompt(existingFilter, FilterGUTMatrizTXT.GetOptions());
            // Instruções específicas para GUTMatriz
            var additionalInstructions = "";
            // Processar com o serviço genérico
            var filtroProcessado = await _voiceFilterService.ProcessVoiceCommandWithFallbackAsync(message.Message, context, existingFilter, additionalInstructions, uri);
            return filtroProcessado;
        }
        catch (Exception)
        {
            // Se falhar, retornar filtro existente ou novo
            return existingFilter ?? new Filters.FilterGUTMatriz();
        }
    }

    private async Task<IEnumerable<GUTMatrizResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken cancellationToken)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            return[];
        }

        try
        {
            // Usar o reader para obter os dados
            return await reader.ListarAsync(max, uri, where.Replace(TSql.Where, ""), parameters, "", cancellationToken);
        }
        catch (Exception)
        {
            return[];
        }
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterGUTMatriz? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-1
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("GUTMatriz: URI inválida");
        }

        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new Exception($"Coneão nula.");
        }

        var filtroResult = filtro == null ? null : servicesFilter.WFiltroGUTMatriz(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter>? parameters = filtroResult?.parametros ?? [];
        var cacheKey = $"{uri}-GUTMatriz-{max}-{where.GetHashCode()}-{parameters.GetHashCode()}GetListN";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxGetListSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync<IEnumerable<NomeID>>(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter>? parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarNAsync(max, uri, where, parameters, DBGUTMatrizDicInfo.CampoNome);
        if (lista == null || !lista.Any())
        {
            return result;
        }

        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                continue;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    public async Task<IEnumerable<GUTMatrizResponseAll>> GetAll(int max, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        ThrowIfDisposed();
        max = Math.Min(Math.Max(max, 1), BaseConsts.PMaxItens);
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("GUTMatriz: URI inválida");
        }

        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache),
            LocalCacheExpiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache)
        };
        var cacheKey = $"{uri}-GUTMatriz-GetAll-{max}";
        var result = await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataAllAsync2(max, string.Empty, [], uri, cancel), entryOptions, cancellationToken: token);
        return result;
    }

    private async Task<IEnumerable<GUTMatrizResponseAll>> GetDataAllAsync2(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken token)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.ListarAsync(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}