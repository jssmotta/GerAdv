// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProcessOutputSourcesForm, useProcessOutputSourcesList, useValidationsProcessOutputSources } from '../GerAdv_TS/ProcessOutputSources/Hooks/hookProcessOutputSources';
import { IProcessOutputSources } from '../GerAdv_TS/ProcessOutputSources/Interfaces/interface.ProcessOutputSources';
import { IProcessOutputSourcesService } from '../GerAdv_TS/ProcessOutputSources/Services/ProcessOutputSources.service';
import { ProcessOutputSourcesTestEmpty } from '../GerAdv_TS/Models/ProcessOutputSources';
import { useProcessOutputSourcesComboBox } from '../GerAdv_TS/ProcessOutputSources/Hooks/hookProcessOutputSources';

// Mock do serviço
const mockProcessOutputSourcesService: jest.Mocked<IProcessOutputSourcesService> = {
  fetchProcessOutputSourcesById: jest.fn(),
  saveProcessOutputSources: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteProcessOutputSources: jest.fn(),
  validateProcessOutputSources: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProcessOutputSources: IProcessOutputSources = { ...ProcessOutputSourcesTestEmpty() };

describe('useProcessOutputSourcesForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    expect(result.current.data).toEqual(initialProcessOutputSources);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Process Output Sources',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Process Output Sources');
  });

   test('deve carregar Process Output Sources por ID', async () => {
    const mockProcessOutputSources = { ...initialProcessOutputSources, id: 1, nome: 'Process Output Sources Teste' };
    mockProcessOutputSourcesService.fetchProcessOutputSourcesById.mockResolvedValue(mockProcessOutputSources);

    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.loadProcessOutputSources(1);
    });

    expect(mockProcessOutputSourcesService.fetchProcessOutputSourcesById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProcessOutputSources);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Process Output Sources', async () => {
    const errorMessage = 'Erro ao carregar Process Output Sources';
    mockProcessOutputSourcesService.fetchProcessOutputSourcesById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.loadProcessOutputSources(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProcessOutputSources, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProcessOutputSources);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesForm(initialProcessOutputSources, mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.loadProcessOutputSources(0);
    });

    expect(mockProcessOutputSourcesService.fetchProcessOutputSourcesById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProcessOutputSources);
  });
});

describe('useProcessOutputSourcesList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesList(mockProcessOutputSourcesService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProcessOutputSources, id: 1, nome: 'Process Output Sources 1' },
      { ...initialProcessOutputSources, id: 2, nome: 'Process Output Sources 2' }
    ];
    mockProcessOutputSourcesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProcessOutputSourcesList(mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProcessOutputSourcesService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProcessOutputSourcesService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProcessOutputSourcesList(mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProcessOutputSources, id: 1, nome: 'Process Output Sources Filtrado' }];
    const filtro = { nome: 'Process Output Sources' };
    mockProcessOutputSourcesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProcessOutputSourcesList(mockProcessOutputSourcesService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProcessOutputSourcesService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProcessOutputSources', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProcessOutputSources());

    const validData = { ...initialProcessOutputSources, nome: 'Process Output Sources Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsProcessOutputSources());

    const invalidData = { ...initialProcessOutputSources, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsProcessOutputSources());

    const invalidData = { 
      ...initialProcessOutputSources, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProcessOutputSources());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProcessOutputSources, id: 1, nome: 'Process Output Sources Teste' }];
    mockProcessOutputSourcesService.getAll.mockResolvedValue(mockData);
    mockProcessOutputSourcesService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProcessOutputSourcesList(mockProcessOutputSourcesService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsProcessOutputSources()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Process Output Sources Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Process Output Sources Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Process Output Sources 1' },
      { id: 2, nome: 'Process Output Sources 2' }
    ];
    mockProcessOutputSourcesService.getList.mockResolvedValue(mockOptions as IProcessOutputSources[]);


    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Process Output Sources 1' },
        { id: 2, nome: 'Process Output Sources 2' }
      ]);
    });


    expect(mockProcessOutputSourcesService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Process Output Sources ABC' },
      { id: 2, nome: 'Process Output Sources XYZ' }
    ];
    mockProcessOutputSourcesService.getList.mockResolvedValue(mockOptions as IProcessOutputSources[]);   


 const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Process Output Sources ABC' },
        { id: 2, nome: 'Process Output Sources XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Process Output Sources ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Process Output Sources ABC' },
      { id: 2, nome: 'Process Output Sources XYZ' }
    ];
    mockProcessOutputSourcesService.getList.mockResolvedValue(mockOptions as IProcessOutputSources[]);
  


    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Process Output Sources ABC' },
        { id: 2, nome: 'Process Output Sources XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Process Output Sources ABC' },
          {id: 2, nome: 'Process Output Sources XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );

    const newValue = { id: 1, nome: 'Process Output Sources Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Process Output Sources Inicial' };
    
    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useProcessOutputSourcesComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Process Output Sources Inicial' };
    
    const { result } = renderHook(() => 
      useProcessOutputSourcesComboBox(mockProcessOutputSourcesService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






