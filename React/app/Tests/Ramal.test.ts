// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useRamalForm, useRamalList, useValidationsRamal } from '../GerAdv_TS/Ramal/Hooks/hookRamal';
import { IRamal } from '../GerAdv_TS/Ramal/Interfaces/interface.Ramal';
import { IRamalService } from '../GerAdv_TS/Ramal/Services/Ramal.service';
import { RamalTestEmpty } from '../GerAdv_TS/Models/Ramal';
import { useRamalComboBox } from '../GerAdv_TS/Ramal/Hooks/hookRamal';

// Mock do serviço
const mockRamalService: jest.Mocked<IRamalService> = {
  fetchRamalById: jest.fn(),
  saveRamal: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteRamal: jest.fn(),
  validateRamal: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialRamal: IRamal = { ...RamalTestEmpty() };

describe('useRamalForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    expect(result.current.data).toEqual(initialRamal);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Ramal',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Ramal');
  });

   test('deve carregar Ramal por ID', async () => {
    const mockRamal = { ...initialRamal, id: 1, nome: 'Ramal Teste' };
    mockRamalService.fetchRamalById.mockResolvedValue(mockRamal);

    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    await act(async () => {
      await result.current.loadRamal(1);
    });

    expect(mockRamalService.fetchRamalById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockRamal);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Ramal', async () => {
    const errorMessage = 'Erro ao carregar Ramal';
    mockRamalService.fetchRamalById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    await act(async () => {
      await result.current.loadRamal(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialRamal, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialRamal);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useRamalForm(initialRamal, mockRamalService)
    );

    await act(async () => {
      await result.current.loadRamal(0);
    });

    expect(mockRamalService.fetchRamalById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialRamal);
  });
});

describe('useRamalList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useRamalList(mockRamalService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialRamal, id: 1, nome: 'Ramal 1' },
      { ...initialRamal, id: 2, nome: 'Ramal 2' }
    ];
    mockRamalService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRamalList(mockRamalService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockRamalService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockRamalService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRamalList(mockRamalService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialRamal, id: 1, nome: 'Ramal Filtrado' }];
    const filtro = { nome: 'Ramal' };
    mockRamalService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRamalList(mockRamalService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockRamalService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsRamal', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsRamal());

    const validData = { ...initialRamal, nome: 'Ramal Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsRamal());

    const invalidData = { ...initialRamal, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsRamal());

    const invalidData = { 
      ...initialRamal, 
      nome: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsRamal());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialRamal, id: 1, nome: 'Ramal Teste' }];
    mockRamalService.getAll.mockResolvedValue(mockData);
    mockRamalService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useRamalList(mockRamalService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsRamal()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Ramal Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Ramal Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Ramal 1' },
      { id: 2, nome: 'Ramal 2' }
    ];
    mockRamalService.getList.mockResolvedValue(mockOptions as IRamal[]);


    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Ramal 1' },
        { id: 2, nome: 'Ramal 2' }
      ]);
    });


    expect(mockRamalService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Ramal ABC' },
      { id: 2, nome: 'Ramal XYZ' }
    ];
    mockRamalService.getList.mockResolvedValue(mockOptions as IRamal[]);   


 const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Ramal ABC' },
        { id: 2, nome: 'Ramal XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Ramal ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Ramal ABC' },
      { id: 2, nome: 'Ramal XYZ' }
    ];
    mockRamalService.getList.mockResolvedValue(mockOptions as IRamal[]);
  


    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Ramal ABC' },
        { id: 2, nome: 'Ramal XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Ramal ABC' },
          {id: 2, nome: 'Ramal XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );

    const newValue = { id: 1, nome: 'Ramal Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Ramal Inicial' };
    
    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useRamalComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Ramal Inicial' };
    
    const { result } = renderHook(() => 
      useRamalComboBox(mockRamalService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






