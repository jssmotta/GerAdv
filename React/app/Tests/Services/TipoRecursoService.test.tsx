// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoRecursoService, TipoRecursoValidator } from '@/app/GerAdv_TS/TipoRecurso/Services/TipoRecurso.service';
import { TipoRecursoApi, TipoRecursoApiError } from '@/app/GerAdv_TS/TipoRecurso/Apis/ApiTipoRecurso';
import { ITipoRecurso } from '@/app/GerAdv_TS/TipoRecurso/Interfaces/interface.TipoRecurso';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoRecursoApi;

const sample: ITipoRecurso = { id: 1, nome: 'Teste' };

describe('TipoRecursoService', () => {
  let service: TipoRecursoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoRecursoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoRecursoValidator, 'validateTipoRecurso').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoRecursoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoRecursoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoRecursoById lança TipoRecursoApiError para id inválido', async () => {
    await expect(service.fetchTipoRecursoById(0)).rejects.toBeInstanceOf(TipoRecursoApiError);
  });

  it('saveTipoRecurso chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoRecurso(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoRecurso lança TipoRecursoApiError quando validação falha', async () => {
    (TipoRecursoValidator.validateTipoRecurso as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoRecurso(sample)).rejects.toBeInstanceOf(TipoRecursoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoRecurso chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoRecurso(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoRecurso lança TipoRecursoApiError para id inválido', async () => {
    await expect(service.deleteTipoRecurso(0)).rejects.toBeInstanceOf(TipoRecursoApiError);
  });

  it('validateTipoRecurso delega para TipoRecursoValidator', () => {
    (TipoRecursoValidator.validateTipoRecurso as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoRecurso(sample);
    expect(r.isValid).toBe(true);
  });
});
