// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ModelosDocumentos
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterModelosDocumentosTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithNome_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Nome = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nome\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithRemuneracao_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Remuneracao = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"remuneracao\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithAssinatura_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Assinatura = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"assinatura\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithHeader_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Header = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"header\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithFooter_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Footer = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"footer\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithExtra1_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Extra1 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"extra1\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithExtra2_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Extra2 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"extra2\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithExtra3_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Extra3 = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"extra3\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithOutorgante_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Outorgante = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"outorgante\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithOutorgados_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Outorgados = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"outorgados\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithPoderes_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Poderes = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"poderes\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithObjeto_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Objeto = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"objeto\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithTitulo_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Titulo = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"titulo\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithTestemunhas_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Testemunhas = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"testemunhas\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithTipoModeloDocumento_SerializesNumericValue()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            TipoModeloDocumento = 123
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"tipomodelodocumento\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCSS_SerializesCorrectly()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            CSS = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"css\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterTipoModeloDocumento\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(new FilterModelosDocumentos(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(new FilterModelosDocumentos(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(new FilterModelosDocumentos(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(new FilterModelosDocumentos(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterModelosDocumentosTXT.GetOptions();
        var filter = new FilterModelosDocumentos
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterModelosDocumentosTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}