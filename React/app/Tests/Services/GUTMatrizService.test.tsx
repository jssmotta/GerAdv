// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTMatrizService, GUTMatrizValidator } from '@/app/GerAdv_TS/GUTMatriz/Services/GUTMatriz.service';
import { GUTMatrizApi, GUTMatrizApiError } from '@/app/GerAdv_TS/GUTMatriz/Apis/ApiGUTMatriz';
import { IGUTMatriz } from '@/app/GerAdv_TS/GUTMatriz/Interfaces/interface.GUTMatriz';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTMatrizApi;

const sample: IGUTMatriz = { id: 1, nome: 'Teste' };

describe('GUTMatrizService', () => {
  let service: GUTMatrizService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTMatrizService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTMatrizValidator, 'validateGUTMatriz').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTMatrizById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTMatrizById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTMatrizById lança GUTMatrizApiError para id inválido', async () => {
    await expect(service.fetchGUTMatrizById(0)).rejects.toBeInstanceOf(GUTMatrizApiError);
  });

  it('saveGUTMatriz chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTMatriz(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTMatriz lança GUTMatrizApiError quando validação falha', async () => {
    (GUTMatrizValidator.validateGUTMatriz as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTMatriz(sample)).rejects.toBeInstanceOf(GUTMatrizApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTMatriz chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTMatriz(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTMatriz lança GUTMatrizApiError para id inválido', async () => {
    await expect(service.deleteGUTMatriz(0)).rejects.toBeInstanceOf(GUTMatrizApiError);
  });

  it('validateGUTMatriz delega para GUTMatrizValidator', () => {
    (GUTMatrizValidator.validateGUTMatriz as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTMatriz(sample);
    expect(r.isValid).toBe(true);
  });
});
