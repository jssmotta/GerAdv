// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { OperadorEMailPopupEmpty } from '../../../Models/OperadorEMailPopup';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IOperadorEMailPopup } from '../../Interfaces/interface.OperadorEMailPopup';
import { OperadorEMailPopupService } from '../../Services/OperadorEMailPopup.service';
import { OperadorEMailPopupApi } from '../../Apis/ApiOperadorEMailPopup';
import { OperadorEMailPopupGridMobileComponent } from '../GridsMobile/OperadorEMailPopupMobile';
import { OperadorEMailPopupGridDesktopComponent } from '../GridsDesktop/OperadorEMailPopupDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterOperadorEMailPopup } from '../../Filters/OperadorEMailPopup';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import OperadorEMailPopupWindow from './OperadorEMailPopupWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useOperadorEMailPopupList } from '../../Hooks/hookOperadorEMailPopup';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useOperadorEMailPopupFilter } from '../../Hooks/hookOperadorEMailPopupFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const OperadorEMailPopupGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedOperadorEMailPopup, setSelectedOperadorEMailPopup] = useState<IOperadorEMailPopup>(OperadorEMailPopupEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterOperadorEMailPopup | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [operadoremailpopupData, setOperadorEMailPopupData] = useState<IOperadorEMailPopup[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const operadoremailpopupService = useMemo(() => {
    return new OperadorEMailPopupService(
    new OperadorEMailPopupApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: operadoremailpopup, 
  fetchData, 
  refreshData
} = useOperadorEMailPopupList(operadoremailpopupService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterOperadorEMailPopup | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('OperadorEMailPopupFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await operadoremailpopupService.getAll(
  filtro ?? {} as FilterOperadorEMailPopup, 
  onlineData => {
    setOperadorEMailPopupData(onlineData);
    setLoading(false);
  }
);
setOperadorEMailPopupData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterOperadorEMailPopup;

    const filtroMem = sessionStorage.getItem(btoa('OperadorEMailPopupFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('OperadorEMailPopupFilter')) ?? '') || '{}') : {} as FilterOperadorEMailPopup;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (operadoremailpopup: IOperadorEMailPopup) => {
    setSelectedOperadorEMailPopup(operadoremailpopup);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedOperadorEMailPopup(OperadorEMailPopupEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const operadoremailpopup = e.dataItem;
    setDeleteId(operadoremailpopup.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await operadoremailpopupService.deleteOperadorEMailPopup(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useOperadorEMailPopupFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <OperadorEMailPopupGridMobileComponent
        data={operadoremailpopupData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <OperadorEMailPopupGridDesktopComponent
        data={operadoremailpopupData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <OperadorEMailPopupWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedOperadorEMailPopup={selectedOperadorEMailPopup}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Operador E Mail Popup'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default OperadorEMailPopupGrid;