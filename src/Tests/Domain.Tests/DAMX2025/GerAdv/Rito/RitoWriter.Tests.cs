// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class RitoWriterTests
{
    private readonly Mock<IFRitoFactory> _mockRitoFactory;
    private readonly Mock<FRito> _mockFRito;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly RitoWriter _ritoWriter;
    public RitoWriterTests()
    {
        _mockRitoFactory = new Mock<IFRitoFactory>();
        _mockFRito = new Mock<FRito>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _ritoWriter = new RitoWriter(_mockRitoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var rito = CreateValidRitoModel();
        rito.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockRitoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFRito.Object);
        _mockFRito.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _ritoWriter.WriteAsync(rito, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFRito.Object);
        _mockRitoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockRitoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFRito.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var rito = CreateValidRitoModel();
        rito.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockRitoFactory.Setup(x => x.CreateFromIdAsync(rito.Id, _mockConnection.Object)).ReturnsAsync(_mockFRito.Object);
        _mockFRito.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _ritoWriter.WriteAsync(rito, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFRito.Object);
        _mockRitoFactory.Verify(x => x.CreateFromIdAsync(rito.Id, _mockConnection.Object), Times.Once);
        _mockRitoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFRito.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var rito = CreateValidRitoModel();
        var auditorQuem = 99;
        _mockRitoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFRito.Object);
        _mockFRito.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _ritoWriter.WriteAsync(rito, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFRito.Object);
        _mockFRito.VerifySet(x => x.FDescricao = rito.Descricao, Times.Once);
        _mockFRito.VerifySet(x => x.FGUID = rito.GUID, Times.Once);
        _mockFRito.VerifySet(x => x.FTop = rito.Top, Times.Once);
        _mockFRito.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var rito = CreateValidRitoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockRitoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _ritoWriter.WriteAsync(rito, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var rito = CreateValidRitoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockRitoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFRito.Object);
        _mockFRito.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _ritoWriter.WriteAsync(rito, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var ritoResponse = new RitoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockRitoFactory.Setup(x => x.DeleteAsync(operadorId, ritoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _ritoWriter.Delete(ritoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockRitoFactory.Verify(x => x.DeleteAsync(operadorId, ritoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var ritoResponse = new RitoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockRitoFactory.Setup(x => x.DeleteAsync(operadorId, ritoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _ritoWriter.Delete(ritoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var ritoResponse = new RitoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockRitoFactory.Setup(x => x.DeleteAsync(operadorId, ritoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _ritoWriter.Delete(ritoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new RitoWriter(_mockRitoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new RitoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Rito CreateValidRitoModel()
    {
        return new Models.Rito
        {
            Id = 0,
            Descricao = "AAAAAAAAAAAAAAAAAA",
            GUID = Guid.NewGuid().ToString(),
            Top = false
        };
    }
#endregion
}