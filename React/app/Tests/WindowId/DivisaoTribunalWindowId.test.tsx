// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import DivisaoTribunalWindowId from '@/app/GerAdv_TS/DivisaoTribunal/Crud/Grids/DivisaoTribunalWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import DivisaoTribunalWindowId from '@/app/GerAdv_TS/DivisaoTribunal/Crud/Grids/DivisaoTribunalWindowId';
import { DivisaoTribunalEmpty } from '@/app/GerAdv_TS/Models/DivisaoTribunal';

describe('DivisaoTribunalWindowId (basic)', () => {
  it('exports the component', () => {
    expect(DivisaoTribunalWindowId).toBeDefined();
    expect(typeof DivisaoTribunalWindowId === 'function' || typeof DivisaoTribunalWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the DivisaoTribunalService class so constructor usage doesn't throw and we can control fetchDivisaoTribunalById
const mockFetchDivisaoTribunalById = jest.fn();
jest.mock('@/app/GerAdv_TS/DivisaoTribunal/Services/DivisaoTribunal.service', () => ({
  DivisaoTribunalService: jest.fn().mockImplementation(() => ({
    fetchDivisaoTribunalById: (...args: any[]) => mockFetchDivisaoTribunalById(...args),
  })),
}));

// Mock the child DivisaoTribunalWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/DivisaoTribunal/Crud/Grids/DivisaoTribunalWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='divisaotribunal-window' {...p} />,
}));

describe('DivisaoTribunalWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(DivisaoTribunalWindowId).toBeDefined();
    expect(typeof DivisaoTribunalWindowId === 'function' || typeof DivisaoTribunalWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <DivisaoTribunalWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders DivisaoTribunalWindow with DivisaoTribunalEmpty when id is 0', async () => {
    render(
      <DivisaoTribunalWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // DivisaoTribunalWindow should be rendered with selectedDivisaoTribunal equal to DivisaoTribunalEmpty()
    const node = await screen.findByTestId('divisaotribunal-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedDivisaoTribunal"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selecteddivisaotribunal') || node.getAttribute('selectedDivisaoTribunal')).not.toBeNull();
  });

  it('fetches divisaotribunal by id and passes it to DivisaoTribunalWindow when id > 0', async () => {
    const fakeDivisaoTribunal = { id: 123, nomecliente: 'Test' };
    mockFetchDivisaoTribunalById.mockResolvedValue(fakeDivisaoTribunal);

    render(
      <DivisaoTribunalWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('divisaotribunal-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchDivisaoTribunalById).toHaveBeenCalledWith(123));
  });
});
