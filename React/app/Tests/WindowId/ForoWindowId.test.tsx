// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ForoWindowId from '@/app/GerAdv_TS/Foro/Crud/Grids/ForoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ForoWindowId from '@/app/GerAdv_TS/Foro/Crud/Grids/ForoWindowId';
import { ForoEmpty } from '@/app/GerAdv_TS/Models/Foro';

describe('ForoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ForoWindowId).toBeDefined();
    expect(typeof ForoWindowId === 'function' || typeof ForoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ForoService class so constructor usage doesn't throw and we can control fetchForoById
const mockFetchForoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Foro/Services/Foro.service', () => ({
  ForoService: jest.fn().mockImplementation(() => ({
    fetchForoById: (...args: any[]) => mockFetchForoById(...args),
  })),
}));

// Mock the child ForoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Foro/Crud/Grids/ForoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='foro-window' {...p} />,
}));

describe('ForoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ForoWindowId).toBeDefined();
    expect(typeof ForoWindowId === 'function' || typeof ForoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ForoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ForoWindow with ForoEmpty when id is 0', async () => {
    render(
      <ForoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ForoWindow should be rendered with selectedForo equal to ForoEmpty()
    const node = await screen.findByTestId('foro-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedForo"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedforo') || node.getAttribute('selectedForo')).not.toBeNull();
  });

  it('fetches foro by id and passes it to ForoWindow when id > 0', async () => {
    const fakeForo = { id: 123, nomecliente: 'Test' };
    mockFetchForoById.mockResolvedValue(fakeForo);

    render(
      <ForoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('foro-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchForoById).toHaveBeenCalledWith(123));
  });
});
