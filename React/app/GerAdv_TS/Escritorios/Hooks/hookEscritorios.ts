// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IEscritoriosService } from '../Services/Escritorios.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IEscritorios } from '../Interfaces/interface.Escritorios';
import { FilterEscritorios } from '../Filters/Escritorios';
import { isValidDate } from '@/app/tools/datetime';
import { EscritoriosApi } from '../Apis/ApiEscritorios';

export const useEscritoriosForm = (
  initialEscritorios: IEscritorios,
  dataService: IEscritoriosService
) => {
  const [data, setData] = useState<IEscritorios>(initialEscritorios);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadEscritorios = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialEscritorios);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchEscritoriosById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Escritorios';
      setError(errorMessage);
      //console.log('Erro ao carregar Escritorios');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialEscritorios]);

  const resetForm = useCallback(() => {
    setData(initialEscritorios);
    setError(null);
  }, [initialEscritorios]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadEscritorios,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadEscritorios, resetForm]);
  return returnValue;
};


export const useEscritoriosNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('Escritorios', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useEscritoriosList = (dataService: IEscritoriosService, currentFilter?: FilterEscritorios | undefined | null) => {
  const [data, setData] = useState<IEscritorios[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterEscritorios | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterEscritorios | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterEscritorios);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar escritorios';
      setError(errorMessage);
      //console.log('Erro ao carregar escritorios');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useEscritoriosNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsEscritorios() {

  async function runValidation(data: IEscritorios, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const escritoriosApi = new EscritoriosApi(uri ?? '', token ?? '');

    const result = await escritoriosApi.validation(data);

    return result;
  }

  function validate(data: IEscritorios): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.nome.length <= 0) { 
                                             return { isValid: false, message: 'O campo Nome não pode ficar vazio.' };
                                         } 
if (data.nome.length > 50) { 
                                             return { isValid: false, message: 'O campo Nome não pode ter mais de 50 caracteres.' };
                                         } 
if (data.oab.length > 15) { 
                                             return { isValid: false, message: 'O campo OAB não pode ter mais de 15 caracteres.' };
                                         } 
if (data.endereco.length > 50) { 
                                             return { isValid: false, message: 'O campo Endereco não pode ter mais de 50 caracteres.' };
                                         } 
if (data.bairro.length > 30) { 
                                             return { isValid: false, message: 'O campo Bairro não pode ter mais de 30 caracteres.' };
                                         } 
if (data.cep.length > 10) { 
                                             return { isValid: false, message: 'O campo CEP não pode ter mais de 10 caracteres.' };
                                         } 
if (data.fone.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Fone não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.fax.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Fax não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.site.length > 200) { 
                                             return { isValid: false, message: 'O campo Site não pode ter mais de 200 caracteres.' };
                                         } 
if (data.email.length > 100) { 
                                             return { isValid: false, message: 'O campo EMail não pode ter mais de 100 caracteres.' };
                                         } 
if (data.obs.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo OBS não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.advresponsavel.length > 80) { 
                                             return { isValid: false, message: 'O campo AdvResponsavel não pode ter mais de 80 caracteres.' };
                                         } 
if (data.secretaria.length > 80) { 
                                             return { isValid: false, message: 'O campo Secretaria não pode ter mais de 80 caracteres.' };
                                         } 
if (data.inscest.length > 15) { 
                                             return { isValid: false, message: 'O campo InscEst não pode ter mais de 15 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}export const useEscritoriosComboBox = (
  dataService: IEscritoriosService,
  initialValue?: any
) => {
  const [options, setOptions] = useState<any[]>([]);
  const [filteredOptions, setFilteredOptions] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [selectedValue, setSelectedValue] = useState(initialValue);
  const [hasLoaded, setHasLoaded] = useState(false);

  const fetchOptions = useCallback(async () => {
    if (loading) return; // Evita múltiplas requisições simultâneas
    
    setLoading(true);
    try {
      const response = await dataService.getList();
      const mappedOptions = response.map(item => ({
        id: item.id,
        nome: item.nome
      }));
      setOptions(mappedOptions);
      setFilteredOptions(mappedOptions);
      setHasLoaded(true);
    } catch (err) {
      //console.log('Erro ao buscar opções do ComboBox');
    } finally {
      setLoading(false);
    }
  }, [dataService, loading]);

  const handleFilter = useCallback((filterText: string) => {
    if (!filterText) {
      setFilteredOptions(options);
      return;
    }
    
    const filter = filterText.toLowerCase();
    const filtered = options.filter(option =>
      option.nome.toLowerCase().includes(filter)
    );
    setFilteredOptions(filtered);
  }, [options]);

  const handleValueChange = useCallback((newValue: any) => {
    setSelectedValue(newValue);
  }, []);
  
  useEffect(() => {
    if (!hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  const refreshCallback = useCallback(() => {
    if (hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  useEscritoriosNotifications(
    refreshCallback, // onUpdate
    refreshCallback, // onDelete
    refreshCallback  // onAdd
  );

  return {
    options: filteredOptions,
    loading,
    selectedValue,
    handleFilter,
    handleValueChange,
    refreshOptions: fetchOptions,
    clearValue: () => setSelectedValue(null)
  };
};