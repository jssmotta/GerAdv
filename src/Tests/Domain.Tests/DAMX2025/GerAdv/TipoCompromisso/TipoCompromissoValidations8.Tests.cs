// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoCompromissoValidationTests : IDisposable
{
    private readonly TipoCompromissoValidation _validation;
    private readonly Mock<ITipoCompromissoService> _mockTipoCompromissoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoCompromissoService _service;
    private readonly string _validUri = "test-uri";
    public TipoCompromissoValidationTests()
    {
        _validation = new TipoCompromissoValidation();
        _mockTipoCompromissoService = new Mock<ITipoCompromissoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoCompromissoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoCompromissoValidation>();
    }

    [Fact]
    public void TipoCompromissoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoCompromissoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoCompromissoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoCompromissoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.TipoCompromisso CreateValidTipoCompromisso()
    {
        return new Models.TipoCompromisso
        {
            Id = 1,
            Icone = 0,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Financeiro = false
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoCompromissoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoCompromisso>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoCompromissos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoCompromissoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoCompromisso>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoCompromissos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipocompromisso = new Models.TipoCompromisso
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipocompromisso, _mockTipoCompromissoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoCompromissoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}