// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useLivroCaixaForm, useLivroCaixaList, useValidationsLivroCaixa } from '../GerAdv_TS/LivroCaixa/Hooks/hookLivroCaixa';
import { ILivroCaixa } from '../GerAdv_TS/LivroCaixa/Interfaces/interface.LivroCaixa';
import { ILivroCaixaService } from '../GerAdv_TS/LivroCaixa/Services/LivroCaixa.service';
import { LivroCaixaTestEmpty } from '../GerAdv_TS/Models/LivroCaixa';


// Mock do serviço
const mockLivroCaixaService: jest.Mocked<ILivroCaixaService> = {
  fetchLivroCaixaById: jest.fn(),
  saveLivroCaixa: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteLivroCaixa: jest.fn(),
  validateLivroCaixa: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialLivroCaixa: ILivroCaixa = { ...LivroCaixaTestEmpty() };

describe('useLivroCaixaForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    expect(result.current.data).toEqual(initialLivroCaixa);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    const mockEvent = {
      target: {
        name: 'data',
        value: 'Novo Livro Caixa',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.data).toBe('Novo Livro Caixa');
  });

   test('deve carregar Livro Caixa por ID', async () => {
    const mockLivroCaixa = { ...initialLivroCaixa, id: 1, data: 'Livro Caixa Teste' };
    mockLivroCaixaService.fetchLivroCaixaById.mockResolvedValue(mockLivroCaixa);

    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.loadLivroCaixa(1);
    });

    expect(mockLivroCaixaService.fetchLivroCaixaById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockLivroCaixa);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Livro Caixa', async () => {
    const errorMessage = 'Erro ao carregar Livro Caixa';
    mockLivroCaixaService.fetchLivroCaixaById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.loadLivroCaixa(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialLivroCaixa, data: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialLivroCaixa);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.loadLivroCaixa(0);
    });

    expect(mockLivroCaixaService.fetchLivroCaixaById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialLivroCaixa);
  });
});

describe('useLivroCaixaList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useLivroCaixaList(mockLivroCaixaService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialLivroCaixa, id: 1, data: 'Livro Caixa 1' },
      { ...initialLivroCaixa, id: 2, data: 'Livro Caixa 2' }
    ];
    mockLivroCaixaService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useLivroCaixaList(mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockLivroCaixaService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockLivroCaixaService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useLivroCaixaList(mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialLivroCaixa, id: 1, data: 'Livro Caixa Filtrado' }];
    const filtro = { data: 'Livro Caixa' };
    mockLivroCaixaService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useLivroCaixaList(mockLivroCaixaService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockLivroCaixaService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsLivroCaixa', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsLivroCaixa());

    const validData = { ...initialLivroCaixa, data: 'Livro Caixa Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar data vazio', () => {
    const { result } = renderHook(() => useValidationsLivroCaixa());

    const invalidData = { ...initialLivroCaixa, data: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ficar vazio.');
  });

  
  test('deve invalidar data muito longo', () => {
    const { result } = renderHook(() => useValidationsLivroCaixa());

    const invalidData = { 
      ...initialLivroCaixa, 
      data: 'a'.repeat(-1+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ter mais de -1 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsLivroCaixa());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialLivroCaixa, id: 1, data: 'Livro Caixa Teste' }];
    mockLivroCaixaService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useLivroCaixaList(mockLivroCaixaService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsLivroCaixa()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useLivroCaixaForm(initialLivroCaixa, mockLivroCaixaService)
    );

    const mockEvent = {
      target: {
        name: 'ajuste',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.ajuste).toBe(true);
  });


