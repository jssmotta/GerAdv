// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterAgenda2Agenda } from '@/app/GerAdv_TS/Agenda2Agenda/Filters/Agenda2Agenda';
import AgendaComboBox from '@/app/GerAdv_TS/Agenda/ComboBox/Agenda';
interface UseAgenda2AgendaFilterProps {
  handleFetchWithFilter: (filtro?: FilterAgenda2Agenda | undefined | null) => Promise<void>;
}
interface UseAgenda2AgendaFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterAgenda2Agenda;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterAgenda2Agenda>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterAgenda2Agenda) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterAgenda2Agenda>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useAgenda2AgendaFilter = ({ handleFetchWithFilter }: UseAgenda2AgendaFilterProps): UseAgenda2AgendaFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterAgenda2Agenda>({} as FilterAgenda2Agenda);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterAgenda2Agenda;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterAgenda2Agenda) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('Agenda2AgendaFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterAgenda2Agenda>) => (
  <>
  <InputInput
  type='text'
  id='master'
  name='master'
  value={handlers.windowFilter?.master ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Master'
  label='Master (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='master_end'
  name='master_end'
  value={handlers.windowFilter?.master_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Master final'
  label='Master final'
  disabled={handlers.windowFilter?.master ? false: true}
  className='inputIncNome inputSearch'
  />
  <AgendaComboBox
  name='agenda'
  dataForm={null}
  value={handlers.windowFilter?.agenda}
  setValue={(e:any) => handlers.handleComboChange(e, 'agenda')}
  className='inputSearch inputSearchComboboxTab'
  label='Compromisso'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};