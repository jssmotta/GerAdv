// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { OperadorGruposAgendaEmpty } from '../../../Models/OperadorGruposAgenda';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IOperadorGruposAgenda } from '../../Interfaces/interface.OperadorGruposAgenda';
import { OperadorGruposAgendaService } from '../../Services/OperadorGruposAgenda.service';
import { OperadorGruposAgendaApi } from '../../Apis/ApiOperadorGruposAgenda';
import { OperadorGruposAgendaGridMobileComponent } from '../GridsMobile/OperadorGruposAgendaMobile';
import { OperadorGruposAgendaGridDesktopComponent } from '../GridsDesktop/OperadorGruposAgendaDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterOperadorGruposAgenda } from '../../Filters/OperadorGruposAgenda';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import OperadorGruposAgendaWindow from './OperadorGruposAgendaWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useOperadorGruposAgendaList } from '../../Hooks/hookOperadorGruposAgenda';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useOperadorGruposAgendaFilter } from '../../Hooks/hookOperadorGruposAgendaFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const OperadorGruposAgendaGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedOperadorGruposAgenda, setSelectedOperadorGruposAgenda] = useState<IOperadorGruposAgenda>(OperadorGruposAgendaEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterOperadorGruposAgenda | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [operadorgruposagendaData, setOperadorGruposAgendaData] = useState<IOperadorGruposAgenda[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const operadorgruposagendaService = useMemo(() => {
    return new OperadorGruposAgendaService(
    new OperadorGruposAgendaApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: operadorgruposagenda, 
  fetchData, 
  refreshData
} = useOperadorGruposAgendaList(operadorgruposagendaService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterOperadorGruposAgenda | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('OperadorGruposAgendaFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await operadorgruposagendaService.getAll(
  filtro ?? {} as FilterOperadorGruposAgenda, 
  onlineData => {
    setOperadorGruposAgendaData(onlineData);
    setLoading(false);
  }
);
setOperadorGruposAgendaData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterOperadorGruposAgenda;

    const filtroMem = sessionStorage.getItem(btoa('OperadorGruposAgendaFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('OperadorGruposAgendaFilter')) ?? '') || '{}') : {} as FilterOperadorGruposAgenda;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (operadorgruposagenda: IOperadorGruposAgenda) => {
    setSelectedOperadorGruposAgenda(operadorgruposagenda);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedOperadorGruposAgenda(OperadorGruposAgendaEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const operadorgruposagenda = e.dataItem;
    setDeleteId(operadorgruposagenda.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await operadorgruposagendaService.deleteOperadorGruposAgenda(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useOperadorGruposAgendaFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <OperadorGruposAgendaGridMobileComponent
        data={operadorgruposagendaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <OperadorGruposAgendaGridDesktopComponent
        data={operadorgruposagendaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <OperadorGruposAgendaWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedOperadorGruposAgenda={selectedOperadorGruposAgenda}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Operador Grupos Agenda'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default OperadorGruposAgendaGrid;