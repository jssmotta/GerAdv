// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OperadorGruposAgendaService, OperadorGruposAgendaValidator } from '@/app/GerAdv_TS/OperadorGruposAgenda/Services/OperadorGruposAgenda.service';
import { OperadorGruposAgendaApi, OperadorGruposAgendaApiError } from '@/app/GerAdv_TS/OperadorGruposAgenda/Apis/ApiOperadorGruposAgenda';
import { IOperadorGruposAgenda } from '@/app/GerAdv_TS/OperadorGruposAgenda/Interfaces/interface.OperadorGruposAgenda';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OperadorGruposAgendaApi;

const sample: IOperadorGruposAgenda = { id: 1, nome: 'Teste' };

describe('OperadorGruposAgendaService', () => {
  let service: OperadorGruposAgendaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OperadorGruposAgendaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OperadorGruposAgendaValidator, 'validateOperadorGruposAgenda').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOperadorGruposAgendaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOperadorGruposAgendaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOperadorGruposAgendaById lança OperadorGruposAgendaApiError para id inválido', async () => {
    await expect(service.fetchOperadorGruposAgendaById(0)).rejects.toBeInstanceOf(OperadorGruposAgendaApiError);
  });

  it('saveOperadorGruposAgenda chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOperadorGruposAgenda(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOperadorGruposAgenda lança OperadorGruposAgendaApiError quando validação falha', async () => {
    (OperadorGruposAgendaValidator.validateOperadorGruposAgenda as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOperadorGruposAgenda(sample)).rejects.toBeInstanceOf(OperadorGruposAgendaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOperadorGruposAgenda chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOperadorGruposAgenda(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOperadorGruposAgenda lança OperadorGruposAgendaApiError para id inválido', async () => {
    await expect(service.deleteOperadorGruposAgenda(0)).rejects.toBeInstanceOf(OperadorGruposAgendaApiError);
  });

  it('validateOperadorGruposAgenda delega para OperadorGruposAgendaValidator', () => {
    (OperadorGruposAgendaValidator.validateOperadorGruposAgenda as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOperadorGruposAgenda(sample);
    expect(r.isValid).toBe(true);
  });
});
