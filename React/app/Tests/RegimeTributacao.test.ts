// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useRegimeTributacaoForm, useRegimeTributacaoList, useValidationsRegimeTributacao } from '../GerAdv_TS/RegimeTributacao/Hooks/hookRegimeTributacao';
import { IRegimeTributacao } from '../GerAdv_TS/RegimeTributacao/Interfaces/interface.RegimeTributacao';
import { IRegimeTributacaoService } from '../GerAdv_TS/RegimeTributacao/Services/RegimeTributacao.service';
import { RegimeTributacaoTestEmpty } from '../GerAdv_TS/Models/RegimeTributacao';
import { useRegimeTributacaoComboBox } from '../GerAdv_TS/RegimeTributacao/Hooks/hookRegimeTributacao';

// Mock do serviço
const mockRegimeTributacaoService: jest.Mocked<IRegimeTributacaoService> = {
  fetchRegimeTributacaoById: jest.fn(),
  saveRegimeTributacao: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteRegimeTributacao: jest.fn(),
  validateRegimeTributacao: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialRegimeTributacao: IRegimeTributacao = { ...RegimeTributacaoTestEmpty() };

describe('useRegimeTributacaoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    expect(result.current.data).toEqual(initialRegimeTributacao);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Regime Tributacao',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Regime Tributacao');
  });

   test('deve carregar Regime Tributacao por ID', async () => {
    const mockRegimeTributacao = { ...initialRegimeTributacao, id: 1, nome: 'Regime Tributacao Teste' };
    mockRegimeTributacaoService.fetchRegimeTributacaoById.mockResolvedValue(mockRegimeTributacao);

    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.loadRegimeTributacao(1);
    });

    expect(mockRegimeTributacaoService.fetchRegimeTributacaoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockRegimeTributacao);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Regime Tributacao', async () => {
    const errorMessage = 'Erro ao carregar Regime Tributacao';
    mockRegimeTributacaoService.fetchRegimeTributacaoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.loadRegimeTributacao(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialRegimeTributacao, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialRegimeTributacao);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoForm(initialRegimeTributacao, mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.loadRegimeTributacao(0);
    });

    expect(mockRegimeTributacaoService.fetchRegimeTributacaoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialRegimeTributacao);
  });
});

describe('useRegimeTributacaoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoList(mockRegimeTributacaoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialRegimeTributacao, id: 1, nome: 'Regime Tributacao 1' },
      { ...initialRegimeTributacao, id: 2, nome: 'Regime Tributacao 2' }
    ];
    mockRegimeTributacaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRegimeTributacaoList(mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockRegimeTributacaoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockRegimeTributacaoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRegimeTributacaoList(mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialRegimeTributacao, id: 1, nome: 'Regime Tributacao Filtrado' }];
    const filtro = { nome: 'Regime Tributacao' };
    mockRegimeTributacaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRegimeTributacaoList(mockRegimeTributacaoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockRegimeTributacaoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsRegimeTributacao', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsRegimeTributacao());

    const validData = { ...initialRegimeTributacao, nome: 'Regime Tributacao Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsRegimeTributacao());

    const invalidData = { ...initialRegimeTributacao, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsRegimeTributacao());

    const invalidData = { 
      ...initialRegimeTributacao, 
      nome: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsRegimeTributacao());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialRegimeTributacao, id: 1, nome: 'Regime Tributacao Teste' }];
    mockRegimeTributacaoService.getAll.mockResolvedValue(mockData);
    mockRegimeTributacaoService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useRegimeTributacaoList(mockRegimeTributacaoService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsRegimeTributacao()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Regime Tributacao Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Regime Tributacao Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Regime Tributacao 1' },
      { id: 2, nome: 'Regime Tributacao 2' }
    ];
    mockRegimeTributacaoService.getList.mockResolvedValue(mockOptions as IRegimeTributacao[]);


    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Regime Tributacao 1' },
        { id: 2, nome: 'Regime Tributacao 2' }
      ]);
    });


    expect(mockRegimeTributacaoService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Regime Tributacao ABC' },
      { id: 2, nome: 'Regime Tributacao XYZ' }
    ];
    mockRegimeTributacaoService.getList.mockResolvedValue(mockOptions as IRegimeTributacao[]);   


 const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Regime Tributacao ABC' },
        { id: 2, nome: 'Regime Tributacao XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Regime Tributacao ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Regime Tributacao ABC' },
      { id: 2, nome: 'Regime Tributacao XYZ' }
    ];
    mockRegimeTributacaoService.getList.mockResolvedValue(mockOptions as IRegimeTributacao[]);
  


    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Regime Tributacao ABC' },
        { id: 2, nome: 'Regime Tributacao XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Regime Tributacao ABC' },
          {id: 2, nome: 'Regime Tributacao XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );

    const newValue = { id: 1, nome: 'Regime Tributacao Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Regime Tributacao Inicial' };
    
    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useRegimeTributacaoComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Regime Tributacao Inicial' };
    
    const { result } = renderHook(() => 
      useRegimeTributacaoComboBox(mockRegimeTributacaoService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






