// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class TiposAcaoWriterTests
{
    private readonly Mock<IFTiposAcaoFactory> _mockTiposAcaoFactory;
    private readonly Mock<FTiposAcao> _mockFTiposAcao;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TiposAcaoWriter _tiposacaoWriter;
    public TiposAcaoWriterTests()
    {
        _mockTiposAcaoFactory = new Mock<IFTiposAcaoFactory>();
        _mockFTiposAcao = new Mock<FTiposAcao>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _tiposacaoWriter = new TiposAcaoWriter(_mockTiposAcaoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var tiposacao = CreateValidTiposAcaoModel();
        tiposacao.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockTiposAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTiposAcao.Object);
        _mockFTiposAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tiposacaoWriter.WriteAsync(tiposacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTiposAcao.Object);
        _mockTiposAcaoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockTiposAcaoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFTiposAcao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var tiposacao = CreateValidTiposAcaoModel();
        tiposacao.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockTiposAcaoFactory.Setup(x => x.CreateFromIdAsync(tiposacao.Id, _mockConnection.Object)).ReturnsAsync(_mockFTiposAcao.Object);
        _mockFTiposAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _tiposacaoWriter.WriteAsync(tiposacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTiposAcao.Object);
        _mockTiposAcaoFactory.Verify(x => x.CreateFromIdAsync(tiposacao.Id, _mockConnection.Object), Times.Once);
        _mockTiposAcaoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFTiposAcao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var tiposacao = CreateValidTiposAcaoModel();
        var auditorQuem = 99;
        _mockTiposAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTiposAcao.Object);
        _mockFTiposAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tiposacaoWriter.WriteAsync(tiposacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTiposAcao.Object);
        _mockFTiposAcao.VerifySet(x => x.FNome = tiposacao.Nome, Times.Once);
        _mockFTiposAcao.VerifySet(x => x.FInativo = tiposacao.Inativo, Times.Once);
        _mockFTiposAcao.VerifySet(x => x.FGUID = tiposacao.GUID, Times.Once);
        _mockFTiposAcao.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var tiposacao = CreateValidTiposAcaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTiposAcaoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiposacaoWriter.WriteAsync(tiposacao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tiposacao = CreateValidTiposAcaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTiposAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTiposAcao.Object);
        _mockFTiposAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiposacaoWriter.WriteAsync(tiposacao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var tiposacaoResponse = new TiposAcaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockTiposAcaoFactory.Setup(x => x.DeleteAsync(operadorId, tiposacaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _tiposacaoWriter.Delete(tiposacaoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockTiposAcaoFactory.Verify(x => x.DeleteAsync(operadorId, tiposacaoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var tiposacaoResponse = new TiposAcaoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockTiposAcaoFactory.Setup(x => x.DeleteAsync(operadorId, tiposacaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _tiposacaoWriter.Delete(tiposacaoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tiposacaoResponse = new TiposAcaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockTiposAcaoFactory.Setup(x => x.DeleteAsync(operadorId, tiposacaoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiposacaoWriter.Delete(tiposacaoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new TiposAcaoWriter(_mockTiposAcaoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new TiposAcaoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.TiposAcao CreateValidTiposAcaoModel()
    {
        return new Models.TiposAcao
        {
            Id = 0,
            Nome = "João",
            Inativo = false,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}