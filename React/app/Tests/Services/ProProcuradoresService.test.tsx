// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProProcuradoresService, ProProcuradoresValidator } from '@/app/GerAdv_TS/ProProcuradores/Services/ProProcuradores.service';
import { ProProcuradoresApi, ProProcuradoresApiError } from '@/app/GerAdv_TS/ProProcuradores/Apis/ApiProProcuradores';
import { IProProcuradores } from '@/app/GerAdv_TS/ProProcuradores/Interfaces/interface.ProProcuradores';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProProcuradoresApi;

const sample: IProProcuradores = { id: 1, nome: 'Teste' };

describe('ProProcuradoresService', () => {
  let service: ProProcuradoresService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProProcuradoresService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProProcuradoresValidator, 'validateProProcuradores').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProProcuradoresById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProProcuradoresById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProProcuradoresById lança ProProcuradoresApiError para id inválido', async () => {
    await expect(service.fetchProProcuradoresById(0)).rejects.toBeInstanceOf(ProProcuradoresApiError);
  });

  it('saveProProcuradores chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProProcuradores(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProProcuradores lança ProProcuradoresApiError quando validação falha', async () => {
    (ProProcuradoresValidator.validateProProcuradores as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProProcuradores(sample)).rejects.toBeInstanceOf(ProProcuradoresApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProProcuradores chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProProcuradores(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProProcuradores lança ProProcuradoresApiError para id inválido', async () => {
    await expect(service.deleteProProcuradores(0)).rejects.toBeInstanceOf(ProProcuradoresApiError);
  });

  it('validateProProcuradores delega para ProProcuradoresValidator', () => {
    (ProProcuradoresValidator.validateProProcuradores as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProProcuradores(sample);
    expect(r.isValid).toBe(true);
  });
});
