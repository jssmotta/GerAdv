// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoOrigemSucumbenciaService, TipoOrigemSucumbenciaValidator } from '@/app/GerAdv_TS/TipoOrigemSucumbencia/Services/TipoOrigemSucumbencia.service';
import { TipoOrigemSucumbenciaApi, TipoOrigemSucumbenciaApiError } from '@/app/GerAdv_TS/TipoOrigemSucumbencia/Apis/ApiTipoOrigemSucumbencia';
import { ITipoOrigemSucumbencia } from '@/app/GerAdv_TS/TipoOrigemSucumbencia/Interfaces/interface.TipoOrigemSucumbencia';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoOrigemSucumbenciaApi;

const sample: ITipoOrigemSucumbencia = { id: 1, nome: 'Teste' };

describe('TipoOrigemSucumbenciaService', () => {
  let service: TipoOrigemSucumbenciaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoOrigemSucumbenciaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoOrigemSucumbenciaValidator, 'validateTipoOrigemSucumbencia').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoOrigemSucumbenciaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoOrigemSucumbenciaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoOrigemSucumbenciaById lança TipoOrigemSucumbenciaApiError para id inválido', async () => {
    await expect(service.fetchTipoOrigemSucumbenciaById(0)).rejects.toBeInstanceOf(TipoOrigemSucumbenciaApiError);
  });

  it('saveTipoOrigemSucumbencia chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoOrigemSucumbencia(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoOrigemSucumbencia lança TipoOrigemSucumbenciaApiError quando validação falha', async () => {
    (TipoOrigemSucumbenciaValidator.validateTipoOrigemSucumbencia as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoOrigemSucumbencia(sample)).rejects.toBeInstanceOf(TipoOrigemSucumbenciaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoOrigemSucumbencia chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoOrigemSucumbencia(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoOrigemSucumbencia lança TipoOrigemSucumbenciaApiError para id inválido', async () => {
    await expect(service.deleteTipoOrigemSucumbencia(0)).rejects.toBeInstanceOf(TipoOrigemSucumbenciaApiError);
  });

  it('validateTipoOrigemSucumbencia delega para TipoOrigemSucumbenciaValidator', () => {
    (TipoOrigemSucumbenciaValidator.validateTipoOrigemSucumbencia as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoOrigemSucumbencia(sample);
    expect(r.isValid).toBe(true);
  });
});
