// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class PenhoraStatusValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFPenhoraStatusFactory> _mockPenhoraStatusFactory;
    private readonly Mock<IPenhoraStatusReader> _mockReader;
    private readonly PenhoraStatusValidation _validation;
    private readonly Mock<IPenhoraStatusService> _mockPenhoraStatusService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly PenhoraStatusService _service;
    private readonly string _validUri = "test-uri";
    public PenhoraStatusValidationTests()
    {
        _mockPenhoraStatusFactory = new Mock<IFPenhoraStatusFactory>();
        _mockReader = new Mock<IPenhoraStatusReader>();
        _validation = new PenhoraStatusValidation();
        _mockPenhoraStatusService = new Mock<IPenhoraStatusService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void PenhoraStatusValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IPenhoraStatusValidation>();
    }

    [Fact]
    public void PenhoraStatusValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(PenhoraStatusValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void PenhoraStatusValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(PenhoraStatusValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.PenhoraStatus CreateValidPenhoraStatus()
    {
        return new Models.PenhoraStatus
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockPenhoraStatusService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPenhoraStatus>(), It.IsAny<string>())).ReturnsAsync(new List<PenhoraStatusResponseAll>());
    // Setup other mocks but don't override the PenhoraStatuss service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockPenhoraStatusService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPenhoraStatus>(), It.IsAny<string>())).ReturnsAsync(new List<PenhoraStatusResponseAll>());
    // Setup other mocks but don't override the PenhoraStatuss service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var penhorastatus = new Models.PenhoraStatus
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(penhorastatus, _mockPenhoraStatusService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockPenhoraStatusService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}