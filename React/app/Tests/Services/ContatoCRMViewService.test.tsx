// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ContatoCRMViewService, ContatoCRMViewValidator } from '@/app/GerAdv_TS/ContatoCRMView/Services/ContatoCRMView.service';
import { ContatoCRMViewApi, ContatoCRMViewApiError } from '@/app/GerAdv_TS/ContatoCRMView/Apis/ApiContatoCRMView';
import { IContatoCRMView } from '@/app/GerAdv_TS/ContatoCRMView/Interfaces/interface.ContatoCRMView';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ContatoCRMViewApi;

const sample: IContatoCRMView = { id: 1, nome: 'Teste' };

describe('ContatoCRMViewService', () => {
  let service: ContatoCRMViewService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ContatoCRMViewService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ContatoCRMViewValidator, 'validateContatoCRMView').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchContatoCRMViewById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchContatoCRMViewById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchContatoCRMViewById lança ContatoCRMViewApiError para id inválido', async () => {
    await expect(service.fetchContatoCRMViewById(0)).rejects.toBeInstanceOf(ContatoCRMViewApiError);
  });

  it('saveContatoCRMView chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveContatoCRMView(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveContatoCRMView lança ContatoCRMViewApiError quando validação falha', async () => {
    (ContatoCRMViewValidator.validateContatoCRMView as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveContatoCRMView(sample)).rejects.toBeInstanceOf(ContatoCRMViewApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteContatoCRMView chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteContatoCRMView(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteContatoCRMView lança ContatoCRMViewApiError para id inválido', async () => {
    await expect(service.deleteContatoCRMView(0)).rejects.toBeInstanceOf(ContatoCRMViewApiError);
  });

  it('validateContatoCRMView delega para ContatoCRMViewValidator', () => {
    (ContatoCRMViewValidator.validateContatoCRMView as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateContatoCRMView(sample);
    expect(r.isValid).toBe(true);
  });
});
