// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IReuniaoPessoasService } from '../Services/ReuniaoPessoas.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IReuniaoPessoas } from '../Interfaces/interface.ReuniaoPessoas';
import { FilterReuniaoPessoas } from '../Filters/ReuniaoPessoas';
import { isValidDate } from '@/app/tools/datetime';
import { ReuniaoPessoasApi } from '../Apis/ApiReuniaoPessoas';

export const useReuniaoPessoasForm = (
  initialReuniaoPessoas: IReuniaoPessoas,
  dataService: IReuniaoPessoasService
) => {
  const [data, setData] = useState<IReuniaoPessoas>(initialReuniaoPessoas);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadReuniaoPessoas = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialReuniaoPessoas);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchReuniaoPessoasById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Reuniao Pessoas';
      setError(errorMessage);
      //console.log('Erro ao carregar Reuniao Pessoas');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialReuniaoPessoas]);

  const resetForm = useCallback(() => {
    setData(initialReuniaoPessoas);
    setError(null);
  }, [initialReuniaoPessoas]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadReuniaoPessoas,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadReuniaoPessoas, resetForm]);
  return returnValue;
};


export const useReuniaoPessoasNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('ReuniaoPessoas', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useReuniaoPessoasList = (dataService: IReuniaoPessoasService, currentFilter?: FilterReuniaoPessoas | undefined | null) => {
  const [data, setData] = useState<IReuniaoPessoas[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterReuniaoPessoas | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterReuniaoPessoas | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterReuniaoPessoas);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar reuniaopessoas';
      setError(errorMessage);
      //console.log('Erro ao carregar reuniaopessoas');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useReuniaoPessoasNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsReuniaoPessoas() {

  async function runValidation(data: IReuniaoPessoas, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const reuniaopessoasApi = new ReuniaoPessoasApi(uri ?? '', token ?? '');

    const result = await reuniaopessoasApi.validation(data);

    return result;
  }

  function validate(data: IReuniaoPessoas): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}