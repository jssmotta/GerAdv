// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class SituacaoWriterTests
{
    private readonly Mock<IFSituacaoFactory> _mockSituacaoFactory;
    private readonly Mock<FSituacao> _mockFSituacao;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly SituacaoWriter _situacaoWriter;
    public SituacaoWriterTests()
    {
        _mockSituacaoFactory = new Mock<IFSituacaoFactory>();
        _mockFSituacao = new Mock<FSituacao>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _situacaoWriter = new SituacaoWriter(_mockSituacaoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var situacao = CreateValidSituacaoModel();
        situacao.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockSituacaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSituacao.Object);
        _mockFSituacao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _situacaoWriter.WriteAsync(situacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSituacao.Object);
        _mockSituacaoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockSituacaoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFSituacao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var situacao = CreateValidSituacaoModel();
        situacao.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockSituacaoFactory.Setup(x => x.CreateFromIdAsync(situacao.Id, _mockConnection.Object)).ReturnsAsync(_mockFSituacao.Object);
        _mockFSituacao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _situacaoWriter.WriteAsync(situacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSituacao.Object);
        _mockSituacaoFactory.Verify(x => x.CreateFromIdAsync(situacao.Id, _mockConnection.Object), Times.Once);
        _mockSituacaoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFSituacao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var situacao = CreateValidSituacaoModel();
        var auditorQuem = 99;
        _mockSituacaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSituacao.Object);
        _mockFSituacao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _situacaoWriter.WriteAsync(situacao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFSituacao.Object);
        _mockFSituacao.VerifySet(x => x.FParte_Int = situacao.Parte_Int, Times.Once);
        _mockFSituacao.VerifySet(x => x.FParte_Opo = situacao.Parte_Opo, Times.Once);
        _mockFSituacao.VerifySet(x => x.FTop = situacao.Top, Times.Once);
        _mockFSituacao.VerifySet(x => x.FGUID = situacao.GUID, Times.Once);
        _mockFSituacao.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var situacao = CreateValidSituacaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSituacaoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _situacaoWriter.WriteAsync(situacao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var situacao = CreateValidSituacaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockSituacaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFSituacao.Object);
        _mockFSituacao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _situacaoWriter.WriteAsync(situacao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var situacaoResponse = new SituacaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockSituacaoFactory.Setup(x => x.DeleteAsync(operadorId, situacaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _situacaoWriter.Delete(situacaoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockSituacaoFactory.Verify(x => x.DeleteAsync(operadorId, situacaoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var situacaoResponse = new SituacaoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockSituacaoFactory.Setup(x => x.DeleteAsync(operadorId, situacaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _situacaoWriter.Delete(situacaoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var situacaoResponse = new SituacaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockSituacaoFactory.Setup(x => x.DeleteAsync(operadorId, situacaoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _situacaoWriter.Delete(situacaoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new SituacaoWriter(_mockSituacaoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new SituacaoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Situacao CreateValidSituacaoModel()
    {
        return new Models.Situacao
        {
            Id = 0,
            Parte_Int = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Parte_Opo = "AAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            Top = false,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}