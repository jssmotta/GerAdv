// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IUltimosProcessosService } from '../Services/UltimosProcessos.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IUltimosProcessos } from '../Interfaces/interface.UltimosProcessos';
import { FilterUltimosProcessos } from '../Filters/UltimosProcessos';
import { isValidDate } from '@/app/tools/datetime';
import { UltimosProcessosApi } from '../Apis/ApiUltimosProcessos';

export const useUltimosProcessosForm = (
  initialUltimosProcessos: IUltimosProcessos,
  dataService: IUltimosProcessosService
) => {
  const [data, setData] = useState<IUltimosProcessos>(initialUltimosProcessos);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadUltimosProcessos = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialUltimosProcessos);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchUltimosProcessosById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Ultimos Processos';
      setError(errorMessage);
      //console.log('Erro ao carregar Ultimos Processos');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialUltimosProcessos]);

  const resetForm = useCallback(() => {
    setData(initialUltimosProcessos);
    setError(null);
  }, [initialUltimosProcessos]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadUltimosProcessos,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadUltimosProcessos, resetForm]);
  return returnValue;
};


export const useUltimosProcessosNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('UltimosProcessos', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useUltimosProcessosList = (dataService: IUltimosProcessosService, currentFilter?: FilterUltimosProcessos | undefined | null) => {
  const [data, setData] = useState<IUltimosProcessos[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterUltimosProcessos | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterUltimosProcessos | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterUltimosProcessos);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar ultimosprocessos';
      setError(errorMessage);
      //console.log('Erro ao carregar ultimosprocessos');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useUltimosProcessosNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsUltimosProcessos() {

  async function runValidation(data: IUltimosProcessos, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const ultimosprocessosApi = new UltimosProcessosApi(uri ?? '', token ?? '');

    const result = await ultimosprocessosApi.validation(data);

    return result;
  }

  function validate(data: IUltimosProcessos): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}