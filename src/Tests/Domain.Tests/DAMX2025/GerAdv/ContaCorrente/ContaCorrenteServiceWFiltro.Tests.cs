// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ContaCorrente
// Fonte:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class ContaCorrenteServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFContaCorrenteFactory> _mockContaCorrenteFactory;
    private readonly Mock<IContaCorrenteReader> _mockReader;
    private readonly Mock<IContaCorrenteValidation> _mockValidation;
    private readonly Mock<IContaCorrenteWriter> _mockWriter;
    private readonly Mock<IClientesReader> _mockClientesReader;
    private readonly Mock<IHttpContextAccessor> _mockHttpContextAccessor;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IGenericVoiceFilterService<Filters.FilterContaCorrente>> _mockVoiceFilterService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly ContaCorrenteService _service;
    private readonly ServiceFilter _serviceS;
    public ContaCorrenteServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockContaCorrenteFactory = new Mock<IFContaCorrenteFactory>();
        _mockReader = new Mock<IContaCorrenteReader>();
        _mockValidation = new Mock<IContaCorrenteValidation>();
        _mockWriter = new Mock<IContaCorrenteWriter>();
        _mockClientesReader = new Mock<IClientesReader>();
        _mockHttpContextAccessor = new Mock<IHttpContextAccessor>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockVoiceFilterService = new Mock<IGenericVoiceFilterService<Filters.FilterContaCorrente>>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new ContaCorrenteService(_mockAppSettings.Object, _mockContaCorrenteFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockClientesReader.Object, _mockHttpContextAccessor.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _mockVoiceFilterService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterContaCorrente? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterContaCorrente(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuemCad = 1,
            DtCad = "24/04/1975",
            QuemAtu = 1,
            DtAtu = "24/04/1975",
            Visto = 2,
            Guid = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            CIAcordo = 1,
            Quitado = 1,
            IDContrato = 1,
            QuitadoID = 1,
            DebitoID = 1,
            LivroCaixaID = 1,
            Sucumbencia = 2,
            DistRegra = 1,
            DtOriginal = "24/04/1975",
            Processo = 1,
            ParcelaX = 1,
            Valor = 1m,
            Data = "24/04/1975",
            Cliente = 1,
            Historico = "",
            Contrato = 2,
            Pago = 1,
            Distribuir = 2,
            LC = 1,
            IDHTrab = 1,
            NroParcelas = 1,
            ValorPrincipal = 1m,
            ParcelaPrincipalID = 1,
            Hide = 2,
            DataPgto = "24/04/1975",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Guid_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string GuidComWhitespace)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = GuidComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = new string ('A', 1000);
        var filtro = new FilterContaCorrente
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{GuidGigante}%");
    }

    [Fact]
    public void WFiltro_Guid_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var GuidGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterContaCorrente
        {
            Guid = GuidGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Guid_ComCaracteresEspeciais_DeveProcessarCorretamente(string GuidComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = GuidComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(GuidComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_GuidContaCorrente_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_GuidContaCorrente_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_GuidContaCorrente_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_GuidContaCorrente_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Guid_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Guid_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Guid}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Guid_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Guid_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Historico_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string HistoricoComWhitespace)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = HistoricoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var HistoricoGigante = new string ('A', 1000);
        var filtro = new FilterContaCorrente
        {
            Historico = HistoricoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{HistoricoGigante}%");
    }

    [Fact]
    public void WFiltro_Historico_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var HistoricoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterContaCorrente
        {
            Historico = HistoricoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Historico_ComCaracteresEspeciais_DeveProcessarCorretamente(string HistoricoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = HistoricoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(HistoricoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_HistoricoContaCorrente_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_HistoricoContaCorrente_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_HistoricoContaCorrente_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_HistoricoContaCorrente_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Historico_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "Historico",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Historico_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Historico_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Historico}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Historico_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Historico_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Historico = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region QuemCad Filter Tests
    [Fact]
    public void WFiltro_QuemCadEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuemCad = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.QuemCad));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.QuemCad);
    }

    [Fact]
    public void WFiltro_QuemCadHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemCadValue = 1;
        var filtro = new FilterContaCorrente
        {
            QuemCad = QuemCadValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemCadParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemCad}");
        QuemCadParameter.Should().NotBeNull();
        QuemCadParameter.Value.Should().Be(QuemCadValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuemCad}] = @{DBContaCorrenteDicInfo.QuemCad}");
    }

    [Fact]
    public void WFiltro_QuemCadWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemCad = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            QuemCad = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemCad}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemCad = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            QuemCad = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemCad}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemCadAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuemCad}] = @{DBContaCorrenteDicInfo.QuemCad}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemCadWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemCad = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region QuemAtu Filter Tests
    [Fact]
    public void WFiltro_QuemAtuEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuemAtu = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.QuemAtu));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.QuemAtu);
    }

    [Fact]
    public void WFiltro_QuemAtuHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuemAtuValue = 1;
        var filtro = new FilterContaCorrente
        {
            QuemAtu = QuemAtuValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuemAtuParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemAtu}");
        QuemAtuParameter.Should().NotBeNull();
        QuemAtuParameter.Value.Should().Be(QuemAtuValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuemAtu}] = @{DBContaCorrenteDicInfo.QuemAtu}");
    }

    [Fact]
    public void WFiltro_QuemAtuWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuemAtu = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            QuemAtu = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemAtu}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuemAtu = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            QuemAtu = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuemAtu}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuemAtuAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuemAtu}] = @{DBContaCorrenteDicInfo.QuemAtu}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuemAtuWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuemAtu = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region CIAcordo Filter Tests
    [Fact]
    public void WFiltro_CIAcordoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            CIAcordo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.CIAcordo));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.CIAcordo);
    }

    [Fact]
    public void WFiltro_CIAcordoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var CIAcordoValue = 1;
        var filtro = new FilterContaCorrente
        {
            CIAcordo = CIAcordoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var CIAcordoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.CIAcordo}");
        CIAcordoParameter.Should().NotBeNull();
        CIAcordoParameter.Value.Should().Be(CIAcordoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.CIAcordo}] = @{DBContaCorrenteDicInfo.CIAcordo}");
    }

    [Fact]
    public void WFiltro_CIAcordoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for CIAcordo = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            CIAcordo = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.CIAcordo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for CIAcordo = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            CIAcordo = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.CIAcordo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_CIAcordoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            CIAcordo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.CIAcordo}] = @{DBContaCorrenteDicInfo.CIAcordo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_CIAcordoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            CIAcordo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_CIAcordoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            CIAcordo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDContrato Filter Tests
    [Fact]
    public void WFiltro_IDContratoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            IDContrato = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.IDContrato));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.IDContrato);
    }

    [Fact]
    public void WFiltro_IDContratoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDContratoValue = 1;
        var filtro = new FilterContaCorrente
        {
            IDContrato = IDContratoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDContratoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDContrato}");
        IDContratoParameter.Should().NotBeNull();
        IDContratoParameter.Value.Should().Be(IDContratoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.IDContrato}] = @{DBContaCorrenteDicInfo.IDContrato}");
    }

    [Fact]
    public void WFiltro_IDContratoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDContrato = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            IDContrato = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDContrato}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDContrato = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            IDContrato = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDContrato}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDContratoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            IDContrato = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.IDContrato}] = @{DBContaCorrenteDicInfo.IDContrato}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDContratoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            IDContrato = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDContratoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            IDContrato = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region QuitadoID Filter Tests
    [Fact]
    public void WFiltro_QuitadoIDEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuitadoID = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.QuitadoID));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.QuitadoID);
    }

    [Fact]
    public void WFiltro_QuitadoIDHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuitadoIDValue = 1;
        var filtro = new FilterContaCorrente
        {
            QuitadoID = QuitadoIDValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuitadoIDParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuitadoID}");
        QuitadoIDParameter.Should().NotBeNull();
        QuitadoIDParameter.Value.Should().Be(QuitadoIDValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuitadoID}] = @{DBContaCorrenteDicInfo.QuitadoID}");
    }

    [Fact]
    public void WFiltro_QuitadoIDWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for QuitadoID = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            QuitadoID = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuitadoID}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for QuitadoID = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            QuitadoID = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.QuitadoID}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuitadoIDAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            QuitadoID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.QuitadoID}] = @{DBContaCorrenteDicInfo.QuitadoID}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuitadoIDWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuitadoID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuitadoIDWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            QuitadoID = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region DebitoID Filter Tests
    [Fact]
    public void WFiltro_DebitoIDEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DebitoID = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DebitoID));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.DebitoID);
    }

    [Fact]
    public void WFiltro_DebitoIDHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var DebitoIDValue = 1;
        var filtro = new FilterContaCorrente
        {
            DebitoID = DebitoIDValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var DebitoIDParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DebitoID}");
        DebitoIDParameter.Should().NotBeNull();
        DebitoIDParameter.Value.Should().Be(DebitoIDValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DebitoID}] = @{DBContaCorrenteDicInfo.DebitoID}");
    }

    [Fact]
    public void WFiltro_DebitoIDWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for DebitoID = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            DebitoID = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DebitoID}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for DebitoID = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            DebitoID = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DebitoID}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_DebitoIDAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DebitoID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DebitoID}] = @{DBContaCorrenteDicInfo.DebitoID}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_DebitoIDWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            DebitoID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_DebitoIDWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            DebitoID = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LivroCaixaID Filter Tests
    [Fact]
    public void WFiltro_LivroCaixaIDEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            LivroCaixaID = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.LivroCaixaID));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.LivroCaixaID);
    }

    [Fact]
    public void WFiltro_LivroCaixaIDHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LivroCaixaIDValue = 1;
        var filtro = new FilterContaCorrente
        {
            LivroCaixaID = LivroCaixaIDValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LivroCaixaIDParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LivroCaixaID}");
        LivroCaixaIDParameter.Should().NotBeNull();
        LivroCaixaIDParameter.Value.Should().Be(LivroCaixaIDValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.LivroCaixaID}] = @{DBContaCorrenteDicInfo.LivroCaixaID}");
    }

    [Fact]
    public void WFiltro_LivroCaixaIDWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LivroCaixaID = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            LivroCaixaID = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LivroCaixaID}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LivroCaixaID = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            LivroCaixaID = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LivroCaixaID}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LivroCaixaIDAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            LivroCaixaID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.LivroCaixaID}] = @{DBContaCorrenteDicInfo.LivroCaixaID}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LivroCaixaIDWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            LivroCaixaID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LivroCaixaIDWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            LivroCaixaID = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Processo Filter Tests
    [Fact]
    public void WFiltro_ProcessoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Processo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Processo));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Processo);
    }

    [Fact]
    public void WFiltro_ProcessoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ProcessoValue = 1;
        var filtro = new FilterContaCorrente
        {
            Processo = ProcessoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ProcessoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Processo}");
        ProcessoParameter.Should().NotBeNull();
        ProcessoParameter.Value.Should().Be(ProcessoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Processo}] = @{DBContaCorrenteDicInfo.Processo}");
    }

    [Fact]
    public void WFiltro_ProcessoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Processo = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Processo = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Processo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Processo = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Processo = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Processo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ProcessoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Processo}] = @{DBContaCorrenteDicInfo.Processo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ProcessoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Processo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region ParcelaX Filter Tests
    [Fact]
    public void WFiltro_ParcelaXEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ParcelaX = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.ParcelaX));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.ParcelaX);
    }

    [Fact]
    public void WFiltro_ParcelaXHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ParcelaXValue = 1;
        var filtro = new FilterContaCorrente
        {
            ParcelaX = ParcelaXValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ParcelaXParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaX}");
        ParcelaXParameter.Should().NotBeNull();
        ParcelaXParameter.Value.Should().Be(ParcelaXValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ParcelaX}] = @{DBContaCorrenteDicInfo.ParcelaX}");
    }

    [Fact]
    public void WFiltro_ParcelaXWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for ParcelaX = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            ParcelaX = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaX}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for ParcelaX = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            ParcelaX = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaX}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ParcelaXAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ParcelaX = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ParcelaX}] = @{DBContaCorrenteDicInfo.ParcelaX}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ParcelaXWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            ParcelaX = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ParcelaXWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            ParcelaX = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Cliente Filter Tests
    [Fact]
    public void WFiltro_ClienteEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Cliente = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Cliente));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Cliente);
    }

    [Fact]
    public void WFiltro_ClienteHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ClienteValue = 1;
        var filtro = new FilterContaCorrente
        {
            Cliente = ClienteValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ClienteParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Cliente}");
        ClienteParameter.Should().NotBeNull();
        ClienteParameter.Value.Should().Be(ClienteValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Cliente}] = @{DBContaCorrenteDicInfo.Cliente}");
    }

    [Fact]
    public void WFiltro_ClienteWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Cliente = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Cliente = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Cliente}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Cliente = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Cliente = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Cliente}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ClienteAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Cliente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Cliente}] = @{DBContaCorrenteDicInfo.Cliente}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ClienteWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Cliente = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ClienteWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Cliente = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region IDHTrab Filter Tests
    [Fact]
    public void WFiltro_IDHTrabEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            IDHTrab = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.IDHTrab));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.IDHTrab);
    }

    [Fact]
    public void WFiltro_IDHTrabHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var IDHTrabValue = 1;
        var filtro = new FilterContaCorrente
        {
            IDHTrab = IDHTrabValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var IDHTrabParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDHTrab}");
        IDHTrabParameter.Should().NotBeNull();
        IDHTrabParameter.Value.Should().Be(IDHTrabValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.IDHTrab}] = @{DBContaCorrenteDicInfo.IDHTrab}");
    }

    [Fact]
    public void WFiltro_IDHTrabWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for IDHTrab = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            IDHTrab = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDHTrab}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for IDHTrab = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            IDHTrab = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.IDHTrab}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_IDHTrabAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            IDHTrab = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.IDHTrab}] = @{DBContaCorrenteDicInfo.IDHTrab}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_IDHTrabWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            IDHTrab = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_IDHTrabWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            IDHTrab = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region NroParcelas Filter Tests
    [Fact]
    public void WFiltro_NroParcelasEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            NroParcelas = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.NroParcelas));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.NroParcelas);
    }

    [Fact]
    public void WFiltro_NroParcelasHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var NroParcelasValue = 1;
        var filtro = new FilterContaCorrente
        {
            NroParcelas = NroParcelasValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var NroParcelasParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.NroParcelas}");
        NroParcelasParameter.Should().NotBeNull();
        NroParcelasParameter.Value.Should().Be(NroParcelasValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.NroParcelas}] = @{DBContaCorrenteDicInfo.NroParcelas}");
    }

    [Fact]
    public void WFiltro_NroParcelasWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for NroParcelas = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            NroParcelas = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.NroParcelas}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for NroParcelas = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            NroParcelas = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.NroParcelas}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_NroParcelasAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            NroParcelas = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.NroParcelas}] = @{DBContaCorrenteDicInfo.NroParcelas}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_NroParcelasWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            NroParcelas = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_NroParcelasWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            NroParcelas = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region ParcelaPrincipalID Filter Tests
    [Fact]
    public void WFiltro_ParcelaPrincipalIDEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ParcelaPrincipalID = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.ParcelaPrincipalID));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.ParcelaPrincipalID);
    }

    [Fact]
    public void WFiltro_ParcelaPrincipalIDHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ParcelaPrincipalIDValue = 1;
        var filtro = new FilterContaCorrente
        {
            ParcelaPrincipalID = ParcelaPrincipalIDValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ParcelaPrincipalIDParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaPrincipalID}");
        ParcelaPrincipalIDParameter.Should().NotBeNull();
        ParcelaPrincipalIDParameter.Value.Should().Be(ParcelaPrincipalIDValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ParcelaPrincipalID}] = @{DBContaCorrenteDicInfo.ParcelaPrincipalID}");
    }

    [Fact]
    public void WFiltro_ParcelaPrincipalIDWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for ParcelaPrincipalID = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            ParcelaPrincipalID = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaPrincipalID}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for ParcelaPrincipalID = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            ParcelaPrincipalID = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ParcelaPrincipalID}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ParcelaPrincipalIDAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ParcelaPrincipalID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ParcelaPrincipalID}] = @{DBContaCorrenteDicInfo.ParcelaPrincipalID}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ParcelaPrincipalIDWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            ParcelaPrincipalID = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ParcelaPrincipalIDWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            ParcelaPrincipalID = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#region Valor Filter Tests
    [Fact]
    public void WFiltro_Valor_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Valor = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Valor));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Valor);
    }

    [Fact]
    public void WFiltro_Valor_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorValue = 123.45m;
        var filtro = new FilterContaCorrente
        {
            Valor = ValorValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Valor}");
        ValorParameter.Should().NotBeNull();
        ValorParameter.Value.Should().Be(ValorValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Valor}] = @{DBContaCorrenteDicInfo.Valor}");
    }

    [Fact]
    public void WFiltro_Valor_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Valor = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Valor}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_Valor_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Valor = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Valor}]");
    }

    [Fact]
    public void WFiltro_Valor_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Valor = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_Valor_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Valor = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#region ValorPrincipal Filter Tests
    [Fact]
    public void WFiltro_ValorPrincipal_WhenDecimalMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ValorPrincipal = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.ValorPrincipal));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.ValorPrincipal);
    }

    [Fact]
    public void WFiltro_ValorPrincipal_WhenHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorPrincipalValue = 123.45m;
        var filtro = new FilterContaCorrente
        {
            ValorPrincipal = ValorPrincipalValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorPrincipalParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ValorPrincipal}");
        ValorPrincipalParameter.Should().NotBeNull();
        ValorPrincipalParameter.Value.Should().Be(ValorPrincipalValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ValorPrincipal}] = @{DBContaCorrenteDicInfo.ValorPrincipal}");
    }

    [Fact]
    public void WFiltro_ValorPrincipal_WhenZero_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ValorPrincipal = 0m,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        var param = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.ValorPrincipal}");
        param.Should().NotBeNull();
        param.Value.Should().Be(0m);
    }

    [Fact]
    public void WFiltro_ValorPrincipal_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ValorPrincipal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.ValorPrincipal}]");
    }

    [Fact]
    public void WFiltro_ValorPrincipal_WithOtherFilters_ShouldIncludeLogicalOperator()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            ValorPrincipal = 100.50m,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.And);
    }

    [Theory]
    [InlineData(0.01)]
    [InlineData(999999.99)]
    [InlineData(-100.50)]
    public void WFiltro_ValorPrincipal_WithVariousValues_ShouldAddCorrectParameter(decimal value)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            ValorPrincipal = value,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        param.Value.Should().Be(value);
    }

#endregion
#endregion
#region DateTime Tests
#region DtCad Filter Tests
    [Fact]
    public void WFiltro_DtCad_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DtCad}], 103)");
    }

    [Fact]
    public void WFiltro_DtCad_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtCad = testDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DtCad));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBContaCorrenteDicInfo.DtCad);
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtCad = startDate,
            DtCad_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtCad_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtCad = string.Empty,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtCad = null,
            DtCad_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtCad_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterContaCorrente
        {
            DtCad = minValueDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtCad_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtCad = inputDate,
            DtCad_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DtAtu Filter Tests
    [Fact]
    public void WFiltro_DtAtu_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DtAtu}], 103)");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtAtu = testDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DtAtu));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBContaCorrenteDicInfo.DtAtu);
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtAtu = startDate,
            DtAtu_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtAtu_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtAtu = string.Empty,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtAtu = null,
            DtAtu_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtAtu_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterContaCorrente
        {
            DtAtu = minValueDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtAtu_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtAtu = inputDate,
            DtAtu_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DtOriginal Filter Tests
    [Fact]
    public void WFiltro_DtOriginal_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtOriginal = testDate,
            DtOriginal_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DtOriginal}], 103)");
    }

    [Fact]
    public void WFiltro_DtOriginal_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtOriginal = testDate,
            DtOriginal_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DtOriginal));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBContaCorrenteDicInfo.DtOriginal);
    }

    [Fact]
    public void WFiltro_DtOriginal_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterContaCorrente
        {
            DtOriginal = startDate,
            DtOriginal_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DtOriginal_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtOriginal = string.Empty,
            DtOriginal_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtOriginal_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtOriginal = null,
            DtOriginal_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DtOriginal_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterContaCorrente
        {
            DtOriginal = minValueDate,
            DtOriginal_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DtOriginal_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DtOriginal = inputDate,
            DtOriginal_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region Data Filter Tests
    [Fact]
    public void WFiltro_Data_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Data}], 103)");
    }

    [Fact]
    public void WFiltro_Data_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            Data = testDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Data));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBContaCorrenteDicInfo.Data);
    }

    [Fact]
    public void WFiltro_Data_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterContaCorrente
        {
            Data = startDate,
            Data_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_Data_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Data = string.Empty,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Data = null,
            Data_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Data_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterContaCorrente
        {
            Data = minValueDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_Data_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Data = inputDate,
            Data_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#region DataPgto Filter Tests
    [Fact]
    public void WFiltro_DataPgto_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DataPgto = testDate,
            DataPgto_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"CONVERT(DATE,[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DataPgto}], 103)");
    }

    [Fact]
    public void WFiltro_DataPgto_WhenValidDateProvided_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var testDate = "15/06/2023";
        var filtro = new FilterContaCorrente
        {
            DataPgto = testDate,
            DataPgto_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var dateParam = result.Value.parametros.FirstOrDefault(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DataPgto));
        dateParam.Should().NotBeNull();
        // Verify the where clause contains the date field
        result.Value.where.Should().Contain(DBContaCorrenteDicInfo.DataPgto);
    }

    [Fact]
    public void WFiltro_DataPgto_WhenDateRangeProvided_ShouldAddBothParametersAndBetweenClause()
    {
        // Arrange
        var startDate = "01/06/2023";
        var endDate = "30/06/2023";
        var filtro = new FilterContaCorrente
        {
            DataPgto = startDate,
            DataPgto_end = endDate,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(2);
        result.Value.where.Should().Contain("BETWEEN");
    }

    [Fact]
    public void WFiltro_DataPgto_WhenEmptyString_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DataPgto = string.Empty,
            DataPgto_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DataPgto_WhenNullValue_ShouldNotAddParameters()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DataPgto = null,
            DataPgto_end = null,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DataPgto_WhenDateTimeMinValue_ShouldNotAddParameters()
    {
        // Arrange
        var minValueDate = DateTime.MinValue.ToString("dd/MM/yyyy");
        var filtro = new FilterContaCorrente
        {
            DataPgto = minValueDate,
            DataPgto_end = string.Empty,
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // The IsEmptyDX extension method should catch DateTime.MinValue and return true
        result.Value.parametros.Should().BeEmpty();
        result.Value.where.Should().BeEmpty();
    }

    [Theory]
    [InlineData("15/06/2023", "2023-06-15T00:00:00")]
    [InlineData("01/01/2024", "2024-01-01T00:00:00")]
    [InlineData("31/12/2022", "2022-12-31T00:00:00")]
    public void WFiltro_DataPgto_WhenVariousValidDateFormats_ShouldParseCorrectly(string inputDate, string expectedDateTimeString)
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DataPgto = inputDate,
            DataPgto_end = string.Empty,
            LogicalOperator = TSql.And
        };
        var expectedDateTime = DateTime.Parse(expectedDateTimeString);
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var parameter = result.Value.parametros.First();
        ((DateTime)parameter.Value).Should().Be(expectedDateTime);
    }

#endregion
#endregion
#region Bool Tests
#region Visto Filter Tests
    [Fact]
    public void WFiltro_VistoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Visto = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Visto));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Visto);
    }

    [Fact]
    public void WFiltro_VistoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var VistoValue = 1;
        var filtro = new FilterContaCorrente
        {
            Visto = VistoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var VistoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Visto}");
        VistoParameter.Should().NotBeNull();
        VistoParameter.Value.Should().Be(VistoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Visto}] = @{DBContaCorrenteDicInfo.Visto}");
    }

    [Fact]
    public void WFiltro_VistoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Visto = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Visto = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Visto}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Visto = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Visto = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Visto}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_VistoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Visto}] = @{DBContaCorrenteDicInfo.Visto}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_VistoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Visto = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Quitado Filter Tests
    [Fact]
    public void WFiltro_QuitadoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Quitado = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Quitado));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Quitado);
    }

    [Fact]
    public void WFiltro_QuitadoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var QuitadoValue = 1;
        var filtro = new FilterContaCorrente
        {
            Quitado = QuitadoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var QuitadoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Quitado}");
        QuitadoParameter.Should().NotBeNull();
        QuitadoParameter.Value.Should().Be(QuitadoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Quitado}] = @{DBContaCorrenteDicInfo.Quitado}");
    }

    [Fact]
    public void WFiltro_QuitadoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Quitado = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Quitado = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Quitado}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Quitado = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Quitado = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Quitado}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_QuitadoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Quitado = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Quitado}] = @{DBContaCorrenteDicInfo.Quitado}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_QuitadoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Quitado = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_QuitadoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Quitado = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Sucumbencia Filter Tests
    [Fact]
    public void WFiltro_SucumbenciaEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Sucumbencia = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Sucumbencia));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Sucumbencia);
    }

    [Fact]
    public void WFiltro_SucumbenciaHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var SucumbenciaValue = 1;
        var filtro = new FilterContaCorrente
        {
            Sucumbencia = SucumbenciaValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var SucumbenciaParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Sucumbencia}");
        SucumbenciaParameter.Should().NotBeNull();
        SucumbenciaParameter.Value.Should().Be(SucumbenciaValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Sucumbencia}] = @{DBContaCorrenteDicInfo.Sucumbencia}");
    }

    [Fact]
    public void WFiltro_SucumbenciaWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Sucumbencia = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Sucumbencia = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Sucumbencia}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Sucumbencia = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Sucumbencia = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Sucumbencia}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_SucumbenciaAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Sucumbencia = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Sucumbencia}] = @{DBContaCorrenteDicInfo.Sucumbencia}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_SucumbenciaWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Sucumbencia = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_SucumbenciaWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Sucumbencia = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region DistRegra Filter Tests
    [Fact]
    public void WFiltro_DistRegraEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DistRegra = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.DistRegra));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.DistRegra);
    }

    [Fact]
    public void WFiltro_DistRegraHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var DistRegraValue = 1;
        var filtro = new FilterContaCorrente
        {
            DistRegra = DistRegraValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var DistRegraParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DistRegra}");
        DistRegraParameter.Should().NotBeNull();
        DistRegraParameter.Value.Should().Be(DistRegraValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DistRegra}] = @{DBContaCorrenteDicInfo.DistRegra}");
    }

    [Fact]
    public void WFiltro_DistRegraWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for DistRegra = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            DistRegra = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DistRegra}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for DistRegra = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            DistRegra = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.DistRegra}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_DistRegraAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            DistRegra = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.DistRegra}] = @{DBContaCorrenteDicInfo.DistRegra}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_DistRegraWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            DistRegra = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_DistRegraWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            DistRegra = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Contrato Filter Tests
    [Fact]
    public void WFiltro_ContratoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Contrato = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Contrato));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Contrato);
    }

    [Fact]
    public void WFiltro_ContratoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ContratoValue = 1;
        var filtro = new FilterContaCorrente
        {
            Contrato = ContratoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ContratoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Contrato}");
        ContratoParameter.Should().NotBeNull();
        ContratoParameter.Value.Should().Be(ContratoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Contrato}] = @{DBContaCorrenteDicInfo.Contrato}");
    }

    [Fact]
    public void WFiltro_ContratoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Contrato = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Contrato = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Contrato}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Contrato = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Contrato = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Contrato}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ContratoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Contrato = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Contrato}] = @{DBContaCorrenteDicInfo.Contrato}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ContratoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Contrato = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ContratoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Contrato = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Pago Filter Tests
    [Fact]
    public void WFiltro_PagoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Pago = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Pago));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Pago);
    }

    [Fact]
    public void WFiltro_PagoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var PagoValue = 1;
        var filtro = new FilterContaCorrente
        {
            Pago = PagoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var PagoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Pago}");
        PagoParameter.Should().NotBeNull();
        PagoParameter.Value.Should().Be(PagoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Pago}] = @{DBContaCorrenteDicInfo.Pago}");
    }

    [Fact]
    public void WFiltro_PagoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Pago = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Pago = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Pago}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Pago = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Pago = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Pago}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_PagoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Pago = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Pago}] = @{DBContaCorrenteDicInfo.Pago}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_PagoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Pago = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_PagoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Pago = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Distribuir Filter Tests
    [Fact]
    public void WFiltro_DistribuirEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Distribuir = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Distribuir));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Distribuir);
    }

    [Fact]
    public void WFiltro_DistribuirHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var DistribuirValue = 1;
        var filtro = new FilterContaCorrente
        {
            Distribuir = DistribuirValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var DistribuirParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Distribuir}");
        DistribuirParameter.Should().NotBeNull();
        DistribuirParameter.Value.Should().Be(DistribuirValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Distribuir}] = @{DBContaCorrenteDicInfo.Distribuir}");
    }

    [Fact]
    public void WFiltro_DistribuirWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Distribuir = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Distribuir = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Distribuir}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Distribuir = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Distribuir = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Distribuir}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_DistribuirAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Distribuir = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Distribuir}] = @{DBContaCorrenteDicInfo.Distribuir}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_DistribuirWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Distribuir = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_DistribuirWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Distribuir = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region LC Filter Tests
    [Fact]
    public void WFiltro_LCEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            LC = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.LC));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.LC);
    }

    [Fact]
    public void WFiltro_LCHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var LCValue = 1;
        var filtro = new FilterContaCorrente
        {
            LC = LCValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var LCParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LC}");
        LCParameter.Should().NotBeNull();
        LCParameter.Value.Should().Be(LCValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.LC}] = @{DBContaCorrenteDicInfo.LC}");
    }

    [Fact]
    public void WFiltro_LCWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for LC = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            LC = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LC}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for LC = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            LC = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.LC}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_LCAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            LC = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.LC}] = @{DBContaCorrenteDicInfo.LC}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_LCWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            LC = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_LCWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            LC = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Hide Filter Tests
    [Fact]
    public void WFiltro_HideEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Hide = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBContaCorrenteDicInfo.Hide));
        result.Value.where.Should().NotContain(DBContaCorrenteDicInfo.Hide);
    }

    [Fact]
    public void WFiltro_HideHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var HideValue = 1;
        var filtro = new FilterContaCorrente
        {
            Hide = HideValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var HideParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Hide}");
        HideParameter.Should().NotBeNull();
        HideParameter.Value.Should().Be(HideValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Hide}] = @{DBContaCorrenteDicInfo.Hide}");
    }

    [Fact]
    public void WFiltro_HideWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Hide = 0 (typically female)
        var filtroFemale = new FilterContaCorrente
        {
            Hide = 0
        };
        var resultFemale = _serviceS.WFiltroContaCorrente(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Hide}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Hide = 1 (typically male)
        var filtroMale = new FilterContaCorrente
        {
            Hide = 1
        };
        var resultMale = _serviceS.WFiltroContaCorrente(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBContaCorrenteDicInfo.Hide}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_HideAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Hide = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBContaCorrenteDicInfo.PTabelaNome}].[{DBContaCorrenteDicInfo.Hide}] = @{DBContaCorrenteDicInfo.Hide}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_HideWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Hide = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_HideWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterContaCorrente
        {
            Guid = "Guid", // Campo string para testar combinação
            Hide = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroContaCorrente(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}