// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class TipoStatusBiuWriterTests
{
    private readonly Mock<IFTipoStatusBiuFactory> _mockTipoStatusBiuFactory;
    private readonly Mock<FTipoStatusBiu> _mockFTipoStatusBiu;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoStatusBiuWriter _tipostatusbiuWriter;
    public TipoStatusBiuWriterTests()
    {
        _mockTipoStatusBiuFactory = new Mock<IFTipoStatusBiuFactory>();
        _mockFTipoStatusBiu = new Mock<FTipoStatusBiu>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _tipostatusbiuWriter = new TipoStatusBiuWriter(_mockTipoStatusBiuFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var tipostatusbiu = CreateValidTipoStatusBiuModel();
        tipostatusbiu.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockTipoStatusBiuFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoStatusBiu.Object);
        _mockFTipoStatusBiu.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tipostatusbiuWriter.WriteAsync(tipostatusbiu, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoStatusBiu.Object);
        _mockTipoStatusBiuFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockTipoStatusBiuFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFTipoStatusBiu.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var tipostatusbiu = CreateValidTipoStatusBiuModel();
        tipostatusbiu.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockTipoStatusBiuFactory.Setup(x => x.CreateFromIdAsync(tipostatusbiu.Id, _mockConnection.Object)).ReturnsAsync(_mockFTipoStatusBiu.Object);
        _mockFTipoStatusBiu.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _tipostatusbiuWriter.WriteAsync(tipostatusbiu, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoStatusBiu.Object);
        _mockTipoStatusBiuFactory.Verify(x => x.CreateFromIdAsync(tipostatusbiu.Id, _mockConnection.Object), Times.Once);
        _mockTipoStatusBiuFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFTipoStatusBiu.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var tipostatusbiu = CreateValidTipoStatusBiuModel();
        var auditorQuem = 99;
        _mockTipoStatusBiuFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoStatusBiu.Object);
        _mockFTipoStatusBiu.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tipostatusbiuWriter.WriteAsync(tipostatusbiu, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoStatusBiu.Object);
        _mockFTipoStatusBiu.VerifySet(x => x.FNome = tipostatusbiu.Nome, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var tipostatusbiu = CreateValidTipoStatusBiuModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoStatusBiuFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipostatusbiuWriter.WriteAsync(tipostatusbiu, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tipostatusbiu = CreateValidTipoStatusBiuModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoStatusBiuFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoStatusBiu.Object);
        _mockFTipoStatusBiu.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipostatusbiuWriter.WriteAsync(tipostatusbiu, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var tipostatusbiuResponse = new TipoStatusBiuResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockTipoStatusBiuFactory.Setup(x => x.DeleteAsync(operadorId, tipostatusbiuResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _tipostatusbiuWriter.Delete(tipostatusbiuResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockTipoStatusBiuFactory.Verify(x => x.DeleteAsync(operadorId, tipostatusbiuResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var tipostatusbiuResponse = new TipoStatusBiuResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockTipoStatusBiuFactory.Setup(x => x.DeleteAsync(operadorId, tipostatusbiuResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _tipostatusbiuWriter.Delete(tipostatusbiuResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tipostatusbiuResponse = new TipoStatusBiuResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockTipoStatusBiuFactory.Setup(x => x.DeleteAsync(operadorId, tipostatusbiuResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tipostatusbiuWriter.Delete(tipostatusbiuResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new TipoStatusBiuWriter(_mockTipoStatusBiuFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new TipoStatusBiuWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.TipoStatusBiu CreateValidTipoStatusBiuModel()
    {
        return new Models.TipoStatusBiu
        {
            Id = 0,
            Nome = "João"
        };
    }
#endregion
}