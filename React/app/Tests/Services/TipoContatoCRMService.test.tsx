// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoContatoCRMService, TipoContatoCRMValidator } from '@/app/GerAdv_TS/TipoContatoCRM/Services/TipoContatoCRM.service';
import { TipoContatoCRMApi, TipoContatoCRMApiError } from '@/app/GerAdv_TS/TipoContatoCRM/Apis/ApiTipoContatoCRM';
import { ITipoContatoCRM } from '@/app/GerAdv_TS/TipoContatoCRM/Interfaces/interface.TipoContatoCRM';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoContatoCRMApi;

const sample: ITipoContatoCRM = { id: 1, nome: 'Teste' };

describe('TipoContatoCRMService', () => {
  let service: TipoContatoCRMService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoContatoCRMService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoContatoCRMValidator, 'validateTipoContatoCRM').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoContatoCRMById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoContatoCRMById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoContatoCRMById lança TipoContatoCRMApiError para id inválido', async () => {
    await expect(service.fetchTipoContatoCRMById(0)).rejects.toBeInstanceOf(TipoContatoCRMApiError);
  });

  it('saveTipoContatoCRM chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoContatoCRM(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoContatoCRM lança TipoContatoCRMApiError quando validação falha', async () => {
    (TipoContatoCRMValidator.validateTipoContatoCRM as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoContatoCRM(sample)).rejects.toBeInstanceOf(TipoContatoCRMApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoContatoCRM chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoContatoCRM(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoContatoCRM lança TipoContatoCRMApiError para id inválido', async () => {
    await expect(service.deleteTipoContatoCRM(0)).rejects.toBeInstanceOf(TipoContatoCRMApiError);
  });

  it('validateTipoContatoCRM delega para TipoContatoCRMValidator', () => {
    (TipoContatoCRMValidator.validateTipoContatoCRM as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoContatoCRM(sample);
    expect(r.isValid).toBe(true);
  });
});
