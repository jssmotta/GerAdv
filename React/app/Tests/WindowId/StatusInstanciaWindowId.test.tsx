// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import StatusInstanciaWindowId from '@/app/GerAdv_TS/StatusInstancia/Crud/Grids/StatusInstanciaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import StatusInstanciaWindowId from '@/app/GerAdv_TS/StatusInstancia/Crud/Grids/StatusInstanciaWindowId';
import { StatusInstanciaEmpty } from '@/app/GerAdv_TS/Models/StatusInstancia';

describe('StatusInstanciaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(StatusInstanciaWindowId).toBeDefined();
    expect(typeof StatusInstanciaWindowId === 'function' || typeof StatusInstanciaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the StatusInstanciaService class so constructor usage doesn't throw and we can control fetchStatusInstanciaById
const mockFetchStatusInstanciaById = jest.fn();
jest.mock('@/app/GerAdv_TS/StatusInstancia/Services/StatusInstancia.service', () => ({
  StatusInstanciaService: jest.fn().mockImplementation(() => ({
    fetchStatusInstanciaById: (...args: any[]) => mockFetchStatusInstanciaById(...args),
  })),
}));

// Mock the child StatusInstanciaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/StatusInstancia/Crud/Grids/StatusInstanciaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='statusinstancia-window' {...p} />,
}));

describe('StatusInstanciaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(StatusInstanciaWindowId).toBeDefined();
    expect(typeof StatusInstanciaWindowId === 'function' || typeof StatusInstanciaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <StatusInstanciaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders StatusInstanciaWindow with StatusInstanciaEmpty when id is 0', async () => {
    render(
      <StatusInstanciaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // StatusInstanciaWindow should be rendered with selectedStatusInstancia equal to StatusInstanciaEmpty()
    const node = await screen.findByTestId('statusinstancia-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedStatusInstancia"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedstatusinstancia') || node.getAttribute('selectedStatusInstancia')).not.toBeNull();
  });

  it('fetches statusinstancia by id and passes it to StatusInstanciaWindow when id > 0', async () => {
    const fakeStatusInstancia = { id: 123, nomecliente: 'Test' };
    mockFetchStatusInstanciaById.mockResolvedValue(fakeStatusInstancia);

    render(
      <StatusInstanciaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('statusinstancia-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchStatusInstanciaById).toHaveBeenCalledWith(123));
  });
});
