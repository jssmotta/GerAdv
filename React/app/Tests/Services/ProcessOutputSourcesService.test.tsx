// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProcessOutputSourcesService, ProcessOutputSourcesValidator } from '@/app/GerAdv_TS/ProcessOutputSources/Services/ProcessOutputSources.service';
import { ProcessOutputSourcesApi, ProcessOutputSourcesApiError } from '@/app/GerAdv_TS/ProcessOutputSources/Apis/ApiProcessOutputSources';
import { IProcessOutputSources } from '@/app/GerAdv_TS/ProcessOutputSources/Interfaces/interface.ProcessOutputSources';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProcessOutputSourcesApi;

const sample: IProcessOutputSources = { id: 1, nome: 'Teste' };

describe('ProcessOutputSourcesService', () => {
  let service: ProcessOutputSourcesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProcessOutputSourcesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProcessOutputSourcesValidator, 'validateProcessOutputSources').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProcessOutputSourcesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProcessOutputSourcesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProcessOutputSourcesById lança ProcessOutputSourcesApiError para id inválido', async () => {
    await expect(service.fetchProcessOutputSourcesById(0)).rejects.toBeInstanceOf(ProcessOutputSourcesApiError);
  });

  it('saveProcessOutputSources chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProcessOutputSources(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProcessOutputSources lança ProcessOutputSourcesApiError quando validação falha', async () => {
    (ProcessOutputSourcesValidator.validateProcessOutputSources as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProcessOutputSources(sample)).rejects.toBeInstanceOf(ProcessOutputSourcesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProcessOutputSources chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProcessOutputSources(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProcessOutputSources lança ProcessOutputSourcesApiError para id inválido', async () => {
    await expect(service.deleteProcessOutputSources(0)).rejects.toBeInstanceOf(ProcessOutputSourcesApiError);
  });

  it('validateProcessOutputSources delega para ProcessOutputSourcesValidator', () => {
    (ProcessOutputSourcesValidator.validateProcessOutputSources as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProcessOutputSources(sample);
    expect(r.isValid).toBe(true);
  });
});
