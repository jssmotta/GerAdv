// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTPeriodicidadeStatusService, GUTPeriodicidadeStatusValidator } from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Services/GUTPeriodicidadeStatus.service';
import { GUTPeriodicidadeStatusApi, GUTPeriodicidadeStatusApiError } from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Apis/ApiGUTPeriodicidadeStatus';
import { IGUTPeriodicidadeStatus } from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Interfaces/interface.GUTPeriodicidadeStatus';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTPeriodicidadeStatusApi;

const sample: IGUTPeriodicidadeStatus = { id: 1, nome: 'Teste' };

describe('GUTPeriodicidadeStatusService', () => {
  let service: GUTPeriodicidadeStatusService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTPeriodicidadeStatusService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTPeriodicidadeStatusValidator, 'validateGUTPeriodicidadeStatus').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTPeriodicidadeStatusById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTPeriodicidadeStatusById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTPeriodicidadeStatusById lança GUTPeriodicidadeStatusApiError para id inválido', async () => {
    await expect(service.fetchGUTPeriodicidadeStatusById(0)).rejects.toBeInstanceOf(GUTPeriodicidadeStatusApiError);
  });

  it('saveGUTPeriodicidadeStatus chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTPeriodicidadeStatus(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTPeriodicidadeStatus lança GUTPeriodicidadeStatusApiError quando validação falha', async () => {
    (GUTPeriodicidadeStatusValidator.validateGUTPeriodicidadeStatus as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTPeriodicidadeStatus(sample)).rejects.toBeInstanceOf(GUTPeriodicidadeStatusApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTPeriodicidadeStatus chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTPeriodicidadeStatus(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTPeriodicidadeStatus lança GUTPeriodicidadeStatusApiError para id inválido', async () => {
    await expect(service.deleteGUTPeriodicidadeStatus(0)).rejects.toBeInstanceOf(GUTPeriodicidadeStatusApiError);
  });

  it('validateGUTPeriodicidadeStatus delega para GUTPeriodicidadeStatusValidator', () => {
    (GUTPeriodicidadeStatusValidator.validateGUTPeriodicidadeStatus as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTPeriodicidadeStatus(sample);
    expect(r.isValid).toBe(true);
  });
});
