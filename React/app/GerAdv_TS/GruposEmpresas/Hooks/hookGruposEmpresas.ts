// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IGruposEmpresasService } from '../Services/GruposEmpresas.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IGruposEmpresas } from '../Interfaces/interface.GruposEmpresas';
import { FilterGruposEmpresas } from '../Filters/GruposEmpresas';
import { isValidDate } from '@/app/tools/datetime';
import { GruposEmpresasApi } from '../Apis/ApiGruposEmpresas';

export const useGruposEmpresasForm = (
  initialGruposEmpresas: IGruposEmpresas,
  dataService: IGruposEmpresasService
) => {
  const [data, setData] = useState<IGruposEmpresas>(initialGruposEmpresas);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadGruposEmpresas = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialGruposEmpresas);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchGruposEmpresasById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Grupos Empresas';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar Grupos Empresas');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialGruposEmpresas]);

  const resetForm = useCallback(() => {
    setData(initialGruposEmpresas);
    setError(null);
  }, [initialGruposEmpresas]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadGruposEmpresas,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadGruposEmpresas, resetForm]);
  return returnValue;
};


export const useGruposEmpresasNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('GruposEmpresas', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
            console.log('Erro no listener de notificações.');
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useGruposEmpresasList = (dataService: IGruposEmpresasService, currentFilter?: FilterGruposEmpresas | undefined | null) => {
  const [data, setData] = useState<IGruposEmpresas[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterGruposEmpresas | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterGruposEmpresas | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterGruposEmpresas);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar gruposempresas';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar gruposempresas');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useGruposEmpresasNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsGruposEmpresas() {

  async function runValidation(data: IGruposEmpresas, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const gruposempresasApi = new GruposEmpresasApi(uri ?? '', token ?? '');

    const result = await gruposempresasApi.validation(data);

    return result;
  }

  function validate(data: IGruposEmpresas): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.descricao.length <= 0) { 
                                             return { isValid: false, message: 'O campo Descricao não pode ficar vazio.' };
                                         } 
if (data.email.length > 255) { 
                                             return { isValid: false, message: 'O campo EMail não pode ter mais de 255 caracteres.' };
                                         } 
if (data.descricao.length > 50) { 
                                             return { isValid: false, message: 'O campo Descricao não pode ter mais de 50 caracteres.' };
                                         } 
if (data.observacoes.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo Observacoes não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.icone.length > 255) { 
                                             return { isValid: false, message: 'O campo Icone não pode ter mais de 255 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}export const useGruposEmpresasComboBox = (
  dataService: IGruposEmpresasService,
  initialValue?: any
) => {
  const [options, setOptions] = useState<any[]>([]);
  const [filteredOptions, setFilteredOptions] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const [selectedValue, setSelectedValue] = useState(initialValue);
  const [hasLoaded, setHasLoaded] = useState(false);

  const fetchOptions = useCallback(async () => {
    if (loading) return; // Evita múltiplas requisições simultâneas
    
    setLoading(true);
    try {
      const response = await dataService.getList();
      const mappedOptions = response.map(item => ({
        id: item.id,
        nome: item.descricao
      }));
      setOptions(mappedOptions);
      setFilteredOptions(mappedOptions);
      setHasLoaded(true);
    } catch (err) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
          console.log('Erro ao buscar opções do ComboBox');
    } finally {
      setLoading(false);
    }
  }, [dataService, loading]);

  const handleFilter = useCallback((filterText: string) => {
    if (!filterText) {
      setFilteredOptions(options);
      return;
    }
    
    const filter = filterText.toLowerCase();
    const filtered = options.filter(option =>
      option.nome.toLowerCase().includes(filter)
    );
    setFilteredOptions(filtered);
  }, [options]);

  const handleValueChange = useCallback((newValue: any) => {
    setSelectedValue(newValue);
  }, []);
  
  useEffect(() => {
    if (!hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  const refreshCallback = useCallback(() => {
    if (hasLoaded) {
      fetchOptions();
    }
  }, [fetchOptions, hasLoaded]);

  useGruposEmpresasNotifications(
    refreshCallback, // onUpdate
    refreshCallback, // onDelete
    refreshCallback  // onAdd
  );

  return {
    options: filteredOptions,
    loading,
    selectedValue,
    handleFilter,
    handleValueChange,
    refreshOptions: fetchOptions,
    clearValue: () => setSelectedValue(null)
  };
};