// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import InstanciaWindowId from '@/app/GerAdv_TS/Instancia/Crud/Grids/InstanciaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import InstanciaWindowId from '@/app/GerAdv_TS/Instancia/Crud/Grids/InstanciaWindowId';
import { InstanciaEmpty } from '@/app/GerAdv_TS/Models/Instancia';

describe('InstanciaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(InstanciaWindowId).toBeDefined();
    expect(typeof InstanciaWindowId === 'function' || typeof InstanciaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the InstanciaService class so constructor usage doesn't throw and we can control fetchInstanciaById
const mockFetchInstanciaById = jest.fn();
jest.mock('@/app/GerAdv_TS/Instancia/Services/Instancia.service', () => ({
  InstanciaService: jest.fn().mockImplementation(() => ({
    fetchInstanciaById: (...args: any[]) => mockFetchInstanciaById(...args),
  })),
}));

// Mock the child InstanciaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Instancia/Crud/Grids/InstanciaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='instancia-window' {...p} />,
}));

describe('InstanciaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(InstanciaWindowId).toBeDefined();
    expect(typeof InstanciaWindowId === 'function' || typeof InstanciaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <InstanciaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders InstanciaWindow with InstanciaEmpty when id is 0', async () => {
    render(
      <InstanciaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // InstanciaWindow should be rendered with selectedInstancia equal to InstanciaEmpty()
    const node = await screen.findByTestId('instancia-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedInstancia"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedinstancia') || node.getAttribute('selectedInstancia')).not.toBeNull();
  });

  it('fetches instancia by id and passes it to InstanciaWindow when id > 0', async () => {
    const fakeInstancia = { id: 123, nomecliente: 'Test' };
    mockFetchInstanciaById.mockResolvedValue(fakeInstancia);

    render(
      <InstanciaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('instancia-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchInstanciaById).toHaveBeenCalledWith(123));
  });
});
