// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTTipoService, GUTTipoValidator } from '@/app/GerAdv_TS/GUTTipo/Services/GUTTipo.service';
import { GUTTipoApi, GUTTipoApiError } from '@/app/GerAdv_TS/GUTTipo/Apis/ApiGUTTipo';
import { IGUTTipo } from '@/app/GerAdv_TS/GUTTipo/Interfaces/interface.GUTTipo';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTTipoApi;

const sample: IGUTTipo = { id: 1, nome: 'Teste' };

describe('GUTTipoService', () => {
  let service: GUTTipoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTTipoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTTipoValidator, 'validateGUTTipo').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTTipoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTTipoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTTipoById lança GUTTipoApiError para id inválido', async () => {
    await expect(service.fetchGUTTipoById(0)).rejects.toBeInstanceOf(GUTTipoApiError);
  });

  it('saveGUTTipo chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTTipo(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTTipo lança GUTTipoApiError quando validação falha', async () => {
    (GUTTipoValidator.validateGUTTipo as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTTipo(sample)).rejects.toBeInstanceOf(GUTTipoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTTipo chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTTipo(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTTipo lança GUTTipoApiError para id inválido', async () => {
    await expect(service.deleteGUTTipo(0)).rejects.toBeInstanceOf(GUTTipoApiError);
  });

  it('validateGUTTipo delega para GUTTipoValidator', () => {
    (GUTTipoValidator.validateGUTTipo as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTTipo(sample);
    expect(r.isValid).toBe(true);
  });
});
