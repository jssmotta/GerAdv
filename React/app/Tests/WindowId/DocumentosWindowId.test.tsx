// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import DocumentosWindowId from '@/app/GerAdv_TS/Documentos/Crud/Grids/DocumentosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import DocumentosWindowId from '@/app/GerAdv_TS/Documentos/Crud/Grids/DocumentosWindowId';
import { DocumentosEmpty } from '@/app/GerAdv_TS/Models/Documentos';

describe('DocumentosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(DocumentosWindowId).toBeDefined();
    expect(typeof DocumentosWindowId === 'function' || typeof DocumentosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the DocumentosService class so constructor usage doesn't throw and we can control fetchDocumentosById
const mockFetchDocumentosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Documentos/Services/Documentos.service', () => ({
  DocumentosService: jest.fn().mockImplementation(() => ({
    fetchDocumentosById: (...args: any[]) => mockFetchDocumentosById(...args),
  })),
}));

// Mock the child DocumentosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Documentos/Crud/Grids/DocumentosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='documentos-window' {...p} />,
}));

describe('DocumentosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(DocumentosWindowId).toBeDefined();
    expect(typeof DocumentosWindowId === 'function' || typeof DocumentosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <DocumentosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders DocumentosWindow with DocumentosEmpty when id is 0', async () => {
    render(
      <DocumentosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // DocumentosWindow should be rendered with selectedDocumentos equal to DocumentosEmpty()
    const node = await screen.findByTestId('documentos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedDocumentos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selecteddocumentos') || node.getAttribute('selectedDocumentos')).not.toBeNull();
  });

  it('fetches documentos by id and passes it to DocumentosWindow when id > 0', async () => {
    const fakeDocumentos = { id: 123, nomecliente: 'Test' };
    mockFetchDocumentosById.mockResolvedValue(fakeDocumentos);

    render(
      <DocumentosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('documentos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchDocumentosById).toHaveBeenCalledWith(123));
  });
});
