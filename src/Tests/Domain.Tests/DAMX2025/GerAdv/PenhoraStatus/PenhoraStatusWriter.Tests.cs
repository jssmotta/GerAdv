// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class PenhoraStatusWriterTests
{
    private readonly Mock<IFPenhoraStatusFactory> _mockPenhoraStatusFactory;
    private readonly Mock<FPenhoraStatus> _mockFPenhoraStatus;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly PenhoraStatusWriter _penhorastatusWriter;
    public PenhoraStatusWriterTests()
    {
        _mockPenhoraStatusFactory = new Mock<IFPenhoraStatusFactory>();
        _mockFPenhoraStatus = new Mock<FPenhoraStatus>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _penhorastatusWriter = new PenhoraStatusWriter(_mockPenhoraStatusFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var penhorastatus = CreateValidPenhoraStatusModel();
        penhorastatus.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockPenhoraStatusFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFPenhoraStatus.Object);
        _mockFPenhoraStatus.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _penhorastatusWriter.WriteAsync(penhorastatus, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFPenhoraStatus.Object);
        _mockPenhoraStatusFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockPenhoraStatusFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFPenhoraStatus.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var penhorastatus = CreateValidPenhoraStatusModel();
        penhorastatus.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockPenhoraStatusFactory.Setup(x => x.CreateFromIdAsync(penhorastatus.Id, _mockConnection.Object)).ReturnsAsync(_mockFPenhoraStatus.Object);
        _mockFPenhoraStatus.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _penhorastatusWriter.WriteAsync(penhorastatus, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFPenhoraStatus.Object);
        _mockPenhoraStatusFactory.Verify(x => x.CreateFromIdAsync(penhorastatus.Id, _mockConnection.Object), Times.Once);
        _mockPenhoraStatusFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFPenhoraStatus.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var penhorastatus = CreateValidPenhoraStatusModel();
        var auditorQuem = 99;
        _mockPenhoraStatusFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFPenhoraStatus.Object);
        _mockFPenhoraStatus.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _penhorastatusWriter.WriteAsync(penhorastatus, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFPenhoraStatus.Object);
        _mockFPenhoraStatus.VerifySet(x => x.FNome = penhorastatus.Nome, Times.Once);
        _mockFPenhoraStatus.VerifySet(x => x.FGUID = penhorastatus.GUID, Times.Once);
        _mockFPenhoraStatus.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var penhorastatus = CreateValidPenhoraStatusModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockPenhoraStatusFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _penhorastatusWriter.WriteAsync(penhorastatus, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var penhorastatus = CreateValidPenhoraStatusModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockPenhoraStatusFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFPenhoraStatus.Object);
        _mockFPenhoraStatus.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _penhorastatusWriter.WriteAsync(penhorastatus, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var penhorastatusResponse = new PenhoraStatusResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockPenhoraStatusFactory.Setup(x => x.DeleteAsync(operadorId, penhorastatusResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _penhorastatusWriter.Delete(penhorastatusResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockPenhoraStatusFactory.Verify(x => x.DeleteAsync(operadorId, penhorastatusResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var penhorastatusResponse = new PenhoraStatusResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockPenhoraStatusFactory.Setup(x => x.DeleteAsync(operadorId, penhorastatusResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _penhorastatusWriter.Delete(penhorastatusResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var penhorastatusResponse = new PenhoraStatusResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockPenhoraStatusFactory.Setup(x => x.DeleteAsync(operadorId, penhorastatusResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _penhorastatusWriter.Delete(penhorastatusResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new PenhoraStatusWriter(_mockPenhoraStatusFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new PenhoraStatusWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.PenhoraStatus CreateValidPenhoraStatusModel()
    {
        return new Models.PenhoraStatus
        {
            Id = 0,
            Nome = "João",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}