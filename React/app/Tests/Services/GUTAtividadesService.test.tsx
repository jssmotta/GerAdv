// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GUTAtividadesService, GUTAtividadesValidator } from '@/app/GerAdv_TS/GUTAtividades/Services/GUTAtividades.service';
import { GUTAtividadesApi, GUTAtividadesApiError } from '@/app/GerAdv_TS/GUTAtividades/Apis/ApiGUTAtividades';
import { IGUTAtividades } from '@/app/GerAdv_TS/GUTAtividades/Interfaces/interface.GUTAtividades';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GUTAtividadesApi;

const sample: IGUTAtividades = { id: 1, nome: 'Teste' };

describe('GUTAtividadesService', () => {
  let service: GUTAtividadesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GUTAtividadesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GUTAtividadesValidator, 'validateGUTAtividades').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGUTAtividadesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGUTAtividadesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGUTAtividadesById lança GUTAtividadesApiError para id inválido', async () => {
    await expect(service.fetchGUTAtividadesById(0)).rejects.toBeInstanceOf(GUTAtividadesApiError);
  });

  it('saveGUTAtividades chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGUTAtividades(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGUTAtividades lança GUTAtividadesApiError quando validação falha', async () => {
    (GUTAtividadesValidator.validateGUTAtividades as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGUTAtividades(sample)).rejects.toBeInstanceOf(GUTAtividadesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGUTAtividades chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGUTAtividades(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGUTAtividades lança GUTAtividadesApiError para id inválido', async () => {
    await expect(service.deleteGUTAtividades(0)).rejects.toBeInstanceOf(GUTAtividadesApiError);
  });

  it('validateGUTAtividades delega para GUTAtividadesValidator', () => {
    (GUTAtividadesValidator.validateGUTAtividades as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGUTAtividades(sample);
    expect(r.isValid).toBe(true);
  });
});
