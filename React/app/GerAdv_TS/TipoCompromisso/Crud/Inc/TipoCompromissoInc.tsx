// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking: CrudInc.tsx.txt
"use client";
import React, { useEffect, useRef } from "react";
import { useRouter } from "next/navigation";
import { TipoCompromissoApi } from "../../Apis/TipoCompromissoApi";
import { useIsMobile } from "@/app/context/MobileContext";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { NotificationService } from "@/app/services/notification.service";
import { NotificationComponent } from "@/app/components/Cruds/NotificationComponent";
import { ITipoCompromissoFormProps } from "../../Interfaces/interface.TipoCompromisso";
import { TipoCompromissoService } from "../../Services/TipoCompromisso.service";
import {
  useTipoCompromissoForm,
  useValidationsTipoCompromisso,
} from "../../Hooks/hookTipoCompromisso";
import { TipoCompromissoEmpty } from "../../../Models/TipoCompromisso";
import { TipoCompromissoForm } from "../Forms/TipoCompromissoForm";

const TipoCompromissoInc: React.FC<ITipoCompromissoFormProps> = ({
  id,
  onClose,
  onError,
  onSuccess,
}) => {
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const router = useRouter();

  const tipocompromissoService = new TipoCompromissoService(
    new TipoCompromissoApi(
      systemContext?.Uri ?? "",
      systemContext?.Token ?? "",
    ),
  );
  const notificationService = new NotificationService();

  const { data, handleChange, loadTipoCompromisso } = useTipoCompromissoForm(
    TipoCompromissoEmpty(),
    tipocompromissoService,
  );

  useEffect(() => {
    loadTipoCompromisso(id);
  }, [id]);

  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    return () => {
      if (timerRef.current) clearTimeout(timerRef.current);
    };
  }, []);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    try {
      const savedTipoCompromisso =
        await tipocompromissoService.saveTipoCompromisso(data);

      if (savedTipoCompromisso.id) {
        notificationService.showNotification(
          "Registro salvo com sucesso!",
          "success",
        );

        const PDelayApiWrite = 333;

        timerRef.current = setTimeout(() => {
          if (onSuccess) {
            onSuccess(savedTipoCompromisso);
          }

          if (isMobile) {
            onClose();
          }
        }, PDelayApiWrite);
      } else {
        if (onError) {
          onError();
        }
        notificationService.showNotification(
          "Error salvando registro.",
          "error",
        );
      }
    } catch (error) {
      if (onError) {
        onError();
      }
      notificationService.showNotification("Error salvando registro.", "error");
    }
  };

  const handleReload = () => {
    loadTipoCompromisso(id);
  };

  return (
    <>
      <NotificationComponent notificationService={notificationService} />
      <TipoCompromissoForm
        tipocompromissoData={data}
        onChange={handleChange}
        onSubmit={handleSubmit}
        onClose={onClose}
        onError={onError}
        onReload={handleReload}
        onSuccess={onSuccess}
      />
    </>
  );
};

export default TipoCompromissoInc;
