// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import LigacoesWindowId from '@/app/GerAdv_TS/Ligacoes/Crud/Grids/LigacoesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import LigacoesWindowId from '@/app/GerAdv_TS/Ligacoes/Crud/Grids/LigacoesWindowId';
import { LigacoesEmpty } from '@/app/GerAdv_TS/Models/Ligacoes';

describe('LigacoesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(LigacoesWindowId).toBeDefined();
    expect(typeof LigacoesWindowId === 'function' || typeof LigacoesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the LigacoesService class so constructor usage doesn't throw and we can control fetchLigacoesById
const mockFetchLigacoesById = jest.fn();
jest.mock('@/app/GerAdv_TS/Ligacoes/Services/Ligacoes.service', () => ({
  LigacoesService: jest.fn().mockImplementation(() => ({
    fetchLigacoesById: (...args: any[]) => mockFetchLigacoesById(...args),
  })),
}));

// Mock the child LigacoesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Ligacoes/Crud/Grids/LigacoesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='ligacoes-window' {...p} />,
}));

describe('LigacoesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(LigacoesWindowId).toBeDefined();
    expect(typeof LigacoesWindowId === 'function' || typeof LigacoesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <LigacoesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders LigacoesWindow with LigacoesEmpty when id is 0', async () => {
    render(
      <LigacoesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // LigacoesWindow should be rendered with selectedLigacoes equal to LigacoesEmpty()
    const node = await screen.findByTestId('ligacoes-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedLigacoes"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedligacoes') || node.getAttribute('selectedLigacoes')).not.toBeNull();
  });

  it('fetches ligacoes by id and passes it to LigacoesWindow when id > 0', async () => {
    const fakeLigacoes = { id: 123, nomecliente: 'Test' };
    mockFetchLigacoesById.mockResolvedValue(fakeLigacoes);

    render(
      <LigacoesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('ligacoes-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchLigacoesById).toHaveBeenCalledWith(123));
  });
});
