// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IProcessOutputRequestService } from '../Services/ProcessOutputRequest.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IProcessOutputRequest } from '../Interfaces/interface.ProcessOutputRequest';
import { FilterProcessOutputRequest } from '../Filters/ProcessOutputRequest';
import { isValidDate } from '@/app/tools/datetime';
import { ProcessOutputRequestApi } from '../Apis/ApiProcessOutputRequest';

export const useProcessOutputRequestForm = (
  initialProcessOutputRequest: IProcessOutputRequest,
  dataService: IProcessOutputRequestService
) => {
  const [data, setData] = useState<IProcessOutputRequest>(initialProcessOutputRequest);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadProcessOutputRequest = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialProcessOutputRequest);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchProcessOutputRequestById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Process Output Request';
      setError(errorMessage);
      //console.log('Erro ao carregar Process Output Request');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialProcessOutputRequest]);

  const resetForm = useCallback(() => {
    setData(initialProcessOutputRequest);
    setError(null);
  }, [initialProcessOutputRequest]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadProcessOutputRequest,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadProcessOutputRequest, resetForm]);
  return returnValue;
};


export const useProcessOutputRequestNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('ProcessOutputRequest', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useProcessOutputRequestList = (dataService: IProcessOutputRequestService, currentFilter?: FilterProcessOutputRequest | undefined | null) => {
  const [data, setData] = useState<IProcessOutputRequest[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterProcessOutputRequest | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterProcessOutputRequest | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterProcessOutputRequest);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar processoutputrequest';
      setError(errorMessage);
      //console.log('Erro ao carregar processoutputrequest');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useProcessOutputRequestNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsProcessOutputRequest() {

  async function runValidation(data: IProcessOutputRequest, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const processoutputrequestApi = new ProcessOutputRequestApi(uri ?? '', token ?? '');

    const result = await processoutputrequestApi.validation(data);

    return result;
  }

  function validate(data: IProcessOutputRequest): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.guid.length <= 0) { 
                                             return { isValid: false, message: 'O campo GUID não pode ficar vazio.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}