// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoRecursoWindowId from '@/app/GerAdv_TS/TipoRecurso/Crud/Grids/TipoRecursoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoRecursoWindowId from '@/app/GerAdv_TS/TipoRecurso/Crud/Grids/TipoRecursoWindowId';
import { TipoRecursoEmpty } from '@/app/GerAdv_TS/Models/TipoRecurso';

describe('TipoRecursoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoRecursoWindowId).toBeDefined();
    expect(typeof TipoRecursoWindowId === 'function' || typeof TipoRecursoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoRecursoService class so constructor usage doesn't throw and we can control fetchTipoRecursoById
const mockFetchTipoRecursoById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoRecurso/Services/TipoRecurso.service', () => ({
  TipoRecursoService: jest.fn().mockImplementation(() => ({
    fetchTipoRecursoById: (...args: any[]) => mockFetchTipoRecursoById(...args),
  })),
}));

// Mock the child TipoRecursoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoRecurso/Crud/Grids/TipoRecursoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tiporecurso-window' {...p} />,
}));

describe('TipoRecursoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoRecursoWindowId).toBeDefined();
    expect(typeof TipoRecursoWindowId === 'function' || typeof TipoRecursoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoRecursoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoRecursoWindow with TipoRecursoEmpty when id is 0', async () => {
    render(
      <TipoRecursoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoRecursoWindow should be rendered with selectedTipoRecurso equal to TipoRecursoEmpty()
    const node = await screen.findByTestId('tiporecurso-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoRecurso"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtiporecurso') || node.getAttribute('selectedTipoRecurso')).not.toBeNull();
  });

  it('fetches tiporecurso by id and passes it to TipoRecursoWindow when id > 0', async () => {
    const fakeTipoRecurso = { id: 123, nomecliente: 'Test' };
    mockFetchTipoRecursoById.mockResolvedValue(fakeTipoRecurso);

    render(
      <TipoRecursoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tiporecurso-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoRecursoById).toHaveBeenCalledWith(123));
  });
});
