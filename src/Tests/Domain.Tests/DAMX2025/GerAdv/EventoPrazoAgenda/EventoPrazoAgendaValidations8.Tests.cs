// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class EventoPrazoAgendaValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFEventoPrazoAgendaFactory> _mockEventoPrazoAgendaFactory;
    private readonly Mock<IEventoPrazoAgendaReader> _mockReader;
    private readonly EventoPrazoAgendaValidation _validation;
    private readonly Mock<IEventoPrazoAgendaService> _mockEventoPrazoAgendaService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly EventoPrazoAgendaService _service;
    private readonly string _validUri = "test-uri";
    public EventoPrazoAgendaValidationTests()
    {
        _mockEventoPrazoAgendaFactory = new Mock<IFEventoPrazoAgendaFactory>();
        _mockReader = new Mock<IEventoPrazoAgendaReader>();
        _validation = new EventoPrazoAgendaValidation();
        _mockEventoPrazoAgendaService = new Mock<IEventoPrazoAgendaService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void EventoPrazoAgendaValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IEventoPrazoAgendaValidation>();
    }

    [Fact]
    public void EventoPrazoAgendaValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(EventoPrazoAgendaValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void EventoPrazoAgendaValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(EventoPrazoAgendaValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.EventoPrazoAgenda CreateValidEventoPrazoAgenda()
    {
        return new Models.EventoPrazoAgenda
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockEventoPrazoAgendaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterEventoPrazoAgenda>(), It.IsAny<string>())).ReturnsAsync(new List<EventoPrazoAgendaResponseAll>());
    // Setup other mocks but don't override the EventoPrazoAgendas service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockEventoPrazoAgendaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterEventoPrazoAgenda>(), It.IsAny<string>())).ReturnsAsync(new List<EventoPrazoAgendaResponseAll>());
    // Setup other mocks but don't override the EventoPrazoAgendas service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var eventoprazoagenda = new Models.EventoPrazoAgenda
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(eventoprazoagenda, _mockEventoPrazoAgendaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockEventoPrazoAgendaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}