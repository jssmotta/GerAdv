// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class StatusInstanciaValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFStatusInstanciaFactory> _mockStatusInstanciaFactory;
    private readonly Mock<IStatusInstanciaReader> _mockReader;
    private readonly StatusInstanciaValidation _validation;
    private readonly Mock<IStatusInstanciaService> _mockStatusInstanciaService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly StatusInstanciaService _service;
    private readonly string _validUri = "test-uri";
    public StatusInstanciaValidationTests()
    {
        _mockStatusInstanciaFactory = new Mock<IFStatusInstanciaFactory>();
        _mockReader = new Mock<IStatusInstanciaReader>();
        _validation = new StatusInstanciaValidation();
        _mockStatusInstanciaService = new Mock<IStatusInstanciaService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void StatusInstanciaValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IStatusInstanciaValidation>();
    }

    [Fact]
    public void StatusInstanciaValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(StatusInstanciaValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void StatusInstanciaValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(StatusInstanciaValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.StatusInstancia CreateValidStatusInstancia()
    {
        return new Models.StatusInstancia
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockStatusInstanciaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusInstancia>(), It.IsAny<string>())).ReturnsAsync(new List<StatusInstanciaResponseAll>());
    // Setup other mocks but don't override the StatusInstancias service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockStatusInstanciaService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterStatusInstancia>(), It.IsAny<string>())).ReturnsAsync(new List<StatusInstanciaResponseAll>());
    // Setup other mocks but don't override the StatusInstancias service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var statusinstancia = new Models.StatusInstancia
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(statusinstancia, _mockStatusInstanciaService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockStatusInstanciaService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}