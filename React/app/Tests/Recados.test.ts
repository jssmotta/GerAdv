// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useRecadosForm, useRecadosList, useValidationsRecados } from '../GerAdv_TS/Recados/Hooks/hookRecados';
import { IRecados } from '../GerAdv_TS/Recados/Interfaces/interface.Recados';
import { IRecadosService } from '../GerAdv_TS/Recados/Services/Recados.service';
import { RecadosTestEmpty } from '../GerAdv_TS/Models/Recados';


// Mock do serviço
const mockRecadosService: jest.Mocked<IRecadosService> = {
  fetchRecadosById: jest.fn(),
  saveRecados: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteRecados: jest.fn(),
  validateRecados: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialRecados: IRecados = { ...RecadosTestEmpty() };

describe('useRecadosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    expect(result.current.data).toEqual(initialRecados);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    const mockEvent = {
      target: {
        name: 'data',
        value: 'Novo Recados',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.data).toBe('Novo Recados');
  });

   test('deve carregar Recados por ID', async () => {
    const mockRecados = { ...initialRecados, id: 1, data: 'Recados Teste' };
    mockRecadosService.fetchRecadosById.mockResolvedValue(mockRecados);

    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    await act(async () => {
      await result.current.loadRecados(1);
    });

    expect(mockRecadosService.fetchRecadosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockRecados);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Recados', async () => {
    const errorMessage = 'Erro ao carregar Recados';
    mockRecadosService.fetchRecadosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    await act(async () => {
      await result.current.loadRecados(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialRecados, data: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialRecados);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    await act(async () => {
      await result.current.loadRecados(0);
    });

    expect(mockRecadosService.fetchRecadosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialRecados);
  });
});

describe('useRecadosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useRecadosList(mockRecadosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialRecados, id: 1, data: 'Recados 1' },
      { ...initialRecados, id: 2, data: 'Recados 2' }
    ];
    mockRecadosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRecadosList(mockRecadosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockRecadosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockRecadosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useRecadosList(mockRecadosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialRecados, id: 1, data: 'Recados Filtrado' }];
    const filtro = { data: 'Recados' };
    mockRecadosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useRecadosList(mockRecadosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockRecadosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsRecados', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsRecados());

    const validData = { ...initialRecados, data: 'Recados Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar data vazio', () => {
    const { result } = renderHook(() => useValidationsRecados());

    const invalidData = { ...initialRecados, data: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ficar vazio.');
  });

  
  test('deve invalidar data muito longo', () => {
    const { result } = renderHook(() => useValidationsRecados());

    const invalidData = { 
      ...initialRecados, 
      data: 'a'.repeat(-1+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Data não pode ter mais de -1 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsRecados());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialRecados, id: 1, data: 'Recados Teste' }];
    mockRecadosService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useRecadosList(mockRecadosService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsRecados()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useRecadosForm(initialRecados, mockRecadosService)
    );

    const mockEvent = {
      target: {
        name: 'concluido',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.concluido).toBe(true);
  });


