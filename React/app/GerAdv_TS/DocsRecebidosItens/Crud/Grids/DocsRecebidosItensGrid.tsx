// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { DocsRecebidosItensEmpty } from '../../../Models/DocsRecebidosItens';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IDocsRecebidosItens } from '../../Interfaces/interface.DocsRecebidosItens';
import { DocsRecebidosItensService } from '../../Services/DocsRecebidosItens.service';
import { DocsRecebidosItensApi } from '../../Apis/ApiDocsRecebidosItens';
import { DocsRecebidosItensGridMobileComponent } from '../GridsMobile/DocsRecebidosItens';
import { DocsRecebidosItensGridDesktopComponent } from '../GridsDesktop/DocsRecebidosItens';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterDocsRecebidosItens } from '../../Filters/DocsRecebidosItens';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import DocsRecebidosItensWindow from './DocsRecebidosItensWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useDocsRecebidosItensList } from '../../Hooks/hookDocsRecebidosItens';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useDocsRecebidosItensFilter } from '../../Hooks/hookDocsRecebidosItensFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const DocsRecebidosItensGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedDocsRecebidosItens, setSelectedDocsRecebidosItens] = useState<IDocsRecebidosItens>(DocsRecebidosItensEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterDocsRecebidosItens | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [docsrecebidositensData, setDocsRecebidosItensData] = useState<IDocsRecebidosItens[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const docsrecebidositensService = useMemo(() => {
    return new DocsRecebidosItensService(
    new DocsRecebidosItensApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: docsrecebidositens, 
  fetchData, 
  refreshData
} = useDocsRecebidosItensList(docsrecebidositensService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterDocsRecebidosItens | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('DocsRecebidosItensFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await docsrecebidositensService.getAll(
  filtro ?? {} as FilterDocsRecebidosItens, 
  onlineData => {
    setDocsRecebidosItensData(onlineData);
    setLoading(false);
  }
);
setDocsRecebidosItensData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterDocsRecebidosItens;

    const filtroMem = sessionStorage.getItem(btoa('DocsRecebidosItensFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('DocsRecebidosItensFilter')) ?? '') || '{}') : {} as FilterDocsRecebidosItens;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (docsrecebidositens: IDocsRecebidosItens) => {
    setSelectedDocsRecebidosItens(docsrecebidositens);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedDocsRecebidosItens(DocsRecebidosItensEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const docsrecebidositens = e.dataItem;
    setDeleteId(docsrecebidositens.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await docsrecebidositensService.deleteDocsRecebidosItens(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useDocsRecebidosItensFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <DocsRecebidosItensGridMobileComponent
        data={docsrecebidositensData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <DocsRecebidosItensGridDesktopComponent
        data={docsrecebidositensData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <DocsRecebidosItensWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedDocsRecebidosItens={selectedDocsRecebidosItens}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Docs Recebidos Itens'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default DocsRecebidosItensGrid;