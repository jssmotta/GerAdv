// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { PosicaoOutrasPartesService, PosicaoOutrasPartesValidator } from '@/app/GerAdv_TS/PosicaoOutrasPartes/Services/PosicaoOutrasPartes.service';
import { PosicaoOutrasPartesApi, PosicaoOutrasPartesApiError } from '@/app/GerAdv_TS/PosicaoOutrasPartes/Apis/ApiPosicaoOutrasPartes';
import { IPosicaoOutrasPartes } from '@/app/GerAdv_TS/PosicaoOutrasPartes/Interfaces/interface.PosicaoOutrasPartes';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as PosicaoOutrasPartesApi;

const sample: IPosicaoOutrasPartes = { id: 1, nome: 'Teste' };

describe('PosicaoOutrasPartesService', () => {
  let service: PosicaoOutrasPartesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new PosicaoOutrasPartesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(PosicaoOutrasPartesValidator, 'validatePosicaoOutrasPartes').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchPosicaoOutrasPartesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchPosicaoOutrasPartesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchPosicaoOutrasPartesById lança PosicaoOutrasPartesApiError para id inválido', async () => {
    await expect(service.fetchPosicaoOutrasPartesById(0)).rejects.toBeInstanceOf(PosicaoOutrasPartesApiError);
  });

  it('savePosicaoOutrasPartes chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.savePosicaoOutrasPartes(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('savePosicaoOutrasPartes lança PosicaoOutrasPartesApiError quando validação falha', async () => {
    (PosicaoOutrasPartesValidator.validatePosicaoOutrasPartes as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.savePosicaoOutrasPartes(sample)).rejects.toBeInstanceOf(PosicaoOutrasPartesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deletePosicaoOutrasPartes chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deletePosicaoOutrasPartes(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deletePosicaoOutrasPartes lança PosicaoOutrasPartesApiError para id inválido', async () => {
    await expect(service.deletePosicaoOutrasPartes(0)).rejects.toBeInstanceOf(PosicaoOutrasPartesApiError);
  });

  it('validatePosicaoOutrasPartes delega para PosicaoOutrasPartesValidator', () => {
    (PosicaoOutrasPartesValidator.validatePosicaoOutrasPartes as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validatePosicaoOutrasPartes(sample);
    expect(r.isValid).toBe(true);
  });
});
