// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { EMPClassRiscosService, EMPClassRiscosValidator } from '@/app/GerAdv_TS/EMPClassRiscos/Services/EMPClassRiscos.service';
import { EMPClassRiscosApi, EMPClassRiscosApiError } from '@/app/GerAdv_TS/EMPClassRiscos/Apis/ApiEMPClassRiscos';
import { IEMPClassRiscos } from '@/app/GerAdv_TS/EMPClassRiscos/Interfaces/interface.EMPClassRiscos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as EMPClassRiscosApi;

const sample: IEMPClassRiscos = { id: 1, nome: 'Teste' };

describe('EMPClassRiscosService', () => {
  let service: EMPClassRiscosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new EMPClassRiscosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(EMPClassRiscosValidator, 'validateEMPClassRiscos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchEMPClassRiscosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchEMPClassRiscosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchEMPClassRiscosById lança EMPClassRiscosApiError para id inválido', async () => {
    await expect(service.fetchEMPClassRiscosById(0)).rejects.toBeInstanceOf(EMPClassRiscosApiError);
  });

  it('saveEMPClassRiscos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveEMPClassRiscos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveEMPClassRiscos lança EMPClassRiscosApiError quando validação falha', async () => {
    (EMPClassRiscosValidator.validateEMPClassRiscos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveEMPClassRiscos(sample)).rejects.toBeInstanceOf(EMPClassRiscosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteEMPClassRiscos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteEMPClassRiscos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteEMPClassRiscos lança EMPClassRiscosApiError para id inválido', async () => {
    await expect(service.deleteEMPClassRiscos(0)).rejects.toBeInstanceOf(EMPClassRiscosApiError);
  });

  it('validateEMPClassRiscos delega para EMPClassRiscosValidator', () => {
    (EMPClassRiscosValidator.validateEMPClassRiscos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateEMPClassRiscos(sample);
    expect(r.isValid).toBe(true);
  });
});
