// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ClientesSociosService, ClientesSociosValidator } from '@/app/GerAdv_TS/ClientesSocios/Services/ClientesSocios.service';
import { ClientesSociosApi, ClientesSociosApiError } from '@/app/GerAdv_TS/ClientesSocios/Apis/ApiClientesSocios';
import { IClientesSocios } from '@/app/GerAdv_TS/ClientesSocios/Interfaces/interface.ClientesSocios';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ClientesSociosApi;

const sample: IClientesSocios = { id: 1, nome: 'Teste' };

describe('ClientesSociosService', () => {
  let service: ClientesSociosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ClientesSociosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ClientesSociosValidator, 'validateClientesSocios').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchClientesSociosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchClientesSociosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchClientesSociosById lança ClientesSociosApiError para id inválido', async () => {
    await expect(service.fetchClientesSociosById(0)).rejects.toBeInstanceOf(ClientesSociosApiError);
  });

  it('saveClientesSocios chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveClientesSocios(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveClientesSocios lança ClientesSociosApiError quando validação falha', async () => {
    (ClientesSociosValidator.validateClientesSocios as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveClientesSocios(sample)).rejects.toBeInstanceOf(ClientesSociosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteClientesSocios chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteClientesSocios(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteClientesSocios lança ClientesSociosApiError para id inválido', async () => {
    await expect(service.deleteClientesSocios(0)).rejects.toBeInstanceOf(ClientesSociosApiError);
  });

  it('validateClientesSocios delega para ClientesSociosValidator', () => {
    (ClientesSociosValidator.validateClientesSocios as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateClientesSocios(sample);
    expect(r.isValid).toBe(true);
  });
});
