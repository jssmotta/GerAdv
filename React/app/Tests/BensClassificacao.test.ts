// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useBensClassificacaoForm, useBensClassificacaoList, useValidationsBensClassificacao } from '../GerAdv_TS/BensClassificacao/Hooks/hookBensClassificacao';
import { IBensClassificacao } from '../GerAdv_TS/BensClassificacao/Interfaces/interface.BensClassificacao';
import { IBensClassificacaoService } from '../GerAdv_TS/BensClassificacao/Services/BensClassificacao.service';
import { BensClassificacaoTestEmpty } from '../GerAdv_TS/Models/BensClassificacao';
import { useBensClassificacaoComboBox } from '../GerAdv_TS/BensClassificacao/Hooks/hookBensClassificacao';

// Mock do serviço
const mockBensClassificacaoService: jest.Mocked<IBensClassificacaoService> = {
  fetchBensClassificacaoById: jest.fn(),
  saveBensClassificacao: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteBensClassificacao: jest.fn(),
  validateBensClassificacao: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialBensClassificacao: IBensClassificacao = { ...BensClassificacaoTestEmpty() };

describe('useBensClassificacaoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    expect(result.current.data).toEqual(initialBensClassificacao);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Bens Classificacao',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Bens Classificacao');
  });

   test('deve carregar Bens Classificacao por ID', async () => {
    const mockBensClassificacao = { ...initialBensClassificacao, id: 1, nome: 'Bens Classificacao Teste' };
    mockBensClassificacaoService.fetchBensClassificacaoById.mockResolvedValue(mockBensClassificacao);

    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.loadBensClassificacao(1);
    });

    expect(mockBensClassificacaoService.fetchBensClassificacaoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockBensClassificacao);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Bens Classificacao', async () => {
    const errorMessage = 'Erro ao carregar Bens Classificacao';
    mockBensClassificacaoService.fetchBensClassificacaoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.loadBensClassificacao(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialBensClassificacao, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialBensClassificacao);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useBensClassificacaoForm(initialBensClassificacao, mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.loadBensClassificacao(0);
    });

    expect(mockBensClassificacaoService.fetchBensClassificacaoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialBensClassificacao);
  });
});

describe('useBensClassificacaoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoList(mockBensClassificacaoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialBensClassificacao, id: 1, nome: 'Bens Classificacao 1' },
      { ...initialBensClassificacao, id: 2, nome: 'Bens Classificacao 2' }
    ];
    mockBensClassificacaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useBensClassificacaoList(mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockBensClassificacaoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockBensClassificacaoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useBensClassificacaoList(mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialBensClassificacao, id: 1, nome: 'Bens Classificacao Filtrado' }];
    const filtro = { nome: 'Bens Classificacao' };
    mockBensClassificacaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useBensClassificacaoList(mockBensClassificacaoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockBensClassificacaoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsBensClassificacao', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsBensClassificacao());

    const validData = { ...initialBensClassificacao, nome: 'Bens Classificacao Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsBensClassificacao());

    const invalidData = { ...initialBensClassificacao, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsBensClassificacao());

    const invalidData = { 
      ...initialBensClassificacao, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsBensClassificacao());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialBensClassificacao, id: 1, nome: 'Bens Classificacao Teste' }];
    mockBensClassificacaoService.getAll.mockResolvedValue(mockData);
    mockBensClassificacaoService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useBensClassificacaoList(mockBensClassificacaoService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsBensClassificacao()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Bens Classificacao Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Bens Classificacao Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Bens Classificacao 1' },
      { id: 2, nome: 'Bens Classificacao 2' }
    ];
    mockBensClassificacaoService.getList.mockResolvedValue(mockOptions as IBensClassificacao[]);


    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Bens Classificacao 1' },
        { id: 2, nome: 'Bens Classificacao 2' }
      ]);
    });


    expect(mockBensClassificacaoService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Bens Classificacao ABC' },
      { id: 2, nome: 'Bens Classificacao XYZ' }
    ];
    mockBensClassificacaoService.getList.mockResolvedValue(mockOptions as IBensClassificacao[]);   


 const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Bens Classificacao ABC' },
        { id: 2, nome: 'Bens Classificacao XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Bens Classificacao ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Bens Classificacao ABC' },
      { id: 2, nome: 'Bens Classificacao XYZ' }
    ];
    mockBensClassificacaoService.getList.mockResolvedValue(mockOptions as IBensClassificacao[]);
  


    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Bens Classificacao ABC' },
        { id: 2, nome: 'Bens Classificacao XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Bens Classificacao ABC' },
          {id: 2, nome: 'Bens Classificacao XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );

    const newValue = { id: 1, nome: 'Bens Classificacao Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Bens Classificacao Inicial' };
    
    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useBensClassificacaoComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Bens Classificacao Inicial' };
    
    const { result } = renderHook(() => 
      useBensClassificacaoComboBox(mockBensClassificacaoService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






