// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Fonte:ReadersGenerator
namespace MenphisSI.GerAdv.Readers;
public partial class GraphReader(IFGraphFactory graphFactory) : IGraphReader
{
    private readonly IFGraphFactory _graphFactory = graphFactory ?? throw new ArgumentNullException();
    public async Task<IEnumerable<GraphResponseAll>> ListarAsync(MsiSqlConnection oCnn, int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        var stopwatch = GraphDatabaseMetrics.StartTimer();
        var connStopwatch = GraphDatabaseMetrics.StartTimer();
        try
        {
            GraphDatabaseMetrics.IncrementActiveConnections("ListarAsync", oCnn?.Uri);
            GraphDatabaseMetrics.RecordConnectionOpen("ListarAsync", oCnn?.Uri, connStopwatch);
            var result = await ListarTabelaAsync(oCnn, BuildSqlQuery(DBGraph.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
            GraphDatabaseMetrics.RecordSqlQuery("ListarAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count());
            return result;
        }
        catch (SqlException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ListarAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ListarAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            GraphDatabaseMetrics.DecrementActiveConnections("ListarAsync", oCnn?.Uri);
        }
    }

    private async Task<IEnumerable<GraphResponseAll>> ListarTabelaAsync(MsiSqlConnection? oCnn, string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var stopwatch = GraphDatabaseMetrics.StartTimer();
        var result = new List<GraphResponseAll>(max);
        try
        {
            await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: oCnn?.InnerConnection)
            {
                CommandTimeout = 120
            };
            if (parameters != null && parameters.Count > 0)
                foreach (var param in parameters)
                {
                    if (!cmd.Parameters.Contains(param.ParameterName))
                    {
                        var newParam = new SqlParameter(param.ParameterName, param.Value)
                        {
                            SqlDbType = param.SqlDbType,
                            Direction = param.Direction,
                            Size = param.Size,
                            Precision = param.Precision,
                            Scale = param.Scale
                        };
                        cmd.Parameters.Add(newParam);
                    }
                }

            await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.Default, cancellationToken);
            while (await reader.ReadAsync(cancellationToken))
            {
                if (cancellationToken.IsCancellationRequested)
                    return result;
                result.Add(ReadAll(_graphFactory.CreateFromReader(reader), reader)!);
            }

            GraphDatabaseMetrics.RecordSqlQuery("ListarTabelaAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count);
            return result;
        }
        catch (SqlException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "Timeout", oCnn?.Uri);
            throw;
        }
    }

    public async Task<GraphResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        var stopwatch = GraphDatabaseMetrics.StartTimer();
        try
        {
            GraphDatabaseMetrics.IncrementActiveConnections("ReadAsync", oCnn?.Uri);
            using var dbRec = await _graphFactory.CreateFromIdAsync(id, oCnn);
            var result = dbRec == null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
            GraphDatabaseMetrics.RecordSqlQuery("ReadAsync", "SELECT", oCnn?.Uri, stopwatch, result != null ? 1 : 0);
            return result;
        }
        catch (SqlException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ReadAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            GraphDatabaseMetrics.RecordDatabaseError("ReadAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
        // Ignore
        }
    }

    public async Task<Models.Graph?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _graphFactory.CreateFromIdAsync(id, oCnn);
        var graph = new Models.Graph
        {
            Id = dbRec.ID,
            Tabela = dbRec.FTabela ?? string.Empty,
            TabelaId = dbRec.FTabelaId,
            Imagem = dbRec.FImagem,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return graph;
    }

    public GraphResponse? Read(FGraph dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public GraphResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _graphFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec is null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public GraphResponse? Read(FGraph dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var graph = new GraphResponse
        {
            Id = dbRec.ID,
            Tabela = dbRec.FTabela ?? string.Empty,
            TabelaId = dbRec.FTabelaId,
            Imagem = dbRec.FImagem,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return graph;
    }

    public GraphResponse? Read(SG.GerAdv.DBGraph dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var graph = new GraphResponse
        {
            Id = dbRec.ID,
            Tabela = dbRec.FTabela ?? string.Empty,
            TabelaId = dbRec.FTabelaId,
            Imagem = dbRec.FImagem,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return graph;
    }

    public GraphResponseAll? ReadAll(FGraph dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var graph = new GraphResponseAll
        {
            Id = dbRec.ID,
            Tabela = dbRec.FTabela ?? string.Empty,
            TabelaId = dbRec.FTabelaId,
            Imagem = dbRec.FImagem,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return graph;
    }

    public GraphResponseAll? ReadAll(SG.GerAdv.DBGraph dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var graph = new GraphResponseAll
        {
            Id = dbRec.ID,
            Tabela = dbRec.FTabela ?? string.Empty,
            TabelaId = dbRec.FTabelaId,
            Imagem = dbRec.FImagem,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return graph;
    }
}