// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Fonte:ReadersGenerator
namespace MenphisSI.GerAdv.Readers;
public partial class SMSAliceReader(IFSMSAliceFactory smsaliceFactory) : ISMSAliceReader
{
    private readonly IFSMSAliceFactory _smsaliceFactory = smsaliceFactory ?? throw new ArgumentNullException();
    public async Task<IEnumerable<DBNomeID>?> ListarNAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order) => await DevourerSqlData.ListarNomeID(BuildSqlQuery("smaCodigo, smaNome", cWhere, order, max), parameters, uri, caching: false, max: max);
    public async Task<IEnumerable<SMSAliceResponseAll>> ListarAsync(MsiSqlConnection oCnn, int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        var stopwatch = SMSAliceDatabaseMetrics.StartTimer();
        var connStopwatch = SMSAliceDatabaseMetrics.StartTimer();
        try
        {
            SMSAliceDatabaseMetrics.IncrementActiveConnections("ListarAsync", oCnn?.Uri);
            SMSAliceDatabaseMetrics.RecordConnectionOpen("ListarAsync", oCnn?.Uri, connStopwatch);
            var result = await ListarTabelaAsync(oCnn, BuildSqlQuery(DBSMSAlice.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
            SMSAliceDatabaseMetrics.RecordSqlQuery("ListarAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count());
            return result;
        }
        catch (SqlException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ListarAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ListarAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            SMSAliceDatabaseMetrics.DecrementActiveConnections("ListarAsync", oCnn?.Uri);
        }
    }

    private async Task<IEnumerable<SMSAliceResponseAll>> ListarTabelaAsync(MsiSqlConnection? oCnn, string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var stopwatch = SMSAliceDatabaseMetrics.StartTimer();
        var result = new List<SMSAliceResponseAll>(max);
        try
        {
            await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: oCnn?.InnerConnection)
            {
                CommandTimeout = 120
            };
            if (parameters != null && parameters.Count > 0)
                foreach (var param in parameters)
                {
                    if (!cmd.Parameters.Contains(param.ParameterName))
                    {
                        var newParam = new SqlParameter(param.ParameterName, param.Value)
                        {
                            SqlDbType = param.SqlDbType,
                            Direction = param.Direction,
                            Size = param.Size,
                            Precision = param.Precision,
                            Scale = param.Scale
                        };
                        cmd.Parameters.Add(newParam);
                    }
                }

            await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.Default, cancellationToken);
            while (await reader.ReadAsync(cancellationToken))
            {
                if (cancellationToken.IsCancellationRequested)
                    return result;
                result.Add(ReadAll(_smsaliceFactory.CreateFromReader(reader), reader)!);
            }

            SMSAliceDatabaseMetrics.RecordSqlQuery("ListarTabelaAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count);
            return result;
        }
        catch (SqlException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "Timeout", oCnn?.Uri);
            throw;
        }
    }

    public async Task<SMSAliceResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        var stopwatch = SMSAliceDatabaseMetrics.StartTimer();
        try
        {
            SMSAliceDatabaseMetrics.IncrementActiveConnections("ReadAsync", oCnn?.Uri);
            using var dbRec = await _smsaliceFactory.CreateFromIdAsync(id, oCnn);
            var result = dbRec == null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
            SMSAliceDatabaseMetrics.RecordSqlQuery("ReadAsync", "SELECT", oCnn?.Uri, stopwatch, result != null ? 1 : 0);
            return result;
        }
        catch (SqlException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ReadAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            SMSAliceDatabaseMetrics.RecordDatabaseError("ReadAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
        // Ignore
        }
    }

    public async Task<Models.SMSAlice?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _smsaliceFactory.CreateFromIdAsync(id, oCnn);
        var smsalice = new Models.SMSAlice
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Nome = dbRec.FNome ?? string.Empty,
            TipoEMail = dbRec.FTipoEMail,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return smsalice;
    }

    public SMSAliceResponse? Read(FSMSAlice dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public SMSAliceResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _smsaliceFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec is null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public SMSAliceResponse? Read(FSMSAlice dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var smsalice = new SMSAliceResponse
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Nome = dbRec.FNome ?? string.Empty,
            TipoEMail = dbRec.FTipoEMail,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return smsalice;
    }

    public SMSAliceResponse? Read(SG.GerAdv.DBSMSAlice dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var smsalice = new SMSAliceResponse
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Nome = dbRec.FNome ?? string.Empty,
            TipoEMail = dbRec.FTipoEMail,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return smsalice;
    }

    public SMSAliceResponseAll? ReadAll(FSMSAlice dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var smsalice = new SMSAliceResponseAll
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Nome = dbRec.FNome ?? string.Empty,
            TipoEMail = dbRec.FTipoEMail,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        try
        {
            smsalice.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            smsalice.NomeTipoEMail = dr[DBTipoEMailDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return smsalice;
    }

    public SMSAliceResponseAll? ReadAll(SG.GerAdv.DBSMSAlice dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var smsalice = new SMSAliceResponseAll
        {
            Id = dbRec.ID,
            Operador = dbRec.FOperador,
            Nome = dbRec.FNome ?? string.Empty,
            TipoEMail = dbRec.FTipoEMail,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        try
        {
            smsalice.NomeOperador = dr[DBOperadorDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        try
        {
            smsalice.NomeTipoEMail = dr[DBTipoEMailDicInfo.CampoNome]?.ToString() ?? string.Empty;
        }
        catch
        {
        }

        return smsalice;
    }
}