// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { CRUD_CONSTANTS } from '@/app/tools/crud';
import { CargosEscClassApi, CargosEscClassApiError } from '../Apis/ApiCargosEscClass';
import { FilterCargosEscClass } from '../Filters/CargosEscClass';
import { ICargosEscClass } from '../Interfaces/interface.CargosEscClass';
import { CargosEscClassEmpty } from '../../Models/CargosEscClass';

export class CargosEscClassValidator {
  static validateCargosEscClass(cargosescclass: ICargosEscClass): { isValid: boolean; errors: string[] } {
    const errors: string[] = [];

    // Atualmente não há validações de regras de negócio específicas
    // Todas as validações são feitas nos inputs correspondentes
    
    return {
      isValid: errors.length === 0,
      errors
    };
  }
}

export interface ICargosEscClassService {
  fetchCargosEscClassById: (id: number) => Promise<ICargosEscClass>;
  saveCargosEscClass: (cargosescclass: ICargosEscClass) => Promise<ICargosEscClass>;  
  getList: (filtro?: FilterCargosEscClass) => Promise<ICargosEscClass[]>;
  getAll: (filtro?: FilterCargosEscClass) => Promise<ICargosEscClass[]>;
  deleteCargosEscClass: (id: number) => Promise<void>;
  validateCargosEscClass: (cargosescclass: ICargosEscClass) => { isValid: boolean; errors: string[] };
}

export class CargosEscClassService implements ICargosEscClassService {
  constructor(private api: CargosEscClassApi) {}

  async fetchCargosEscClassById(id: number): Promise<ICargosEscClass> {
    if (id <= 0) {
      throw new CargosEscClassApiError('ID inválido', 400, 'INVALID_ID');
    }

    try {
      
      const response = await this.api.getById(id);
      return response.data;

    } catch (error) {
      if (error instanceof CargosEscClassApiError) {
        throw error;
      }
      throw new CargosEscClassApiError('Erro ao buscar cargosescclass', 500, 'FETCH_ERROR', error);
    }
  }

  async saveCargosEscClass(cargosescclass: ICargosEscClass): Promise<ICargosEscClass> {    
    const validation = this.validateCargosEscClass(cargosescclass);
    if (!validation.isValid) {
      throw new CargosEscClassApiError(
        `Dados inválidos: ${validation.errors.join(', ')}`,
        400,
        'VALIDATION_ERROR'
      );
    }

    try {
      const response = await this.api.addAndUpdate(cargosescclass);
      return response.data;
    } catch (error) {
      if (error instanceof CargosEscClassApiError) {
        throw error;
      }
      throw new CargosEscClassApiError('Erro ao salvar cargosescclass', 500, 'SAVE_ERROR', error);
    }
  }

  
    async getList(filtro?: FilterCargosEscClass): Promise<ICargosEscClass[]> {
    try {
      const response = await this.api.getListN(CRUD_CONSTANTS.MAX_RECORDS_COMBO, filtro);
      return response.data || [];
    } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Error fetching CargosEscClass list');
      return [];
    }
  }

 
  

   async getAll(
    filtro?: FilterCargosEscClass,
    onOnlineData?: (data: ICargosEscClass[]) => void
  ): Promise<ICargosEscClass[]> {
    try {
      // Carrega dados offline primeiro
      const preloadResponse = await this.api.filterPreload(0, filtro ?? {});
      const offlineData = preloadResponse?.data || [];

      if (onOnlineData) {
        // Busca dados online em background e envia via callback
        this.api.filter(0, filtro ?? {})
          .then(response => {
            if (response?.data) {
              onOnlineData(response.data);
            }
          })
          .catch(error => {
            if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                console.log('Error fetching online CargosEscClass');
          });
        // Retorna offline imediatamente
        return offlineData;
      } else {
        // Se não há callback, aguarda dados online e retorna
        try {
          const onlineResponse = await this.api.filter(0, filtro ?? {});
          return onlineResponse?.data || offlineData;
        } catch (error) {
            if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
                console.log('Error fetching online CargosEscClass');
          return offlineData;
        }
      }
    } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Error fetching all CargosEscClass:');
      return [];
    }
  }


  async deleteCargosEscClass(id: number): Promise<void> {
    if (id <= 0) {
      throw new CargosEscClassApiError('ID inválido para exclusão', 400, 'INVALID_ID');
    }

    try {
      await this.api.delete(id);
    } catch (error) {
      if (error instanceof CargosEscClassApiError) {
        throw error;
      }
      throw new CargosEscClassApiError('Erro ao excluir cargosescclass', 500, 'DELETE_ERROR', error);
    }
  }

  validateCargosEscClass(cargosescclass: ICargosEscClass): { isValid: boolean; errors: string[] } {
    return CargosEscClassValidator.validateCargosEscClass(cargosescclass);
  }
}