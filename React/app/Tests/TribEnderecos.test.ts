// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useTribEnderecosForm, useTribEnderecosList, useValidationsTribEnderecos } from '../GerAdv_TS/TribEnderecos/Hooks/hookTribEnderecos';
import { ITribEnderecos } from '../GerAdv_TS/TribEnderecos/Interfaces/interface.TribEnderecos';
import { ITribEnderecosService } from '../GerAdv_TS/TribEnderecos/Services/TribEnderecos.service';
import { TribEnderecosTestEmpty } from '../GerAdv_TS/Models/TribEnderecos';


// Mock do serviço
const mockTribEnderecosService: jest.Mocked<ITribEnderecosService> = {
  fetchTribEnderecosById: jest.fn(),
  saveTribEnderecos: jest.fn(),
  
  getAll: jest.fn(),
  deleteTribEnderecos: jest.fn(),
  validateTribEnderecos: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialTribEnderecos: ITribEnderecos = { ...TribEnderecosTestEmpty() };

describe('useTribEnderecosForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    expect(result.current.data).toEqual(initialTribEnderecos);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    const mockEvent = {
      target: {
        name: 'endereco',
        value: 'Novo Trib Endereços',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.endereco).toBe('Novo Trib Endereços');
  });

   test('deve carregar Trib Endereços por ID', async () => {
    const mockTribEnderecos = { ...initialTribEnderecos, id: 1, endereco: 'Trib Endereços Teste' };
    mockTribEnderecosService.fetchTribEnderecosById.mockResolvedValue(mockTribEnderecos);

    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.loadTribEnderecos(1);
    });

    expect(mockTribEnderecosService.fetchTribEnderecosById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockTribEnderecos);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Trib Endereços', async () => {
    const errorMessage = 'Erro ao carregar Trib Endereços';
    mockTribEnderecosService.fetchTribEnderecosById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.loadTribEnderecos(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialTribEnderecos, endereco: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialTribEnderecos);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useTribEnderecosForm(initialTribEnderecos, mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.loadTribEnderecos(0);
    });

    expect(mockTribEnderecosService.fetchTribEnderecosById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialTribEnderecos);
  });
});

describe('useTribEnderecosList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useTribEnderecosList(mockTribEnderecosService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialTribEnderecos, id: 1, endereco: 'Trib Endereços 1' },
      { ...initialTribEnderecos, id: 2, endereco: 'Trib Endereços 2' }
    ];
    mockTribEnderecosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTribEnderecosList(mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockTribEnderecosService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockTribEnderecosService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTribEnderecosList(mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialTribEnderecos, id: 1, endereco: 'Trib Endereços Filtrado' }];
    const filtro = { endereco: 'Trib Endereços' };
    mockTribEnderecosService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTribEnderecosList(mockTribEnderecosService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockTribEnderecosService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsTribEnderecos', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsTribEnderecos());

    const validData = { ...initialTribEnderecos, endereco: 'Trib Endereços Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsTribEnderecos());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialTribEnderecos, id: 1, endereco: 'Trib Endereços Teste' }];
    mockTribEnderecosService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useTribEnderecosList(mockTribEnderecosService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsTribEnderecos()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});