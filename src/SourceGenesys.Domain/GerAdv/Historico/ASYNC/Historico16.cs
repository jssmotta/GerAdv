// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
public partial class DBHistorico
{
    /// <summary>
    /// Factory method async para carregar [tableMin]
    /// </summary>
    public static async Task<DBHistorico> CreateAsync(int nCodigo, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        var registro = new DBHistorico();
        await registro.CarregarAsync(nCodigo, oCnn, cancellationToken);
        return registro;
    }

    /// <summary>
    /// Carregar dados async
    /// </summary>
    protected async Task CarregarAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (id.IsEmptyIDNumber() || oCnn is null)
            return;
        using var cmd = new SqlCommand($"{ConfiguracoesDBT.SQLNoCount}SELECT TOP (1) {CamposSqlX} FROM {PTabelaNome.dbo(oCnn)} WHERE [{CampoCodigo}] = @ThisIDToLoad", oCnn.InnerConnection);
        cmd.Parameters.AddWithValue("@ThisIDToLoad", id);
        using var ds = await ConfiguracoesDBT.GetDataTableAsync(cmd, CommandBehavior.SingleRow, oCnn, cancellationToken);
        if (ds?.Rows.Count > 0)
        {
            CarregarDadosBd(ds.Rows[0]);
        }
    }

#if (!NOTSTORED_Historico)
    /// <summary>
    /// Update async com timeout e cancellation
    /// </summary>
    public async Task<int> UpdateAsync(MsiSqlConnection? oCnn, int insertId = 0, CancellationToken cancellationToken = default, int timeoutSeconds = 30)
    {
        var isInsert = insertId == 0 && ID == 0;
        if (!isInsert && !HasAnyFieldChanged())
            return 0;
        if (oCnn is null)
        {
#if DEBUG
            PTabelaNome.PopupBox("oCnn is null - UpdateAsync()");
#endif
            return 0;
        }

        try
        {
            var updateTool = new DBToolWTable32Async(PTabelaNome, CampoCodigo, ID == 0)
            {
                IsMachineCode = true,
            };
            updateTool.Where = isInsert ? string.Empty : $"{CampoCodigo}={ID}";
            ConfigureUpdateFields(updateTool);
            ConfigureAuditorFields(updateTool);
            if (insertId != 0)
            {
                return await GravaNewIdAsync(updateTool, insertId, oCnn, cancellationToken);
            }

            var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken);
            if (updateTool.Insert && result == "OK")
            {
                ID = updateTool.GetCodigo();
                if (ID == 0)
                {
                    Error = -2;
                    ErrorDescription = "O registro não pode ser incluído, tente mais tarde.";
                    return Error;
                }
            }

            return result == "OK" ? 0 : -1;
        }
        catch (OperationCanceledException)
        {
            throw;
        }
        catch (Exception ex)
        {
            Error = -1;
            ErrorDescription = ex.Message;
            return Error;
        }
    }

#endif
    /// <summary>
    /// Verificar se existe async
    /// </summary>
    public static async Task<bool> ExisteAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var existe = await ConfiguracoesDBT.GetScalarAsync<int?>($"SELECT 1 FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = {id}", oCnn, cancellationToken);
        return existe.HasValue;
    }

    /// <summary>
    /// Delete async
    /// </summary>
    /// <param name = "operadorId">For future Audity</param>
    /// <param name = "id"></param>
    /// <param name = "oCnn"></param>
    /// <param name = "cancellationToken"></param>
    /// <returns></returns>
    /// <exception cref = "InvalidOperationException"></exception>
    public static async Task<bool> DeleteRecordAsync(int operadorId, int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var sql = $"DELETE FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = @Id";
        try
        {
            using var cmd = new SqlCommand(sql, oCnn.InnerConnection);
            cmd.Parameters.AddWithValue("@Id", id);
            var rowsAffected = await cmd.ExecuteNonQueryAsync(cancellationToken);
            return rowsAffected > 0;
        }
        catch (SqlException ex)when (ex.Number == 547)
        {
            throw new InvalidOperationException("Este registro não pode ser excluído, pois está vinculado a outros registros.");
        }
    }

#if (!NOTSTORED_Historico)
    // Helper methods
    private bool HasAnyFieldChanged() => pFldFExtraID || pFldFIDNE || pFldFExtraGUID || pFldFLiminarOrigem || pFldFNaoPublicavel || pFldFProcesso || pFldFPrecatoria || pFldFApenso || pFldFIDInstProcesso || pFldFFase || pFldFData || pFldFObservacao || pFldFAgendado || pFldFConcluido || pFldFMesmaAgenda || pFldFSAD || pFldFResumido || pFldFStatusAndamento || pFldFTop || pFldFGUID;
    private void ConfigureUpdateFields(DBToolWTable32Async updateTool)
    {
        if (pFldFExtraID)
            updateTool.Fields(DBHistoricoDicInfo.ExtraID, m_FExtraID, ETiposCampos.FNumber);
        if (pFldFIDNE)
            updateTool.Fields(DBHistoricoDicInfo.IDNE, m_FIDNE, ETiposCampos.FNumber);
        if (pFldFExtraGUID)
            updateTool.Fields(DBHistoricoDicInfo.ExtraGUID, m_FExtraGUID, ETiposCampos.FString);
        if (pFldFLiminarOrigem)
            updateTool.Fields(DBHistoricoDicInfo.LiminarOrigem, m_FLiminarOrigem, ETiposCampos.FNumber);
        if (pFldFNaoPublicavel)
            updateTool.Fields(DBHistoricoDicInfo.NaoPublicavel, m_FNaoPublicavel, ETiposCampos.FBoolean);
        if (pFldFProcesso)
            updateTool.Fields(DBHistoricoDicInfo.Processo, m_FProcesso, ETiposCampos.FNumber);
        if (pFldFPrecatoria)
            updateTool.Fields(DBHistoricoDicInfo.Precatoria, m_FPrecatoria, ETiposCampos.FNumber);
        if (pFldFApenso)
            updateTool.Fields(DBHistoricoDicInfo.Apenso, m_FApenso, ETiposCampos.FNumber);
        if (pFldFIDInstProcesso)
            updateTool.Fields(DBHistoricoDicInfo.IDInstProcesso, m_FIDInstProcesso, ETiposCampos.FNumber);
        if (pFldFFase)
            updateTool.Fields(DBHistoricoDicInfo.Fase, m_FFase, ETiposCampos.FNumber);
        if (pFldFData)
            updateTool.Fields(DBHistoricoDicInfo.Data, m_FData, ETiposCampos.FString);
        if (pFldFObservacao)
            updateTool.Fields(DBHistoricoDicInfo.Observacao, m_FObservacao, ETiposCampos.FString);
        if (pFldFAgendado)
            updateTool.Fields(DBHistoricoDicInfo.Agendado, m_FAgendado, ETiposCampos.FBoolean);
        if (pFldFConcluido)
            updateTool.Fields(DBHistoricoDicInfo.Concluido, m_FConcluido, ETiposCampos.FBoolean);
        if (pFldFMesmaAgenda)
            updateTool.Fields(DBHistoricoDicInfo.MesmaAgenda, m_FMesmaAgenda, ETiposCampos.FBoolean);
        if (pFldFSAD)
            updateTool.Fields(DBHistoricoDicInfo.SAD, m_FSAD, ETiposCampos.FNumber);
        if (pFldFResumido)
            updateTool.Fields(DBHistoricoDicInfo.Resumido, m_FResumido, ETiposCampos.FBoolean);
        if (pFldFStatusAndamento)
            updateTool.Fields(DBHistoricoDicInfo.StatusAndamento, m_FStatusAndamento, ETiposCampos.FNumber);
        if (pFldFTop)
            updateTool.Fields(DBHistoricoDicInfo.Top, m_FTop, ETiposCampos.FBoolean);
        if (pFldFGUID)
            updateTool.Fields(DBHistoricoDicInfo.GUID, m_FGUID, ETiposCampos.FString);
    }

#endif
    private void ConfigureAuditorFields(DBToolWTable32Async updateTool)
    {
        var isInsert = ID.IsEmptyIDNumber();
        if (m_AuditorQuem == 0)
            AuditorQuem = 1;
        if (isInsert)
            updateTool.Fields(DBHistoricoDicInfo.QuemCad, AuditorQuem, ETiposCampos.FNumber);
        if (isInsert)
            updateTool.Fields(DBHistoricoDicInfo.DtCad, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        if (!isInsert)
            updateTool.Fields(DBHistoricoDicInfo.QuemAtu, AuditorQuem, ETiposCampos.FNumber);
        if (!isInsert)
            updateTool.Fields(DBHistoricoDicInfo.DtAtu, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        updateTool.Fields(DBHistoricoDicInfo.Visto, false, ETiposCampos.FBoolean);
    }

    private async Task<int> GravaNewIdAsync(DBToolWTable32Async updateTool, int insertId, MsiSqlConnection oCnn, CancellationToken cancellationToken)
    {
        ID = insertId;
        updateTool.Fields(CampoCodigo, insertId, ETiposCampos.FNumber);
        var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken, true);
        return result == "OK" ? 0 : -3;
    }
}