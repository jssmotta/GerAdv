// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import CargosEscClassWindowId from '@/app/GerAdv_TS/CargosEscClass/Crud/Grids/CargosEscClassWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import CargosEscClassWindowId from '@/app/GerAdv_TS/CargosEscClass/Crud/Grids/CargosEscClassWindowId';
import { CargosEscClassEmpty } from '@/app/GerAdv_TS/Models/CargosEscClass';

describe('CargosEscClassWindowId (basic)', () => {
  it('exports the component', () => {
    expect(CargosEscClassWindowId).toBeDefined();
    expect(typeof CargosEscClassWindowId === 'function' || typeof CargosEscClassWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the CargosEscClassService class so constructor usage doesn't throw and we can control fetchCargosEscClassById
const mockFetchCargosEscClassById = jest.fn();
jest.mock('@/app/GerAdv_TS/CargosEscClass/Services/CargosEscClass.service', () => ({
  CargosEscClassService: jest.fn().mockImplementation(() => ({
    fetchCargosEscClassById: (...args: any[]) => mockFetchCargosEscClassById(...args),
  })),
}));

// Mock the child CargosEscClassWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/CargosEscClass/Crud/Grids/CargosEscClassWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='cargosescclass-window' {...p} />,
}));

describe('CargosEscClassWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(CargosEscClassWindowId).toBeDefined();
    expect(typeof CargosEscClassWindowId === 'function' || typeof CargosEscClassWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <CargosEscClassWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders CargosEscClassWindow with CargosEscClassEmpty when id is 0', async () => {
    render(
      <CargosEscClassWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // CargosEscClassWindow should be rendered with selectedCargosEscClass equal to CargosEscClassEmpty()
    const node = await screen.findByTestId('cargosescclass-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedCargosEscClass"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcargosescclass') || node.getAttribute('selectedCargosEscClass')).not.toBeNull();
  });

  it('fetches cargosescclass by id and passes it to CargosEscClassWindow when id > 0', async () => {
    const fakeCargosEscClass = { id: 123, nomecliente: 'Test' };
    mockFetchCargosEscClassById.mockResolvedValue(fakeCargosEscClass);

    render(
      <CargosEscClassWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('cargosescclass-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchCargosEscClassById).toHaveBeenCalledWith(123));
  });
});
