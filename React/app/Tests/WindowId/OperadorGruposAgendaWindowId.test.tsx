// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import OperadorGruposAgendaWindowId from '@/app/GerAdv_TS/OperadorGruposAgenda/Crud/Grids/OperadorGruposAgendaWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import OperadorGruposAgendaWindowId from '@/app/GerAdv_TS/OperadorGruposAgenda/Crud/Grids/OperadorGruposAgendaWindowId';
import { OperadorGruposAgendaEmpty } from '@/app/GerAdv_TS/Models/OperadorGruposAgenda';

describe('OperadorGruposAgendaWindowId (basic)', () => {
  it('exports the component', () => {
    expect(OperadorGruposAgendaWindowId).toBeDefined();
    expect(typeof OperadorGruposAgendaWindowId === 'function' || typeof OperadorGruposAgendaWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the OperadorGruposAgendaService class so constructor usage doesn't throw and we can control fetchOperadorGruposAgendaById
const mockFetchOperadorGruposAgendaById = jest.fn();
jest.mock('@/app/GerAdv_TS/OperadorGruposAgenda/Services/OperadorGruposAgenda.service', () => ({
  OperadorGruposAgendaService: jest.fn().mockImplementation(() => ({
    fetchOperadorGruposAgendaById: (...args: any[]) => mockFetchOperadorGruposAgendaById(...args),
  })),
}));

// Mock the child OperadorGruposAgendaWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/OperadorGruposAgenda/Crud/Grids/OperadorGruposAgendaWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='operadorgruposagenda-window' {...p} />,
}));

describe('OperadorGruposAgendaWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(OperadorGruposAgendaWindowId).toBeDefined();
    expect(typeof OperadorGruposAgendaWindowId === 'function' || typeof OperadorGruposAgendaWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <OperadorGruposAgendaWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders OperadorGruposAgendaWindow with OperadorGruposAgendaEmpty when id is 0', async () => {
    render(
      <OperadorGruposAgendaWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // OperadorGruposAgendaWindow should be rendered with selectedOperadorGruposAgenda equal to OperadorGruposAgendaEmpty()
    const node = await screen.findByTestId('operadorgruposagenda-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedOperadorGruposAgenda"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedoperadorgruposagenda') || node.getAttribute('selectedOperadorGruposAgenda')).not.toBeNull();
  });

  it('fetches operadorgruposagenda by id and passes it to OperadorGruposAgendaWindow when id > 0', async () => {
    const fakeOperadorGruposAgenda = { id: 123, nomecliente: 'Test' };
    mockFetchOperadorGruposAgendaById.mockResolvedValue(fakeOperadorGruposAgenda);

    render(
      <OperadorGruposAgendaWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('operadorgruposagenda-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchOperadorGruposAgendaById).toHaveBeenCalledWith(123));
  });
});
