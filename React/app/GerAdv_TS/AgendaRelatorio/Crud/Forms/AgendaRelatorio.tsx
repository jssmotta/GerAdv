// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IAgendaRelatorio } from "@/app/GerAdv_TS/AgendaRelatorio/Interfaces/interface.AgendaRelatorio";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useSystemContext } from "@/app/context/SystemContext";
import { getParamFromUrl } from "@/app/tools/helpers";
import FooterIncDocker from "@/app/components/Cruds/FooterIncDocker";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms.css"; // [ INDEX_SIZE ]

import ButtonSalvarCrud from "@/app/components/Cruds/ButtonSalvarCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import DeleteButton from "@/app/components/Cruds/DeleteButton";
import AuditorButton from "@/app/components/Cruds/AuditorButton";
import PrintButton from "@/app/components/Cruds/PrintButton";
import CopyButton from "@/app/components/Cruds/CopyButton";
import { AgendaRelatorioApi } from "../../Apis/ApiAgendaRelatorio";
import { useValidationsAgendaRelatorio } from "../../Hooks/hookAgendaRelatorio";

import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";

interface AgendaRelatorioFormProps {
  agendarelatorioData: IAgendaRelatorio;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const AgendaRelatorioForm: React.FC<AgendaRelatorioFormProps> = ({
  agendarelatorioData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dadoApi = new AgendaRelatorioApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsAgendaRelatorio();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de AgendaRelatorio:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload(); // Recarrega os dados originais
    } else {
      onClose(); // Comportamento padrão se não há callback de recarga
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `AgendaRelatorioForm-${agendarelatorioData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar AgendaRelatorio diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        agendarelatorioData?.id == 0
          ? "Editar AgendaRelatorio"
          : "Adicionar Agenda Relatorio";
    }
  }, [agendarelatorioData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-AgendaRelatorio"
            : "form-container form-container-AgendaRelatorio"
        }
      >
        <form
          className="formInputCadInc"
          id={`AgendaRelatorioForm-${agendarelatorioData.id}`}
          onSubmit={onConfirm}
        >
          {!isMobile && (
            <ButtonSalvarCrud
              isMobile={false}
              validationForm={validationForm}
              entity="AgendaRelatorio"
              data={agendarelatorioData}
              isSubmitting={isSubmitting}
              setIsSubmitting={setIsSubmitting}
              onClose={onClose}
              formId={`AgendaRelatorioForm-${agendarelatorioData.id}`}
              preventPropagation={true}
              onSave={handleDirectSave}
              onCancel={handleCancel}
            />
          )}
          <div className="grid-container">
            <InputName
              type="text"
              id="campo"
              label="advNome"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="campo"
              value={agendarelatorioData.campo}
              placeholder={`Informe advNome`}
              onChange={onChange}
              required
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="vqadata"
              label="Data"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="vqadata"
              value={agendarelatorioData.vqadata}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="vqaprocesso"
              label="Processo"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="vqaprocesso"
              value={agendarelatorioData.vqaprocesso}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={60}
              id="xxxparanome"
              label="ParaNome"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxparanome"
              value={agendarelatorioData.xxxparanome}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2147483647}
              id="xxxparapessoas"
              label="ParaPessoas"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxparapessoas"
              value={agendarelatorioData.xxxparapessoas}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2147483647}
              id="xxxboxaudiencia"
              label="BoxAudiencia"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxboxaudiencia"
              value={agendarelatorioData.xxxboxaudiencia}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2147483647}
              id="xxxboxaudienciamobile"
              label="BoxAudienciaMobile"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxboxaudienciamobile"
              value={agendarelatorioData.xxxboxaudienciamobile}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={50}
              id="xxxnomeadvogado"
              label="NomeAdvogado"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxnomeadvogado"
              value={agendarelatorioData.xxxnomeadvogado}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={40}
              id="xxxnomeforo"
              label="NomeForo"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxnomeforo"
              value={agendarelatorioData.xxxnomeforo}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputInput
              type="text"
              maxLength={50}
              id="xxxnomejustica"
              label="NomeJustica"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxnomejustica"
              value={agendarelatorioData.xxxnomejustica}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={40}
              id="xxxnomearea"
              label="NomeArea"
              dataForm={agendarelatorioData}
              className="input-default-main"
              name="xxxnomearea"
              value={agendarelatorioData.xxxnomearea}
              onChange={onChange}
            />
          </div>
        </form>

        {isMobile && (
          <ButtonSalvarCrud
            isMobile={true}
            validationForm={validationForm}
            entity="AgendaRelatorio"
            data={agendarelatorioData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`AgendaRelatorioForm-${agendarelatorioData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />
        )}

        <FooterIncDocker>
          <CopyButton
            formId={`AgendaRelatorioForm-${agendarelatorioData.id}`}
          />

          <PrintButton
            formId={`AgendaRelatorioForm-${agendarelatorioData.id}`}
          />

          <AuditorButton
            tableName="AgendaRelatorio"
            coolName="Agenda Relatorio"
            id={agendarelatorioData.id}
          />

          <DeleteButton
            page={"/pages/agendarelatorio"}
            id={agendarelatorioData.id}
            dadoApi={dadoApi}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>
    </>
  );
};
