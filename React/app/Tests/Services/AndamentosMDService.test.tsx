// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { AndamentosMDService, AndamentosMDValidator } from '@/app/GerAdv_TS/AndamentosMD/Services/AndamentosMD.service';
import { AndamentosMDApi, AndamentosMDApiError } from '@/app/GerAdv_TS/AndamentosMD/Apis/ApiAndamentosMD';
import { IAndamentosMD } from '@/app/GerAdv_TS/AndamentosMD/Interfaces/interface.AndamentosMD';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as AndamentosMDApi;

const sample: IAndamentosMD = { id: 1, nome: 'Teste' };

describe('AndamentosMDService', () => {
  let service: AndamentosMDService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new AndamentosMDService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(AndamentosMDValidator, 'validateAndamentosMD').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchAndamentosMDById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchAndamentosMDById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchAndamentosMDById lança AndamentosMDApiError para id inválido', async () => {
    await expect(service.fetchAndamentosMDById(0)).rejects.toBeInstanceOf(AndamentosMDApiError);
  });

  it('saveAndamentosMD chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveAndamentosMD(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveAndamentosMD lança AndamentosMDApiError quando validação falha', async () => {
    (AndamentosMDValidator.validateAndamentosMD as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveAndamentosMD(sample)).rejects.toBeInstanceOf(AndamentosMDApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteAndamentosMD chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteAndamentosMD(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteAndamentosMD lança AndamentosMDApiError para id inválido', async () => {
    await expect(service.deleteAndamentosMD(0)).rejects.toBeInstanceOf(AndamentosMDApiError);
  });

  it('validateAndamentosMD delega para AndamentosMDValidator', () => {
    (AndamentosMDValidator.validateAndamentosMD as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateAndamentosMD(sample);
    expect(r.isValid).toBe(true);
  });
});
