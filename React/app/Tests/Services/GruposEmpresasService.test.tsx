// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { GruposEmpresasService, GruposEmpresasValidator } from '@/app/GerAdv_TS/GruposEmpresas/Services/GruposEmpresas.service';
import { GruposEmpresasApi, GruposEmpresasApiError } from '@/app/GerAdv_TS/GruposEmpresas/Apis/ApiGruposEmpresas';
import { IGruposEmpresas } from '@/app/GerAdv_TS/GruposEmpresas/Interfaces/interface.GruposEmpresas';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as GruposEmpresasApi;

const sample: IGruposEmpresas = { id: 1, nome: 'Teste' };

describe('GruposEmpresasService', () => {
  let service: GruposEmpresasService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new GruposEmpresasService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(GruposEmpresasValidator, 'validateGruposEmpresas').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchGruposEmpresasById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchGruposEmpresasById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchGruposEmpresasById lança GruposEmpresasApiError para id inválido', async () => {
    await expect(service.fetchGruposEmpresasById(0)).rejects.toBeInstanceOf(GruposEmpresasApiError);
  });

  it('saveGruposEmpresas chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveGruposEmpresas(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveGruposEmpresas lança GruposEmpresasApiError quando validação falha', async () => {
    (GruposEmpresasValidator.validateGruposEmpresas as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveGruposEmpresas(sample)).rejects.toBeInstanceOf(GruposEmpresasApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteGruposEmpresas chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteGruposEmpresas(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteGruposEmpresas lança GruposEmpresasApiError para id inválido', async () => {
    await expect(service.deleteGruposEmpresas(0)).rejects.toBeInstanceOf(GruposEmpresasApiError);
  });

  it('validateGruposEmpresas delega para GruposEmpresasValidator', () => {
    (GruposEmpresasValidator.validateGruposEmpresas as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateGruposEmpresas(sample);
    expect(r.isValid).toBe(true);
  });
});
