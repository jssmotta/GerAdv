// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { PosicaoOutrasPartesEmpty } from '../../../Models/PosicaoOutrasPartes';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IPosicaoOutrasPartes } from '../../Interfaces/interface.PosicaoOutrasPartes';
import { PosicaoOutrasPartesService } from '../../Services/PosicaoOutrasPartes.service';
import { PosicaoOutrasPartesApi } from '../../Apis/ApiPosicaoOutrasPartes';
import { PosicaoOutrasPartesGridMobileComponent } from '../GridsMobile/PosicaoOutrasPartes';
import { PosicaoOutrasPartesGridDesktopComponent } from '../GridsDesktop/PosicaoOutrasPartes';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterPosicaoOutrasPartes } from '../../Filters/PosicaoOutrasPartes';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import PosicaoOutrasPartesWindow from './PosicaoOutrasPartesWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { usePosicaoOutrasPartesList } from '../../Hooks/hookPosicaoOutrasPartes';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { usePosicaoOutrasPartesFilter } from '../../Hooks/hookPosicaoOutrasPartesFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const PosicaoOutrasPartesGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedPosicaoOutrasPartes, setSelectedPosicaoOutrasPartes] = useState<IPosicaoOutrasPartes>(PosicaoOutrasPartesEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterPosicaoOutrasPartes | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [posicaooutraspartesData, setPosicaoOutrasPartesData] = useState<IPosicaoOutrasPartes[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const posicaooutraspartesService = useMemo(() => {
    return new PosicaoOutrasPartesService(
    new PosicaoOutrasPartesApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: posicaooutraspartes, 
  fetchData, 
  refreshData
} = usePosicaoOutrasPartesList(posicaooutraspartesService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterPosicaoOutrasPartes | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('PosicaoOutrasPartesFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await posicaooutraspartesService.getAll(
  filtro ?? {} as FilterPosicaoOutrasPartes, 
  onlineData => {
    setPosicaoOutrasPartesData(onlineData);
    setLoading(false);
  }
);
setPosicaoOutrasPartesData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterPosicaoOutrasPartes;

    const filtroMem = sessionStorage.getItem(btoa('PosicaoOutrasPartesFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('PosicaoOutrasPartesFilter')) ?? '') || '{}') : {} as FilterPosicaoOutrasPartes;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (posicaooutraspartes: IPosicaoOutrasPartes) => {
    setSelectedPosicaoOutrasPartes(posicaooutraspartes);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedPosicaoOutrasPartes(PosicaoOutrasPartesEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const posicaooutraspartes = e.dataItem;
    setDeleteId(posicaooutraspartes.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await posicaooutraspartesService.deletePosicaoOutrasPartes(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = usePosicaoOutrasPartesFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <PosicaoOutrasPartesGridMobileComponent
        data={posicaooutraspartesData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <PosicaoOutrasPartesGridDesktopComponent
        data={posicaooutraspartesData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <PosicaoOutrasPartesWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedPosicaoOutrasPartes={selectedPosicaoOutrasPartes}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Posicao Outras Partes'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default PosicaoOutrasPartesGrid;