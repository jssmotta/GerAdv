// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for ProcessosObsReport page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock ProcessosObsReportGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/ProcessosObsReport/Adapter/ProcessosObsReportGridAdapter', () => ({
  ProcessosObsReportGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock ProcessosObsReportGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/ProcessosObsReport/Components/ProcessosObsReportGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='processosobsreport-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import ProcessosObsReportPage from '@/app/pages/processosobsreport/page';

describe('ProcessosObsReport page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and ProcessosObsReportGridContainer', () => {
    render(<ProcessosObsReportPage />);

    expect(screen.getByRole('heading', { name: /Processos Obs Report/i })).toBeInTheDocument();
    expect(screen.getByTestId('processosobsreport-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

