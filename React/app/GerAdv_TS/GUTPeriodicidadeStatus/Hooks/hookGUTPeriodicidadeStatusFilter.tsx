// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterGUTPeriodicidadeStatus } from '@/app/GerAdv_TS/GUTPeriodicidadeStatus/Filters/GUTPeriodicidadeStatus';
import GUTAtividadesComboBox from '@/app/GerAdv_TS/GUTAtividades/ComboBox/GUTAtividades';
interface UseGUTPeriodicidadeStatusFilterProps {
  handleFetchWithFilter: (filtro?: FilterGUTPeriodicidadeStatus | undefined | null) => Promise<void>;
}
interface UseGUTPeriodicidadeStatusFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterGUTPeriodicidadeStatus;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterGUTPeriodicidadeStatus>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterGUTPeriodicidadeStatus) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterGUTPeriodicidadeStatus>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useGUTPeriodicidadeStatusFilter = ({ handleFetchWithFilter }: UseGUTPeriodicidadeStatusFilterProps): UseGUTPeriodicidadeStatusFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterGUTPeriodicidadeStatus>({} as FilterGUTPeriodicidadeStatus);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterGUTPeriodicidadeStatus;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterGUTPeriodicidadeStatus) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('GUTPeriodicidadeStatusFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterGUTPeriodicidadeStatus>) => (
  <>
  <GUTAtividadesComboBox
  name='gutatividade'
  dataForm={null}
  value={handlers.windowFilter?.gutatividade}
  setValue={(e:any) => handlers.handleComboChange(e, 'gutatividade')}
  className='inputSearch inputSearchComboboxTab'
  label='G U T Atividades'
  />
  <InputDate
  type='text'
  id='datarealizado'
  label='DataRealizado (igual ou início)'
  dataForm={null}
  className='inputSearch'
  name='datarealizado'
  value={handlers.windowFilter?.datarealizado ?? ''}
  onChange={(value: string) => handlers.handleDateChange('datarealizado', value)}
  />
  <InputDate
  type='text'
  id='datarealizado_end'
  label='DataRealizado (final)'
  dataForm={null}
  className='inputSearch'
  name='datarealizado_end'
  value={handlers.windowFilter?.datarealizado_end ?? ''}
  disabled={handlers.windowFilter?.datarealizado ? false: true}
  onChange={(value: string) => handlers.handleDateChange('datarealizado_end', value)}
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};