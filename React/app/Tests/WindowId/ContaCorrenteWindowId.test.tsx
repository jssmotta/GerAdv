// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ContaCorrenteWindowId from '@/app/GerAdv_TS/ContaCorrente/Crud/Grids/ContaCorrenteWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ContaCorrenteWindowId from '@/app/GerAdv_TS/ContaCorrente/Crud/Grids/ContaCorrenteWindowId';
import { ContaCorrenteEmpty } from '@/app/GerAdv_TS/Models/ContaCorrente';

describe('ContaCorrenteWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ContaCorrenteWindowId).toBeDefined();
    expect(typeof ContaCorrenteWindowId === 'function' || typeof ContaCorrenteWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ContaCorrenteService class so constructor usage doesn't throw and we can control fetchContaCorrenteById
const mockFetchContaCorrenteById = jest.fn();
jest.mock('@/app/GerAdv_TS/ContaCorrente/Services/ContaCorrente.service', () => ({
  ContaCorrenteService: jest.fn().mockImplementation(() => ({
    fetchContaCorrenteById: (...args: any[]) => mockFetchContaCorrenteById(...args),
  })),
}));

// Mock the child ContaCorrenteWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ContaCorrente/Crud/Grids/ContaCorrenteWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='contacorrente-window' {...p} />,
}));

describe('ContaCorrenteWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ContaCorrenteWindowId).toBeDefined();
    expect(typeof ContaCorrenteWindowId === 'function' || typeof ContaCorrenteWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ContaCorrenteWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ContaCorrenteWindow with ContaCorrenteEmpty when id is 0', async () => {
    render(
      <ContaCorrenteWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ContaCorrenteWindow should be rendered with selectedContaCorrente equal to ContaCorrenteEmpty()
    const node = await screen.findByTestId('contacorrente-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedContaCorrente"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcontacorrente') || node.getAttribute('selectedContaCorrente')).not.toBeNull();
  });

  it('fetches contacorrente by id and passes it to ContaCorrenteWindow when id > 0', async () => {
    const fakeContaCorrente = { id: 123, nomecliente: 'Test' };
    mockFetchContaCorrenteById.mockResolvedValue(fakeContaCorrente);

    render(
      <ContaCorrenteWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('contacorrente-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchContaCorrenteById).toHaveBeenCalledWith(123));
  });
});
