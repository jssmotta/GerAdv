// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterTipoCompromisso } from '@/app/GerAdv_TS/TipoCompromisso/Filters/TipoCompromisso';
interface UseTipoCompromissoFilterProps {
  handleFetchWithFilter: (filtro?: FilterTipoCompromisso | undefined | null) => Promise<void>;
}
interface UseTipoCompromissoFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterTipoCompromisso;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterTipoCompromisso>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterTipoCompromisso) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterTipoCompromisso>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useTipoCompromissoFilter = ({ handleFetchWithFilter }: UseTipoCompromissoFilterProps): UseTipoCompromissoFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterTipoCompromisso>({} as FilterTipoCompromisso);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterTipoCompromisso;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterTipoCompromisso) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('TipoCompromissoFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterTipoCompromisso>) => (
  <>
  <InputInput
  type='text'
  id='icone'
  name='icone'
  value={handlers.windowFilter?.icone ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Icone'
  label='Icone (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='icone_end'
  name='icone_end'
  value={handlers.windowFilter?.icone_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Icone final'
  label='Icone final'
  disabled={handlers.windowFilter?.icone ? false: true}
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='descricao'
  name='descricao'
  value={handlers.windowFilter?.descricao ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe Descricao'
  label='Descricao'
  className='inputIncNome inputSearch'
  />
  <InputComboFilterYesNo
  type='text'
  id='financeiro'
  name='financeiro'
  value={handlers.windowFilter?.financeiro ?? -2147483648}
  onChange={handlers.handleInputChange}
  label='Financeiro'
  className='inputSearch inputSearchCheckbox'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};