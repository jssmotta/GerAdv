// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IDivisaoTribunal } from "@/app/GerAdv_TS/DivisaoTribunal/Interfaces/interface.DivisaoTribunal";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { DivisaoTribunalApi } from "../../Apis/DivisaoTribunalApi";
import { useValidationsDivisaoTribunal } from "../../Hooks/hookDivisaoTribunal";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpDivisaoTribunal from "../../Help/HelpDivisaoTribunal";
import DivisaoTribunalPage from "@/app/pages/divisaotribunal/page";

import JusticaComboBox from "@/app/GerAdv_TS/Justica/ComboBox/JusticaComboBox";
import AreaComboBox from "@/app/GerAdv_TS/Area/ComboBox/AreaComboBox";
import CidadeComboBox from "@/app/GerAdv_TS/Cidade/ComboBox/CidadeComboBox";
import ForoComboBox from "@/app/GerAdv_TS/Foro/ComboBox/ForoComboBox";
import TribunalComboBox from "@/app/GerAdv_TS/Tribunal/ComboBox/TribunalComboBox";
import { JusticaApi } from "@/app/GerAdv_TS/Justica/Apis/JusticaApi";
import { AreaApi } from "@/app/GerAdv_TS/Area/Apis/AreaApi";
import { CidadeApi } from "@/app/GerAdv_TS/Cidade/Apis/CidadeApi";
import { ForoApi } from "@/app/GerAdv_TS/Foro/Apis/ForoApi";
import { TribunalApi } from "@/app/GerAdv_TS/Tribunal/Apis/TribunalApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCep from "@/app/components/Inputs/InputCep";

interface DivisaoTribunalFormProps {
  divisaotribunalData: IDivisaoTribunal;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const DivisaoTribunalForm: React.FC<DivisaoTribunalFormProps> = ({
  divisaotribunalData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new DivisaoTribunalApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsDivisaoTribunal();

  const [_nomeJustica, setNomeJustica] = useState("");
  const justicaApi = new JusticaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeArea, setNomeArea] = useState("");
  const areaApi = new AreaApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeCidade, setNomeCidade] = useState("");
  const cidadeApi = new CidadeApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeForo, setNomeForo] = useState("");
  const foroApi = new ForoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [_nomeTribunal, setNomeTribunal] = useState("");
  const tribunalApi = new TribunalApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("justica") > 0) {
    if (divisaotribunalData.id === 0 && divisaotribunalData.justica == 0) {
      justicaApi
        .getById(getParamFromUrl("justica"))
        .then((response) => {
          setNomeJustica(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      divisaotribunalData.justica = getParamFromUrl("justica");
    }
  }

  if (getParamFromUrl("area") > 0) {
    if (divisaotribunalData.id === 0 && divisaotribunalData.area == 0) {
      areaApi
        .getById(getParamFromUrl("area"))
        .then((response) => {
          setNomeArea(response.data.descricao);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      divisaotribunalData.area = getParamFromUrl("area");
    }
  }

  if (getParamFromUrl("cidade") > 0) {
    if (divisaotribunalData.id === 0 && divisaotribunalData.cidade == 0) {
      cidadeApi
        .getById(getParamFromUrl("cidade"))
        .then((response) => {
          setNomeCidade(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      divisaotribunalData.cidade = getParamFromUrl("cidade");
    }
  }

  if (getParamFromUrl("foro") > 0) {
    if (divisaotribunalData.id === 0 && divisaotribunalData.foro == 0) {
      foroApi
        .getById(getParamFromUrl("foro"))
        .then((response) => {
          setNomeForo(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      divisaotribunalData.foro = getParamFromUrl("foro");
    }
  }

  if (getParamFromUrl("tribunal") > 0) {
    if (divisaotribunalData.id === 0 && divisaotribunalData.tribunal == 0) {
      tribunalApi
        .getById(getParamFromUrl("tribunal"))
        .then((response) => {
          setNomeTribunal(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      divisaotribunalData.tribunal = getParamFromUrl("tribunal");
    }
  }
  const addValorJustica = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "justica", value: e.id } });
  };
  const addValorArea = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "area", value: e.id } });
  };
  const addValorCidade = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "cidade", value: e.id } });
  };
  const addValorForo = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "foro", value: e.id } });
  };
  const addValorTribunal = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "tribunal", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de DivisaoTribunal:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `DivisaoTribunalForm-${divisaotribunalData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar DivisaoTribunal diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        divisaotribunalData?.id == 0
          ? "Editar DivisaoTribunal"
          : "Adicionar Divisao Tribunal";
    }
  }, [divisaotribunalData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-DivisaoTribunal"
            : "form-container form-container-DivisaoTribunal"
        }
      >
        <form
          className="formInputCadInc"
          id={`DivisaoTribunalForm-${divisaotribunalData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="DivisaoTribunal"
            data={divisaotribunalData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`DivisaoTribunalForm-${divisaotribunalData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputInput
              tabIndex={0}
              type="text"
              maxLength={2048}
              id="numcodigo"
              label="NumCodigo"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="numcodigo"
              value={divisaotribunalData.numcodigo}
              onChange={onChange}
            />

            <JusticaComboBox
              name={"justica"}
              dataForm={divisaotribunalData}
              value={divisaotribunalData.justica}
              setValue={addValorJustica}
              label={"Justiça"}
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={255}
              id="nomeespecial"
              label="NomeEspecial"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="nomeespecial"
              value={divisaotribunalData.nomeespecial}
              onChange={onChange}
            />

            <AreaComboBox
              name={"area"}
              dataForm={divisaotribunalData}
              value={divisaotribunalData.area}
              setValue={addValorArea}
              label={"Área"}
            />

            <CidadeComboBox
              name={"cidade"}
              dataForm={divisaotribunalData}
              value={divisaotribunalData.cidade}
              setValue={addValorCidade}
              label={"Cidade"}
            />

            <ForoComboBox
              name={"foro"}
              dataForm={divisaotribunalData}
              value={divisaotribunalData.foro}
              setValue={addValorForo}
              label={"Foro"}
            />

            <TribunalComboBox
              name={"tribunal"}
              dataForm={divisaotribunalData}
              value={divisaotribunalData.tribunal}
              setValue={addValorTribunal}
              label={"Tribunal"}
            />

            <InputInput
              required
              tabIndex={2}
              type="text"
              maxLength={5}
              id="codigodiv"
              label="CodigoDiv"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="codigodiv"
              value={divisaotribunalData.codigodiv}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputInput
              tabIndex={3}
              type="text"
              maxLength={40}
              id="endereco"
              label="Endereco"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="endereco"
              value={divisaotribunalData.endereco}
              onChange={onChange}
            />

            <InputInput
              tabIndex={4}
              type="text"
              maxLength={2147483647}
              id="fone"
              label="Fone"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="fone"
              value={divisaotribunalData.fone}
              onChange={onChange}
            />

            <InputInput
              tabIndex={5}
              type="text"
              maxLength={2147483647}
              id="fax"
              label="Fax"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="fax"
              value={divisaotribunalData.fax}
              onChange={onChange}
            />

            <InputCep
              tabIndex={6}
              type="text"
              id="cep"
              label="CEP"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="cep"
              value={divisaotribunalData.cep}
              onChange={onChange}
            />

            <InputInput
              tabIndex={7}
              type="text"
              maxLength={2147483647}
              id="obs"
              label="Obs"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="obs"
              value={divisaotribunalData.obs}
              onChange={onChange}
            />

            <InputInput
              tabIndex={8}
              type="email"
              maxLength={150}
              id="email"
              label="EMail"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="email"
              value={divisaotribunalData.email}
              onChange={onChange}
            />

            <InputInput
              tabIndex={9}
              type="text"
              maxLength={12}
              id="andar"
              label="Andar"
              dataForm={divisaotribunalData}
              className="input-default-main"
              name="andar"
              value={divisaotribunalData.andar}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton
            title="Divisao Tribunal"
            windowCrud={DivisaoTribunalPage}
          />

          <CopyButton
            formId={`DivisaoTribunalForm-${divisaotribunalData.id}`}
          />

          <PrintButton
            formId={`DivisaoTribunalForm-${divisaotribunalData.id}`}
          />

          <AuditorButton
            tableName="DivisaoTribunal"
            coolName="Divisao Tribunal"
            id={divisaotribunalData.id}
          />

          <DeleteButton
            page={"/pages/divisaotribunal"}
            id={divisaotribunalData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Divisao Tribunal"
            page={(onClose) => <HelpDivisaoTribunal onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="DivisaoTribunal"
          data={divisaotribunalData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`DivisaoTribunalForm-${divisaotribunalData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
