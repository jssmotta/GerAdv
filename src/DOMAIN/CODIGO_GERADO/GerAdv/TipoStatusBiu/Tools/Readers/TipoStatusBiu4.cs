// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Fonte:ReadersGenerator
namespace MenphisSI.GerAdv.Readers;
public partial class TipoStatusBiuReader(IFTipoStatusBiuFactory tipostatusbiuFactory) : ITipoStatusBiuReader
{
    private readonly IFTipoStatusBiuFactory _tipostatusbiuFactory = tipostatusbiuFactory ?? throw new ArgumentNullException();
    public async Task<IEnumerable<DBNomeID>?> ListarNAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order) => await DevourerSqlData.ListarNomeID(BuildSqlQuery("tsbCodigo, tsbNome", cWhere, order, max), parameters, uri, caching: false, max: max);
    public async Task<IEnumerable<TipoStatusBiuResponseAll>> ListarAsync(MsiSqlConnection oCnn, int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        var stopwatch = TipoStatusBiuDatabaseMetrics.StartTimer();
        var connStopwatch = TipoStatusBiuDatabaseMetrics.StartTimer();
        try
        {
            TipoStatusBiuDatabaseMetrics.IncrementActiveConnections("ListarAsync", oCnn?.Uri);
            TipoStatusBiuDatabaseMetrics.RecordConnectionOpen("ListarAsync", oCnn?.Uri, connStopwatch);
            var result = await ListarTabelaAsync(oCnn, BuildSqlQuery(DBTipoStatusBiu.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
            TipoStatusBiuDatabaseMetrics.RecordSqlQuery("ListarAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count());
            return result;
        }
        catch (SqlException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ListarAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ListarAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            TipoStatusBiuDatabaseMetrics.DecrementActiveConnections("ListarAsync", oCnn?.Uri);
        }
    }

    private async Task<IEnumerable<TipoStatusBiuResponseAll>> ListarTabelaAsync(MsiSqlConnection? oCnn, string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var stopwatch = TipoStatusBiuDatabaseMetrics.StartTimer();
        var result = new List<TipoStatusBiuResponseAll>(max);
        try
        {
            await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: oCnn?.InnerConnection)
            {
                CommandTimeout = 120
            };
            if (parameters != null && parameters.Count > 0)
                foreach (var param in parameters)
                {
                    if (!cmd.Parameters.Contains(param.ParameterName))
                    {
                        var newParam = new SqlParameter(param.ParameterName, param.Value)
                        {
                            SqlDbType = param.SqlDbType,
                            Direction = param.Direction,
                            Size = param.Size,
                            Precision = param.Precision,
                            Scale = param.Scale
                        };
                        cmd.Parameters.Add(newParam);
                    }
                }

            await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.Default, cancellationToken);
            while (await reader.ReadAsync(cancellationToken))
            {
                if (cancellationToken.IsCancellationRequested)
                    return result;
                result.Add(ReadAll(_tipostatusbiuFactory.CreateFromReader(reader), reader)!);
            }

            TipoStatusBiuDatabaseMetrics.RecordSqlQuery("ListarTabelaAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count);
            return result;
        }
        catch (SqlException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "Timeout", oCnn?.Uri);
            throw;
        }
    }

    public async Task<TipoStatusBiuResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        var stopwatch = TipoStatusBiuDatabaseMetrics.StartTimer();
        try
        {
            TipoStatusBiuDatabaseMetrics.IncrementActiveConnections("ReadAsync", oCnn?.Uri);
            using var dbRec = await _tipostatusbiuFactory.CreateFromIdAsync(id, oCnn);
            var result = dbRec == null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
            TipoStatusBiuDatabaseMetrics.RecordSqlQuery("ReadAsync", "SELECT", oCnn?.Uri, stopwatch, result != null ? 1 : 0);
            return result;
        }
        catch (SqlException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ReadAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            TipoStatusBiuDatabaseMetrics.RecordDatabaseError("ReadAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
        // Ignore
        }
    }

    public async Task<Models.TipoStatusBiu?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _tipostatusbiuFactory.CreateFromIdAsync(id, oCnn);
        var tipostatusbiu = new Models.TipoStatusBiu
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
        };
        return tipostatusbiu;
    }

    public TipoStatusBiuResponse? Read(FTipoStatusBiu dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public TipoStatusBiuResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _tipostatusbiuFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec is null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public TipoStatusBiuResponse? Read(FTipoStatusBiu dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var tipostatusbiu = new TipoStatusBiuResponse
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
        };
        return tipostatusbiu;
    }

    public TipoStatusBiuResponse? Read(SG.GerAdv.DBTipoStatusBiu dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var tipostatusbiu = new TipoStatusBiuResponse
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
        };
        return tipostatusbiu;
    }

    public TipoStatusBiuResponseAll? ReadAll(FTipoStatusBiu dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var tipostatusbiu = new TipoStatusBiuResponseAll
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
        };
        return tipostatusbiu;
    }

    public TipoStatusBiuResponseAll? ReadAll(SG.GerAdv.DBTipoStatusBiu dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var tipostatusbiu = new TipoStatusBiuResponseAll
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
        };
        return tipostatusbiu;
    }
}