// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IViaRecebimento } from "@/app/GerAdv_TS/ViaRecebimento/Interfaces/interface.ViaRecebimento";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useSystemContext } from "@/app/context/SystemContext";
import { getParamFromUrl } from "@/app/tools/helpers";
import FooterIncDocker from "@/app/components/Cruds/FooterIncDocker";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import ButtonSalvarCrud from "@/app/components/Cruds/ButtonSalvarCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import DeleteButton from "@/app/components/Cruds/DeleteButton";
import AuditorButton from "@/app/components/Cruds/AuditorButton";
import PrintButton from "@/app/components/Cruds/PrintButton";
import CopyButton from "@/app/components/Cruds/CopyButton";
import { ViaRecebimentoApi } from "../../Apis/ApiViaRecebimento";
import { useValidationsViaRecebimento } from "../../Hooks/hookViaRecebimento";

import InputName from "@/app/components/Inputs/InputName";

interface ViaRecebimentoFormProps {
  viarecebimentoData: IViaRecebimento;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const ViaRecebimentoForm: React.FC<ViaRecebimentoFormProps> = ({
  viarecebimentoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dadoApi = new ViaRecebimentoApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsViaRecebimento();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de ViaRecebimento:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload(); // Recarrega os dados originais
    } else {
      onClose(); // Comportamento padrão se não há callback de recarga
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `ViaRecebimentoForm-${viarecebimentoData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar ViaRecebimento diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        viarecebimentoData?.id == 0
          ? "Editar ViaRecebimento"
          : "Adicionar Via Recebimento";
    }
  }, [viarecebimentoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-ViaRecebimento"
            : "form-container5 form-container-ViaRecebimento"
        }
      >
        <form
          className="formInputCadInc"
          id={`ViaRecebimentoForm-${viarecebimentoData.id}`}
          onSubmit={onConfirm}
        >
          {!isMobile && (
            <ButtonSalvarCrud
              isMobile={false}
              validationForm={validationForm}
              entity="ViaRecebimento"
              data={viarecebimentoData}
              isSubmitting={isSubmitting}
              setIsSubmitting={setIsSubmitting}
              onClose={onClose}
              formId={`ViaRecebimentoForm-${viarecebimentoData.id}`}
              preventPropagation={true}
              onSave={handleDirectSave}
              onCancel={handleCancel}
            />
          )}
          <div className="grid-container">
            <InputName
              type="text"
              id="nome"
              label="Nome"
              dataForm={viarecebimentoData}
              className="input-default-main"
              name="nome"
              value={viarecebimentoData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />
          </div>
        </form>

        {isMobile && (
          <ButtonSalvarCrud
            isMobile={true}
            validationForm={validationForm}
            entity="ViaRecebimento"
            data={viarecebimentoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`ViaRecebimentoForm-${viarecebimentoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />
        )}

        <FooterIncDocker>
          <CopyButton formId={`ViaRecebimentoForm-${viarecebimentoData.id}`} />

          <PrintButton formId={`ViaRecebimentoForm-${viarecebimentoData.id}`} />

          <AuditorButton
            tableName="ViaRecebimento"
            coolName="Via Recebimento"
            id={viarecebimentoData.id}
          />

          <DeleteButton
            page={"/pages/viarecebimento"}
            id={viarecebimentoData.id}
            dadoApi={dadoApi}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>
    </>
  );
};
