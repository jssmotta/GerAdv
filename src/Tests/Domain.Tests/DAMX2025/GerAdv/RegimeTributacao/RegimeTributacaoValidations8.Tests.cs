// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:RegimeTributacao
// Fonte:ValidationsGeneratorTests
namespace MenphisSI.GerAdv.Tests;
public class RegimeTributacaoValidationTests : IDisposable
{
    private readonly RegimeTributacaoValidation _validation;
    private readonly Mock<IRegimeTributacaoService> _mockRegimeTributacaoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly string _validUri = "test-uri";
    public RegimeTributacaoValidationTests()
    {
        _validation = new RegimeTributacaoValidation();
        _mockRegimeTributacaoService = new Mock<IRegimeTributacaoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void RegimeTributacaoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IRegimeTributacaoValidation>();
    }

    [Fact]
    public void RegimeTributacaoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(RegimeTributacaoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void RegimeTributacaoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(RegimeTributacaoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static GerAdv.Models.RegimeTributacao CreateValidRegimeTributacao()
    {
        return new GerAdv.Models.RegimeTributacao
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockRegimeTributacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterRegimeTributacao>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the RegimeTributacaos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockRegimeTributacaoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterRegimeTributacao>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the RegimeTributacaos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var regimetributacao = new GerAdv.Models.RegimeTributacao
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(regimetributacao, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null !, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var regimetributacao = CreateValidRegimeTributacao();
        regimetributacao.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(regimetributacao, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().MatchRegex("(é obrigatório|não encontrado)");
    }

    [Fact]
    public async Task ValidateReg_WithNull_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var regimetributacao = CreateValidRegimeTributacao();
        regimetributacao.Nome = null !;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(regimetributacao, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidData_Nome_ShouldReturnTrue()
    {
        // Arrange
        var regimetributacao = CreateValidRegimeTributacao();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(regimetributacao, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespace_Nome_ShouldThrowSGValidationException()
    {
        // Arrange
        var regimetributacao = CreateValidRegimeTributacao();
        regimetributacao.Nome = " ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(regimetributacao, _mockRegimeTributacaoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        GC.SuppressFinalize(this);
    }
}