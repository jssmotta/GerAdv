// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class PaisesValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFPaisesFactory> _mockPaisesFactory;
    private readonly Mock<IPaisesReader> _mockReader;
    private readonly PaisesValidation _validation;
    private readonly Mock<IPaisesService> _mockPaisesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly PaisesService _service;
    private readonly string _validUri = "test-uri";
    public PaisesValidationTests()
    {
        _mockPaisesFactory = new Mock<IFPaisesFactory>();
        _mockReader = new Mock<IPaisesReader>();
        _validation = new PaisesValidation();
        _mockPaisesService = new Mock<IPaisesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void PaisesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IPaisesValidation>();
    }

    [Fact]
    public void PaisesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(PaisesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void PaisesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(PaisesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Paises CreateValidPaises()
    {
        return new Models.Paises
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockPaisesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPaises>(), It.IsAny<string>())).ReturnsAsync(new List<PaisesResponseAll>());
    // Setup other mocks but don't override the Paisess service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockPaisesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterPaises>(), It.IsAny<string>())).ReturnsAsync(new List<PaisesResponseAll>());
    // Setup other mocks but don't override the Paisess service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var paises = new Models.Paises
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(paises, _mockPaisesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockPaisesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}