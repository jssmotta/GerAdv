// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { PontoVirtualAcessosEmpty } from '../../../Models/PontoVirtualAcessos';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IPontoVirtualAcessos } from '../../Interfaces/interface.PontoVirtualAcessos';
import { PontoVirtualAcessosService } from '../../Services/PontoVirtualAcessos.service';
import { PontoVirtualAcessosApi } from '../../Apis/ApiPontoVirtualAcessos';
import { PontoVirtualAcessosGridMobileComponent } from '../GridsMobile/PontoVirtualAcessosMobile';
import { PontoVirtualAcessosGridDesktopComponent } from '../GridsDesktop/PontoVirtualAcessosDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterPontoVirtualAcessos } from '../../Filters/PontoVirtualAcessos';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import PontoVirtualAcessosWindow from './PontoVirtualAcessosWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { usePontoVirtualAcessosList } from '../../Hooks/hookPontoVirtualAcessos';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { usePontoVirtualAcessosFilter } from '../../Hooks/hookPontoVirtualAcessosFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const PontoVirtualAcessosGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedPontoVirtualAcessos, setSelectedPontoVirtualAcessos] = useState<IPontoVirtualAcessos>(PontoVirtualAcessosEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterPontoVirtualAcessos | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [pontovirtualacessosData, setPontoVirtualAcessosData] = useState<IPontoVirtualAcessos[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const pontovirtualacessosService = useMemo(() => {
    return new PontoVirtualAcessosService(
    new PontoVirtualAcessosApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: pontovirtualacessos, 
  fetchData, 
  refreshData
} = usePontoVirtualAcessosList(pontovirtualacessosService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterPontoVirtualAcessos | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('PontoVirtualAcessosFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await pontovirtualacessosService.getAll(
  filtro ?? {} as FilterPontoVirtualAcessos, 
  onlineData => {
    setPontoVirtualAcessosData(onlineData);
    setLoading(false);
  }
);
setPontoVirtualAcessosData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterPontoVirtualAcessos;

    const filtroMem = sessionStorage.getItem(btoa('PontoVirtualAcessosFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('PontoVirtualAcessosFilter')) ?? '') || '{}') : {} as FilterPontoVirtualAcessos;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (pontovirtualacessos: IPontoVirtualAcessos) => {
    setSelectedPontoVirtualAcessos(pontovirtualacessos);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedPontoVirtualAcessos(PontoVirtualAcessosEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const pontovirtualacessos = e.dataItem;
    setDeleteId(pontovirtualacessos.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await pontovirtualacessosService.deletePontoVirtualAcessos(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = usePontoVirtualAcessosFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <PontoVirtualAcessosGridMobileComponent
        data={pontovirtualacessosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <PontoVirtualAcessosGridDesktopComponent
        data={pontovirtualacessosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <PontoVirtualAcessosWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedPontoVirtualAcessos={selectedPontoVirtualAcessos}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Ponto Virtual Acessos'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default PontoVirtualAcessosGrid;