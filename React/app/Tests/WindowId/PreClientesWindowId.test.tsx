// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PreClientesWindowId from '@/app/GerAdv_TS/PreClientes/Crud/Grids/PreClientesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PreClientesWindowId from '@/app/GerAdv_TS/PreClientes/Crud/Grids/PreClientesWindowId';
import { PreClientesEmpty } from '@/app/GerAdv_TS/Models/PreClientes';

describe('PreClientesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PreClientesWindowId).toBeDefined();
    expect(typeof PreClientesWindowId === 'function' || typeof PreClientesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PreClientesService class so constructor usage doesn't throw and we can control fetchPreClientesById
const mockFetchPreClientesById = jest.fn();
jest.mock('@/app/GerAdv_TS/PreClientes/Services/PreClientes.service', () => ({
  PreClientesService: jest.fn().mockImplementation(() => ({
    fetchPreClientesById: (...args: any[]) => mockFetchPreClientesById(...args),
  })),
}));

// Mock the child PreClientesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/PreClientes/Crud/Grids/PreClientesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='preclientes-window' {...p} />,
}));

describe('PreClientesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PreClientesWindowId).toBeDefined();
    expect(typeof PreClientesWindowId === 'function' || typeof PreClientesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PreClientesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PreClientesWindow with PreClientesEmpty when id is 0', async () => {
    render(
      <PreClientesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PreClientesWindow should be rendered with selectedPreClientes equal to PreClientesEmpty()
    const node = await screen.findByTestId('preclientes-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPreClientes"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedpreclientes') || node.getAttribute('selectedPreClientes')).not.toBeNull();
  });

  it('fetches preclientes by id and passes it to PreClientesWindow when id > 0', async () => {
    const fakePreClientes = { id: 123, nomecliente: 'Test' };
    mockFetchPreClientesById.mockResolvedValue(fakePreClientes);

    render(
      <PreClientesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('preclientes-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPreClientesById).toHaveBeenCalledWith(123));
  });
});
