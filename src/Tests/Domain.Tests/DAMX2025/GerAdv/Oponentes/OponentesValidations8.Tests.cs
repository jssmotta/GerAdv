// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class OponentesValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFOponentesFactory> _mockOponentesFactory;
    private readonly Mock<IOponentesReader> _mockReader;
    private readonly OponentesValidation _validation;
    private readonly Mock<IOponentesService> _mockOponentesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly Mock<ICidadeReader> _mockCidadeReader;
    private readonly OponentesService _service;
    private readonly string _validUri = "test-uri";
    public OponentesValidationTests()
    {
        _mockOponentesFactory = new Mock<IFOponentesFactory>();
        _mockReader = new Mock<IOponentesReader>();
        _validation = new OponentesValidation();
        _mockOponentesService = new Mock<IOponentesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _mockCidadeReader = new Mock<ICidadeReader>();
    }

#region Class Structure Tests
    [Fact]
    public void OponentesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IOponentesValidation>();
    }

    [Fact]
    public void OponentesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(OponentesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void OponentesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(OponentesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Oponentes CreateValidOponentes()
    {
        return new Models.Oponentes
        {
            Id = 1,
            EMPFuncao = 0,
            CTPSNumero = "AAAAAAAAAAAAA",
            Site = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            CTPSSerie = "AAAAAAAA",
            Nome = "João",
            Adv = 0,
            EMPCliente = 0,
            IDRep = 0,
            PIS = "AAAAAAAAAAAAAAAAAA",
            Contato = "",
            CNPJ = "93016944000138",
            RG = "12.345.678-9",
            Juridica = false,
            Tipo = true,
            Sexo = false,
            CPF = "544.506.718-13",
            Endereco = "Rua das Flores, 123",
            Fone = "(11) 99999-9999",
            Fax = "(11) 88888-9999",
            Cidade = 0,
            Bairro = "Centro",
            CEP = "01234-567",
            InscEst = "AAAAAAAAAAAAA",
            Observacao = "Observação teste",
            EMail = "test@email.com",
            Class = "A",
            Top = true
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll>());
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll>());
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = 0 }));
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var oponentes = new Models.Oponentes
        {
            Id = 1,
            CTPSNumero = null,
            Site = null,
            CTPSSerie = null,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            PIS = null,
            Contato = null,
            CNPJ = null,
            RG = null,
            Juridica = false,
            Tipo = true,
            Sexo = false,
            CPF = null,
            Endereco = null,
            Fone = null,
            Fax = null,
            Bairro = null,
            CEP = null,
            InscEst = null,
            Observacao = null,
            EMail = null,
            Class = null
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region CPF Validation Tests
    [Fact]
    public async Task ValidateReg_WithDuplicateCPF_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CPF = "81833354036";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 2,
            Nome = "Existing User",
            CPF = "81833354036"
        };
        // Setup the Filter method with any filter to see if it's being called
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        // Verify that the Filter method was called
        _mockOponentesService.Verify(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>()), Times.AtLeastOnce);
    }

    [Fact]
    public async Task ValidateReg_WithDuplicateCPF_NoName_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CPF = "34666807004";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 2,
            Nome = "João",
            CPF = "34666807004"
        };
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        exception.Message.Should().Contain("(");
    }

    [Fact]
    public async Task ValidateReg_WithSameCPF_SameId_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CPF = "67290209117";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 1, // Same ID
            Nome = "Same User",
            CPF = "67290209117"
        };
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyCPF_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CPF = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullCPF_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CPF = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region CNPJ Validation Tests
    [Fact]
    public async Task ValidateReg_WithDuplicateCNPJ_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CNPJ = "90724474000179";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 2,
            Nome = "Existing User",
            CNPJ = "90724474000179"
        };
        // Setup the Filter method with any filter to see if it's being called
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
        // Verify that the Filter method was called
        _mockOponentesService.Verify(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>()), Times.AtLeastOnce);
    }

    [Fact]
    public async Task ValidateReg_WithDuplicateCNPJ_NoName_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CNPJ = "84590488000144";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 2,
            Nome = "João",
            CNPJ = "84590488000144"
        };
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("já cadastrado");
    }

    [Fact]
    public async Task ValidateReg_WithSameCNPJ_SameId_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CNPJ = "34692293000137";
        oponentes.Id = 1;
        var existingOponentes = new OponentesResponseAll
        {
            Id = 1, // Same ID
            Nome = "Same User",
            CNPJ = "34692293000137"
        };
        _mockOponentesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterOponentes>(), It.IsAny<string>())).ReturnsAsync(new List<OponentesResponseAll> { existingOponentes });
        // Setup other mocks but don't override the Oponentess service mock
        _mockCidadeReader.Setup(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>())).Returns<int, MsiSqlConnection>((id, conn) => Task.FromResult(new Models.Response.CidadeResponse { Id = id }));
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyCNPJ_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CNPJ = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithNullCNPJ_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.CNPJ = null;
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region EMail Validation Tests
    [Fact]
    public async Task ValidateReg_WithInvalidEMail_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.EMail = "invalid-email";
        SetupValidMocks();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("formato inválido.");
    }

    [Fact]
    public async Task ValidateReg_WithValidEMail_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.EMail = "menphis@example.com";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithEmptyEMail_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.EMail = "";
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
#region Foreign Key Validation Tests - Cidade
    [Fact]
    public async Task ValidateReg_WithInvalidCidade_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.Cidade = 999;
        _mockCidadeReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult<Models.Response.CidadeResponse>(null));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithMismatchedCidadeId_ShouldThrowSGValidationException()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.Cidade = 999;
        var reg888 = new Models.Response.CidadeResponse
        {
            Id = 888
        }; // Different ID
        _mockCidadeReader.Setup(x => x.Read(999, _mockConnection.Object)).Returns(Task.FromResult(reg888));
        SetupValidMocksInvalid();
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("não encontrado");
    }

    [Fact]
    public async Task ValidateReg_WithValidCidade_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.Cidade = 123;
        var reg123 = new Models.Response.CidadeResponse
        {
            Id = 123
        };
        _mockCidadeReader.Setup(x => x.Read(123, _mockConnection.Object)).Returns(Task.FromResult(reg123));
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithEmptyCidade_ShouldPass()
    {
        // Arrange
        var oponentes = CreateValidOponentes();
        oponentes.Cidade = 0; // Assuming 0 is considered empty
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(oponentes, _mockOponentesService.Object, _mockCidadeReader.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
        _mockCidadeReader.Verify(x => x.Read(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}