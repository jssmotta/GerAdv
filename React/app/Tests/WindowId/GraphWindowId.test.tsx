// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GraphWindowId from '@/app/GerAdv_TS/Graph/Crud/Grids/GraphWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GraphWindowId from '@/app/GerAdv_TS/Graph/Crud/Grids/GraphWindowId';
import { GraphEmpty } from '@/app/GerAdv_TS/Models/Graph';

describe('GraphWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GraphWindowId).toBeDefined();
    expect(typeof GraphWindowId === 'function' || typeof GraphWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GraphService class so constructor usage doesn't throw and we can control fetchGraphById
const mockFetchGraphById = jest.fn();
jest.mock('@/app/GerAdv_TS/Graph/Services/Graph.service', () => ({
  GraphService: jest.fn().mockImplementation(() => ({
    fetchGraphById: (...args: any[]) => mockFetchGraphById(...args),
  })),
}));

// Mock the child GraphWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Graph/Crud/Grids/GraphWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='graph-window' {...p} />,
}));

describe('GraphWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GraphWindowId).toBeDefined();
    expect(typeof GraphWindowId === 'function' || typeof GraphWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GraphWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GraphWindow with GraphEmpty when id is 0', async () => {
    render(
      <GraphWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GraphWindow should be rendered with selectedGraph equal to GraphEmpty()
    const node = await screen.findByTestId('graph-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGraph"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgraph') || node.getAttribute('selectedGraph')).not.toBeNull();
  });

  it('fetches graph by id and passes it to GraphWindow when id > 0', async () => {
    const fakeGraph = { id: 123, nomecliente: 'Test' };
    mockFetchGraphById.mockResolvedValue(fakeGraph);

    render(
      <GraphWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('graph-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGraphById).toHaveBeenCalledWith(123));
  });
});
