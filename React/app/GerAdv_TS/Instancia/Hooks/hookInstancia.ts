// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IInstanciaService } from '../Services/Instancia.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IInstancia } from '../Interfaces/interface.Instancia';
import { FilterInstancia } from '../Filters/Instancia';
import { isValidDate } from '@/app/tools/datetime';
import { InstanciaApi } from '../Apis/ApiInstancia';

export const useInstanciaForm = (
  initialInstancia: IInstancia,
  dataService: IInstanciaService
) => {
  const [data, setData] = useState<IInstancia>(initialInstancia);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadInstancia = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialInstancia);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchInstanciaById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Instancia';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar Instancia');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialInstancia]);

  const resetForm = useCallback(() => {
    setData(initialInstancia);
    setError(null);
  }, [initialInstancia]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadInstancia,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadInstancia, resetForm]);
  return returnValue;
};


export const useInstanciaNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('Instancia', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
            console.log('Erro no listener de notificações.');
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useInstanciaList = (dataService: IInstanciaService, currentFilter?: FilterInstancia | undefined | null) => {
  const [data, setData] = useState<IInstancia[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterInstancia | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterInstancia | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterInstancia);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar instancia';
      setError(errorMessage);
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao carregar instancia');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useInstanciaNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsInstancia() {

  async function runValidation(data: IInstancia, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const instanciaApi = new InstanciaApi(uri ?? '', token ?? '');

    const result = await instanciaApi.validation(data);

    return result;
  }

  function validate(data: IInstancia): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        if (data.nroprocesso.length <= 0) { 
                                             return { isValid: false, message: 'O campo NroProcesso não pode ficar vazio.' };
                                         } 
if (data.liminarpedida.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo LiminarPedida não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.objeto.length > 255) { 
                                             return { isValid: false, message: 'O campo Objeto não pode ter mais de 255 caracteres.' };
                                         } 
if (data.liminarresultado.length > 2147483647) { 
                                             return { isValid: false, message: 'O campo LiminarResultado não pode ter mais de 2147483647 caracteres.' };
                                         } 
if (data.nroprocesso.length > 25) { 
                                             return { isValid: false, message: 'O campo NroProcesso não pode ter mais de 25 caracteres.' };
                                         } 
if (data.zkey.length > 25) { 
                                             return { isValid: false, message: 'O campo ZKey não pode ter mais de 25 caracteres.' };
                                         } 
if (data.nroantigo.length > 25) { 
                                             return { isValid: false, message: 'O campo NroAntigo não pode ter mais de 25 caracteres.' };
                                         } 
if (data.accesscode.length > 100) { 
                                             return { isValid: false, message: 'O campo AccessCode não pode ter mais de 100 caracteres.' };
                                         } 
if (data.zkeyia.length > 25) { 
                                             return { isValid: false, message: 'O campo ZKeyIA não pode ter mais de 25 caracteres.' };
                                         } 



        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}