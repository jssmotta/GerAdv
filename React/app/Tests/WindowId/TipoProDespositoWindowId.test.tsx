// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import TipoProDespositoWindowId from '@/app/GerAdv_TS/TipoProDesposito/Crud/Grids/TipoProDespositoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import TipoProDespositoWindowId from '@/app/GerAdv_TS/TipoProDesposito/Crud/Grids/TipoProDespositoWindowId';
import { TipoProDespositoEmpty } from '@/app/GerAdv_TS/Models/TipoProDesposito';

describe('TipoProDespositoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(TipoProDespositoWindowId).toBeDefined();
    expect(typeof TipoProDespositoWindowId === 'function' || typeof TipoProDespositoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the TipoProDespositoService class so constructor usage doesn't throw and we can control fetchTipoProDespositoById
const mockFetchTipoProDespositoById = jest.fn();
jest.mock('@/app/GerAdv_TS/TipoProDesposito/Services/TipoProDesposito.service', () => ({
  TipoProDespositoService: jest.fn().mockImplementation(() => ({
    fetchTipoProDespositoById: (...args: any[]) => mockFetchTipoProDespositoById(...args),
  })),
}));

// Mock the child TipoProDespositoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/TipoProDesposito/Crud/Grids/TipoProDespositoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='tipoprodesposito-window' {...p} />,
}));

describe('TipoProDespositoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(TipoProDespositoWindowId).toBeDefined();
    expect(typeof TipoProDespositoWindowId === 'function' || typeof TipoProDespositoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <TipoProDespositoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders TipoProDespositoWindow with TipoProDespositoEmpty when id is 0', async () => {
    render(
      <TipoProDespositoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // TipoProDespositoWindow should be rendered with selectedTipoProDesposito equal to TipoProDespositoEmpty()
    const node = await screen.findByTestId('tipoprodesposito-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedTipoProDesposito"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedtipoprodesposito') || node.getAttribute('selectedTipoProDesposito')).not.toBeNull();
  });

  it('fetches tipoprodesposito by id and passes it to TipoProDespositoWindow when id > 0', async () => {
    const fakeTipoProDesposito = { id: 123, nomecliente: 'Test' };
    mockFetchTipoProDespositoById.mockResolvedValue(fakeTipoProDesposito);

    render(
      <TipoProDespositoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('tipoprodesposito-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchTipoProDespositoById).toHaveBeenCalledWith(123));
  });
});
