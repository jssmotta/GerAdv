// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useFaseForm, useFaseList, useValidationsFase } from '../GerAdv_TS/Fase/Hooks/hookFase';
import { IFase } from '../GerAdv_TS/Fase/Interfaces/interface.Fase';
import { IFaseService } from '../GerAdv_TS/Fase/Services/Fase.service';
import { FaseTestEmpty } from '../GerAdv_TS/Models/Fase';
import { useFaseComboBox } from '../GerAdv_TS/Fase/Hooks/hookFase';

// Mock do serviço
const mockFaseService: jest.Mocked<IFaseService> = {
  fetchFaseById: jest.fn(),
  saveFase: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteFase: jest.fn(),
  validateFase: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialFase: IFase = { ...FaseTestEmpty() };

describe('useFaseForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    expect(result.current.data).toEqual(initialFase);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Fase',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Fase');
  });

   test('deve carregar Fase por ID', async () => {
    const mockFase = { ...initialFase, id: 1, descricao: 'Fase Teste' };
    mockFaseService.fetchFaseById.mockResolvedValue(mockFase);

    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    await act(async () => {
      await result.current.loadFase(1);
    });

    expect(mockFaseService.fetchFaseById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockFase);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Fase', async () => {
    const errorMessage = 'Erro ao carregar Fase';
    mockFaseService.fetchFaseById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    await act(async () => {
      await result.current.loadFase(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialFase, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialFase);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useFaseForm(initialFase, mockFaseService)
    );

    await act(async () => {
      await result.current.loadFase(0);
    });

    expect(mockFaseService.fetchFaseById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialFase);
  });
});

describe('useFaseList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useFaseList(mockFaseService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialFase, id: 1, descricao: 'Fase 1' },
      { ...initialFase, id: 2, descricao: 'Fase 2' }
    ];
    mockFaseService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useFaseList(mockFaseService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockFaseService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockFaseService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useFaseList(mockFaseService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialFase, id: 1, descricao: 'Fase Filtrado' }];
    const filtro = { descricao: 'Fase' };
    mockFaseService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useFaseList(mockFaseService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockFaseService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsFase', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsFase());

    const validData = { ...initialFase, descricao: 'Fase Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsFase());

    const invalidData = { ...initialFase, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsFase());

    const invalidData = { 
      ...initialFase, 
      descricao: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsFase());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialFase, id: 1, descricao: 'Fase Teste' }];
    mockFaseService.getAll.mockResolvedValue(mockData);
    mockFaseService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useFaseList(mockFaseService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsFase()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Fase Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Fase Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Fase 1' },
      { id: 2, descricao: 'Fase 2' }
    ];
    mockFaseService.getList.mockResolvedValue(mockOptions as IFase[]);


    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Fase 1' },
        { id: 2, nome: 'Fase 2' }
      ]);
    });


    expect(mockFaseService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Fase ABC' },
      { id: 2, descricao: 'Fase XYZ' }
    ];
    mockFaseService.getList.mockResolvedValue(mockOptions as IFase[]);   


 const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Fase ABC' },
        { id: 2, nome: 'Fase XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Fase ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Fase ABC' },
      { id: 2, descricao: 'Fase XYZ' }
    ];
    mockFaseService.getList.mockResolvedValue(mockOptions as IFase[]);
  


    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Fase ABC' },
        { id: 2, nome: 'Fase XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Fase ABC' },
          {id: 2, nome: 'Fase XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );

    const newValue = { id: 1, descricao: 'Fase Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Fase Inicial' };
    
    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useFaseComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Fase Inicial' };
    
    const { result } = renderHook(() => 
      useFaseComboBox(mockFaseService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






