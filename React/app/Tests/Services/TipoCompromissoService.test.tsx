// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoCompromissoService, TipoCompromissoValidator } from '@/app/GerAdv_TS/TipoCompromisso/Services/TipoCompromisso.service';
import { TipoCompromissoApi, TipoCompromissoApiError } from '@/app/GerAdv_TS/TipoCompromisso/Apis/ApiTipoCompromisso';
import { ITipoCompromisso } from '@/app/GerAdv_TS/TipoCompromisso/Interfaces/interface.TipoCompromisso';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoCompromissoApi;

const sample: ITipoCompromisso = { id: 1, nome: 'Teste' };

describe('TipoCompromissoService', () => {
  let service: TipoCompromissoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoCompromissoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoCompromissoValidator, 'validateTipoCompromisso').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoCompromissoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoCompromissoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoCompromissoById lança TipoCompromissoApiError para id inválido', async () => {
    await expect(service.fetchTipoCompromissoById(0)).rejects.toBeInstanceOf(TipoCompromissoApiError);
  });

  it('saveTipoCompromisso chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoCompromisso(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoCompromisso lança TipoCompromissoApiError quando validação falha', async () => {
    (TipoCompromissoValidator.validateTipoCompromisso as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoCompromisso(sample)).rejects.toBeInstanceOf(TipoCompromissoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoCompromisso chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoCompromisso(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoCompromisso lança TipoCompromissoApiError para id inválido', async () => {
    await expect(service.deleteTipoCompromisso(0)).rejects.toBeInstanceOf(TipoCompromissoApiError);
  });

  it('validateTipoCompromisso delega para TipoCompromissoValidator', () => {
    (TipoCompromissoValidator.validateTipoCompromisso as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoCompromisso(sample);
    expect(r.isValid).toBe(true);
  });
});
