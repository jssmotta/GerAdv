// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class CargosValidationTests : IDisposable
{
    private readonly CargosValidation _validation;
    private readonly Mock<ICargosService> _mockCargosService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly CargosService _service;
    private readonly string _validUri = "test-uri";
    public CargosValidationTests()
    {
        _validation = new CargosValidation();
        _mockCargosService = new Mock<ICargosService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void CargosValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ICargosValidation>();
    }

    [Fact]
    public void CargosValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(CargosValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void CargosValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(CargosValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.Cargos CreateValidCargos()
    {
        return new Models.Cargos
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockCargosService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargos>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the Cargoss service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockCargosService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterCargos>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the Cargoss service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var cargos = new Models.Cargos
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(cargos, _mockCargosService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockCargosService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}