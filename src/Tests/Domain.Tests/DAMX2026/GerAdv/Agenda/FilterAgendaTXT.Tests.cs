// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Agenda
// Fonte:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterAgendaTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterAgendaTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithIDCOB_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            IDCOB = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"idcob\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithIDNE_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            IDNE = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"idne\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCidade_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Cidade = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cidade\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithOculto_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Oculto = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"oculto\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCartaPrecatoria_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            CartaPrecatoria = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cartaprecatoria\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithHrFinal_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            HrFinal = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"hrfinal\"");
    }

    [Fact]
    public void GetPrompt_WithAdvogado_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Advogado = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"advogado\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithEventoGerador_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            EventoGerador = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"eventogerador\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithEventoData_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            EventoData = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"eventodata\"");
    }

    [Fact]
    public void GetPrompt_WithFuncionario_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Funcionario = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"funcionario\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithData_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Data = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"data\"");
    }

    [Fact]
    public void GetPrompt_WithEventoPrazo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            EventoPrazo = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"eventoprazo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithHora_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Hora = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"hora\"");
    }

    [Fact]
    public void GetPrompt_WithCompromisso_SerializesCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Compromisso = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"compromisso\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithTipoCompromisso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            TipoCompromisso = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"tipocompromisso\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithCliente_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Cliente = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"cliente\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithArea_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Area = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"area\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithJustica_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Justica = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"justica\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithProcesso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Processo = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"processo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithIDHistorico_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            IDHistorico = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"idhistorico\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithIDInsProcesso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            IDInsProcesso = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"idinsprocesso\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithUsuario_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Usuario = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"usuario\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithPreposto_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Preposto = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"preposto\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithQuemID_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            QuemID = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemid\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithQuemCodigo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            QuemCodigo = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcodigo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithStatus_SerializesCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Status = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"status\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithValor_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Valor = 123.45m
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"valor\"");
        prompt.Should().Contain("123.45");
    }

    [Fact]
    public void GetPrompt_WithDecisao_SerializesCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Decisao = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"decisao\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithSempre_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Sempre = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"sempre\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithPrazoDias_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            PrazoDias = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"prazodias\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithProtocoloIntegrado_SerializesNumericValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            ProtocoloIntegrado = 123
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"protocolointegrado\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDataInicioPrazo_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            DataInicioPrazo = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"datainicioprazo\"");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterCidade\"");
        prompt.Should().Contain("\"filterAdvogados\"");
        prompt.Should().Contain("\"filterFuncionarios\"");
        prompt.Should().Contain("\"filterTipoCompromisso\"");
        prompt.Should().Contain("\"filterClientes\"");
        prompt.Should().Contain("\"filterArea\"");
        prompt.Should().Contain("\"filterJustica\"");
        prompt.Should().Contain("\"filterOperador\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(new FilterAgenda(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(new FilterAgenda(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(new FilterAgenda(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(new FilterAgenda(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterAgendaTXT.GetOptions();
        var filter = new FilterAgenda
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterAgendaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}