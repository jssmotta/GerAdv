// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoProDespositoService, TipoProDespositoValidator } from '@/app/GerAdv_TS/TipoProDesposito/Services/TipoProDesposito.service';
import { TipoProDespositoApi, TipoProDespositoApiError } from '@/app/GerAdv_TS/TipoProDesposito/Apis/ApiTipoProDesposito';
import { ITipoProDesposito } from '@/app/GerAdv_TS/TipoProDesposito/Interfaces/interface.TipoProDesposito';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoProDespositoApi;

const sample: ITipoProDesposito = { id: 1, nome: 'Teste' };

describe('TipoProDespositoService', () => {
  let service: TipoProDespositoService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoProDespositoService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoProDespositoValidator, 'validateTipoProDesposito').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoProDespositoById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoProDespositoById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoProDespositoById lança TipoProDespositoApiError para id inválido', async () => {
    await expect(service.fetchTipoProDespositoById(0)).rejects.toBeInstanceOf(TipoProDespositoApiError);
  });

  it('saveTipoProDesposito chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoProDesposito(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoProDesposito lança TipoProDespositoApiError quando validação falha', async () => {
    (TipoProDespositoValidator.validateTipoProDesposito as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoProDesposito(sample)).rejects.toBeInstanceOf(TipoProDespositoApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoProDesposito chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoProDesposito(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoProDesposito lança TipoProDespositoApiError para id inválido', async () => {
    await expect(service.deleteTipoProDesposito(0)).rejects.toBeInstanceOf(TipoProDespositoApiError);
  });

  it('validateTipoProDesposito delega para TipoProDespositoValidator', () => {
    (TipoProDespositoValidator.validateTipoProDesposito as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoProDesposito(sample);
    expect(r.isValid).toBe(true);
  });
});
