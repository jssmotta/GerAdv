// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ModelosDocumentosWindowId from '@/app/GerAdv_TS/ModelosDocumentos/Crud/Grids/ModelosDocumentosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ModelosDocumentosWindowId from '@/app/GerAdv_TS/ModelosDocumentos/Crud/Grids/ModelosDocumentosWindowId';
import { ModelosDocumentosEmpty } from '@/app/GerAdv_TS/Models/ModelosDocumentos';

describe('ModelosDocumentosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ModelosDocumentosWindowId).toBeDefined();
    expect(typeof ModelosDocumentosWindowId === 'function' || typeof ModelosDocumentosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ModelosDocumentosService class so constructor usage doesn't throw and we can control fetchModelosDocumentosById
const mockFetchModelosDocumentosById = jest.fn();
jest.mock('@/app/GerAdv_TS/ModelosDocumentos/Services/ModelosDocumentos.service', () => ({
  ModelosDocumentosService: jest.fn().mockImplementation(() => ({
    fetchModelosDocumentosById: (...args: any[]) => mockFetchModelosDocumentosById(...args),
  })),
}));

// Mock the child ModelosDocumentosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/ModelosDocumentos/Crud/Grids/ModelosDocumentosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='modelosdocumentos-window' {...p} />,
}));

describe('ModelosDocumentosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ModelosDocumentosWindowId).toBeDefined();
    expect(typeof ModelosDocumentosWindowId === 'function' || typeof ModelosDocumentosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ModelosDocumentosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ModelosDocumentosWindow with ModelosDocumentosEmpty when id is 0', async () => {
    render(
      <ModelosDocumentosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ModelosDocumentosWindow should be rendered with selectedModelosDocumentos equal to ModelosDocumentosEmpty()
    const node = await screen.findByTestId('modelosdocumentos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedModelosDocumentos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedmodelosdocumentos') || node.getAttribute('selectedModelosDocumentos')).not.toBeNull();
  });

  it('fetches modelosdocumentos by id and passes it to ModelosDocumentosWindow when id > 0', async () => {
    const fakeModelosDocumentos = { id: 123, nomecliente: 'Test' };
    mockFetchModelosDocumentosById.mockResolvedValue(fakeModelosDocumentos);

    render(
      <ModelosDocumentosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('modelosdocumentos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchModelosDocumentosById).toHaveBeenCalledWith(123));
  });
});
