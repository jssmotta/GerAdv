// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { SetorService, SetorValidator } from '@/app/GerAdv_TS/Setor/Services/Setor.service';
import { SetorApi, SetorApiError } from '@/app/GerAdv_TS/Setor/Apis/ApiSetor';
import { ISetor } from '@/app/GerAdv_TS/Setor/Interfaces/interface.Setor';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as SetorApi;

const sample: ISetor = { id: 1, nome: 'Teste' };

describe('SetorService', () => {
  let service: SetorService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new SetorService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(SetorValidator, 'validateSetor').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchSetorById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchSetorById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchSetorById lança SetorApiError para id inválido', async () => {
    await expect(service.fetchSetorById(0)).rejects.toBeInstanceOf(SetorApiError);
  });

  it('saveSetor chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveSetor(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveSetor lança SetorApiError quando validação falha', async () => {
    (SetorValidator.validateSetor as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveSetor(sample)).rejects.toBeInstanceOf(SetorApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteSetor chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteSetor(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteSetor lança SetorApiError para id inválido', async () => {
    await expect(service.deleteSetor(0)).rejects.toBeInstanceOf(SetorApiError);
  });

  it('validateSetor delega para SetorValidator', () => {
    (SetorValidator.validateSetor as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateSetor(sample);
    expect(r.isValid).toBe(true);
  });
});
