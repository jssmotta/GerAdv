// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ServicosService, ServicosValidator } from '@/app/GerAdv_TS/Servicos/Services/Servicos.service';
import { ServicosApi, ServicosApiError } from '@/app/GerAdv_TS/Servicos/Apis/ApiServicos';
import { IServicos } from '@/app/GerAdv_TS/Servicos/Interfaces/interface.Servicos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ServicosApi;

const sample: IServicos = { id: 1, nome: 'Teste' };

describe('ServicosService', () => {
  let service: ServicosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ServicosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ServicosValidator, 'validateServicos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchServicosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchServicosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchServicosById lança ServicosApiError para id inválido', async () => {
    await expect(service.fetchServicosById(0)).rejects.toBeInstanceOf(ServicosApiError);
  });

  it('saveServicos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveServicos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveServicos lança ServicosApiError quando validação falha', async () => {
    (ServicosValidator.validateServicos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveServicos(sample)).rejects.toBeInstanceOf(ServicosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteServicos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteServicos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteServicos lança ServicosApiError para id inválido', async () => {
    await expect(service.deleteServicos(0)).rejects.toBeInstanceOf(ServicosApiError);
  });

  it('validateServicos delega para ServicosValidator', () => {
    (ServicosValidator.validateServicos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateServicos(sample);
    expect(r.isValid).toBe(true);
  });
});
