// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ModelosDocumentosService, ModelosDocumentosValidator } from '@/app/GerAdv_TS/ModelosDocumentos/Services/ModelosDocumentos.service';
import { ModelosDocumentosApi, ModelosDocumentosApiError } from '@/app/GerAdv_TS/ModelosDocumentos/Apis/ApiModelosDocumentos';
import { IModelosDocumentos } from '@/app/GerAdv_TS/ModelosDocumentos/Interfaces/interface.ModelosDocumentos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ModelosDocumentosApi;

const sample: IModelosDocumentos = { id: 1, nome: 'Teste' };

describe('ModelosDocumentosService', () => {
  let service: ModelosDocumentosService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ModelosDocumentosService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ModelosDocumentosValidator, 'validateModelosDocumentos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchModelosDocumentosById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchModelosDocumentosById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchModelosDocumentosById lança ModelosDocumentosApiError para id inválido', async () => {
    await expect(service.fetchModelosDocumentosById(0)).rejects.toBeInstanceOf(ModelosDocumentosApiError);
  });

  it('saveModelosDocumentos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveModelosDocumentos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveModelosDocumentos lança ModelosDocumentosApiError quando validação falha', async () => {
    (ModelosDocumentosValidator.validateModelosDocumentos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveModelosDocumentos(sample)).rejects.toBeInstanceOf(ModelosDocumentosApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteModelosDocumentos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteModelosDocumentos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteModelosDocumentos lança ModelosDocumentosApiError para id inválido', async () => {
    await expect(service.deleteModelosDocumentos(0)).rejects.toBeInstanceOf(ModelosDocumentosApiError);
  });

  it('validateModelosDocumentos delega para ModelosDocumentosValidator', () => {
    (ModelosDocumentosValidator.validateModelosDocumentos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateModelosDocumentos(sample);
    expect(r.isValid).toBe(true);
  });
});
