// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useProPartesForm, useProPartesList, useValidationsProPartes } from '../GerAdv_TS/ProPartes/Hooks/hookProPartes';
import { IProPartes } from '../GerAdv_TS/ProPartes/Interfaces/interface.ProPartes';
import { IProPartesService } from '../GerAdv_TS/ProPartes/Services/ProPartes.service';
import { ProPartesTestEmpty } from '../GerAdv_TS/Models/ProPartes';


// Mock do serviço
const mockProPartesService: jest.Mocked<IProPartesService> = {
  fetchProPartesById: jest.fn(),
  saveProPartes: jest.fn(),
  
  getAll: jest.fn(),
  deleteProPartes: jest.fn(),
  validateProPartes: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialProPartes: IProPartes = { ...ProPartesTestEmpty() };

describe('useProPartesForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    expect(result.current.data).toEqual(initialProPartes);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    const mockEvent = {
      target: {
        name: 'campo',
        value: 'Novo Pro Partes',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.campo).toBe('Novo Pro Partes');
  });

   test('deve carregar Pro Partes por ID', async () => {
    const mockProPartes = { ...initialProPartes, id: 1, campo: 'Pro Partes Teste' };
    mockProPartesService.fetchProPartesById.mockResolvedValue(mockProPartes);

    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    await act(async () => {
      await result.current.loadProPartes(1);
    });

    expect(mockProPartesService.fetchProPartesById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockProPartes);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Pro Partes', async () => {
    const errorMessage = 'Erro ao carregar Pro Partes';
    mockProPartesService.fetchProPartesById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    await act(async () => {
      await result.current.loadProPartes(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialProPartes, campo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialProPartes);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useProPartesForm(initialProPartes, mockProPartesService)
    );

    await act(async () => {
      await result.current.loadProPartes(0);
    });

    expect(mockProPartesService.fetchProPartesById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialProPartes);
  });
});

describe('useProPartesList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useProPartesList(mockProPartesService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialProPartes, id: 1, campo: 'Pro Partes 1' },
      { ...initialProPartes, id: 2, campo: 'Pro Partes 2' }
    ];
    mockProPartesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProPartesList(mockProPartesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockProPartesService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockProPartesService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useProPartesList(mockProPartesService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialProPartes, id: 1, campo: 'Pro Partes Filtrado' }];
    const filtro = { campo: 'Pro Partes' };
    mockProPartesService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useProPartesList(mockProPartesService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockProPartesService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsProPartes', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsProPartes());

    const validData = { ...initialProPartes, campo: 'Pro Partes Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsProPartes());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialProPartes, id: 1, campo: 'Pro Partes Teste' }];
    mockProPartesService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useProPartesList(mockProPartesService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsProPartes()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});