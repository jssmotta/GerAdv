// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBRamal - Nome da classe (ex: DBClientes)
/// Ramal - Nome da tabela (ex: Clientes)
/// ram - Prefixo dos campos (ex: cli)
/// ramCodigo - Nome do campo código (ex: cliCodigo)
/// ramNome - Nome do campo nome (ex: cliNome)
/// DBRamal.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBRamalTests : IDisposable
{
    private readonly DBRamal _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBRamalTests()
    {
        _instance = new DBRamal();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("ramCodigo", typeof(int));
        dt.Columns.Add("ramQuemCad", typeof(int));
        dt.Columns.Add("ramDtCad", typeof(DateTime));
        dt.Columns.Add("ramQuemAtu", typeof(int));
        dt.Columns.Add("ramDtAtu", typeof(DateTime));
        dt.Columns.Add("ramVisto", typeof(bool));
        dt.Columns.Add("ramNome", typeof(string));
        dt.Columns.Add("ramObs", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("Ramal", DBRamal.PTabelaNome);
        Assert.Equal(" Ramal.* ", DBRamal.CamposSqlX);
        Assert.Equal("ramCodigo", DBRamal.CampoCodigo);
        Assert.Equal("ramNome", DBRamal.CampoNome);
        Assert.Equal("ram", DBRamal.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBRamal();
        Assert.Equal(0, instance.ID);
        Assert.Equal("Ramal", instance.ITabelaName());
        Assert.Equal("ram", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBRamal((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBRamal((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["ramCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBRamal(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("Ramal", cadastro.ITabelaName());
        Assert.Equal("ramCodigo", cadastro.ICampoCodigo());
        Assert.Equal("ramNome", cadastro.ICampoNome());
        Assert.Equal("ram", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBRamal.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBRamal.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBRamal.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBRamal();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBRamal();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 50 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 50);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Obs_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FObs = input;
        Assert.Equal(expected, _instance.FObs);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}