// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import CidadeWindowId from '@/app/GerAdv_TS/Cidade/Crud/Grids/CidadeWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import CidadeWindowId from '@/app/GerAdv_TS/Cidade/Crud/Grids/CidadeWindowId';
import { CidadeEmpty } from '@/app/GerAdv_TS/Models/Cidade';

describe('CidadeWindowId (basic)', () => {
  it('exports the component', () => {
    expect(CidadeWindowId).toBeDefined();
    expect(typeof CidadeWindowId === 'function' || typeof CidadeWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the CidadeService class so constructor usage doesn't throw and we can control fetchCidadeById
const mockFetchCidadeById = jest.fn();
jest.mock('@/app/GerAdv_TS/Cidade/Services/Cidade.service', () => ({
  CidadeService: jest.fn().mockImplementation(() => ({
    fetchCidadeById: (...args: any[]) => mockFetchCidadeById(...args),
  })),
}));

// Mock the child CidadeWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Cidade/Crud/Grids/CidadeWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='cidade-window' {...p} />,
}));

describe('CidadeWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(CidadeWindowId).toBeDefined();
    expect(typeof CidadeWindowId === 'function' || typeof CidadeWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <CidadeWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders CidadeWindow with CidadeEmpty when id is 0', async () => {
    render(
      <CidadeWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // CidadeWindow should be rendered with selectedCidade equal to CidadeEmpty()
    const node = await screen.findByTestId('cidade-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedCidade"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedcidade') || node.getAttribute('selectedCidade')).not.toBeNull();
  });

  it('fetches cidade by id and passes it to CidadeWindow when id > 0', async () => {
    const fakeCidade = { id: 123, nomecliente: 'Test' };
    mockFetchCidadeById.mockResolvedValue(fakeCidade);

    render(
      <CidadeWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('cidade-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchCidadeById).toHaveBeenCalledWith(123));
  });
});
