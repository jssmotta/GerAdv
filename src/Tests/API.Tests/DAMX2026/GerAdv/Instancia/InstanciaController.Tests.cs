// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Entity:Instancia
// Source:ControllerGeneratorTests
using Resp = MenphisSI.GerAdv.Models.Response;
using RespAll = MenphisSI.GerAdv.Models.Response.All;

namespace MenphisSI.GerAdv.Tests.Controllers;
public class InstanciaControllerTests
{
    private readonly Mock<IInstanciaService> _mockService;
    private readonly InstanciaController _controller;
    public InstanciaControllerTests()
    {
        _mockService = new Mock<IInstanciaService>();
        _controller = new InstanciaController(_mockService.Object);
        // Setup ModelState for validation tests
        _controller.ControllerContext = new ControllerContext
        {
            HttpContext = new DefaultHttpContext()
        };
    }

    [Fact]
    public async Task GetAll_WithValidParameters_ReturnsOkWithData()
    {
        // Arrange
        var max = 10;
        var expectedData = new List<RespAll.InstanciaResponseAll>
        {
            new()
            {
                Id = 1
            },
            new()
            {
                Id = 2
            }
        };
        _mockService.Setup(x => x.GetAll(max, "test-tenantKey", It.IsAny<CancellationToken>())).ReturnsAsync(ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>.Ok(expectedData));
        // Act
        var actionResult = await _controller.GetAll(max, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>;
        resultApi?.Success.Should().BeTrue();
        resultApi?.Data.Should().BeEquivalentTo(expectedData);
        _mockService.Verify(x => x.GetAll(max, "test-tenantKey", It.IsAny<CancellationToken>()), Times.Once);
    }

    [Fact]
    public async Task GetAll_WithZeroMax_ReturnsEmptyList()
    {
        // Arrange
        var max = 0;
        var expectedData = new List<RespAll.InstanciaResponseAll>();
        _mockService.Setup(x => x.GetAll(max, "test-tenantKey", It.IsAny<CancellationToken>())).ReturnsAsync(ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>.Ok(expectedData));
        // Act
        var actionResult = await _controller.GetAll(max, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>;
        resultApi?.Data.Should().BeEquivalentTo(expectedData);
    }

    [Fact]
    public async Task Filter_WithValidFilter_ReturnsFilteredData()
    {
        // Arrange
        var max = 10;
        var filter = new Filters.FilterInstancia
        { /* Set filter properties */
        };
        var expectedData = new List<RespAll.InstanciaResponseAll>
        {
            new()
            {
                Id = 1
            }
        };
        _mockService.Setup(x => x.Filter(max, filter, "test-tenantKey")).ReturnsAsync(ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>.Ok(expectedData));
        // Act
        var actionResult = await _controller.Filter(max, filter, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<IEnumerable<RespAll.InstanciaResponseAll>>;
        resultApi?.Data.Should().BeEquivalentTo(expectedData);
    }

    [Fact]
    public async Task Filter_WithInvalidModelState_ReturnsBadRequest()
    {
        // Arrange
        _controller.ModelState.AddModelError("test", "Test error");
        var filter = new Filters.FilterInstancia();
        // Act
        var actionResult = await _controller.Filter(10, filter, "test-tenantKey");
        // Assert
        actionResult.Result.Should().BeOfType<BadRequestObjectResult>();
    }

    [Fact]
    public async Task GetById_WithExistingId_ReturnsOkWithData()
    {
        // Arrange
        int id = 1;
        var expectedData = new Resp.InstanciaResponse
        {
            Id = id
        };
        _mockService.Setup(x => x.GetById(id, "test-tenantKey", It.IsAny<CancellationToken>())).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Ok(expectedData));
        // Act
        var actionResult = await _controller.GetById(id, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Data.Should().BeEquivalentTo(expectedData);
    }

    [Fact]
    public async Task GetById_WithNonExistingId_ReturnsNotFound()
    {
        // Arrange
        int id = 999;
        _mockService.Setup(x => x.GetById(id, "test-tenantKey", It.IsAny<CancellationToken>())).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.NotFound($"Registro não encontrado para id {id}"));
        // Act
        var actionResult = await _controller.GetById(id, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(404);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
    }

    [Fact]
    public async Task GetById_WithCancellation_PropagatesCancellationToken()
    {
        // Arrange
        int id = 1;
        var cts = new CancellationTokenSource();
        cts.Cancel();
        _mockService.Setup(x => x.GetById(id, "test-tenantKey", It.IsAny<CancellationToken>())).ThrowsAsync(new OperationCanceledException());
        // Act & Assert
        await Assert.ThrowsAsync<OperationCanceledException>(() => _controller.GetById(id, "test-tenantKey", cts.Token));
    }

    [Fact]
    public async Task GetListN_WithValidParameters_ReturnsOkWithData()
    {
        // Arrange
        var max = 10;
        var filter = new Filters.FilterInstancia();
        var expectedData = new List<NomeID>
        {
            new NomeID
            {
                ID = 1,
                Nome = "Teste"
            }
        };
        _mockService.Setup(x => x.GetListN(max, filter, "test-tenantKey", It.IsAny<CancellationToken>())).ReturnsAsync(ResultApi<IEnumerable<NomeID>>.Ok(expectedData));
        // Act
        var actionResult = await _controller.GetListN(max, filter, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<IEnumerable<NomeID>>;
        resultApi?.Data.Should().BeEquivalentTo(expectedData);
    }

    [Fact]
    public async Task AddAndUpdate_WithValidData_ReturnsCreatedWithResult()
    {
        // Arrange
        var model = new Models.Instancia
        {
            Id = 0,
        // Set other required properties
        };
        var expectedResult = new Resp.InstanciaResponse
        {
            Id = 1
        };
        _mockService.Setup(x => x.AddAndUpdate(It.IsAny<Models.Instancia>(), "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Created(expectedResult));
        // Act
        var actionResult = await _controller.AddAndUpdate(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(201);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Data.Should().BeEquivalentTo(expectedResult);
    }

    [Fact]
    public async Task AddAndUpdate_WithInvalidModelState_ReturnsBadRequest()
    {
        // Arrange
        _controller.ModelState.AddModelError("test", "Test error");
        var model = new Models.Instancia();
        // Act
        var actionResult = await _controller.AddAndUpdate(model, "test-tenantKey");
        // Assert
        actionResult.Result.Should().BeOfType<BadRequestObjectResult>();
    }

    [Fact]
    public async Task AddAndUpdate_WhenServiceReturnsFail_ReturnsBadRequest()
    {
        // Arrange
        var model = new Models.Instancia();
        _mockService.Setup(x => x.AddAndUpdate(It.IsAny<Models.Instancia>(), "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Fail("Falha ao processar", 400));
        // Act
        var actionResult = await _controller.AddAndUpdate(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(400);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
    }

    [Fact]
    public async Task AddAndUpdate_WhenExceptionOccurs_ReturnsInternalServerError()
    {
        // Arrange
        var model = new Models.Instancia();
        _mockService.Setup(x => x.AddAndUpdate(It.IsAny<Models.Instancia>(), "test-tenantKey")).ThrowsAsync(new Exception("Test exception"));
        // Act
        var actionResult = await _controller.AddAndUpdate(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(500);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
        resultApi?.Message.Should().Be("Test exception");
    }

    [Fact]
    public async Task Delete_WithExistingId_ReturnsOkWithResult()
    {
        // Arrange
        int id = 1;
        var expectedResult = new Resp.InstanciaResponse
        {
            Id = id
        };
        _mockService.Setup(x => x.Delete(id, "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Ok(expectedResult));
        // Act
        var actionResult = await _controller.Delete(id, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Data.Should().BeEquivalentTo(expectedResult);
    }

    [Fact]
    public async Task Delete_WithNonExistingId_ReturnsNotFound()
    {
        // Arrange
        int id = 999;
        _mockService.Setup(x => x.Delete(id, "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.NotFound("Registro não encontrado"));
        // Act
        var actionResult = await _controller.Delete(id, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(404);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
    }

    [Fact]
    public async Task Delete_WhenGenericExceptionOccurs_ReturnsInternalServerError()
    {
        // Arrange
        int id = 1;
        _mockService.Setup(x => x.Delete(id, "test-tenantKey")).ThrowsAsync(new Exception("Delete failed"));
        // Act
        var actionResult = await _controller.Delete(id, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(500);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
        resultApi?.Message.Should().Be("Delete failed");
    }

    [Fact]
    public async Task Validation_WithValidData_ReturnsOkWithResult()
    {
        // Arrange
        var model = new Models.Instancia
        {
        // Set required properties
        };
        var expectedResult = new Resp.InstanciaResponse
        {
            Id = 1
        };
        _mockService.Setup(x => x.Validation(It.IsAny<Models.Instancia>(), "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Ok(expectedResult));
        // Act
        var actionResult = await _controller.Validation(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(200);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Data.Should().BeEquivalentTo(expectedResult);
    }

    [Fact]
    public async Task Validation_WithInvalidModelState_ReturnsBadRequest()
    {
        // Arrange
        _controller.ModelState.AddModelError("test", "Test error");
        var model = new Models.Instancia();
        // Act
        var actionResult = await _controller.Validation(model, "test-tenantKey");
        // Assert
        actionResult.Result.Should().BeOfType<BadRequestObjectResult>();
    }

    [Fact]
    public async Task Validation_WhenServiceReturnsFail_ReturnsBadRequest()
    {
        // Arrange
        var model = new Models.Instancia();
        _mockService.Setup(x => x.Validation(It.IsAny<Models.Instancia>(), "test-tenantKey")).ReturnsAsync(ResultApi<Resp.InstanciaResponse>.Fail("Falha na validação", 400));
        // Act
        var actionResult = await _controller.Validation(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(400);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
    }

    [Fact]
    public async Task Validation_WhenExceptionOccurs_ReturnsInternalServerError()
    {
        // Arrange
        var model = new Models.Instancia();
        _mockService.Setup(x => x.Validation(It.IsAny<Models.Instancia>(), "test-tenantKey")).ThrowsAsync(new Exception("Validation error"));
        // Act
        var actionResult = await _controller.Validation(model, "test-tenantKey");
        // Assert
        var objectResult = actionResult.Result as ObjectResult;
        objectResult.Should().NotBeNull();
        objectResult?.StatusCode.Should().Be(500);
        var resultApi = objectResult?.Value as ResultApi<Resp.InstanciaResponse>;
        resultApi?.Success.Should().BeFalse();
    }
}