// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useTipoModeloDocumentoForm, useTipoModeloDocumentoList, useValidationsTipoModeloDocumento } from '../GerAdv_TS/TipoModeloDocumento/Hooks/hookTipoModeloDocumento';
import { ITipoModeloDocumento } from '../GerAdv_TS/TipoModeloDocumento/Interfaces/interface.TipoModeloDocumento';
import { ITipoModeloDocumentoService } from '../GerAdv_TS/TipoModeloDocumento/Services/TipoModeloDocumento.service';
import { TipoModeloDocumentoTestEmpty } from '../GerAdv_TS/Models/TipoModeloDocumento';
import { useTipoModeloDocumentoComboBox } from '../GerAdv_TS/TipoModeloDocumento/Hooks/hookTipoModeloDocumento';

// Mock do serviço
const mockTipoModeloDocumentoService: jest.Mocked<ITipoModeloDocumentoService> = {
  fetchTipoModeloDocumentoById: jest.fn(),
  saveTipoModeloDocumento: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteTipoModeloDocumento: jest.fn(),
  validateTipoModeloDocumento: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialTipoModeloDocumento: ITipoModeloDocumento = { ...TipoModeloDocumentoTestEmpty() };

describe('useTipoModeloDocumentoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    expect(result.current.data).toEqual(initialTipoModeloDocumento);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Tipo Modelo Documento',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Tipo Modelo Documento');
  });

   test('deve carregar Tipo Modelo Documento por ID', async () => {
    const mockTipoModeloDocumento = { ...initialTipoModeloDocumento, id: 1, nome: 'Tipo Modelo Documento Teste' };
    mockTipoModeloDocumentoService.fetchTipoModeloDocumentoById.mockResolvedValue(mockTipoModeloDocumento);

    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.loadTipoModeloDocumento(1);
    });

    expect(mockTipoModeloDocumentoService.fetchTipoModeloDocumentoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockTipoModeloDocumento);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Tipo Modelo Documento', async () => {
    const errorMessage = 'Erro ao carregar Tipo Modelo Documento';
    mockTipoModeloDocumentoService.fetchTipoModeloDocumentoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.loadTipoModeloDocumento(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialTipoModeloDocumento, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialTipoModeloDocumento);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoForm(initialTipoModeloDocumento, mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.loadTipoModeloDocumento(0);
    });

    expect(mockTipoModeloDocumentoService.fetchTipoModeloDocumentoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialTipoModeloDocumento);
  });
});

describe('useTipoModeloDocumentoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoList(mockTipoModeloDocumentoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialTipoModeloDocumento, id: 1, nome: 'Tipo Modelo Documento 1' },
      { ...initialTipoModeloDocumento, id: 2, nome: 'Tipo Modelo Documento 2' }
    ];
    mockTipoModeloDocumentoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTipoModeloDocumentoList(mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockTipoModeloDocumentoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockTipoModeloDocumentoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTipoModeloDocumentoList(mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialTipoModeloDocumento, id: 1, nome: 'Tipo Modelo Documento Filtrado' }];
    const filtro = { nome: 'Tipo Modelo Documento' };
    mockTipoModeloDocumentoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTipoModeloDocumentoList(mockTipoModeloDocumentoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockTipoModeloDocumentoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsTipoModeloDocumento', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsTipoModeloDocumento());

    const validData = { ...initialTipoModeloDocumento, nome: 'Tipo Modelo Documento Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsTipoModeloDocumento());

    const invalidData = { ...initialTipoModeloDocumento, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsTipoModeloDocumento());

    const invalidData = { 
      ...initialTipoModeloDocumento, 
      nome: 'a'.repeat(50+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 50 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsTipoModeloDocumento());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialTipoModeloDocumento, id: 1, nome: 'Tipo Modelo Documento Teste' }];
    mockTipoModeloDocumentoService.getAll.mockResolvedValue(mockData);
    mockTipoModeloDocumentoService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useTipoModeloDocumentoList(mockTipoModeloDocumentoService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsTipoModeloDocumento()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Tipo Modelo Documento Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Tipo Modelo Documento Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Tipo Modelo Documento 1' },
      { id: 2, nome: 'Tipo Modelo Documento 2' }
    ];
    mockTipoModeloDocumentoService.getList.mockResolvedValue(mockOptions as ITipoModeloDocumento[]);


    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Modelo Documento 1' },
        { id: 2, nome: 'Tipo Modelo Documento 2' }
      ]);
    });


    expect(mockTipoModeloDocumentoService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Tipo Modelo Documento ABC' },
      { id: 2, nome: 'Tipo Modelo Documento XYZ' }
    ];
    mockTipoModeloDocumentoService.getList.mockResolvedValue(mockOptions as ITipoModeloDocumento[]);   


 const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Modelo Documento ABC' },
        { id: 2, nome: 'Tipo Modelo Documento XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Tipo Modelo Documento ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Tipo Modelo Documento ABC' },
      { id: 2, nome: 'Tipo Modelo Documento XYZ' }
    ];
    mockTipoModeloDocumentoService.getList.mockResolvedValue(mockOptions as ITipoModeloDocumento[]);
  


    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Modelo Documento ABC' },
        { id: 2, nome: 'Tipo Modelo Documento XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Tipo Modelo Documento ABC' },
          {id: 2, nome: 'Tipo Modelo Documento XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );

    const newValue = { id: 1, nome: 'Tipo Modelo Documento Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Tipo Modelo Documento Inicial' };
    
    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useTipoModeloDocumentoComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Tipo Modelo Documento Inicial' };
    
    const { result } = renderHook(() => 
      useTipoModeloDocumentoComboBox(mockTipoModeloDocumentoService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






