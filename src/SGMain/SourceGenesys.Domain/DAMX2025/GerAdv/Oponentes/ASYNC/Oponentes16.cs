// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.SG.GerAdv;
public partial class DBOponentes
{
    /// <summary>
    /// Factory method async para carregar [tableMin]
    /// </summary>
    public static async Task<DBOponentes> CreateAsync(int nCodigo, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        var registro = new DBOponentes();
        await registro.CarregarAsync(nCodigo, oCnn, cancellationToken);
        return registro;
    }

    /// <summary>
    /// Carregar dados async
    /// </summary>
    protected async Task CarregarAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (id.IsEmptyIDNumber() || oCnn is null)
            return;
        using var cmd = new SqlCommand($"{ConfiguracoesDBT.SQLNoCount}SELECT TOP (1) {CamposSqlX} FROM {PTabelaNome.dbo(oCnn)} WHERE [{CampoCodigo}] = @ThisIDToLoad", oCnn.InnerConnection);
        cmd.Parameters.AddWithValue("@ThisIDToLoad", id);
        using var ds = await ConfiguracoesDBT.GetDataTableAsync(cmd, CommandBehavior.SingleRow, oCnn, cancellationToken);
        if (ds is null)
        {
            throw new InvalidOperationException($"Erro ao carregar registro {this.TabelaNome}.");
        }

        if (ds?.Rows.Count > 0)
        {
            CarregarDadosBd(ds.Rows[0]);
        }
    }

#if (!NOTSTORED_Oponentes)
    /// <summary>
    /// Update async com timeout e cancellation
    /// </summary>
    public virtual async Task<int> UpdateAsync(MsiSqlConnection? oCnn, int insertId = 0, CancellationToken cancellationToken = default, int timeoutSeconds = 30)
    {
        var isInsert = insertId == 0 && ID == 0;
        if (!isInsert && !HasAnyFieldChanged())
            return 0;
        if (oCnn is null)
        {
#if DEBUG
            PTabelaNome.PopupBox("oCnn is null - UpdateAsync()");
#endif
            return 0;
        }

        try
        {
            var updateTool = new DBToolWTable32Async(PTabelaNome, CampoCodigo, ID == 0)
            {
                IsMachineCode = true,
                Identity = this.IdIsIdentity()
            };
            updateTool.Where = isInsert ? string.Empty : $"{CampoCodigo}={ID}";
            ConfigureUpdateFields(updateTool);
            ConfigureAuditorFields(updateTool);
            if (insertId != 0)
            {
                return await GravaNewIdAsync(updateTool, insertId, oCnn, cancellationToken);
            }

            var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken);
            if (updateTool.Insert && result == "OK")
            {
                ID = updateTool.GetCodigo();
                if (ID == 0)
                {
                    Error = -2;
                    ErrorDescription = "O registro não pode ser incluído, tente mais tarde.";
                    return Error;
                }
            }

            return result == "OK" ? 0 : -1;
        }
        catch (OperationCanceledException)
        {
            throw;
        }
        catch (Exception ex)
        {
            Error = -1;
            ErrorDescription = ex.Message;
            throw new InvalidOperationException($"Factory error");
        }
    }

#endif
    /// <summary>
    /// Verificar se existe async
    /// </summary>
    public static async Task<bool> ExisteAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var existe = await ConfiguracoesDBT.GetScalarAsync<int?>($"SELECT 1 FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = {id}", oCnn, cancellationToken);
        return existe.HasValue;
    }

    /// <summary>
    /// Delete async
    /// </summary>
    /// <param name = "operadorId">For future Audity</param>
    /// <param name = "id"></param>
    /// <param name = "oCnn"></param>
    /// <param name = "cancellationToken"></param>
    /// <returns></returns>
    /// <exception cref = "InvalidOperationException"></exception>
    public static async Task<bool> DeleteRecordAsync(int operadorId, int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var sql = $"DELETE FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = @Id";
        try
        {
            using var cmd = new SqlCommand(sql, oCnn.InnerConnection);
            cmd.Parameters.AddWithValue("@Id", id);
            var rowsAffected = await cmd.ExecuteNonQueryAsync(cancellationToken);
            return rowsAffected > 0;
        }
        catch (SqlException ex)when (ex.Number == 547)
        {
            throw new InvalidOperationException("Este registro não pode ser excluído, pois está vinculado a outros registros.");
        }
    }

#if (!NOTSTORED_Oponentes)
    // Helper methods
    private bool HasAnyFieldChanged() => pFldFEMPFuncao || pFldFGUID || pFldFCTPSNumero || pFldFSite || pFldFCTPSSerie || pFldFNome || pFldFAdv || pFldFEMPCliente || pFldFIDRep || pFldFPIS || pFldFContato || pFldFCNPJ || pFldFRG || pFldFJuridica || pFldFTipo || pFldFSexo || pFldFCPF || pFldFEndereco || pFldFFone || pFldFFax || pFldFCidade || pFldFBairro || pFldFCEP || pFldFInscEst || pFldFObservacao || pFldFEMail || pFldFClass || pFldFTop;
    private void ConfigureUpdateFields(DBToolWTable32Async updateTool)
    {
        if (pFldFEMPFuncao)
            updateTool.Fields(DBOponentesDicInfo.EMPFuncao, m_FEMPFuncao, ETiposCampos.FNumber);
        if (pFldFGUID)
            updateTool.Fields(DBOponentesDicInfo.GUID, m_FGUID, ETiposCampos.FString);
        if (pFldFCTPSNumero)
            updateTool.Fields(DBOponentesDicInfo.CTPSNumero, m_FCTPSNumero, ETiposCampos.FString);
        if (pFldFSite)
            updateTool.Fields(DBOponentesDicInfo.Site, m_FSite, ETiposCampos.FString);
        if (pFldFCTPSSerie)
            updateTool.Fields(DBOponentesDicInfo.CTPSSerie, m_FCTPSSerie, ETiposCampos.FString);
        if (pFldFNome)
            updateTool.Fields(DBOponentesDicInfo.Nome, m_FNome, ETiposCampos.FString);
        if (pFldFAdv)
            updateTool.Fields(DBOponentesDicInfo.Adv, m_FAdv, ETiposCampos.FNumber);
        if (pFldFEMPCliente)
            updateTool.Fields(DBOponentesDicInfo.EMPCliente, m_FEMPCliente, ETiposCampos.FNumber);
        if (pFldFIDRep)
            updateTool.Fields(DBOponentesDicInfo.IDRep, m_FIDRep, ETiposCampos.FNumber);
        if (pFldFPIS)
            updateTool.Fields(DBOponentesDicInfo.PIS, m_FPIS, ETiposCampos.FString);
        if (pFldFContato)
            updateTool.Fields(DBOponentesDicInfo.Contato, m_FContato, ETiposCampos.FString);
        if (pFldFCNPJ)
            updateTool.Fields(DBOponentesDicInfo.CNPJ, m_FCNPJ, ETiposCampos.FString);
        if (pFldFRG)
            updateTool.Fields(DBOponentesDicInfo.RG, m_FRG, ETiposCampos.FString);
        if (pFldFJuridica || updateTool.Insert)
            updateTool.Fields(DBOponentesDicInfo.Juridica, m_FJuridica, ETiposCampos.FBoolean);
        if (pFldFTipo || updateTool.Insert)
            updateTool.Fields(DBOponentesDicInfo.Tipo, m_FTipo, ETiposCampos.FBoolean);
        if (pFldFSexo || updateTool.Insert)
            updateTool.Fields(DBOponentesDicInfo.Sexo, m_FSexo, ETiposCampos.FBoolean);
        if (pFldFCPF)
            updateTool.Fields(DBOponentesDicInfo.CPF, m_FCPF, ETiposCampos.FString);
        if (pFldFEndereco)
            updateTool.Fields(DBOponentesDicInfo.Endereco, m_FEndereco, ETiposCampos.FString);
        if (pFldFFone)
            updateTool.Fields(DBOponentesDicInfo.Fone, m_FFone, ETiposCampos.FString);
        if (pFldFFax)
            updateTool.Fields(DBOponentesDicInfo.Fax, m_FFax, ETiposCampos.FString);
        if (pFldFCidade)
            updateTool.Fields(DBOponentesDicInfo.Cidade, m_FCidade, ETiposCampos.FNumber);
        if (pFldFBairro)
            updateTool.Fields(DBOponentesDicInfo.Bairro, m_FBairro, ETiposCampos.FString);
        if (pFldFCEP)
            updateTool.Fields(DBOponentesDicInfo.CEP, m_FCEP, ETiposCampos.FString);
        if (pFldFInscEst)
            updateTool.Fields(DBOponentesDicInfo.InscEst, m_FInscEst, ETiposCampos.FString);
        if (pFldFObservacao)
            updateTool.Fields(DBOponentesDicInfo.Observacao, m_FObservacao, ETiposCampos.FString);
        if (pFldFEMail)
            updateTool.Fields(DBOponentesDicInfo.EMail, m_FEMail, ETiposCampos.FString);
        if (pFldFClass)
            updateTool.Fields(DBOponentesDicInfo.Class, m_FClass, ETiposCampos.FString);
        if (pFldFTop || updateTool.Insert)
            updateTool.Fields(DBOponentesDicInfo.Top, m_FTop, ETiposCampos.FBoolean);
    }

#endif
#if (!NOTSTORED_Oponentes)
    private void ConfigureAuditorFields(DBToolWTable32Async updateTool)
    {
        // Tracking: CodeAuditor(202508121515)
        var isInsert = ID.IsEmptyIDNumber();
        if (m_AuditorQuem == 0)
            AuditorQuem = 1;
        if (isInsert)
            updateTool.Fields(DBOponentesDicInfo.QuemCad, AuditorQuem, ETiposCampos.FNumber);
        if (isInsert)
            updateTool.Fields(DBOponentesDicInfo.DtCad, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        if (!isInsert)
            updateTool.Fields(DBOponentesDicInfo.QuemAtu, AuditorQuem, ETiposCampos.FNumber);
        if (!isInsert)
            updateTool.Fields(DBOponentesDicInfo.DtAtu, DevourerOne.DateTimeUtc, ETiposCampos.FDate);
        updateTool.Fields(DBOponentesDicInfo.Visto, false, ETiposCampos.FBoolean);
        if (string.IsNullOrWhiteSpace(m_FGUID))
        {
            this.FGUID = Guid.NewGuid().ToString();
        }
    }

    private async Task<int> GravaNewIdAsync(DBToolWTable32Async updateTool, int insertId, MsiSqlConnection oCnn, CancellationToken cancellationToken)
    {
        ID = insertId;
        updateTool.Fields(CampoCodigo, insertId, ETiposCampos.FNumber);
        var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken, true);
        return result == "OK" ? 0 : -3;
    }
#endif
}