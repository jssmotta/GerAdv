// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoContatoCRMValidationTests : IDisposable
{
    private readonly TipoContatoCRMValidation _validation;
    private readonly Mock<ITipoContatoCRMService> _mockTipoContatoCRMService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoContatoCRMService _service;
    private readonly string _validUri = "test-uri";
    public TipoContatoCRMValidationTests()
    {
        _validation = new TipoContatoCRMValidation();
        _mockTipoContatoCRMService = new Mock<ITipoContatoCRMService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoContatoCRMValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoContatoCRMValidation>();
    }

    [Fact]
    public void TipoContatoCRMValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoContatoCRMValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoContatoCRMValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoContatoCRMValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.TipoContatoCRM CreateValidTipoContatoCRM()
    {
        return new Models.TipoContatoCRM
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoContatoCRMService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoContatoCRM>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoContatoCRMs service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoContatoCRMService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoContatoCRM>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoContatoCRMs service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipocontatocrm = new Models.TipoContatoCRM
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipocontatocrm, _mockTipoContatoCRMService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoContatoCRMService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}