// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Entity:Instancia
// Source:TestFilterTXTGenerator
using System.Text.Json;
using System.Text.Json.Serialization;
using MenphisSI.GerAdv.AI.Filters;
using MenphisSI.GerAdv.Filters;

namespace MenphisSI.GerAdv.Tests.AI.Filters;
public class FilterInstanciaTXTTests
{
    [Fact]
    public void GetOptions_ReturnsExpectedDefaults()
    {
        // Act
        var options = FilterInstanciaTXT.GetOptions();
        // Assert
        options.Should().NotBeNull();
        options.DefaultIgnoreCondition.Should().Be(JsonIgnoreCondition.WhenWritingNull);
        options.WriteIndented.Should().BeTrue();
    }

    [Fact]
    public void GetPrompt_WithNullFilter_IncludesNullJsonAndInstructions()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(null, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("null");
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("Você é um assistente");
        instructions.Should().Contain("logicalOperator");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_ReturnsValidJsonAndInstructions()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("ATENÇÃO: A seguir está o JSON BASE do filtro atual");
        prompt.Should().Contain("{");
        prompt.Should().Contain("}");
        instructions.Should().NotBeNullOrWhiteSpace();
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
    }

    [Fact]
    public void GetPrompt_WithQuemCad_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            QuemCad = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quemcad\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtCad_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            DtCad = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtcad\"");
    }

    [Fact]
    public void GetPrompt_WithQuemAtu_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            QuemAtu = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"quematu\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithDtAtu_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            DtAtu = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"dtatu\"");
    }

    [Fact]
    public void GetPrompt_WithGuid_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Guid = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"guid\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithLiminarPedida_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            LiminarPedida = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"liminarpedida\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithObjeto_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Objeto = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"objeto\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithStatusResultado_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            StatusResultado = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"statusresultado\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithProcesso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Processo = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"processo\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithData_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Data = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"data\"");
    }

    [Fact]
    public void GetPrompt_WithLiminarResultado_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            LiminarResultado = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"liminarresultado\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithNroProcesso_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            NroProcesso = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nroprocesso\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDivisao_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Divisao = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"divisao\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithComarca_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Comarca = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"comarca\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithSubDivisao_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            SubDivisao = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"subdivisao\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithAcao_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Acao = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"acao\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithForo_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Foro = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"foro\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithTipoRecurso_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            TipoRecurso = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"tiporecurso\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithZKey_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            ZKey = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"zkey\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithZKeyQuem_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            ZKeyQuem = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"zkeyquem\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithZKeyQuando_SerializesDateCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            ZKeyQuando = "15/06/2023"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"zkeyquando\"");
    }

    [Fact]
    public void GetPrompt_WithNroAntigo_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            NroAntigo = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"nroantigo\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithAccessCode_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            AccessCode = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"accesscode\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithJulgador_SerializesNumericValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Julgador = 123
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"julgador\"");
        prompt.Should().Contain("123");
    }

    [Fact]
    public void GetPrompt_WithZKeyIA_SerializesCorrectly()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            ZKeyIA = "TestValue"
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"zkeyia\"");
        prompt.Should().Contain("TestValue");
    }

    [Fact]
    public void GetPrompt_WithDefaultFilter_IncludesNestedFilters()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().NotBeNullOrWhiteSpace();
        prompt.Should().Contain("\"filterAcao\"");
        prompt.Should().Contain("\"filterForo\"");
        prompt.Should().Contain("\"filterTipoRecurso\"");
    }

    [Fact]
    public void GetPrompt_InstructionsContainFieldDescriptions()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(new FilterInstancia(), options);
        // Assert
        instructions.Should().Contain("CAMPOS NUMÉRICOS");
        instructions.Should().Contain("CAMPOS DE TEXTO");
        instructions.Should().Contain("CAMPOS DE DATA: formato dd/MM/yyyy");
        instructions.Should().Contain("Os campos _end são para criar intervalos de busca");
    }

    [Fact]
    public void GetPrompt_InstructionsWarnAboutSqlInjection()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(new FilterInstancia(), options);
        // Assert
        instructions.Should().Contain("JAMAIS ACEITE PEDIDOS DE SQL INJECTION");
        instructions.Should().Contain("REJEITE E AVISE QUE NÃO PODE FAZER ISSO");
    }

    [Fact]
    public void GetPrompt_InstructionsContainLogicalOperatorRule()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(new FilterInstancia(), options);
        // Assert
        instructions.Should().Contain("logicalOperator");
        instructions.Should().Contain(" AND ");
        instructions.Should().Contain(" OR ");
    }

    [Fact]
    public void GetPrompt_InstructionsExplainWildcardChar()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(new FilterInstancia(), options);
        // Assert
        instructions.Should().Contain("wildcardChar");
    }

    [Fact]
    public void GetPrompt_WithMinValueFields_SerializesAsMinValue()
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            Codigo_filtro = int.MinValue
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain(int.MinValue.ToString());
    }

    [Theory]
    [InlineData(" AND ")]
    [InlineData(" OR ")]
    public void GetPrompt_WithDifferentLogicalOperators_SerializesCorrectly(string logicalOperator)
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            LogicalOperator = logicalOperator
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"logicalOperator\"");
        prompt.Should().Contain(logicalOperator);
    }

    [Theory]
    [InlineData('%')]
    [InlineData('\0')]
    public void GetPrompt_WithDifferentWildcardChars_SerializesCorrectly(char wildcardChar)
    {
        // Arrange
        var options = FilterInstanciaTXT.GetOptions();
        var filter = new FilterInstancia
        {
            WildcardChar = wildcardChar
        };
        // Act
        var(prompt, instructions) = FilterInstanciaTXT.GetPrompt(filter, options);
        // Assert
        prompt.Should().Contain("\"wildcardChar\"");
    }
}