// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class AcaoWriterTests
{
    private readonly Mock<IFAcaoFactory> _mockAcaoFactory;
    private readonly Mock<FAcao> _mockFAcao;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly AcaoWriter _acaoWriter;
    public AcaoWriterTests()
    {
        _mockAcaoFactory = new Mock<IFAcaoFactory>();
        _mockFAcao = new Mock<FAcao>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _acaoWriter = new AcaoWriter(_mockAcaoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var acao = CreateValidAcaoModel();
        acao.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAcao.Object);
        _mockFAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _acaoWriter.WriteAsync(acao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAcao.Object);
        _mockAcaoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockAcaoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFAcao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var acao = CreateValidAcaoModel();
        acao.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockAcaoFactory.Setup(x => x.CreateFromIdAsync(acao.Id, _mockConnection.Object)).ReturnsAsync(_mockFAcao.Object);
        _mockFAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _acaoWriter.WriteAsync(acao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAcao.Object);
        _mockAcaoFactory.Verify(x => x.CreateFromIdAsync(acao.Id, _mockConnection.Object), Times.Once);
        _mockAcaoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFAcao.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var acao = CreateValidAcaoModel();
        var auditorQuem = 99;
        _mockAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAcao.Object);
        _mockFAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _acaoWriter.WriteAsync(acao, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAcao.Object);
        _mockFAcao.VerifySet(x => x.FGUID = acao.GUID, Times.Once);
        _mockFAcao.VerifySet(x => x.FJustica = acao.Justica, Times.Once);
        _mockFAcao.VerifySet(x => x.FArea = acao.Area, Times.Once);
        _mockFAcao.VerifySet(x => x.FDescricao = acao.Descricao, Times.Once);
        _mockFAcao.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var acao = CreateValidAcaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAcaoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _acaoWriter.WriteAsync(acao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var acao = CreateValidAcaoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAcaoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAcao.Object);
        _mockFAcao.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _acaoWriter.WriteAsync(acao, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var acaoResponse = new AcaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockAcaoFactory.Setup(x => x.DeleteAsync(operadorId, acaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _acaoWriter.Delete(acaoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockAcaoFactory.Verify(x => x.DeleteAsync(operadorId, acaoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var acaoResponse = new AcaoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockAcaoFactory.Setup(x => x.DeleteAsync(operadorId, acaoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _acaoWriter.Delete(acaoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var acaoResponse = new AcaoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockAcaoFactory.Setup(x => x.DeleteAsync(operadorId, acaoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _acaoWriter.Delete(acaoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new AcaoWriter(_mockAcaoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new AcaoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Acao CreateValidAcaoModel()
    {
        return new Models.Acao
        {
            Id = 0,
            GUID = Guid.NewGuid().ToString(),
            Justica = 1,
            Area = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }
#endregion
}