// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBGraph - Nome da classe (ex: DBClientes)
/// Graph - Nome da tabela (ex: Clientes)
/// gph - Prefixo dos campos (ex: cli)
/// gphCodigo - Nome do campo código (ex: cliCodigo)
///  - Nome do campo nome (ex: cliNome)
/// DBGraph.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBGraphTests : IDisposable
{
    private readonly DBGraph _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBGraphTests()
    {
        _instance = new DBGraph();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("gphCodigo", typeof(int));
        dt.Columns.Add("gphQuemCad", typeof(int));
        dt.Columns.Add("gphDtCad", typeof(DateTime));
        dt.Columns.Add("gphQuemAtu", typeof(int));
        dt.Columns.Add("gphDtAtu", typeof(DateTime));
        dt.Columns.Add("gphVisto", typeof(bool));
        dt.Columns.Add("gphTabela", typeof(string));
        dt.Columns.Add("gphTabelaId", typeof(int));
        dt.Columns.Add("gphImagem", typeof(string));
        dt.Columns.Add("gphGUID", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("Graph", DBGraph.PTabelaNome);
        Assert.Equal(" Graph.* ", DBGraph.CamposSqlX);
        Assert.Equal("gphCodigo", DBGraph.CampoCodigo);
        Assert.Equal("", DBGraph.CampoNome);
        Assert.Equal("gph", DBGraph.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBGraph();
        Assert.Equal(0, instance.ID);
        Assert.Equal("Graph", instance.ITabelaName());
        Assert.Equal("gph", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBGraph((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBGraph((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["gphCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBGraph(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("Graph", cadastro.ITabelaName());
        Assert.Equal("gphCodigo", cadastro.ICampoCodigo());
        Assert.Equal("", cadastro.ICampoNome());
        Assert.Equal("gph", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBGraph.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBGraph.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBGraph.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBGraph();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBGraph();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnFalse()
    {
        Assert.False(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Tabela_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FTabela = input;
        Assert.Equal(expected, _instance.FTabela);
    }

    [Fact]
    public void Tabela_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 80 + 10);
        _instance.FTabela = longString;
        Assert.True(_instance.FTabela.Length <= 80);
    }

    [Theory]
    [InlineData(0)]
    [InlineData(1)]
    [InlineData(-1)]
    [InlineData(int.MaxValue)]
    [InlineData(int.MinValue)]
    public void TabelaId_ShouldAcceptIntegerValues(int value)
    {
        _instance.FTabelaId = value;
        Assert.Equal(value, _instance.FTabelaId);
    }

    [Fact]
    public void TabelaId_DefaultValue_ShouldBeZero()
    {
        var instance = new DBGraph();
        Assert.Equal(0, instance.FTabelaId);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void GUID_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FGUID = input;
        Assert.Equal(expected, _instance.FGUID);
    }

    [Fact]
    public void GUID_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 150 + 10);
        _instance.FGUID = longString;
        Assert.True(_instance.FGUID.Length <= 150);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}