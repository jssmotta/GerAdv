// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { EnquadramentoEmpresaEmpty } from '../../../Models/EnquadramentoEmpresa';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IEnquadramentoEmpresa } from '../../Interfaces/interface.EnquadramentoEmpresa';
import { EnquadramentoEmpresaService } from '../../Services/EnquadramentoEmpresa.service';
import { EnquadramentoEmpresaApi } from '../../Apis/ApiEnquadramentoEmpresa';
import { EnquadramentoEmpresaGridMobileComponent } from '../GridsMobile/EnquadramentoEmpresaMobile';
import { EnquadramentoEmpresaGridDesktopComponent } from '../GridsDesktop/EnquadramentoEmpresaDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterEnquadramentoEmpresa } from '../../Filters/EnquadramentoEmpresa';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import EnquadramentoEmpresaWindow from './EnquadramentoEmpresaWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useEnquadramentoEmpresaList } from '../../Hooks/hookEnquadramentoEmpresa';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useEnquadramentoEmpresaFilter } from '../../Hooks/hookEnquadramentoEmpresaFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const EnquadramentoEmpresaGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedEnquadramentoEmpresa, setSelectedEnquadramentoEmpresa] = useState<IEnquadramentoEmpresa>(EnquadramentoEmpresaEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterEnquadramentoEmpresa | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [enquadramentoempresaData, setEnquadramentoEmpresaData] = useState<IEnquadramentoEmpresa[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const enquadramentoempresaService = useMemo(() => {
    return new EnquadramentoEmpresaService(
    new EnquadramentoEmpresaApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: enquadramentoempresa, 
  fetchData, 
  refreshData
} = useEnquadramentoEmpresaList(enquadramentoempresaService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterEnquadramentoEmpresa | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('EnquadramentoEmpresaFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await enquadramentoempresaService.getAll(
  filtro ?? {} as FilterEnquadramentoEmpresa, 
  onlineData => {
    setEnquadramentoEmpresaData(onlineData);
    setLoading(false);
  }
);
setEnquadramentoEmpresaData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterEnquadramentoEmpresa;

    const filtroMem = sessionStorage.getItem(btoa('EnquadramentoEmpresaFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('EnquadramentoEmpresaFilter')) ?? '') || '{}') : {} as FilterEnquadramentoEmpresa;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (enquadramentoempresa: IEnquadramentoEmpresa) => {
    setSelectedEnquadramentoEmpresa(enquadramentoempresa);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedEnquadramentoEmpresa(EnquadramentoEmpresaEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const enquadramentoempresa = e.dataItem;
    setDeleteId(enquadramentoempresa.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await enquadramentoempresaService.deleteEnquadramentoEmpresa(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      if (process.env.NEXT_PUBLIC_SHOW_LOG === '1')
        console.log('Erro ao excluir');
        setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
      } finally {
      setDeleteId(null);
      setIsModalOpen(false);
    }
  }
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useEnquadramentoEmpresaFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <EnquadramentoEmpresaGridMobileComponent
        data={enquadramentoempresaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <EnquadramentoEmpresaGridDesktopComponent
        data={enquadramentoempresaData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <EnquadramentoEmpresaWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedEnquadramentoEmpresa={selectedEnquadramentoEmpresa}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Enquadramento Empresa'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default EnquadramentoEmpresaGrid;