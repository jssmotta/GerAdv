// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoEnderecoValidationTests : IDisposable
{
    private readonly TipoEnderecoValidation _validation;
    private readonly Mock<ITipoEnderecoService> _mockTipoEnderecoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoEnderecoService _service;
    private readonly string _validUri = "test-uri";
    public TipoEnderecoValidationTests()
    {
        _validation = new TipoEnderecoValidation();
        _mockTipoEnderecoService = new Mock<ITipoEnderecoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoEnderecoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoEnderecoValidation>();
    }

    [Fact]
    public void TipoEnderecoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoEnderecoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoEnderecoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoEnderecoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private static Models.TipoEndereco CreateValidTipoEndereco()
    {
        return new Models.TipoEndereco
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoEnderecoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoEndereco>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoEnderecos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoEnderecoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoEndereco>(), It.IsAny<string>())).ReturnsAsync([]);
    // Setup other mocks but don't override the TipoEnderecos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipoendereco = new Models.TipoEndereco
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipoendereco, _mockTipoEnderecoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoEnderecoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}