// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import RamalWindowId from '@/app/GerAdv_TS/Ramal/Crud/Grids/RamalWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import RamalWindowId from '@/app/GerAdv_TS/Ramal/Crud/Grids/RamalWindowId';
import { RamalEmpty } from '@/app/GerAdv_TS/Models/Ramal';

describe('RamalWindowId (basic)', () => {
  it('exports the component', () => {
    expect(RamalWindowId).toBeDefined();
    expect(typeof RamalWindowId === 'function' || typeof RamalWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the RamalService class so constructor usage doesn't throw and we can control fetchRamalById
const mockFetchRamalById = jest.fn();
jest.mock('@/app/GerAdv_TS/Ramal/Services/Ramal.service', () => ({
  RamalService: jest.fn().mockImplementation(() => ({
    fetchRamalById: (...args: any[]) => mockFetchRamalById(...args),
  })),
}));

// Mock the child RamalWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Ramal/Crud/Grids/RamalWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='ramal-window' {...p} />,
}));

describe('RamalWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(RamalWindowId).toBeDefined();
    expect(typeof RamalWindowId === 'function' || typeof RamalWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <RamalWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders RamalWindow with RamalEmpty when id is 0', async () => {
    render(
      <RamalWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // RamalWindow should be rendered with selectedRamal equal to RamalEmpty()
    const node = await screen.findByTestId('ramal-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedRamal"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedramal') || node.getAttribute('selectedRamal')).not.toBeNull();
  });

  it('fetches ramal by id and passes it to RamalWindow when id > 0', async () => {
    const fakeRamal = { id: 123, nomecliente: 'Test' };
    mockFetchRamalById.mockResolvedValue(fakeRamal);

    render(
      <RamalWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('ramal-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchRamalById).toHaveBeenCalledWith(123));
  });
});
