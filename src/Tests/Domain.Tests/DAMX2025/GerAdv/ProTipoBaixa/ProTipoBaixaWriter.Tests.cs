// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class ProTipoBaixaWriterTests
{
    private readonly Mock<IFProTipoBaixaFactory> _mockProTipoBaixaFactory;
    private readonly Mock<FProTipoBaixa> _mockFProTipoBaixa;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly ProTipoBaixaWriter _protipobaixaWriter;
    public ProTipoBaixaWriterTests()
    {
        _mockProTipoBaixaFactory = new Mock<IFProTipoBaixaFactory>();
        _mockFProTipoBaixa = new Mock<FProTipoBaixa>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _protipobaixaWriter = new ProTipoBaixaWriter(_mockProTipoBaixaFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var protipobaixa = CreateValidProTipoBaixaModel();
        protipobaixa.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockProTipoBaixaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFProTipoBaixa.Object);
        _mockFProTipoBaixa.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _protipobaixaWriter.WriteAsync(protipobaixa, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFProTipoBaixa.Object);
        _mockProTipoBaixaFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockProTipoBaixaFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFProTipoBaixa.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var protipobaixa = CreateValidProTipoBaixaModel();
        protipobaixa.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockProTipoBaixaFactory.Setup(x => x.CreateFromIdAsync(protipobaixa.Id, _mockConnection.Object)).ReturnsAsync(_mockFProTipoBaixa.Object);
        _mockFProTipoBaixa.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _protipobaixaWriter.WriteAsync(protipobaixa, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFProTipoBaixa.Object);
        _mockProTipoBaixaFactory.Verify(x => x.CreateFromIdAsync(protipobaixa.Id, _mockConnection.Object), Times.Once);
        _mockProTipoBaixaFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFProTipoBaixa.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var protipobaixa = CreateValidProTipoBaixaModel();
        var auditorQuem = 99;
        _mockProTipoBaixaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFProTipoBaixa.Object);
        _mockFProTipoBaixa.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _protipobaixaWriter.WriteAsync(protipobaixa, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFProTipoBaixa.Object);
        _mockFProTipoBaixa.VerifySet(x => x.FNome = protipobaixa.Nome, Times.Once);
        _mockFProTipoBaixa.VerifySet(x => x.FGUID = protipobaixa.GUID, Times.Once);
        _mockFProTipoBaixa.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var protipobaixa = CreateValidProTipoBaixaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockProTipoBaixaFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _protipobaixaWriter.WriteAsync(protipobaixa, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var protipobaixa = CreateValidProTipoBaixaModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockProTipoBaixaFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFProTipoBaixa.Object);
        _mockFProTipoBaixa.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _protipobaixaWriter.WriteAsync(protipobaixa, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var protipobaixaResponse = new ProTipoBaixaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockProTipoBaixaFactory.Setup(x => x.DeleteAsync(operadorId, protipobaixaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _protipobaixaWriter.Delete(protipobaixaResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockProTipoBaixaFactory.Verify(x => x.DeleteAsync(operadorId, protipobaixaResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var protipobaixaResponse = new ProTipoBaixaResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockProTipoBaixaFactory.Setup(x => x.DeleteAsync(operadorId, protipobaixaResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _protipobaixaWriter.Delete(protipobaixaResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var protipobaixaResponse = new ProTipoBaixaResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockProTipoBaixaFactory.Setup(x => x.DeleteAsync(operadorId, protipobaixaResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _protipobaixaWriter.Delete(protipobaixaResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new ProTipoBaixaWriter(_mockProTipoBaixaFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new ProTipoBaixaWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.ProTipoBaixa CreateValidProTipoBaixaModel()
    {
        return new Models.ProTipoBaixa
        {
            Id = 0,
            Nome = "João",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}