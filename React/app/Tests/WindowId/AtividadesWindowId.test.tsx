// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AtividadesWindowId from '@/app/GerAdv_TS/Atividades/Crud/Grids/AtividadesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AtividadesWindowId from '@/app/GerAdv_TS/Atividades/Crud/Grids/AtividadesWindowId';
import { AtividadesEmpty } from '@/app/GerAdv_TS/Models/Atividades';

describe('AtividadesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AtividadesWindowId).toBeDefined();
    expect(typeof AtividadesWindowId === 'function' || typeof AtividadesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AtividadesService class so constructor usage doesn't throw and we can control fetchAtividadesById
const mockFetchAtividadesById = jest.fn();
jest.mock('@/app/GerAdv_TS/Atividades/Services/Atividades.service', () => ({
  AtividadesService: jest.fn().mockImplementation(() => ({
    fetchAtividadesById: (...args: any[]) => mockFetchAtividadesById(...args),
  })),
}));

// Mock the child AtividadesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Atividades/Crud/Grids/AtividadesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='atividades-window' {...p} />,
}));

describe('AtividadesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AtividadesWindowId).toBeDefined();
    expect(typeof AtividadesWindowId === 'function' || typeof AtividadesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AtividadesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AtividadesWindow with AtividadesEmpty when id is 0', async () => {
    render(
      <AtividadesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AtividadesWindow should be rendered with selectedAtividades equal to AtividadesEmpty()
    const node = await screen.findByTestId('atividades-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedAtividades"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedatividades') || node.getAttribute('selectedAtividades')).not.toBeNull();
  });

  it('fetches atividades by id and passes it to AtividadesWindow when id > 0', async () => {
    const fakeAtividades = { id: 123, nomecliente: 'Test' };
    mockFetchAtividadesById.mockResolvedValue(fakeAtividades);

    render(
      <AtividadesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('atividades-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAtividadesById).toHaveBeenCalledWith(123));
  });
});
