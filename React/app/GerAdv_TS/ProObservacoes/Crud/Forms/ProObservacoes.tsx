// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IProObservacoes } from "@/app/GerAdv_TS/ProObservacoes/Interfaces/interface.ProObservacoes";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useSystemContext } from "@/app/context/SystemContext";
import { getParamFromUrl } from "@/app/tools/helpers";
import FooterIncDocker from "@/app/components/Cruds/FooterIncDocker";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms5.css"; // [ INDEX_SIZE ]

import ButtonSalvarCrud from "@/app/components/Cruds/ButtonSalvarCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import DeleteButton from "@/app/components/Cruds/DeleteButton";
import AuditorButton from "@/app/components/Cruds/AuditorButton";
import PrintButton from "@/app/components/Cruds/PrintButton";
import CopyButton from "@/app/components/Cruds/CopyButton";
import { ProObservacoesApi } from "../../Apis/ApiProObservacoes";
import { useValidationsProObservacoes } from "../../Hooks/hookProObservacoes";

import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";

interface ProObservacoesFormProps {
  proobservacoesData: IProObservacoes;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const ProObservacoesForm: React.FC<ProObservacoesFormProps> = ({
  proobservacoesData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dadoApi = new ProObservacoesApi(
    systemContext?.Uri ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsProObservacoes();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de ProObservacoes:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload(); // Recarrega os dados originais
    } else {
      onClose(); // Comportamento padrão se não há callback de recarga
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `ProObservacoesForm-${proobservacoesData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar ProObservacoes diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        proobservacoesData?.id == 0
          ? "Editar ProObservacoes"
          : "Adicionar Pro Observacoes";
    }
  }, [proobservacoesData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-ProObservacoes"
            : "form-container5 form-container-ProObservacoes"
        }
      >
        <form
          className="formInputCadInc"
          id={`ProObservacoesForm-${proobservacoesData.id}`}
          onSubmit={onConfirm}
        >
          {!isMobile && (
            <ButtonSalvarCrud
              isMobile={false}
              validationForm={validationForm}
              entity="ProObservacoes"
              data={proobservacoesData}
              isSubmitting={isSubmitting}
              setIsSubmitting={setIsSubmitting}
              onClose={onClose}
              formId={`ProObservacoesForm-${proobservacoesData.id}`}
              preventPropagation={true}
              onSave={handleDirectSave}
              onCancel={handleCancel}
            />
          )}
          <div className="grid-container">
            <InputName
              type="text"
              id="nome"
              label="Nome"
              dataForm={proobservacoesData}
              className="input-default-main"
              name="nome"
              value={proobservacoesData.nome}
              placeholder={`Informe Nome`}
              onChange={onChange}
              required
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="processo"
              label="Processo"
              dataForm={proobservacoesData}
              className="input-default-main"
              name="processo"
              value={proobservacoesData.processo}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2147483647}
              id="observacoes"
              label="Observacoes"
              dataForm={proobservacoesData}
              className="input-default-main"
              name="observacoes"
              value={proobservacoesData.observacoes}
              onChange={onChange}
            />

            <InputInput
              type="text"
              maxLength={2048}
              id="data"
              label="Data"
              dataForm={proobservacoesData}
              className="input-default-main"
              name="data"
              value={proobservacoesData.data}
              onChange={onChange}
            />
          </div>
        </form>

        {isMobile && (
          <ButtonSalvarCrud
            isMobile={true}
            validationForm={validationForm}
            entity="ProObservacoes"
            data={proobservacoesData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`ProObservacoesForm-${proobservacoesData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />
        )}

        <FooterIncDocker>
          <CopyButton formId={`ProObservacoesForm-${proobservacoesData.id}`} />

          <PrintButton formId={`ProObservacoesForm-${proobservacoesData.id}`} />

          <AuditorButton
            tableName="ProObservacoes"
            coolName="Pro Observacoes"
            id={proobservacoesData.id}
          />

          <DeleteButton
            page={"/pages/proobservacoes"}
            id={proobservacoesData.id}
            dadoApi={dadoApi}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>
    </>
  );
};
