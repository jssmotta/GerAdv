// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IProDespesas } from "@/app/GerAdv_TS/ProDespesas/Interfaces/interface.ProDespesas";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";
import "@/app/styles/CrudFormsBase.css";
import "@/app/styles/CrudFormsMobile.css";
import "@/app/styles/CrudForms.css"; // [ INDEX_SIZE ]

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { ProDespesasApi } from "../../Apis/ProDespesasApi";
import { useValidationsProDespesas } from "../../Hooks/hookProDespesas";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpProDespesas from "../../Help/HelpProDespesas";
import ProDespesasPage from "@/app/pages/prodespesas/page";

import ClientesComboBox from "@/app/GerAdv_TS/Clientes/ComboBox/ClientesComboBox";
import { ClientesApi } from "@/app/GerAdv_TS/Clientes/Apis/ClientesApi";
import InputName from "@/app/components/Inputs/InputName";
import InputInput from "@/app/components/Inputs/InputInput";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface ProDespesasFormProps {
  prodespesasData: IProDespesas;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const ProDespesasForm: React.FC<ProDespesasFormProps> = ({
  prodespesasData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new ProDespesasApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsProDespesas();

  const [_nomeClientes, setNomeClientes] = useState("");
  const clientesApi = new ClientesApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );

  if (getParamFromUrl("clientes") > 0) {
    if (prodespesasData.id === 0 && prodespesasData.cliente == 0) {
      clientesApi
        .getById(getParamFromUrl("clientes"))
        .then((response) => {
          setNomeClientes(response.data.nome);
        })
        .catch((error) => {
          if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
            console.log("Error unexpected");
        });

      prodespesasData.cliente = getParamFromUrl("clientes");
    }
  }
  const addValorCliente = (e: any) => {
    if (e?.id > 0) onChange({ target: { name: "cliente", value: e.id } });
  };

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de ProDespesas:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(
            `ProDespesasForm-${prodespesasData.id}`,
          ),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar ProDespesas diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent =
        prodespesasData?.id == 0
          ? "Editar ProDespesas"
          : "Adicionar Pro Despesas";
    }
  }, [prodespesasData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-ProDespesas"
            : "form-container form-container-ProDespesas"
        }
      >
        <form
          className="formInputCadInc"
          id={`ProDespesasForm-${prodespesasData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="ProDespesas"
            data={prodespesasData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`ProDespesasForm-${prodespesasData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputName
              tabIndex={0}
              type="text"
              id="data"
              label="Data"
              dataForm={prodespesasData}
              className="input-default-main"
              name="data"
              value={prodespesasData.data}
              placeholder={`Informe Data`}
              onChange={onChange}
              required
            />

            <InputInput
              tabIndex={1}
              type="text"
              maxLength={2048}
              id="ligacaoid"
              label="LigacaoID"
              dataForm={prodespesasData}
              className="input-default-main"
              name="ligacaoid"
              value={prodespesasData.ligacaoid}
              onChange={onChange}
            />

            <ClientesComboBox
              name={"cliente"}
              dataForm={prodespesasData}
              value={prodespesasData.cliente}
              setValue={addValorCliente}
              label={"Clientes"}
            />

            <InputCheckbox
              dataForm={prodespesasData}
              label="Corrigido"
              name="corrigido"
              checked={prodespesasData.corrigido}
              onChange={onChange}
            />

            <InputInput
              tabIndex={2}
              type="text"
              maxLength={2048}
              id="valororiginal"
              label="ValorOriginal"
              dataForm={prodespesasData}
              className="input-default-main"
              name="valororiginal"
              value={prodespesasData.valororiginal}
              onChange={onChange}
            />

            <InputInput
              tabIndex={3}
              type="text"
              maxLength={2048}
              id="processo"
              label="Processo"
              dataForm={prodespesasData}
              className="input-default-main"
              name="processo"
              value={prodespesasData.processo}
              onChange={onChange}
            />

            <InputInput
              tabIndex={4}
              type="text"
              maxLength={2048}
              id="quitado"
              label="Quitado"
              dataForm={prodespesasData}
              className="input-default-main"
              name="quitado"
              value={prodespesasData.quitado}
              onChange={onChange}
            />

            <InputInput
              tabIndex={5}
              type="text"
              maxLength={2048}
              id="datacorrecao"
              label="DataCorrecao"
              dataForm={prodespesasData}
              className="input-default-main"
              name="datacorrecao"
              value={prodespesasData.datacorrecao}
              onChange={onChange}
            />

            <InputInput
              tabIndex={6}
              type="text"
              maxLength={2048}
              id="valor"
              label="Valor"
              dataForm={prodespesasData}
              className="input-default-main"
              name="valor"
              value={prodespesasData.valor}
              onChange={onChange}
            />
          </div>
          <div className="grid-container">
            <InputCheckbox
              dataForm={prodespesasData}
              label="Tipo"
              name="tipo"
              checked={prodespesasData.tipo}
              onChange={onChange}
            />

            <InputInput
              tabIndex={7}
              type="text"
              maxLength={100}
              id="historico"
              label="Historico"
              dataForm={prodespesasData}
              className="input-default-main"
              name="historico"
              value={prodespesasData.historico}
              onChange={onChange}
            />

            <InputCheckbox
              dataForm={prodespesasData}
              label="LivroCaixa"
              name="livrocaixa"
              checked={prodespesasData.livrocaixa}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Pro Despesas" windowCrud={ProDespesasPage} />

          <CopyButton formId={`ProDespesasForm-${prodespesasData.id}`} />

          <PrintButton formId={`ProDespesasForm-${prodespesasData.id}`} />

          <AuditorButton
            tableName="ProDespesas"
            coolName="Pro Despesas"
            id={prodespesasData.id}
          />

          <DeleteButton
            page={"/pages/prodespesas"}
            id={prodespesasData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Pro Despesas"
            page={(onClose) => <HelpProDespesas onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="ProDespesas"
          data={prodespesasData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`ProDespesasForm-${prodespesasData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
