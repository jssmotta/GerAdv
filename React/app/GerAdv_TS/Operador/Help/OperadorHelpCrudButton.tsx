// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:HelpCrudGenerator

"use client";
import React, { useState } from "react";
import { Button } from "@progress/kendo-react-buttons";
import { questionCircleIcon } from "@progress/kendo-svg-icons";
import HelpCrudScreen from "@/app/components/Cruds/Help/HelpCrudScreen";

declare global {
  interface Window {
    storage?: any;
  }
}

interface OperadorHelpCrudButtonProps {
  isMobile: boolean;
}

const OperadorHelpCrudButton: React.FC<OperadorHelpCrudButtonProps> = ({
  isMobile,
}) => {
  const [showHelp, setShowHelp] = useState(false);

  const handleOpenHelp = async () => {
    setShowHelp(true);
  };

  return (
    <>
      <Button
        onClick={handleOpenHelp}
        aria-label="Ajuda da tela/cadastro Operador"
        svgIcon={questionCircleIcon}
      ></Button>
      {showHelp && (
        <HelpCrudScreen title="Operador" onBack={() => setShowHelp(false)} />
      )}
    </>
  );
};

export default OperadorHelpCrudButton;
