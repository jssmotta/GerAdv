// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBSMSAlice - Nome da classe (ex: DBClientes)
/// SMSAlice - Nome da tabela (ex: Clientes)
/// sma - Prefixo dos campos (ex: cli)
/// smaCodigo - Nome do campo código (ex: cliCodigo)
/// smaNome - Nome do campo nome (ex: cliNome)
/// DBSMSAlice.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBSMSAliceTests : IDisposable
{
    private readonly DBSMSAlice _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBSMSAliceTests()
    {
        _instance = new DBSMSAlice();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("smaCodigo", typeof(int));
        dt.Columns.Add("smaQuemCad", typeof(int));
        dt.Columns.Add("smaDtCad", typeof(DateTime));
        dt.Columns.Add("smaQuemAtu", typeof(int));
        dt.Columns.Add("smaDtAtu", typeof(DateTime));
        dt.Columns.Add("smaVisto", typeof(bool));
        dt.Columns.Add("smaOperador", typeof(int));
        dt.Columns.Add("smaNome", typeof(string));
        dt.Columns.Add("smaTipoEMail", typeof(int));
        dt.Columns.Add("smaGUID", typeof(string));
        return dt;
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("SMSAlice", DBSMSAlice.PTabelaNome);
        Assert.Equal(" SMSAlice.* ", DBSMSAlice.CamposSqlX);
        Assert.Equal("smaCodigo", DBSMSAlice.CampoCodigo);
        Assert.Equal("smaNome", DBSMSAlice.CampoNome);
        Assert.Equal("sma", DBSMSAlice.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBSMSAlice();
        Assert.Equal(0, instance.ID);
        Assert.Equal("SMSAlice", instance.ITabelaName());
        Assert.Equal("sma", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBSMSAlice((DataRow)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBSMSAlice((SqlDataReader)null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["smaCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBSMSAlice(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICadastros_Implementation_ShouldWork()
    {
        ICadastros cadastro = (ICadastros)_instance;
        Assert.Equal("SMSAlice", cadastro.ITabelaName());
        Assert.Equal("smaCodigo", cadastro.ICampoCodigo());
        Assert.Equal("smaNome", cadastro.ICampoNome());
        Assert.Equal("sma", cadastro.IPrefixo());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBSMSAlice.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBSMSAlice.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBSMSAlice.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBSMSAlice();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBSMSAlice();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IIsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IIsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData(0)]
    [InlineData(1)]
    [InlineData(-1)]
    [InlineData(int.MaxValue)]
    [InlineData(int.MinValue)]
    public void Operador_ShouldAcceptIntegerValues(int value)
    {
        _instance.FOperador = value;
        Assert.Equal(value, _instance.FOperador);
    }

    [Fact]
    public void Operador_DefaultValue_ShouldBeZero()
    {
        var instance = new DBSMSAlice();
        Assert.Equal(0, instance.FOperador);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 150 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 150);
    }

    [Theory]
    [InlineData(0)]
    [InlineData(1)]
    [InlineData(-1)]
    [InlineData(int.MaxValue)]
    [InlineData(int.MinValue)]
    public void TipoEMail_ShouldAcceptIntegerValues(int value)
    {
        _instance.FTipoEMail = value;
        Assert.Equal(value, _instance.FTipoEMail);
    }

    [Fact]
    public void TipoEMail_DefaultValue_ShouldBeZero()
    {
        var instance = new DBSMSAlice();
        Assert.Equal(0, instance.FTipoEMail);
    }

    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void GUID_ShouldTrimAndHandleNulls(string input, string expected)
    {
        _instance.FGUID = input;
        Assert.Equal(expected, _instance.FGUID);
    }

    [Fact]
    public void GUID_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 100 + 10);
        _instance.FGUID = longString;
        Assert.True(_instance.FGUID.Length <= 100);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}