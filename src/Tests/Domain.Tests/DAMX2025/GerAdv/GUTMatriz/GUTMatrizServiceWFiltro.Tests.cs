// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:GUTMatriz
// Fonte:TestWFiltroGenerator
namespace MenphisSI.GerAdv.WFiltro.Tests;
public class GUTMatrizServiceWFiltroTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFGUTMatrizFactory> _mockGUTMatrizFactory;
    private readonly Mock<IGUTMatrizReader> _mockReader;
    private readonly Mock<IGUTMatrizValidation> _mockValidation;
    private readonly Mock<IGUTMatrizWriter> _mockWriter;
    private readonly Mock<IGUTAtividadesMatrizService> _mockGUTAtividadesMatrizService;
    private readonly Mock<IGUTTipoReader> _mockGUTTipoReader;
    private readonly Mock<IHybridCache> _mockCache;
    private readonly Mock<IMemoryCache> _mockMemoryCache;
    private readonly Mock<IConnectionService> _mockConnectionService;
    private readonly Mock<IGenericVoiceFilterService<Filters.FilterGUTMatriz>> _mockVoiceFilterService;
    private readonly Mock<IEntityService> _mockEntityService;
    private readonly GUTMatrizService _service;
    private readonly ServiceFilter _serviceS;
    public GUTMatrizServiceWFiltroTests()
    {
        _mockAppSettings = new Mock<IOptions<AppSettings>>();
        _mockGUTMatrizFactory = new Mock<IFGUTMatrizFactory>();
        _mockReader = new Mock<IGUTMatrizReader>();
        _mockValidation = new Mock<IGUTMatrizValidation>();
        _mockWriter = new Mock<IGUTMatrizWriter>();
        _mockGUTAtividadesMatrizService = new Mock<IGUTAtividadesMatrizService>();
        _mockGUTTipoReader = new Mock<IGUTTipoReader>();
        _mockCache = new Mock<IHybridCache>();
        _mockMemoryCache = new Mock<IMemoryCache>();
        _mockConnectionService = new Mock<IConnectionService>();
        _mockVoiceFilterService = new Mock<IGenericVoiceFilterService<Filters.FilterGUTMatriz>>();
        _mockEntityService = new Mock<IEntityService>();
        var appSettings = new AppSettings();
        _mockAppSettings.Setup(x => x.Value).Returns(appSettings);
        _serviceS = new ServiceFilter();
        _service = new GUTMatrizService(_mockAppSettings.Object, _mockGUTMatrizFactory.Object, _mockReader.Object, _mockValidation.Object, _mockWriter.Object, _mockGUTTipoReader.Object, _mockGUTAtividadesMatrizService.Object, _mockCache.Object, _mockMemoryCache.Object, _mockConnectionService.Object, _mockVoiceFilterService.Object, _serviceS, _mockEntityService.Object);
    }

    [Fact]
    public void WFiltro_QuandoFiltroEhNull_DeveRetornarResultadoVazio()
    {
        // Arrange
        FilterGUTMatriz? filtro = null;
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().BeNull();
    }

    [Fact]
    public void WFiltro_QuandoTodosCamposSaoValoresPadrao_DeveRetornarResultadoVazio()
    {
        // Arrange
        var filtro = new FilterGUTMatriz(); // Todos os valores padrão
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_ComTodosOsCamposPreenchidos_DeveProcessarRapidamente()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            GUTTipo = 1,
            Valor = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var stopwatch = System.Diagnostics.Stopwatch.StartNew();
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        stopwatch.Stop();
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
        resultado.Value.where.Should().NotBeEmpty();
        stopwatch.ElapsedMilliseconds.Should().BeLessThan(100); // Deve processar em menos de 100ms
    }

#region String Tests
    [Theory]
    [InlineData("     ")] // Apenas espaços
    [InlineData("\t\t\t")] // Apenas tabs
    [InlineData("\n\n\n")] // Apenas quebras de linha
    [InlineData("\r\n\r\n")] // Carriage return + line feed
    public void WFiltro_Descricao_ComStringComApenasWhitespace_NaoDeveIncluirNoFiltro(string DescricaoComWhitespace)
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = DescricaoComWhitespace,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_Descricao_ComStringMuitoLonga_DeveProcessarCorretamente()
    {
        // Arrange
        var DescricaoGigante = new string ('A', 1000);
        var filtro = new FilterGUTMatriz
        {
            Descricao = DescricaoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"{DescricaoGigante}%");
    }

    [Fact]
    public void WFiltro_Descricao_ComStringMuitoLongaEAsterisco_DeveProcessarCorretamente()
    {
        // Arrange
        var DescricaoGigante = "*" + new string ('A', 999);
        var resultExpectedGigante = new string ('A', 999);
        var filtro = new FilterGUTMatriz
        {
            Descricao = DescricaoGigante,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.Should().Be($"%{resultExpectedGigante}%");
    }

    [Theory]
    [InlineData("João'Silva")] // Com aspas simples
    [InlineData("João\"Silva")] // Com aspas duplas
    [InlineData("João;DROP%TABLE--")] // SQL injection attempt
    [InlineData("João<script>alert('xss')</script>")] // XSS attempt
    public void WFiltro_Descricao_ComCaracteresEspeciais_DeveProcessarCorretamente(string DescricaoComCaracteresEspeciais)
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = DescricaoComCaracteresEspeciais,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().HaveCount(1);
        var parametro = resultado.Value.parametros.First();
        parametro.Value.ToString().Should().Contain(DescricaoComCaracteresEspeciais);
    }

    [Fact]
    public void WFiltro_DescricaoGUTMatriz_QuandoStringVazia_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = string.Empty,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty(); // WHERE vazio quando string vazia
        resultado.Value.parametros.Should().BeEmpty(); // Sem parâmetros
    }

    [Fact]
    public void WFiltro_DescricaoGUTMatriz_QuandoStringNull_NaoDeveIncluirNoFiltro()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = null,
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().BeEmpty();
        resultado.Value.parametros.Should().BeEmpty();
    }

    [Fact]
    public void WFiltro_DescricaoGUTMatriz_ComValorEWildcard_DeveUsarLIKEEIncluirWildcards()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "TestValue",
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando LIKE com wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_DescricaoGUTMatriz_ComValorSemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like"); // Usando = sem wildcard
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Descricao_ComStringNormal_DeveIncluirNoFiltroCorretamente()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Descricao",
            Codigo_filtro = 1, // Campo INT para testar combinação
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().NotBeEmpty();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Descricao_ComMultiplosWildcards_DeveProcessarCorretamente()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "%Test%Value%",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.parametros.Should().NotBeEmpty();
    }

    [Fact]
    public void WFiltro_Descricao_SemWildcard_DeveUsarIgualdade()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "TestValue",
            WildcardChar = '\0', // Sem wildcard
            LogicalOperator = " AND "
        };
        // Act
        var resultado = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        resultado.Should().NotBeNull();
        resultado.Value.where.Should().Contain("like");
        resultado.Value.parametros.Should().HaveCount(1);
    }

    [Fact]
    public void WFiltro_Descricao_AsFirstFilter_ShouldNotIncludeLogicalOperatorAtStart()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Test",
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().NotStartWith(TSql.And);
        result.Value.where.Should().StartWith($"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.Descricao}]");
    }

    [Theory]
    [InlineData("A", 1)]
    [InlineData("AB", 2)]
    [InlineData("ABC", 3)]
    [InlineData("ABCDEFGHIJ", 10)]
    public void WFiltro_Descricao_WithDifferentLengths_ShouldAddParameter(string value, int expectedLength)
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = value,
            WildcardChar = '%',
            LogicalOperator = TSql.And
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().HaveCount(1);
        var param = result.Value.parametros.First();
        // Value should be %value% so length is value.Length + 2
        param?.Value?.ToString()?.Length.Should().Be(expectedLength + 1);
    }

    [Fact]
    public void WFiltro_Descricao_WithORLogicalOperator_ShouldUseORWhenMultipleFilters()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Test",
            Codigo_filtro = 1,
            WildcardChar = '%',
            LogicalOperator = TSql.OR
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(TSql.OR);
    }

#endregion
#region Integer Tests
#region GUTTipo Filter Tests
    [Fact]
    public void WFiltro_GUTTipoEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            GUTTipo = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBGUTMatrizDicInfo.GUTTipo));
        result.Value.where.Should().NotContain(DBGUTMatrizDicInfo.GUTTipo);
    }

    [Fact]
    public void WFiltro_GUTTipoHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var GUTTipoValue = 1;
        var filtro = new FilterGUTMatriz
        {
            GUTTipo = GUTTipoValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var GUTTipoParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.GUTTipo}");
        GUTTipoParameter.Should().NotBeNull();
        GUTTipoParameter.Value.Should().Be(GUTTipoValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.GUTTipo}] = @{DBGUTMatrizDicInfo.GUTTipo}");
    }

    [Fact]
    public void WFiltro_GUTTipoWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for GUTTipo = 0 (typically female)
        var filtroFemale = new FilterGUTMatriz
        {
            GUTTipo = 0
        };
        var resultFemale = _serviceS.WFiltroGUTMatriz(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.GUTTipo}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for GUTTipo = 1 (typically male)
        var filtroMale = new FilterGUTMatriz
        {
            GUTTipo = 1
        };
        var resultMale = _serviceS.WFiltroGUTMatriz(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.GUTTipo}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_GUTTipoAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            GUTTipo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.GUTTipo}] = @{DBGUTMatrizDicInfo.GUTTipo}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_GUTTipoWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Descricao", // Campo string para testar combinação
            GUTTipo = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_GUTTipoWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Descricao", // Campo string para testar combinação
            GUTTipo = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#region Valor Filter Tests
    [Fact]
    public void WFiltro_ValorEqualsIntMinValue_ShouldNotAddParameterOrWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Valor = int.MinValue
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.parametros.Should().NotContain(p => p.ParameterName.Contains(DBGUTMatrizDicInfo.Valor));
        result.Value.where.Should().NotContain(DBGUTMatrizDicInfo.Valor);
    }

    [Fact]
    public void WFiltro_ValorHasValidValue_ShouldAddParameterAndWhereClause()
    {
        // Arrange
        var ValorValue = 1;
        var filtro = new FilterGUTMatriz
        {
            Valor = ValorValue,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        // Check parameter
        var ValorParameter = result.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.Valor}");
        ValorParameter.Should().NotBeNull();
        ValorParameter.Value.Should().Be(ValorValue);
        // Check where clause
        result.Value.where.Should().Contain($"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.Valor}] = @{DBGUTMatrizDicInfo.Valor}");
    }

    [Fact]
    public void WFiltro_ValorWithDifferentValues_ShouldAddCorrectParameterValue()
    {
        // Arrange & Act & Assert for Valor = 0 (typically female)
        var filtroFemale = new FilterGUTMatriz
        {
            Valor = 0
        };
        var resultFemale = _serviceS.WFiltroGUTMatriz(filtroFemale);
        resultFemale.Should().NotBeNull();
        var femaleParam = resultFemale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.Valor}");
        femaleParam.Should().NotBeNull();
        femaleParam.Value.Should().Be(0);
        // Arrange & Act & Assert for Valor = 1 (typically male)
        var filtroMale = new FilterGUTMatriz
        {
            Valor = 1
        };
        var resultMale = _serviceS.WFiltroGUTMatriz(filtroMale);
        resultMale.Should().NotBeNull();
        var maleParam = resultMale.Value.parametros.FirstOrDefault(p => p.ParameterName == $"@{DBGUTMatrizDicInfo.Valor}");
        maleParam.Should().NotBeNull();
        maleParam.Value.Should().Be(1);
    }

    [Fact]
    public void WFiltro_ValorAsFirstFilter_ShouldNotIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Valor = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().StartWith($"[{DBGUTMatrizDicInfo.PTabelaNome}].[{DBGUTMatrizDicInfo.Valor}] = @{DBGUTMatrizDicInfo.Valor}");
        result.Value.where.Should().NotStartWith(" AND ");
    }

    [Fact]
    public void WFiltro_ValorWithOtherFilters_ShouldIncludeLogicalOperatorInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Descricao", // Campo string para testar combinação
            Valor = 1,
            LogicalOperator = " AND "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain($" AND ");
    }

    [Fact]
    public void WFiltro_ValorWithORLogicalOperator_ShouldUseORInWhereClause()
    {
        // Arrange
        var filtro = new FilterGUTMatriz
        {
            Descricao = "Descricao", // Campo string para testar combinação
            Valor = 1,
            LogicalOperator = " OR "
        };
        // Act
        var result = _serviceS.WFiltroGUTMatriz(filtro);
        // Assert
        result.Should().NotBeNull();
        result.Value.where.Should().Contain(" OR ");
    }

#endregion
#endregion
#region Decimal Tests
#endregion
#region DateTime Tests
#endregion
#region Bool Tests
#endregion
    [Fact]
    public void Dispose_CalledOnce_DisposesCorrectly()
    {
        // Act
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    [Fact]
    public void Dispose_CalledTwice_DoesNotThrow()
    {
        // Act
        _service.Dispose();
        _service.Dispose();
        // Assert - Should not throw any exception
        Assert.True(true);
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
        GC.SuppressFinalize(this);
    }
}