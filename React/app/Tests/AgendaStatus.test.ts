// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useAgendaStatusForm, useAgendaStatusList, useValidationsAgendaStatus } from '../GerAdv_TS/AgendaStatus/Hooks/hookAgendaStatus';
import { IAgendaStatus } from '../GerAdv_TS/AgendaStatus/Interfaces/interface.AgendaStatus';
import { IAgendaStatusService } from '../GerAdv_TS/AgendaStatus/Services/AgendaStatus.service';
import { AgendaStatusTestEmpty } from '../GerAdv_TS/Models/AgendaStatus';


// Mock do serviço
const mockAgendaStatusService: jest.Mocked<IAgendaStatusService> = {
  fetchAgendaStatusById: jest.fn(),
  saveAgendaStatus: jest.fn(),
  
  getAll: jest.fn(),
  deleteAgendaStatus: jest.fn(),
  validateAgendaStatus: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialAgendaStatus: IAgendaStatus = { ...AgendaStatusTestEmpty() };

describe('useAgendaStatusForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    expect(result.current.data).toEqual(initialAgendaStatus);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    const mockEvent = {
      target: {
        name: 'campo',
        value: 'Novo Agenda Status',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.campo).toBe('Novo Agenda Status');
  });

   test('deve carregar Agenda Status por ID', async () => {
    const mockAgendaStatus = { ...initialAgendaStatus, id: 1, campo: 'Agenda Status Teste' };
    mockAgendaStatusService.fetchAgendaStatusById.mockResolvedValue(mockAgendaStatus);

    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.loadAgendaStatus(1);
    });

    expect(mockAgendaStatusService.fetchAgendaStatusById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockAgendaStatus);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Agenda Status', async () => {
    const errorMessage = 'Erro ao carregar Agenda Status';
    mockAgendaStatusService.fetchAgendaStatusById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.loadAgendaStatus(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialAgendaStatus, campo: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialAgendaStatus);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useAgendaStatusForm(initialAgendaStatus, mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.loadAgendaStatus(0);
    });

    expect(mockAgendaStatusService.fetchAgendaStatusById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialAgendaStatus);
  });
});

describe('useAgendaStatusList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAgendaStatusList(mockAgendaStatusService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialAgendaStatus, id: 1, campo: 'Agenda Status 1' },
      { ...initialAgendaStatus, id: 2, campo: 'Agenda Status 2' }
    ];
    mockAgendaStatusService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAgendaStatusList(mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockAgendaStatusService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockAgendaStatusService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAgendaStatusList(mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialAgendaStatus, id: 1, campo: 'Agenda Status Filtrado' }];
    const filtro = { campo: 'Agenda Status' };
    mockAgendaStatusService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAgendaStatusList(mockAgendaStatusService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockAgendaStatusService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsAgendaStatus', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsAgendaStatus());

    const validData = { ...initialAgendaStatus, campo: 'Agenda Status Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


  

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsAgendaStatus());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialAgendaStatus, id: 1, campo: 'Agenda Status Teste' }];
    mockAgendaStatusService.getAll.mockResolvedValue(mockData);
    

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useAgendaStatusList(mockAgendaStatusService)
    );
    
       

    const { result: validationResult } = renderHook(() => 
      useValidationsAgendaStatus()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
    
  
    expect(validation.isValid).toBe(true);
  });
});