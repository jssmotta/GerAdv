// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { ProTipoBaixaService, ProTipoBaixaValidator } from '@/app/GerAdv_TS/ProTipoBaixa/Services/ProTipoBaixa.service';
import { ProTipoBaixaApi, ProTipoBaixaApiError } from '@/app/GerAdv_TS/ProTipoBaixa/Apis/ApiProTipoBaixa';
import { IProTipoBaixa } from '@/app/GerAdv_TS/ProTipoBaixa/Interfaces/interface.ProTipoBaixa';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as ProTipoBaixaApi;

const sample: IProTipoBaixa = { id: 1, nome: 'Teste' };

describe('ProTipoBaixaService', () => {
  let service: ProTipoBaixaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new ProTipoBaixaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(ProTipoBaixaValidator, 'validateProTipoBaixa').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchProTipoBaixaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchProTipoBaixaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchProTipoBaixaById lança ProTipoBaixaApiError para id inválido', async () => {
    await expect(service.fetchProTipoBaixaById(0)).rejects.toBeInstanceOf(ProTipoBaixaApiError);
  });

  it('saveProTipoBaixa chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveProTipoBaixa(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveProTipoBaixa lança ProTipoBaixaApiError quando validação falha', async () => {
    (ProTipoBaixaValidator.validateProTipoBaixa as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveProTipoBaixa(sample)).rejects.toBeInstanceOf(ProTipoBaixaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteProTipoBaixa chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteProTipoBaixa(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteProTipoBaixa lança ProTipoBaixaApiError para id inválido', async () => {
    await expect(service.deleteProTipoBaixa(0)).rejects.toBeInstanceOf(ProTipoBaixaApiError);
  });

  it('validateProTipoBaixa delega para ProTipoBaixaValidator', () => {
    (ProTipoBaixaValidator.validateProTipoBaixa as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateProTipoBaixa(sample);
    expect(r.isValid).toBe(true);
  });
});
