// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class GUTTipoWriterTests
{
    private readonly Mock<IFGUTTipoFactory> _mockGUTTipoFactory;
    private readonly Mock<FGUTTipo> _mockFGUTTipo;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly GUTTipoWriter _guttipoWriter;
    public GUTTipoWriterTests()
    {
        _mockGUTTipoFactory = new Mock<IFGUTTipoFactory>();
        _mockFGUTTipo = new Mock<FGUTTipo>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _guttipoWriter = new GUTTipoWriter(_mockGUTTipoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var guttipo = CreateValidGUTTipoModel();
        guttipo.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockGUTTipoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGUTTipo.Object);
        _mockFGUTTipo.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _guttipoWriter.WriteAsync(guttipo, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGUTTipo.Object);
        _mockGUTTipoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockGUTTipoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFGUTTipo.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var guttipo = CreateValidGUTTipoModel();
        guttipo.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockGUTTipoFactory.Setup(x => x.CreateFromIdAsync(guttipo.Id, _mockConnection.Object)).ReturnsAsync(_mockFGUTTipo.Object);
        _mockFGUTTipo.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _guttipoWriter.WriteAsync(guttipo, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGUTTipo.Object);
        _mockGUTTipoFactory.Verify(x => x.CreateFromIdAsync(guttipo.Id, _mockConnection.Object), Times.Once);
        _mockGUTTipoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFGUTTipo.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var guttipo = CreateValidGUTTipoModel();
        var auditorQuem = 99;
        _mockGUTTipoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGUTTipo.Object);
        _mockFGUTTipo.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _guttipoWriter.WriteAsync(guttipo, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFGUTTipo.Object);
        _mockFGUTTipo.VerifySet(x => x.FNome = guttipo.Nome, Times.Once);
        _mockFGUTTipo.VerifySet(x => x.FOrdem = guttipo.Ordem, Times.Once);
        _mockFGUTTipo.VerifySet(x => x.FGUID = guttipo.GUID, Times.Once);
        _mockFGUTTipo.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var guttipo = CreateValidGUTTipoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockGUTTipoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _guttipoWriter.WriteAsync(guttipo, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var guttipo = CreateValidGUTTipoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockGUTTipoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFGUTTipo.Object);
        _mockFGUTTipo.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _guttipoWriter.WriteAsync(guttipo, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var guttipoResponse = new GUTTipoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockGUTTipoFactory.Setup(x => x.DeleteAsync(operadorId, guttipoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _guttipoWriter.Delete(guttipoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockGUTTipoFactory.Verify(x => x.DeleteAsync(operadorId, guttipoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var guttipoResponse = new GUTTipoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockGUTTipoFactory.Setup(x => x.DeleteAsync(operadorId, guttipoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _guttipoWriter.Delete(guttipoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var guttipoResponse = new GUTTipoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockGUTTipoFactory.Setup(x => x.DeleteAsync(operadorId, guttipoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _guttipoWriter.Delete(guttipoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new GUTTipoWriter(_mockGUTTipoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new GUTTipoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.GUTTipo CreateValidGUTTipoModel()
    {
        return new Models.GUTTipo
        {
            Id = 0,
            Nome = "João",
            Ordem = 1,
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}