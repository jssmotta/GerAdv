// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { OperadorGruposService, OperadorGruposValidator } from '@/app/GerAdv_TS/OperadorGrupos/Services/OperadorGrupos.service';
import { OperadorGruposApi, OperadorGruposApiError } from '@/app/GerAdv_TS/OperadorGrupos/Apis/ApiOperadorGrupos';
import { IOperadorGrupos } from '@/app/GerAdv_TS/OperadorGrupos/Interfaces/interface.OperadorGrupos';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as OperadorGruposApi;

const sample: IOperadorGrupos = { id: 1, nome: 'Teste' };

describe('OperadorGruposService', () => {
  let service: OperadorGruposService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new OperadorGruposService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(OperadorGruposValidator, 'validateOperadorGrupos').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchOperadorGruposById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchOperadorGruposById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchOperadorGruposById lança OperadorGruposApiError para id inválido', async () => {
    await expect(service.fetchOperadorGruposById(0)).rejects.toBeInstanceOf(OperadorGruposApiError);
  });

  it('saveOperadorGrupos chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveOperadorGrupos(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveOperadorGrupos lança OperadorGruposApiError quando validação falha', async () => {
    (OperadorGruposValidator.validateOperadorGrupos as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveOperadorGrupos(sample)).rejects.toBeInstanceOf(OperadorGruposApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteOperadorGrupos chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteOperadorGrupos(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteOperadorGrupos lança OperadorGruposApiError para id inválido', async () => {
    await expect(service.deleteOperadorGrupos(0)).rejects.toBeInstanceOf(OperadorGruposApiError);
  });

  it('validateOperadorGrupos delega para OperadorGruposValidator', () => {
    (OperadorGruposValidator.validateOperadorGrupos as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateOperadorGrupos(sample);
    expect(r.isValid).toBe(true);
  });
});
