// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for TipoOrigemSucumbencia page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock TipoOrigemSucumbenciaGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/TipoOrigemSucumbencia/Adapter/TipoOrigemSucumbenciaGridAdapter', () => ({
  TipoOrigemSucumbenciaGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock TipoOrigemSucumbenciaGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/TipoOrigemSucumbencia/Components/TipoOrigemSucumbenciaGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='tipoorigemsucumbencia-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import TipoOrigemSucumbenciaPage from '@/app/pages/tipoorigemsucumbencia/page';

describe('TipoOrigemSucumbencia page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and TipoOrigemSucumbenciaGridContainer', () => {
    render(<TipoOrigemSucumbenciaPage />);

    expect(screen.getByRole('heading', { name: /Tipo Origem Sucumbencia/i })).toBeInTheDocument();
    expect(screen.getByTestId('tipoorigemsucumbencia-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

