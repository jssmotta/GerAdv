// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useTipoEnderecoForm, useTipoEnderecoList, useValidationsTipoEndereco } from '../GerAdv_TS/TipoEndereco/Hooks/hookTipoEndereco';
import { ITipoEndereco } from '../GerAdv_TS/TipoEndereco/Interfaces/interface.TipoEndereco';
import { ITipoEnderecoService } from '../GerAdv_TS/TipoEndereco/Services/TipoEndereco.service';
import { TipoEnderecoTestEmpty } from '../GerAdv_TS/Models/TipoEndereco';
import { useTipoEnderecoComboBox } from '../GerAdv_TS/TipoEndereco/Hooks/hookTipoEndereco';

// Mock do serviço
const mockTipoEnderecoService: jest.Mocked<ITipoEnderecoService> = {
  fetchTipoEnderecoById: jest.fn(),
  saveTipoEndereco: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteTipoEndereco: jest.fn(),
  validateTipoEndereco: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialTipoEndereco: ITipoEndereco = { ...TipoEnderecoTestEmpty() };

describe('useTipoEnderecoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    expect(result.current.data).toEqual(initialTipoEndereco);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Tipo Endereco',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Tipo Endereco');
  });

   test('deve carregar Tipo Endereco por ID', async () => {
    const mockTipoEndereco = { ...initialTipoEndereco, id: 1, descricao: 'Tipo Endereco Teste' };
    mockTipoEnderecoService.fetchTipoEnderecoById.mockResolvedValue(mockTipoEndereco);

    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.loadTipoEndereco(1);
    });

    expect(mockTipoEnderecoService.fetchTipoEnderecoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockTipoEndereco);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Tipo Endereco', async () => {
    const errorMessage = 'Erro ao carregar Tipo Endereco';
    mockTipoEnderecoService.fetchTipoEnderecoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.loadTipoEndereco(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialTipoEndereco, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialTipoEndereco);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useTipoEnderecoForm(initialTipoEndereco, mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.loadTipoEndereco(0);
    });

    expect(mockTipoEnderecoService.fetchTipoEnderecoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialTipoEndereco);
  });
});

describe('useTipoEnderecoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoList(mockTipoEnderecoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialTipoEndereco, id: 1, descricao: 'Tipo Endereco 1' },
      { ...initialTipoEndereco, id: 2, descricao: 'Tipo Endereco 2' }
    ];
    mockTipoEnderecoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTipoEnderecoList(mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockTipoEnderecoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockTipoEnderecoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useTipoEnderecoList(mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialTipoEndereco, id: 1, descricao: 'Tipo Endereco Filtrado' }];
    const filtro = { descricao: 'Tipo Endereco' };
    mockTipoEnderecoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useTipoEnderecoList(mockTipoEnderecoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockTipoEnderecoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsTipoEndereco', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsTipoEndereco());

    const validData = { ...initialTipoEndereco, descricao: 'Tipo Endereco Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsTipoEndereco());

    const invalidData = { ...initialTipoEndereco, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsTipoEndereco());

    const invalidData = { 
      ...initialTipoEndereco, 
      descricao: 'a'.repeat(40+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 40 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsTipoEndereco());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialTipoEndereco, id: 1, descricao: 'Tipo Endereco Teste' }];
    mockTipoEnderecoService.getAll.mockResolvedValue(mockData);
    mockTipoEnderecoService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useTipoEnderecoList(mockTipoEnderecoService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsTipoEndereco()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Tipo Endereco Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Tipo Endereco Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Tipo Endereco 1' },
      { id: 2, descricao: 'Tipo Endereco 2' }
    ];
    mockTipoEnderecoService.getList.mockResolvedValue(mockOptions as ITipoEndereco[]);


    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Endereco 1' },
        { id: 2, nome: 'Tipo Endereco 2' }
      ]);
    });


    expect(mockTipoEnderecoService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Tipo Endereco ABC' },
      { id: 2, descricao: 'Tipo Endereco XYZ' }
    ];
    mockTipoEnderecoService.getList.mockResolvedValue(mockOptions as ITipoEndereco[]);   


 const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Endereco ABC' },
        { id: 2, nome: 'Tipo Endereco XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Tipo Endereco ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Tipo Endereco ABC' },
      { id: 2, descricao: 'Tipo Endereco XYZ' }
    ];
    mockTipoEnderecoService.getList.mockResolvedValue(mockOptions as ITipoEndereco[]);
  


    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Tipo Endereco ABC' },
        { id: 2, nome: 'Tipo Endereco XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Tipo Endereco ABC' },
          {id: 2, nome: 'Tipo Endereco XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );

    const newValue = { id: 1, descricao: 'Tipo Endereco Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Tipo Endereco Inicial' };
    
    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useTipoEnderecoComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Tipo Endereco Inicial' };
    
    const { result } = renderHook(() => 
      useTipoEnderecoComboBox(mockTipoEnderecoService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






