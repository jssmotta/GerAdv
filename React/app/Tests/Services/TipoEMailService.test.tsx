// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { TipoEMailService, TipoEMailValidator } from '@/app/GerAdv_TS/TipoEMail/Services/TipoEMail.service';
import { TipoEMailApi, TipoEMailApiError } from '@/app/GerAdv_TS/TipoEMail/Apis/ApiTipoEMail';
import { ITipoEMail } from '@/app/GerAdv_TS/TipoEMail/Interfaces/interface.TipoEMail';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as TipoEMailApi;

const sample: ITipoEMail = { id: 1, nome: 'Teste' };

describe('TipoEMailService', () => {
  let service: TipoEMailService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new TipoEMailService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(TipoEMailValidator, 'validateTipoEMail').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchTipoEMailById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchTipoEMailById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchTipoEMailById lança TipoEMailApiError para id inválido', async () => {
    await expect(service.fetchTipoEMailById(0)).rejects.toBeInstanceOf(TipoEMailApiError);
  });

  it('saveTipoEMail chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveTipoEMail(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveTipoEMail lança TipoEMailApiError quando validação falha', async () => {
    (TipoEMailValidator.validateTipoEMail as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveTipoEMail(sample)).rejects.toBeInstanceOf(TipoEMailApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteTipoEMail chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteTipoEMail(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteTipoEMail lança TipoEMailApiError para id inválido', async () => {
    await expect(service.deleteTipoEMail(0)).rejects.toBeInstanceOf(TipoEMailApiError);
  });

  it('validateTipoEMail delega para TipoEMailValidator', () => {
    (TipoEMailValidator.validateTipoEMail as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateTipoEMail(sample);
    expect(r.isValid).toBe(true);
  });
});
