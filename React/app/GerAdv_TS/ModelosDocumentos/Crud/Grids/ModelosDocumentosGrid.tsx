// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
//CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { ModelosDocumentosEmpty } from '../../../Models/ModelosDocumentos';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IModelosDocumentos } from '../../Interfaces/interface.ModelosDocumentos';
import { ModelosDocumentosService } from '../../Services/ModelosDocumentos.service';
import { ModelosDocumentosApi } from '../../Apis/ApiModelosDocumentos';
import { ModelosDocumentosGridMobileComponent } from '../GridsMobile/ModelosDocumentosMobile';
import { ModelosDocumentosGridDesktopComponent } from '../GridsDesktop/ModelosDocumentosDesktop';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterModelosDocumentos } from '../../Filters/ModelosDocumentos';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import ModelosDocumentosWindow from './ModelosDocumentosWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useModelosDocumentosList } from '../../Hooks/hookModelosDocumentos';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useModelosDocumentosFilter } from '../../Hooks/hookModelosDocumentosFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const ModelosDocumentosGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedModelosDocumentos, setSelectedModelosDocumentos] = useState<IModelosDocumentos>(ModelosDocumentosEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterModelosDocumentos | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [modelosdocumentosData, setModelosDocumentosData] = useState<IModelosDocumentos[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const modelosdocumentosService = useMemo(() => {
    return new ModelosDocumentosService(
    new ModelosDocumentosApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: modelosdocumentos, 
  fetchData, 
  refreshData
} = useModelosDocumentosList(modelosdocumentosService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterModelosDocumentos | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('ModelosDocumentosFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await modelosdocumentosService.getAll(
  filtro ?? {} as FilterModelosDocumentos, 
  onlineData => {
    setModelosDocumentosData(onlineData);
    setLoading(false);
  }
);
setModelosDocumentosData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterModelosDocumentos;

    const filtroMem = sessionStorage.getItem(btoa('ModelosDocumentosFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('ModelosDocumentosFilter')) ?? '') || '{}') : {} as FilterModelosDocumentos;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (modelosdocumentos: IModelosDocumentos) => {
    setSelectedModelosDocumentos(modelosdocumentos);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedModelosDocumentos(ModelosDocumentosEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const modelosdocumentos = e.dataItem;
    setDeleteId(modelosdocumentos.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await modelosdocumentosService.deleteModelosDocumentos(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useModelosDocumentosFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <ModelosDocumentosGridMobileComponent
        data={modelosdocumentosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <ModelosDocumentosGridDesktopComponent
        data={modelosdocumentosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <ModelosDocumentosWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedModelosDocumentos={selectedModelosDocumentos}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Modelos Documentos'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default ModelosDocumentosGrid;