// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ClientesWindowId from '@/app/GerAdv_TS/Clientes/Crud/Grids/ClientesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ClientesWindowId from '@/app/GerAdv_TS/Clientes/Crud/Grids/ClientesWindowId';
import { ClientesEmpty } from '@/app/GerAdv_TS/Models/Clientes';

describe('ClientesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ClientesWindowId).toBeDefined();
    expect(typeof ClientesWindowId === 'function' || typeof ClientesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ClientesService class so constructor usage doesn't throw and we can control fetchClientesById
const mockFetchClientesById = jest.fn();
jest.mock('@/app/GerAdv_TS/Clientes/Services/Clientes.service', () => ({
  ClientesService: jest.fn().mockImplementation(() => ({
    fetchClientesById: (...args: any[]) => mockFetchClientesById(...args),
  })),
}));

// Mock the child ClientesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Clientes/Crud/Grids/ClientesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='clientes-window' {...p} />,
}));

describe('ClientesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ClientesWindowId).toBeDefined();
    expect(typeof ClientesWindowId === 'function' || typeof ClientesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ClientesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ClientesWindow with ClientesEmpty when id is 0', async () => {
    render(
      <ClientesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ClientesWindow should be rendered with selectedClientes equal to ClientesEmpty()
    const node = await screen.findByTestId('clientes-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedClientes"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedclientes') || node.getAttribute('selectedClientes')).not.toBeNull();
  });

  it('fetches clientes by id and passes it to ClientesWindow when id > 0', async () => {
    const fakeClientes = { id: 123, nomecliente: 'Test' };
    mockFetchClientesById.mockResolvedValue(fakeClientes);

    render(
      <ClientesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('clientes-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchClientesById).toHaveBeenCalledWith(123));
  });
});
