// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Fonte:ReadersGenerator
namespace MenphisSI.GerAdv.Readers;
public partial class PenhoraStatusReader(IFPenhoraStatusFactory penhorastatusFactory) : IPenhoraStatusReader
{
    private readonly IFPenhoraStatusFactory _penhorastatusFactory = penhorastatusFactory ?? throw new ArgumentNullException();
    public async Task<IEnumerable<DBNomeID>?> ListarNAsync(int max, string uri, string cWhere, List<SqlParameter>? parameters, string order) => await DevourerSqlData.ListarNomeID(BuildSqlQuery("phsCodigo, phsNome", cWhere, order, max), parameters, uri, caching: false, max: max);
    public async Task<IEnumerable<PenhoraStatusResponseAll>> ListarAsync(MsiSqlConnection oCnn, int max, string uri, string cWhere, List<SqlParameter>? parameters, string order, CancellationToken cancellationToken)
    {
        var stopwatch = PenhoraStatusDatabaseMetrics.StartTimer();
        var connStopwatch = PenhoraStatusDatabaseMetrics.StartTimer();
        try
        {
            PenhoraStatusDatabaseMetrics.IncrementActiveConnections("ListarAsync", oCnn?.Uri);
            PenhoraStatusDatabaseMetrics.RecordConnectionOpen("ListarAsync", oCnn?.Uri, connStopwatch);
            var result = await ListarTabelaAsync(oCnn, BuildSqlQuery(DBPenhoraStatus.CamposSqlX, cWhere, order, max), parameters, uri, caching: false, max: max, cancellationToken: cancellationToken);
            PenhoraStatusDatabaseMetrics.RecordSqlQuery("ListarAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count());
            return result;
        }
        catch (SqlException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ListarAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ListarAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
            PenhoraStatusDatabaseMetrics.DecrementActiveConnections("ListarAsync", oCnn?.Uri);
        }
    }

    private async Task<IEnumerable<PenhoraStatusResponseAll>> ListarTabelaAsync(MsiSqlConnection? oCnn, string sql, List<SqlParameter>? parameters, string uri, bool caching = false, int max = 200, CancellationToken cancellationToken = default)
    {
        var stopwatch = PenhoraStatusDatabaseMetrics.StartTimer();
        var result = new List<PenhoraStatusResponseAll>(max);
        try
        {
            await using var cmd = new SqlCommand(cmdText: ConfiguracoesDBT.CmdSql(sql), connection: oCnn?.InnerConnection)
            {
                CommandTimeout = 120
            };
            if (parameters != null && parameters.Count > 0)
                foreach (var param in parameters)
                {
                    if (!cmd.Parameters.Contains(param.ParameterName))
                    {
                        var newParam = new SqlParameter(param.ParameterName, param.Value)
                        {
                            SqlDbType = param.SqlDbType,
                            Direction = param.Direction,
                            Size = param.Size,
                            Precision = param.Precision,
                            Scale = param.Scale
                        };
                        cmd.Parameters.Add(newParam);
                    }
                }

            await using var reader = await cmd.ExecuteReaderAsync(CommandBehavior.Default, cancellationToken);
            while (await reader.ReadAsync(cancellationToken))
            {
                if (cancellationToken.IsCancellationRequested)
                    return result;
                result.Add(ReadAll(_penhorastatusFactory.CreateFromReader(reader), reader)!);
            }

            PenhoraStatusDatabaseMetrics.RecordSqlQuery("ListarTabelaAsync", "SELECT", oCnn?.Uri, stopwatch, result.Count);
            return result;
        }
        catch (SqlException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ListarTabelaAsync", "Timeout", oCnn?.Uri);
            throw;
        }
    }

    public async Task<PenhoraStatusResponse?> ReadAsync(int id, MsiSqlConnection? oCnn)
    {
        var stopwatch = PenhoraStatusDatabaseMetrics.StartTimer();
        try
        {
            PenhoraStatusDatabaseMetrics.IncrementActiveConnections("ReadAsync", oCnn?.Uri);
            using var dbRec = await _penhorastatusFactory.CreateFromIdAsync(id, oCnn);
            var result = dbRec == null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
            PenhoraStatusDatabaseMetrics.RecordSqlQuery("ReadAsync", "SELECT", oCnn?.Uri, stopwatch, result != null ? 1 : 0);
            return result;
        }
        catch (SqlException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ReadAsync", "SqlException", oCnn?.Uri);
            throw;
        }
        catch (TimeoutException)
        {
            PenhoraStatusDatabaseMetrics.RecordDatabaseError("ReadAsync", "Timeout", oCnn?.Uri);
            throw;
        }
        finally
        {
        // Ignore
        }
    }

    public async Task<Models.PenhoraStatus?> ReadMAsync(int id, MsiSqlConnection? oCnn)
    {
        using var dbRec = await _penhorastatusFactory.CreateFromIdAsync(id, oCnn);
        var penhorastatus = new Models.PenhoraStatus
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return penhorastatus;
    }

    public PenhoraStatusResponse? Read(FPenhoraStatus dbRec, MsiSqlConnection? oCnn)
    {
        return Read(dbRec);
    }

    public PenhoraStatusResponse? Read(string where, List<SqlParameter>? parameters, MsiSqlConnection? oCnn)
    {
        using var dbRec = _penhorastatusFactory.CreateFromParameters(parameters, oCnn, sqlWhere: where);
        return dbRec is null || dbRec.ID.IsEmptyIDNumber() ? null : Read(dbRec);
    }

    public PenhoraStatusResponse? Read(FPenhoraStatus dbRec) // Track 001.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var penhorastatus = new PenhoraStatusResponse
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return penhorastatus;
    }

    public PenhoraStatusResponse? Read(SG.GerAdv.DBPenhoraStatus dbRec) // Track 002.16072025
    {
        if (dbRec == null)
        {
            return null;
        }

        var penhorastatus = new PenhoraStatusResponse
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return penhorastatus;
    }

    public PenhoraStatusResponseAll? ReadAll(FPenhoraStatus dbRec, IDataRecord dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var penhorastatus = new PenhoraStatusResponseAll
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return penhorastatus;
    }

    public PenhoraStatusResponseAll? ReadAll(SG.GerAdv.DBPenhoraStatus dbRec, DataRow dr)
    {
        if (dbRec == null)
        {
            return null;
        }

        var penhorastatus = new PenhoraStatusResponseAll
        {
            Id = dbRec.ID,
            Nome = dbRec.FNome ?? string.Empty,
            Guid = dbRec.FGuid ?? string.Empty,
        };
        return penhorastatus;
    }
}