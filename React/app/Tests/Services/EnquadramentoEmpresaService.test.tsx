// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { EnquadramentoEmpresaService, EnquadramentoEmpresaValidator } from '@/app/GerAdv_TS/EnquadramentoEmpresa/Services/EnquadramentoEmpresa.service';
import { EnquadramentoEmpresaApi, EnquadramentoEmpresaApiError } from '@/app/GerAdv_TS/EnquadramentoEmpresa/Apis/ApiEnquadramentoEmpresa';
import { IEnquadramentoEmpresa } from '@/app/GerAdv_TS/EnquadramentoEmpresa/Interfaces/interface.EnquadramentoEmpresa';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as EnquadramentoEmpresaApi;

const sample: IEnquadramentoEmpresa = { id: 1, nome: 'Teste' };

describe('EnquadramentoEmpresaService', () => {
  let service: EnquadramentoEmpresaService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new EnquadramentoEmpresaService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(EnquadramentoEmpresaValidator, 'validateEnquadramentoEmpresa').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchEnquadramentoEmpresaById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchEnquadramentoEmpresaById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchEnquadramentoEmpresaById lança EnquadramentoEmpresaApiError para id inválido', async () => {
    await expect(service.fetchEnquadramentoEmpresaById(0)).rejects.toBeInstanceOf(EnquadramentoEmpresaApiError);
  });

  it('saveEnquadramentoEmpresa chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveEnquadramentoEmpresa(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveEnquadramentoEmpresa lança EnquadramentoEmpresaApiError quando validação falha', async () => {
    (EnquadramentoEmpresaValidator.validateEnquadramentoEmpresa as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveEnquadramentoEmpresa(sample)).rejects.toBeInstanceOf(EnquadramentoEmpresaApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteEnquadramentoEmpresa chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteEnquadramentoEmpresa(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteEnquadramentoEmpresa lança EnquadramentoEmpresaApiError para id inválido', async () => {
    await expect(service.deleteEnquadramentoEmpresa(0)).rejects.toBeInstanceOf(EnquadramentoEmpresaApiError);
  });

  it('validateEnquadramentoEmpresa delega para EnquadramentoEmpresaValidator', () => {
    (EnquadramentoEmpresaValidator.validateEnquadramentoEmpresa as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateEnquadramentoEmpresa(sample);
    expect(r.isValid).toBe(true);
  });
});
