// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
'use client';
import { useState, useEffect, useCallback, useRef, useMemo } from 'react';
import { IAreasJusticaService } from '../Services/AreasJustica.service';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { IAreasJustica } from '../Interfaces/interface.AreasJustica';
import { FilterAreasJustica } from '../Filters/AreasJustica';
import { isValidDate } from '@/app/tools/datetime';
import { AreasJusticaApi } from '../Apis/ApiAreasJustica';

export const useAreasJusticaForm = (
  initialAreasJustica: IAreasJustica,
  dataService: IAreasJusticaService
) => {
  const [data, setData] = useState<IAreasJustica>(initialAreasJustica);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleChange = useCallback((e: any) => {
    const { name, value, type, checked } = e.target;
    setData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));    
  }, []);

  const loadAreasJustica = useCallback(async (id: number) => {
    if (!id || id === 0) {
      setData(initialAreasJustica);
      return;
    }

    setLoading(true);
    setError(null);
    
    try {
      const dados = await dataService.fetchAreasJusticaById(id);
      setData(dados);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar Areas Justica';
      setError(errorMessage);
      //console.log('Erro ao carregar Areas Justica');
    } finally {
      setLoading(false);
    }
  }, [dataService, initialAreasJustica]);

  const resetForm = useCallback(() => {
    setData(initialAreasJustica);
    setError(null);
  }, [initialAreasJustica]);

  const returnValue = useMemo(() => ({
    data,
    loading,
    error,
    handleChange,
    loadAreasJustica,
    resetForm,
    setData
  }), [data, loading, error, handleChange, loadAreasJustica, resetForm]);
  return returnValue;
};


export const useAreasJusticaNotifications = (
  onUpdate?: (entity: any) => void,
  onDelete?: (entity: any) => void,
  onAdd?: (entity: any) => void
) => {
  const callbacksRef = useRef({ onUpdate, onDelete, onAdd });
  
  useEffect(() => {
    callbacksRef.current = { onUpdate, onDelete, onAdd };
  }, [onUpdate, onDelete, onAdd]);

  useEffect(() => {
    const unsubscribe = subscribeToNotifications('AreasJustica', (entity) => {
      try {
        const { onUpdate, onDelete, onAdd } = callbacksRef.current;
        
        switch (entity.action) {
          case NotifySystemActions.DELETE:
            onDelete?.(entity);
            break;
          case NotifySystemActions.UPDATE:
            onUpdate?.(entity);
            break;
          case NotifySystemActions.ADD:
            onAdd?.(entity);
            break;
        }
      } catch (err) {
        //console.log("Erro no listener de notificações.");
      }
    });

    return () => {
      unsubscribe();
    };
  }, []);
};


export const useAreasJusticaList = (dataService: IAreasJusticaService, currentFilter?: FilterAreasJustica | undefined | null) => {
  const [data, setData] = useState<IAreasJustica[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null); 
  const currentFilterRef = useRef<FilterAreasJustica | undefined | null>(currentFilter);

  useEffect(() => {
    currentFilterRef.current = currentFilter;
  }, [currentFilter]);

  const fetchData = useCallback(async (filtro?: FilterAreasJustica | undefined | null) => {
   
    setLoading(true);
    setError(null);
    
    try {
      const result = await dataService.getAll(filtro ?? {} as FilterAreasJustica);
      setData(result);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : 'Erro ao carregar areasjustica';
      setError(errorMessage);
      //console.log('Erro ao carregar areasjustica');
    } finally {
      setLoading(false);
    }
  }, [dataService]);   

 const refreshData = useCallback(() => {
     fetchData(currentFilterRef.current);
  }, [fetchData]);
  
  useAreasJusticaNotifications(
    refreshData, // onUpdate
    refreshData, // onDelete  
    refreshData  // onAdd
  );
   

  return {
    data,
    loading,
    error,
    fetchData,
    refreshData
  };
};


export function useValidationsAreasJustica() {

  async function runValidation(data: IAreasJustica, uri?: string, token?: string): Promise<{ isValid: boolean; message: string } | null> {

    const areasjusticaApi = new AreasJusticaApi(uri ?? '', token ?? '');

    const result = await areasjusticaApi.validation(data);

    return result;
  }

  function validate(data: IAreasJustica): { isValid: boolean; message: string } {
    if (!data) return { isValid: false, message: 'Dados não informados.' };
    
      try {
   
        


        return { isValid: true, message: '' };

         } catch (error) {
          return { isValid: true, message: 'Erro ao validar os dados.' };
    }

  }

 return { validate, runValidation };
}