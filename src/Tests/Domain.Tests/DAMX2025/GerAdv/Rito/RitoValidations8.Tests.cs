// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class RitoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFRitoFactory> _mockRitoFactory;
    private readonly Mock<IRitoReader> _mockReader;
    private readonly RitoValidation _validation;
    private readonly Mock<IRitoService> _mockRitoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly RitoService _service;
    private readonly string _validUri = "test-uri";
    public RitoValidationTests()
    {
        _mockRitoFactory = new Mock<IFRitoFactory>();
        _mockReader = new Mock<IRitoReader>();
        _validation = new RitoValidation();
        _mockRitoService = new Mock<IRitoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void RitoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IRitoValidation>();
    }

    [Fact]
    public void RitoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(RitoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void RitoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(RitoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.Rito CreateValidRito()
    {
        return new Models.Rito
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAA",
            Top = false
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockRitoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterRito>(), It.IsAny<string>())).ReturnsAsync(new List<RitoResponseAll>());
    // Setup other mocks but don't override the Ritos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockRitoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterRito>(), It.IsAny<string>())).ReturnsAsync(new List<RitoResponseAll>());
    // Setup other mocks but don't override the Ritos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var rito = new Models.Rito
        {
            Id = 1,
            Descricao = "AAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(rito, _mockRitoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockRitoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}