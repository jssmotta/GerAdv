// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { NEPalavrasChavesService, NEPalavrasChavesValidator } from '@/app/GerAdv_TS/NEPalavrasChaves/Services/NEPalavrasChaves.service';
import { NEPalavrasChavesApi, NEPalavrasChavesApiError } from '@/app/GerAdv_TS/NEPalavrasChaves/Apis/ApiNEPalavrasChaves';
import { INEPalavrasChaves } from '@/app/GerAdv_TS/NEPalavrasChaves/Interfaces/interface.NEPalavrasChaves';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as NEPalavrasChavesApi;

const sample: INEPalavrasChaves = { id: 1, nome: 'Teste' };

describe('NEPalavrasChavesService', () => {
  let service: NEPalavrasChavesService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new NEPalavrasChavesService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(NEPalavrasChavesValidator, 'validateNEPalavrasChaves').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchNEPalavrasChavesById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchNEPalavrasChavesById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchNEPalavrasChavesById lança NEPalavrasChavesApiError para id inválido', async () => {
    await expect(service.fetchNEPalavrasChavesById(0)).rejects.toBeInstanceOf(NEPalavrasChavesApiError);
  });

  it('saveNEPalavrasChaves chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveNEPalavrasChaves(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveNEPalavrasChaves lança NEPalavrasChavesApiError quando validação falha', async () => {
    (NEPalavrasChavesValidator.validateNEPalavrasChaves as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveNEPalavrasChaves(sample)).rejects.toBeInstanceOf(NEPalavrasChavesApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteNEPalavrasChaves chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteNEPalavrasChaves(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteNEPalavrasChaves lança NEPalavrasChavesApiError para id inválido', async () => {
    await expect(service.deleteNEPalavrasChaves(0)).rejects.toBeInstanceOf(NEPalavrasChavesApiError);
  });

  it('validateNEPalavrasChaves delega para NEPalavrasChavesValidator', () => {
    (NEPalavrasChavesValidator.validateNEPalavrasChaves as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateNEPalavrasChaves(sample);
    expect(r.isValid).toBe(true);
  });
});
