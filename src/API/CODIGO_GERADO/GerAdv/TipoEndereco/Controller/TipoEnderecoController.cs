// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:TipoEndereco
// Fonte:ControllerGenerator
namespace MenphisSI.GerAdv.Controller;
[Route("api/v{version:apiVersion}/{uri}/[controller]/[action]")]
[ApiController]
[ApiVersion("1.0")]
public partial class TipoEnderecoController(ITipoEnderecoService tipoenderecoService) : ControllerBase
{
    private readonly ITipoEnderecoService _tipoenderecoService = tipoenderecoService;
    private static readonly Logger _logger = LogManager.GetCurrentClassLogger();
    [HttpGet]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAll([FromQuery] int max, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "GetAll";
        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.GetAll(max, uri);
            });
            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoEnderecoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: GetAll failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Filter([FromQuery] int max, [FromBody] MenphisSI.GerAdv.Filters.FilterTipoEndereco filter, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "Filter";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.Filter(max, filter, uri);
            });
            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoEnderecoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: Filter failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> FilterVoice([FromBody] MenphisSI.GerAdv.Filters.FilterTipoEnderecoWithVoiceRequest request, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "FilterVoice";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.RelatoriosPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.FilterVoice(request.Filter, request.VoiceCommand, uri);
            });
            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: FilterVoice failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetById(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "GetById";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.GetById(id, uri, token);
            });
            if (result == null)
            {
                TipoEnderecoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetById: No TipoEndereco found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoEnderecoMetrics.RecordReadByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: GetById failed with exception for id = {0}, {1}", id, uri);
            throw;
        }
    }

    [HttpGet("{id}")]
    [ProducesResponseType(typeof(AuditorResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetAuditor(int id, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "GetAuditor";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.GetAuditor(id, uri, token);
            });
            if (result == null)
            {
                TipoEnderecoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("GetAuditor: No TipoEndereco found with id = {0}, {1}", id, uri);
                return NotFound();
            }

            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: GetAuditor failed with exception for id = {0}, {1}", id, uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = "Erro 500 Auditor" });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> GetListN([FromQuery] int max, [FromBody] Filters.FilterTipoEndereco? filtro, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "GetListN";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.LeituraPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.GetListN(max, filtro, uri);
            });
            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoEnderecoMetrics.RecordRecordsCount(result?.Count() ?? 0, uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: GetListN failed with exception for uri = {0}", uri);
            throw;
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [ProducesResponseType(typeof(TipoEnderecoResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(TipoEnderecoResponse), StatusCodes.Status201Created)]
    [ProducesResponseType(StatusCodes.Status400BadRequest)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status403Forbidden)]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> AddAndUpdate([FromBody] Models.TipoEndereco regTipoEndereco, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        var isNew = regTipoEndereco.Id == 0;
        var operacao = isNew ? "Create" : "Update";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.AddAndUpdate(regTipoEndereco, uri);
            });
            if (result == null)
            {
                TipoEnderecoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("TipoEndereco: AddAndUpdate failed to add or update TipoEndereco, {0}", uri);
                return BadRequest();
            }

            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            if (isNew)
            {
                TipoEnderecoMetrics.RecordCreated(uri);
                TipoEnderecoMetrics.RecordCreatedByHour(uri);
            }
            else
            {
                TipoEnderecoMetrics.RecordUpdated(uri);
                TipoEnderecoMetrics.RecordUpdatedByHour(uri);
            }

            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: AddAndUpdate failed with exception for uri = {0}", uri);
            return StatusCode(500, new Error500 { success = false, data = "", message = ex.Message });
        }
    }

    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    [HttpDelete]
    [ProducesResponseType(typeof(Error500), StatusCodes.Status500InternalServerError)]
    public async Task<IActionResult> Delete([FromQuery] int id, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "Delete";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.Delete(id, uri);
            });
            if (result == null)
            {
                TipoEnderecoMetrics.RecordNotFound(operacao, uri, stopwatch);
                _logger.Warn("Delete: No TipoEndereco found to delete with id = {0}, {1}", id, uri);
                return NotFound();
            }

            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            TipoEnderecoMetrics.RecordDeleted(uri);
            TipoEnderecoMetrics.RecordDeletedByHour(uri);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordConflict(operacao, uri, stopwatch);
            _logger.Error(ex, "TipoEndereco: Delete failed with exception for id = {0}, {1}", id, uri);
            return Conflict(new Error500 { success = false, data = "", message = "Não é possível excluir o registro porque ele está sendo referenciado/em uso em outra tabela." });
        }
    }

    [HttpPost]
    [EnableRateLimiting("DefaultPolicy")]
    [Authorize]
    public async Task<IActionResult> Validation([FromBody] Models.TipoEndereco regTipoEndereco, [FromRoute, Required] string uri)
    {
        var stopwatch = TipoEnderecoMetrics.StartTimer();
        const string operacao = "Validation";
        if (!ModelState.IsValid)
        {
            TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
            return BadRequest(ModelState);
        }

        try
        {
            var result = await BulkheadPolicies.EscritaPolicy.ExecuteAsync(async () =>
            {
                return await _tipoenderecoService.Validation(regTipoEndereco, uri);
            });
            if (result == null)
            {
                TipoEnderecoMetrics.RecordInvalid(operacao, uri, stopwatch);
                _logger.Warn("TipoEndereco: Validation failed to add or update TipoEndereco, {0}", uri);
                return BadRequest();
            }

            TipoEnderecoMetrics.RecordSuccess(operacao, uri, stopwatch);
            return Ok(result);
        }
        catch (BulkheadRejectedException)
        {
            TipoEnderecoMetrics.RecordBulkheadRejection(operacao, uri, stopwatch);
            _logger.Warn("TipoEndereco: {0} rejeitado por sobrecarga para uri = {1}", operacao, uri);
            return StatusCode(503, new { message = "Serviço temporariamente sobrecarregado. Tente novamente." });
        }
        catch (Exception ex)
        {
            TipoEnderecoMetrics.RecordError(operacao, uri, ex, stopwatch);
            _logger.Error(ex, "TipoEndereco: Validation failed with exception for uri = {0}", uri);
            return StatusCode(500, new { success = false, data = "", message = ex.Message });
        }
    }
}