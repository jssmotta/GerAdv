// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useAlarmSMSForm, useAlarmSMSList, useValidationsAlarmSMS } from '../GerAdv_TS/AlarmSMS/Hooks/hookAlarmSMS';
import { IAlarmSMS } from '../GerAdv_TS/AlarmSMS/Interfaces/interface.AlarmSMS';
import { IAlarmSMSService } from '../GerAdv_TS/AlarmSMS/Services/AlarmSMS.service';
import { AlarmSMSTestEmpty } from '../GerAdv_TS/Models/AlarmSMS';
import { useAlarmSMSComboBox } from '../GerAdv_TS/AlarmSMS/Hooks/hookAlarmSMS';

// Mock do serviço
const mockAlarmSMSService: jest.Mocked<IAlarmSMSService> = {
  fetchAlarmSMSById: jest.fn(),
  saveAlarmSMS: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteAlarmSMS: jest.fn(),
  validateAlarmSMS: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialAlarmSMS: IAlarmSMS = { ...AlarmSMSTestEmpty() };

describe('useAlarmSMSForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    expect(result.current.data).toEqual(initialAlarmSMS);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Alarm S M S',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Alarm S M S');
  });

   test('deve carregar Alarm S M S por ID', async () => {
    const mockAlarmSMS = { ...initialAlarmSMS, id: 1, descricao: 'Alarm S M S Teste' };
    mockAlarmSMSService.fetchAlarmSMSById.mockResolvedValue(mockAlarmSMS);

    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.loadAlarmSMS(1);
    });

    expect(mockAlarmSMSService.fetchAlarmSMSById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockAlarmSMS);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Alarm S M S', async () => {
    const errorMessage = 'Erro ao carregar Alarm S M S';
    mockAlarmSMSService.fetchAlarmSMSById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.loadAlarmSMS(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialAlarmSMS, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialAlarmSMS);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.loadAlarmSMS(0);
    });

    expect(mockAlarmSMSService.fetchAlarmSMSById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialAlarmSMS);
  });
});

describe('useAlarmSMSList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAlarmSMSList(mockAlarmSMSService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialAlarmSMS, id: 1, descricao: 'Alarm S M S 1' },
      { ...initialAlarmSMS, id: 2, descricao: 'Alarm S M S 2' }
    ];
    mockAlarmSMSService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAlarmSMSList(mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockAlarmSMSService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockAlarmSMSService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAlarmSMSList(mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialAlarmSMS, id: 1, descricao: 'Alarm S M S Filtrado' }];
    const filtro = { descricao: 'Alarm S M S' };
    mockAlarmSMSService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAlarmSMSList(mockAlarmSMSService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockAlarmSMSService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsAlarmSMS', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsAlarmSMS());

    const validData = { ...initialAlarmSMS, descricao: 'Alarm S M S Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsAlarmSMS());

    const invalidData = { ...initialAlarmSMS, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  

  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsAlarmSMS());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialAlarmSMS, id: 1, descricao: 'Alarm S M S Teste' }];
    mockAlarmSMSService.getAll.mockResolvedValue(mockData);
    mockAlarmSMSService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useAlarmSMSList(mockAlarmSMSService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsAlarmSMS()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Alarm S M S Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Alarm S M S Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
});  test('deve lidar com checkbox no handleChange', () => {
    const { result } = renderHook(() => 
      useAlarmSMSForm(initialAlarmSMS, mockAlarmSMSService)
    );

    const mockEvent = {
      target: {
        name: 'd1',
        value: '',
        type: 'checkbox',
        checked: true
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.d1).toBe(true);
  });


 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Alarm S M S 1' },
      { id: 2, descricao: 'Alarm S M S 2' }
    ];
    mockAlarmSMSService.getList.mockResolvedValue(mockOptions as IAlarmSMS[]);


    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alarm S M S 1' },
        { id: 2, nome: 'Alarm S M S 2' }
      ]);
    });


    expect(mockAlarmSMSService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Alarm S M S ABC' },
      { id: 2, descricao: 'Alarm S M S XYZ' }
    ];
    mockAlarmSMSService.getList.mockResolvedValue(mockOptions as IAlarmSMS[]);   


 const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alarm S M S ABC' },
        { id: 2, nome: 'Alarm S M S XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Alarm S M S ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Alarm S M S ABC' },
      { id: 2, descricao: 'Alarm S M S XYZ' }
    ];
    mockAlarmSMSService.getList.mockResolvedValue(mockOptions as IAlarmSMS[]);
  


    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alarm S M S ABC' },
        { id: 2, nome: 'Alarm S M S XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Alarm S M S ABC' },
          {id: 2, nome: 'Alarm S M S XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );

    const newValue = { id: 1, descricao: 'Alarm S M S Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Alarm S M S Inicial' };
    
    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useAlarmSMSComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Alarm S M S Inicial' };
    
    const { result } = renderHook(() => 
      useAlarmSMSComboBox(mockAlarmSMSService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






