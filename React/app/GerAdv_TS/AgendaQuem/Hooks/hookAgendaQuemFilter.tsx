// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { useState } from 'react';
import InputInput from '@/app/components/Inputs/InputInput';
import InputDate from '@/app/components/Inputs/InputDate';
import InputHora from '@/app/components/Inputs/InputHora';
import InputValor from '@/app/components/Inputs/InputValor';
import InputComboFilterYesNo from '@/app/components/Inputs/InputComboFilterYesNo';
import { FilterHandlers } from '@/app/components/Cruds/GenericFilterDialog';
import { FilterAgendaQuem } from '@/app/GerAdv_TS/AgendaQuem/Filters/AgendaQuem';
import AdvogadosComboBox from '@/app/GerAdv_TS/Advogados/ComboBox/Advogados';
import FuncionariosComboBox from '@/app/GerAdv_TS/Funcionarios/ComboBox/Funcionarios';
import PrepostosComboBox from '@/app/GerAdv_TS/Prepostos/ComboBox/Prepostos';
interface UseAgendaQuemFilterProps {
  handleFetchWithFilter: (filtro?: FilterAgendaQuem | undefined | null) => Promise<void>;
}
interface UseAgendaQuemFilterReturn {
  // Estados
  showSearch: boolean;
  windowFilter: FilterAgendaQuem;
  setWindowFilter: React.Dispatch<React.SetStateAction<FilterAgendaQuem>>;
  // Handlers do Dialog
  handleSearch: () => void;
  handleCloseSearch: () => void;
  handleConfirmSearch: (filter: FilterAgendaQuem) => Promise<void>;
  // Render function
  renderInputFilters: (handlers: FilterHandlers<FilterAgendaQuem>) => React.ReactNode;
  // Utilitários
  clearFilter: () => void;
  hasActiveFilter: boolean;
}
export const useAgendaQuemFilter = ({ handleFetchWithFilter }: UseAgendaQuemFilterProps): UseAgendaQuemFilterReturn => {
  const [showSearch, setShowSearch] = useState(false);
  const [windowFilter, setWindowFilter] = useState<FilterAgendaQuem>({} as FilterAgendaQuem);
  // Handlers do Dialog
  const handleSearch = () => {
    setShowSearch(true);
    const filterWildechar = {...windowFilter, wildcardChar: '%' } as FilterAgendaQuem;
    setWindowFilter(filterWildechar);
  };
  const handleCloseSearch = () => {
    setShowSearch(false);
  };
  const handleConfirmSearch = async (filter: FilterAgendaQuem) => {
    await handleFetchWithFilter(filter);
  };
  // Função para limpar filtros
  const clearFilter = () => {
    setWindowFilter({});
    sessionStorage.removeItem(btoa('AgendaQuemFilter'));
    handleFetchWithFilter({});
  };
  // Verificar se há filtros ativos
  const hasActiveFilter = Object.values(windowFilter).some(value =>
    value !== undefined && value !== null && value !== '' && value !== -2147483648
  );
  // Função para renderizar os campos de filtro
  const renderInputFilters = (handlers: FilterHandlers<FilterAgendaQuem>) => (
  <>
  <InputInput
  type='text'
  id='idagenda'
  name='idagenda'
  value={handlers.windowFilter?.idagenda ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe IDAgenda'
  label='IDAgenda (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='idagenda_end'
  name='idagenda_end'
  value={handlers.windowFilter?.idagenda_end ?? ''}
  onChange={handlers.handleInputChange}
  placeholder='Informe IDAgenda final'
  label='IDAgenda final'
  disabled={handlers.windowFilter?.idagenda ? false: true}
  className='inputIncNome inputSearch'
  />
  <AdvogadosComboBox
  name='advogado'
  dataForm={null}
  value={handlers.windowFilter?.advogado}
  setValue={(e:any) => handlers.handleComboChange(e, 'advogado')}
  className='inputSearch inputSearchComboboxTab'
  label='Advogados'
  />
  <FuncionariosComboBox
  name='funcionario'
  dataForm={null}
  value={handlers.windowFilter?.funcionario}
  setValue={(e:any) => handlers.handleComboChange(e, 'funcionario')}
  className='inputSearch inputSearchComboboxTab'
  label='Colaborador'
  />
  <PrepostosComboBox
  name='preposto'
  dataForm={null}
  value={handlers.windowFilter?.preposto}
  setValue={(e:any) => handlers.handleComboChange(e, 'preposto')}
  className='inputSearch inputSearchComboboxTab'
  label='Prepostos'
  />
  <InputInput
  type='text'
  id='codigo_filtro'
  name='codigo_filtro'
  value={handlers.windowFilter?.codigo_filtro ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código do cadastro'
  label='Código (igual ou inicial)'
  className='inputIncNome inputSearch'
  />
  <InputInput
  type='text'
  id='codigo_filtro_end'
  name='codigo_filtro_end'
  value={handlers.windowFilter?.codigo_filtro_end ?? ''}
  onChange={handlers.handleInputChange}
  dataForm={handlers.windowFilter}
  placeholder='Código final do cadastro'
  label='Código final'
  disabled={handlers.windowFilter?.codigo_filtro ? false: true}
  className='inputIncNome inputSearch'
  />
</>
);
return {
  // Estados
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  // Handlers
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  // Render function
  renderInputFilters, 
  // Utilitários
  clearFilter, 
  hasActiveFilter
};
};