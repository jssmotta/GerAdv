// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class ProcessOutputSourcesValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFProcessOutputSourcesFactory> _mockProcessOutputSourcesFactory;
    private readonly Mock<IProcessOutputSourcesReader> _mockReader;
    private readonly ProcessOutputSourcesValidation _validation;
    private readonly Mock<IProcessOutputSourcesService> _mockProcessOutputSourcesService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly ProcessOutputSourcesService _service;
    private readonly string _validUri = "test-uri";
    public ProcessOutputSourcesValidationTests()
    {
        _mockProcessOutputSourcesFactory = new Mock<IFProcessOutputSourcesFactory>();
        _mockReader = new Mock<IProcessOutputSourcesReader>();
        _validation = new ProcessOutputSourcesValidation();
        _mockProcessOutputSourcesService = new Mock<IProcessOutputSourcesService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void ProcessOutputSourcesValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<IProcessOutputSourcesValidation>();
    }

    [Fact]
    public void ProcessOutputSourcesValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(ProcessOutputSourcesValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void ProcessOutputSourcesValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(ProcessOutputSourcesValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.ProcessOutputSources CreateValidProcessOutputSources()
    {
        return new Models.ProcessOutputSources
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockProcessOutputSourcesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterProcessOutputSources>(), It.IsAny<string>())).ReturnsAsync(new List<ProcessOutputSourcesResponseAll>());
    // Setup other mocks but don't override the ProcessOutputSourcess service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockProcessOutputSourcesService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterProcessOutputSources>(), It.IsAny<string>())).ReturnsAsync(new List<ProcessOutputSourcesResponseAll>());
    // Setup other mocks but don't override the ProcessOutputSourcess service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var processoutputsources = new Models.ProcessOutputSources
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(processoutputsources, _mockProcessOutputSourcesService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockProcessOutputSourcesService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}