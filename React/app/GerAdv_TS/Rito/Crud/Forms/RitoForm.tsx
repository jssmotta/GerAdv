// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// Tracking 21-08-25: Forms.tsx.txt
"use client";
import { IRito } from "@/app/GerAdv_TS/Rito/Interfaces/interface.Rito";
import { useRouter } from "next/navigation";
import React, { useEffect, useState, useRef } from "react";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { getParamFromUrl } from "@/app/tools/helpers";

import SaveButtonCrud from "@/app/components/Cruds/SaveButtonCrud";
import { useIsMobile } from "@/app/context/MobileContext";
import { RitoApi } from "../../Apis/RitoApi";
import { useValidationsRito } from "../../Hooks/hookRito";
import HelpButton from "@/app/components/Cruds/DockerInc/HelpButton";
import DeleteButton from "@/app/components/Cruds/DockerInc/DeleteButton";
import AuditorButton from "@/app/components/Cruds/DockerInc/AuditorButton";
import PrintButton from "@/app/components/Cruds/DockerInc/PrintButton";
import CopyButton from "@/app/components/Cruds/DockerInc/CopyButton";
import FooterIncDocker from "@/app/components/Cruds/DockerInc/FooterIncDocker";
import CrudButton from "@/app/components/Cruds/DockerInc/CrudButton";
import HelpRito from "../../Help/HelpRito";
import RitoPage from "@/app/pages/rito/page";

import InputDescription from "@/app/components/Inputs/InputDescription";
import InputCheckbox from "@/app/components/Inputs/InputCheckbox";

interface RitoFormProps {
  ritoData: IRito;
  onChange: (e: any) => void;
  onSubmit: (e: React.FormEvent) => void;
  onClose: () => void;
  onError?: () => void;
  onReload?: () => void;
  onSuccess?: (registro?: any) => void;
}

export const RitoForm: React.FC<RitoFormProps> = ({
  ritoData,
  onChange,
  onSubmit,
  onClose,
  onError,
  onReload,
  onSuccess,
}) => {
  const router = useRouter();
  const systemContext = useAppSelector(selectSystemContext);
  const isMobile = useIsMobile();
  const dadoApi = new RitoApi(
    systemContext?.TenantApp ?? "",
    systemContext?.Token ?? "",
  );
  const [isSubmitting, setIsSubmitting] = useState(false);
  const initialized = useRef(false);
  const validationForm = useValidationsRito();

  const onConfirm = (e: React.FormEvent) => {
    e.preventDefault();
    if (e.stopPropagation) e.stopPropagation();

    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        onSubmit(e);
      } catch (error) {
        console.log("Erro ao submeter formulário de Rito:");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  const handleCancel = () => {
    if (onReload) {
      onReload();
    } else {
      onClose();
    }
  };

  const handleDirectSave = () => {
    if (!isSubmitting) {
      setIsSubmitting(true);

      try {
        const syntheticEvent = {
          preventDefault: () => {},
          target: document.getElementById(`RitoForm-${ritoData.id}`),
        } as unknown as React.FormEvent;

        onSubmit(syntheticEvent);
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.log("Erro ao salvar Rito diretamente");
        setIsSubmitting(false);
        if (onError) onError();
      }
    }
  };

  useEffect(() => {
    const el = document.querySelector(".nameFormMobile");
    if (el) {
      el.textContent = ritoData?.id == 0 ? "Editar Rito" : "Adicionar Rito";
    }
  }, [ritoData.id]);

  return (
    <>
      <div
        className={
          isMobile
            ? "form-container form-container-Rito"
            : "form-container5 form-container-Rito"
        }
      >
        <form
          className="formInputCadInc"
          id={`RitoForm-${ritoData.id}`}
          onSubmit={onConfirm}
        >
          <SaveButtonCrud
            isMobile={isMobile}
            validationForm={validationForm}
            entity="Rito"
            data={ritoData}
            isSubmitting={isSubmitting}
            setIsSubmitting={setIsSubmitting}
            onClose={onClose}
            formId={`RitoForm-${ritoData.id}`}
            preventPropagation={true}
            onSave={handleDirectSave}
            onCancel={handleCancel}
          />

          <div className="grid-container">
            <InputDescription
              tabIndex={0}
              type="text"
              id="descricao"
              label="Rito"
              dataForm={ritoData}
              className="input-default-main"
              name="descricao"
              value={ritoData.descricao}
              placeholder={`Digite nome/descrição do rito`}
              onChange={onChange}
              required
            />

            <InputCheckbox
              dataForm={ritoData}
              label="Top"
              name="top"
              checked={ritoData.top}
              onChange={onChange}
            />
          </div>
        </form>

        <FooterIncDocker>
          <CrudButton title="Rito" windowCrud={RitoPage} />

          <CopyButton formId={`RitoForm-${ritoData.id}`} />

          <PrintButton formId={`RitoForm-${ritoData.id}`} />

          <AuditorButton tableName="Rito" coolName="Rito" id={ritoData.id} />

          <DeleteButton
            page={"/pages/rito"}
            id={ritoData.id}
            dadoApi={dadoApi}
          />

          <HelpButton
            title="Rito"
            page={(onClose) => <HelpRito onClose={onClose} />}
          />
        </FooterIncDocker>
      </div>
      <div className="form-spacer"></div>

      {isMobile && (
        <SaveButtonCrud
          bottomMobile={true}
          isMobile={true}
          validationForm={validationForm}
          entity="Rito"
          data={ritoData}
          isSubmitting={isSubmitting}
          setIsSubmitting={setIsSubmitting}
          onClose={onClose}
          formId={`RitoForm-${ritoData.id}`}
          preventPropagation={true}
          onSave={handleDirectSave}
          onCancel={handleCancel}
        />
      )}
    </>
  );
};
