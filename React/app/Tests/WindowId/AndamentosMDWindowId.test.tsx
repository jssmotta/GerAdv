// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AndamentosMDWindowId from '@/app/GerAdv_TS/AndamentosMD/Crud/Grids/AndamentosMDWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AndamentosMDWindowId from '@/app/GerAdv_TS/AndamentosMD/Crud/Grids/AndamentosMDWindowId';
import { AndamentosMDEmpty } from '@/app/GerAdv_TS/Models/AndamentosMD';

describe('AndamentosMDWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AndamentosMDWindowId).toBeDefined();
    expect(typeof AndamentosMDWindowId === 'function' || typeof AndamentosMDWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AndamentosMDService class so constructor usage doesn't throw and we can control fetchAndamentosMDById
const mockFetchAndamentosMDById = jest.fn();
jest.mock('@/app/GerAdv_TS/AndamentosMD/Services/AndamentosMD.service', () => ({
  AndamentosMDService: jest.fn().mockImplementation(() => ({
    fetchAndamentosMDById: (...args: any[]) => mockFetchAndamentosMDById(...args),
  })),
}));

// Mock the child AndamentosMDWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/AndamentosMD/Crud/Grids/AndamentosMDWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='andamentosmd-window' {...p} />,
}));

describe('AndamentosMDWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AndamentosMDWindowId).toBeDefined();
    expect(typeof AndamentosMDWindowId === 'function' || typeof AndamentosMDWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AndamentosMDWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AndamentosMDWindow with AndamentosMDEmpty when id is 0', async () => {
    render(
      <AndamentosMDWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AndamentosMDWindow should be rendered with selectedAndamentosMD equal to AndamentosMDEmpty()
    const node = await screen.findByTestId('andamentosmd-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedAndamentosMD"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedandamentosmd') || node.getAttribute('selectedAndamentosMD')).not.toBeNull();
  });

  it('fetches andamentosmd by id and passes it to AndamentosMDWindow when id > 0', async () => {
    const fakeAndamentosMD = { id: 123, nomecliente: 'Test' };
    mockFetchAndamentosMDById.mockResolvedValue(fakeAndamentosMD);

    render(
      <AndamentosMDWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('andamentosmd-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAndamentosMDById).toHaveBeenCalledWith(123));
  });
});
