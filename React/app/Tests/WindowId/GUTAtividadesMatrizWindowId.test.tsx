// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import GUTAtividadesMatrizWindowId from '@/app/GerAdv_TS/GUTAtividadesMatriz/Crud/Grids/GUTAtividadesMatrizWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import GUTAtividadesMatrizWindowId from '@/app/GerAdv_TS/GUTAtividadesMatriz/Crud/Grids/GUTAtividadesMatrizWindowId';
import { GUTAtividadesMatrizEmpty } from '@/app/GerAdv_TS/Models/GUTAtividadesMatriz';

describe('GUTAtividadesMatrizWindowId (basic)', () => {
  it('exports the component', () => {
    expect(GUTAtividadesMatrizWindowId).toBeDefined();
    expect(typeof GUTAtividadesMatrizWindowId === 'function' || typeof GUTAtividadesMatrizWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the GUTAtividadesMatrizService class so constructor usage doesn't throw and we can control fetchGUTAtividadesMatrizById
const mockFetchGUTAtividadesMatrizById = jest.fn();
jest.mock('@/app/GerAdv_TS/GUTAtividadesMatriz/Services/GUTAtividadesMatriz.service', () => ({
  GUTAtividadesMatrizService: jest.fn().mockImplementation(() => ({
    fetchGUTAtividadesMatrizById: (...args: any[]) => mockFetchGUTAtividadesMatrizById(...args),
  })),
}));

// Mock the child GUTAtividadesMatrizWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/GUTAtividadesMatriz/Crud/Grids/GUTAtividadesMatrizWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='gutatividadesmatriz-window' {...p} />,
}));

describe('GUTAtividadesMatrizWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(GUTAtividadesMatrizWindowId).toBeDefined();
    expect(typeof GUTAtividadesMatrizWindowId === 'function' || typeof GUTAtividadesMatrizWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <GUTAtividadesMatrizWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders GUTAtividadesMatrizWindow with GUTAtividadesMatrizEmpty when id is 0', async () => {
    render(
      <GUTAtividadesMatrizWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // GUTAtividadesMatrizWindow should be rendered with selectedGUTAtividadesMatriz equal to GUTAtividadesMatrizEmpty()
    const node = await screen.findByTestId('gutatividadesmatriz-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedGUTAtividadesMatriz"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedgutatividadesmatriz') || node.getAttribute('selectedGUTAtividadesMatriz')).not.toBeNull();
  });

  it('fetches gutatividadesmatriz by id and passes it to GUTAtividadesMatrizWindow when id > 0', async () => {
    const fakeGUTAtividadesMatriz = { id: 123, nomecliente: 'Test' };
    mockFetchGUTAtividadesMatrizById.mockResolvedValue(fakeGUTAtividadesMatriz);

    render(
      <GUTAtividadesMatrizWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('gutatividadesmatriz-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchGUTAtividadesMatrizById).toHaveBeenCalledWith(123));
  });
});
