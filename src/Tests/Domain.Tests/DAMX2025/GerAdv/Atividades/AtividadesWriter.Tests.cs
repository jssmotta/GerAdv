// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class AtividadesWriterTests
{
    private readonly Mock<IFAtividadesFactory> _mockAtividadesFactory;
    private readonly Mock<FAtividades> _mockFAtividades;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly AtividadesWriter _atividadesWriter;
    public AtividadesWriterTests()
    {
        _mockAtividadesFactory = new Mock<IFAtividadesFactory>();
        _mockFAtividades = new Mock<FAtividades>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _atividadesWriter = new AtividadesWriter(_mockAtividadesFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var atividades = CreateValidAtividadesModel();
        atividades.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockAtividadesFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAtividades.Object);
        _mockFAtividades.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _atividadesWriter.WriteAsync(atividades, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAtividades.Object);
        _mockAtividadesFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockAtividadesFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFAtividades.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var atividades = CreateValidAtividadesModel();
        atividades.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockAtividadesFactory.Setup(x => x.CreateFromIdAsync(atividades.Id, _mockConnection.Object)).ReturnsAsync(_mockFAtividades.Object);
        _mockFAtividades.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _atividadesWriter.WriteAsync(atividades, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAtividades.Object);
        _mockAtividadesFactory.Verify(x => x.CreateFromIdAsync(atividades.Id, _mockConnection.Object), Times.Once);
        _mockAtividadesFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFAtividades.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var atividades = CreateValidAtividadesModel();
        var auditorQuem = 99;
        _mockAtividadesFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAtividades.Object);
        _mockFAtividades.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _atividadesWriter.WriteAsync(atividades, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFAtividades.Object);
        _mockFAtividades.VerifySet(x => x.FDescricao = atividades.Descricao, Times.Once);
        _mockFAtividades.VerifySet(x => x.FGUID = atividades.GUID, Times.Once);
        _mockFAtividades.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var atividades = CreateValidAtividadesModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAtividadesFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _atividadesWriter.WriteAsync(atividades, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var atividades = CreateValidAtividadesModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockAtividadesFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFAtividades.Object);
        _mockFAtividades.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _atividadesWriter.WriteAsync(atividades, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var atividadesResponse = new AtividadesResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockAtividadesFactory.Setup(x => x.DeleteAsync(operadorId, atividadesResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _atividadesWriter.Delete(atividadesResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockAtividadesFactory.Verify(x => x.DeleteAsync(operadorId, atividadesResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var atividadesResponse = new AtividadesResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockAtividadesFactory.Setup(x => x.DeleteAsync(operadorId, atividadesResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _atividadesWriter.Delete(atividadesResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var atividadesResponse = new AtividadesResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockAtividadesFactory.Setup(x => x.DeleteAsync(operadorId, atividadesResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _atividadesWriter.Delete(atividadesResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new AtividadesWriter(_mockAtividadesFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new AtividadesWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.Atividades CreateValidAtividadesModel()
    {
        return new Models.Atividades
        {
            Id = 0,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
            GUID = Guid.NewGuid().ToString()
        };
    }
#endregion
}