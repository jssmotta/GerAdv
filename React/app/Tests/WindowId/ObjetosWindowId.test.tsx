// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import ObjetosWindowId from '@/app/GerAdv_TS/Objetos/Crud/Grids/ObjetosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import ObjetosWindowId from '@/app/GerAdv_TS/Objetos/Crud/Grids/ObjetosWindowId';
import { ObjetosEmpty } from '@/app/GerAdv_TS/Models/Objetos';

describe('ObjetosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(ObjetosWindowId).toBeDefined();
    expect(typeof ObjetosWindowId === 'function' || typeof ObjetosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the ObjetosService class so constructor usage doesn't throw and we can control fetchObjetosById
const mockFetchObjetosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Objetos/Services/Objetos.service', () => ({
  ObjetosService: jest.fn().mockImplementation(() => ({
    fetchObjetosById: (...args: any[]) => mockFetchObjetosById(...args),
  })),
}));

// Mock the child ObjetosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Objetos/Crud/Grids/ObjetosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='objetos-window' {...p} />,
}));

describe('ObjetosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(ObjetosWindowId).toBeDefined();
    expect(typeof ObjetosWindowId === 'function' || typeof ObjetosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <ObjetosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders ObjetosWindow with ObjetosEmpty when id is 0', async () => {
    render(
      <ObjetosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // ObjetosWindow should be rendered with selectedObjetos equal to ObjetosEmpty()
    const node = await screen.findByTestId('objetos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedObjetos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedobjetos') || node.getAttribute('selectedObjetos')).not.toBeNull();
  });

  it('fetches objetos by id and passes it to ObjetosWindow when id > 0', async () => {
    const fakeObjetos = { id: 123, nomecliente: 'Test' };
    mockFetchObjetosById.mockResolvedValue(fakeObjetos);

    render(
      <ObjetosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('objetos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchObjetosById).toHaveBeenCalledWith(123));
  });
});
