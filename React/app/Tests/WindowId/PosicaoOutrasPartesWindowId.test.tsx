// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PosicaoOutrasPartesWindowId from '@/app/GerAdv_TS/PosicaoOutrasPartes/Crud/Grids/PosicaoOutrasPartesWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PosicaoOutrasPartesWindowId from '@/app/GerAdv_TS/PosicaoOutrasPartes/Crud/Grids/PosicaoOutrasPartesWindowId';
import { PosicaoOutrasPartesEmpty } from '@/app/GerAdv_TS/Models/PosicaoOutrasPartes';

describe('PosicaoOutrasPartesWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PosicaoOutrasPartesWindowId).toBeDefined();
    expect(typeof PosicaoOutrasPartesWindowId === 'function' || typeof PosicaoOutrasPartesWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PosicaoOutrasPartesService class so constructor usage doesn't throw and we can control fetchPosicaoOutrasPartesById
const mockFetchPosicaoOutrasPartesById = jest.fn();
jest.mock('@/app/GerAdv_TS/PosicaoOutrasPartes/Services/PosicaoOutrasPartes.service', () => ({
  PosicaoOutrasPartesService: jest.fn().mockImplementation(() => ({
    fetchPosicaoOutrasPartesById: (...args: any[]) => mockFetchPosicaoOutrasPartesById(...args),
  })),
}));

// Mock the child PosicaoOutrasPartesWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/PosicaoOutrasPartes/Crud/Grids/PosicaoOutrasPartesWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='posicaooutraspartes-window' {...p} />,
}));

describe('PosicaoOutrasPartesWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PosicaoOutrasPartesWindowId).toBeDefined();
    expect(typeof PosicaoOutrasPartesWindowId === 'function' || typeof PosicaoOutrasPartesWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PosicaoOutrasPartesWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PosicaoOutrasPartesWindow with PosicaoOutrasPartesEmpty when id is 0', async () => {
    render(
      <PosicaoOutrasPartesWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PosicaoOutrasPartesWindow should be rendered with selectedPosicaoOutrasPartes equal to PosicaoOutrasPartesEmpty()
    const node = await screen.findByTestId('posicaooutraspartes-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPosicaoOutrasPartes"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedposicaooutraspartes') || node.getAttribute('selectedPosicaoOutrasPartes')).not.toBeNull();
  });

  it('fetches posicaooutraspartes by id and passes it to PosicaoOutrasPartesWindow when id > 0', async () => {
    const fakePosicaoOutrasPartes = { id: 123, nomecliente: 'Test' };
    mockFetchPosicaoOutrasPartesById.mockResolvedValue(fakePosicaoOutrasPartes);

    render(
      <PosicaoOutrasPartesWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('posicaooutraspartes-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPosicaoOutrasPartesById).toHaveBeenCalledWith(123));
  });
});
