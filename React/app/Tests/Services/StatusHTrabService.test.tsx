// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
import { StatusHTrabService, StatusHTrabValidator } from '@/app/GerAdv_TS/StatusHTrab/Services/StatusHTrab.service';
import { StatusHTrabApi, StatusHTrabApiError } from '@/app/GerAdv_TS/StatusHTrab/Apis/ApiStatusHTrab';
import { IStatusHTrab } from '@/app/GerAdv_TS/StatusHTrab/Interfaces/interface.StatusHTrab';

// Mocks para a API
const mockApi = {
  getById: jest.fn(),
  addAndUpdate: jest.fn(), 
  filterPreload: jest.fn(),
  filter: jest.fn(),
  delete: jest.fn(),
} as unknown as StatusHTrabApi;

const sample: IStatusHTrab = { id: 1, nome: 'Teste' };

describe('StatusHTrabService', () => {
  let service: StatusHTrabService;

  beforeEach(() => {
    jest.clearAllMocks();
    service = new StatusHTrabService(mockApi);
    // força validator padrão (vazio -> válido)
    jest.spyOn(StatusHTrabValidator, 'validateStatusHTrab').mockReturnValue({ isValid: true, errors: [] });
  });

  it('fetchStatusHTrabById retorna dados quando id válido', async () => {
    (mockApi.getById as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.fetchStatusHTrabById(1);
    expect(res).toEqual(sample);
    expect(mockApi.getById).toHaveBeenCalledWith(1);
  });

  it('fetchStatusHTrabById lança StatusHTrabApiError para id inválido', async () => {
    await expect(service.fetchStatusHTrabById(0)).rejects.toBeInstanceOf(StatusHTrabApiError);
  });

  it('saveStatusHTrab chama addAndUpdate e retorna registro salvo', async () => {
    (mockApi.addAndUpdate as jest.Mock).mockResolvedValue({ data: sample });
    const res = await service.saveStatusHTrab(sample);
    expect(res).toEqual(sample);
    expect(mockApi.addAndUpdate).toHaveBeenCalledWith(sample);
  });

  it('saveStatusHTrab lança StatusHTrabApiError quando validação falha', async () => {
    (StatusHTrabValidator.validateStatusHTrab as jest.Mock).mockReturnValue({ isValid: false, errors: ['nome'] });
    await expect(service.saveStatusHTrab(sample)).rejects.toBeInstanceOf(StatusHTrabApiError);
  });  

  it('getAll com onOnlineData usa preload e dispara callback', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });

    const onOnline = jest.fn();
    const res = await service.getAll(undefined, onOnline);
    expect(res).toEqual([sample]);
    // permitir que a promise interna resolva
    await Promise.resolve();
    expect(onOnline).toHaveBeenCalledWith([sample]);
  });

  it('getAll sem callback retorna dados online quando disponíveis', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [] });
    (mockApi.filter as jest.Mock).mockResolvedValue({ data: [sample] });
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('getAll sem callback retorna offline quando online falha', async () => {
    (mockApi.filterPreload as jest.Mock).mockResolvedValue({ data: [sample] });
    (mockApi.filter as jest.Mock).mockRejectedValue(new Error('fail'));
    const res = await service.getAll();
    expect(res).toEqual([sample]);
  });

  it('deleteStatusHTrab chama api.delete quando id válido', async () => {
    (mockApi.delete as jest.Mock).mockResolvedValue({});
    await service.deleteStatusHTrab(1);
    expect(mockApi.delete).toHaveBeenCalledWith(1);
  });

  it('deleteStatusHTrab lança StatusHTrabApiError para id inválido', async () => {
    await expect(service.deleteStatusHTrab(0)).rejects.toBeInstanceOf(StatusHTrabApiError);
  });

  it('validateStatusHTrab delega para StatusHTrabValidator', () => {
    (StatusHTrabValidator.validateStatusHTrab as jest.Mock).mockReturnValue({ isValid: true, errors: [] });
    const r = service.validateStatusHTrab(sample);
    expect(r.isValid).toBe(true);
  });
});
