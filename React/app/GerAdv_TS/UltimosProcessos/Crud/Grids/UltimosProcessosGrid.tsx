// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    //CrudGrid.tsx.txt
'use client';
import { useCallback, useEffect, useMemo, useState } from 'react';
import { AppGridToolbar } from '@/app/components/Cruds/GridToolbar';
import { useIsMobile } from '@/app/context/MobileContext';
import { useSystemContext } from '@/app/context/SystemContext';
import { UltimosProcessosEmpty } from '../../../Models/UltimosProcessos';
import { useWindow } from '@/app/hooks/useWindows';
import { useRouter } from 'next/navigation';
import { IUltimosProcessos } from '../../Interfaces/interface.UltimosProcessos';
import { UltimosProcessosService } from '../../Services/UltimosProcessos.service';
import { UltimosProcessosApi } from '../../Apis/ApiUltimosProcessos';
import { UltimosProcessosGridMobileComponent } from '../GridsMobile/UltimosProcessos';
import { UltimosProcessosGridDesktopComponent } from '../GridsDesktop/UltimosProcessos';
import { getParamFromUrl } from '@/app/tools/helpers';
import { FilterUltimosProcessos } from '../../Filters/UltimosProcessos';
import { ConfirmationModal } from '@/app/components/Cruds/ConfirmationModal';
import UltimosProcessosWindow from './UltimosProcessosWindow';
import ErrorMessage from '@/app/components/Cruds/ErrorMessage';
import { useUltimosProcessosList } from '../../Hooks/hookUltimosProcessos';
import { LoadingSpinner } from '@/app/components/Cruds/LoadingSpinner';
import { NotifySystemActions, subscribeToNotifications } from '@/app/tools/NotifySystem';
import { useUltimosProcessosFilter } from '../../Hooks/hookUltimosProcessosFilter';
import GenericFilterDialog from '@/app/components/Cruds/GenericFilterDialog';
const UltimosProcessosGrid: React.FC = () => {
  const { systemContext } = useSystemContext();
  const isMobile = useIsMobile();
  const dimensions = useWindow();
  // Estados locais para UI
  const [showInc, setShowInc] = useState(false);
  const [selectedUltimosProcessos, setSelectedUltimosProcessos] = useState<IUltimosProcessos>(UltimosProcessosEmpty());
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [currFilter, setCurrFilter] = useState<FilterUltimosProcessos | undefined | null>(null);
  const [isInitialized, setIsInitialized] = useState(false);
  const [ultimosprocessosData, setUltimosProcessosData] = useState<IUltimosProcessos[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Service
  const ultimosprocessosService = useMemo(() => {
    return new UltimosProcessosService(
    new UltimosProcessosApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
  );
}, [systemContext?.Uri, systemContext?.Token]);
// Hook customizado para lista
const {
  data: ultimosprocessos, 
  fetchData, 
  refreshData
} = useUltimosProcessosList(ultimosprocessosService, currFilter);

const handleFetchWithFilter = useCallback(async (filtro?: FilterUltimosProcessos | undefined | null) => {
  setCurrFilter(filtro);
  if (filtro && Object.keys(filtro).length > 0) {
    sessionStorage.setItem(btoa('UltimosProcessosFilter'), btoa(JSON.stringify(filtro)));
  }


  setLoading(true);
  setError(null);
  // Primeiro mostra offline, depois atualiza com online quando chegar
  const offlineData = await ultimosprocessosService.getAll(
  filtro ?? {} as FilterUltimosProcessos, 
  onlineData => {
    setUltimosProcessosData(onlineData);
    setLoading(false);
  }
);
setUltimosProcessosData(offlineData);
setLoading(false);

}, []);
const loadFilter = useCallback(() => {
  if (isInitialized) return; // Evita múltiplas chamadas
    const filtro = {} as FilterUltimosProcessos;

    const filtroMem = sessionStorage.getItem(btoa('UltimosProcessosFilter')) ? JSON.parse(atob(sessionStorage.getItem(btoa('UltimosProcessosFilter')) ?? '') || '{}') : {} as FilterUltimosProcessos;
    setWindowFilter(filtroMem);
    handleFetchWithFilter(filtroMem);

    setIsInitialized(true);
  }, [isInitialized, handleFetchWithFilter]);
  // Handlers para o grid
  const handleRowClick = (ultimosprocessos: IUltimosProcessos) => {
    setSelectedUltimosProcessos(ultimosprocessos);
    setShowInc(true);
  };
  useEffect(() => {
    if (!isInitialized) {
      loadFilter();
    }
  }, [isInitialized, loadFilter]);
  const handleAdd = () => {
    setSelectedUltimosProcessos(UltimosProcessosEmpty());
    setShowInc(true);
  };
  const handleClose = () => {
    setShowInc(false);
  };
  const handleSuccess = () => {
    setShowInc(false);
    // O hook já escuta as notificações e recarrega automaticamente
    refreshData(); // Força o reload do grid após salvar
  };
  const handleError = () => {
    setShowInc(false);
  };
  // Handlers para exclusão
  const onDeleteClick = (e: any) => {
    const ultimosprocessos = e.dataItem;
    setDeleteId(ultimosprocessos.id);
    setIsModalOpen(true);
  };
  const confirmDelete = async () => {
    if (deleteId !== null) {
      try {
        await ultimosprocessosService.deleteUltimosProcessos(deleteId);
        // O hook já escuta as notificações e recarrega automaticamente
      } catch (error) {
      console.log('Erro ao excluir');
      setErrorMessage('Erro ao excluir o registro. Verifique se ele não está vinculado a outros registros.');
    } finally {
    setDeleteId(null);
    setIsModalOpen(false);
  }
}
};
const cancelDelete = () => {
  setDeleteId(null);
  setIsModalOpen(false);
};
// Combinar erro do hook com erro local
const displayError = error || errorMessage;
useEffect(() => {
  const unsubscribe = subscribeToNotifications('*', (entity) => {
    if (entity.action == NotifySystemActions.ERROR) {
      return;
    }
    handleFetchWithFilter(currFilter);
  });
  return () => {
    unsubscribe();
  };
}, [currFilter]);
const {
  showSearch, 
  windowFilter, 
  setWindowFilter, 
  handleSearch, 
  handleCloseSearch, 
  handleConfirmSearch, 
  renderInputFilters, 
  clearFilter, 
  hasActiveFilter
} = useUltimosProcessosFilter({ handleFetchWithFilter });
  useEffect(() => {
    if (currFilter && Object.keys(currFilter).length > 0) {
      setWindowFilter(currFilter);
    }
  }, [currFilter, setWindowFilter]);
  return (
  <>
  <AppGridToolbar onAdd={handleAdd}
    onSearch={handleSearch}
    onClearFilter={hasActiveFilter ? clearFilter: undefined}
    hasActiveFilter={hasActiveFilter} />
    {loading && (
      <LoadingSpinner />
      )}
      {isMobile ? (
        <UltimosProcessosGridMobileComponent
        data={ultimosprocessosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        ) : (
        <UltimosProcessosGridDesktopComponent
        data={ultimosprocessosData}
        onRowClick={handleRowClick}
        onDeleteClick={onDeleteClick}
        setSelectedId={setSelectedId}

        />
        )}

        <UltimosProcessosWindow
        isOpen={showInc}
        onClose={handleClose}
        dimensions={dimensions}
        onSuccess={handleSuccess}
        onError={handleError}
        selectedUltimosProcessos={selectedUltimosProcessos}
        />

        <ConfirmationModal
        isOpen={isModalOpen}
        onConfirm={confirmDelete}
        onCancel={cancelDelete}
        message={`Deseja realmente excluir o registro?`}
        />
        <ErrorMessage
        mensagem={displayError}
        setErrorMessage={setErrorMessage}
        />
        <GenericFilterDialog
        isOpen={showSearch}
        onClose={handleCloseSearch}
        onConfirm={handleConfirmSearch}
        title='Filtrar Ultimos Processos'
        windowFilter={windowFilter}
        setWindowFilter={setWindowFilter}
        renderInputFilters={renderInputFilters}
        minWidth={480}
        confirmText='Buscar'
        cancelText='Cancelar'
        />
      </>
    );
  };
  export default UltimosProcessosGrid;