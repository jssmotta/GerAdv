// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import EnderecosWindowId from '@/app/GerAdv_TS/Enderecos/Crud/Grids/EnderecosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import EnderecosWindowId from '@/app/GerAdv_TS/Enderecos/Crud/Grids/EnderecosWindowId';
import { EnderecosEmpty } from '@/app/GerAdv_TS/Models/Enderecos';

describe('EnderecosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(EnderecosWindowId).toBeDefined();
    expect(typeof EnderecosWindowId === 'function' || typeof EnderecosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the EnderecosService class so constructor usage doesn't throw and we can control fetchEnderecosById
const mockFetchEnderecosById = jest.fn();
jest.mock('@/app/GerAdv_TS/Enderecos/Services/Enderecos.service', () => ({
  EnderecosService: jest.fn().mockImplementation(() => ({
    fetchEnderecosById: (...args: any[]) => mockFetchEnderecosById(...args),
  })),
}));

// Mock the child EnderecosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Enderecos/Crud/Grids/EnderecosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='enderecos-window' {...p} />,
}));

describe('EnderecosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(EnderecosWindowId).toBeDefined();
    expect(typeof EnderecosWindowId === 'function' || typeof EnderecosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <EnderecosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders EnderecosWindow with EnderecosEmpty when id is 0', async () => {
    render(
      <EnderecosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // EnderecosWindow should be rendered with selectedEnderecos equal to EnderecosEmpty()
    const node = await screen.findByTestId('enderecos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedEnderecos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedenderecos') || node.getAttribute('selectedEnderecos')).not.toBeNull();
  });

  it('fetches enderecos by id and passes it to EnderecosWindow when id > 0', async () => {
    const fakeEnderecos = { id: 123, nomecliente: 'Test' };
    mockFetchEnderecosById.mockResolvedValue(fakeEnderecos);

    render(
      <EnderecosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('enderecos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchEnderecosById).toHaveBeenCalledWith(123));
  });
});
