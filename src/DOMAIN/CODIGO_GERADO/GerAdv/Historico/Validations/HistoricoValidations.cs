// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Validations;
#pragma warning restore IDE0130 // Namespace does not match folder structure

public partial interface IHistoricoValidation
{
    Task<bool> ValidateReg(Models.Historico reg, IHistoricoService service, IProcessosReader processosReader, IPrecatoriaReader precatoriaReader, IApensoReader apensoReader, IFaseReader faseReader, IStatusAndamentoReader statusandamentoReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
    Task<bool> CanDelete(int id, IHistoricoService service, IProcessosObsReportService processosobsreportService, IRecadosService recadosService, [FromRoute, Required] string uri, MsiSqlConnection oCnn);
}

public class HistoricoValidation : IHistoricoValidation
{
    public async Task<bool> CanDelete(int id, IHistoricoService service, IProcessosObsReportService processosobsreportService, IRecadosService recadosService, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (id <= 0)
            throw new SGValidationException("Id inválido");
        var reg = await service.GetById(id, uri, default);
        if (reg == null)
            throw new SGValidationException($"Registro com id {id} não encontrado.");
        var processosobsreportExists0 = await processosobsreportService.Filter(new Filters.FilterProcessosObsReport { Historico = id }, uri);
        if (processosobsreportExists0 != null && processosobsreportExists0.Any())
            throw new SGValidationException("Não é possível excluir o registro, pois existem registros da tabela Processos Obs Report associados a ele.");
        var recadosExists1 = await recadosService.Filter(new Filters.FilterRecados { Historico = id }, uri);
        if (recadosExists1 != null && recadosExists1.Any())
            throw new SGValidationException("Não é possível excluir o registro, pois existem registros da tabela Recados associados a ele.");
        return true;
    }

    private bool ValidSizes(Models.Historico reg)
    {
        if (reg.ExtraGUID.Length > 100)
            throw new SGValidationException($"ExtraGUID deve ter no máximo 100 caracteres.");
        if (reg.GUID.Length > 100)
            throw new SGValidationException($"GUID deve ter no máximo 100 caracteres.");
        return true;
    }

    public async Task<bool> ValidateReg(Models.Historico reg, IHistoricoService service, IProcessosReader processosReader, IPrecatoriaReader precatoriaReader, IApensoReader apensoReader, IFaseReader faseReader, IStatusAndamentoReader statusandamentoReader, [FromRoute, Required] string uri, MsiSqlConnection oCnn)
    {
        if (reg == null)
            throw new SGValidationException("Objeto está nulo");
        var validSizes = ValidSizes(reg);
        if (!validSizes)
            return false;
        // Processos
        if (!reg.Processo.IsEmptyIDNumber())
        {
            var regProcessos = await processosReader.Read(reg.Processo, oCnn);
            if (regProcessos == null || regProcessos.Id != reg.Processo)
            {
                throw new SGValidationException($"Processos não encontrado ({regProcessos?.Id}).");
            }
        }

        // Precatoria
        if (!reg.Precatoria.IsEmptyIDNumber())
        {
            var regPrecatoria = await precatoriaReader.Read(reg.Precatoria, oCnn);
            if (regPrecatoria == null || regPrecatoria.Id != reg.Precatoria)
            {
                throw new SGValidationException($"Precatoria não encontrado ({regPrecatoria?.Id}).");
            }
        }

        // Apenso
        if (!reg.Apenso.IsEmptyIDNumber())
        {
            var regApenso = await apensoReader.Read(reg.Apenso, oCnn);
            if (regApenso == null || regApenso.Id != reg.Apenso)
            {
                throw new SGValidationException($"Apenso não encontrado ({regApenso?.Id}).");
            }
        }

        // Fase
        if (!reg.Fase.IsEmptyIDNumber())
        {
            var regFase = await faseReader.Read(reg.Fase, oCnn);
            if (regFase == null || regFase.Id != reg.Fase)
            {
                throw new SGValidationException($"Fase não encontrado ({regFase?.Id}).");
            }
        }

        // StatusAndamento
        if (!reg.StatusAndamento.IsEmptyIDNumber())
        {
            var regStatusAndamento = await statusandamentoReader.Read(reg.StatusAndamento, oCnn);
            if (regStatusAndamento == null || regStatusAndamento.Id != reg.StatusAndamento)
            {
                throw new SGValidationException($"Status Andamento não encontrado ({regStatusAndamento?.Id}).");
            }
        }

        return true;
    }
}