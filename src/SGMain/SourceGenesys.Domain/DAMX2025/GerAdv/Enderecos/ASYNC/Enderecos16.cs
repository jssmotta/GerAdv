// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:Enderecos
// Fonte:DBAsyncDBGenerator
namespace MenphisSI.SG.GerAdv;
public partial class DBEnderecos
{
    /// <summary>
    /// Factory method async para carregar [TABLE_LOWER]
    /// </summary>
    public static async Task<DBEnderecos> CreateAsync(int nCodigo, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        var registro = new DBEnderecos();
        await registro.CarregarAsync(nCodigo, oCnn, cancellationToken);
        return registro;
    }

    private void CreateGuid()
    {
        if (string.IsNullOrWhiteSpace(FGuid))
        {
            this.FGuid = Guid.NewGuid().ToString();
        }
    }

    /// <summary>
    /// Carregar dados async
    /// </summary>
    protected async Task CarregarAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (id.IsEmptyIDNumber() || oCnn is null)
            return;
        using var cmd = new SqlCommand($"{ConfiguracoesDBT.SQLNoCount}SELECT TOP (1) {CamposSqlX} FROM {PTabelaNome.dbo(oCnn)} WHERE [{CampoCodigo}] = @ThisIDToLoad", oCnn.InnerConnection);
        cmd.Parameters.AddWithValue("@ThisIDToLoad", id);
        using var ds = await ConfiguracoesDBT.GetDataTableAsync(cmd, CommandBehavior.SingleRow, oCnn, cancellationToken);
        if (ds is null)
        {
            throw new InvalidOperationException($"Erro ao carregar registro {this.TabelaNome}.");
        }

        if (ds?.Rows.Count > 0)
        {
            LoadDataBd(ds.Rows[0]);
        }
    }

#if (!NOTSTORED_Enderecos)
    /// <summary>
    /// Update async com timeout e cancellation
    /// </summary>
    public virtual async Task<int> UpdateAsync(MsiSqlConnection? oCnn, int insertId = 0, CancellationToken cancellationToken = default, int timeoutSeconds = 30)
    {
        var isInsert = insertId == 0 && ID == 0;
        if (!isInsert && !HasAnyFieldChanged())
            return 0;
        if (oCnn is null)
        {
#if DEBUG
            PTabelaNome.PopupBox("oCnn is null - UpdateAsync()");
#endif
            return 0;
        }

        try
        {
            var updateTool = new DBToolWTable32Async(PTabelaNome, CampoCodigo, ID == 0)
            {
                IsMachineCode = true,
                Identity = this.IdIsIdentity()
            };
            updateTool.Where = isInsert ? string.Empty : $"{CampoCodigo}={ID}";
            ConfigureUpdateFields(updateTool);
            ConfigureAuditorFields(updateTool);
            if (insertId != 0)
            {
                return await GravaNewIdAsync(updateTool, insertId, oCnn, cancellationToken);
            }

            var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken);
            if (updateTool.Insert && result == "OK")
            {
                ID = updateTool.GetCodigo();
                if (ID == 0)
                {
                    Error = -2;
                    ErrorDescription = "O registro não pode ser incluído, tente mais tarde.";
                    return Error;
                }
            }

            return result == "OK" ? 0 : -1;
        }
        catch (OperationCanceledException)
        {
            throw;
        }
        catch (Exception ex)
        {
            Error = -1;
            ErrorDescription = ex.Message;
            throw new InvalidOperationException($"Factory error");
        }
    }

#endif
    /// <summary>
    /// Verificar se existe async
    /// </summary>
    public static async Task<bool> ExisteAsync(int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var existe = await ConfiguracoesDBT.GetScalarAsync<int?>($"SELECT 1 FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = {id}", oCnn, cancellationToken);
        return existe.HasValue;
    }

    /// <summary>
    /// Delete async
    /// </summary>
    /// <param name = "operadorId">For future Audity</param>
    /// <param name = "id"></param>
    /// <param name = "oCnn"></param>
    /// <param name = "cancellationToken"></param>
    /// <returns></returns>
    /// <exception cref = "InvalidOperationException"></exception>
    public static async Task<bool> DeleteRecordAsync(int operadorId, int id, MsiSqlConnection? oCnn, CancellationToken cancellationToken = default)
    {
        if (oCnn is null || id <= 0)
            return false;
        var sql = $"DELETE FROM {PTabelaNome.dbo(oCnn)} WHERE {CampoCodigo} = @Id";
        try
        {
            using var cmd = new SqlCommand(sql, oCnn.InnerConnection);
            cmd.Parameters.AddWithValue("@Id", id);
            var rowsAffected = await cmd.ExecuteNonQueryAsync(cancellationToken);
            return rowsAffected > 0;
        }
        catch (SqlException ex)when (ex.Number == 547)
        {
            throw new InvalidOperationException("Este registro não pode ser excluído, pois está vinculado a outros registros.");
        }
    }

#if (!NOTSTORED_Enderecos)
    // Helper methods
    private bool HasAnyFieldChanged() => pFldFTopIndex || pFldFDescricao || pFldFContato || pFldFDtNasc || pFldFEndereco || pFldFBairro || pFldFPrivativo || pFldFAddContato || pFldFCEP || pFldFOAB || pFldFOBS || pFldFFone || pFldFFax || pFldFTratamento || pFldFCidade || pFldFSite || pFldFEMail || pFldFQuem || pFldFQuemIndicou || pFldFReportECBOnly || pFldFEtiqueta || pFldFAni || pFldFBold || pFldFGuid;
    private void ConfigureUpdateFields(DBToolWTable32Async updateTool)
    {
        if (pFldFTopIndex || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.TopIndex, FTopIndex, EGenericTypeFields.FBoolean);
        if (pFldFDescricao)
            updateTool.Fields(DBEnderecosDicInfo.Descricao, FDescricao, EGenericTypeFields.FString);
        if (pFldFContato)
            updateTool.Fields(DBEnderecosDicInfo.Contato, FContato, EGenericTypeFields.FString);
        if (pFldFDtNasc)
            updateTool.Fields(DBEnderecosDicInfo.DtNasc, FDtNasc, EGenericTypeFields.FDate);
        if (pFldFEndereco)
            updateTool.Fields(DBEnderecosDicInfo.Endereco, FEndereco, EGenericTypeFields.FString);
        if (pFldFBairro)
            updateTool.Fields(DBEnderecosDicInfo.Bairro, FBairro, EGenericTypeFields.FString);
        if (pFldFPrivativo || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.Privativo, FPrivativo, EGenericTypeFields.FBoolean);
        if (pFldFAddContato || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.AddContato, FAddContato, EGenericTypeFields.FBoolean);
        if (pFldFCEP)
            updateTool.Fields(DBEnderecosDicInfo.CEP, FCEP, EGenericTypeFields.FString);
        if (pFldFOAB)
            updateTool.Fields(DBEnderecosDicInfo.OAB, FOAB, EGenericTypeFields.FString);
        if (pFldFOBS)
            updateTool.Fields(DBEnderecosDicInfo.OBS, FOBS, EGenericTypeFields.FString);
        if (pFldFFone)
            updateTool.Fields(DBEnderecosDicInfo.Fone, FFone, EGenericTypeFields.FString);
        if (pFldFFax)
            updateTool.Fields(DBEnderecosDicInfo.Fax, FFax, EGenericTypeFields.FString);
        if (pFldFTratamento)
            updateTool.Fields(DBEnderecosDicInfo.Tratamento, FTratamento, EGenericTypeFields.FString);
        if (pFldFCidade)
            updateTool.Fields(DBEnderecosDicInfo.Cidade, FCidade, EGenericTypeFields.FNumber);
        if (pFldFSite)
            updateTool.Fields(DBEnderecosDicInfo.Site, FSite, EGenericTypeFields.FString);
        if (pFldFEMail)
            updateTool.Fields(DBEnderecosDicInfo.EMail, FEMail, EGenericTypeFields.FString);
        if (pFldFQuem)
            updateTool.Fields(DBEnderecosDicInfo.Quem, FQuem, EGenericTypeFields.FNumber);
        if (pFldFQuemIndicou)
            updateTool.Fields(DBEnderecosDicInfo.QuemIndicou, FQuemIndicou, EGenericTypeFields.FString);
        if (pFldFReportECBOnly || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.ReportECBOnly, FReportECBOnly, EGenericTypeFields.FBoolean);
        if (pFldFEtiqueta || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.Etiqueta, FEtiqueta, EGenericTypeFields.FBoolean);
        if (pFldFAni || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.Ani, FAni, EGenericTypeFields.FBoolean);
        if (pFldFBold || updateTool.Insert)
            updateTool.Fields(DBEnderecosDicInfo.Bold, FBold, EGenericTypeFields.FBoolean);
        if (pFldFGuid)
            updateTool.Fields(DBEnderecosDicInfo.Guid, FGuid, EGenericTypeFields.FString);
    }

#endif
#if (!NOTSTORED_Enderecos)
    private void ConfigureAuditorFields(DBToolWTable32Async updateTool)
    {
        // Tracking: CodeAuditor(202508121515)
        var isInsert = ID.IsEmptyIDNumber();
        if (AuditorQuem == 0)
            AuditorQuem = 1;
        if (isInsert)
            updateTool.Fields(DBEnderecosDicInfo.QuemCad, AuditorQuem, EGenericTypeFields.FNumber);
        if (isInsert)
            updateTool.Fields(DBEnderecosDicInfo.DtCad, DevourerOne.DateTimeUtc, EGenericTypeFields.FDate);
        if (!isInsert)
            updateTool.Fields(DBEnderecosDicInfo.QuemAtu, AuditorQuem, EGenericTypeFields.FNumber);
        if (!isInsert)
            updateTool.Fields(DBEnderecosDicInfo.DtAtu, DevourerOne.DateTimeUtc, EGenericTypeFields.FDate);
        updateTool.Fields(DBEnderecosDicInfo.Visto, false, EGenericTypeFields.FBoolean);
        CreateGuid();
        if (isInsert)
            updateTool.Fields(DBEnderecosDicInfo.Guid, FGuid, EGenericTypeFields.FString);
    }

    private async Task<int> GravaNewIdAsync(DBToolWTable32Async updateTool, int insertId, MsiSqlConnection? oCnn, CancellationToken cancellationToken)
    {
        ID = insertId;
        updateTool.Fields(CampoCodigo, insertId, EGenericTypeFields.FNumber);
        var result = await updateTool.RecUpdateAsync(oCnn, cancellationToken, true);
        return result == "OK" ? 0 : -3;
    }
#endif
}