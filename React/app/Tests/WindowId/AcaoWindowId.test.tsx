// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import AcaoWindowId from '@/app/GerAdv_TS/Acao/Crud/Grids/AcaoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import AcaoWindowId from '@/app/GerAdv_TS/Acao/Crud/Grids/AcaoWindowId';
import { AcaoEmpty } from '@/app/GerAdv_TS/Models/Acao';

describe('AcaoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(AcaoWindowId).toBeDefined();
    expect(typeof AcaoWindowId === 'function' || typeof AcaoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the AcaoService class so constructor usage doesn't throw and we can control fetchAcaoById
const mockFetchAcaoById = jest.fn();
jest.mock('@/app/GerAdv_TS/Acao/Services/Acao.service', () => ({
  AcaoService: jest.fn().mockImplementation(() => ({
    fetchAcaoById: (...args: any[]) => mockFetchAcaoById(...args),
  })),
}));

// Mock the child AcaoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Acao/Crud/Grids/AcaoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='acao-window' {...p} />,
}));

describe('AcaoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(AcaoWindowId).toBeDefined();
    expect(typeof AcaoWindowId === 'function' || typeof AcaoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <AcaoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders AcaoWindow with AcaoEmpty when id is 0', async () => {
    render(
      <AcaoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // AcaoWindow should be rendered with selectedAcao equal to AcaoEmpty()
    const node = await screen.findByTestId('acao-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedAcao"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedacao') || node.getAttribute('selectedAcao')).not.toBeNull();
  });

  it('fetches acao by id and passes it to AcaoWindow when id > 0', async () => {
    const fakeAcao = { id: 123, nomecliente: 'Test' };
    mockFetchAcaoById.mockResolvedValue(fakeAcao);

    render(
      <AcaoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('acao-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchAcaoById).toHaveBeenCalledWith(123));
  });
});
