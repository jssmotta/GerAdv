// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import StatusAndamentoWindowId from '@/app/GerAdv_TS/StatusAndamento/Crud/Grids/StatusAndamentoWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import StatusAndamentoWindowId from '@/app/GerAdv_TS/StatusAndamento/Crud/Grids/StatusAndamentoWindowId';
import { StatusAndamentoEmpty } from '@/app/GerAdv_TS/Models/StatusAndamento';

describe('StatusAndamentoWindowId (basic)', () => {
  it('exports the component', () => {
    expect(StatusAndamentoWindowId).toBeDefined();
    expect(typeof StatusAndamentoWindowId === 'function' || typeof StatusAndamentoWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the StatusAndamentoService class so constructor usage doesn't throw and we can control fetchStatusAndamentoById
const mockFetchStatusAndamentoById = jest.fn();
jest.mock('@/app/GerAdv_TS/StatusAndamento/Services/StatusAndamento.service', () => ({
  StatusAndamentoService: jest.fn().mockImplementation(() => ({
    fetchStatusAndamentoById: (...args: any[]) => mockFetchStatusAndamentoById(...args),
  })),
}));

// Mock the child StatusAndamentoWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/StatusAndamento/Crud/Grids/StatusAndamentoWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='statusandamento-window' {...p} />,
}));

describe('StatusAndamentoWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(StatusAndamentoWindowId).toBeDefined();
    expect(typeof StatusAndamentoWindowId === 'function' || typeof StatusAndamentoWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <StatusAndamentoWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders StatusAndamentoWindow with StatusAndamentoEmpty when id is 0', async () => {
    render(
      <StatusAndamentoWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // StatusAndamentoWindow should be rendered with selectedStatusAndamento equal to StatusAndamentoEmpty()
    const node = await screen.findByTestId('statusandamento-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedStatusAndamento"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedstatusandamento') || node.getAttribute('selectedStatusAndamento')).not.toBeNull();
  });

  it('fetches statusandamento by id and passes it to StatusAndamentoWindow when id > 0', async () => {
    const fakeStatusAndamento = { id: 123, nomecliente: 'Test' };
    mockFetchStatusAndamentoById.mockResolvedValue(fakeStatusAndamento);

    render(
      <StatusAndamentoWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('statusandamento-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchStatusAndamentoById).toHaveBeenCalledWith(123));
  });
});
