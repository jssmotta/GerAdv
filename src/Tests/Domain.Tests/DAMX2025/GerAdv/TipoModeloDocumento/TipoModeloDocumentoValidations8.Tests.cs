// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
namespace MenphisSI.GerAdv.Tests;
public class TipoModeloDocumentoValidationTests : IDisposable
{
    private readonly Mock<IOptions<AppSettings>> _mockAppSettings;
    private readonly Mock<IFTipoModeloDocumentoFactory> _mockTipoModeloDocumentoFactory;
    private readonly Mock<ITipoModeloDocumentoReader> _mockReader;
    private readonly TipoModeloDocumentoValidation _validation;
    private readonly Mock<ITipoModeloDocumentoService> _mockTipoModeloDocumentoService;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoModeloDocumentoService _service;
    private readonly string _validUri = "test-uri";
    public TipoModeloDocumentoValidationTests()
    {
        _mockTipoModeloDocumentoFactory = new Mock<IFTipoModeloDocumentoFactory>();
        _mockReader = new Mock<ITipoModeloDocumentoReader>();
        _validation = new TipoModeloDocumentoValidation();
        _mockTipoModeloDocumentoService = new Mock<ITipoModeloDocumentoService>();
        _mockConnection = new Mock<MsiSqlConnection>();
    }

#region Class Structure Tests
    [Fact]
    public void TipoModeloDocumentoValidation_ShouldImplementInterface()
    {
        // Assert
        _validation.Should().BeAssignableTo<ITipoModeloDocumentoValidation>();
    }

    [Fact]
    public void TipoModeloDocumentoValidation_ShouldBePublicClass()
    {
        // Arrange
        var type = typeof(TipoModeloDocumentoValidation);
        // Assert
        type.IsPublic.Should().BeTrue();
        type.IsClass.Should().BeTrue();
        type.IsAbstract.Should().BeFalse();
        type.IsSealed.Should().BeFalse();
    }

    [Fact]
    public void TipoModeloDocumentoValidation_ShouldHaveParameterlessConstructor()
    {
        // Arrange
        var type = typeof(TipoModeloDocumentoValidation);
        var constructors = type.GetConstructors();
        // Assert
        constructors.Should().ContainSingle();
        constructors[0].GetParameters().Should().BeEmpty();
        constructors[0].IsPublic.Should().BeTrue();
    }

    private Models.TipoModeloDocumento CreateValidTipoModeloDocumento()
    {
        return new Models.TipoModeloDocumento
        {
            Id = 1,
            Nome = "João"
        };
    }

    private void SetupValidMocks()
    {
        // Setup default valid responses for all mocks
        _mockTipoModeloDocumentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoModeloDocumento>(), It.IsAny<string>())).ReturnsAsync(new List<TipoModeloDocumentoResponseAll>());
    // Setup other mocks but don't override the TipoModeloDocumentos service mock
    }

    private void SetupValidMocksInvalid()
    {
        // Setup default valid responses for all mocks
        _mockTipoModeloDocumentoService.Setup(x => x.Filter(It.IsAny<int>(), It.IsAny<FilterTipoModeloDocumento>(), It.IsAny<string>())).ReturnsAsync(new List<TipoModeloDocumentoResponseAll>());
    // Setup other mocks but don't override the TipoModeloDocumentos service mock
    }

    [Fact]
    public async Task ValidateReg_WithNullStringFields_ShouldNotThrow()
    {
        // Arrange
        var tipomodelodocumento = new Models.TipoModeloDocumento
        {
            Id = 1,
            Nome = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipomodelodocumento, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

#endregion
    [Fact]
    public async Task ValidateReg_WithNullInput_ShouldThrowSGValidationException()
    {
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(null, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Be("Objeto está nulo");
    }

#region ValidateReg Required Nome Method Tests 
    [Fact]
    public async Task ValidateReg_WithEmptyNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipomodelodocumento = CreateValidTipoModeloDocumento();
        tipomodelodocumento.Nome = "";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipomodelodocumento, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithNullNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipomodelodocumento = CreateValidTipoModeloDocumento();
        tipomodelodocumento.Nome = null;
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipomodelodocumento, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

    [Fact]
    public async Task ValidateReg_WithValidDataNome_ShouldReturnTrue()
    {
        // Arrange
        var tipomodelodocumento = CreateValidTipoModeloDocumento();
        SetupValidMocks();
        // Act
        var result = await _validation.ValidateReg(tipomodelodocumento, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object);
        // Assert
        result.Should().BeTrue();
    }

    [Fact]
    public async Task ValidateReg_WithWhitespaceNome_ShouldThrowSGValidationException()
    {
        // Arrange
        var tipomodelodocumento = CreateValidTipoModeloDocumento();
        tipomodelodocumento.Nome = "   ";
        // Act & Assert
        var exception = await Assert.ThrowsAsync<SGValidationException>(() => _validation.ValidateReg(tipomodelodocumento, _mockTipoModeloDocumentoService.Object, _validUri, _mockConnection.Object));
        exception.Message.Should().Contain("é obrigatório");
    }

#endregion
    public virtual void Dispose()
    {
        _service?.Dispose();
    }
}