// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
using Xunit;
using FluentAssertions;
using Moq;
using System;
using System.Threading.Tasks;
using MenphisSI.GerAdv.Writers;
using MenphisSI.GerAdv.Models;

namespace MenphisSI.GerAdv.Tests.Writers;
public class TipoRecursoWriterTests
{
    private readonly Mock<IFTipoRecursoFactory> _mockTipoRecursoFactory;
    private readonly Mock<FTipoRecurso> _mockFTipoRecurso;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly TipoRecursoWriter _tiporecursoWriter;
    public TipoRecursoWriterTests()
    {
        _mockTipoRecursoFactory = new Mock<IFTipoRecursoFactory>();
        _mockFTipoRecurso = new Mock<FTipoRecurso>();
        _mockConnection = new Mock<MsiSqlConnection>();
        _tiporecursoWriter = new TipoRecursoWriter(_mockTipoRecursoFactory.Object);
    }

#region WriteAsync Tests
    [Fact]
    public async Task WriteAsync_WithNewClient_ShouldCreateNewRecord()
    {
        // Arrange
        var tiporecurso = CreateValidTipoRecursoModel();
        tiporecurso.Id = 0; // ID vazio para novo registro
        var auditorQuem = 123;
        _mockTipoRecursoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoRecurso.Object);
        _mockFTipoRecurso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tiporecursoWriter.WriteAsync(tiporecurso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoRecurso.Object);
        _mockTipoRecursoFactory.Verify(x => x.CreateAsync(), Times.Once);
        _mockTipoRecursoFactory.Verify(x => x.CreateFromIdAsync(It.IsAny<int>(), It.IsAny<MsiSqlConnection>()), Times.Never);
        _mockFTipoRecurso.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WithExistingClient_ShouldLoadExistingRecord()
    {
        // Arrange
        var tiporecurso = CreateValidTipoRecursoModel();
        tiporecurso.Id = 456; // ID existente
        var auditorQuem = 123;
        _mockTipoRecursoFactory.Setup(x => x.CreateFromIdAsync(tiporecurso.Id, _mockConnection.Object)).ReturnsAsync(_mockFTipoRecurso.Object);
        _mockFTipoRecurso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        ;
        // Act
        var result = await _tiporecursoWriter.WriteAsync(tiporecurso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoRecurso.Object);
        _mockTipoRecursoFactory.Verify(x => x.CreateFromIdAsync(tiporecurso.Id, _mockConnection.Object), Times.Once);
        _mockTipoRecursoFactory.Verify(x => x.CreateAsync(), Times.Never);
        _mockFTipoRecurso.Verify(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>()), Times.Once);
    }

    [Fact]
    public async Task WriteAsync_ShouldMapAllPropertiesCorrectly()
    {
        // Arrange
        var tiporecurso = CreateValidTipoRecursoModel();
        var auditorQuem = 99;
        _mockTipoRecursoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoRecurso.Object);
        _mockFTipoRecurso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ReturnsAsync(0);
        // Act
        var result = await _tiporecursoWriter.WriteAsync(tiporecurso, auditorQuem, _mockConnection.Object);
        // Assert
        result.Should().Be(_mockFTipoRecurso.Object);
        _mockFTipoRecurso.VerifySet(x => x.FGUID = tiporecurso.GUID, Times.Once);
        _mockFTipoRecurso.VerifySet(x => x.FJustica = tiporecurso.Justica, Times.Once);
        _mockFTipoRecurso.VerifySet(x => x.FArea = tiporecurso.Area, Times.Once);
        _mockFTipoRecurso.VerifySet(x => x.FDescricao = tiporecurso.Descricao, Times.Once);
        _mockFTipoRecurso.VerifySet(x => x.AuditorQuem = auditorQuem, Times.Once);
    }

    [Fact]
    public async Task WriteAsync_WhenFactoryThrowsException_ShouldPropagateException()
    {
        // Arrange
        var tiporecurso = CreateValidTipoRecursoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoRecursoFactory.Setup(x => x.CreateAsync()).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiporecursoWriter.WriteAsync(tiporecurso, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

    [Fact]
    public async Task WriteAsync_WhenUpdateAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tiporecurso = CreateValidTipoRecursoModel();
        var auditorQuem = 123;
        var expectedException = new InvalidOperationException("Factory error");
        _mockTipoRecursoFactory.Setup(x => x.CreateAsync()).ReturnsAsync(_mockFTipoRecurso.Object);
        _mockFTipoRecurso.Setup(x => x.UpdateAsync(It.IsAny<MsiSqlConnection>(), It.IsAny<int>(), It.IsAny<CancellationToken>(), It.IsAny<int>())).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiporecursoWriter.WriteAsync(tiporecurso, auditorQuem, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Delete Tests
    [Fact]
    public async Task Delete_ShouldCallFactoryDeleteAsync()
    {
        // Arrange
        var tiporecursoResponse = new TipoRecursoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        _mockTipoRecursoFactory.Setup(x => x.DeleteAsync(operadorId, tiporecursoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        await _tiporecursoWriter.Delete(tiporecursoResponse, operadorId, _mockConnection.Object);
        // Assert
        _mockTipoRecursoFactory.Verify(x => x.DeleteAsync(operadorId, tiporecursoResponse.Id, _mockConnection.Object), Times.Once);
    }

    [Fact]
    public async Task Delete_WithValidParameters_ShouldCompleteSuccessfully()
    {
        // Arrange
        var tiporecursoResponse = new TipoRecursoResponse
        {
            Id = 999
        };
        var operadorId = 111;
        _mockTipoRecursoFactory.Setup(x => x.DeleteAsync(operadorId, tiporecursoResponse.Id, _mockConnection.Object)).Returns(Task.CompletedTask);
        // Act
        Func<Task> act = async () => await _tiporecursoWriter.Delete(tiporecursoResponse, operadorId, _mockConnection.Object);
        // Assert
        await act.Should().NotThrowAsync();
    }

    [Fact]
    public async Task Delete_WhenFactoryDeleteAsyncFails_ShouldPropagateException()
    {
        // Arrange
        var tiporecursoResponse = new TipoRecursoResponse
        {
            Id = 123
        };
        var operadorId = 456;
        var expectedException = new InvalidOperationException("Delete failed");
        _mockTipoRecursoFactory.Setup(x => x.DeleteAsync(operadorId, tiporecursoResponse.Id, _mockConnection.Object)).ThrowsAsync(expectedException);
        // Act & Assert
        var exception = await Assert.ThrowsAsync<InvalidOperationException>(() => _tiporecursoWriter.Delete(tiporecursoResponse, operadorId, _mockConnection.Object));
        exception.Should().Be(expectedException);
    }

#endregion
#region Constructor Tests
    [Fact]
    public void Constructor_WithValidFactory_ShouldCreateInstance()
    {
        // Act
        var writer = new TipoRecursoWriter(_mockTipoRecursoFactory.Object);
        // Assert
        writer.Should().NotBeNull();
    }

    [Fact]
    public void Constructor_WithNullFactory_ShouldThrowArgumentNullException()
    {
        // Act & Assert
        Action act = () => new TipoRecursoWriter(null);
        act.Should().Throw<ArgumentNullException>();
    }

#endregion
#region Helper Methods
    private static Models.TipoRecurso CreateValidTipoRecursoModel()
    {
        return new Models.TipoRecurso
        {
            Id = 0,
            GUID = Guid.NewGuid().ToString(),
            Justica = 1,
            Area = 1,
            Descricao = "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        };
    }
#endregion
}