// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
#pragma warning disable IDE0130 // Namespace does not match folder structure

namespace MenphisSI.GerAdv.Services;
#pragma warning restore IDE0130 // Namespace does not match folder structure

using MenphisSI.GerAdv.AI.Filters;

public partial class ServiceFilter
{
    public (string where, List<SqlParameter> parametros)? WFiltroDiario2(Filters.FilterDiario2? filtro)
    {
        if (filtro == null)
            return null;
        var parameters = new List<SqlParameter>();
        if (filtro.QuemCad != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.QuemCad)}", filtro.QuemCad));
            if (filtro.QuemCad_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBDiario2DicInfo.QuemCad)}_end", filtro.QuemCad_end));
            }
        }

        if (!filtro.DtCad.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtCad, out var dataParam1))
                parameters.Add(new($"@{(DBDiario2DicInfo.DtCad)}", dataParam1));
            if (!filtro.DtCad_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtCad_end, out var dataParam2))
                    parameters.Add(new($"@{(DBDiario2DicInfo.DtCad)}_end", dataParam2));
            }
        }

        if (filtro.QuemAtu != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.QuemAtu)}", filtro.QuemAtu));
            if (filtro.QuemAtu_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBDiario2DicInfo.QuemAtu)}_end", filtro.QuemAtu_end));
            }
        }

        if (!filtro.DtAtu.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.DtAtu, out var dataParam3))
                parameters.Add(new($"@{(DBDiario2DicInfo.DtAtu)}", dataParam3));
            if (!filtro.DtAtu_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.DtAtu_end, out var dataParam4))
                    parameters.Add(new($"@{(DBDiario2DicInfo.DtAtu)}_end", dataParam4));
            }
        }

        if (filtro.Visto != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Visto)}", filtro.Visto));
        }

        if (!string.IsNullOrWhiteSpace(filtro.Guid))
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Guid)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Guid)));
        }

        if (!filtro.Data.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.Data, out var dataParam5))
                parameters.Add(new($"@{(DBDiario2DicInfo.Data)}", dataParam5));
            if (!filtro.Data_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.Data_end, out var dataParam6))
                    parameters.Add(new($"@{(DBDiario2DicInfo.Data)}_end", dataParam6));
            }
        }

        if (!filtro.Hora.IsEmptyDX())
        {
            if (DateTime.TryParse(filtro.Hora, out var dataParam7))
                parameters.Add(new($"@{(DBDiario2DicInfo.Hora)}", dataParam7));
            if (!filtro.Hora_end.IsEmptyDX())
            {
                if (DateTime.TryParse(filtro.Hora_end, out var dataParam8))
                    parameters.Add(new($"@{(DBDiario2DicInfo.Hora)}_end", dataParam8));
            }
        }

        if (filtro.Operador != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Operador)}", filtro.Operador));
            if (filtro.Operador_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBDiario2DicInfo.Operador)}_end", filtro.Operador_end));
            }
        }

        if (!string.IsNullOrWhiteSpace(filtro.Nome))
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Nome)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Nome)));
        }

        if (!string.IsNullOrWhiteSpace(filtro.Ocorrencia))
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Ocorrencia)}", DevourerOne.ApplyWildCard(filtro.WildcardChar, filtro.Ocorrencia)));
        }

        if (filtro.Cliente != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.Cliente)}", filtro.Cliente));
            if (filtro.Cliente_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBDiario2DicInfo.Cliente)}_end", filtro.Cliente_end));
            }
        }

        if (filtro.Codigo_filtro != int.MinValue)
        {
            parameters.Add(new($"@{(DBDiario2DicInfo.CampoCodigo)}", filtro.Codigo_filtro));
            if (filtro.Codigo_filtro_end != int.MinValue)
            {
                parameters.Add(new($"@{(DBDiario2DicInfo.CampoCodigo)}_end", filtro.Codigo_filtro_end));
            }
        }

        if (filtro.LogicalOperator.IsEmptyX() || (filtro.LogicalOperator.NotEquals(TSql.And) && filtro.LogicalOperator.NotEquals(TSql.OR)))
        {
            filtro.LogicalOperator = TSql.And;
        }

        var operador = this.WFiltroOperador(filtro.FilterOperador);
        if (operador?.parametros != null)
        {
            parameters.AddRange(operador?.parametros!);
        }

        var clientes = this.WFiltroClientes(filtro.FilterClientes);
        if (clientes?.parametros != null)
        {
            parameters.AddRange(clientes?.parametros!);
        }

        var cWhere = new StringBuilder();
        cWhere.Append(operador?.where);
        if (!string.IsNullOrWhiteSpace(clientes?.where))
        {
            if (cWhere.Length > 0)
            {
                cWhere.Append(filtro.LogicalOperator);
            }

            cWhere.Append(clientes?.where);
        }

        if (!(filtro.QuemCad.IsEmptyX()) && filtro.QuemCad_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemCad.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.QuemCad}] = @{(DBDiario2DicInfo.QuemCad)}");
        }
        else if (!(filtro.QuemCad.IsEmptyX()) && !(filtro.QuemCad_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.QuemCad} BETWEEN @{(DBDiario2DicInfo.QuemCad)} AND @{(DBDiario2DicInfo.QuemCad)}_end");
        }

        if (!(filtro.DtCad.IsEmptyDX()) && filtro.DtCad_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtCad.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.DtCad}], 103) = CONVERT(DATE, @{(DBDiario2DicInfo.DtCad)}, 103)");
        }
        else if (!(filtro.DtCad.IsEmptyDX()) && !(filtro.DtCad_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.DtCad} BETWEEN @{(DBDiario2DicInfo.DtCad)} AND @{(DBDiario2DicInfo.DtCad)}_end");
        }

        if (!(filtro.QuemAtu.IsEmptyX()) && filtro.QuemAtu_end.IsEmptyX())
        {
            cWhere.Append(filtro.QuemAtu.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.QuemAtu}] = @{(DBDiario2DicInfo.QuemAtu)}");
        }
        else if (!(filtro.QuemAtu.IsEmptyX()) && !(filtro.QuemAtu_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.QuemAtu} BETWEEN @{(DBDiario2DicInfo.QuemAtu)} AND @{(DBDiario2DicInfo.QuemAtu)}_end");
        }

        if (!(filtro.DtAtu.IsEmptyDX()) && filtro.DtAtu_end.IsEmptyDX())
        {
            cWhere.Append(filtro.DtAtu.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.DtAtu}], 103) = CONVERT(DATE, @{(DBDiario2DicInfo.DtAtu)}, 103)");
        }
        else if (!(filtro.DtAtu.IsEmptyDX()) && !(filtro.DtAtu_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.DtAtu} BETWEEN @{(DBDiario2DicInfo.DtAtu)} AND @{(DBDiario2DicInfo.DtAtu)}_end");
        }

        cWhere.Append(filtro.Visto == int.MinValue ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Visto}] = @{(DBDiario2DicInfo.Visto)}");
        cWhere.Append(filtro.Guid.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Guid}]  {DevourerConsts.MsiCollate} like @{(DBDiario2DicInfo.Guid)}");
        if (!(filtro.Data.IsEmptyDX()) && filtro.Data_end.IsEmptyDX())
        {
            cWhere.Append(filtro.Data.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"CONVERT(DATE,[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Data}], 103) = CONVERT(DATE, @{(DBDiario2DicInfo.Data)}, 103)");
        }
        else if (!(filtro.Data.IsEmptyDX()) && !(filtro.Data_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.Data} BETWEEN @{(DBDiario2DicInfo.Data)} AND @{(DBDiario2DicInfo.Data)}_end");
        }

        if (!(filtro.Hora.IsEmptyDX()) && filtro.Hora_end.IsEmptyDX())
        {
            cWhere.Append(filtro.Hora.IsEmptyDX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"FORMAT([{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Hora}], 'HH:mm') = FORMAT(@{(DBDiario2DicInfo.Hora)}, 'HH:mm')");
        }
        else if (!(filtro.Hora.IsEmptyDX()) && !(filtro.Hora_end.IsEmptyDX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"FORMAT([{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Hora}], 'HH:mm') BETWEEN FORMAT(@{(DBDiario2DicInfo.Hora)}, 'HH:mm') AND FORMAT(@{(DBDiario2DicInfo.Hora)}_end, 'HH:mm')");
        }

        if (!(filtro.Operador.IsEmptyX()) && filtro.Operador_end.IsEmptyX())
        {
            cWhere.Append(filtro.Operador.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Operador}] = @{(DBDiario2DicInfo.Operador)}");
        }
        else if (!(filtro.Operador.IsEmptyX()) && !(filtro.Operador_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.Operador} BETWEEN @{(DBDiario2DicInfo.Operador)} AND @{(DBDiario2DicInfo.Operador)}_end");
        }

        cWhere.Append(filtro.Nome.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Nome}]  {DevourerConsts.MsiCollate} like @{(DBDiario2DicInfo.Nome)}");
        cWhere.Append(filtro.Ocorrencia.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Ocorrencia}]  {DevourerConsts.MsiCollate} like @{(DBDiario2DicInfo.Ocorrencia)}");
        if (!(filtro.Cliente.IsEmptyX()) && filtro.Cliente_end.IsEmptyX())
        {
            cWhere.Append(filtro.Cliente.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.Cliente}] = @{(DBDiario2DicInfo.Cliente)}");
        }
        else if (!(filtro.Cliente.IsEmptyX()) && !(filtro.Cliente_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.Cliente} BETWEEN @{(DBDiario2DicInfo.Cliente)} AND @{(DBDiario2DicInfo.Cliente)}_end");
        }

        if (!(filtro.Codigo_filtro.IsEmptyX()) && filtro.Codigo_filtro_end.IsEmptyX())
        {
            cWhere.Append(filtro.Codigo_filtro.IsEmptyX() ? string.Empty : (cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].[{DBDiario2DicInfo.CampoCodigo}] = @{(DBDiario2DicInfo.CampoCodigo)}");
        }
        else if (!(filtro.Codigo_filtro.IsEmptyX()) && !(filtro.Codigo_filtro_end.IsEmptyX()))
        {
            cWhere.Append((cWhere.Length == 0 ? string.Empty : filtro.LogicalOperator) + $"[{DBDiario2DicInfo.PTabelaNome}].{DBDiario2DicInfo.CampoCodigo} BETWEEN @{(DBDiario2DicInfo.CampoCodigo)} AND @{(DBDiario2DicInfo.CampoCodigo)}_end");
        }

        return (cWhere.ToString().Trim(), parameters);
    }
}

public partial class Diario2Service
{
    /// <summary>
    /// Processa comando de voz para gerar filtro via OpenAI usando serviço genérico
    /// </summary>
    /// <param name = "message">Requisição com comando de voz</param>
    /// <param name = "existingFilter">Filtro existente para combinar</param>
    /// <param name = "uri">URI da base de dados</param>
    /// <returns>FilterDiario2 processado</returns>
    private async Task<Filters.FilterDiario2?> ProcessFilterVoiceCommandAsync(CommandSpeakerRequest message, Filters.FilterDiario2? existingFilter, string uri)
    {
        try
        {
            // Usar o contexto do FilterDiario2TXT
            var context = FilterDiario2TXT.GetPrompt(existingFilter, FilterDiario2TXT.GetOptions());
            // Instruções específicas para Diario2
            var additionalInstructions = "";
            // Processar com o serviço genérico
            var filtroProcessado = await _voiceFilterService.ProcessVoiceCommandWithFallbackAsync(message.Message, context, existingFilter, additionalInstructions, uri);
            return filtroProcessado;
        }
        catch (Exception)
        {
            // Se falhar, retornar filtro existente ou novo
            return existingFilter ?? new Filters.FilterDiario2();
        }
    }

    private async Task<IEnumerable<Diario2ResponseAll>> GetDataAllAsync(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken cancellationToken)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            return[];
        }

        try
        {
            // Usar o reader para obter os dados
            return await reader.ListarAsync(max, uri, where.Replace(TSql.Where, ""), parameters, "", cancellationToken);
        }
        catch (Exception)
        {
            return[];
        }
    }

    public async Task<IEnumerable<NomeID>> GetListN([FromQuery] int max, [FromBody] Filters.FilterDiario2? filtro, [FromRoute, Required] string uri, CancellationToken token)
    {
        // Tracking: 20250606-0
        ThrowIfDisposed();
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("Diario2: URI inválida");
        }

        var filtroResult = filtro == null ? null : servicesFilter.WFiltroDiario2(filtro!);
        string where = filtroResult?.where ?? string.Empty;
        List<SqlParameter>? parameters = filtroResult?.parametros ?? [];
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new Exception($"Coneão nula.");
        }

        var keyCache = await reader.ReadStringAuditorAsync(max, uri, "", [], oCnn);
        var cacheKey = $"{uri}-Diario2-{max}-{where.GetHashCode()}-GetListN-{keyCache}";
        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId),
            LocalCacheExpiration = TimeSpan.FromSeconds(BaseConsts.PMaxSecondsCacheId)
        };
        return await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataListNAsync(max, uri, where, parameters, cancel), entryOptions, cancellationToken: token) ?? [];
    }

    private async Task<IEnumerable<NomeID>> GetDataListNAsync(int max, string uri, string where, List<SqlParameter>? parameters, CancellationToken token)
    {
        var result = new List<NomeID>(max);
        var lista = await reader.ListarNAsync(max, uri, where, parameters, DBDiario2DicInfo.CampoNome);
        if (lista == null || !lista.Any())
        {
            return result;
        }

        foreach (var item in lista)
        {
            if (token.IsCancellationRequested)
                continue;
            if (item?.FNome != null)
            {
                result.Add(new NomeID { Nome = item.FNome, ID = item.ID });
            }
        }

        return result;
    }

    public async Task<IEnumerable<Diario2ResponseAll>> GetAll(int max, [FromRoute, Required] string uri, CancellationToken token = default)
    {
        ThrowIfDisposed();
        max = Math.Min(Math.Max(max, 1), BaseConsts.PMaxItens);
        if (!(await Uris.ValidaUriAsync(uri, _entityService)))
        {
            throw new Exception("Diario2: URI inválida");
        }

        var entryOptions = new HybridCacheEntryOptions
        {
            Expiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache),
            LocalCacheExpiration = TimeSpan.FromMinutes(BaseConsts.PMaxMinutesCache)
        };
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        var keyCache = await reader.ReadStringAuditorAsync(max, uri, "", [], oCnn);
        var cacheKey = $"{uri}-Diario2-Filter-{max}-{keyCache}";
        var result = await _cache.GetOrCreateAsync(cacheKey, async cancel => await GetDataAllAsync2(max, string.Empty, [], uri, cancel), entryOptions, cancellationToken: token);
        return result;
    }

    private async Task<IEnumerable<Diario2ResponseAll>> GetDataAllAsync2(int max, string where, List<SqlParameter>? parameters, string uri, CancellationToken token)
    {
        using var scope = _connectionService.CreateConnectionScope(uri);
        using var oCnn = scope.Connection;
        if (oCnn == null)
        {
            throw new DatabaseConnectionException();
        }

        var result = await reader.ListarAsync(max, uri, where, parameters, string.Empty, token);
        return result;
    }
}