// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Source:SOLIDGridsEFormsTSGenerator

// GridsDesktopHoook.tsx - Versão Refatorada
"use client";
import React, { useMemo, useCallback, useState, useEffect } from "react";
import {
  Grid,
  GridColumn,
  GridSortChangeEvent,
  GridColumnReorderEvent,
  GridToolbar,
} from "@progress/kendo-react-grid";
import { useRouter } from "next/navigation";
import { useAppSelector } from "@/app/store/hooks";
import { selectSystemContext } from "@/app/store/slices/systemContextSlice";
import { ICidade } from "../../Interfaces/interface.Cidade";
import {
  CRUD_CONSTANTS,
  formatCurrency,
  formatDateBrowser,
} from "@/app/tools/crud";
import { SvgIcon } from "@progress/kendo-react-common";
import { pencilIcon, searchIcon } from "@progress/kendo-svg-icons";
import { useHiddenColumns } from "@/app/hooks/useHiddenColumns";
import { GridColumnMenu } from "@/app/components/Cruds/GridColumnMenu";
import {
  getExportColumns,
  useExportToExcel,
} from "@/app/hooks/useExportToExcel";
import {
  getExportColumnsPdf,
  useExportToPdf,
} from "@/app/hooks/useExportToPdf";
import { orderBy } from "@progress/kendo-data-query";
import { ExportButtons } from "@/app/components/Cruds/DockerInc/ExportButtons";
import { useCidadeGrid } from "../../Hooks/hookCidadeGrid";
import { useIOSScrollFallback } from "@/app/tools/iosScrollFallback";
import "@/app/styles/grid-desktop-performance.css";
import { PageTitle } from "@/app/components/PageTitle";

interface CidadeGridProps {
  data: ICidade[];
  onRowClick: (cidade: ICidade) => void;
  onDeleteClick: (e: any) => void;
  toolbar: React.ReactNode;
  selectItem?: (item: ICidade) => void;
  setSelectedId: (id: number | null) => void;
}

export const CidadeGridDesktopComponent = React.memo(
  ({
    data,
    onRowClick,
    onDeleteClick,
    setSelectedId,
    toolbar,
    selectItem,
  }: CidadeGridProps) => {
    const router = useRouter();
    const systemContext = useAppSelector(selectSystemContext);
    const defaultHiddenColumns = [""];

    const [columnsOrder, setColumnsOrder] = useState<string[]>(["nome", "duf"]);

    useEffect(() => {
      try {
        const savedOrder = localStorage.getItem(btoa("cidade-columns-order"));
        if (savedOrder) {
          const parsedOrder = JSON.parse(savedOrder);
          if (Array.isArray(parsedOrder) && parsedOrder.length > 0) {
            setColumnsOrder(parsedOrder);
          }
        }
      } catch (error) {
        if (process.env.NEXT_PUBLIC_SHOW_LOG === "1")
          console.warn("Não foi possível carregar a ordem das colunas:", error);
      }
    }, []);

    const openSearchCellAdvogados = (id: number) => {
      router.push(`/pages/advogados/?cidade=${id}`);
    };

    const SearchFromCellAdvogados = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellAdvogados(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Advogados">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellAdvogadosMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellAdvogados(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Advogados">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellAgenda = (id: number) => {
      router.push(`/pages/agenda/?cidade=${id}`);
    };

    const SearchFromCellAgenda = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellAgenda(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Compromisso">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellAgendaMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellAgenda(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Compromisso">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellClientes = (id: number) => {
      router.push(`/pages/clientes/?cidade=${id}`);
    };

    const SearchFromCellClientes = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellClientes(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Clientes">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellClientesMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellClientes(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Clientes">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellClientesSocios = (id: number) => {
      router.push(`/pages/clientessocios/?cidade=${id}`);
    };

    const SearchFromCellClientesSocios = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellClientesSocios(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Clientes Socios">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellClientesSociosMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellClientesSocios(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Clientes Socios">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellColaboradores = (id: number) => {
      router.push(`/pages/colaboradores/?cidade=${id}`);
    };

    const SearchFromCellColaboradores = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellColaboradores(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Colaboradores">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellColaboradoresMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellColaboradores(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Colaboradores">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellDivisaoTribunal = (id: number) => {
      router.push(`/pages/divisaotribunal/?cidade=${id}`);
    };

    const SearchFromCellDivisaoTribunal = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellDivisaoTribunal(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Divisao Tribunal">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellDivisaoTribunalMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellDivisaoTribunal(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Divisao Tribunal">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellEscritorios = (id: number) => {
      router.push(`/pages/escritorios/?cidade=${id}`);
    };

    const SearchFromCellEscritorios = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellEscritorios(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Escritorios">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellEscritoriosMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellEscritorios(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Escritorios">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellForo = (id: number) => {
      router.push(`/pages/foro/?cidade=${id}`);
    };

    const SearchFromCellForo = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellForo(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Foro">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellForoMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellForo(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Foro">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellFuncionarios = (id: number) => {
      router.push(`/pages/funcionarios/?cidade=${id}`);
    };

    const SearchFromCellFuncionarios = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellFuncionarios(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Colaborador">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellFuncionariosMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellFuncionarios(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Colaborador">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const openSearchCellOponentes = (id: number) => {
      router.push(`/pages/oponentes/?cidade=${id}`);
    };

    const SearchFromCellOponentes = (props: any) => {
      return (
        <td>
          <div onClick={() => openSearchCellOponentes(props.dataItem.id)}>
            <span title="Pesquisar relacionados em Oponentes">
              <SvgIcon icon={searchIcon} />
            </span>
          </div>
        </td>
      );
    };

    const SearchFromCellOponentesMobile = (props: any) => {
      return (
        <div onClick={() => openSearchCellOponentes(props.dataItem.id)}>
          <span title="Pesquisar relacionados em Oponentes">
            <SvgIcon icon={searchIcon} />
          </span>
        </div>
      );
    };

    const {
      filteredData,
      sort,
      handleSortChange,
      page,
      handlePageChange,
      handleFilterChange,
      handleRowClick: hookHandleRowClick,
      RowNumberCell,
      DeleteRow,
    } = useCidadeGrid({
      data,
      onRowClick,
      onDeleteClick,
      setSelectedId,
      initialTake: CRUD_CONSTANTS.PAGINATION.DEFAULT_TAKE,
      useCustomSort: true,
    });

    const sortedData = useMemo(
      () => orderBy(filteredData, sort),
      [filteredData, sort],
    );

    const handleSortChangeCustom = useCallback(
      (event: GridSortChangeEvent) => {
        handleSortChange(event);
      },
      [handleSortChange],
    );

    const handleColumnReorder = useCallback((event: GridColumnReorderEvent) => {
      if (event.columns) {
        const newOrder = event.columns
          .filter((col: any) => col.field && columnsOrder.includes(col.field))
          .map((col: any) => col.field);
        setColumnsOrder((prevOrder) => {
          if (
            newOrder.length > 0 &&
            JSON.stringify(newOrder) !== JSON.stringify(prevOrder)
          ) {
            try {
              localStorage.setItem(
                btoa("cidade-columns-order"),
                JSON.stringify(newOrder),
              );
            } catch (error) {
              console.warn(
                "Não foi possível salvar a ordem das colunas:",
                error,
              );
            }
            return newOrder;
          }
          return prevOrder;
        });
      }
    }, []);

    const EditRow = useCallback(
      (e: any) => {
        return (
          <td>
            <span onClick={() => hookHandleRowClick(e)} title="Editar item">
              <SvgIcon icon={pencilIcon} />
            </span>
          </td>
        );
      },
      [hookHandleRowClick],
    );

    const basicColumnMap: Record<string, React.ReactElement> = useMemo(
      () => ({
        nome: (
          <GridColumn
            key="nome"
            field="nome"
            title="Nome"
            sortable={true}
            filterable={true}
          />
        ) /* Track G.12 */,

        id_edit_Advogados: (
          <GridColumn
            key="Advogados"
            field="Advogados"
            title="Advogados"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellAdvogados }}
          />
        ) /* Track G.03 */,

        id_edit_Agenda: (
          <GridColumn
            key="Agenda"
            field="Agenda"
            title="Compromisso"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellAgenda }}
          />
        ) /* Track G.03 */,

        id_edit_Clientes: (
          <GridColumn
            key="Clientes"
            field="Clientes"
            title="Clientes"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellClientes }}
          />
        ) /* Track G.03 */,

        id_edit_ClientesSocios: (
          <GridColumn
            key="ClientesSocios"
            field="ClientesSocios"
            title="Clientes Socios"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellClientesSocios }}
          />
        ) /* Track G.03 */,

        id_edit_Colaboradores: (
          <GridColumn
            key="Colaboradores"
            field="Colaboradores"
            title="Colaboradores"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellColaboradores }}
          />
        ) /* Track G.03 */,

        id_edit_DivisaoTribunal: (
          <GridColumn
            key="DivisaoTribunal"
            field="DivisaoTribunal"
            title="Divisao Tribunal"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellDivisaoTribunal }}
          />
        ) /* Track G.03 */,

        id_edit_Escritorios: (
          <GridColumn
            key="Escritorios"
            field="Escritorios"
            title="Escritorios"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellEscritorios }}
          />
        ) /* Track G.03 */,

        id_edit_Foro: (
          <GridColumn
            key="Foro"
            field="Foro"
            title="Foro"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellForo }}
          />
        ) /* Track G.03 */,

        id_edit_Funcionarios: (
          <GridColumn
            key="Funcionarios"
            field="Funcionarios"
            title="Colaborador"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellFuncionarios }}
          />
        ) /* Track G.03 */,

        id_edit_Oponentes: (
          <GridColumn
            key="Oponentes"
            field="Oponentes"
            title="Oponentes"
            width={"65px"}
            sortable={false}
            filterable={false}
            cells={{ data: SearchFromCellOponentes }}
          />
        ) /* Track G.03 */,

        duf: (
          <GridColumn
            key="duf"
            field="duf"
            title="UF"
            sortable={false}
            filterable={false}
          />
        ) /* Track G.04 */,
      }),
      [],
    );

    const baseGridColumns = useMemo(
      () => [
        <GridColumn
          format="{0:n0}"
          field="index"
          title="#"
          sortable={false}
          filterable={false}
          width="55px"
          cells={{ data: RowNumberCell }}
        />,

        <GridColumn
          format="{0:n0}"
          hidden={true}
          field="id"
          title="Código"
          sortable={true}
          filterable={true}
          width="55px"
        />,
        <GridColumn
          field="id_edit_Advogados"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Advogados"
          cells={{ data: SearchFromCellAdvogados }}
        />,
        <GridColumn
          field="id_edit_Agenda"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Compromisso"
          cells={{ data: SearchFromCellAgenda }}
        />,
        <GridColumn
          field="id_edit_Clientes"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Clientes"
          cells={{ data: SearchFromCellClientes }}
        />,
        <GridColumn
          field="id_edit_ClientesSocios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Clientes Socios"
          cells={{ data: SearchFromCellClientesSocios }}
        />,
        <GridColumn
          field="id_edit_Colaboradores"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Colaboradores"
          cells={{ data: SearchFromCellColaboradores }}
        />,
        <GridColumn
          field="id_edit_DivisaoTribunal"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Divisao Tribunal"
          cells={{ data: SearchFromCellDivisaoTribunal }}
        />,
        <GridColumn
          field="id_edit_Escritorios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Escritorios"
          cells={{ data: SearchFromCellEscritorios }}
        />,
        <GridColumn
          field="id_edit_Foro"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Foro"
          cells={{ data: SearchFromCellForo }}
        />,
        <GridColumn
          field="id_edit_Funcionarios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Colaborador"
          cells={{ data: SearchFromCellFuncionarios }}
        />,
        <GridColumn
          field="id_edit_Oponentes"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Oponentes"
          cells={{ data: SearchFromCellOponentes }}
        />,
      ],
      [RowNumberCell, EditRow, DeleteRow],
    );

    const { columnsState, initialized, handleColumnsStateChange } =
      useHiddenColumns({
        gridColumns: baseGridColumns,
        columnMap: basicColumnMap,
        systemContextId: systemContext?.Id,
        tableName: "cidade",
        defaultHiddenColumns,
      });

    useEffect(() => {
      const cleanup = useIOSScrollFallback({
        scrollSelector:
          ".grid-desktop-crud.grid-desktop-clientes .k-grid-content",
        waitForDOM: true,
        retryDelay: 100,
        debug: process.env.NEXT_PUBLIC_SHOW_LOG === "1",
      });

      return cleanup;
    }, []);

    const isColumnVisible = useCallback(
      (field: string) => {
        if (!columnsState) {
          return !defaultHiddenColumns.includes(field);
        }
        const columnState = columnsState.find((state) => state.field === field);
        return columnState ? !columnState.hidden : true;
      },
      [columnsState, defaultHiddenColumns],
    );

    const finalGridColumns = useMemo(() => {
      const finalColumns = [
        <GridColumn
          format="{0:n0}"
          field="index"
          title="#"
          sortable={false}
          filterable={false}
          width="55px"
          cells={{ data: RowNumberCell }}
        />,

        <GridColumn
          format="{0:n0}"
          hidden={true}
          field="id"
          title="Código"
          sortable={true}
          filterable={true}
          width="55px"
        />,
        <GridColumn
          field="id_edit_Advogados"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Advogados"
          cells={{ data: SearchFromCellAdvogados }}
        />,
        <GridColumn
          field="id_edit_Agenda"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Compromisso"
          cells={{ data: SearchFromCellAgenda }}
        />,
        <GridColumn
          field="id_edit_Clientes"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Clientes"
          cells={{ data: SearchFromCellClientes }}
        />,
        <GridColumn
          field="id_edit_ClientesSocios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Clientes Socios"
          cells={{ data: SearchFromCellClientesSocios }}
        />,
        <GridColumn
          field="id_edit_Colaboradores"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Colaboradores"
          cells={{ data: SearchFromCellColaboradores }}
        />,
        <GridColumn
          field="id_edit_DivisaoTribunal"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Divisao Tribunal"
          cells={{ data: SearchFromCellDivisaoTribunal }}
        />,
        <GridColumn
          field="id_edit_Escritorios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Escritorios"
          cells={{ data: SearchFromCellEscritorios }}
        />,
        <GridColumn
          field="id_edit_Foro"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Foro"
          cells={{ data: SearchFromCellForo }}
        />,
        <GridColumn
          field="id_edit_Funcionarios"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Colaborador"
          cells={{ data: SearchFromCellFuncionarios }}
        />,
        <GridColumn
          field="id_edit_Oponentes"
          filterable={false}
          sortable={false}
          width={"65px"}
          title="Oponentes"
          cells={{ data: SearchFromCellOponentes }}
        />,

        ...columnsOrder
          .map((field) => basicColumnMap[field])
          .filter(Boolean)
          .map((column) => {
            const props = (column as React.ReactElement<any>).props;
            return React.cloneElement(column, {
              ...props,
              hidden: !isColumnVisible(props.field),
            });
          }),

        <GridColumn
          field="id_edit_row"
          width={"55px"}
          title="Editar registro"
          sortable={false}
          filterable={false}
          cells={{ data: EditRow }}
          reorderable={false}
        />,
        <GridColumn
          key="delete"
          field="id_delete_row"
          width={"55px"}
          title="Excluir registro"
          sortable={false}
          filterable={false}
          cells={{ data: DeleteRow }}
          reorderable={false}
        />,
      ];

      return finalColumns;
    }, [
      columnsOrder,
      isColumnVisible,
      RowNumberCell,
      EditRow,
      DeleteRow,
      basicColumnMap,
    ]);

    const columnMenuComponent = GridColumnMenu({
      columnsState,
      onColumnsStateChange: handleColumnsStateChange,
    });

    const exportColumns = getExportColumns(finalGridColumns, columnsState, [
      "id",
      "index",
    ]);
    const { exportToExcel } = useExportToExcel({
      filename: "planilha-cidade",
      columns: exportColumns,
      sheetName: "Cidade",
    });

    const handleExportFiltered = useCallback(() => {
      exportToExcel(filteredData, {
        customFilename: `excel-Cidade-${new Date().toISOString().replace("T", "_").substring(0, 16).replace(":", "-")}`,
      });
    }, [exportToExcel, filteredData]);

    const exportColumnsPdf = getExportColumnsPdf(
      finalGridColumns,
      columnsState,
      ["id", "index"],
    );
    const { exportToPdf } = useExportToPdf({
      filename: "pdf-cidade",
      columns: exportColumnsPdf,
      title: "Cidade",
    });

    const handleExportFilteredPdf = useCallback(() => {
      exportToPdf(filteredData, {
        customFilename: `pdf-Cidade-${new Date().toISOString().replace("T", "_").substring(0, 16).replace(":", "-")}`,
      });
    }, [exportToPdf, filteredData]);

    const paginatedSortedData = useMemo(() => {
      if (!sortedData || sortedData.length === 0) {
        return [];
      }
      return sortedData.slice(page.skip, page.skip + page.take);
    }, [sortedData, page.skip, page.take]);

    return (
      <>
        {initialized && (
          <Grid
            key={`cidade-grid-${JSON.stringify(columnsOrder)}-${JSON.stringify(columnsState)}`}
            columnMenu={columnMenuComponent}
            className="grid-desktop-crud grid-desktop-cidade"
            data={paginatedSortedData}
            skip={page.skip}
            take={page.take}
            total={sortedData.length}
            pageable={{
              pageSizes: Array.from(CRUD_CONSTANTS.PAGINATION.PAGE_SIZES),
              buttonCount: CRUD_CONSTANTS.PAGINATION.BUTTON_COUNT,
            }}
            onPageChange={handlePageChange}
            rowReorderable={true}
            sortable={true}
            sort={sort}
            onSortChange={handleSortChangeCustom}
            resizable={true}
            reorderable={true}
            onColumnReorder={handleColumnReorder}
            filterable={true}
            onFilterChange={handleFilterChange}
            onRowDoubleClick={(e) => {
              if (selectItem) {
                selectItem(e.dataItem as ICidade);

                return;
              }
              hookHandleRowClick(e);
            }}
            scrollable="scrollable"
          >
            <GridToolbar>
              {toolbar}

              <PageTitle title="Cidade" />
            </GridToolbar>
            {finalGridColumns}
          </Grid>
        )}
      </>
    );
  },
);
