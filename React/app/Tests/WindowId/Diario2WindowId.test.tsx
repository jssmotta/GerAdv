// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import Diario2WindowId from '@/app/GerAdv_TS/Diario2/Crud/Grids/Diario2WindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import Diario2WindowId from '@/app/GerAdv_TS/Diario2/Crud/Grids/Diario2WindowId';
import { Diario2Empty } from '@/app/GerAdv_TS/Models/Diario2';

describe('Diario2WindowId (basic)', () => {
  it('exports the component', () => {
    expect(Diario2WindowId).toBeDefined();
    expect(typeof Diario2WindowId === 'function' || typeof Diario2WindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the Diario2Service class so constructor usage doesn't throw and we can control fetchDiario2ById
const mockFetchDiario2ById = jest.fn();
jest.mock('@/app/GerAdv_TS/Diario2/Services/Diario2.service', () => ({
  Diario2Service: jest.fn().mockImplementation(() => ({
    fetchDiario2ById: (...args: any[]) => mockFetchDiario2ById(...args),
  })),
}));

// Mock the child Diario2Window to capture props passed to it
jest.mock('@/app/GerAdv_TS/Diario2/Crud/Grids/Diario2Window', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='diario2-window' {...p} />,
}));

describe('Diario2WindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(Diario2WindowId).toBeDefined();
    expect(typeof Diario2WindowId === 'function' || typeof Diario2WindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <Diario2WindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders Diario2Window with Diario2Empty when id is 0', async () => {
    render(
      <Diario2WindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // Diario2Window should be rendered with selectedDiario2 equal to Diario2Empty()
    const node = await screen.findByTestId('diario2-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedDiario2"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selecteddiario2') || node.getAttribute('selectedDiario2')).not.toBeNull();
  });

  it('fetches diario2 by id and passes it to Diario2Window when id > 0', async () => {
    const fakeDiario2 = { id: 123, nomecliente: 'Test' };
    mockFetchDiario2ById.mockResolvedValue(fakeDiario2);

    render(
      <Diario2WindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('diario2-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchDiario2ById).toHaveBeenCalledWith(123));
  });
});
