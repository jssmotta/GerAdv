// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Page.test.tsx.txt
// Test for ProCDA page
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import { jest, describe, test, beforeAll, afterAll, expect } from '@jest/globals';

// Mock PageTitle and PageLayout to keep test focused
jest.mock('@/app/components/PageTitle', () => ({
  PageTitle: ({ title }: any) => <h1>{title}</h1>
}));

jest.mock('@/app/components/Cruds/PageLayout', () => ({
  PageLayout: ({ children }: any) => <div data-testid='page-layout'>{children}</div>
}));

// Mock ProCDAGridAdapter to avoid instantiation complexity
jest.mock('@/app/GerAdv_TS/ProCDA/Adapter/ProCDAGridAdapter', () => ({
  ProCDAGridAdapter: jest.fn().mockImplementation(() => ({
    render: () => <div data-testid='mock-grid'>Mock Grid</div>
  }))
}));

// Mock ProCDAGridContainer to assert it receives the mocked grid
jest.mock('@/app/GerAdv_TS/ProCDA/Components/ProCDAGridContainer', () => ({
  __esModule: true,
  default: ({ grid }: any) => (
    <div data-testid='procda-container'>{grid?.render ? grid.render() : null}</div>
  )
}));

// Import the page after mocks so imports pick them up
import ProCDAPage from '@/app/pages/procda/page';

describe('ProCDA page', () => {
  beforeAll(() => {
    jest.spyOn(console, 'error').mockImplementation(() => {});
  });

  afterAll(() => {
    (console.error as any).mockRestore?.();
  });

  test('renders PageTitle and ProCDAGridContainer', () => {
    render(<ProCDAPage />);

    expect(screen.getByRole('heading', { name: /Pro C D A/i })).toBeInTheDocument();
    expect(screen.getByTestId('procda-container')).toBeInTheDocument();
    expect(screen.getByTestId('mock-grid')).toBeInTheDocument();
  });
});

