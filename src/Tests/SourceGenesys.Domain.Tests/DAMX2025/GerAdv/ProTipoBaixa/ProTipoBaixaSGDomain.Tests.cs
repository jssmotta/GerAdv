// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2026 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project
// Tabela:ProTipoBaixa
// Fonte:SGDomainTests
/// <summary>
/// Template de testes unitários para classes geradas pelo Source Genesys
/// Variáveis de substituição:
/// MenphisSI.GerAdv - Namespace da classe
/// DBProTipoBaixa - Nome da classe (ex: DBClientes)
/// ProTipoBaixa - Nome da tabela (ex: Clientes)
/// ptx - Prefixo dos campos (ex: cli)
/// ptxCodigo - Nome do campo código (ex: cliCodigo)
/// ptxNome - Nome do campo nome (ex: cliNome)
/// DBProTipoBaixa.CadastroGuid - GUID do cadastro
/// </summary>
namespace MenphisSI.GerAdv.Tests;
public class DBProTipoBaixaTests : IDisposable
{
    private readonly DBProTipoBaixa _instance;
    private readonly Mock<MsiSqlConnection> _mockConnection;
    private readonly DataTable _testDataTable;
    public DBProTipoBaixaTests()
    {
        _instance = new DBProTipoBaixa();
        _mockConnection = new Mock<MsiSqlConnection>();
        _testDataTable = CreateTestDataTable();
    }

    private DataTable CreateTestDataTable()
    {
        var dt = new DataTable();
        // Campos obrigatórios Source Genesys
        dt.Columns.Add("ptxCodigo", typeof(int));
        dt.Columns.Add("ptxQuemCad", typeof(int));
        dt.Columns.Add("ptxDtCad", typeof(DateTime));
        dt.Columns.Add("ptxQuemAtu", typeof(int));
        dt.Columns.Add("ptxDtAtu", typeof(DateTime));
        dt.Columns.Add("ptxVisto", typeof(bool));
        dt.Columns.Add("ptxNome", typeof(string));
        dt.Columns.Add("ptxBold", typeof(string));
        dt.Columns.Add("ptxGuid", typeof(string));
        return dt;
    }

    [Fact]
    public void Constructor_WithValidDataRow_ShouldLoadData()
    {
        // Arrange
        var row = _testDataTable.NewRow();
        row["ptxCodigo"] = 123;
        _testDataTable.Rows.Add(row);
        // Act
        var instance = new DBProTipoBaixa(_testDataTable.Rows[0]);
        // Assert
        Assert.Equal(123, instance.ID);
    }

#region Testes de Constantes e Propriedades Estáticas
    [Fact]
    public void Constants_ShouldHaveCorrectValues()
    {
        Assert.Equal("ProTipoBaixa", DBProTipoBaixa.PTabelaNome);
        Assert.Equal(" ProTipoBaixa.* ", DBProTipoBaixa.CamposSqlX);
        Assert.Equal("ptxCodigo", DBProTipoBaixa.CampoCodigo);
        Assert.Equal("ptxNome", DBProTipoBaixa.CampoNome);
        Assert.Equal("ptx", DBProTipoBaixa.PTabelaPrefixo);
    }

#endregion
#region Testes de Construtores
    [Fact]
    public void Constructor_Default_ShouldInitializeCorrectly()
    {
        var instance = new DBProTipoBaixa();
        Assert.Equal(0, instance.ID);
        Assert.Equal("ProTipoBaixa", instance.ITableName());
        Assert.Equal("ptx", instance.Prefixo);
    }

    [Fact]
    public void Constructor_WithNullDataRow_ShouldNotThrow()
    {
        var instance = new DBProTipoBaixa((DataRow)null !);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public void Constructor_WithNullSqlDataReader_ShouldNotThrow()
    {
        var instance = new DBProTipoBaixa((SqlDataReader)null !);
        Assert.Equal(0, instance.ID);
    }

#endregion
#region Testes de Interfaces
    [Fact]
    public void ICrud_Implementation_ShouldWork()
    {
        ICrud cadastro = (ICrud)_instance;
        Assert.Equal("ProTipoBaixa", cadastro.ITableName());
        Assert.Equal("ptxCodigo", cadastro.IFieldId());
        Assert.Equal("ptxNome", cadastro.IFieldNameDescription());
        Assert.Equal("ptx", cadastro.IPrefix());
    }

#endregion
#region Testes de Métodos Assíncronos
    [Fact]
    public async Task CreateAsync_WithInvalidId_ShouldReturnEmptyInstance()
    {
        var instance = await DBProTipoBaixa.CreateAsync(0, null);
        Assert.Equal(0, instance.ID);
    }

    [Fact]
    public async Task ExisteAsync_WithNullConnection_ShouldReturnFalse()
    {
        var exists = await DBProTipoBaixa.ExisteAsync(1, null);
        Assert.False(exists);
    }

    [Fact]
    public async Task DeleteRecordAsync_WithInvalidId_ShouldReturnFalse()
    {
        var result = await DBProTipoBaixa.DeleteRecordAsync(1, 0, null);
        Assert.False(result);
    }

#endregion
#region Testes de Dispose
    [Fact]
    public void Dispose_ShouldNotThrow()
    {
        var instance = new DBProTipoBaixa();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

    [Fact]
    public void Dispose_MultipleCalls_ShouldBeIdempotent()
    {
        var instance = new DBProTipoBaixa();
        instance.Dispose();
        var exception = Record.Exception(() => instance.Dispose());
        Assert.Null(exception);
    }

#endregion
#region Testes de Propriedades Computadas
    [Fact]
    public void HasAuditor_ShouldReturnTrue()
    {
        Assert.True(_instance.HasAuditor());
    }

    [Fact]
    public void HasNameId_ShouldReturnTrue()
    {
        Assert.True(_instance.HasNameId());
    }

    [Fact]
    public void IsStoredProcedureOrView_ShouldReturnFalse()
    {
        Assert.False(_instance.IsStoredProcedureOrView());
    }

#endregion
    [Theory]
    [InlineData("", "")]
    [InlineData(null, "")]
    [InlineData("  Teste  ", "Teste")]
    public void Nome_ShouldTrimAndHandleNulls(string? input, string expected)
    {
        _instance.FNome = input;
        Assert.Equal(expected, _instance.FNome);
    }

    [Fact]
    public void Nome_ShouldRespectMaxLength()
    {
        var longString = new string ('A', 80 + 10);
        _instance.FNome = longString;
        Assert.True(_instance.FNome.Length <= 80);
    }

    public virtual void Dispose()
    {
        _instance?.Dispose();
    }
}