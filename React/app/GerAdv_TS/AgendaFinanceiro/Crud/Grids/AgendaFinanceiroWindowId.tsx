// 100% auto-generated by Source Genesys WebApi Front & Back Creator
                     // copyright © 2000-2025 Menphis - Sistemas Inteligentes
                     // This file is part of the Source Genesys project                     
                    // WindowId.tsx.txt
import React, { useEffect, useMemo } from 'react';
import { useSystemContext } from '@/app/context/SystemContext';
import { IAgendaFinanceiro } from '../../Interfaces/interface.AgendaFinanceiro';
import { AgendaFinanceiroService } from '../../Services/AgendaFinanceiro.service';
import { AgendaFinanceiroApi } from '../../Apis/ApiAgendaFinanceiro';
import AgendaFinanceiroWindow from './AgendaFinanceiroWindow';
import {AgendaFinanceiroEmpty } from '@/app/GerAdv_TS/Models/AgendaFinanceiro';
interface AgendaFinanceiroWindowIdProps {
  isOpen: boolean;
  onClose: () => void;
  id?: number;
  onSuccess: (registro?: any) => void;
  onError: () => void;
}
const AgendaFinanceiroWindowId: React.FC<AgendaFinanceiroWindowIdProps> = ({
  isOpen, 
  onClose, 
  id, 
  onSuccess, 
  onError, 
}) => {
const { systemContext } = useSystemContext();
const agendafinanceiroService = useMemo(() => {
  return new AgendaFinanceiroService(
  new AgendaFinanceiroApi(systemContext?.Uri ?? '', systemContext?.Token ?? '')
);
}, [systemContext?.Uri, systemContext?.Token]);
const [data, setData] = React.useState<IAgendaFinanceiro | null>(null);
useEffect(() => {
  const fetchData = async () => {
    if (id !== null && id === 0) {
      setData(AgendaFinanceiroEmpty() as IAgendaFinanceiro);
      return;
    }
    if (id) {
      const response = await agendafinanceiroService.fetchAgendaFinanceiroById(id??0);
      setData(response);
    }
  };
  fetchData();
}, [isOpen]);

if (!isOpen)
  return null;
  return (
  <>
  {data && (
    <AgendaFinanceiroWindow
    isOpen={isOpen}
    onClose={onClose}
    selectedAgendaFinanceiro={data}
    onSuccess={onSuccess}
    onError={onError} />
    )}
  </>
);
};
export default AgendaFinanceiroWindowId;