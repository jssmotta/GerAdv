// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useAlertasForm, useAlertasList, useValidationsAlertas } from '../GerAdv_TS/Alertas/Hooks/hookAlertas';
import { IAlertas } from '../GerAdv_TS/Alertas/Interfaces/interface.Alertas';
import { IAlertasService } from '../GerAdv_TS/Alertas/Services/Alertas.service';
import { AlertasTestEmpty } from '../GerAdv_TS/Models/Alertas';
import { useAlertasComboBox } from '../GerAdv_TS/Alertas/Hooks/hookAlertas';

// Mock do serviço
const mockAlertasService: jest.Mocked<IAlertasService> = {
  fetchAlertasById: jest.fn(),
  saveAlertas: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteAlertas: jest.fn(),
  validateAlertas: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialAlertas: IAlertas = { ...AlertasTestEmpty() };

describe('useAlertasForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    expect(result.current.data).toEqual(initialAlertas);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Alertas',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Alertas');
  });

   test('deve carregar Alertas por ID', async () => {
    const mockAlertas = { ...initialAlertas, id: 1, nome: 'Alertas Teste' };
    mockAlertasService.fetchAlertasById.mockResolvedValue(mockAlertas);

    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    await act(async () => {
      await result.current.loadAlertas(1);
    });

    expect(mockAlertasService.fetchAlertasById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockAlertas);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Alertas', async () => {
    const errorMessage = 'Erro ao carregar Alertas';
    mockAlertasService.fetchAlertasById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    await act(async () => {
      await result.current.loadAlertas(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialAlertas, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialAlertas);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useAlertasForm(initialAlertas, mockAlertasService)
    );

    await act(async () => {
      await result.current.loadAlertas(0);
    });

    expect(mockAlertasService.fetchAlertasById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialAlertas);
  });
});

describe('useAlertasList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAlertasList(mockAlertasService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialAlertas, id: 1, nome: 'Alertas 1' },
      { ...initialAlertas, id: 2, nome: 'Alertas 2' }
    ];
    mockAlertasService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAlertasList(mockAlertasService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockAlertasService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockAlertasService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useAlertasList(mockAlertasService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialAlertas, id: 1, nome: 'Alertas Filtrado' }];
    const filtro = { nome: 'Alertas' };
    mockAlertasService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useAlertasList(mockAlertasService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockAlertasService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsAlertas', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsAlertas());

    const validData = { ...initialAlertas, nome: 'Alertas Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsAlertas());

    const invalidData = { ...initialAlertas, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsAlertas());

    const invalidData = { 
      ...initialAlertas, 
      nome: 'a'.repeat(2048+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 2048 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsAlertas());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialAlertas, id: 1, nome: 'Alertas Teste' }];
    mockAlertasService.getAll.mockResolvedValue(mockData);
    mockAlertasService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useAlertasList(mockAlertasService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsAlertas()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Alertas Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Alertas Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Alertas 1' },
      { id: 2, nome: 'Alertas 2' }
    ];
    mockAlertasService.getList.mockResolvedValue(mockOptions as IAlertas[]);


    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alertas 1' },
        { id: 2, nome: 'Alertas 2' }
      ]);
    });


    expect(mockAlertasService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Alertas ABC' },
      { id: 2, nome: 'Alertas XYZ' }
    ];
    mockAlertasService.getList.mockResolvedValue(mockOptions as IAlertas[]);   


 const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alertas ABC' },
        { id: 2, nome: 'Alertas XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Alertas ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Alertas ABC' },
      { id: 2, nome: 'Alertas XYZ' }
    ];
    mockAlertasService.getList.mockResolvedValue(mockOptions as IAlertas[]);
  


    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Alertas ABC' },
        { id: 2, nome: 'Alertas XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Alertas ABC' },
          {id: 2, nome: 'Alertas XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );

    const newValue = { id: 1, nome: 'Alertas Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Alertas Inicial' };
    
    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useAlertasComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Alertas Inicial' };
    
    const { result } = renderHook(() => 
      useAlertasComboBox(mockAlertasService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






