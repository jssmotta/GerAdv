// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PontoVirtualAcessosWindowId from '@/app/GerAdv_TS/PontoVirtualAcessos/Crud/Grids/PontoVirtualAcessosWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PontoVirtualAcessosWindowId from '@/app/GerAdv_TS/PontoVirtualAcessos/Crud/Grids/PontoVirtualAcessosWindowId';
import { PontoVirtualAcessosEmpty } from '@/app/GerAdv_TS/Models/PontoVirtualAcessos';

describe('PontoVirtualAcessosWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PontoVirtualAcessosWindowId).toBeDefined();
    expect(typeof PontoVirtualAcessosWindowId === 'function' || typeof PontoVirtualAcessosWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PontoVirtualAcessosService class so constructor usage doesn't throw and we can control fetchPontoVirtualAcessosById
const mockFetchPontoVirtualAcessosById = jest.fn();
jest.mock('@/app/GerAdv_TS/PontoVirtualAcessos/Services/PontoVirtualAcessos.service', () => ({
  PontoVirtualAcessosService: jest.fn().mockImplementation(() => ({
    fetchPontoVirtualAcessosById: (...args: any[]) => mockFetchPontoVirtualAcessosById(...args),
  })),
}));

// Mock the child PontoVirtualAcessosWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/PontoVirtualAcessos/Crud/Grids/PontoVirtualAcessosWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='pontovirtualacessos-window' {...p} />,
}));

describe('PontoVirtualAcessosWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PontoVirtualAcessosWindowId).toBeDefined();
    expect(typeof PontoVirtualAcessosWindowId === 'function' || typeof PontoVirtualAcessosWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PontoVirtualAcessosWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PontoVirtualAcessosWindow with PontoVirtualAcessosEmpty when id is 0', async () => {
    render(
      <PontoVirtualAcessosWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PontoVirtualAcessosWindow should be rendered with selectedPontoVirtualAcessos equal to PontoVirtualAcessosEmpty()
    const node = await screen.findByTestId('pontovirtualacessos-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPontoVirtualAcessos"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedpontovirtualacessos') || node.getAttribute('selectedPontoVirtualAcessos')).not.toBeNull();
  });

  it('fetches pontovirtualacessos by id and passes it to PontoVirtualAcessosWindow when id > 0', async () => {
    const fakePontoVirtualAcessos = { id: 123, nomecliente: 'Test' };
    mockFetchPontoVirtualAcessosById.mockResolvedValue(fakePontoVirtualAcessos);

    render(
      <PontoVirtualAcessosWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('pontovirtualacessos-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPontoVirtualAcessosById).toHaveBeenCalledWith(123));
  });
});
