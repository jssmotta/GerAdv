// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { useFuncaoForm, useFuncaoList, useValidationsFuncao } from '../GerAdv_TS/Funcao/Hooks/hookFuncao';
import { IFuncao } from '../GerAdv_TS/Funcao/Interfaces/interface.Funcao';
import { IFuncaoService } from '../GerAdv_TS/Funcao/Services/Funcao.service';
import { FuncaoTestEmpty } from '../GerAdv_TS/Models/Funcao';
import { useFuncaoComboBox } from '../GerAdv_TS/Funcao/Hooks/hookFuncao';

// Mock do serviço
const mockFuncaoService: jest.Mocked<IFuncaoService> = {
  fetchFuncaoById: jest.fn(),
  saveFuncao: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deleteFuncao: jest.fn(),
  validateFuncao: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialFuncao: IFuncao = { ...FuncaoTestEmpty() };

describe('useFuncaoForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    expect(result.current.data).toEqual(initialFuncao);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    const mockEvent = {
      target: {
        name: 'descricao',
        value: 'Novo Função',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.descricao).toBe('Novo Função');
  });

   test('deve carregar Função por ID', async () => {
    const mockFuncao = { ...initialFuncao, id: 1, descricao: 'Função Teste' };
    mockFuncaoService.fetchFuncaoById.mockResolvedValue(mockFuncao);

    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    await act(async () => {
      await result.current.loadFuncao(1);
    });

    expect(mockFuncaoService.fetchFuncaoById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockFuncao);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Função', async () => {
    const errorMessage = 'Erro ao carregar Função';
    mockFuncaoService.fetchFuncaoById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    await act(async () => {
      await result.current.loadFuncao(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialFuncao, descricao: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialFuncao);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      useFuncaoForm(initialFuncao, mockFuncaoService)
    );

    await act(async () => {
      await result.current.loadFuncao(0);
    });

    expect(mockFuncaoService.fetchFuncaoById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialFuncao);
  });
});

describe('useFuncaoList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useFuncaoList(mockFuncaoService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialFuncao, id: 1, descricao: 'Função 1' },
      { ...initialFuncao, id: 2, descricao: 'Função 2' }
    ];
    mockFuncaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useFuncaoList(mockFuncaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockFuncaoService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockFuncaoService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      useFuncaoList(mockFuncaoService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialFuncao, id: 1, descricao: 'Função Filtrado' }];
    const filtro = { descricao: 'Função' };
    mockFuncaoService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      useFuncaoList(mockFuncaoService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockFuncaoService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsFuncao', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsFuncao());

    const validData = { ...initialFuncao, descricao: 'Função Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar descricao vazio', () => {
    const { result } = renderHook(() => useValidationsFuncao());

    const invalidData = { ...initialFuncao, descricao: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ficar vazio.');
  });

  
  test('deve invalidar descricao muito longo', () => {
    const { result } = renderHook(() => useValidationsFuncao());

    const invalidData = { 
      ...initialFuncao, 
      descricao: 'a'.repeat(40+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Descricao não pode ter mais de 40 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsFuncao());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialFuncao, id: 1, descricao: 'Função Teste' }];
    mockFuncaoService.getAll.mockResolvedValue(mockData);
    mockFuncaoService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      useFuncaoList(mockFuncaoService)
    );
    
     const { result: comboResult } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsFuncao()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Função Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Função Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Função 1' },
      { id: 2, descricao: 'Função 2' }
    ];
    mockFuncaoService.getList.mockResolvedValue(mockOptions as IFuncao[]);


    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Função 1' },
        { id: 2, nome: 'Função 2' }
      ]);
    });


    expect(mockFuncaoService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Função ABC' },
      { id: 2, descricao: 'Função XYZ' }
    ];
    mockFuncaoService.getList.mockResolvedValue(mockOptions as IFuncao[]);   


 const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Função ABC' },
        { id: 2, nome: 'Função XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Função ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, descricao: 'Função ABC' },
      { id: 2, descricao: 'Função XYZ' }
    ];
    mockFuncaoService.getList.mockResolvedValue(mockOptions as IFuncao[]);
  


    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Função ABC' },
        { id: 2, nome: 'Função XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Função ABC' },
          {id: 2, nome: 'Função XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );

    const newValue = { id: 1, descricao: 'Função Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, descricao: 'Função Inicial' };
    
    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('useFuncaoComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, descricao: 'Função Inicial' };
    
    const { result } = renderHook(() => 
      useFuncaoComboBox(mockFuncaoService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






