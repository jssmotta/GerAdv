// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// WindowId.test.tsx.txt
import PenhoraWindowId from '@/app/GerAdv_TS/Penhora/Crud/Grids/PenhoraWindowId';
import React from 'react';
import { render, screen, waitFor } from '@testing-library/react';
import PenhoraWindowId from '@/app/GerAdv_TS/Penhora/Crud/Grids/PenhoraWindowId';
import { PenhoraEmpty } from '@/app/GerAdv_TS/Models/Penhora';

describe('PenhoraWindowId (basic)', () => {
  it('exports the component', () => {
    expect(PenhoraWindowId).toBeDefined();
    expect(typeof PenhoraWindowId === 'function' || typeof PenhoraWindowId === 'object').toBe(true);
  });
});



// Mock system context used by the component
const mockUseSystemContext = jest.fn();
jest.mock('@/app/context/SystemContext', () => ({
  useSystemContext: () => mockUseSystemContext(),
}));

// Mock the PenhoraService class so constructor usage doesn't throw and we can control fetchPenhoraById
const mockFetchPenhoraById = jest.fn();
jest.mock('@/app/GerAdv_TS/Penhora/Services/Penhora.service', () => ({
  PenhoraService: jest.fn().mockImplementation(() => ({
    fetchPenhoraById: (...args: any[]) => mockFetchPenhoraById(...args),
  })),
}));

// Mock the child PenhoraWindow to capture props passed to it
jest.mock('@/app/GerAdv_TS/Penhora/Crud/Grids/PenhoraWindow', () => ({
  __esModule: true,
  default: (p: any) => <div data-testid='penhora-window' {...p} />,
}));

describe('PenhoraWindowId', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockUseSystemContext.mockReturnValue({ systemContext: { Uri: 'u', Token: 't' } });
  });

  it('is defined and is a component', () => {
    expect(PenhoraWindowId).toBeDefined();
    expect(typeof PenhoraWindowId === 'function' || typeof PenhoraWindowId === 'object').toBe(true);
  });

  it('returns null (renders nothing) when isOpen is false', () => {
    const { container } = render(
      <PenhoraWindowId isOpen={false} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders PenhoraWindow with PenhoraEmpty when id is 0', async () => {
    render(
      <PenhoraWindowId isOpen={true} id={0} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // PenhoraWindow should be rendered with selectedPenhora equal to PenhoraEmpty()
    const node = await screen.findByTestId('penhora-window');
    expect(node).toBeInTheDocument();
    // prop exists on the mocked node as an attribute named "selectedPenhora"
    // React testing library renders object props as [object Object] when stringified; assert presence
    expect(node.getAttribute('selectedpenhora') || node.getAttribute('selectedPenhora')).not.toBeNull();
  });

  it('fetches penhora by id and passes it to PenhoraWindow when id > 0', async () => {
    const fakePenhora = { id: 123, nomecliente: 'Test' };
    mockFetchPenhoraById.mockResolvedValue(fakePenhora);

    render(
      <PenhoraWindowId isOpen={true} id={123} onClose={() => {}} onSuccess={() => {}} onError={() => {}} />
    );

    // wait for child to be rendered after async fetch
    const node = await screen.findByTestId('penhora-window');
    expect(node).toBeInTheDocument();
    // ensure our mock was called with the id
    await waitFor(() => expect(mockFetchPenhoraById).toHaveBeenCalledWith(123));
  });
});
