// 100% auto-generated by Source Genesys WebApi Front & Back Creator
// copyright © 2000-2025 Menphis - Sistemas Inteligentes
// This file is part of the Source Genesys project                     
// Tests.tsx.txt
import { renderHook, act, waitFor } from '@testing-library/react';
import { jest, describe, beforeEach, test, expect } from '@jest/globals';
import { usePenhoraStatusForm, usePenhoraStatusList, useValidationsPenhoraStatus } from '../GerAdv_TS/PenhoraStatus/Hooks/hookPenhoraStatus';
import { IPenhoraStatus } from '../GerAdv_TS/PenhoraStatus/Interfaces/interface.PenhoraStatus';
import { IPenhoraStatusService } from '../GerAdv_TS/PenhoraStatus/Services/PenhoraStatus.service';
import { PenhoraStatusTestEmpty } from '../GerAdv_TS/Models/PenhoraStatus';
import { usePenhoraStatusComboBox } from '../GerAdv_TS/PenhoraStatus/Hooks/hookPenhoraStatus';

// Mock do serviço
const mockPenhoraStatusService: jest.Mocked<IPenhoraStatusService> = {
  fetchPenhoraStatusById: jest.fn(),
  savePenhoraStatus: jest.fn(),
  getList: jest.fn(),
  getAll: jest.fn(),
  deletePenhoraStatus: jest.fn(),
  validatePenhoraStatus: jest.fn(),
};

beforeAll(() => {
  jest.spyOn(console, 'log').mockImplementation(() => {});
  jest.spyOn(console, 'error').mockImplementation(() => {});
});

// Mock dos dados iniciais
const initialPenhoraStatus: IPenhoraStatus = { ...PenhoraStatusTestEmpty() };

describe('usePenhoraStatusForm', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com dados corretos', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    expect(result.current.data).toEqual(initialPenhoraStatus);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve atualizar dados com handleChange', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    const mockEvent = {
      target: {
        name: 'nome',
        value: 'Novo Penhora Status',
        type: 'text',
        checked: false
      }
    };

    act(() => {
      result.current.handleChange(mockEvent);
    });

    expect(result.current.data.nome).toBe('Novo Penhora Status');
  });

   test('deve carregar Penhora Status por ID', async () => {
    const mockPenhoraStatus = { ...initialPenhoraStatus, id: 1, nome: 'Penhora Status Teste' };
    mockPenhoraStatusService.fetchPenhoraStatusById.mockResolvedValue(mockPenhoraStatus);

    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.loadPenhoraStatus(1);
    });

    expect(mockPenhoraStatusService.fetchPenhoraStatusById).toHaveBeenCalledWith(1);
    expect(result.current.data).toEqual(mockPenhoraStatus);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro ao carregar Penhora Status', async () => {
    const errorMessage = 'Erro ao carregar Penhora Status';
    mockPenhoraStatusService.fetchPenhoraStatusById.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.loadPenhoraStatus(1);
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve resetar formulário', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    // Primeiro, modifica os dados
    act(() => {
      result.current.setData({ ...initialPenhoraStatus, nome: 'Teste' });
    });

    // Depois reseta
    act(() => {
      result.current.resetForm();
    });

    expect(result.current.data).toEqual(initialPenhoraStatus);
    expect(result.current.error).toBe(null);
  });

  test('não deve carregar quando ID é 0', async () => {
    const { result } = renderHook(() => 
      usePenhoraStatusForm(initialPenhoraStatus, mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.loadPenhoraStatus(0);
    });

    expect(mockPenhoraStatusService.fetchPenhoraStatusById).not.toHaveBeenCalled();
    expect(result.current.data).toEqual(initialPenhoraStatus);
  });
});

describe('usePenhoraStatusList', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusList(mockPenhoraStatusService)
    );

    expect(result.current.data).toEqual([]);
    expect(result.current.loading).toBe(false);
    expect(result.current.error).toBe(null);
  });

  test('deve buscar dados com fetchData', async () => {
    const mockData = [
      { ...initialPenhoraStatus, id: 1, nome: 'Penhora Status 1' },
      { ...initialPenhoraStatus, id: 2, nome: 'Penhora Status 2' }
    ];
    mockPenhoraStatusService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePenhoraStatusList(mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(mockPenhoraStatusService.getAll).toHaveBeenCalled();
    expect(result.current.data).toEqual(mockData);
    expect(result.current.loading).toBe(false);
  });

  test('deve lidar com erro na busca', async () => {
    const errorMessage = 'Erro ao carregar lista';
    mockPenhoraStatusService.getAll.mockRejectedValue(new Error(errorMessage));

    const { result } = renderHook(() => 
      usePenhoraStatusList(mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.fetchData();
    });

    expect(result.current.error).toBe(errorMessage);
    expect(result.current.loading).toBe(false);
  });

  test('deve buscar dados com filtro', async () => {
    const mockData = [{ ...initialPenhoraStatus, id: 1, nome: 'Penhora Status Filtrado' }];
    const filtro = { nome: 'Penhora Status' };
    mockPenhoraStatusService.getAll.mockResolvedValue(mockData);

    const { result } = renderHook(() => 
      usePenhoraStatusList(mockPenhoraStatusService)
    );

    await act(async () => {
      await result.current.fetchData(filtro);
    });

    expect(mockPenhoraStatusService.getAll).toHaveBeenCalledWith(filtro);
    expect(result.current.data).toEqual(mockData);
  });
});

describe('useValidationsPenhoraStatus', () => {
  test('deve validar dados corretos', () => {
    const { result } = renderHook(() => useValidationsPenhoraStatus());

    const validData = { ...initialPenhoraStatus, nome: 'Penhora Status Válido' };
    const validation = result.current.validate(validData);

    expect(validation.isValid).toBe(true);
    expect(validation.message).toBe('');
  });


    test('deve invalidar nome vazio', () => {
    const { result } = renderHook(() => useValidationsPenhoraStatus());

    const invalidData = { ...initialPenhoraStatus, nome: '' };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ficar vazio.');
  });

  
  test('deve invalidar nome muito longo', () => {
    const { result } = renderHook(() => useValidationsPenhoraStatus());

    const invalidData = { 
      ...initialPenhoraStatus, 
      nome: 'a'.repeat(80+1)
    };
    const validation = result.current.validate(invalidData);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('O campo Nome não pode ter mais de 80 caracteres.');
  });


  test('deve invalidar dados nulos', () => {
    const { result } = renderHook(() => useValidationsPenhoraStatus());

    const validation = result.current.validate(null as any);

    expect(validation.isValid).toBe(false);
    expect(validation.message).toBe('Dados não informados.');
  });
});


// Teste de integração para múltiplos hooks
describe('Integração de hooks', () => {
  test('deve funcionar em conjunto', async () => {
    const mockData = [{ ...initialPenhoraStatus, id: 1, nome: 'Penhora Status Teste' }];
    mockPenhoraStatusService.getAll.mockResolvedValue(mockData);
    mockPenhoraStatusService.getList.mockResolvedValue(mockData);

    // Usa múltiplos hooks
    const { result: listResult } = renderHook(() => 
      usePenhoraStatusList(mockPenhoraStatusService)
    );
    
     const { result: comboResult } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );   

    const { result: validationResult } = renderHook(() => 
      useValidationsPenhoraStatus()
    );

    // Busca dados na lista
    await act(async () => {
      await listResult.current.fetchData();
    });

     
    // Aguarda carregar opções no combo
    
      expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Penhora Status Teste' }]);
    
   

    // Valida dados
    const validation = validationResult.current.validate(mockData[0]);

    expect(listResult.current.data).toEqual(mockData);
     expect(comboResult.current.options).toEqual([{ id: 1, nome: 'Penhora Status Teste' }]);
  
    expect(validation.isValid).toBe(true);
  });
}); 
test('deve carregar opções na inicialização', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora Status 1' },
      { id: 2, nome: 'Penhora Status 2' }
    ];
    mockPenhoraStatusService.getList.mockResolvedValue(mockOptions as IPenhoraStatus[]);


    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );

    await waitFor(() => {
      // Aguarda carregar as opções antes de verificar
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora Status 1' },
        { id: 2, nome: 'Penhora Status 2' }
      ]);
    });


    expect(mockPenhoraStatusService.getList).toHaveBeenCalled();
  });

  test('deve filtrar opções', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora Status ABC' },
      { id: 2, nome: 'Penhora Status XYZ' }
    ];
    mockPenhoraStatusService.getList.mockResolvedValue(mockOptions as IPenhoraStatus[]);   


 const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );
    // Aguarda carregar as opções
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora Status ABC' },
        { id: 2, nome: 'Penhora Status XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    expect(result.current.options).toEqual([{ id: 1, nome: 'Penhora Status ABC' }]);
  });


  test('deve limpar filtro quando texto vazio', async () => {
    const mockOptions = [
      { id: 1, nome: 'Penhora Status ABC' },
      { id: 2, nome: 'Penhora Status XYZ' }
    ];
    mockPenhoraStatusService.getList.mockResolvedValue(mockOptions as IPenhoraStatus[]);
  


    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );
    await waitFor(() => {
      expect(result.current.options).toEqual([
        { id: 1, nome: 'Penhora Status ABC' },
        { id: 2, nome: 'Penhora Status XYZ' }
      ]);
    });

    // Aplica filtro
    act(() => {
      result.current.handleFilter('ABC');
    });

    // Remove filtro
    act(() => {
      result.current.handleFilter('');
    });
 

     expect(result.current.options).toEqual([
          {id: 1, nome: 'Penhora Status ABC' },
          {id: 2, nome: 'Penhora Status XYZ' }
        ]);

  });



 test('deve alterar valor selecionado', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );

    const newValue = { id: 1, nome: 'Penhora Status Selecionado' };

    act(() => {
      result.current.handleValueChange(newValue);
    });

    expect(result.current.selectedValue).toEqual(newValue);
  });

  test('deve limpar valor selecionado', () => {
    const initialValue = { id: 1, nome: 'Penhora Status Inicial' };
    
    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService, initialValue)
    );

    act(() => {
      result.current.clearValue();
    });

    expect(result.current.selectedValue).toBe(null);
  });


describe('usePenhoraStatusComboBox', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('deve inicializar com estado correto', () => {
    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService)
    );

    expect(result.current.options).toEqual([]);
    expect(result.current.loading).toBe(true);
    expect(result.current.selectedValue).toBeUndefined();
  });

 
  test('deve inicializar com valor inicial', () => {
    const initialValue = { id: 1, nome: 'Penhora Status Inicial' };
    
    const { result } = renderHook(() => 
      usePenhoraStatusComboBox(mockPenhoraStatusService, initialValue)
    );

    expect(result.current.selectedValue).toEqual(initialValue);
  });
});






